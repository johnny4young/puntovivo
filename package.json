{
  "name": "open-yojob",
  "version": "1.0.0",
  "description": "Modern POS Solutions System - Web & Desktop Application",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "clean": "turbo run clean && rm -rf node_modules",
    "dev:web": "npm run dev --workspace=@open-yojob/web",
    "dev:desktop": "npm run dev --workspace=@open-yojob/desktop",
    "dev:backend": "cd backend && go run ./cmd/server",
    "build:web": "npm run build --workspace=@open-yojob/web",
    "build:desktop": "npm run build --workspace=@open-yojob/desktop"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
