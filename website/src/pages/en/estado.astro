---
import Base from '../../layouts/Base.astro';
import Estado from '../../pages-content/Estado.astro';

const lang = 'en';
const route = '/estado';
---

<Base lang={lang} route={route}>
  <Estado lang={lang} />
</Base>
