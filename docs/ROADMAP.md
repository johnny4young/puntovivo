# Puntovivo Roadmap

> Updated: 2026-06-01
> Single source of truth for project status, priorities, and actionable work plan.
> Replaces: `IMPLEMENTATION_STATUS.md`, `OPEN_BACKLOG.md`, `MIGRATION_PLAN.md`
> Strategic reference: see [PLAN.md](./PLAN.md) for competitive analysis, academic frameworks, and detailed technical designs.
> Sellability reference: see [SELLABILITY.md](./SELLABILITY.md) for the Colombian retail go/no-go checklist.
> Architecture diagram: [architecture.svg](./architecture.svg) (source: [architecture.mmd](./architecture.mmd))
> Shipped-history archive: see [ARCHIVED.md](./ARCHIVED.md). Keep this roadmap focused on current status and acceptance criteria.

---

## 0. MVP Colombia — Definition of Done

Concrete go/no-go checklist for declaring Puntovivo **ready to be sold to a
Colombian retail business**. Derived from the April 2026 audit of features,
DIAN regulation (Resolución 000165/2023), and hardware expectations of
neighborhood retail.

| # | Capability | Status | Blocking phase |
| --- | --- | --- | --- |
| 1 | Multi-tenant POS with cash sessions, sales, returns, voids, split payments, quotations | **Shipped** | Phase 0-5 done |
| 2 | Site-owned inventory + atomic transfers with discrepancy reporting | **Shipped** | Phase 2 done |
| 3 | Audit trail on sensitive actions (void, refund, cash close, stock adjust) | **Shipped** | Tier-2 #8 done |
| 4 | **Electronic invoicing (DEE + Factura Electrónica) via DIAN-authorized PT** | **Gated** — domain/model shipped, real PT pending | Phase 11 (was deferred, now **P0**) |
| 5 | **CUFE/CUDE generation + QR on printed receipt + XML ≥5-year retention** | **Partial** — CUFE/model/XML surfaces exist; final QR/retention proof pending | Phase 11 |
| 6 | **Contingency mode** (offline → queued → sent when online) for fiscal docs | **Partial** — status/UI scaffold exists; retry daemon pending | Phase 11 |
| 7 | **Barcode scanner support** (USB HID keyboard-wedge + EAN-13 checksum) | **Not started** | Phase 12 Hardware |
| 8 | **ESC/POS thermal printer driver + cash drawer opening via RJ11** | **Not started** | Phase 12 Hardware |
| 9 | **Payment terminal integration** (manual capture exists; Bold/Wompi adapter needed) | **Partial (manual only)** | Phase 12 Hardware |
| 10 | **Park-and-resume multiple carts** (suspended sales workspace) | **Shipped** | `ENG-018`, `ENG-018b`, `ENG-018c` |

**Overall MVP Colombia retail readiness: ~76% by shipped foundation,
but not sellable yet**. DIAN PT, fiscal contingency, ESC/POS + drawer,
scanner, and payment-terminal policy remain the go/no-go blockers. See
[SELLABILITY.md](./SELLABILITY.md) for the pilot / production checklist,
and section "Market Segments Coverage" below for the three-ring definition
(retail → restaurant/pharmacy → services).

**Sellability stage verdict** — this block mirrors
[SELLABILITY.md](./SELLABILITY.md#current-verdict) and the README Current Status
table; keep all three in sync when any verdict changes:

| Stage | Verdict | Meaning |
| --- | --- | --- |
| Development demo | **Yes** | Demo tenant, sales, inventory, cash sessions, quotations, fiscal mock, AI, sync center, and receipt templates can be shown. |
| Private retail pilot | **Not yet** | Needs fiscal contingency, a hardware scanner/printer path, and clear operator recovery before a real store runs daily sales. |
| Production sale | **No** | Requires a DIAN-authorized Proveedor Tecnologico, legal XML retention, hardware validation, and payment-terminal policy. |

---

## 1. Current State — What's Done

The application is past early migration. The core POS surface is live and operational.

### Completed Phases

| Phase | Scope | Status |
| --- | --- | --- |
| Phase 0 | Foundation, schema, transport baseline | **Complete** |
| Phase 1 | Administration and master catalogs | **Complete** |
| Phase 2 | Product management and pricing | **Complete** |
| Phase 3 | Inventory | **Complete** |
| Phase 4 | Sales / POS | **Complete** |
| Phase 5 | Procurement | **Complete** |
| Phase 6 | Reporting, sync, desktop ops, UX polish | **Advanced** |

### Implemented Surface

**Backend tRPC routers**: the authoritative list lives in [packages/server/src/trpc/router.ts](../packages/server/src/trpc/router.ts). `/api/trpc` is the primary application API; `/api/health` and `/api/realtime/*` remain compatibility/realtime surfaces.

**Web route modules**: Dashboard, Company/setup, Sites, Sequentials, Locations, Customer Catalogs, Geography, Providers, Categories, Units, VAT Rates, Products, Orders, Purchases, Customers, Sales, Cash Sessions, Inventory, Transfers, Quotations, Audit Logs, Users, Operations, Co-pilot, AI configuration, fiscal documents, receipt templates, peripherals, KDS, touch, mobile waiter, and customer display shells.

**Desktop features**: embedded backend lifecycle, sandboxed renderer bridge, receipt printing, backup/restore, tray/theme/update settings, sync status/trigger APIs, and dev/runtime guards for Electron binaries and native modules.

### Unique Differentiators

- True offline-first with local SQLite
- Desktop-native Electron (no competitor offers this)
- Local-first deployment with a future hosted-SaaS substrate tracked separately
- Colombian/LatAm focus
- tRPC-first transport with type safety end-to-end

---

## 2. What to Build Next — Priority Order

This is the recommended implementation sequence. Each item links to its detailed phase below.

### Tier 1: Deployment Blockers (must ship before pilots)

| # | Item | Why | Phase |
| --- | --- | --- | --- |
| 1 | **i18n foundation** (es-CO/es/en) | English-only UI blocks LatAm deployment. Every new feature adds more hardcoded strings. | Pre-Phase 1 — **Shipped** |
| 2 | **Integer → real migration** for stock/quantity | Blocks ferreterías (2.5m cable) and supermarkets (0.75kg produce). #1 schema blocker. | Phase 1 — **Shipped** |
| 3 | **Cash management and shift control** | Every competitor has this. No cash session = no accountability = no LatAm retail adoption. | Phase 1 — **Shipped** |
| **3a** | **Colombia DIAN fiscal compliance** (DEE + Factura Electrónica via Proveedor Tecnológico) | **Legal blocker since May/July 2024.** Selling Puntovivo to a CO business without this creates fiscal exposure for the user and brand damage for us. See [FISCAL-INTEGRATION.md](./FISCAL-INTEGRATION.md) and [RECEIPT-TEMPLATES.md](./RECEIPT-TEMPLATES.md) (QR + CUFE on the print representation). | **Phase 11 → now P0** |
| **3b** | **POS hardware basics** (ESC/POS printer, cash drawer, barcode scanner) | Operational blocker — real stores need a physical receipt with cut, a drawer that opens, and scan-to-add. See [HARDWARE-POS.md](./HARDWARE-POS.md). | **Phase 12 → now P0** |
| **3c** | **POS touch UI + multi-surface routes** | Retail stores with all-in-one touch terminals and restaurants with KDS / customer display / mobile waiter need variant UI over the same bundle. See [UI-SURFACES.md](./UI-SURFACES.md) and [RECEIPT-TEMPLATES.md](./RECEIPT-TEMPLATES.md) for the print-layout editor that ships alongside. | **Phase 6c → P1** |

### Tier 2: Core Commercial Gaps (competitive table stakes)

| # | Item | Why | Phase |
| --- | --- | --- | --- |
| 4 | Site-owned inventory + transfers | Stock must belong to a site, not the tenant. Transfer workflows need in-transit state. | Phase 2 |
| 5 | Split payments / multi-tender | Basic expectation: pay partially with cash and card. **Shipped**: `sale_payments` table, split-aware `sales.create`, split-tender UI, and cash-method-only cash-session accounting; credit-mix deferred to Phase 5 with on-account sales and abonos. | Phase 5 |
| 6 | Quotations / estimates | B2B, ferreterías, service businesses all need pre-sale conversion. **MVP shipped**: `quotations` + `quotation_items` tables, full CRUD tRPC, Quotations page with status badges and draft-only delete, printable receipt + Electron bridge, history export, and the `accepted → converted` terminal close; version history, margin analysis, reminders, and auto-prefill sale flow deferred. | Phase 5 |
| 7 | Credit sales (ventas a crédito) | Deeply embedded LatAm practice — installments, abonos, contractor accounts. | Phase 5 Ext |
| 8 | Audit trail for sensitive actions | Required for operational trust — void, refund, price override, user change. **Shipped**: `audit_logs` table + transactional `writeAuditLog`; admin-only `auditLogs.list` tRPC with filters; `/audit-logs` page with badges, summary, and export. Coverage spans void, refund, cash close (over/short), stock adjust, transfers.void, and quotations; price override, `purchases.void`, and `user.disable` remain addable without migration. | Phase 8 |

### Tier 3: Market Differentiation

| # | Item | Why | Phase |
| --- | --- | --- | --- |
| 9 | Outbound logistics (pick/pack/ship) | Needed for any store with delivery. | Phase 3 |
| 10 | Promotions / loyalty / gift cards | Behind mainstream POS expectations. | Phase 7 |
| 11 | Colombia DIAN fiscal compliance | DEE + factura electrónica mandatory. Siigo/Alegra own this today. | Phase 11 |
| 12 | Country-parametrizable fiscal rules | Current Colombia-hardcoded logic blocks any non-CO deployment. | Phase 11 Ext |

### Tier 4: Vertical Expansion (after platform is solid)

| # | Item | Phase |
| --- | --- | --- |
| 13 | Multi-vertical module activation system | Phase 0 Foundation |
| 14 | Product variants, serial/lot/batch/expiry | Phase 6 |
| 15 | Restaurant module (tables, KDS, modifiers, tips) | Phase 12 |
| 16 | Pharmacy module (Rx, controlled substances, INVIMA) | Phase 13 |
| 17 | Supermarket module (scales, PLU, perishables) | Phase 14 |
| 18 | Ferretería module (unit conversion, project quoting) | Phase 15 |

### Tier 5: Platform Maturity

| # | Item | Phase |
| --- | --- | --- |
| 19 | Advanced reporting / BI (GMROI, ABC, CLV) | Phase 9 |
| 20 | Hybrid SQLite + PostgreSQL data topology | Phase 10 |
| 21 | Public API, webhooks, integration ecosystem | Phase 11 |
| 22 | Employee shifts, commissions, time tracking | Phase 8 |
| 23 | Transport execution (dispatch, tracking, POD) | Phase 4 |

### Market Segments Coverage — Three Rings

Puntovivo's market strategy is organized into three concentric rings. Each
ring activates via the **module activation system** (see [MODULE-ACTIVATION.md](./MODULE-ACTIVATION.md))
so a minimarket does not carry restaurant or pharmacy code at runtime.

**Ring 1 — Generic retail MVP (target: 8-10 weeks)**
- Neighborhood store, minimarket, papelería, ferretería, boutique with
  variants, carnicería/fruver with scale integration, panadería, heladería.
- Prerequisites: Tier-1 items 3a (DIAN fiscal) + 3b (hardware) + product
  variants + scale.
- Coverage goal: ~80% of Colombian retail POS market.

**Ring 2 — Restaurant + Pharmacy (target: +8-10 weeks after Ring 1)**
- Restaurant: composition/BOM + preparation lifecycle (KDS + tables +
  modifiers) + touch UI + one delivery integration (Rappi). See
  [RESTAURANT-LIFECYCLE.md](./RESTAURANT-LIFECYCLE.md) and
  [PRODUCT-COMPOSITION.md](./PRODUCT-COMPOSITION.md).
- Pharmacy/droguería: batches + expiration (Phase 7) + INVIMA code
  validation + prescription workflow.
- Coverage goal: ~35% additional market (gastronomy + pharma chains).

**Ring 3 — Service verticals (target: +12 weeks after Ring 2)**
- Salons, barbers, spas, vet clinics, workshops: shared appointments
  module + services-as-products + commissions + client assets.
- Optional extensions: gyms (subscriptions), laundromats (tickets),
  motels (rooms).

**Beyond Ring 3**: future verticals (CO + LatAm), LatAm country fiscal
expansion, cross-cutting platform features (BI, franchises, public API,
AI). See [FUTURE-VERTICALS.md](./FUTURE-VERTICALS.md),
[LATAM-EXPANSION.md](./LATAM-EXPANSION.md), and
[LONG-TERM-VISION.md](./LONG-TERM-VISION.md).

---

## 3. Open Technical Risks

### Platform Foundation

- **Fiscal packs are still not production-ready across countries** — Colombia, Mexico, and Chile foundations exist and `ENG-185` shipped the truth guard (mock/draft packs are labelled Demo/Draft, unsupported countries fail with a typed error). Real provider/certificate/transmission paths remain gated behind `ENG-021` (CO), `ENG-035c` (MX), and `ENG-036c` (CL) — no pack reaches `certified` maturity until those land.
- **Module activation exists, but retail defaults are still too broad** — `ENG-183` must classify modules and keep new retail tenants on the Ring-1 surface by default.
- **Credit sales are partial** — split-credit and customer-ledger flows exist, but installment schedules, abono UX maturity, and configurable credit policy still need product hardening.
- **Schema changes now flow through versioned Drizzle migrations** after `ENG-002`. `runSchemaSync()` and the raw-DDL mirror in `packages/server/src/db/index.ts` are retired; new schema work edits `schema.ts`, generates a migration, and updates the Drizzle journal. Remaining risk: non-additive migrations still need careful hand-written transition scripts and upgrade testing for existing SQLite installs. (Follow-up: `TEST-002`)

### Sync and Offline

- ADR-0001 and the sync outbox protect local authority today; the hosted SaaS data substrate is still undecided and explicitly tracked by `ENG-164`.
- Persistence is still tightly coupled to SQLite-specific Drizzle and `better-sqlite3`; repository interfaces are not broadly extracted from router procedures.
- Remote replication beyond the existing sync/outbox contracts remains a product decision, not a hidden implementation detail.
- Structured logging and tRPC tracing exist; continue extending correlation through new async workers and external adapters as those surfaces ship.

### Security

- ~~Main `BrowserWindow` still runs with `sandbox: false`~~ — **shipped as ENG-004** (see Tier-6 table below). The main window now runs under `sandbox: true` with `contextIsolation: true` and `nodeIntegration: false`; the invariant is pinned by `apps/desktop/src/main/__tests__/window-config.test.ts` and enforced in `ci:desktop`.
- ~~`writeAuditLog` wired into only three operations~~ — **fully shipped as ENG-007** (2026-05-19). `writeAuditLog` fires transactionally from `transfers.void`, `quotations.delete`, `quotations.updateStatus` (convert only), `sales.void`, `sales.returnSale`, `sales.price_override`, `sales.complete`, `cashSessions.close`, `inventory.adjustStock`, `purchases.void`, `users.create`, `users.update` (role/isActive changes), `customers.update` (creditLimit changes only), and `completeSale` (when admin credit-override actually rescues an exceeded cupo). The original `company_credit_settings` gap was closed by auditing the two real mutation surfaces that ENG-090 chose instead of creating that table — see the §3b ENG-007 Shipped summary for the design rationale.
- ~~Rate limiting only global~~ — **shipped as ENG-008 + ENG-008b**. `auth.login` has dedicated rate limits (10/IP/60s, 5-failure/username/15min) with `AUTH_RATE_LIMIT_EXCEEDED` error codes and 14 tests from ENG-008; ENG-008b promoted the counters to a DB-backed `login_attempts` table with the in-memory Maps demoted to a write-through cache so a server restart no longer wipes the buckets mid-attack.

### Testing

- `apps/desktop` now has a main-process unit suite behind `pnpm run ci:desktop`, but packaging/signing and cross-OS installer behavior still require manual workflow validation before release.
- A cross-surface E2E suite against the renderer + embedded backend + Electron bridge is partially shipped. The web Playwright suite (`pnpm run test:e2e:web`) covers the core business flows locally; the local-only Electron smoke (`pnpm run test:e2e:electron`) covers embedded-server boot, login, dashboard, and the zero-client-issue invariant. Remaining gap: Electron-in-CI. (Ticket `ENG-001`)
- Coverage thresholds are enforced in `ci:web` and `ci:server` via `vitest --coverage --run`. Web thresholds are now `70/70/70/70` after ENG-003b shipped the 13 new + 6 extended test files closing every axis. (Tickets `ENG-003`, `ENG-003b`)

### Build / CI

- Desktop packaging for `.dmg` (macOS) and `.exe` (Windows) is still exercised exclusively via the manual `release.yml` workflow. `ENG-005` now covers `ci:desktop` on Linux/macOS/Windows, but packaging/signing regressions can still hide until release time. (Ticket `ENG-005`)
- Production dependency audit is wired through `pnpm run ci:audit`; dev-only advisories are tracked separately when upstream build tooling lags.

### Frontend Health

- Several feature components exceed 900 lines (`ProductFormModal.tsx`, `InventoryPage.tsx`, `SalesPage.tsx`) and mix data-fetch, state, and presentation. Candidates for extraction of subcomponents + custom hooks. (Ticket `ENG-011`)
- ~~`zustand` is declared in `apps/web/package.json` but has **zero imports** in `apps/web/src/`~~ — **adopted via ENG-018b's `useCartWorkspaceStore`**. Zustand is now a live, tested dependency backing the multi-cart sales workspace (ENG-012 is Shipped). Migrating the remaining Context providers (`AuthProvider`, `TenantProvider`) stays available as a future refactor if ever desired.

### Performance and UX

- Responsive/mobile refinement is weaker in admin/maintenance screens.
- Not every screen uses the same feedback quality level yet.

---

## 3b. Engineering Quality Backlog — Cross-Cutting

These tickets don't belong to any single vertical or commercial phase. They protect the app's long-term maintainability and scalability, and should be sequenced against Tier 1–3 work rather than deferred to "someday". Each ticket is actionable in isolation and has a clear acceptance test.

**Status values** (machine-readable pool discovery):

- `Pending` — never started; eligible for next sprint.
- `Partial` — some sub-steps shipped, remainder listed at the end of the Scope cell under "Remaining:". Still eligible; the agent executes the remaining items as the ticket scope.
- `Shipped` — closed; Scope cell ends with a "Shipped: …" summary. Excluded from pool.
- `Gated` — external dependency (hardware, contract, credentials) blocks start. The gate is documented inside Scope. Excluded from pool until gate clears.
- `Deferred` — explicitly postponed by the operator, not a blocker but not in the active sprint plan. Excluded from pool.

Agents picking the next ticket read this column first, then follow the sequencing recommendation at the end of the section. New tickets are created with `Status: Pending`.

Historical `Shipped` rows preserve the closeout prose from the commit that
landed them. Current operational commands, runtime versions, and onboarding
rules live in `README.md`, `AGENTS.md`, and the runbooks linked from
`docs/README.md`; compact verbose shipped rows into `ARCHIVED.md` instead of
expanding this ledger further.

| ID | Title | Status | Scope | Acceptance | Priority |
| --- | --- | --- | --- | --- | --- |
| `ENG-001` | E2E test harness | Partial | Add Playwright against the web app + embedded backend in headless Electron. Cover: login → open cash session → create sale (split tender) → refund → close session. **Step 1 shipped**: `npm run test:e2e:web` runs a Playwright web suite against the standalone backend and Vite app. Global-setup seeds an idempotent baseline (admin/manager/cashier/viewer template users plus a secondary active site) and prunes prior-run artefacts. **Step 2 shipped**: 16 business-flow tests cover sales (complete / refund / void + split-payment details), purchases (complete / return / void), stock adjustments, inter-site transfers (discrepancy + perfect-receipt + over-receipt-blocked), cash-session opening from zero, manual paid-in movements, and cash-session closures (overage / shortage / balanced). Together with the smoke and quotations slices the suite now runs 25 tests in parallel. Every test seeds unique actors and products (random UUID suffix on email, sku, register name) so the suite is fully parallelisable. Every test attaches a client issue tracker that fails the run on unexpected console errors / page errors / 4xx-5xx responses. A `data-row-id` attribute on DataTable rows lets tests pick specific rows deterministically under parallelism. See `e2e/README.md` for the operator guide. **Step 3 shipped**: baseline helpers extracted into `e2e/shared/baseline.ts` so both runners share the same user + site seeding; new `e2e/electron/` suite launches Electron via `_electron.launch()` against a pre-seeded tmpdir DB under `test-results/electron-userdata/` with a smoke test covering login → dashboard and zero-console-error invariant; new `playwright.electron.config.ts`; new root `test:e2e:electron` + `test:e2e` scripts; new `scripts/ensure-electron-main-build.mjs` guard fails fast with an actionable message when the Vite main bundle is absent. New `e2e-web` job in `ci.yml` runs the web suite on every PR with a cached Playwright browser binary keyed on the `package-lock.json` hash and uploads `playwright-report/web` always plus `test-results/playwright-web` on failure (traces, screenshots, videos; retention 7 days). **Remaining**: Electron-in-CI — requires xvfb on ubuntu + a packaged-binary build step that `ci:desktop` does not produce today. Captured as a follow-up; the Electron suite stays local-only until the first externally-signed installer approaches. | `npm run test:e2e` green in CI on Linux; screenshots on failure uploaded as CI artifact. | **High** |
| `ENG-002` | Versioned Drizzle migrations | Shipped | Generate baseline migration from current schema and replace `runSchemaSync()` raw DDL bootstrap with `migrate()` on startup. **Shipped**: `drizzleMigrate()` is the single schema path (baseline `0000` + journal seeding for adopted DBs, packaged-Electron migrations via `extraResource`); `runSchemaSync()` and its helpers removed, `db/index.ts` shrank 1,638→444 lines, catalog seeding hoisted to a gated `seedCatalogs()`, parity test pins the invariant. | Adding a column requires only editing `schema.ts` + `drizzle-kit generate`; re-running against an existing DB is a no-op. | **High** |
| `ENG-003` | CI coverage threshold | Shipped | Run `vitest --coverage --run` in `ci:web` and `ci:server`, fail below the thresholds in `vitest.config.ts`, upload LCOV. **Shipped**: both workspaces declare enforceable v8 thresholds and gate on them via a `test:coverage` variant; `lcov` reporter wired and uploaded as a CI artifact. Follow-up `ENG-003b` raised the web floor back to 70%. | PR lowering coverage below threshold fails CI. | **High** |
| `ENG-003b` | Raise web coverage back to 70% | Shipped | Write new component + route tests to bring every axis back to 70% and raise the thresholds in `apps/web/vitest.config.ts`; test-writing only. **Shipped**: 13 new + 6 extended test files lifted final coverage to 78.64 / 79.31 / 80.16 / 70.20, all four above the new 70/70/70/70 floor (branches was the toughest gap). | All four axes ≥ 70% in `apps/web/vitest.config.ts`. | **Medium** |
| `ENG-004` | Electron main window sandbox | Shipped | Audit every `ipcMain` handler and preload export, remove Node-only APIs from the renderer path, flip `sandbox: true`. **Shipped**: security-critical webPreferences centralized in `window-config.ts` (`MAIN_WINDOW_WEB_PREFERENCES`); bridge audit confirmed all preload exports are pure `contextBridge → ipcRenderer.invoke` wrappers; a `ci:desktop` node-test pins `sandbox`/`contextIsolation`/`nodeIntegration`. | App boots with `sandbox: true`; all renderer→main calls still pass; add regression test that asserts `webPreferences.sandbox === true`. | **High** |
| `ENG-005` | Desktop CI build matrix | Shipped | Extend `ci.yml` to run `ci:desktop` on ubuntu/macos/windows; packaging may stay in `release.yml`. **Shipped**: the desktop job uses `strategy.matrix.os` across all three runners with `fail-fast: false`; a cross-platform audit of the invoked scripts found zero POSIX-only hazards and the native deps ship prebuilt binaries for every target. (Later moved to ubuntu-only with manual cross-OS validation — see ENG-046.) | CI badge reflects all three OS; a PR that breaks a Windows-only path fails CI. | **Medium** |
| `ENG-006` | Structured logging | Shipped | Replace `console.*` in `packages/server` and `apps/desktop/src/main` with a per-module `pino` logger; redact PII. **Shipped**: `logging/logger.ts` exports `rootLogger` + `createModuleLogger(name)` with a redact policy (`[Redacted]`) covering passwords/tokens/email/auth/cookie + wildcards; 41 console sites migrated, Fastify shares the stream, a `no-console` ESLint rule enforces it, and 14 tests lock the redact contract. | Logs are single-line JSON in production; `logger.child({ module: 'sync' })` works; no raw `console.*` remains outside tests. | **Medium** |
| `ENG-007` | Audit trail expansion | Shipped | Call `writeAuditLog` from void / refund / purchases.void / users mutations / price overrides / credit-policy changes, recording `before`/`after` JSON. **Shipped**: all named surfaces emit audit rows inside their own tx; the credit-policy gap closed via `customer.credit_limit.update` and `sale.credit_override` actions (cupo lives on `customers.creditLimit`, no separate settings table), plus the missing KDS audit translations backfilled. | Unit test per site asserts an `audit_logs` row is written with the correct `action` + `resource_type`. | **High** |
| `ENG-008` | Auth hardening | Shipped | Rate-limit `auth.login` per IP and per username with lockout escalation; document in `docs/SECURITY.md`. **Shipped**: `security/loginRateLimit.ts` enforces 10/IP/60s and 5 failures/username/15min, throwing `AUTH_RATE_LIMIT_EXCEEDED`; success clears the username bucket, the IP bucket decays via TTL, and 14 tests plus the SECURITY.md policy back it. | Load test: 50 bad logins from one IP returns `429` before the 60-second window completes. | **Medium** |
| `ENG-008b` | Persistent login rate-limit state | Shipped | Promote `loginRateLimit` to a `login_attempts` Drizzle table so the limit survives restarts, keeping the in-memory path as a cache. **Shipped**: migration `0006_login_attempts.sql` keyed on `(kind, key)` (not tenant-scoped so one IP can't evade the global cap); the service is now write-through over the DB with a table-existence fallback, and a restart-survival test asserts the cap still trips after `closeDatabase()`/`initDatabase()`. | Attacker tripping the username cap, server restart, next attempt still 429. | **Medium** |
| `ENG-009` | Dependency audit gate | Shipped | Add `npm audit --production --audit-level=high` to all `ci:*` scripts plus a Dependabot config. **Shipped**: a root `ci:audit` script runs first in every per-workspace CI script so a new HIGH/CRITICAL prod CVE fails CI; three pre-existing vulns cleared, and `.github/dependabot.yml` rewritten for grouped monthly npm + weekly actions PRs (Electron excluded for manual smoke). | A new transitive dependency with a known `high` CVE fails CI. | **Medium** |
| `ENG-010` | Repository interfaces (Phase 10 prep) | Pending | Extract persistence from routers into per-domain repository interfaces (`SalesRepository`, `InventoryRepository`, …) implemented today by a Drizzle-SQLite adapter. Keeps routers dialect-neutral. | All router procedures touch the DB exclusively through an interface; no router imports `better-sqlite3`. | **Medium** |
| `ENG-011` | Break up oversized components | Pending | Split `ProductFormModal.tsx` (960 l), `InventoryPage.tsx` (935 l), `SalesPage.tsx` (581 l), `QuotationCreateModal.tsx` (567 l), and `sales.ts`/`purchases.ts` server routers (1.4–1.6 k l) into focused subcomponents + custom hooks + per-sub-feature service files. No file over ~400 lines without justification. | Touched files drop below 400 l; behavior parity covered by existing + new unit tests. | **Medium** |
| `ENG-012` | Remove or adopt `zustand` | Shipped | Zustand is declared but had zero imports — either delete it or migrate the Context providers. **Shipped (no-op, closed during ENG-016 docs audit)**: the premise is stale — ENG-018b adopted Zustand in `useCartWorkspaceStore.ts`, consumed by `SalesPage`, `SuspendedSalesPanel`, and `AuthProvider`, so the adopt branch is satisfied. | Either `zustand` is gone from `package.json`, or both providers are refactored to a Zustand store with tests. | **Low** |
| `ENG-013` | Consolidate `CLAUDE.md` / `AGENTS.md` | Shipped | Make one file canonical and the other a pointer to remove drift. **Shipped (no-op, closed during ENG-016 docs audit)**: already in place — `CLAUDE.md` is a filesystem symlink to `AGENTS.md`, enforcing single-source-of-truth at the filesystem level (a divergence would surface as a file-mode change in `git diff`). | A single source of truth remains for operational guidance; CI fails if the two files diverge. | **Low** |
| `ENG-014` | Split payments — credit mix | Shipped | Extend split tender to mix on-account installments with immediate tender (layaway / abono a crédito). **Shipped**: `splitPaymentMethodEnum` gained `'credit'` (requiring `customerId`); `completeSale` sums the credit slice into `creditSaleAmount`, enforces the cupo per-portion, and writes only that slice to the customer ledger while other tenders settle in cash; split-with-credit lands as `'partial'`, refunds of credit-tender sales are blocked, and the modal exposes a gated credit row. | `sales.create` accepts `credit` + `cash` in the same payload; the sale becomes partial-credit and the `credit_sales` ledger is created only for the credit portion. | **Medium** |
| `ENG-015` | Dev seed command | Shipped | Single command loading a realistic multi-site/user/product dataset into a fresh dev install; dev-only, refuses to run in production. Spec: [DEV-SEED.md](./DEV-SEED.md). **Shipped**: `seedDevData()` builds the isolated `demo-co` tenant (6 users, 2 sites, 50 products, 30 customers, ~20 sales/cashier) via a guarded CLI with `SEED_PRESET`/`SEED_RESET`/`SEED_TARGET`; tests pin row counts, the stock invariant, login roundtrip, and idempotency. | `npm run seed:dev` populates 1 tenant, 2 sites, 6 users, 50 products, 30 customers, 5 providers, ~40 historical sales, 6 purchases, 5 quotations, and 1 receipt template per kind. Idempotent without `--reset`. | **Medium** |
| `ENG-016` | Receipt templates editor UX pass | Partial | Follow-up improvements tracked in [RECEIPT-TEMPLATES.md](./RECEIPT-TEMPLATES.md) §Follow-up improvements after the Iter 2 release: drag-and-drop reorder with animation, `{{autocomplete}}` + syntax highlighting + auto-close pairs in `text.value`, template functions (`limit`, `max`, `currency`, `date`, …), bindings captions for `itemsTable` and `totalsBlock`, a Puntovivo-branded footer atomic block, explicit variable-resolution error markers in the preview, and block-move animation. Pulls design cues from JasperReports / Crystal / Siigo / Metabase. **Shipped (pass 1)**: items 4, 5, 6 landed together. Item #4 — bindings captions above `itemsTable` (permanent) and `totalsBlock` (collapsible explainer) keyed off new i18n strings under `receiptTemplates.editor.blockFields`. Item #5 — new `appFooter` atomic block type in the Zod discriminated union + renderer case (`services/receipt-renderer.ts APP_FOOTER_METADATA`) that emits `Puntovivo <version>`, URL, and support contact in HTML + ESC/POS; soft-hidden by `show: false`; included in every default preset (editor starter layouts + dev seed) with a toggle in the editor form. Item #6 — small framework-agnostic FLIP helper at `apps/web/src/lib/flipAnimate.ts` wired into `ReceiptTemplateEditor.moveBlock` so `↑`/`↓` presses animate each card to its new position over 180ms, short-circuits instantly under `prefers-reduced-motion: reduce`. 3 new server tests + 9 FLIP-helper tests + 2 defaultLayouts assertions + 5 editor component tests; en + es i18n updated in neutral LATAM Spanish. **Shipped (pass 2)**: item #1 — drag-and-drop reorder via `@dnd-kit/sortable`. Adopted `@dnd-kit/core` + `@dnd-kit/sortable` (~8kB gzipped) and wrapped the block list with `<DndContext>` + `<SortableContext>` (vertical strategy + `PointerSensor` with 4px activation + `KeyboardSensor` with `sortableKeyboardCoordinates`). New internal `SortableBlockRow` attaches the drag listeners to a dedicated `GripVertical` icon so the row title stays clickable and the `↑/↓` buttons + trash button keep working as the a11y fallback. `<DragOverlay>` portal renders the dragged-card clone for visual continuity. `onDragEnd` routes through a new `moveBlockTo(fromIndex, toIndex)` helper that captures a FLIP snapshot before the state mutation so pass-1's `flipAnimate` plays the post-drop landing transition. New i18n keys under `editor.dragAndDrop.*` (en + es neutral LATAM). Three new component tests pin grip aria-label presence, `data-flip-key` survival across the wrapper, and `↑/↓` button coexistence. **Shipped (pass 3)**: item #3 — template functions. New `services/template-expression.ts` ships a recursive-descent parser, AST evaluator, and 12-function whitelist (`currency`, `date`, `upper`, `lower`, `round`, `limit`, `concat`, `default`, `abs`, `max`, `min`, `sum`). `currency()` reuses the renderer's `formatReceiptAmount` so it inherits ENG-017's tenant-locale formatting; `date()` defaults to the tenant's `dateFormatShort` (now exposed on `ReceiptRenderLocale`) with a `yyyy MM dd HH mm ss` mini-DSL. Zod refinement walks the AST, rejecting unknown functions, wrong arity, unknown namespaces, and `concat("javascript:", …)` scheme bypasses on `qr.source`. Defense-in-depth: `lookupPath` swapped from `in` to `Object.prototype.hasOwnProperty.call` so prototype-chain segments cannot leak; the evaluator re-checks arity before dispatch; `currency`/`round` clamp decimals to 20 to keep `Math.pow(10, n)` finite. HTML escape boundary preserved (escape runs after concat). Editor: a collapsible `<details>` "Available functions" cheat-sheet now sits below the `text.value` textarea, listing each function's signature + translatable description + canonical example. 12 new server-rendering tests (HTML, ESC/POS, escape boundary, Zod rejections, default-preset regression) plus 46 expression-engine tests (tokenizer, parser, evaluator per function, validator per failure mode, prototype-chain / arity / decimals-clamp regressions) plus 2 new editor component tests. Conditional family (`if/eq/gt`) deliberately deferred per the spec's "(later)" tag. **Shipped (pass 4)**: item #2 closed and item #7 parser-side error markers shipped via CodeMirror 6 adoption. New `apps/web/src/features/receipt-templates/{templateLanguage.ts, templateAutocomplete.ts, templateLinter.ts, TextBlockEditor.tsx}` + tests + 9 new deps (codemirror, @codemirror/{state,view,language,autocomplete,lint,commands}, @lezer/highlight, @uiw/react-codemirror, lazy-loaded into the admin-only `/receipt-templates` route; verified build emits ReceiptTemplatesPage at 493.00 kB / 158.00 kB gzip and main index at 681.98 kB / 210.76 kB gzip). The new `<TextBlockEditor>` replaces the inline `<textarea>` for `text.value` and ships: (a) custom `{{` ↔ `}}` auto-close via an EditorView.inputHandler watching for `{` after `{`; (b) syntax highlighting via a StreamLanguage tokenizer + standard `@lezer/highlight` tags (`bracket`, `variableName`, `function(variableName)`, `string`, `number`, `punctuation`, `invalid`); (c) keyboard-navigable autocomplete dropdown sourced from `templateAutocomplete.planSuggestions()` (5 namespaces + 12 functions + per-namespace property catalog mirroring the documented whitelist); (d) inline error markers via `@codemirror/lint` integration — unknown namespaces, unknown functions, wrong arity, and unparseable expressions surface as red squiggly underlines with translatable hover tooltips. The web-side parser in `templateLinter.ts` is a deliberate clone of the server parser (server import would pull in fastify/drizzle into the web bundle); two parity tests pin drift — function names on both web + server, plus a length-cap mirror at `MAX_EXPRESSION_LENGTH=200`. 60 new tests across the four files plus a 30-test TextBlockEditor harness driving real CodeMirror transactions. **Shipped (pass 5)**: item #7's runtime hints closed via `receiptTemplates.variableAvailability` admin tRPC procedure (reads the active tenant's `companies` row + `tenants.settings.fiscal_dian_enabled`, returns a `Record<namespace, Record<field, boolean>>` map; admin-only; sale/item/tender keys pin to true, fiscal keys reflect the tenant flag, company keys reflect actual column population) + a new CodeMirror 6 Decoration extension (`apps/web/src/features/receipt-templates/templateUnavailableDecorations.ts`) that walks each `{{ namespace.field }}` token in the buffer and dims paths whose availability resolves false — `opacity: 0.55; font-style: italic; underline dotted` style + a `hoverTooltip` rendering the translatable `editor.codeEditor.unavailableVariable` copy with the offending path interpolated. The extension layers under the linter so a single token can carry both severities. `useVariableAvailability` hook caches the response with a 60s staleTime; `TextBlockEditor` accepts an `unavailableVariables` prop plus a `useEffect` that dispatches a CM6 `StateEffect` to update the StateField without remounting on locale or settings changes. 6 server tests + 17 web decoration tests + 3 TextBlockEditor tests; locale-parity green; coverage floor preserved. **Remaining**: item 8 — parked low-priority ideas (WYSIWYG drag, per-site templates, JSON import/export). Item #7 fully shipped. | All eight items in RECEIPT-TEMPLATES.md §Follow-up improvements land with the matching tests and i18n. | **Medium** |
| `ENG-017` | Country / locale / currency configuration | Shipped | Replace the hardcoded USD default with global country/currency catalogs + per-tenant locale settings that every formatter resolves through one helper, plus an admin Company section. Spec: [LOCALE-CURRENCY.md](./LOCALE-CURRENCY.md). **Shipped**: migration 0003 adds the 3 catalog tables; `services/tenant-locale.ts` resolves locale (override-shadow + US fallback); `tenantLocale` router + a `LocaleProvider` singleton feed ~140 `formatCurrency` call sites without per-site edits; `CompanyLocaleSettingsCard` ships the picker + live preview. | A tenant with `countryCode='CO'` renders all currency displays as COP with 0 display decimals and `dd/MM/yyyy` dates; `countryCode='US'` stays as USD / MM/dd/yyyy; switching the setting invalidates the resolver cache and propagates to every page without a reload. | **Medium** |
| `ENG-018` | Sales park-and-resume (Iter 6) | Shipped | Build `sales.suspend/resume/listDrafts/discardDraft` with per-cashier locks, a Zustand multi-cart workspace, `SuspendedSalesPanel`, suspend button, `Ctrl+P`/`Ctrl+R` shortcuts, and a close-session draft warning. Spec: [SPRINT-PLAN.md §3](./SPRINT-PLAN.md). **Shipped**: server procedures + audit labels + the ENG-018b/018c UI and stock-reversal slices, with an E2E round-trip covering cart A → suspend → cart B → charge → resume A → charge. | Cashier creates cart A, suspends, creates cart B, charges B, resumes A, charges A. Both complete. Stock decrements correctly per site. Two cashiers cannot resume the same draft concurrently. | **High** |
| `ENG-019` | Sales receipt reprint (Iter 7) | Shipped | Add `sales.getForReprint` with reprint-count columns, `sale.reprint` audit rows, role-aware permissions, and a `SaleDetailsModal` reprint action with reason picker. **Shipped**: server + UI landed (reason picker, history banner, en/es i18n, audit summary), reusing the existing receipt renderer; the optional row-level `Ctrl+Shift+P` action landed with ENG-018b. | Admin completes a sale, navigates to history, reprints; DB row shows `reprintCount = 1` and an audit row with `action = 'sale.reprint'` exists. Cashier cannot reprint another cashier's past-shift sale. | **High** |
| `ENG-018b` | Sales park-and-resume UI (follow-up to ENG-018) | Shipped | Deliver the operator UX for ENG-018: Zustand `useCartWorkspaceStore`, `SuspendedSalesPanel`, suspend button, `Ctrl+P`/`Ctrl+R`/`Ctrl+Shift+P` shortcuts, close-session prompt, and E2E. **Shipped**: persisted owner-scoped store, panel with resume/discard, suspend/resume orchestration, resumed-cart banner routing Charge through `completeDraft`, the drafts warning, history row-selection + reprint shortcut, and the `business.spec.ts` round-trip. | Cashier creates cart A, suspends, creates cart B, charges B, resumes A, charges A. Two cashiers cannot resume concurrently (one gets FORBIDDEN). Close-session dialog warns when ≥1 draft outstanding. Ctrl+P / Ctrl+R / Ctrl+Shift+P fire outside editable inputs and are suppressed inside them. E2E extension green on all three runners. | **High** |
| `ENG-018c` | Sales draft completion procedure + discardDraft stock reversal | Shipped | Add `sales.completeDraft` (flips a draft to completed, posts real payments + cash movement, re-binds `cashSessionId`, audits) and fix `discardDraft` to mirror `sales.void` stock reversal. **Shipped**: items stay locked at draft-create so stock is never double-debited; `discardDraft` restores stock + inventory balances + a `return` movement, and the ownership gate accepts `createdBy` for orphan drafts; tests extended to 20 cases. | After ENG-018c ships, a cashier can create a draft via `sales.create({ status: 'draft' })`, optionally suspend + resume, and finalize with `sales.completeDraft` — the draft's sale row becomes `status='completed'` with payments and cash movement attached and zero extra stock movement. Discarding a draft with N items restores N units to inventory, matching the void semantics. No existing tests regress. | **High** |
| `ENG-020` | Fiscal DIAN data model + MockAdapter (Iter 3 Fase A) | Shipped | Build the fiscal DIAN domain without a PT contract: DIAN ID catalog + 4 fiscal tables with immutable buyer/line snapshots, a `FiscalAdapter` interface + deterministic `MockAdapter`, sale-lifecycle hooks, and an architectural lint keeping fiscal reports immutable. Depends on ENG-017. Spec: [FISCAL-INTEGRATION.md](./FISCAL-INTEGRATION.md). **Shipped**: migrations 0004/0005, SHA-384 CUFE per Resolución 165/2023, idempotent `orchestrator.emitFiscalDocument` behind a default-off flag, admin-only fiscal reports + UI placeholders, dev-seed parity, and 33 tests; real-PT swap is the one-file change gated as ENG-021. | Selling a sale emits a `fiscal_document` with a frozen buyer snapshot; editing the customer row afterwards does not change the emitted document. Consumidor final sales (no `customerId`) use NIT 222222222222 without creating a fake customer. Report queries parse-lint clean (no joins with customers/products). | **High** |
| `ENG-021` | Fiscal DIAN PT integration (Iter 3 Fase B) | Gated | 🟡 **Gated**: requires signed contract with a Proveedor Tecnológico (Facture / HKA / Gosocket), sandbox + production credentials, the tenant pilot's DIAN digital certificate, DIAN numbering resolution associated with the software, a validated PT POC (DEE + FEV + NC + ND + fetchStatus out-of-repo), and an agreed PT error-code → `ServerErrorWithCode` map. Swap the `MockAdapter` from ENG-020 for `FactureAdapter` (or `HkaAdapter`), add real CUFE SHA-384 signing with the tenant certificate, XAdES-EPES, contingency daemon with retry backoff, and the habilitation wizard wired to the real adapter. No domain changes beyond the adapter file — the seams from ENG-020 hold. | Production habilitation completes the DIAN test set; a simulated PT 503 writes to the contingency queue and resends on recovery; adapter-swap test proves Facture ↔ HKA parity on canonical vectors. | **High (gated)** |
| `ENG-022` | Hardware POS base (Iter 4) | Gated | 🟡 **Gated**: requires physical test lab hardware (Xprinter / Epson / Bixolon 58mm or 80mm thermal printer, Honeywell / Zebra USB HID scanner, RJ11 cash drawer chained to the printer). Add `site_peripherals` table, `PrinterAdapter` interface with `system` (current `webContents.print()`) and `escpos` (new — node-thermal-printer or escpos) drivers, cash drawer kick (`ESC p m t1 t2`) before paper cut, `useBarcodeScanner` hook capturing HID keyboard-wedge bursts with EAN-13 checksum and prefix 20-29 price-embedded parsing, and a peripherals configuration page under Setup. Full spec in [HARDWARE-POS.md](./HARDWARE-POS.md). | Sale print emits valid ESC/POS bytes, drawer opens before the cut, scanner captures EAN-13 and adds the product to the cart within 100ms of Enter. | **High (gated)** |
| `ENG-023` | Bold payment terminal (Iter 5) | Gated | 🟡 **Gated**: requires Bold sandbox credentials + physical Bluetooth terminal for tests, AND depends on **ENG-022 commit 1** which installs the `PaymentTerminalAdapter` interface. Implement `BoldAdapter` with `charge(amount, reference)`, `void(txnId)`, `printSlip(txnId)` via the Bold Bluetooth SDK. Integrate into `SalePaymentModal` — when method `card` is selected and a Bold adapter is configured for the site, invoke the adapter and persist the returned `authCode` in `sale_payments.reference`. Keep `ManualAdapter` as the fallback. | Happy path: charge → approved → `authCode` persisted. Decline → sale remains unpaid with a clear translated error. Timeout (60s no response) falls back to manual entry. Cancel mid-transaction cleans up. | **High (gated)** |
| `ENG-024` | Inter-site transfer requests with reservation (Iter 8) | Deferred | ⚪ **Deferred proposal** raised by the operator as a concern, not a firm requirement. Extend `transferOrderStatusEnum` with `requested` / `approved` / `rejected`, add `initiatedBy` / `requestedBy` / `approvedBy` / `rejectedBy` / `rejectionReason` / `expectedArrivalAt`, and finally wire `inventory_balances.reserved` (currently always 0). State machine: `requested → approved → in_transit → completed`; `rejected` or `void` paths release the reservation. UI: `TransferRequestsInboxPage` for approvers, `TransferRequestCreatePage` for requesters, extended `TransferDetailsPage` with a lifecycle timeline, sidebar badge for pending requests in the active site. Full spec in [SPRINT-PLAN.md §9](./SPRINT-PLAN.md). | Cashier at Sede A creates a request for a SKU with no stock at A; manager at Sede B approves (stock shows as reserved at B); manager at B dispatches (B's `on_hand` debits, `reserved` decrements); cashier at A receives with a discrepancy line; both sites reconcile. | **Medium (deferred)** |
| `ENG-025` | Critical security closure (multi-tenant + rate-limit + XSS + logout) | Shipped | Close 1 HIGH + 3 MED 2026-Q2 audit findings: (1) renderer-supplied `tenantId` in the Electron IPC bridge, (2) `/api/trpc/*` not rate-limited, (3) unescaped/any-scheme `logoDataUrl`/`imageUrl`, (4) `auth.logout` not bumping `sessionVersion`. **Shipped**: server-validated `desktopSession` singleton derives `tenantId`; onRoute rate-limit hook; URL scheme blocklist + `escapeHtml`; logout promoted to `protectedProcedure` + version bump. All four verified by live smoke + regression tests. | All four vectors closed, regression tests added (`desktopSession.test.ts`, `ipc-handlers.test.ts`, `rateLimit.test.ts`, extended `auth.test.ts` + `receipt-renderer.test.ts`), `npm audit --omit=dev` stays at 0 high. | **Critical** |
| `ENG-026` | Vite 8 + plugin-react 6 + types/node 24 | Shipped | Coordinated major bump (Vite 7→8 + plugin-react 4→6 + @types/node 22→24), pinned via lockfile with the full `ci:web`/`ci:desktop` matrix. **Shipped**: ranges bumped across all workspaces, no `overrides` needed, React Compiler left opt-out, bundle delta <1%, Vite 8/Rolldown build ~470ms; two collateral fixes (tsconfig `lib` ES2020→ES2022, a dead initializer) kept the gates green. | All `ci:web` and `ci:desktop` gates green; no runtime regressions in receipt-templates editor and lazy chunks. | **Low** |
| `ENG-027` | Dead code + dependency hygiene | Shipped | Delete ~12 confirmed-unused named exports flagged by the 2026-Q2 audit and drop the unused `@tanstack/react-virtual` dependency, plus redundant default exports. **Shipped**: stale server/web exports + 5 test mock factories removed, the dependency dropped from both apps, every removal verified by 0-import grep, and gates green across all three workspaces. | Each removal verified by 0-import grep across `apps/web/src` + `packages/server/src` + `apps/desktop/src/main`; `ci:web` + `ci:server` + `ci:desktop` green. | **Low** |
| `ENG-028` | Cross-cutting helpers (mutate + sumBy + errorToast) | Shipped | Three helpers collapsing 2026-Q2-audit boilerplate: `invalidateGroups`, `sumBy`, and a non-hook `onErrorToast` builder (chosen over a wrapper hook to stay a one-line drop-in). **Shipped**: helpers + 20 unit tests, plus a debt-burn migration of 11 invalidate blocks, 3 reducers, and ~30 onError callsites across ~32 feature files (net −325 LOC); error handlers gain locale-aware translation, gates stay green. | Each helper used in at least 3 call sites with the old pattern removed; coverage stays ≥70/70/70/70. | **Medium** |
| `ENG-029` | Hotspot file split (schema / sales router / desktop main) | Pending | Defensive splits triggered when those files are next touched by another ticket: `db/schema.ts` (3005 LOC) by domain, `trpc/routers/sales.ts` (2591 LOC) extracts `create` / `returnSale` / `void` / `discardDraft` to subhelpers, `apps/desktop/src/main/index.ts` (1728 LOC) moves IPC handlers to `main/ipc/{db,sync,print,auth}.ts`, `purchases.ts` (1648 LOC) follows the sales pattern, `ReceiptTemplateEditor.tsx` (1179 LOC) extracts `BlockForm` to its own file, `SalesPage.tsx` (1117 LOC) hoists the 31 hooks into `useSalesMutations()`. Trigger when each file enters another ticket's diff; do not refactor speculatively. | Each split keeps tests green and produces files under 500 LOC; touched ENG ticket signs off the split. | **Low** |
| `ENG-030` | AI-FOUNDATION (Vercel AI SDK + audit + settings) | Shipped | Wire Vercel AI SDK v6 with a provider Strategy/Factory (`anthropic` default, `openai`/`ollama` stubs), prompt-caching, an `ai_audit_log` table, per-tenant settings + budget, and an admin `CompanyAISettingsCard`. **Shipped**: full pipeline + 5 test files (provider strategy, cost math, budget enforcement, per-call audit, failure-path, isolation, role guards); key from `ANTHROPIC_API_KEY`; live smoke + gates green; two MEDIUM follow-ups captured to BACKLOG. | Audit log writes per call, budget cutoff enforced, multi-tenant isolation tested, role guards admin-only, `ai.enabled=false` short-circuits all features without errors. | **Medium** |
| `ENG-031` | AI conversational analytics co-pilot | Shipped | Server-side tool-calling `ai.copilot.chat` (manager/admin) reusing ENG-030 settings/budget/registry, with a guarded `runReadOnlySQL` over a tenant-scoped `:memory:` analytics snapshot, plus a `/co-pilot` route showing answer + SQL + table + bar chart. **Shipped**: SQL guard rejects mutation/PRAGMA/multi-statement/non-snapshot tables; server + web tests cover role gating, AI_DISABLED, budget, guard rejection, CTE, isolation, and rendering. | Demo conversation "¿cuánto vendí ayer en Sur?" returns answer + SQL + result rows + chart inline when provider credentials are present; without `ANTHROPIC_API_KEY`, UI/permissions/errors remain validated and provider call is correctly blocked. | **Medium** |
| `ENG-032` | AI anomaly + fraud detection | Shipped | Local-only (no LLM) detectors in `services/ai/anomalyDetection.ts` covering five common POS fraud patterns via z-score with leave-one-out baseline (3σ/4.5σ thresholds), surfaced through a manager+ `ai.anomalies.list` and an `AnomalyDetectionCard` + drill-down modal. **Shipped**: tunable constants, `AI-ANOMALY-DETECTION.md` rationale doc, and 20 tests (detectors, integration, card states). | Synthetic anomaly fixture triggers alert; clean baseline does not; multi-tenant isolation tested; cashier role does not see other cashiers' anomalies (manager+ only). | **Medium** |
| `ENG-033` | AI semantic product search + auto-categorization | Shipped | Backend de búsqueda semántica: migración `0009_product_embeddings.sql` agrega columnas de embedding a `products`; `services/ai/embeddings.ts` hace cosine similarity en JS + `embedText`/`regenerateProductEmbeddings`/`semanticSearchProducts` (floor 0.30)/`suggestProductCategory` (z.enum, no hallucina). **Shipped**: routers `products.semanticSearch/regenerateEmbeddings/suggestCategory`, fallback a LIKE cuando AI está unavailable, 15 tests + doc `AI-SEMANTIC-SEARCH.md`; UI integrada en ENG-048. | "vino tinto reserva" matches embedded productos sin substring exacto. Nuevo producto recibe `categoryId` sugerido con `confidence` 0..1 antes de guardar. AI disabled o tenant en Anthropic → procedures retornan `unavailable` sin error y caller cae a LIKE. | **Medium** |
| `ENG-034` | FISCAL-CORE refactor pluggable | Shipped | Promote `services/fiscal/registry.ts` from an implicit singleton to a typed factory keyed by tenant `countryCode`, with country packs at `services/fiscal/packs/{co,mx,cl}/`. **Shipped**: Colombia migrated to `ColombiaMockAdapter`, MX/CL ship as `NotImplemented` stubs throwing `FISCAL_PACK_NOT_AVAILABLE`; the adapter contract gained `validateConfig` + `countryCode`, a sync `getFiscalAdapter`/`listFiscalAdapterCountries`, an unknown-country fallback, and 42 tests; mirrors the AI factory. | Sale completed under tenant `countryCode='CO'` writes the same CUFE shape as pre-refactor (regression smoke); flipping to `countryCode='MX'` makes the fiscal emission throw `FISCAL_PACK_NOT_AVAILABLE` with the sale itself completing per the existing non-blocking try/catch in `sales.ts`. Adapter contract documented in `FISCAL-INTEGRATION.md` ENG-034 section. ENG-035 + ENG-036 unblocked. | **High** |
| `ENG-035` | Pack México CFDI 4.0 | Partial | Pack completo de México con CFDI 4.0, PAC integration, CSD signing, complemento Pago 2.0. Demasiado grande para un solo commit (2-3 semanas + dependencias externas: contrato PAC, sandbox SAT, certificados CSD de prueba). Split en tres slices: ENG-035a (fundación: validador RFC + catálogos SAT + ajustes admin + adapter validateConfig real), ENG-035b (modelado XML CFDI 4.0 + emisión sin firmar), ENG-035c (PAC integration + firmado CSD + complemento Pago 2.0). **Remaining**: ENG-035c. | Tenant con `country_code='MX'` emite CFDI 4.0 válido contra PAC sandbox; tenant CO sin afectación; XML round-trip valida contra XSD del SAT. | **High** |
| `ENG-035a` | Pack México — fundación | Shipped | Primer slice del pack México. **Shipped**: validador `validateRfc` (checksum SAT + genéricos), 4 catálogos SAT como TS modules, `mx/settings.ts`, y `MexicoCFDIAdapter` (reemplaza el stub; `validateConfig` lee settings reales, `issue` aún tira `FISCAL_PACK_NOT_AVAILABLE`→ENG-035b); router `fiscal.settings.{getByCountry,updateMx}`, tab Fiscal + `CompanyMxFiscalCard`, error codes `FISCAL_RFC_INVALID`/`FISCAL_REGIMEN_INVALID`, y 57 tests. Convención: docs/JSDoc fiscal en español desde aquí. | Admin con tenant flagged `country_code='MX'` ve la tab Fiscal, captura RFC válido + régimen 601 + lugar 06700 → readiness flips de rojo a verde; RFC inválido es rechazado server-side con `FISCAL_RFC_INVALID`; régimen no en catálogo rechazado con `FISCAL_REGIMEN_INVALID`. Tenants CO y CL ven placeholder apuntando a ENG-035c / ENG-036. | **High** |
| `ENG-035b` | Pack México — modelado XML CFDI 4.0 | Shipped | Slice 2: serializador CFDI 4.0. **Shipped**: `packs/mx/cfdi40-xml.ts` genera CFDI 4.0 estructuralmente válido (Anexo 20 SAT, conceptos tax-exclusive, catálogo `claveProdServ` curado); `MexicoCFDIAdapter` emite draft sin firmar (`status='pending'`), modal "Ver XML", dep `fast-xml-parser`. Firma + transmisión PAC → ENG-035c. | Sale completado en tenant MX persiste `fiscal_documents` con XML CFDI 4.0 estructuralmente válido (pasa XSD); `MexicoCFDIAdapter.notImplemented` se desactiva pero el documento queda en `status='pending'` hasta que ENG-035c lo firme + transmita. | **High** |
| `ENG-035c` | Pack México — PAC integration + firmado CSD + complemento Pago 2.0 | Pending | Slice 3 del pack México. Selección de vendor PAC (Solución Factible o Edicom — operator decide), integración HTTPS REST con el sandbox del PAC, firmado XAdES con CSD del emisor (carga del `.cer` + `.key` + password en `tenants.settings.fiscal.mx.csd.*` con encryption-at-rest), complemento Pago 2.0 para ventas a crédito + parcialidades, retry daemon para timbrado fallido, rename de `tenants.settings.fiscal_dian_enabled` a namespace country-aware `tenants.settings.fiscal.{country}.enabled`. | Tenant MX con CSD configurado y `OPENAI_API_KEY` (o equivalente PAC sandbox) cargado emite un CFDI 4.0 firmado y timbrado en el sandbox del PAC — incluye sello digital del SAT + folio fiscal UUID; `cancel` y `pago 2.0` shippean en este mismo ticket. | **High** |
| `ENG-036` | Pack Chile SII | Partial | Pack completo de Chile con DTE 1.0, certificación SII, firmado XAdES, entrega digital obligatoria desde mar-2026. Demasiado grande para un solo commit (2-3 semanas + dependencias externas: certificado digital del emisor, sandbox de certificación SII, CAF folios). Split en tres slices: ENG-036a (fundación: validador RUT + catálogos SII + ajustes admin + adapter validateConfig real), ENG-036b (modelado XML DTE + emisión sin firmar + manejo CAF), ENG-036c (certificación SII + firmado XAdES + entrega digital + retry daemon). **Remaining**: ENG-036c. **Shipped slice b 2026-05-07**: nueva migración `0019_fiscal_cafs.sql` agrega tabla `fiscal_cafs` con partial unique idx `(tenant_id, tipo_dte) WHERE status='active'` que enforce un solo CAF activo por (tenant, TipoDTE) — mirror del shape ENG-064 sync_outbox idempotency. Servicio nuevo `services/fiscal/packs/cl/caf-allocator.ts` con `allocateNextFolio(tx, args)` atómico (avanza el cursor + flippea status='exhausted' cuando se llega al último folio) + `peekActiveCaf(db, tenant, tipoDte)` read-only para la admin tab. Nuevo serializador `services/fiscal/packs/cl/dte10-xml.ts` (~450 LOC, espejo estructural de cfdi40-xml.ts) emite XML DTE 1.0 con namespace SII v1.0 + Encabezado/IdDoc/Emisor/Receptor/Totales + Detalle 1..n + Referencia para NC + TED.DD completo (RE/TD/F/FE/RR/RSR/MNT/IT1/CAF<DA>/TSTED) y FRMT placeholder vacío hasta ENG-036c. Mappings helper `mappings.ts` traduce paymentMethod a FmaPago SII (1=contado, 2=crédito), unit a UnmdItem ('un'/'kg'/'lt'/...), source+buyerHasRut a TipoDTE ('33'/'39'/'61') y compute totals afecto+exento+IVA con redondeo CLP. ChileSIIAdapter sale del stub NotImplementedFiscalAdapter — `issue()` real retorna `{cufe='sii-cl:<RUT>:<TipoDTE>:<F>', status='pending', xmlRef=string}`; `voidDocument()` y `fetchStatus()` siguen apuntando a ENG-036c. Adapter contract extendido con optional `chileAllocation` field (orchestrator pre-allocates inside writeTx → folio + fiscal_documents insert + outbox enqueue commit atómicos; si el allocator throws CAF_NOT_AVAILABLE/CAF_EXHAUSTED el tx rollback completo no quema folio). Dos errorCodes nuevos `CAF_NOT_AVAILABLE` + `CAF_EXHAUSTED` con i18n en + es neutral LATAM tú. tRPC procedure nueva `fiscalSettings.getActiveCaf({countryCode, tipoDte})` adminProcedure read-only para la card. CompanyClFiscalCard muestra sección read-only "CAF activo" con tipoDTE / rango / folios disponibles cuando hay CAF activo, mensaje de "Sube uno desde el portal SII" cuando no. SEED_COUNTRY=cl en dev-seed pobla fiscal.cl.* con valores de prueba (RUT 76123456-0, giro 4711, comuna 13101 Santiago, casa matriz Av. Principal 123, ambiente certificación) + inserta CAFs fixture fiscal_cafs (TipoDTE 33/39/61, folios 1..100). Helper `buildTenantSettings(country)` extraído del seed para limpiar el ternary chain de 3 ramas. ~28 nuevos tests server (caf-allocator 11 cases, dte10-xml 25 cases, mappings 12 cases, chile-adapter 6 cases). Counter Plan v2.0 sin cambio (10/16 still — ENG-036 sigue Partial hasta ENG-036c); narrative shifts de "1/3 slice Chile" a "2/3 slices Phase 2 Chile". | Tenant con `country_code='CL'` emite DTE válido contra SII sandbox; canal de entrega digital seleccionable por tenant; timbre impreso suprimido cuando locale es CL. | **High** |
| `ENG-036a` | Pack Chile — fundación | Shipped | Primer slice del pack Chile, espejo de ENG-035a. **Shipped**: validador `validateRut` (SII módulo 11 + genérico extranjero + threshold persona/jurídica), 3 catálogos SII como TS modules, `cl/settings.ts`, y `ChileSIIAdapter` (reemplaza el stub; `validateConfig` real, `issue`→`FISCAL_PACK_NOT_AVAILABLE`/ENG-036b); router `fiscal.settings.updateCl`, `CompanyClFiscalCard` con dispatch de país movido a `CompanyPage`, error code `FISCAL_RUT_INVALID`, y 48 tests. | Admin con tenant flagged `country_code='CL'` ve la tab Fiscal con `CompanyClFiscalCard`, captura RUT válido `55555555-5` + giro 4711 + comuna 13101 + casa matriz "Av Apoquindo 4500" → readiness flips de rojo a verde; RUT inválido rechazado server-side con `FISCAL_RUT_INVALID`; giro no en catálogo CIIU.cl rechazado con `FISCAL_REGIMEN_INVALID`. Tenants MX siguen viendo `CompanyMxFiscalCard`; tenants CO ven placeholder apuntando a ENG-035c. | **High** |
| `ENG-036b` | Pack Chile — modelado XML DTE | Shipped | Slice 2: modelado XML DTE 1.0. **Shipped**: serialización DTE estructural con TipoDTE por source + RUT, folio vía CAF atómico, `ChileSIIAdapter.issue()` real con status `pending`, `fiscalSettings.getActiveCaf` y CAFs fixture en `SEED_COUNTRY=cl`. Firmado XAdES + transmisión SII y catálogo completo de comunas → ENG-036c / follow-up. | Sale completado en tenant CL persiste `fiscal_documents` con XML DTE estructuralmente válido; `ChileSIIAdapter.notImplemented` se desactiva pero el documento queda en `status='pending'` hasta que ENG-036c lo firme + transmita. | **High** |
| `ENG-036c` | Pack Chile — certificación SII + firmado XAdES + entrega digital | Pending | Slice 3 del pack Chile. Flujo de certificación SII (registro del emisor + envío de prueba), firmado XAdES con certificado digital del emisor (carga del `.pfx` + password en `tenants.settings.fiscal.cl.certificate.*` con encryption-at-rest), entrega digital obligatoria desde mar-2026 (email + SMS + QR para boleta), eliminación del timbre impreso (1-jan-2026), retry daemon para timbrado fallido, rename de `tenants.settings.fiscal_dian_enabled` a namespace country-aware `tenants.settings.fiscal.{country}.enabled`. | Tenant CL con certificado configurado emite un DTE firmado y aceptado por el SII en el ambiente de certificación — incluye sello digital + folio CAF; cancel y entrega digital shippean en este mismo ticket. | **High** |
| `ENG-037` | libSQL/Turso embedded replicas spike | Shipped | 1-week investigation into a read-replica + cloud-backup pattern without migrating off SQLite; the spike report is the deliverable. **Shipped**: report at [`docs/SPIKE-LIBSQL-TURSO.md`](./SPIKE-LIBSQL-TURSO.md), recommendation **Defer (revisit after Phase 4)** — Embedded Replicas conflict with ADR-0001, Turso Sync's Last-Push-Wins is incompatible with the manual lane, and libSQL is no simpler than `better-sqlite3`; reopen triggers documented in spike §2. | Spike report committed (decision document); if greenlit, embedded replica works against Turso staging with conflict-resolution strategy documented and a fallback to local-only when offline. | **Medium** |
| `ENG-038` | LATAM payment rails + reconciliation | Partial | Unified abstraction over Wompi + Bold + ePayco + Mercado Pago + Nequi/Daviplata. Each rail behind a `PaymentRailAdapter` with `charge`, `refund`, `getStatus`. Plus AI-assisted reconciliation (idea AI #16) that maps gateway transaction reports to POS sales nightly via a server-side cron, surfacing mismatches in an admin dashboard. **Shipped slice 1 (2026-05-11)**: payment rail manifest + deterministic adapters for Wompi, Bold, ePayco, Mercado Pago, Nequi and Daviplata; new `payment_outbox` table with idempotency key, provider transaction reference, status/retry projection and sale-payment linkage; manager/admin read-only `payments.{getContract,peekOutbox,reconciliation}` tRPC namespace; `/operations?tab=payments` panel with rail summary, mismatch list and outbox tail; server + web tests cover contract, tenant isolation, role gate, ordering and mismatch classification. **Shipped slice 2 (2026-05-11)**: tenant credential model + admin settings UI. `PaymentRailAdapter` gained an optional `validateConfig` method; the shared `DeterministicPaymentRailAdapter` implements it by walking a new per-rail descriptor `CREDENTIAL_FIELDS_BY_RAIL` and emitting `PAYMENT_CREDENTIAL_MISSING` issues for each empty required field. New `services/payments/credentials.ts` exposes `readPaymentRailCredentials` / `mergePaymentRailCredentialsIntoTenantSettings` / `projectRailCredentials` (mask = `••••••••` + last-3 chars; full plaintext is never re-served after save). New admin tRPC namespace `paymentSettings.{getAll, updateRail}` writes to `tenants.settings.payments.<railId>.credentials.*`, rejects undeclared field keys with new error code `PAYMENT_CREDENTIAL_UNKNOWN_FIELD`, and treats empty-string + null as "clear the field". New `/company?tab=payments` admin tab renders one `<RailSection>` per rail with a readiness badge, live/stub badge, and per-field password input + Show/Hide reveal toggle; masked stored values surface in a hint paragraph below the input (never in the `placeholder` attribute). Diagnostic sanitizer's denylist extended with `merchantid`, `customerid`, `pkey` so the export bundle redacts every credential field automatically. 13 new server tests (8 router + 4 contract validateConfig + 1 sanitize) + 5 new web component tests + the CompanyPage tab-count assertion bumped to 8. Live smoke verified: admin saves Wompi credentials → readiness flips from "Missing credentials" to "Ready"; masked tail `••••••••abc` / `••••••••xyz` lands in the hint paragraph. **Shipped slice 3 (2026-05-13)** — closes the reconciliation half of the AC; see `ENG-038c` row below for the matcher + worker skeleton + 95% fixture proof + AI tie-break. **Remaining**: rail-specific API clients/workers (gated by provider sandbox contracts) and live statement-import HTTP wiring (the worker's `fetchStatement` injection point is in place). Admin retry/requeue + per-method breakdown carved out as ENG-065d closeout. | All five rails callable via single interface; reconciliation cron correctly matches 95%+ of transactions on a 30-day fixture; mismatches surface with proposed resolutions. | **High** |
| `ENG-038c` | Reconciliation cron + 30-day fixture + AI tie-break matcher | Shipped | Carved from ENG-038 so the matcher half could ship without live-provider contracts. **Shipped**: `payment-worker.ts` (two timers + boot catch-up sweep), `runReconciliationPass` with a deterministic taxonomy + `ambiguous` bucket, `ai-tiebreak.ts` that degrades silently on AI short-circuit, and a seeded 1068-row fixture + benchmark hitting 96.63%. Live charge dispatch and the retry UI stay deferred (ENG-065d). | Reconciliation cron correctly matches 95%+ of synthetic statement rows on the 30-day fixture; ambiguous candidates flow through the AI tie-break with graceful degradation when the provider is unavailable; the worker boots a catch-up sweep at start so an offline POS reconciles missed windows. | **High** |
| `ENG-039` | Vertical restaurant | Partial | Umbrella for the full restaurant vertical — multi-country LATAM (Colombia, México, Perú, Chile, etc.). Only the CFDI MX issuance child (`ENG-039f`) is México-specific because it depends on the SAT PAC contract from `ENG-035c`. The umbrella itself does not add runtime behavior until its child slices close. **Slices closed**: ENG-039a (voice-ordering MVP, 2026-05-12) + ENG-039b (table catalog schema + admin CRUD + voice-ordering dropdown, 2026-05-14) + ENG-039c (sales.tableId FK + open/transfer semantics + restaurantTables.listWithDraftStatus + SuspendedSalesPanel table badge, 2026-05-14) + ENG-039c2 (transfer-to-table CTA UI on SuspendedSalesPanel + first consumer of listWithDraftStatus, 2026-05-14) + ENG-039c3 (split-bill mutation + SplitBillModal UI off SuspendedSalesPanel, 2026-05-14) + ENG-039d (tip / propina capture + persistence, 2026-05-14) + ENG-039d3 (service charge / propina sugerida math + tenant settings, 2026-05-14) + ENG-039e (KDS surface — shipped as the standalone `ENG-098` 2026-05-19) + ENG-039d2 (structured modifiers per-line `sale_items.notes`, 2026-05-19; the voice-ordering surface now persists modifiers inline on each `sale_items` row instead of aggregating them into the sale-level `sales.notes` field, and the KDS card renders them dim under each product name; legacy `sales.notes` panel kept as fallback for pre-ENG-039d2 cards; renamed the umbrella title to drop the legacy "Mexico" suffix because every closed slice is country-agnostic — only ENG-039f stays explicitly tagged as MX-specific). **Remaining**: ENG-039f (CFDI MX issuance from restaurant sales — depends on ENG-035c, Gated). | Full restaurant tenant flow runs end-to-end: open table → modify items → split bill → tip → print receipt → CFDI emitted (the CFDI part is México-only and waits for ENG-039f). Course-timing flag visible to KDS. | **Medium** |
| `ENG-039d3` | Restaurant service charge / propina sugerida math | Shipped | Seventh slice of `ENG-039`: a tenant-configured mandatory service-charge percentage that auto-applies to every restaurant-mode checkout. **Shipped**: migration `0026_sales_service_charge.sql` + Zod fields (0–30 ceiling); `services/restaurant/settings.ts` + `assertServiceChargeMatchesTenant` (drift/disabled guards); `completeSale` total formula extended without draft-time compounding; `restaurantSettings` router + `CompanyRestaurantSettingsCard` tab; `SalePaymentModal` Servicio section; 18 tests. | Admin opens `/company?tab=restaurant`, sets the rate to 10%, saves. Cashier opens `/sales`, adds items, hits "Cobrar"; the SalePaymentModal shows a read-only Servicio section auto-populated with 10% of subtotal; the persisted sale row carries `service_charge_amount` + `service_charge_rate`; non-restaurant tenants see no surface impact. | **Medium** |
| `ENG-039d` | Restaurant tip / propina capture | Shipped | Sixth slice of `ENG-039`: tip persistence on `sales`, threading through `completeSale`, and a `SalePaymentModal` tip section (the bundled modifiers + service-charge concerns re-promoted to `ENG-039d2`/`ENG-039d3`). **Shipped**: migration `0025_sales_tip.sql` + Zod fields with a stale-method refinement; fresh + fromDraft total layering with negative clamp + audit metadata; Propina presets (0/10/15% + custom) with split-tender Σ vs `total + tip`; 12 tests. | Cashier opens a sale or resumes a draft, picks 10% propina, the grand-total header shows base + tip, the customer pays the new total, the persisted sale row has `tip_amount` + `tip_method`, and the receipt renders the tip line. | **Medium** |
| `ENG-039a` | Restaurant voice-ordering MVP | Shipped | First actionable slice of `ENG-039`: a dedicated `/touch` + `/m` ordering flow over the sales draft model (table label, push-to-talk transcribe → parsed cart lines → review → suspend); free-form modifiers preserved as notes, no new table/KDS schema. **Shipped**: parser gains an optional per-item `note`; `VoiceOrderingScreen` + `TouchHome`/`MobileWaiterHome` wrappers replace the ENG-069 placeholders; `restaurants` i18n namespace; 18-case server + web suites; live smoke confirmed the surface, notes round-trip, and module gate. | In a restaurant tenant with `pos-touch` or `mobile-waiter` enabled, a waiter opens the dedicated surface, chooses `Mesa 5`, says "agrega 2 cocas y una hamburguesa sin queso", reviews the transcript and parsed lines, confirms them into the cart, and suspends/saves the order under `Mesa 5`; no sidebar, sales history, KDS, floor plan, structured tips, or administrative options are visible on the surface. | **High** |
| `ENG-039c3` | Restaurant split-bill mutation + SplitBillModal UI | Shipped | Fifth slice of `ENG-039`: a manager/admin `sales.splitDraft` mutation + `SplitBillModal` to carve items out of a suspended draft into a new one (per-guest billing). **Shipped**: critical-command procedure validates status + item ownership (collapsing unknown ids to `SALE_SPLIT_ITEMS_NOT_FOUND`), allocates a new sale number, relocates items with a TOCTOU guard (never re-debiting stock), recomputes both drafts, and audits in-tx; modal gated on role + catalog + item count; 3 error codes, `sale.splitDraft` audit action, 19 tests. | Manager or admin opens `/sales`, picks "Dividir cuenta" on a suspended draft, ticks 1+ items, picks a target table (or "Liberar mesa" + free-text label), confirms; the source draft loses the moved items and recomputes totals; a brand-new suspended draft appears in the panel with the moved items + chosen table badge; cashier / cross-tenant / non-suspended attempts surface localized hints without closing the modal. | **Medium** |
| `ENG-039c2` | Restaurant transfer-to-table CTA on SuspendedSalesPanel | Shipped | Fourth slice of `ENG-039`: wire the manager/admin `sales.changeTable` mutation into a "Cambiar mesa" CTA on each `SuspendedSalesPanel` row, the first consumer of `restaurantTables.listWithDraftStatus`. **Shipped**: `TransferTableModal` with `(actual)`/`(ocupada)` suffixes + a "Liberar mesa" detach option; role + catalog-gated CTA (hidden while loading/error); `restaurants:transfer.*` i18n; 10 web cases + live smoke. | A manager or admin on `/sales` opens the suspended panel, clicks "Cambiar mesa" on a draft, picks a different table (or "Liberar mesa"), confirms; the draft's badge updates on the panel; the source table flips to free in `listWithDraftStatus` and the target flips to occupied; cashier / cross-tenant / non-suspended attempts surface localized hints without closing the modal. | **Medium** |
| `ENG-039c` | Restaurant `sales.tableId` FK + open-table read surface + change-table mutation | Shipped | Third slice of `ENG-039`: turn the ENG-039b free-text label into a real FK so the server can report open-table status and managers can transfer drafts between tables. **Shipped**: migration `0024_sales_table_id.sql` + relation; optional `tableId` on create/suspend with a tenant+active `resolveActiveRestaurantTable` guard (cross-tenant/archived → `RESTAURANT_TABLE_NOT_FOUND`); `sales.changeTable` critical-command + audit; `restaurantTables.listWithDraftStatus`; a `MapPin` draft badge; 16 tests + a new `SuspendedSalesPanel.test.tsx`. | A cashier picks a restaurant table on `/touch`, saves the order; the server persists `sales.table_id`; `restaurantTables.listWithDraftStatus` reports that table as occupied with the sale total; the SuspendedSalesPanel renders the table name as a badge next to the label; a manager can move a different cashier's draft between tables and an audit row captures both ends. Cross-tenant access collapses to `RESTAURANT_TABLE_NOT_FOUND`; non-suspended drafts can't be moved. | **Medium** |
| `ENG-039b` | Restaurant table catalog (schema + admin CRUD + voice-ordering dropdown) | Shipped | Second slice of `ENG-039`: replace ENG-039a's free-text label with a `(tenantId, siteId)`-scoped catalog of physical tables. **Shipped**: migration `0023_restaurant_tables.sql` with a hand-appended partial-unique idx (archived rows free the name); `restaurantTables.{list,getById,create,update,archive}` (admin mutations, idempotent archive, in-tx audit); `RESTAURANT_TABLE_NOT_FOUND`/`_NAME_DUPLICATE` codes; admin `/restaurants/tables` page; `VoiceOrderingScreen` dropdown with free-text fallback; 23 tests. | Admin curates a per-site list of tables; the waiter on `/touch` or `/m` picks from the dropdown instead of typing the label. Cross-tenant access collapses to NOT_FOUND. Existing tenants without tables keep the free-text input. | **Medium** |
| `ENG-040` | AI Wave 2 (vision + voice) | Shipped | Provider-invoice OCR via `generateObject` vision plus Whisper voice ordering into structured cart commands, with an optional local Ollama provider. **Shipped** (ENG-040a/b/c/d): `ai.extractInvoiceLines` + embedding line-matching, Ollama provider, `ai.transcribeAudio` + voice parser + reusable `VoiceCartCommandModal`, and mobile camera capture; the 10-invoice OCR benchmark hits 100%, closing Phase 4. | Receipt photo round-trip extracts ≥80% of line items on a 10-receipt fixture; voice transcription infrastructure remains behind `ai.enabled`; restaurant cart-command acceptance lives in `ENG-039a`. | Medium |
| `ENG-042` | Sync resilience UX (stale conflicts + network errors) | Shipped | Out-of-band: stale-conflict queue recovery, localized network-error copy, and localized Company-card error toasts. **Shipped**: `sync.*` expose `localRecordExists` and refuse keepLocal/merged on a missing row; `companySyncDisplay.ts` + `isNetworkConnectivityError` localize labels/errors; the TOCTOU close-out hoisted `sync.resolve` findEntity in-tx and added `assertCashSessionStillOpen` (see ENG-055). | Operator with a stale local-record conflict can clear it via "Discard Local Change" without database surgery; offline boot logs no console error; every server error in Company cards renders localized copy from `errors:server.*`. | Medium |
| `ENG-049` | Purga UX — headers + heroes minimalistas | Shipped | Out-of-band, operator-driven: pasada de minimalismo sobre el chrome textual redundante de ENG-045. **Shipped**: header reescrito a una columna sin kicker/h2/badge, sidebar brand plano, heroes de Dashboard/SalesOverview sin bloque kicker+description, 12 páginas estándar sin `page.description`, `ResourcePage` con `description?` opcional, ~30 keys i18n huérfanas borradas (parity 104/104) y dos collateral fixes de tipos huérfanos. | Header sin chrome textual; sidebar con "Puntovivo" plano; cada página estándar abre con h1 corto + acción + contenido (sin párrafo descriptivo abajo). Mobile sigue mostrando hamburger; locale-parity verde; cero `t('page.kicker')` rendered como string crudo. | **Medium** |
| `ENG-050` | Sellability index + roadmap truth sync | Shipped | Align the roadmap with the current shipped state and add `docs/SELLABILITY.md` as the Colombian retail go/no-go index for demo/pilot/production. **Shipped**: ROADMAP MVP status matches the repo, SELLABILITY captures pilot/production blockers and next tickets, and the docs indexes point to it. | `ROADMAP.md`, `SPRINT-PLAN.md`, and `SELLABILITY.md` do not contradict each other; every sellability blocker has a gate and next ticket. | **High** |
| `ENG-051` | Foundation ADR pack | Shipped | Add short ADRs for Local Store Authority, Command Envelope, Outbox taxonomy, and Conflict policy. **Shipped**: `docs/architecture/` con README + cuatro ADRs `Accepted` — `0001` (Electron como autoridad del local store), `0002` (envelope `operationId`/`deviceId`/`idempotencyKey`/`clientCreatedAt` con lista cerrada de comandos críticos), `0003` (cinco outboxes con kernel compartido), `0004` (manual-only para money/fiscal/cash, auto-LWW auditado para catalog/preferences); enlazados desde `docs/README.md` + `PLAN.md §18.1`. | Each ADR records decision, rejected alternatives, implementation impact, and affected future tickets. | **High** |
| `ENG-052` | Device registry + command envelope | Shipped | Add formal `devices` + idempotency-key models; propagate `x-device-id` and accept `operationId`/`idempotencyKey`/`clientCreatedAt` on critical commands. **Shipped** (ENG-052a/b): migrations 0010/0011, a `commandEnvelope` middleware that reserves the key and returns `COMMAND_IN_PROGRESS`/replay-cached/`IDEMPOTENCY_KEY_CONFLICT`, `criticalCommandProcedure*` decorators on all 17 ADR-0002 mutations, and web/Electron envelope minting + atomic device-id persistence. | Replaying the same idempotency key returns the same resource or rejects a different payload; logs can correlate UI click → tRPC → DB transaction. | **High** |
| `ENG-053` | Operation journal + outbox kernel | Shipped | Add an operational journal (`operation_events`/`_effects`/`_errors`) plus a shared outbox kernel with status/attempts/`nextRetryAt`/payload version; not event sourcing. **Shipped**: migración `0012`, `services/operation-journal/journal.ts` (idempotent lifecycle), `lib/outbox/` kernel (`createOutboxKernel` con atomic `claim_token` + BOUNDED_EXPONENTIAL_BACKOFF) + worker + metadata; `commandEnvelope` records start/complete/error; pattern docs en `architecture/patterns/`; 34 tests + live smoke. | Critical commands leave a correlated trail; post-commit failures are recorded without rolling back the original sale/cash/inventory operation. | **High** |
| `ENG-054` | Extract completeSale application service | Shipped | Move complete-sale orchestration out of the tRPC router into `application/sales/completeSale`, coordinating cash session, payments, stock, audit, outbox, and journal effects behind a stable use-case boundary. **Shipped**: new `application/` directory + the `completeSale(ctx, input)` use-case (discriminated `fresh`/`fromDraft`) with pre-checks outside one tx and best-effort POST-commit journal effects; pure policies + `sale-read` + `safelyEmitFiscalDocument` extracted; router `sales.ts` shrank 2670→~1600 lines to thin wrappers; first journal consumer; 13 invariant tests, zero tRPC-contract changes, live smoke OK. | Behavior parity with current `sales.create` / `completeDraft`; invariant tests run without HTTP/tRPC. | **High** |
| `ENG-055` | Extract sale lifecycle services | Shipped | Extract `returnSale`, `voidSale`, and `discardDraft` use-cases and consolidate shared sale/payment/inventory/fiscal policy rules. **Shipped**: tres use-cases nuevos en `application/sales/` con shared `inventory-policy.ts`/`fiscal-policy.ts`/`policies.ts`; `assertCashSessionStillOpen` + `insertCashMovement` quedan con una sola fuente en `services/cash-session.ts`; router `sales.ts` 1692→~990 líneas a thin wrappers, dead code eliminado, journal effects automáticos; 27 invariant tests, zero cambios de contrato tRPC. | No duplicated cash/stock/fiscal lifecycle rules remain across sale mutations; existing sales tests stay green. | **High** |
| `ENG-056` | Cash session aggregate boundary | Shipped | Route every cash-affecting operation (sale/refund/void/paid-in-out/close/over-short + pending checks) through cash-session use-cases. **Shipped**: `application/cash-sessions/` con open/close/recordMovement use-cases (close emite `pending_warning` effects warning-only), shared `pending-checks.ts` + `journal-effects.ts`, router a thin wrappers + nueva `cashSessions.pendingChecks` query; `insertCashMovement` widened, `closeCashSession` gana TOCTOU re-check, 2 audit actions TS-only; 38 tests, live smoke, zero cambios de contrato. | Tests cover no-sale-without-session, no cash movement outside a session, close-session totals, over/short, and pending fiscal/payment warnings. | **High** |
| `ENG-057` | Fiscal outbox + contingency engine | Shipped | Introduce a fiscal outbox separate from generic sync with a 7-state lifecycle, retry backoff, normalized provider errors, and operator-visible recovery. **Shipped**: migration `0013_fiscal_outbox.sql` (first concrete table on the ENG-053 kernel), a 10-kind `FiscalProviderError` union, `enqueueFiscalEmission` that pre-creates the document + advances numbering + enqueues in ONE write tx (see ENG-057 marker), an out-of-band `fiscal-worker`, and `reports.fiscal.retryDocument`. | Offline sale completes locally and leaves a visible pending fiscal document; simulated provider outage writes contingency state and does not roll back the sale. | **High** |
| `ENG-058` | Receipt fiscal finalization | Shipped | Finalize fiscal proof rendering on receipts/reports: CUFE/CUDE or provider UUID, QR, XML reference, and explicit accepted/pending/contingency/rejected status. **Shipped**: `services/fiscal/qr-builder.ts` builds the per-country verification QR (CO/MX; CL→ENG-036b) behind placeholder-leak guards; `getSaleRecord` exposes the documents with a computed `qrPayload`; web `FiscalStatusBadge` + `SaleDetailsFiscalBlock` + a lazy-`qrcode` section render the proof fields, status copy as single source of truth. | Fiscalized receipt contains the legal proof fields; contingency documents never render as accepted. | **High** |
| `ENG-059` | Real DIAN PT adapter | Gated | Swap the mock Colombia adapter for the selected DIAN-authorized Proveedor Tecnologico adapter. Gate: signed PT contract, sandbox/prod credentials, tenant certificate, numbering resolution, and provider error-code map. | DIAN/PT sandbox set passes; adapter swap does not change sales or application-service code. | **High (gated)** |
| `ENG-060` | Peripheral registry + hardware ports | Shipped | Add `site_peripherals` and hardware contracts for `ReceiptPrinter`, `CashDrawer`, `BarcodeScanner`, and `PaymentTerminal`, plus site-level configuration UI. **Shipped**: migration `0014_site_peripherals.sql` (one active peripheral per kind/site via a partial unique index), four typed hardware contracts + `BasePeripheralAdapter` + `NormalizedHardwareError`, a static adapter registry (`getPeripheralAdapter` returns null when unregistered so sales keep working), an admin-only `peripherals` router (6 procedures), and the `/peripherals` admin UI. Default drivers: `system` printer + `manual` terminal; `hardware_outbox` deferred to ENG-062. | Current system-printer path still works; ESC/POS, scanner, and payment terminal drivers can be registered without touching sales logic. | High |
| `ENG-061` | Barcode scanner pipeline | Shipped | Implement USB HID keyboard-wedge barcode capture, EAN-13 checksum, UPC/Code128 basics, and prefix 20-29 handling for weighted/price-embedded labels. **Shipped**: pure `barcode/parser.ts` (EAN-13/EAN-8/UPC-A checksums + GS1 prefix-2x weight/price), `KeyboardWedgeScannerAdapter` (driver `wedge`), `products.lookupByBarcode` + `peripherals.activeForSite` procedures (cashier-accessible), and a `useBarcodeWedgeListener` hook wired into SalesPage whose inter-char-gap timing rejects manual typing. Per-country GS1 schemes piggyback on generic until a pilot diverges. | Scanner adds a product to the cart within the target latency; manual typing is not misclassified as scanner input. | High |
| `ENG-062` | ESC/POS printer + cash drawer | Shipped | Implement the ESC/POS printer driver + RJ11 cash-drawer kick with the system printer as fallback; full validation gated on hardware. **Shipped**: migration `0015_hardware_outbox.sql`, pure `escpos/byte-builder.ts` (cp858, 58/80mm), a Mock+TCP transport (USB/serial stubs throw `DRIVER_NOT_IMPLEMENTED`), printer + drawer drivers, a `hardware-worker` daemon, and `peripherals.printReceipt`/`kickCashDrawer`/`peekHardwareOutbox`; server-side dispatch falls back to the system HTML path. | ESC/POS bytes print, cut, and open drawer in hardware smoke; driver failure surfaces an actionable error and never duplicates a sale. | High |
| `ENG-063` | Payment terminal adapter | Gated | Add `ManualPaymentTerminalAdapter` fallback and prepare Bold/Wompi adapter seams. Gate: provider choice, sandbox credentials, and physical terminal. | Approved payment persists auth/reference in `sale_payments`; decline/timeout leaves the sale recoverable; offline-payment risk limits are explicit. | **High (gated)** |
| `ENG-064` | Sync contract v1 | Shipped | Formalize the sync payload contract: entity type, operation, payload version, idempotency key, device ID, priority, dependencies, ordering, retry, duplicate suppression, and conflict policy. **Shipped**: migration `0016_sync_contract_v1.sql` adds `sync_outbox` (kernel projection + the 7 contract columns + an idempotent partial-unique index) and backfills from `sync_queue`; a pure `services/sync/contract.ts` manifest (`SYNC_PAYLOAD_VERSION`, conflict-policy map, priority defaults — see ADR-0005); the `enqueueSync` helper + `sync.getContract`/`peekOutbox`/`retry` procedures. Writer cutover + `sync_queue` drop were parked to ENG-064b. | Tests cover ordering, retry, duplicate suppression, and manual conflict paths for high-risk entities. | High |
| `ENG-064b` | Sync writer cutover + drop `sync_queue` | Shipped | Close the ENG-064 writer/procedure cutover: route every `db.insert(syncQueue)` through `enqueueSync`, cut the legacy `sync.*` procedures to `sync_outbox`, drop `sync_queue`, and rename the web `syncQueue.ts` to `offlineQueue.ts`. **Shipped**: writer rewrite across 19 routers + 4 sale-lifecycle services (post-tx idempotent enqueue), procedures aliased so `useOfflineSync` is unchanged, migration `0017_drop_sync_queue.sql`, and the client rename (ObjectStore name preserved). | Every writer routes through `enqueueSync`; `sync_queue` table is gone; `sync.*` procedures all read/write `sync_outbox`; web client file no longer collides on name. | High |
| `ENG-065` | Operations Center | Shipped | Expand the sync center into an operations center covering Sync, Fiscal, Cash, Payment, Inventory, Device health, and diagnostic export. **Shipped** across ENG-065a (sync/fiscal/device), ENG-065b (cash/inventory), ENG-065c (diagnostic export), the ENG-038 payment baseline, and the ENG-065d closeout — every sub-tab now ships the admin actions to clear a queue without reading logs. | Operator can see what is pending, what failed, and which action to take without reading logs. | **High** |
| `ENG-065a` | Operations Center: Sync + Fiscal + Device Health | Shipped | First slice of ENG-065: the `/operations` page scaffold + three health surfaces whose data is already on the wire (`sync.peekOutbox`, `reports.fiscal.list`, `peripherals.peekHardwareOutbox`). **Shipped**: `/operations` (managerOrAdmin) with sync/fiscal/device tabs — `SyncHealthPanel` reuses `CompanySyncCard`, `FiscalHealthPanel` lists by status with an admin-only retry, `DeviceHealthPanel` aggregates peripherals + the hardware-outbox tail with an admin retry via the new `peripherals.retryHardwareOutbox`. `reports.fiscal.list` widened to managerOrAdmin (read-only). No new tables. Cash/inventory reconciliation → ENG-065b; diagnostic export → ENG-065c. | Operator can see what is pending, what failed, and which action to take without reading logs. | **High** |
| `ENG-065b` | Operations Center: Cash + Inventory Reconciliation | Shipped | Second slice of ENG-065: Cash + Inventory reconciliation tabs on `/operations` using only tRPC procedures (no migrations). Payment carved out to ENG-065d (gated on ENG-063 `payment_outbox`). **Shipped**: `reports.cash.reconciliation` (tenant-wide open + 30-day closed sessions with per-site net over/short and top discrepancies) + `reports.inventory.discrepancies` (cache-vs-cache `products.stock` vs `Σ(inventory_balances)` drift — exactly what `reconcileBalances` heals, since `inventory_movements` has no `site_id`). `CashHealthPanel` is read-only (recount is physical); `InventoryHealthPanel` has an admin-only Reconciliar balances action. No new tables. | Manager + admin opens `/operations?tab=cash` and sees open sessions + closed-window net over/short + per-site discrepancies; admin opens `?tab=inventory`, sees products in drift, and Reconciliar balances heals the cache + invalidates the query. Cross-tenant isolation enforced on both procedures. | **High** |
| `ENG-065c` | Operations Center: Diagnostic Export | Shipped | Third slice de ENG-065: admin-only bulk export para support tickets — bundle JSON con journal + 3 outboxes filtrado por rango de fechas, empaquetado vía `jszip` en el cliente. **Shipped**: `reports.diagnostics.{preview,export}` (adminProcedure) con manifest schemaVersion=1 + 5-name keyset, `fromDate<=toDate` Zod, hard cap 10000/tabla con warning; tab `Diagnóstico` + `DiagnosticExportPanel` (presets + tiles + Descargar ZIP), manager/cashier disabled; 18 tests. Sin PII redaction (admin + tenant-scoped). | Admin abre `/operations?tab=diagnostics`, escoge rango de fechas + outboxes, click Vista previa → ve counts. Click Descargar ZIP → archivo descarga; unzip muestra manifest.json + 5 JSON files. Manager ve el tab pero los botones de diagnóstico están disabled con tooltip. Cross-tenant isolation enforzado. | **High** |
| `ENG-065d` | Operations Center: Payment Reconciliation | Shipped | Carved from ENG-065b once `payment_outbox` existed (ENG-038): payment recon actions on the `payments.*` router. **Shipped**: `payments.retryOutbox` (refuses terminal `settled` with `PAYMENT_OUTBOX_NOT_RETRIABLE`), `payments.markSettled` (idempotent + optional provider-tx), and `payments.methodBreakdown`; both mutations audit in-tx, 2 audit actions + 2 error codes; `PaymentHealthPanel` gains row actions + a rail/status breakdown card; 26 tests. Closes ENG-065 too. | Operator can see pending settlements, failed captures, and per-method breakdown for the recent payment window without reading logs. | **High** |
| `ENG-066` | Backup, restore, and local security | Shipped | Threat-model local POS data; harden backup/restore around outbox + device identity; document no PAN/CVV storage. **Shipped**: atomic `createBackupBundle()` via `better-sqlite3.backup()` + integrity check into a ZIP (db + device-id + manifest); `extractBackupBundle()` validates by magic bytes + integrity before swap (backwards-compat with raw `.db`); recursive denylist `sanitizePayload()` on every diagnostic-export JSON column; an anti-PAN/CVV architectural lint; ADR-0006 + 36 tests. | Backup/restore preserves sales, fiscal docs, outboxes, audit history, and device identity; logs/exported diagnostics avoid secrets and card data. | **High** |
| `ENG-067` | Chaos and resilience suite | Shipped | Add automated scenarios for app restart after sale, large queues, duplicate retry, provider outage, intermittent network, sync conflict, and close-session with pending work. **Shipped**: 5 chaos test files (4 server + 1 web) covering the uncovered scenarios — stale-claim sweep on worker bounce, 10k-row queue depth + read latency, cross-tenant idempotency, recoverable-vs-terminal provider outage, and offline→online auto-sync; the pre-commit review also fixed the kernel priority drain to `priority DESC, createdAt ASC`. 18 new tests. | Suite runs in CI where possible and local-only for Electron/hardware cases; failures point to a concrete recovery gap. | **High** |
| `ENG-067b` | `hardware_outbox` envelope-keyed idempotency | Shipped | Cierra el gap que ENG-067 documentó: dos enqueues del mismo comando lógico producían DOS rows y el recibo se imprimía/el cajón se pateaba dos veces. **Shipped**: migración `0018` agrega `idempotency_key` + partial unique idx (mirror de sync_outbox), helper `enqueueHardware(ctx, args)` que colapsa duplicados a `{deduped:true}`, los 2 callsites inline cutover, opt-in via input (sin upgrade a critical), empty-string normalize, y 8 tests; el chaos test flippeó a `toHaveLength(1)`. | Same-envelope retry to `hardware_outbox` collapses to one row; legacy null-key path stays independent. | **High** |
| `ENG-068` | Module activation kernel | Shipped | Implement the module manifest, per-tenant `tenants.settings.modules` state, server/web route + procedure gating, and soft-disable semantics as the foundation for vertical packs. **Shipped**: `services/modules/manifest.ts` (exhaustive `Record<ModuleId,...>`), `createModuleGuard` + `*WithModule` procedures (`MODULE_NOT_ACTIVATED`), `modules.{list,getEffective,setActive}` (in-tx `module.toggle` audit), 5 demo modules wired end-to-end (default-on), a renderer kernel (`ModulesProvider`/`RequireModule`) + `CompanyModulesCard`, ADR-0007 — closes the Foundation Reset wave 21/21. | Tenant A can activate a module without Tenant B seeing its routes or procedures; deactivation hides behavior without deleting rows. | **Medium** |
| `ENG-069` | Multi-surface POS shell | Shipped | Formalize POS Desktop, POS Touch, KDS, Customer Display, and Mobile Waiter as configured surfaces over the same bundle without the full vertical workflows yet. **Shipped**: surfaces-as-modules over the ENG-068 kernel — `services/surfaces/manifest.ts` (exhaustive), `surfaces.list`, 4 module-gated shell components + placeholder pages routed outside `MainLayout`, a gated "Surfaces" sidebar section, ADR-0007 subsection, and ~24 tests; the 4 surface modules default off. | Each surface has permissions, route config, and layout shell without forking the app. | **Medium** |
| `ENG-070` | Event-based public API + webhook foundation | Shipped | Design public events from the journal/outbox (`sale.completed`/`refunded`, `inventory.adjusted`, `cash_session.closed`, `fiscal_document.accepted`) for the future central server; no Kafka from POS devices. **Shipped**: migración `0020_webhook_outbox.sql` (5ª outbox per ADR-0003), `services/events/manifest.ts` (exhaustive Zod, v1) + pure `projector.ts`, idempotent `enqueueWebhook`, a best-effort hook in `markOperationCompleted` gated by the `events-api` module, `events.{getContract,peekOutbox}`, and ~50 tests; HTTP delivery worker + config UI parked to ENG-070b. | Future central server can publish integrations without reading internal POS tables arbitrarily. | **Medium** |
| `ENG-048` | Semantic search UI surface on ProductsPage | Shipped | Out-of-band: conecta el backend semántico de ENG-033 a UI accionable (las procedures sólo eran llamables vía consola/curl). **Shipped**: toolbar en `ProductsPage` con toggle "Semántica" (debounce 300ms), input dedicado, botón admin "Regenerar embeddings", columna "Coincidencia" con badge `%`, y fallback a búsqueda literal cuando el server responde `unavailable`; i18n en/es; helper `DisplayProduct` mantiene la columna optional. | Manager+ activa el toggle, escribe "leche descremada" → 300ms después la tabla muestra productos rankeados por similarity y la columna Coincidencia exhibe `92%`, `78%`, `65%`. Admin click "Regenerar embeddings" → toast `"Se generaron embeddings de N productos"`. Sin `OPENAI_API_KEY` → toast warning "no se puede regenerar". Sin AI activada → semantic search retorna `unavailable` y un hint de fallback aparece bajo el input mientras la tabla filtra literal por el mismo texto. Cashier no ve ni el toggle ni el botón regenerar (page.permissionNote ya redirige). | **Medium** |
| `ENG-047` | AI anomaly follow-ups (sidebar badge + audit_logs persistence + snooze) | Shipped | Out-of-band: cierra los tres `[ai]` follow-ups del review de ENG-032. **Shipped**: (1) badge rojo en el ítem Dashboard cuando hay alertas Alta (query manager+ reusa `ai.anomalies.list`); (2) `detectAnomalies` escribe `ai.anomaly.detected` audit rows con ID determinístico + `INSERT OR IGNORE` best-effort; (3) tabla `ai_anomaly_snoozes` (migración `0008`) + `ai.anomalies.snooze` + botón "Silenciar 7 días" en `AnomalyDetailsModal`. | Manager con anomalía Alta abierta → badge rojo en el ítem Dashboard del sidebar visible desde cualquier pantalla. Click en "Silenciar 7 días" en una fila del modal → toast de éxito + la alerta desaparece del listado próximo. Misma alerta no reaparece por 7 días aún si el evento subyacente sigue dentro de la ventana de 30 días. `audit_logs` registra cada detección nueva con metadata estructurada para cross-reference desde `/audit-logs`. | **Medium** |
| `ENG-046` | CI minutes reduction — manual cross-OS desktop build | Shipped | Out-of-band: reducir minutos de Actions por push (el desktop matrix cross-OS gastaba ~12-15 minute-equivalents, macOS factura 10x). **Shipped**: el job `desktop` en `ci.yml` queda ubuntu-only corriendo `ci:desktop` en cada push; nuevo `build-desktop.yml` `workflow_dispatch`-only con inputs `linux`/`mac`/`win`/`validate_only` para construir on-demand (artifacts unsigned); `release.yml` intacto. | Push a main dispara solo jobs ubuntu (web + backend + desktop validation + e2e + release-automation); cero minutos macOS/Windows facturados por push. Operador puede correr el matrix completo a demanda desde la UI de GitHub Actions seleccionando solo los OS que necesita validar. | **Medium** |
| `ENG-045` | Design system audit + tabbed Company page | Shipped | Out-of-band: audit a Claude Design handoff (reverse-engineered from the repo, near-bit-identical) and apply targeted polish rather than a redesign. **Shipped**: `/company` refactored from 8 stacked cards to a five-tab URL-stateful segmented control using the existing canonical recipe; AnomalyDetectionCard CTA deep-links `?tab=ai`; 6 render tests. A cross-surface audit confirmed every kit diff is post-handoff feature work the kit predates, so no other surface was changed. | Operator with admin role lands on `/company` and sees the segmented control with five tabs. Click "IA" or open `/company?tab=ai` shows the AI settings card directly. AnomalyDetectionCard's CTA opens the AI panel without manual navigation. Cashier role redirected to `/sales` as before. | **Medium** |
| `ENG-071` | Authority Node ADR + runtime config contract | Shipped | Lock the Authority Node runtime-mode architecture before code: accept ADR-0008, keep `device_local` default, define the three-mode `AuthorityMode` + seven runtime config fields, reject shared SQLite over NAS, and document the central-server boundary in `docs/AUTHORITY-NODE.md`. **Shipped**: docs-only closeout — ADR-0008 accepted + indexed, `AUTHORITY-NODE.md` operator reference, contract implemented in `config/runtime.ts`; downstream ENG-072/073/074/074b consumed it before the Status flip. | ROADMAP, SPRINT, ADR index and `docs/AUTHORITY-NODE.md` agree on the modes; no runtime behavior changes; existing installs remain `device_local`. | **High** |
| `ENG-072` | Device-local default hardening | Shipped | Make the current runtime explicit in code without changing behavior: a shared config resolver for Electron + standalone, missing config → `device_local`, loopback bind preserved, and authority mode in diagnostics + banner. **Shipped**: `config/runtime.ts::resolveRuntimeConfig` (defaults `device_local + 127.0.0.1 + 8090`, `PUNTOVIVO_*` env), a `createServer` runtime singleton + structured log + banner line, an additive `runtime` projection on diagnostics, and 28+ tests + live smoke; LAN bind (ENG-073) and hub URL switch (ENG-074) deferred. | Fresh desktop install with no config boots exactly like today and reports `authorityMode='device_local'`; all existing desktop/server checks remain green. | **High** |
| `ENG-073` | Store Hub server mode | Shipped | Productize one local site hub that owns the site SQLite file and accepts LAN clients: `site_hub` binds Fastify to LAN, refuses LAN bind without a strong JWT secret + allowed origins, CORS accepts configured origins, and health reports the node identity. **Shipped**: a fail-fast LAN guard (32+/8-unique/non-placeholder secret + non-empty allow-list), an exact-match CORS merge for `site_hub` only, `lib/runtimeMetadata.ts` (db fingerprint / schema version / active-device count), an extended `/api/health` identity block (legacy fields intact), `appVersion` threading, and 17 tests + live smoke. | A hub machine can run the same server against one site DB and expose tRPC on LAN without allowing arbitrary origins or shared DB-file access. | **High** |
| `ENG-074` | Hub Client mode | Shipped | Let cashier terminals point at a Store Hub instead of their embedded backend: `hub_client` sets the tRPC base URL to `hubUrl`, registers the terminal as a hub client, runs the sale flow against the hub, executes attached peripherals via a client-local bridge, and gates checkout on hub reachability (no local write fallback in v1). **Shipped**: Electron exposes runtime config via sync IPC; `runtimeConfigClient.ts` + `useHubReachability.ts` switch `API_URL` and poll `/api/health`; `OfflineStatusBanner` + checkout surfaces gate on `hubReachable`; `devices.kind` gains `hub_client`; hardware bridge in ENG-074b. SameSite refresh fix deferred. | A second terminal can complete a sale through the hub; the hub DB records the sale, cash session, journal and outbox, with the terminal `deviceId` preserved for audit, while terminal-local hardware execution never writes operational tables. | **High** |
| `ENG-074b` | Hub-client local hardware bridge | Shipped | Carved from ENG-074: a hub_client terminal dispatches ESC/POS receipt prints and drawer kicks locally (hub returns the payload, terminal Electron main writes the device) and per ADR-0008 rule 6 never writes operational tables. **Shipped**: read-only `peripherals.buildReceiptBytes`/`buildDrawerKickBytes` (zero outbox writes, cross-tenant guarded), a DB-free `local-bridge.ts` + IPC channel (architectural lint pins no DB imports), a renderer fork routing by authority mode (missing IPC → `BRIDGE_UNAVAILABLE` fallback), 25 tests + live smoke, plus a tenant-name header fix. | A `hub_client` terminal completes a sale on the hub AND prints the receipt on its terminal-local printer; the hub DB records the sale + outbox row, the terminal main process executes the print without writing any operational table. | **High** |
| `ENG-075` | Device pairing + Authority health | Shipped | Make Store Hub deployments operable: short-lived pairing codes, a device registry tracking role/last-seen/versions/health/site, an Operations Center Authority tab, hub-client revoke, and authority topology in diagnostics. **Shipped**: migration `0021` extends `devices` + adds one-time hashed `device_pairing_codes`; an `authority` router (status / pairing create-consume / revoke with `device.revoke` audit) backs `/operations?tab=authority`; `registerDevice` reports node metadata, diagnostics include `authorityTopology`, and pairing claims are status-guarded against reassignment. | Admin can pair/revoke a terminal and see which devices are connected to a hub, which are stale, and which authority mode the installation is using. | **Medium** |
| `ENG-076` | Hub-client satellite offline fallback spike | Deferred | Decide later whether hub clients should write locally when the hub is down. **Scope**: compare fail-closed hub clients, local draft-only mode and full offline satellite writes; quantify duplicate-sale, cash-session, fiscal and inventory conflict risk; no implementation until a pilot proves fail-closed is insufficient. | Decision doc exists with a go/no-go recommendation and implementation gates; no hub-client local operational writes ship before this is reopened. | **Medium** |
| `ENG-044` | OpenAI provider activation (chat) | Shipped | Out-of-band: turn the ENG-030 Strategy/Factory's second provider real when Anthropic billing stalled two live smokes. **Shipped**: `providers/openai.ts` rewritten to a real `AIProvider` on `@ai-sdk/openai` (default `gpt-4.1-mini`), a 4-model `PRICING_TABLE`, `cacheControlForSystemPrompt()` undefined (OpenAI auto-caches), embeddings deferred to ENG-033; `openai.test.ts` + the registry/router tests flipped (notImplemented assertions moved to ollama), `.env.example` updated. | Admin can pick `openai` in the Settings → AI card, set `gpt-4.1-mini` (or override), click Test connection with `OPENAI_API_KEY` exported and get a translated `pong` toast plus an `ai_audit_log` row with `provider_id='openai'`; the co-pilot answers a Spanish question end-to-end with the same provider. Without `OPENAI_API_KEY`, the configured-badge stays red and calls fail with the existing `AI_PROVIDER_ERROR` code; ollama remains rejected with the `ENG-040` hint when an admin tries to select it. | **Medium** |
| `ENG-077` | Co-pilot Anthropic prompt-cache repair | Shipped | Out-of-band: the co-pilot embedded the analytics window + `siteId` in the cached system prompt, so the cache key changed every call and the hit rate was ~zero. **Shipped**: `buildSystemPrompt()` is now parameter-less + byte-identical; per-call context moves to a `<context>` block prepended to the LAST user message via `buildContextBlock`/`injectContextIntoMessages` (server stays authoritative for the SQL window); 15-case `ai-copilot-cache.test.ts` + Anthropic round-trip smoke proving `cache_read_tokens > 0`. | Anthropic provider co-pilot calls show `cache_read_tokens > 0` on the second consecutive same-day call against the same window; OpenAI route unaffected; existing `ai.copilot.chat` role/budget/provider error gates stay on their current error codes. | **Medium** |
| `ENG-078` | AI category suggestion in the product form modal | Shipped | Out-of-band: wire the UI-orphaned `products.suggestCategory` into the product form with a confidence-tiered UX. **Shipped**: a debounced (800ms) suggestion gated on `semantic-search` module + manager+ role; high-confidence (≥0.7, create-mode, empty field) auto-preselects with a "Sugerido por IA" badge via event-driven `onSuccess` (React-Compiler-safe), mid-confidence shows an "Aplicar sugerencia" chip, manual pick always wins; 5 i18n keys, 14 tests, 10-screenshot smoke. | Manager+ opens the product create modal, types name + description, and the AI suggests a category — high-confidence auto-selects with a "Sugerido por IA" badge; medium-confidence shows a chip with an "Aplicar sugerencia" CTA; the operator's manual pick always wins; the module off / cashier flow is silent. | **Medium** |
| `ENG-079a` | Sidebar slim-down (Phase 1: low-risk compaction) | Shipped | Out-of-band, from operator UX feedback: the sidebar's ~1564px of chrome forced scrolling past ~940px viewports. **Shipped**: pure visual compaction (no behavior change) — `SidebarBrand` drops `hero-surface` for a flat icon+wordmark row, the duplicate SESIÓN INICIADA footer card is deleted (identity lives in the header menu), nav/item/section padding + gaps tighten, the orphaned `common:signedIn` key removed (parity green); ~390px recovered, 4-screenshot smoke; Phase 2 in ENG-079b. | Sidebar at `/dashboard` with admin role + all modules active shows substantially more nav items without scrolling; the SESIÓN INICIADA card is gone (the same identity info lives in the top-right user menu); the brand wordmark stays visible in expanded mode and falls back to the icon when collapsed; every existing nav item stays clickable with the same active-route highlight; module-gating and Dashboard anomaly badge behaviors unchanged. | **Medium** |
| `ENG-080b` | Design system alignment — Inter UI font, fg/brand-accent scales, P-mark path, punto/vivo wordmark | Shipped | Out-of-band: an audit found ENG-080 only partially adopted the handoff (kept Avenir, only one brand-accent stop, shopping-bag mark vs the official P-mark). **Shipped**: Inter as `--font-ui` (local 400/700 + Google 500/600/800), the full semantic type tokens + fg ramp + 3-stop brand-accent + dark shadow→border rule wired into `theme.css`/`@theme`; `BrandMark` rewritten to the official P-mark + counter dot, wordmark to "punto" 400 + "vivo" 700; chrome `font-display` migrated to Inter (preexisting usage left as documented debt); 1077 tests + 1920/1280px smoke. | Operator opens `/login` and sees the official P-mark lockup with the orange dot inside the counter; after login the header brand cluster shows the workspace context (kicker + tenant + site) on `>=2xl` and the sidebar carries the same lockup at every breakpoint; every existing screen still works because preexisting `font-display` use survives unchanged and only my chrome additions migrated to Inter. | **Medium** |
| `ENG-080` | Design system foundation — brand mark with punto naranja + tokens + header brand cluster | Shipped | Ground floor for the Claude Design handoff: brand-accent + motion + print tokens for downstream ENG-081..092. **Shipped**: `--brand-accent-500` + motion/print token block; `BrandMark.tsx` (gradient tile + orange-dot accent) + `BrandLogo`; a `glyph-tile` recipe; SidebarBrand / LoginPage / a `>=2xl` Header brand cluster adopt it; 2 i18n keys; `BrandMark.test.tsx` pins the punto-naranja invariant; live smoke; promotes ENG-081..092. | Operator opens `/login` and sees the Puntovivo mark with a visible orange dot inside the blue tile. After login the sidebar mark carries the same dot in both expanded and collapsed rail. On a `>=2xl` viewport the header shows a left-side brand cluster with PUNTO DE VENTA kicker + tenant name + site name; below that breakpoint the cluster hides but every existing header control (search, status pill, language pill, site selector, notifications, user menu) stays exactly where it was. | **Medium** |
| `ENG-081` | POS Ventas revamp (variants V1 + V4) | Shipped | **Shipped** in two slices. `SalesCheckoutPanel` dark total card gains a radial-gradient glow + `font-display tabular-nums` headline + mono metrics strip, and the shortcut card becomes a 4-tile `<kbd>` grid (F2/F4/F5/F12); `QuickDenominationSelector` V4 tiles stay; keybindings + cash gating + refund + split-bill untouched; live smoke at 1440/800px. | Cashier rings up a sale, gets the V4 dark total card with denomination tiles, sees Cambio computed live, and presses F12 to charge against the existing payment modal without regressions. | **Medium** |
| `ENG-082` | Apertura de caja V11 + extract reusable Overlay primitive | Shipped | **Shipped 2026-05-14** in commit `ac7b77c`. `CashSessionOpenModal.tsx` adds a "Primer día" headerAside badge (warning tone), upgrades the counted-total area to a 3-cell metric strip (Total contado · Base apertura · Diferencia, font-display tabular-nums) with a radial-gradient accent that flips from success (when balanced) to warning (when not), and replaces the plain count input with ± steppers per denomination (rounded-full pill buttons, hover warning/primary tones). The `Overlay` primitive was already extracted in a prior commit; this slice only consumes it. `requireActiveCashSession()` invariant + cash session lifecycle untouched. ci:web green. | Cashier opens the day, sees the V11 first-day activation overlay with the denomination breakdown, confirms the apertura and the existing cash-session row writes correctly. Overlay primitive is consumed at least twice (apertura + a smoke test). | **Medium** |
| `ENG-083` | Cierre de caja V6 (modo arqueo nocturno) | Shipped | **Shipped**: `CashSessionCloseModal` wraps in a dark night-mode arqueo surface with a "Cierre ciego" badge, a 3-cell metric strip, denomination tiles with ± steppers, and an optional `justification` textarea; the blind-close invariant + server invariants are preserved; per-method tiles deferred to ENG-083b. | Manager closes a cash session with mixed payment methods, sees the V6 reconciliation view, the existing overage / shortage / balanced server-side scenarios still hold, and the printed Z-summary keeps the same fields. | **Medium** |
| `ENG-084` | Devoluciones V8 (checkbox selección + motivo + 2×2 action grid) | Shipped | **Shipped**: `RefundConfirmOverlay` gains a 2×2 action grid (dinero · cambio · saldo a favor · vale) + an optional per-line checkbox panel wired from `SaleDetailsModal`; selection + action ride a `[<action>] <reason>` prefix into `returnSale` so the audit captures intent without a schema change; warning card + admin-approval lock preserved; new es/en keys, parity green. | Cashier opens a sale ≥7 days old, marks two lines with reasons, picks "Saldo a favor", and the existing returnSale procedure writes the same return ledger row. Admin-approval lock surfaces when refund > policy threshold. | **Medium** |
| `ENG-085` | Cotizaciones — aplicar layout V7 | Shipped | **Shipped**: `QuotationDetailsModal` gains a V7 customer card (kicker + name + NIT + contact) and a 3-cell Crédito/Cupo/Saldo strip with "—" placeholders tied to ENG-089's ledger; `getQuotationById` joins `customers.taxId/email/phone` and `QuotationDetail` carries the three nullable fields; vigencia/line-items/totals preserved, convert-to-sale untouched; qty steppers + tier badge deferred to ENG-085b. | Manager creates a cotización with the same workflow as today but sees the V7 layout. Convert-to-sale flow unchanged. ci:web green. | **Medium** |
| `ENG-086` | Print thermal renderer + receipt template default | Shipped | Update `receipt-renderer.ts` + the default receipt layout to the handoff thermal preview: 58/80mm presets on the ENG-080 print tokens, mono tabular nums, QR finder patterns, wordmark header, hard 1-bit rendering; editor preview honors the tokens; tenant overrides keep working. **Shipped**: `services/print-tokens.ts` constant (drift trips at review), two new Zod block types (`wordmark`, `metaTable`), an upgraded QR placeholder, rewritten `sale`/`fiscal_dee` defaults + editor controls, new i18n keys, 15 tests; collateral fixes to `.style-muted` opacity and a missing `serviceCharge` totals row; real QR PNG + USB driver → ENG-097. | A test sale prints on a 58 mm printer with the new layout and on a `letter` paper preview, the receipt-template editor preview renders the same HTML, and existing per-tenant template overrides still produce a valid render. | **High** |
| `ENG-087` | Touch POS V1 (grid catálogo) + Restaurant tables V2 (floor map) | Shipped | Two module-gated re-skins: (a) Touch POS V1 grid catalog (`features/pos-touch`) with category tabs, a responsive product-tile grid, and a cart sidebar with a loyalty slot; (b) `RestaurantTablesPage` V2 floor map with mesa status dots + a selected-mesa detail panel. **Shipped** in two slices: Restaurants V2 floor map (`5126381`) + Touch POS V1 (`PosTouchScreen` + grid + cart, responsive down to mobile). Cobrar fires `useCriticalMutation('sales.create')` so the command envelope attaches automatically; `/touch/voice` keeps the ENG-039a voice flow reachable. Loyalty math, surface picker, design fidelity, and grid virtualization deferred to ENG-087b/c/d/e. | Operator with the `pos-touch` module on can navigate to `/touch` and ring up a sale; admin with the same module can land on `/restaurants/tables` and see the floor-map view with mesa state. Sale completion + table CRUD unchanged. | **High** |
| `ENG-088` | Offline mode V12 — capability grid + sync queue UI | Shipped | Surface the V12 Modo offline capability grid (6 feature cards with status pills) plus a pending sync-queue list, sourced from `useOfflineSync` / `useHubReachability`. **Shipped** in two slices: the static `OfflineCapabilityGrid` (`e97cdc8`) then `OfflineModePanel` + `OfflineSyncQueueList` (reads `sync.listQueue`, status derived purely from `attempts`, pluralized elapsed time, and empty/error/retry states). Live device-capability probes (ENG-088b) + a dev QueryClient debug handle (ENG-088c) deferred to BACKLOG. | A cashier without internet sees the V12 capability grid, can complete a sale, the ticket appears in the sync queue, reconnecting drains the queue. The existing hub_client invariants stay green. | **High** |
| `ENG-089` | Customer ledger V5 — net-new (schema + tRPC + UI estado de cuenta) | Shipped | New customer-receivables feature: a `customer_ledger_entries` table (signed delta, balance = `SUM(amount)`, never denormalised), a `customerLedger.*` router (manager+ read/payment, admin-only adjustment, all `ctx.tenantId`-scoped), and a V5 Cuenta corriente panel with balance / cupo / proyección plus abono + adjustment modals. **Shipped** in two slices: schema + router (`e4a1294`, migration `0027`) then the UI + the `creditLimit` column (migration `0031`, `0 = sin cupo` sentinel). 33 tests; cross-tenant isolation pinned. Frecuente badge + PDF statement export deferred to ENG-089b. | Manager opens a customer, sees their ledger with running balance, records a payment via "Recibir abono", and the next sale to that customer reflects the updated balance. Cross-tenant isolation tested. | **High** |
| `ENG-090` | Credit sales / Apartado / Fiar V10 — net-new | Shipped | Depends on ENG-089. Add a "Cargar a cuenta" payment method (only when the customer has a ledger) that writes a `kind='sale'` ledger row, leaves the cash session untouched, and gates on a `requireCreditLimitNotExceeded()` invariant with admin override. **Shipped**: `services/credit-limit.ts` projects the post-sale balance and throws `CREDIT_LIMIT_EXCEEDED` (sin-cupo short-circuit); admin-gated `creditOverride` on `sales.create`/`completeDraft`; a role-gated V10 credit card; `recordCreditSaleLedger` on both branches; layaway in ENG-014, receipt/approval queue in ENG-090b. | A returning customer with a positive balance reaches the limit; the operator can either request admin override or split-pay the excess in efectivo. The receivable ledger from ENG-089 shows the new entry. | **High** |
| `ENG-091` | Domicilios touch V5 — net-new | Shipped | New delivery-queue module (default OFF per ENG-068): a `delivery_orders` table (status enum + per-status timestamps), a `deliveryOrders.*` router (manager+, tenant + site scoped), and a `/delivery` V5 master-detail board with status columns, courier assignment, and advance/cancel. **Shipped** in two slices: the server scaffold (`0c75ca1`, migration `0028`) then the UI + module registration (server + client manifests, `defaultEnabled: false`). `DeliveryPage` master-detail surfaces per-status count badges; advance/cancel invalidate the list. A statusCounts aggregate endpoint (ENG-091b) + a user-facing create-order modal (ENG-091c) deferred. | Admin enables the `delivery` module, manager creates a delivery order from an existing sale, courier moves it through the timeline, the order shows up in the right status column with correct elapsed time. | **High** |
| `ENG-092` | What's-new overlay (reuses primitive from ENG-082) | Shipped | Reuse the ENG-082 `Overlay` primitive for a per-release announcement system: a `whats_new_entries` table (tenant-scoped or product-wide), a `whatsNew.*` router, and an on-login Overlay for the most recent unread; never blocks core flow, per-user seen-state. **Shipped**: migration `0029` (entries + acks), `whatsNew.{listUnseen,markSeen,publish}` (publish admin-gated), fourth Overlay adopter. | After a new entry is published, the next user login renders the overlay; clicking "Lo vi" persists seen-state and the overlay does not reappear for that user. Operator can also reach archive via Settings → Novedades. | **Medium** |
| `ENG-093` | Header v2 per 2026-05-15 handoff (page-kicker + display title + tenant badge, BrandMark moves to sidebar/login only) | Shipped | Out-of-band follow-up to ENG-080/ENG-080b after the operator flagged the post-ENG-080 header "no se parece en nada" to the new bundle. The 2026-05-15 handoff (`/tmp/puntovivo-design-system-2026-05-15/apps/web/src/components/layout/Header.tsx`) drops the brand cluster from the header entirely (lives in Sidebar + LoginPage only) and replaces it with a 3-tier page heading: `page-kicker` + `<h2 className="font-display text-[clamp(1.8rem,2vw,2.3rem)]">` + tenant badge. Right-side cluster restructured: search input + status badge + Select(language) + Select(site) + bell + user menu, with the custom language dropdown replaced by the unified Select component. Adds `apps/web/src/components/layout/useHeaderTitle.ts` (pathname → kicker/title key mapping per route) and 27 `nav:header.<route>.{kicker,title}` keys in en/es. Site name surfaces under the title as a third-tier label for context. Acceptance: cashier opens `/sales`, sees "PUNTO DE VENTA" kicker + "Ventas" display title + tenant badge on the left; navigate to `/inventory` → kicker becomes "OPERACIONES", title "Inventario"; the unified Select preserves keyboard nav for language switching; ci:web green; locale-parity green. **Shipped 2026-05-16** in commit `4b41418` (bundled with the AI Núcleo wave). | **Medium** |
| `ENG-094` | AI Núcleo Sprint 1 — Invoice OCR end-to-end (ai.invoiceOcr + PurchaseDraft + CO normalizer + audit log + dialog) | Shipped | Per AI Núcleo handoff (2026-05-15) §5. New `apps/web/src/features/ai-invoice-ocr/` module: `InvoiceOcrDialog` (idle → uploading → extracting → review → error stages), `ExtractedFieldsForm` (react-hook-form with confidence chips, provider selector, product resolution and totals reconciliation warning blocking confirm when |total − linesSum| > 100), `InvoiceOcrPreview` (image/PDF preview + scan band), `types.ts` (PurchaseDraft contract). Server: new `upload.uploadInvoice` accepts PNG/JPG/PDF up to 10 MB into local DB-backed invoice uploads; `ai.invoiceOcr.extract({ uploadId })` uses AWS Textract as the default/wired provider, layers `normalizeColombianInvoice` (NIT regex `\d{9,10}-?\d` with dots/spaces stripped, IVA rate detection across 0/5/19 within 1% tolerance, invoice number canonicalization), runs `matchInvoiceLinesToProducts` first by exact SKU and then best-effort embeddings, and emits audit rows with provider/cost/latency metadata. `ai.invoiceOcr.confirm({ draft })` creates a real `purchase` in `draft` status via the purchases router helper, blocks unmatched lines/provider/totals drift, and logs `ai.invoice_ocr.confirm`. The purchases page button is gated by `useAiFeatureFlag('invoiceOcr')`. New i18n namespaces `invoiceOcr` + `aiShared` (en/es); microcopy "La IA leyó la factura. Revisa cada campo antes de registrar la compra." pinned verbatim per handoff §5.4. CO normalizer unit-tested in `ai-invoice-normalize-co.test.ts` with five fixture-like invoice cases. **Shipped 2026-05-16** in commit `4b41418` (bundled with ENG-095, ENG-095b, ENG-093, and the ENG-081 V1 dark total card). | **High** |
| `ENG-095` | AI Núcleo Sprints 2-4 plumbing — per-feature flags + audit-log wiring + AiConfigPage | Shipped | Per AI Núcleo handoff §1.4 — every AI capability now gates on `tenants.settings.ai.features.<key>.enabled`. Server: extended `AISettings` with `AIFeatureFlags` type (copilot/anomalies/semanticSearch/invoiceOcr/privacy), updated `resolveAISettings` to merge defaults with `mergeFeatureFlags()`, taught `writeAISettings` to accept partial feature patches via `WriteAISettingsPatch`. Extended `auditLogActionEnum` with `ai.invoice_ocr.{extract,confirm}`, `ai.copilot.query`, `ai.anomaly.silenced`, `ai.semantic_search.regenerate_embeddings`; added `ai_feature` resource type. Wired feature gates into copilot/anomalies and audit rows into anomaly silence. Web: new `apps/web/src/features/ai-shared/` module exports `useAiFeatureFlag(key)` + `AiDisabledState`; new `/settings/ai` route with 4-switch grid, OCR provider selector (textract / docai / azure) and recent AI audit table. Sidebar entry "Configuración IA" is admin-only. Sprint-2 Co-pilot SQL-visible refactor, Sprint-3 Anomalies severity Alta/Media re-label, Sprint-4 Semantic Search ProductsPage toggle remain pending and tracked under **ENG-095b**. **Shipped 2026-05-16** in commit `4b41418` (bundled with ENG-094, ENG-095b, ENG-093, and the ENG-081 V1 dark total card). | **High** |
| `ENG-095b` | AI Núcleo Sprints 2-4 visual reskin (Copilot 2-col + SQL always visible + cost footer; Anomalies severity Alta/Media + AnomaliesDialog; Semantic toggle + score bars) | Shipped | Visual side of AI Núcleo handoff §2-§4. **Shipped 2026-05-16** in commit `4b41418` (bundled with ENG-094, ENG-095, ENG-093, and the ENG-081 V1 dark total card). Copilot: page header with kicker + font-display + subtitle radial-glow card; SQL block is now an always-visible card (no `<details>` collapse — handoff §8 anti-pattern fix); 4-cell result footer grid (provider · cost · rows · truncated) plus the verbatim `aiShared:disclaimer.copilot` line beneath every result. Anomalies: card gains a glyph-tile-warning + radial accent, font-display title, uppercase Alta/Media pills (tracking 0.12em) and the `aiShared:disclaimer.anomaly` line; new `features/ai-anomalies/index.ts` barrel re-exports the existing card + modal as `AnomaliesCard`/`AnomaliesDialog` so the handoff-canonical import path works without file moves. Semantic search: the similarity column now renders a tone-graded horizontal progress bar (success≥80, primary≥60, warning otherwise) with `semanticSearch:score.tooltip` on hover; existing toggle pill + dedicated input are untouched. Contracts unchanged. ci:web + ci:server + locale-parity green. | **Medium** |
| `ENG-096` | USB barcode scanner driver (HID keyboard-wedge + EAN-13 checksum) | Pending | Website promises "Escaneo, búsqueda por SKU o código de barras" but the product only supports software search; physical scanner integration is missing. Add USB HID keyboard-wedge support via Electron's native input layer + EAN-13 checksum validation + ScanLine icon in SalesPage. Hardware-test against at least 2 budget scanner models (Honeywell Voyager, generic 1D). Acceptance: cashier plugs in a USB scanner, scans a barcode, the product row populates in the sale cart without keyboard focus tricks. | **High** |
| `ENG-097` | ESC/POS thermal printer + RJ11 cash drawer | Shipped | Most of the ESC/POS stack shipped in ENG-060/061/062; this closes the two operator gaps: auto-print on sale completion and real QR generation. **Shipped**: an opt-in per-(printer, escpos) `autoPrintOnComplete` flag fires the dispatcher after the success toast (failures fall through to system print); `services/qr-encoder.ts` (`encodeQrSvg` + `encodeQrEscposBytes` via Epson `GS ( k`). USB/serial transports stay `DRIVER_NOT_IMPLEMENTED` (ENG-097b); hardware AC gated on the lab. | A test sale against a (printer, escpos) peripheral with `autoPrintOnComplete: true` auto-dispatches `peripherals.printReceipt` after completion; the receipt QR block emits real `GS ( k` bytes (byte-sequence assertion) and the editor preview renders the matching inline SVG; toggling the flag off silences auto-print without breaking manual reprint. **Hardware AC**: the "Imprimir + Abrir caja" gesture prints on a real Epson TM-T20 and pulses the drawer (gated on physical hardware). | **High** |
| `ENG-098` | KDS comanda (Kitchen Display System) | Shipped | Website promises "Modo restaurante con mesas y comanda". `features/restaurants/` ships the mesa catalog + floor map (ENG-087) but the kitchen display surface — orders queued per station, prep timer, "Listo" gesture — is missing. New `kds_orders` table, new tRPC `kds.*` router with SSE pushes from sale-completion hooks, new `/kds` route (gated `kds` module, default OFF). Acceptance: a sale that touches restaurant mode pushes a kitchen card to `/kds` with the items grouped by station; the cook hits "Listo" and the card greys out. **Shipped 2026-05-19**: new migration `0032_kds_orders.sql` with `UNIQUE(tenant_id, sale_id, station)` for enqueue idempotency + compound index on `(tenant_id, site_id, status)` for the board read. Three best-effort post-tx helpers under `services/kds/` (`enqueueKdsOrder`, `refreshKdsOrderItems`, `removeKdsOrders`) absorb the full restaurant lifecycle: `sales.suspend` (with tableId) and `completeSale` enqueue; `sales.changeTable` + `sales.splitDraft` refresh the snapshot; `sales.discardDraft` + `voidSale` remove. Each helper short-circuits when the `kds` module is off, when the sale has no tableId, or when no items exist — so retail tenants pay zero cost. Items are frozen into `kds_orders.items_json` on enqueue because draft line items are already immutable in the codebase (no `sales.addLineItem` procedure exists). New tRPC `kds.{list, markReady, recall}` namespace (cashier + manager + admin role floor, gated by `tenantProcedureWithModule('kds')`); `list` evicts ready cards older than 5 minutes and re-hydrates `tableLabel` live from `restaurant_tables.name`. `markReady` writes a `kds.order.ready` audit row and broadcasts via `app.sse.broadcast(...)`; `recall` (the cook misclick recovery affordance) flips ready → pending and writes `kds.order.recalled`. Two new error codes (`KDS_ORDER_NOT_FOUND`, `KDS_ORDER_NOT_READY`) with EN + ES neutral-LATAM translations. Web side: new `useRealtimeChannel` hook wraps `EventSource` with visibility-change reconnect + exponential backoff (sandbox-safe, no Node imports); new `features/kds/` folder with `KdsBoard` (orchestrates `kds.list` query + realtime subscription + optimistic mutations), `KdsStationColumn` (CSS-grid per station; v1 single "Cocina" column), `KdsOrderCard` (pure presentational with elapsed-time tick + amber-after-10-min cue + Listo button + ghost recall link), and `KdsEmptyState` (quiet "Sin órdenes pendientes" surface, no marketing copy). `KdsHomePlaceholder` body replaced with `<KdsBoard />`; the ENG-069 fullscreen black chrome stays as the route shell. New `kds.json` i18n namespace EN + ES neutral LATAM `tú`. 22 new audit-emitting + lifecycle test cases in `__tests__/kds.test.ts` covering suspend → enqueue, idempotent re-suspend, no-tableId skip, module-off skip, changeTable label refresh, discardDraft removal, list TTL eviction, markReady audit + idempotency, recall transition, recall on pending → `KDS_ORDER_NOT_READY`, cross-tenant collapse to `KDS_ORDER_NOT_FOUND`. 5 web component tests over the board (empty, populated, Listo click, ready visual, recall flow, SSE-triggered invalidate) + 5 hook tests over the realtime subscription. Phase 2 single-station MVP: every order pools into `station='main'` (UI labels it "Cocina") because no product-level station tagging exists yet — schema + UI already support multiple stations. The coherent kitchen-service follow-up is now `ENG-117`: product station routing, prep-time thresholds, `served` state, mid-service item appends, waiter read-only view, and optional sound cue. | **High** |
| `ENG-099` | Voice payment terminal handoff (Bold/Wompi via VoiceCartCommandModal) | Pending | Website promises "Cobro por voz" but the voice surface (`VoiceCartCommandModal`) currently handles cart entry only — there is no payment-method dictation or terminal handoff. Extend the modal to accept "cobrar con tarjeta" / "cobrar con Bold" / "cobrar con Nequi" intents, fire the existing Bold/Wompi adapters from `payments` router, and confirm the charge via voice. Acceptance: a cashier dictates a payment, the existing payment terminal flow runs end-to-end, the audit log captures voice as the originating channel. | **Medium** |
| `ENG-100` | Offline messaging copy audit (clarify "100% Offline-first" overstatement) | Shipped | Replace the marketing "100% offline" overclaim with accurate offline-first-sync language backed by the `OfflineCapabilityGrid` matrix; no claim should outpace shipped behavior. **Shipped**: `OFFLINE_CAPABILITY_CATALOG` is the canonical source of the six tiles with a JSDoc governance contract; an audit test pins cardinality/id/status, requires each `available` tile to name its backing ENG ticket, and blocks absolute language from non-available copy; `WEBSITE-CAPABILITY-AUDIT.md` mirrors the array; grep confirms zero "100% Offline" in product code. | Every offline claim on the marketing site has a corresponding capability tile in the product that backs it up; no claim outpaces shipped behavior. | **Low** |
| `ENG-101` | Cloud OCR vendor expansion (DocAI / Azure + pricing docs) | Pending | ENG-094 wires Textract as the only production OCR provider. DocAI and Azure remain visible as future selectors but must reject until their credentials and SDK paths are implemented. Add `@google-cloud/documentai` + `@azure/ai-form-recognizer`, implement provider-specific extract pipelines, expose non-zero provider pricing in `auditLog.recordCall`, and document the env vars per provider in `docs/AI-PROVIDERS.md`. Acceptance: an admin can switch the active OCR provider from AiConfigPage and the next extract goes through the chosen vendor; cost + latency reflect the active provider. | **Medium** |
| `ENG-102` | Website claim alignment + AI usage quotas | Partial | Follow-up from [`WEBSITE-CAPABILITY-AUDIT.md`](./WEBSITE-CAPABILITY-AUDIT.md). Before publishing the public website, either remove unsupported claims or ship the backing product behavior. This specifically covers AI quotas/pricing (800 Co-pilot questions + 200 OCR invoices), "four AI features ON by default", "100% Offline-first", hardware wording, and roadmap-vs-shipped labels in `website/index.html` / `website/ai-section.jsx`. If quotas stay in copy, add tenant/site usage counters, monthly reset, blocking/overage policy, and pricing copy sourced from product settings. **Shipped slice 2026-05-19 (AI quotas engine)**: new `services/ai/quotas.ts` with `AI_QUOTAS = { copilot: 800, invoiceOcr: 200 }` constants matching the website draft, `countMonthlyAiCalls` helper reusing the `idx_ai_audit_log_tenant_site_created` composite index for cheap calendar-month counts, `requireAiQuotaAvailable` that throws `AI_QUOTA_EXCEEDED` with `{feature, used, limit, resetsAt}` details. Wired BEFORE the provider call in `ai.copilot.chat` and `ai.invoiceOcr.extract` so a blocked request never writes an audit row; errored upstream calls (provider 5xx, etc.) do NOT consume quota because `errorCode IS NULL` is in the count filter. `ai.settings.get` extended with `quotas: { copilot, invoiceOcr }` projection scoped to `ctx.siteId` so the admin panel reads one coherent payload. New section "Cuotas mensuales de IA" in `CompanyAISettingsCard.tsx` with a progress bar per feature that flips warning at ≥80% and danger at ≥100%, plus a reset-date footer. 7 server tests in `ai-quotas.test.ts` cover under/at quota for both features, errored rows excluded from count, cross-site isolation, cross-tenant isolation, and calendar boundary. 3 web tests pin the idle/warning/danger render and the hide-when-master-toggle-off case. New `AI_QUOTA_EXCEEDED` error code with en + es translations (neutral LATAM `tú`). Calendar-month reset is implicit (no background job): queries use `startOfMonth ≤ createdAt < startOfNextMonth`. The 800/200 limits are hardcoded constants by design — when the operator wants to tune them, they edit `services/ai/quotas.ts::AI_QUOTAS` and ship. A runtime-tunable panel is informal follow-up. **Remaining**: website-copy review when the public site lands. The HTML/JSX of the future public website lives outside this repo; the in-repo enforcement engine is shipped, the marketing copy that consumes it is external work. Voseo / hero-claim audit on `website/index.html` + `website/ai-section.jsx` waits until the operator imports the website into the monorepo. Counter Plan v2.0 NO avanza (out-of-band, sub-slice). | Every public claim maps to Supported / Beta / Gated in the audit doc; no published website text promises behavior that is not shipped or explicitly marked roadmap. | **Medium** |
| `ENG-103` | Audit-grade export and download contract | Shipped | Plan V3: centralize filename/extension/MIME/Blob-download behavior for every export and drop UUID-like statement names for `statement-<provider>-<range>.<ext>`. **Shipped**: `services/exports/envelope.ts` (`ServerExportEnvelope` + filename helpers); `reports.fiscal.getXml` closes the ENG-035b TODO (cross-tenant collapse + `fiscal.xml.downloaded` audit + per-country charset); diagnostics export gains `suggestedFilename`; web `downloadFile` + `MIME_BY_EXT` + `buildSemanticFilename` tagged union; `FiscalDocumentXmlModal` + consumers migrated off the inline anchor dance; 17 tests + i18n. | Every exported artifact has a semantic filename, real extension, correct MIME, and unit/component coverage for the download helper plus at least one live smoke on an affected route. | **High** |
| `ENG-104` | Guided store setup and readiness checklist | Shipped | Plan V3: a first-run + admin readiness flow across locale/fiscal/sites/peripherals/payments/modules/users/AI/catalog with role-based home and actionable empty states. **Shipped (engine + first surface)**: `setupReadiness.get` aggregates 10 sections (status enum + score formula) from existing tables; `companies.acknowledgeSetup`; a `CompanyReadinessCard` first tab + `ReadinessBanner` + `EmptyStateReadinessNudge`; post-login routing to `?tab=readiness` on blockers with a safe `/dashboard` fallback; 10 server + web tests. Wizard / task center / import / BI-home re-routed to other V3 tickets. | Admin sees blocking vs optional setup steps, readiness score persists per tenant, every CTA deep-links to the exact setup surface, completed steps survive reload, and users land on the right home for their role/setup state. | **High** |
| `ENG-105` | Sales Cockpit v2 cashier speed pass | Partial | Plan V3 operator-speed ticket. Refine `/sales` for keyboard-first and scanner-first use: global command palette, shortcut map, stable cart layout, rapid cash checkout, payment drawer, customer attach, checkout preflight, quick-create modals, fast-register mode, undo/recovery, and focus rules that do not fight barcode wedge input. **Shipped 2026-05-20 (slice A — global command palette + canonical shortcut map + aria-keyshortcuts hookup)**: new `apps/web/src/lib/shortcuts.ts` is the single source of truth for every keyboard binding in the renderer — declarative `SHORTCUTS` array with `id / keys / scope / labelKey / roles`, helpers `getShortcutById`, `formatKeysForDisplay` (⌘ / Ctrl branching by `navigator.platform`), `formatKeysForAria` (canonical CamelCase, with `Mod` resolving to `Meta` on macOS and `Control` elsewhere), `ariaKeyshortcutsFor`, and `matchesShortcut` (accepts Ctrl + Meta as `Mod` on non-mac so external mac keyboards work). New `apps/web/src/lib/commandPaletteActions.ts` declares 17 V1 actions (15 navigation + 2 commands) filtered against the active user's role and tenant module map using the same `adminOnlyRoles / managerOrAdminRoles / salesRoles / dashboardRoles` tuples and `RequireModule` ids App.tsx already gates routes with. New `apps/web/src/components/feedback/CommandPalette.tsx` mounts via the shared `Modal` (focus trap + ESC + return-focus-to-opener inherited) with ArrowDown / ArrowUp / Home / End / Enter keyboard navigation, case-insensitive substring filter against translated label + description, role/module-filtered listbox, and shortcut hints rendered on the right gutter when an action references a catalogue id. Items expose `role="option"` + `aria-selected` so screen readers announce the active row. New `apps/web/src/components/feedback/CommandPaletteProvider.tsx` registers a single authenticated-session `Mod+K` listener via `matchesShortcut(event, SHORTCUTS.find(s => s.id === 'palette.open'))`, does not open on the login screen, skips when another modal already owns focus, toggles the modal, and sets `document.body.dataset.commandPaletteOpen` to short-circuit a second open while the palette is up. Mounted inside `ModulesProvider` + `LocaleProvider` so it can read module gates and tenant language. New i18n namespaces `shortcuts.json` + `palette.json` in en + es neutral LATAM (`tú`, no voseo). `SalesCheckoutPanel.tsx` gets `aria-keyshortcuts` on Charge (`F1`), Suspend (`Meta+P` on macOS / `Control+P` elsewhere), and Toggle Suspended (`Meta+R` on macOS / `Control+R` elsewhere) buttons, and the visible shortcut chips now render from the same canonical catalogue — closes the Remaining slice of ENG-134 that was waiting on this map. New `docs/SHORTCUTS.md` documents the catalogue, the palette UX, the `aria-keyshortcuts` contract, browser-shortcut conflicts, and the recipe for adding new shortcuts / actions. 35 unit tests across `shortcuts.test.ts` (matcher, formatters, catalogue surface), `CommandPalette.test.tsx` (render, filter, keyboard nav, role + module filtering, click and Enter activation, empty state), `CommandPaletteProvider.test.tsx` (Mod+K open / close, no-login/no-modal stacking, body dataset flag, useCommandPalette outside-provider throw), and `SalesCheckoutPanel.hubGate.test.tsx` (visible shortcut chips + primary-action ARIA). **Shipped slice 2026-06-11 (ENG-105g — palette most-used / recent ordering)**: the palette records every action activation DEVICE-LOCALLY (new `lib/paletteUsage.ts`, localStorage key `palette_usage:<tenantId>` mirroring the siteStorage pattern; data never travels to the server, so no consent surface) and, when opened with an empty query, surfaces a Recent section — top 5 used actions by count desc with recency tiebreak — above the stable catalogue without duplicating entries below; an active query keeps the plain filter behaviour. The ranking runs AFTER the role/module gate (an admin's usage on a shared device never leaks into a cashier's section); section headers are presentational (aria-hidden) so the listbox stays a flat option list for AT and the ENG-105d wrap-around operates over the combined list; with no usage recorded the palette renders the exact pre-105g catalogue order (pinned by test, so the 17 pre-existing palette tests passed untouched). Defensive store: SSR guards, corrupt-JSON fallback, per-entry validation, best-effort writes, LRU eviction past 64 tracked ids. New i18n key `palette.groups.recent` (en Recent / es Recientes). 11 store + 6 component new tests. The original "(depends on ENG-135 observability)" note resolved as LOCAL-first by design — a cashier's ranking is about their device, not fleet aggregation. **Remaining**: payment drawer redesign and stable layout polish (both await operator visual direction). ENG-105b (checkout preflight panel) + ENG-105c (quick-create modals) + ENG-105c2 (auto-attach del customer recién creado) + ENG-105d (undo/recovery + palette wrap-around) + ENG-105e (fast-cash F2) + ENG-105f (focus rules vs barcode wedge) closed inline — see the dedicated rows below for the full slice summaries. Each slice ships as `ENG-105b..` follow-ups on top of this scaffolding. Counter Plan v2.0 NO avanza (Plan V3 wave). | Cashier can search, add, quick-create missing data, attach customer, see blockers before checkout, take exact cash, print, undo reversible mistakes, and start the next sale without using the mouse; desktop and tablet smokes prove no incoherent layout shifts. | **High** |
| `ENG-106` | Staff PIN mode and manager approval queue | Pending | Plan V3 accountability ticket. Add fast cashier switching, baseline clock-in/out state, and manager approval requests for credit override, void, discount, drawer open, refund, and cashier credit-sale requests. Promotes the ENG-090b approval follow-up into the main work plan. | Sensitive actions carry requester and approver identity in audit rows, manager approval never exposes the manager session to the cashier, and receipts show credit/customer balance context when relevant. | **High** |
| `ENG-107` | Customer-facing display, kiosk/order status, and touch surface picker | Pending | Plan V3 customer-trust and surface-discovery ticket. Productize second-screen customer display and kiosk/read-only order status surfaces using the existing multi-surface shell, Electron second-window support where available, SSE for browser-only displays, and a `/touch` surface picker for catalog / voice / tables / waiter view when modules are active. | Display shows cart lines, totals, payment state, branding, and optional customer name without exposing admin data; Electron and web-only modes both have live smoke proof; touch operators can switch surfaces without memorizing URLs. | **Medium** |
| `ENG-108` | Loyalty, gift cards, and customer wallet | Pending | Plan V3 retention ticket. Add loyalty points, rewards, customer wallet/store credit, gift-card issuance and redemption, liability tracking, and balance history tied to sales and refunds. | Sales can earn and redeem points or wallet balance; gift-card liability is auditable; refunds reverse earned/redeemed value consistently. | **High** |
| `ENG-109` | Promotions and pricing rules engine | Pending | Plan V3 revenue ticket. Add deterministic promotion rules for customer groups, time windows, bundles, mix-and-match offers, happy hour, coupon codes, and priority/conflict resolution. | Checkout applies eligible promotions with an explanation per rule, snapshots applied discounts on sale/receipt/audit, and rejects ambiguous rule conflicts predictably. | **High** |
| `ENG-110` | Advanced product and inventory model | Pending | Plan V3 catalog-depth ticket. Add variants/matrix products, lots, expiry, serial numbers, composite/BOM products, product station routing for KDS, and catalog import mapping/validation without forcing every tenant into every mode. | Products opt into variant, lot, serial, expiry, composite, or station behavior independently; stock movements remain tenant/site scoped; KDS can group by station; catalog import previews and rejects bad rows before persistence. | **High** |
| `ENG-111` | Replenishment, stock counts, and purchase planning | Pending | Plan V3 inventory-control ticket. Add cycle counts, variance resolution, reorder points, supplier lead times, suggested purchase orders, and low-stock forecasting based on recent sales velocity. | Admin can run a stock count, resolve variances with audit rows, and generate a draft purchase order from reorder rules and velocity. | **High** |
| `ENG-112` | WhatsApp receipts, quotation PDF/link sharing, and post-sale messaging | Pending | Plan V3 LatAm-channel ticket. Add a WhatsApp provider abstraction for digital receipts, quotation PDF/link delivery, payment reminders, and opt-in post-sale messaging with localized templates. | A sale receipt or quotation PDF/link can be sent by WhatsApp, consent/message status is tracked, provider failures are visible, and templates ship in en/es neutral LATAM Spanish. | **High** |
| `ENG-113` | Omnichannel commerce bridge | Pending | Plan V3 ecommerce ticket. Build connector foundations for Shopify, Tiendanube, WooCommerce, MercadoLibre, and VTEX around catalog sync, stock sync, order ingestion, and fiscal document emission from ecommerce-originated sales. | At least one connector syncs catalog and stock, ingests an order into Puntovivo, and preserves idempotency/audit/fiscal boundaries. | **High** |
| `ENG-114` | Delivery and logistics execution | Pending | Plan V3 fulfillment ticket. Add delivery notes, pick lists, packing slips, route assignment, proof of delivery, and exception handling for restaurants, pharmacies, hardware stores, and wholesalers. | A sale can become a delivery task, be picked/packed, assigned, completed with proof of delivery, or failed with reason and audit trail. | **Medium** |
| `ENG-115` | Accounting exports and reconciliation packs | Pending | Plan V3 accounting ticket. Add export/push adapters for Alegra, Siigo, World Office, and generic CSV journals covering sales, purchases, taxes, payment rails, cash sessions, and credit/wallet movements. | Admin can export or push a period to an accounting target with idempotency, retry diagnostics, mapping errors, and no duplicate ledger entries. | **High** |
| `ENG-116` | Owner BI control tower and scheduled reports | Pending | Plan V3 management ticket. Build drill-down owner analytics for gross margin, sell-through, inventory valuation, cashier performance, payment reconciliation, anomalies, and scheduled Monday PDF/email summaries. | Dashboard filters by site, cashier, category, rail, and date; scheduled reports generate PDF/email output and cite data freshness. | **High** |
| `ENG-117` | Restaurant KDS and service lifecycle v2 | Pending | Plan V3 restaurant-depth ticket. Promote the ENG-098 follow-ups into one coherent kitchen/service slice: product station routing, per-station prep thresholds, served state, waiter read-only view, append-items-mid-service, and optional sound cues. | Items route by product station, cooks and waiters advance distinct states, appended items create delta KDS cards, and sound is tenant-toggleable off by default. | **High** |
| `ENG-118` | Public API, webhook delivery worker, and integrator kit | Pending | **Depends on `ENG-165`** (tRPC-aware rate limiter must ship first so a partner integration on a shared NAT cannot DOS one tenant). Plan V3 platform ticket. Finish the event API path with subscriber configuration UI, HTTP delivery worker, signing, retry/dead-letter UX, versioned docs, and a minimal sample integration. | Admin can register a webhook endpoint, receive signed events, inspect attempts, replay failures, and follow a stable API contract. | **Medium** |
| `ENG-119` | Services vertical: appointments, commissions, and customer asset history | Pending | Plan V3 vertical-depth ticket promoted from the staged 2026-05-19 design draft. Add scheduling calendar, appointment lifecycle, line-level employee assignment, commission rules, repair-garage asset records, and salon preference history without distracting retail tenants. | Appointment flows can be scheduled, started, completed, and invoiced; cart lines can carry employee attribution; commissions appear in cash-session close; customer asset records stay tenant/site scoped. | **Medium** |
| `ENG-120` | Pharmacy vertical: compliance metadata, generics, and controlled-sale capture | Pending | Plan V3 vertical-depth ticket. Add INVIMA/regulatory metadata, active ingredient search, generic alternatives, expiry/lot warnings, and prescription capture for controlled products on top of the shared product/inventory model. | Search surfaces active ingredient and generic alternatives; controlled products require prescription metadata before checkout; expiry/lot risk is visible before sale completion. | **High** |
| `ENG-121` | Supermarket vertical: scales, PLU barcodes, and category tax rules | Pending | Plan V3 vertical-depth ticket. Add USB/RS232 weight-scale integration, PLU barcode parsing for weighed products, produce workflows, and configurable healthy-food or category-tax rules. | Scanning a PLU or reading a scale creates the correct weighted cart line; tax breakdowns preserve category-specific rules on receipt, fiscal payload, and audit. | **High** |
| `ENG-122` | Hardware-store vertical: fractional units, conversions, project kits, and dense search | Pending | Plan V3 vertical-depth ticket. Add fractional sale units, unit-conversion matrices, contractor credit context, internal barcode generation, project-kit explosion, and SQLite FTS5 search for dense 50k-SKU catalogs. | Cashier can sell by meter, box, roll, or kit; kit lines explode into editable components; generated barcodes are auditable; dense catalog search meets the implementation latency target on low-spec hardware. | **High** |
| `ENG-123` | Launch migration and data-quality workbench | Pending | Plan V3 launch-readiness ticket. Import products, stock, customers, suppliers, prices, credit balances, gift-card balances, opening cash, and fiscal profile data from CSV/Excel with preview, dedupe, row-level validation, and launch reporting. | Admin can run a dry-run import, fix row-level errors, commit a launch batch, see rollback guidance, and export a launch report with accepted/rejected counts. | **High** |
| `ENG-124` | Payment terminal, QR, wallet, and settlement v2 | Pending | Plan V3 money-trust ticket. Productize payment intents, Wompi/PSE/Nequi/Bancolombia QR flows, semi-integrated terminal adapters where possible, statement imports, fees, withholding, GMF, gross/net settlement, and unmatched-payment workflows. | A card/QR/wallet payment can be initiated or recorded with provider reference; imported statements match to tenders; Operations shows unmatched, fee, tax, and settlement variance. | **High** |
| `ENG-125` | Procurement, receiving, supplier invoice, and landed-cost control | Pending | Plan V3 inventory-truth ticket. Add purchase order lifecycle, receiving against PO, supplier invoice matching/OCR handoff, purchase returns, cost adjustments, and landed-cost allocation. | Staff can receive goods against a PO, match supplier invoice lines, update inventory/cost, handle returns, and surface margin impact in BI. | **High** |
| `ENG-126` | Chain HQ and multi-location governance | Pending | Plan V3 scale ticket. Add central catalog with local overrides, price books, site groups, role scopes, transfer approvals, consolidated reporting, and remote policy rollout. | HQ can publish catalog/pricing policy, sites can override allowed fields, transfers require approval where configured, and BI consolidates site performance. | **Medium** |
| `ENG-127` | Customer CRM, consent, segments, and campaigns | Pending | Plan V3 customer-growth ticket. Build customer timeline, consent records, segmentation, birthday/reactivation campaigns, NPS/feedback, coupon attribution, and unsubscribe handling on top of WhatsApp/loyalty. | Admin can segment customers, launch a compliant campaign, attribute redemptions, and prove consent/unsubscribe state per customer. | **Medium** |
| `ENG-128` | Supportability and remote operations readiness | Pending | Plan V3 supportability ticket. Add diagnostic bundles, telemetry opt-in, error tracking, version/update health, feature flags, remote support checklist, and evidence redaction. | Support can receive a redacted diagnostic bundle, see version/module/device health, correlate errors to operations, and guide recovery without raw DB access. | **High** |
| `ENG-129` | Security, privacy, and data-retention pack | Pending | Plan V3 trust/compliance ticket. Add role templates, permission audit, Habeas Data-style export/delete flows, retention policy, backup encryption/key handling, PII minimization, and audit-review screens. | Admin can review permissions, export/delete eligible customer data, configure retention, verify backups are protected, and inspect sensitive audit events. | **High** |
| `ENG-130` | AI automation suite | Pending | Plan V3 automation ticket. Promote AI from isolated features into audited operational assistance: demand forecasting, replenishment suggestions, anomaly triage, support/setup assistant, receipt/invoice explanation, and task suggestions with budget controls. | AI suggestions cite source data, can be accepted/dismissed, write audit rows, respect tenant budgets, and degrade to deterministic flows when providers are unavailable. | **Medium** |
| `ENG-131` | Information architecture and navigation refactor | Partial | Plan V3 UX-simplification ticket. Replace route-list navigation with role workspaces for Sell, Operate, Catalog, Inventory, Procurement, Customers, Finance, and Setup; add a Surface Switcher for touch/KDS/customer display/mobile waiter/restaurant routes; preserve direct URLs through deep links or redirects. **Shipped 2026-05-20 (slice A — workspace data model + collapsible workspace sidebar)**: new `apps/web/src/components/layout/workspaces.ts` is the declarative source of truth for the eight workspaces from UI-REFRACTOR-V3 §3 (Sell, Operate, Catalog, Inventory, Procurement, Customers, Finance, Setup) plus the top-level Dashboard entry. Each workspace declares its label-key, icon, allowedRoles, defaultRoute, and an ordered `items: WorkspaceItem[]` mapping every route the legacy sidebar already declared — no routes move, so every deep link continues to resolve through the existing `App.tsx` Router definitions. Helpers `visibleItemsForWorkspace(workspace, role, modules)` and `visibleWorkspacesForRole(role, modules)` mirror `RequireModule` parity so a workspace whose visible items collapse to zero is omitted entirely instead of rendering an empty header. `Sidebar.tsx` refactor replaces the four flat sections (`overview / flow / surfaces / setup`) with workspace-grouped sections; each workspace is a WAI-ARIA disclosure widget (`role="button"` + `aria-expanded` + `aria-controls`) — closes the disclosure-widget contract documented in `docs/A11Y.md`. Workspace collapse state persists per-workspace in `localStorage` under `puntovivo:sidebar:workspace:<id>:collapsed`; the workspace that contains the active route auto-expands on mount via the `useState` lazy initializer (no setState-in-useEffect cascade). Cashier role sees only the Sell workspace (the other seven gate to manager+ or admin-only and collapse to zero items for cashier); admin sees Dashboard + 8 workspaces. New i18n namespace `workspaces` in + es neutral LATAM (`tú` register, no voseo — sustantivos: Vender / Operar / Catálogo / Inventario / Compras / Clientes / Finanzas / Configuración). 11 cases in `workspaces.test.ts` (catalogue size, route coverage parity with the legacy sidebar, no duplicates, role + module filtering, empty-workspace omission, anonymous shortcut) + 5 cases in `Sidebar.test.tsx` (admin 8 workspaces + Dashboard, cashier only Sell, auto-expand on active route, toggle persists `aria-expanded` + localStorage, localStorage seed restores collapsed state on next mount). **Remaining**: redirects from legacy child routes to the workspace landing page when a workspace adopts a more aggressive default than its current first-item destination, mobile / tablet workspace nav redesign (the drawer renders the same Sidebar but the collapsed-by-default UX may surface drawer-specific affordances), Dashboard fold-into-Operate decision. ENG-131b (Surface Switcher via CommandPalette, 2026-05-23) and ENG-131c (workspace landing routes `/catalog` `/procurement` `/finance`, 2026-05-23) closed inline — see the dedicated rows below for the full slice summaries. Counter Plan v2.0 NO avanza (Plan V3 wave). | Admin first-load sidebar shows no more than eight workspace entries; cashier sees only sell-focused routes; moved routes remain reachable through workspace subnav, command palette, or direct URL; desktop/tablet/mobile navigation smokes pass. | **High** |
| `ENG-132` | Screen simplification and progressive disclosure pass | Shipped | Plan V3 UX-simplification ticket. Refactor Sales, Dashboard, Operations, Company/Setup, Products, Inventory, Orders/Purchases, Customers, Finance/Compliance, and surface launchers so each screen exposes one primary task first and moves history, diagnostics, exports, expert settings, and secondary actions into tabs, drawers, More menus, or dedicated workspaces. **Reconciled 2026-06-04**: the AC's high-traffic screens were delivered by the retail screen-focus wave — `/sales` no-scroll at 1440x900 + persistent mobile search/cart/payment (`ENG-186`), Operations attention-queue default (`ENG-187`), Setup grouped-readiness nav replacing the flat tab strip (`ENG-188`). The remaining work is the list-screen "smallest useful column set + secondary-into-drawer" pass, one screen per commit. **Shipped 2026-06-06**: every list screen trimmed to the smallest useful column set with secondary detail behind a row drawer/modal — Products (`ENG-132a`), Customers (`ENG-132b`), Inventory Stock (`ENG-132c`), Quotations (`ENG-132d`), Orders (`ENG-132e`), Purchases (`ENG-132f`), Inventory Movements/Entries (`ENG-132g`), Fiscal Documents (`ENG-132h`). Finance's other two routes needed no trim (`/fiscal-reports` is a coming-soon shell; `/audit-logs` was already at its minimal 5-column set). Counter Plan v2.0 NO avanza (Plan V3 wave). | `/sales` can complete a normal sale without desktop scroll at 1440x900 for common carts; mobile `/sales` keeps search/cart/payment persistent; Operations opens on an attention queue; Setup replaces the nine-tab flat strip with grouped readiness sections; key tables default to the smallest useful column set; EN/ES live smoke covers touched routes. | **High** |
| `ENG-132a` | Screen simplification — Products list progressive-disclosure pass | Shipped | First list-screen slice of `ENG-132`. **Shipped 2026-06-04** (web-only, no server change): trimmed the `/products` table from 11 columns to the smallest useful set — name(+SKU), category, tier-1 price, stock, status, actions (+ the semantic-mode similarity column unchanged). The four secondary columns (provider, location, tier-2 / tier-3 prices) move into a new read-only `ProductDetailsDrawer` (reuses the ENG-186 `Drawer` primitive + its focus-trap/ESC/labelled a11y contract), opened by an additive Details (eye) action in the row — available to every role and focusable in tab order. The Drawer footer carries an Edit action for manager/admin that hands off to the existing `ProductFormModal`. ENG-134f keyboard edit-on-activate (`onRowActivate`) is preserved unchanged; the CSV/XLSX export (`productExportColumns`) is separate from the display columns so it still carries every field. Added `products:details.*` (en/es, neutral LATAM); `ProductDetailsDrawer.test.tsx` (fields + edit callback + a11y) + `ProductsPage.details.test.tsx` (real-DataTable column-set assertion + drawer round-trip). `ci:web` green; live `/products` smoke proved the trimmed table + the details drawer + the edit hand-off, en/es. | `/products` table defaults to the smallest useful column set; the trimmed fields stay reachable in a row-detail drawer with an edit hand-off; export keeps every field; EN/ES strings complete + neutral LATAM; gates + a live `/products` smoke prove it. | **High** |
| `ENG-132b` | Screen simplification — Customers list progressive-disclosure pass | Shipped | Second list-screen slice of `ENG-132`. **Shipped 2026-06-04** (web-only, no server change): trimmed the `/customers` table from 7 columns to the smallest useful set — name(+identification), status, actions. The four secondary columns (email, phone, type, location) move into a new read-only `CustomerDetailsDrawer` (mirrors `ProductDetailsDrawer` + reuses the ENG-186 `Drawer`), opened by an additive Details (eye) action in the row (all roles, focusable in tab order); the drawer also shows the identification (type + tax id). The Drawer footer Edit action hands off to the existing `CustomerFormModal` — passed unconditionally because the customer edit affordance is ungated (every role can edit a customer), so the drawer mirrors the row. ENG-134f keyboard edit-on-activate (`onRowActivate`) + the ledger (manager+) / delete (admin) gating are unchanged. Added `customers:details.*` (en/es, neutral LATAM); the FIRST tests for the customers feature folder — `CustomerDetailsDrawer.test.tsx` (fields + edit callback + a11y) + `CustomersPage.test.tsx` (real-DataTable column-set + drawer round-trip), net coverage on a previously-untested 355-line page. `ci:web` green; live `/customers` smoke proved the trimmed table + the details drawer + the edit hand-off, en/es. | `/customers` table defaults to the smallest useful column set; the trimmed fields stay reachable in a row-detail drawer with an edit hand-off; EN/ES strings complete + neutral LATAM; gates + a live `/customers` smoke prove it. | **High** |
| `ENG-132c` | Screen simplification — Inventory Stock tab progressive-disclosure pass | Shipped | Third list-screen slice of `ENG-132`, scoped to the Inventory **Stock tab**. **Shipped 2026-06-04** (web-only, no server change): trimmed the Stock table from 8 columns to the smallest useful set — name(+sku/category), stock, status, actions. The four secondary columns (min stock, sell price, valuation, updated date) move into a new read-only `InventoryStockDetailsDrawer` (mirrors `ProductDetailsDrawer` + reuses the ENG-186 `Drawer`), opened by an additive Details (eye) action in the row (all roles, focusable). The drawer footer Adjust action hands off to the existing `InventoryAdjustmentModal`, gated `canManage` (mirrors the row). The Stock table has no `onRowActivate` so there is no ENG-134f concern; the stock export (`inventoryStockExportColumns`) is separate so it keeps every field. `onViewStockDetails` threads through `InventoryDataPanel` exactly like the existing `onAdjust`. Added `inventory:stock.details.*` (en/es, neutral LATAM) + `InventoryStockDetailsDrawer.test.tsx` (fields + adjust callback + a11y) + `InventoryStockColumns.test.ts` (pure-function column-set assertion on the exported `getStockColumns`). Collateral a11y: the Stock Adjust button gained an `aria-label` (was title-only). Scope = Stock tab only; Movements / Entries / Balances untouched. `ci:web` green; live `/inventory` Stock-tab smoke proved the trimmed table + the drawer + the adjust hand-off, en/es. | Stock table defaults to the smallest useful column set; the trimmed fields stay reachable in a row-detail drawer with an adjust hand-off; EN/ES strings complete + neutral LATAM; gates + a live smoke prove it. | **High** |
| `ENG-132d` | Screen simplification — Quotations list progressive-disclosure pass | Shipped | Fourth list-screen slice of `ENG-132`. **Shipped 2026-06-04** (web-only, no server change): trimmed the `/quotations` history table from 9 columns to the smallest useful set — number, customer, total, status, actions. Unlike Products/Customers/Stock (which had no detail view, so they got a new drawer), Quotations already has a View → `QuotationDetailsModal`, so the four trimmed columns (site, items, valid-until, created-at) lean on the EXISTING modal — no redundant drawer. The modal already showed valid-until, created-at and the full line-items table (covers "items"); the only missing field, site, was added to it (the `quotations.getById` payload already carries `siteName`, so web-only). The actions cell (View + status transitions + delete) is unchanged. Added `quotations:details.site` (en/es, neutral LATAM) + a column-set assertion in `QuotationsHistoryTable.test.tsx`. The quotation export keeps every field. `ci:web` green; live `/quotations` smoke proved the trimmed table + the View modal showing Site, en/es. | `/quotations` table defaults to the smallest useful column set; the trimmed fields stay reachable via the existing View modal (Site added); EN/ES strings complete + neutral LATAM; gates + a live smoke prove it. | **High** |
| `ENG-132e` | Screen simplification — Orders list progressive-disclosure pass | Shipped | Fifth list-screen slice of `ENG-132`. **Shipped 2026-06-05** (web-only, no server change): trimmed the `/orders` purchase-orders history table from 8 columns to the smallest useful set — order #, provider, status, total, actions. Like Quotations, Orders already has a View → `OrderDetailsModal` / `OrderDetailsContent`, so the three trimmed columns (date/created-at, site, receipts) lean on the EXISTING modal — no new drawer, no new i18n key, no server change (the cleanest 132 slice). The modal already shows created, site, the staged-delivery panel (receipt count + latest received #) and the full receipts list, so every trimmed field stays reachable; the Status badge (submitted / partial_received / received / voided) keeps receiving progress legible at a glance on the list. The actions cell (View + status-gated Receive) is unchanged; `onRowActivate` (ENG-134f) preserved. The now-orphaned `orders:table.{date,site,receipts,…}` keys stay in place per the established 132 convention. Orders has no table export, so nothing to preserve. Dropped the now-unused `formatDateTime` import. Updated `OrdersHistoryTable.test.tsx` (receipt-progress assertions moved to the modal's responsibility; kept the status-gated Receive + View action assertions) + added a column-set assertion mirroring 132d. `ci:web` green; live `/orders` smoke (created a real order through the UI) proved the trimmed 5-column table + the View modal showing site / created, en/es (a freshly created order has no linked purchases yet, so the modal's conditional staged-delivery / receipts section stays hidden — its render path is unchanged by this slice). | `/orders` table defaults to the smallest useful column set; the trimmed fields stay reachable via the existing View modal; EN/ES live smoke covers it; gates green. | **High** |
| `ENG-132f` | Screen simplification — Purchases list progressive-disclosure pass | Shipped | Sixth list-screen slice of `ENG-132`. **Shipped 2026-06-05** (web-only, no server change): trimmed the `/purchases` history table from 8 columns to the smallest useful set — purchase #, provider, status, total, actions. Like Orders/Quotations, Purchases already has a View → `PurchaseDetailsModal` / `PurchaseDetailsContent`, so the three trimmed columns (date/created-at, site, returns) lean on the EXISTING modal — no new drawer, no new i18n key, no server change. The modal already shows created, site, the supplier-returns summary (count / amount reversed / latest reason / creator) and the full return-history section, so every trimmed field stays reachable; the Status badge (draft / completed / partial_returned / returned / voided) keeps return state legible at a glance. The actions cell (View + status-gated Return) is unchanged; `onRowActivate` (ENG-134f) preserved. The CSV/XLSX export (`purchaseHistoryExportColumns`) is separate from the display columns, so it keeps every field (its column-set test stays green). The now-orphaned `purchases:table.{date,site,returns,…}` keys stay in place per the established 132 convention. Dropped the now-unused `formatDateTime` import. Updated `PurchasesHistoryTable.test.tsx` (returns-cell text assertions moved to the modal's responsibility; kept the status-gated Return + View action assertions) + added a column-set assertion mirroring 132e. `ci:web` green; live `/purchases` smoke (opened a seeded Completado purchase) proved the trimmed 5-column table + the View modal showing site / created, en/es (the seeded purchases carry no returns, so the modal's conditional supplier-returns / return-history section renders only when the purchase has returns — it was not exercised here, but its render path is unchanged by this slice). | `/purchases` table defaults to the smallest useful column set; the trimmed fields stay reachable via the existing View modal; the export keeps every field; EN/ES live smoke covers it; gates green. | **High** |
| `ENG-132g` | Screen simplification — Inventory Movements + Entries tabs progressive-disclosure pass | Shipped | Seventh list-screen slice of `ENG-132`; closes the Inventory screen-trim track. **Shipped 2026-06-05** (web-only, no server change): trimmed the two remaining Inventory tabs in `InventoryPage.tsx`. Movements 7→4+actions (date, product, signed movement, type; trim stock-after, reference, notes); Entries 9→4+actions (date, mode, product, counted qty; trim unit, normalized, cost, stock-after, notes). Unlike the prior 132 slices, these dense log tables had NO existing detail view, so — per operator decision — each gets a NEW row-detail Drawer (`InventoryMovementDetailsDrawer` / `InventoryEntryDetailsDrawer`) mirroring the ENG-132c `InventoryStockDetailsDrawer` (shared `Drawer` primitive, `DetailField` rows, Close-only footer), opened by an additive Details (eye) action; the drawer shows the full record so every trimmed field stays reachable. The inline `movementColumns` / `entryColumns` were extracted to pure `getMovementColumns` / `getEntryColumns` modules (mirrors `inventoryStockColumns`) so the column-set is unit-tested without the heavy page; `getMovementDelta` + `movementBadgeTones` moved with them (the page summary re-imports `getMovementDelta`). The Status/type badge keeps the flow legible at a glance. The CSV/XLSX exports (`inventoryMovementExportColumns` / `inventoryEntryExportColumns`) are separate from the display columns, so they keep every field. Added `inventory:movements.details.*` + `inventory:entries.details.*` (en/es, neutral LATAM) reusing the existing `inventory:table.*` field labels. Dropped now-unused `InventoryPage` imports (`formatDateTime`, default `i18next`, `Package`, `ColumnDef`). Tests: `InventoryMovementColumns.test.ts` (+ getMovementDelta sign), `InventoryEntryColumns.test.ts` (column-set), `InventoryMovementDetailsDrawer.test.tsx` + `InventoryEntryDetailsDrawer.test.tsx` (fields + onClose + null-closed + axe AA). `ci:web` green; live `/inventory` Movements + Entries smoke proved the trimmed tables + the Details drawers, en/es. | Both Inventory log tables default to the smallest useful column set; the trimmed fields stay reachable in a row-detail drawer; exports keep every field; EN/ES strings complete + neutral LATAM; gates + a live smoke prove it. | **High** |
| `ENG-132h` | Screen simplification — Fiscal Documents list progressive-disclosure pass (closes `ENG-132`) | Shipped | Eighth + final list-screen slice of `ENG-132` (Finance/Compliance). **Shipped 2026-06-06** (web-only, no server change): trimmed the `/fiscal-documents` admin compliance table from 8 columns to a compliance-safe set — when, document number, kind, status(+maturity), buyer, total + actions. The two ID-ish columns (raw `providerId`, truncated CUFE hash) move into a new read-only `FiscalDocumentDetailsDrawer` (mirrors the ENG-132c/g drawers; shared `Drawer` primitive + `DetailField`; reuses `FiscalStatusBadge` + `FiscalMaturityBadge`), opened by an additive Details (eye) action; the drawer shows the FULL record incl. the untruncated CUFE and a footer "View XML" hand-off to the existing `FiscalDocumentXmlModal`. The custom HTML table was kept (no DataTable conversion); the frozen-snapshot read invariant (no customers/products join) is untouched (display-only). Of the 3 Finance routes, only Fiscal Documents warranted a trim — `/fiscal-reports` is a coming-soon shell and `/audit-logs` was already at its 5-column minimum. Added `FiscalDocumentListItem` (inferRouterOutputs) to `types/api.ts` + `fiscal:list.details.*` (en/es, neutral LATAM); field labels reuse `fiscal:list.columns.*`. Tests: `FiscalDocumentDetailsDrawer.test.tsx` (fields + XML hand-off gating + onClose + null-closed + axe AA) + `FiscalDocumentListPage.test.tsx` (FIRST test for this page — column-set + drawer round-trip). `ci:web` green; live `/fiscal-documents` smoke proved the trimmed table + the Details drawer (full CUFE + View XML), en/es. **This closes the entire `ENG-132` track.** | `/fiscal-documents` defaults to the smallest useful column set; the trimmed provider/CUFE stay reachable in a row-detail drawer with the full CUFE + XML hand-off; EN/ES strings complete + neutral LATAM; gates + a live smoke prove it. | **High** |
| `ENG-133` | Performance budgets + bundle-size + p95 latency CI gates | Partial | Plan V3 extension ticket. Add Lighthouse-CI for the top user-facing routes, a per-route JS bundle-size budget enforced in `ci:web`, a tRPC p95 latency budget enforced in `ci:server`, and a memory ceiling for the Electron main + renderer process recorded by `ci:desktop`. Baselines + thresholds live in a `perf-budget.json` checked into the repo; a regression breaks the build and surfaces the offending metric. The Operations Center surfaces the latest measured baseline so operators can see the current value. **Shipped 2026-05-20 (engine + 2 of 4 metrics)**: new `perf-budget.json` at repo root anchors all budgets in one place (versioned, JSDoc-commented). New `scripts/check-bundle-size.mjs` runs after `vite build` in `ci:web`, strips Rolldown content hashes from chunk filenames, computes gzipped sizes with `zlib.gzipSync`, and fails the build when any tracked chunk exceeds `budget * (1 + thresholdPercent/100)`. Tolerant paths: new chunks >= 5 KB gz not in budget emit a warning (no fail); chunks in budget but absent from the build emit a warning (no fail). Initial baselines capture the 23 chunks >= 5 KB gz from the last green build with a 5% threshold. New `packages/server/src/perf/budgets.ts` exposes `loadPerfBudget()` + `computePercentile()` (linear interpolation, NumPy default). New `__tests__/perf-trpc-latency.test.ts` boots `:memory:` server, seeds 30 products + 20 customers, then for every key in `perf-budget.json::trpcLatencyMs.p95` (products.list / customers.list / reports.fiscal.list / auditLogs.list / setupReadiness.get) runs 10 warmups + 50 measured samples and asserts p95 stays inside `budget * 1.2`. New `docs/PERF-BUDGETS.md` documents the contract + how to bump baselines (same-PR commit) + how to add new procedures / chunks. Sibling `node --test scripts/check-bundle-size.test.mjs` (7 cases) pins the strip-hash regex + comparison logic so a Vite upgrade can't silently break the gate. Both gates wired into `ci:web` (bundle) and `ci:server` (latency). **Shipped slice 2026-06-08 (ENG-133b — Electron memory ceiling, warn-first)**: new `electronMemoryMb` section in `perf-budget.json` (perProcessMb main/renderer + 25% threshold, baselines captured locally — main 260 / renderer 165 MB). `apps/desktop/src/main/index.ts` gains a flag-gated measure mode (`PUNTOVIVO_MEASURE_MEMORY=1`): on `did-finish-load` + a 2s settle it prints `app.getAppMetrics()` working-set per process and quits. New `scripts/check-electron-memory.mjs` launches the built app (reusing the e2e launch shape; `xvfb-run` on Linux), maps Browser→main / Tab→renderer, compares to budget, and is WARN-FIRST + self-skipping (prints + exits 0 unless `--strict`; skips with a warning when `.vite/build` / a display is absent), so it wires into `ci:desktop` without breaking the green ubuntu job. Sibling `node --test scripts/check-electron-memory.test.mjs` (12 cases) pins the pure summarize/compare/parse logic. `docs/PERF-BUDGETS.md` extended. Functional proof: a real local run measured main ~249-258 / renderer ~159-160 MB and PASSED. **Shipped slice 2026-06-09 (ENG-133c — Lighthouse web vitals, warn-first LOCAL)**: new `lighthouse` section in `perf-budget.json` (perRoute LCP/TTI/CLS/score + 30% threshold; baselines captured locally from a warm dev run). New `scripts/check-lighthouse.mjs` launches Chromium via Playwright, logs in once (the refresh-cookie re-auth survives `disableStorageReset`, so authenticated routes — /dashboard, /sales, /products — measure correctly even though the access token is in-memory), warms each route (Vite compiles route modules on first hit), runs Lighthouse, and compares warn-first + self-skipping. `node --test scripts/check-lighthouse.test.mjs` (14 cases) is wired into `ci:web`; the browser gate stays LOCAL (`pnpm run perf:lighthouse`, NOT push-CI — a Chromium launch + served stack is too heavy, same stance the repo took on the e2e suite). `docs/PERF-BUDGETS.md` extended. **Remaining**: turning the Electron memory gate into a hard-fail once the ubuntu desktop CI job is hardened to launch Electron under xvfb (the gate self-skips there today); promoting the Lighthouse gate into push-CI (needs a browser + the served stack in the runner); Operations Center surface of the latest measured baseline (re-routed to ENG-128 so the readiness signals consolidate with the attention queue instead of growing a parallel panel). Counter Plan v2.0 NO avanza (Plan V3 wave). | Each enforced metric has a baseline + threshold in `perf-budget.json`; CI fails the PR when any metric regresses; the Operations Center surfaces the latest measured baseline; the perf-budget pass is required for every release. | **High** |
| `ENG-134` | Accessibility WCAG 2.2 AA conformance + axe-core CI gate | Partial | Plan V3 extension ticket. Keyboard-only audit on every user-facing route, screen-reader sweep across cashier + admin flows, contrast tokens reconciled with the existing design-system tokens, focus management on every modal/dialog/drawer, axe-core on every component test, ARIA roles on icon-only buttons, accessible names for every dialog and dropdown, `aria-keyshortcuts` declarations aligned with ENG-105. Required for enterprise sales, for some LATAM accessibility laws (Ley 1346 in CO, NMX-R-050 in MX), and for keyboard-first cashier operation that this product already aspires to. **Shipped 2026-05-20 (engine + axe helper + contrast gate + sample sweep)**: new `apps/web/src/test/a11y.ts` exports `assertNoA11yViolations(container, options)` that runs axe-core against the rendered subtree on the WCAG 2 A + AA ruleset and throws on any serious / critical violation (opt-in — existing tests stay green until they call it). `axe-core` lands as a web devDep so the bundle-size gate (ENG-133) traps any accidental inclusion in production. New `scripts/check-contrast.mjs` walks every concrete CSS scope in `apps/web/src/styles/theme.css` (`:root`, `.dark`, …), parses `oklch(L C H)` values, converts to linear sRGB via the canonical OkLab → LMS chain (Björn Ottosson matrices), computes WCAG relative luminance + contrast ratio, and fails the build when an enforced pair (background, card, popover, muted, accent, secondary, primary, destructive) drops below floor. Default floor is 4.5:1 (body text); `primary` and `destructive` pairs also use 4.5:1 because the shared button surfaces render text-sm labels, not WCAG large text. Tolerant paths: missing-side pairs warn, non-oklch values warn, typography utility scopes are skipped. Sibling `node --test scripts/check-contrast.test.mjs` (9 cases) pins the OkLCh parser, the contrast formula, the scope walker, and default 4.5:1 floor for shared button pairs so a design-system token refactor cannot silently break the gate. ARIA sweep on the Header user menu button (icon-only on mobile) adds `aria-label` + `aria-haspopup="menu"` + `aria-expanded` with new i18n keys `common:userMenu.aria` in + es neutral LATAM. `CompanyReadinessCard.test.tsx` gains an axe-AA case to demonstrate the pattern. New `docs/A11Y.md` documents what is enforced today, how to fix a contrast regression, how to silence a single axe violation with mandatory comment, and the ARIA conventions reviewers should check on every UI PR. **Shipped slice 2026-05-21 (slice B — Playwright a11y smoke for top 10 routes + Spanish marketing doc + design-system contrast fixes)**: new `e2e/web/support/a11y.ts` exports `runAxeOnPage(page, options?)` built on `@axe-core/playwright` and mirrors the component-test helper's WCAG 2 A + AA tag set + serious-floor + `renderViolations` indented-bullet output, so a failure reads identically across both surfaces. New `e2e/web/a11y.spec.ts` declares 12 entries over 10 distinct routes after a real login: `/login` (anon), `/dashboard` (admin), `/sales` (cashier + admin lanes), `/inventory`, `/customers`, `/products`, `/purchases`, `/orders`, `/quotations`, `/company`, `/audit-logs`. Each test attaches the shared `ClientIssueTracker`, logs in via `loginAs`, waits for the canonical heading / primary-action button with a 15 s timeout so axe scans a settled DOM, runs `runAxeOnPage`, and asserts zero console errors / network 4xx-5xx. Module-gated surfaces (`/touch`, `/kds`, `/customer-display`, `/m`, `/delivery`) stay out of slice B because the seeded tenant ships them off by default; tracked as a follow-up. **Eight of the twelve entries pass on the AA serious-floor today** (`Login`, `Dashboard`, `Inventory`, `Customers`, `Orders`, `Quotations`, `Company`, `Audit log`). The remaining four (`Sales cashier`, `Sales admin`, `Products`, `Purchases`) carry a per-route `skipUntilEng134c` reason because the smoke caught shared-token debt — the `--primary` Charge button at 2.81:1, `text-primary-600` DataTable number / SKU links at 3.68:1, and `.badge-warning` foreground / warning-50 background at 4.27:1. Each is a single token bump that ENG-134c will close in one sweep, at which point the four `skipUntilEng134c` fields disappear and the smoke goes 12/12 green. The smoke piggybacks on the ENG-001 `e2e-web` CI job (ubuntu-latest, Chromium cache, 30-min timeout, artifact upload on failure) — no CI config change required. `@axe-core/playwright` lands as a root devDep; the production audit gate stays at 0 vulns. Design-system fixes that landed inline with slice B to clear the eight passing routes: (a) `--muted-foreground` bumped from oklch L=0.5 to L=0.4 (light theme) so `text-muted-foreground` clears 4.5:1 against `--background` / `--card` / `--popover`; (b) `--secondary-500` bumped from L=0.61 to L=0.42 so the dozens of `text-secondary-500` body-text usages across cards / Header / LoginPage / data tables / dashboard tiles clear AA (the ramp's 400→500 monotonicity is intentionally relaxed; ENG-134c may migrate those usages to `text-fg2` and restore the ramp); (c) `.page-kicker` switched from `text-primary-600` to direct `color: var(--primary-800)` so the brand-blue kicker clears 4.5:1 on every page; (d) Header page-kicker hard-coded `text-primary-600` swapped to `text-primary-800`; (e) Sidebar workspace headers + Sidebar inactive nav links moved to `text-fg2` (semantic mid-contrast token from ENG-080b); (f) Header + LoginPage + ChangePasswordModal body-text `text-secondary-{500,600}` usages migrated to `text-fg2`; (g) `.label` class default migrated to `text-fg2`; (h) Customers table icon-only buttons (Edit / Delete) gained `aria-label` + `title` tied to `common:actions.edit / delete` (closes axe `button-name [critical]` rule); (i) the CSP `connect-src` widened to include `https://fonts.googleapis.com` and `https://fonts.gstatic.com` so the index.html preconnect hints stop firing CSP errors that the ClientIssueTracker correctly caught. The contrast gate (`scripts/check-contrast.mjs`) gained three new enforced pairs — `(background, muted-foreground)`, `(card, muted-foreground)`, `(popover, muted-foreground)` — so any future relax to the muted-foreground token fails CI instead of a route smoke. New `docs/ACCESIBILIDAD.md` (~140 lines, neutral LATAM `tú` register, no voseo) is the marketing- / pre-sales-facing accessibility commitment doc, citing Ley 1346 (CO) and NMX-R-050 (MX) as regulatory backing and listing the three technical gates (component axe, contrast gate, Playwright smoke) that back the WCAG 2.2 AA promise. Indexed under `docs/PLAN.md §18.1`. `docs/A11Y.md` extended with a new "Playwright a11y smoke" section covering the route catalogue, how to run locally, how to fix a regression, and the suppression contract (mirror of the component-helper rule). Sub-item #4 of the Remaining list (`aria-keyshortcuts` aligned with ENG-105) was already closed inline by ENG-105 slice A — the canonical shortcut catalogue + `aria-keyshortcuts` wiring landed on `SalesCheckoutPanel` (Charge `F1`, Suspend `Mod+P`, Toggle Suspended `Mod+R`). Inline collateral fixes to the E2E baseline (`e2e/shared/baseline.ts`) closed a pre-existing FK constraint failure in `cleanupPriorRunArtifacts` (extra pruning for `product_x_provider`, `initial_inventory`, `order_items`, and by-product-id pruning for `sale_items` / `purchase_items` / `purchase_return_items` / `transfer_order_items`) and added `ensureSetupAcknowledged(db, tenantId)` so the post-ENG-104 admin redirect to `/company?tab=readiness` does not break `loginAs` for any e2e test. **Remaining**: screen-reader sweep across cashier + admin flows with VoiceOver / NVDA (the module-gated surface smoke closed as ENG-134g — only the manual screen-reader sweep is left). ENG-134c (shared-token contrast debt + DataTable keyboard a11y → 12 / 12 smoke green, 2026-05-21), ENG-134e (ProductSearchDialog roving tabindex, 2026-05-22), ENG-134f (DataTable onRowActivate + 6 consumer tables, 2026-05-22), ENG-134d (keyboard-only `/sales` E2E smoke, 2026-05-22), ENG-134g (module-gated surface a11y smoke → 17 / 17 green, 2026-06-09) all closed inline — see the dedicated rows below for the full slice summaries. Counter Plan v2.0 NO avanza (Plan V3 wave). | axe-core runs on every component test and on the Playwright smoke for the top 10 routes with zero serious or critical violations on the AA ruleset; keyboard-only smoke covers `/sales` end-to-end; the a11y pass is required for every release. | **High** |
| `ENG-105f` | Sales Cockpit v2 — focus rules vs barcode wedge | Shipped | Plan V3 slice sobre ENG-105e: reglas de foco afinadas contra el barcode wedge. **Shipped**: `useScannerFocusRestoration` centraliza el foco (mount + restauración al cerrar cualquier modal cashier-flow); el wedge listener gana `scannerInputRef` que procesa el burst aun con el input enfocado y limpia el value tras el flush, con la discriminación scan-vs-typing apoyada en el `interCharGapMs` de 30ms; `Modal` gana `restoreFocusTo?`; 17 tests + e2e `sales-scanner-focus.spec.ts` + live smoke. | Tras montar `/sales` el cajero ve foco automático en la caja de búsqueda; tras cerrar cualquier modal cashier-flow (ProductSearchDialog, SalePaymentModal, QuickCreateProduct/Customer) el foco vuelve a la misma caja; un scan de barcode con el input enfocado se procesa por el wedge y deja el input limpio para el próximo scan; un typing manual lento no se confunde con un scan y abre el diálogo de búsqueda en cambio. | **High** |
| `ENG-105e` | Sales Cockpit v2 — fast-cash F2 (cobro rápido en efectivo) | Shipped | Plan V3 slice sobre ENG-105d: cobro rápido en efectivo con F2. **Shipped**: entry `sales.fastCash` (`F2`) + `onFastCash` en `useSalesKeyboardShortcuts` (activo con modal abierto o cerrado, suprimido en productSearch / inputs fuera del modal); `handleFastCash` reutiliza el gate de F1, el modal observa un `fastCashTrigger` monotónico que pre-llena efectivo + monto exacto + foco en Confirmar; chip "F2 Cobro rápido", `ModalButton` con prop `id?`, i18n, 11 tests, budget bump, `SHORTCUTS.md`. | El cajero presiona F2 → el modal de pago abre con efectivo seleccionado, monto recibido igual al total exacto, y el botón Confirmar enfocado; un Enter cierra la venta; F2 dentro del modal abierto re-aplica el monto exacto sobre lo tipeado; toast `Cobro exacto preparado.` confirma; el chip "F2 Cobro rápido" en el shortcuts card lo hace descubrible; a11y axe Sales sigue verde. | **High** |
| `ENG-105d` | Sales Cockpit v2 — undo/recovery del cart + wrap-around en CommandPalette | Shipped | Plan V3 slice sobre ENG-105c2: undo/recovery del cart y wrap-around en el palette. **Shipped**: `useCartWorkspaceStore` persiste un `historyStack` per workspace (cap 20, FIFO) con `undoCart(id)` + selector de depth + bump de `PERSIST_VERSION` 1→2 (reset en hydrate/remove/resetAll); `Mod+Z` en `useSalesKeyboardShortcuts` (respeta los guards, no interrumpe el undo nativo de texto) + botón "Deshacer" con chip + `aria-keyshortcuts`/`aria-disabled`; `CommandPalette` arrow nav pasa a wrap-around; nuevo entry `sales.undo`, i18n, 22 tests, `SHORTCUTS.md`. | El cajero presiona Mod+Z (o el botón "Deshacer") y la última acción del carro se revierte; el botón muestra el chip del shortcut, queda disabled cuando no hay historia, y respeta WCAG 2 AA via `aria-keyshortcuts` + `aria-disabled`; el palette envuelve la navegación ArrowDown/ArrowUp; el undo nativo de texto en inputs sigue intacto. | **High** |
| `ENG-105c2` | Sales Cockpit v2 — auto-attach del customer recién creado | Shipped | Plan V3 closeout slice sobre ENG-105c, que dejaba al cajero seleccionar manualmente el customer recién creado. **Shipped**: slot one-shot `pendingCustomerAttachId` en `useQuickCreateStore`; `QuickCreateCustomerGate` lo setea + normaliza strings vacíos; `SalePaymentModal` lo consume con un `useEffect` que espera a que la lista traiga el id (cierra la carrera invalidate/refetch) y dispatcha `setValue` + toast; 8 tests. | El customer creado via palette o QuickCreateCustomerGate se auto-attach al sale en la siguiente apertura del SalePaymentModal; toast confirma la acción; el slot es one-shot (consume + clear) por lo que no re-attach espuriamente; el cajero presiona Confirmar sin un clic extra. | **High** |
| `ENG-105c` | Sales Cockpit v2 — quick-create modals mid-sale | Shipped | Plan V3 slice on ENG-105b: el cajero crea un producto sin salir de la venta cuando la búsqueda no encuentra coincidencias (empty-state CTA gated manager/admin; cashier ve hint). **Shipped**: Zustand `useQuickCreateStore` + lazy `QuickCreateProductGate`/`QuickCreateCustomerGate` que montan los form modals con `defaultName` y fold el producto al cart; `ProductFormModal`/`CustomerFormModal` + `ProductSearchDialog` extendidos con props opcionales; 2 acciones de palette role-gated; i18n + 22 tests + smoke. Auto-attach del customer → ENG-105c2. | El cajero crea un producto desde la pantalla de venta sin salir del flow; el carrito recibe el producto recién creado automáticamente; el cashier ve el hint pero no el botón; las 2 acciones del palette están role-gated; el smoke 12/12 a11y AA de ENG-134c sigue verde. | **High** |
| `ENG-105b` | Sales Cockpit v2 — checkout preflight panel | Shipped | Plan V3 slice on ENG-105 A: a visible preflight panel above the F1 Charge button so the cashier sees blockers before pressing F1. **Shipped**: pure `useCheckoutPreflight` hook computes 6 blocker/warning families from existing SalesPage state with recovery CTAs; `CheckoutPreflightPanel` (`role="status"` aria-live, AA palettes) wired with `aria-describedby`; Cobrar disables on hard blockers, warnings leave it enabled; F1 surfaces the first blocker (cash-session blocker still jumps to the modal); `sales:preflight.*` i18n; 26 tests. Pre-modal customer/discount families deferred. | Cashier sees blockers above Cobrar before pressing F1; the primary-action disables on hard blockers (cash session, role, credit limit, discount drift); warnings (stock estimate) leave Cobrar enabled; F1 toast surfaces the first blocker; the panel respects WCAG 2 AA contrast. | **High** |
| `ENG-131c` | Workspace landing routes (`/catalog`, `/procurement`, `/finance`) | Shipped | Plan V3 IA closeout on ENG-131: turn the 8 sidebar workspaces into real navigable URLs with index landing pages (typing `/catalog` in the address bar used to 404). **Shipped**: a generic `WorkspaceLandingPage` (prop `workspaceId`, reuses `visibleItemsForWorkspace`, renders a role + module filtered card grid, redirects an empty set to `/dashboard`); 3 lazy routes gated by `allowedRoles`; the sidebar workspace header split into a `<Link to={defaultRoute}>` + an adjacent `aria-expanded` chevron `<button>`, activating `defaultRoute` as the single source of truth. Deep links to leaf routes stay unchanged. | El operator typea `/catalog`, `/procurement` o `/finance` (o clickea el header del workspace en el sidebar) y aterriza en una landing con grid de cards filtradas por rol + módulo activo; cada card navega a su item; los deep links a hojas siguen funcionando sin redirect; los 5 workspaces sin landing mantienen el header link al primer item; el disclosure widget WAI-ARIA del chevron queda intacto. | **High** |
| `ENG-131b` | Surface Switcher via CommandPalette | Shipped | Plan V3: cierra el ítem Surface Switcher de ENG-131, 100% additive sobre ENG-105 slice A. **Shipped**: 5 acciones nuevas en `commandPaletteActions.ts` (`navigate.posTouch/kds/customerDisplay/mobileWaiter/restaurantTables` con `requiredModule` + role gating) reusando el `visibleActionsForRole` + `filterActionsByQuery` existentes — sin cambios en componentes compartidos; i18n en/es, 5 tests + live smoke. | Cashier/manager/admin con superficies activas usa Mod+K + tipea "touch" / "kds" / "customer display" / "mobile waiter" / "tables" para saltar entre las 5 superficies sin tocar el sidebar; cuando un módulo está OFF, la superficie no aparece en el palette (parity con el sidebar); cuando el rol no califica (cashier intentando ir a `/restaurants/tables`), la acción se oculta en el palette igual que el router redirige. | **High** |
| `ENG-134d` | Keyboard-only `/sales` end-to-end smoke | Shipped | Plan V3 a11y closeout on ENG-134f: permanent E2E coverage for the full keyboard-only cashier flow; per the inline-fix policy it also closed 3 real bugs the tests surfaced. **Shipped**: `e2e/web/sales-keyboard-only.spec.ts` (10 cases across 3 describes, 3 inline axe scans) + a shared `e2e/web/support/sales-keyboard.ts` helper. Inline fixes: dropped an `.opacity-80` color-contrast violation on `OfflineStatusBanner`; whitelisted `#sales-product-search-input` in the F2 / Mod+Z shortcut guards (the cashier focus position); and changed Undo / Clear-cart to render only when usable (Mod+Z stays reachable document-level + via the palette). Spec runs green in ~22s. | A keyboard-only cashier can mount `/sales`, search SKU + Enter, navigate rows + Enter to select, add to cart, open the payment modal with F1/F2 from the search input, complete the sale, undo with Mod+Z, and open a just-made sale via Enter on its row (ENG-134f); the e2e-web job catches any regression of these links. | **High** |
| `ENG-134f` | DataTable row activation + apply to 6 consumer tables | Shipped | Plan V3 a11y closeout on ENG-134e: `DataTable` already had roving tabindex but its Enter/Space branch was gated by `enableRowSelection`, so focused-row activation was a no-op on Sales/Purchases/Orders/Quotations/Customers/Products. **Shipped**: an optional `onRowActivate` prop on `DataTable` (with a `ResourcePage` passthrough); the Enter/Space branch now prioritizes activation ungated. Wired into 6 consumers (Sales/Purchases/Orders/Quotations open the detail modal; Customers/Products open the edit modal, role-gated). `InventoryBalancesPanel` stays read-only. 11 tests; `A11Y.md` reframes the canonical consumer rule to `onRowActivate`. | Enter / Space on a keyboard-focused row in the Sales/Purchases/Orders/Quotations history tables opens its detail modal; on Customers/Products it opens the edit modal (role-gated); mouse `onClick` semantics stay intact; `enableRowSelection` consumers keep the legacy `toggleSelected` fallback when no `onRowActivate` is wired. | **High** |
| `ENG-134e` | ProductSearchDialog keyboard navigation | Shipped | Plan V3 a11y pre-req found during ENG-134d planning: `ProductSearchDialog` rendered its rows with `onClick` only (no `tabIndex` / `onKeyDown` / focus management), so a keyboard-only cashier could not select a product. **Shipped**: a roving-tabindex pattern over the product rows that keeps the `<table>` semantics (no listbox ARIA), `activeRowIndex` reset via the React 19 render-time key pattern (no `useEffect`), ArrowUp/Down/Home/End navigation + Enter/Space selection (same callback as the mouse `onClick`), and a `:focus-visible` ring. The mouse path is untouched. 13 tests; pre-req for ENG-134d. | The cashier opens ProductSearchDialog with Enter on the search input, navigates rows with ArrowDown/Up/Home/End, selects with Enter or Space, sees the focus ring only under keyboard nav, and the post-Escape focus restore (ENG-105F) stays green; the mouse `onClick` path does not regress (existing E2E pass untouched). | **High** |
| `ENG-134g` | Module-gated surface a11y smoke | Shipped | Plan V3 a11y closeout on ENG-134's "module-gated surface smoke" remaining item: the Playwright a11y catalogue (`e2e/web/a11y.spec.ts`) covered the top 10 routes but skipped the five module-gated surfaces because the seeded tenant ships those modules OFF. **Shipped 2026-06-09** (e2e-only, no app change): new `ensureModulesEnabled(db, tenantId, moduleIds)` in `e2e/shared/baseline.ts` force-enables `pos-touch` / `kds` / `customer-display` / `mobile-waiter` / `delivery` for the e2e tenant via the same `json_set` of `$.modules.<id>` the `modules.setActive` mutation performs (so `resolveModulesState` → `getEffective` → `SurfaceShellRoute` renders each surface instead of redirecting), wired into `prepareBaseline`. Five new `A11yRoute` entries (role admin, locale-agnostic `data-testid` settled locators; KDS matches board-or-empty-state since the retail seed has 0 tickets). All five passed the WCAG 2 A + AA serious floor with ZERO inline fixes — the surfaces were already accessible. 17 / 17 a11y entries green. No new i18n, no component change. Surfaced (NOT caused) a pre-existing ENG-186 `/sales` layout regression that fails 4 cash-session E2E tests at 1280x720 — proven independent of this slice (reproduces with the modules off), then promoted and closed as `ENG-189`. | The five module-gated surfaces (`/touch`, `/kds`, `/customer-display`, `/m`, `/delivery`) are in the Playwright a11y catalogue with zero serious/critical axe violations after the seed enables their modules; ENG-134 parent stays Partial with only the VoiceOver/NVDA sweep remaining. | **High** |
| `ENG-134c` | Close inherited a11y contrast debt on shared design tokens | Shipped | Plan V3 closeout on ENG-134 slice B: clear the three shared-token contrast failures that kept 4 routes `skipUntilEng134c`. **Shipped (12/12 smoke green)**: light `--primary` bumped to `oklch(0.43 0.115 244)` (+ hover swept to primary-900) and `--warning-700` to L=0.50; body-text `text-primary-600` swept to `text-primary-800` across ~9 surfaces + Button; contrast gate adopts the `(warning-50, warning-700)` pair; `skipUntilEng134c` mechanism removed; a `scrollable-region-focusable` collateral fix made `.data-table-scroll` a labelled focusable region. | The four routes (`/sales` cashier, `/sales` admin, `/products`, `/purchases`) drop their `skipUntilEng134c` field; the smoke goes 12 / 12 green on the WCAG 2 A + AA serious floor without any axe-rule exclusions. | **High** |
| `ENG-135` | Production observability stack (Sentry + OpenTelemetry + structured logs) | Partial | Plan V3 extension ticket. Wire Sentry (or self-hosted GlitchTip) for the web bundle + Electron main, OpenTelemetry tracing for the tRPC pipeline, structured logs with tenant scope + correlation ID, and a per-tenant error rate dashboard. Telemetry is opt-in per tenant and inherits the redaction rules from ENG-128. Pairs with ENG-128: the local diagnostic bundle remains the operator-side artifact; this ticket is the centralized side that lets support see fleet health. **Shipped 2026-05-20 (engine + sink interface + helpers + opt-in + audit + tracing middleware + render-error wiring)**: new `packages/server/src/observability/` module owns three concerns. `sink.ts` exposes `TelemetrySink` (captureException + recordSpan) plus a default `noopSink` and `registerTelemetrySink(adapter)` so an adapter PR can swap the active pipe at runtime without touching every call site. `capture.ts` exposes `captureException(err, attrs, db)` and `withSpan(name, attrs, fn, db)` — the local pino log emits unconditionally, the sink is invoked only when `tenants.settings.telemetryOptIn === true` (looked up through a 60 s per-tenant cache so error storms do not hammer the DB, with the admin toggle invalidating that tenant cache immediately). `redact.ts` walks the attrs bag and masks `password / token / authorization / refreshToken / email` keys before the sink ever sees them. The sink path is wrapped in a try/catch so a misbehaving adapter never breaks the caller. New `packages/server/src/trpc/middleware/tracing.ts` is applied via `publicProcedure = t.procedure.use(tracingMiddleware)` so every procedure (including unauth surfaces like `auth.login`) logs `procedure / durationMs / outcome / correlationId / tenantId / userId` on success, calls `recordSpan` on ok/error outcomes when the tenant is opted in, and routes failures through `captureException` before re-throwing. The middleware uses a free function plus an init-side `t.middleware()` wrap to avoid the circular load that would otherwise form between `init.ts` and `tracing.ts`. New `companies.updateTelemetryOptIn` admin mutation flips `tenants.settings.telemetryOptIn` via `json_set` + parametrized boolean fragment (no string concatenation), writes an audit row `telemetry.opt_in.updated` with `before` / `after` snapshots in the same transaction, and the toggle is the gate between local-only and centralized capture. `companies.getCurrent` now surfaces the resolved flag on its response shape via `selectCompanyByIdWithTelemetry(...)` so `setData` callers preserve the field across react-query cache writes. New audit enums: `'telemetry.opt_in.updated'` action + `'tenant'` resource type in `auditLogActionEnum` / `auditLogResourceTypeEnum` and mirror in `apps/web/src/types/index.ts`. New `apps/web/src/lib/observability.ts` exposes `captureRenderError(err, ctx)` + `installGlobalErrorListeners()` + `setActiveTenantId(...)` + `registerRenderTelemetrySink(...)`; the console fallback runs unconditionally so a dev tail still sees every event. `AppErrorBoundary.componentDidCatch` funnels into the helper with the React `componentStack`. `main.tsx` calls `installGlobalErrorListeners()` before mount so `window.error` + `window.unhandledrejection` escape the React boundary into the same pipe. `AuthProvider` calls `setActiveTenantId(...)` on init + login + logout so window-level listeners know the active tenant. New `CompanyTelemetryCard` lives in the `data` tab of `/company` under Sync + Backup with a status panel (enabled / disabled), a toggle button, and an inline privacy reminder. New i18n keys `settings.company.telemetry.*` + `auditLogs.actions.telemetry.opt_in.updated` + `auditLogs.resourceTypes.tenant` in + es neutral LATAM. 15 server tests across `observability-capture.test.ts` (sink swap, opt-in / opt-out branches, anonymous capture, redaction, cycle tolerance, cache TTL with fake timers), 4 tests in `trpc-tracing.test.ts` (ok / error / anonymous / parallel-correlationId), 6 tests in `companies-telemetry.test.ts` (flip-up + flip-down + cashier FORBIDDEN + cross-tenant + getCurrent surface + immediate revoke) + extended AppErrorBoundary + CompanyTelemetryCard tests on the web side. New `docs/OBSERVABILITY.md` documents the contract, the redaction policy, the opt-in semantics, and the adapter wiring recipe for the follow-up PR. **Shipped slice 2026-06-10 (ENG-135b — Sentry/GlitchTip adapter + Electron crash path)**: `@sentry/node` (server) + `@sentry/browser` (web) wired to the existing sink interfaces, both DSN-gated; GlitchTip speaks the Sentry protocol so one adapter covers both. Server: new `observability/sentry.ts` dynamic-imports the SDK only when `PUNTOVIVO_SENTRY_DSN` is set, `defaultIntegrations: false` (no http monkey-patching, so the ENG-133 p95 latency budgets stay untouched), retroactive-span mapping via `startInactiveSpan` + explicit timestamps, optional `PUNTOVIVO_SENTRY_TRACES_SAMPLE_RATE` (default 0; errors flow regardless), never-throw init wired inside `createServer` (covers standalone + embedded desktop). Web: new lazy `lib/sentry.ts` chunk behind `VITE_PUNTOVIVO_SENTRY_DSN` (build-time gate — a DSN-less build dead-code-eliminates the chunk to 0 bytes; a DSN build emits ~28 kB gz, deliberately NOT added to perf-budget.json because the chunk is conditional; see OBSERVABILITY.md); render events forwarded tenant-less (tenantId stripped before the SDK sees the context). Electron: new `crash-telemetry.ts` installs the previously-missing `uncaughtException`/`unhandledRejection` handlers — structured log + `captureProcessCrash` (sink-direct; the DSN is the operator-level consent for tenant-less diagnostics, two-layer consent model documented in OBSERVABILITY.md) + bounded flush + fail-fast exit 1 (rejections log + capture without exit). 15 server + 6 web + 8 desktop new tests with the SDK mocked at the module boundary. The slice also closed its own review follow-ups in the same diff: standalone-server crash handlers now forward through captureProcessCrash + bounded flush before the existing graceful shutdown (site_hub supervisor keeps its fail-fast-on-rejection stance, unlike the Electron main); a vitest setupFile strips ambient PUNTOVIVO_SENTRY_DSN so a dev shell can never activate the real SDK across the suite; the CSRF 403 hook now replies a tRPC-shaped envelope (the old plain body rendered as Unable to transform response on the client — verified against the real TRPCClientError.from); and the four drifted i18n/code chunks (settings/sales/AuthProvider/operations) got perf-budget entries, zeroing the new-chunk warnings. **Shipped slice 2026-06-10 (ENG-135c — trace propagation renderer → server)**: the web client mints a fresh correlation id per tRPC request (UUID v4, crypto.randomUUID with a low-entropy fallback) and ships it as x-correlation-id from the existing headers() callback; new `observability/correlation.ts` sanitizes the attacker-controlled header (strict `[A-Za-z0-9_-]{8,64}` intake, correlation-only usage — never logic, the non-spoofable Fastify requestId stays as an independent binding) and feeds `buildRequestScopedLoggerBindings` + the tracing middleware's `resolveCorrelationId` (valid header wins, fallback to reqId keeps SSE/curl/header-less callers byte-identical to before); CORS allowlist gains the header (explicit list, not reflective); `captureRenderError` stamps the page's most recent id on client error events so the renderer event and the server trace share one identifier (last-request approximation under concurrency, documented). The desktop renderer rides the same tRPC client, so the embedded main is covered with zero desktop changes. 22 server + 6 web tests (sanitizer contract, header adoption/fallback/array, minting uniqueness, error-event stamping). SSE stays on reqId (EventSource cannot send custom headers); per-query DB spans do not exist — the DB leg is covered by the request-scoped log bindings. **Remaining**: per-tenant error rate dashboard + crash-free sessions metric (need centralized aggregation), validation against a real provisioned Sentry/GlitchTip instance (the ENG-135b smoke verified envelopes against a local HTTP catcher only). Counter Plan v2.0 NO avanza (Plan V3 wave). | A new error in `apps/web/**` lands in the support pipeline within one minute; trace IDs span renderer → main → server → DB; the operator can see error rate, latency p95, and crash-free sessions per tenant; the telemetry opt-in is auditable per tenant. | **High** |
| `ENG-136` | Backup / restore tooling + cloud vault + scheduled snapshots | Pending | Plan V3 extension ticket. Manual + cron-scheduled snapshots using SQLite `VACUUM INTO` to a per-tenant encrypted blob; optional upload to a configured S3-compatible vault with tenant-managed credentials; automated point-in-time restore drill; and operator-facing restore UI under Setup → Data. Encryption uses the same key-storage decision documented for `ENG-066`. Required so a merchant that loses its laptop can resume operations within a working day. | Admin can configure snapshot frequency + destination, see the last successful snapshot timestamp, trigger a manual snapshot, and run a restore drill that writes an audit row + reports diff vs current DB; restore can be triggered from a fresh install given the encrypted blob + the recovery key. | **High** |
| `ENG-137` | Auto-update + staged rollout + rollback (Electron) | Pending | Plan V3 extension ticket. `electron-updater`-style automatic delivery, per-tenant version pinning, staged rollout (10% → 50% → 100%), telemetry-driven rollback hook tied to ENG-135 observability, and "update health" surfacing in the supportability queue. Tenants can opt into a staged channel (stable / beta) and the renderer reports the current version + last-update timestamp. Without staged rollout every release is a single point of failure for the entire tenant fleet; without rollback a bad release strands every store. | Admin can opt a tenant into a staged channel, the renderer reports the current version + last-update timestamp, and a release marked "rollback" reverts every terminal within one update cycle; telemetry-driven auto-rollback fires when the crash-free-session metric breaches the configured threshold. | **High** |
| `ENG-138` | Subscription / billing / license enforcement | Pending | **Local-license slice is implementable today; cross-tenant billing back-office (aggregate ARR / churn / fleet view) depends on `ENG-164`** (hosted-SaaS substrate spike). Plan V3 extension ticket. Plan tiers (Basic / Pro / Chain), trial period, dunning, license gate at module activation (module activation calls into a license check before flipping the manifest bit), grace-period mode (read-only POS for N days after license expires; sales blocked but exports work), and integration with one local payment rail (Wompi recurring) plus Stripe for non-CO tenants. License state syncs to the local DB so the POS works during a brief offline window. Required so the product earns money inside the product, not on a spreadsheet. Depends on ENG-136 backup so a tenant in grace can still export its data. | Admin can start a trial, convert to paid, fail a payment and enter grace, and recover by adding a new card; a tenant past grace cannot complete sales but can export data; license state survives a brief offline window and reconciles on reconnect. | **High** |
| `ENG-139` | Enterprise SSO + MFA (Google + Microsoft Entra ID + OIDC + TOTP) | Pending | Plan V3 extension ticket. Add Google Workspace OAuth, Microsoft Entra ID OIDC, generic OIDC, optional SAML 2.0, tenant-role mapping (IdP group → Puntovivo role), audit trail of every IdP-driven login, and admin TOTP MFA as the baseline second factor for username/password tenants. Recovery flow for lost MFA via verified support workflow with audit. Chains and pharmacies will not allow per-user passwords by 2027; admin MFA is overdue regardless. | Admin can connect a tenant to an IdP, map IdP groups to tenant roles, enforce MFA for admin actions, and recover lost MFA through a verified recovery flow with audit; every IdP login writes an audit row. | **High** |
| `ENG-140` | Shift management + LATAM labor compliance | Pending | Plan V3 extension ticket. Schedules per site, break tracking, overtime calculation per country (CO / MX / CL / PE / AR labor codes), attendance audit, integration with cash sessions (clock-in opens shift; cash session close closes shift), and a manager-facing schedule editor. Promotes the ENG-106 "clock-in/out baseline" stub into a full feature. Required because LATAM labor codes treat shift records as durable evidence; payroll exports must reconcile to these. Hooks into ENG-160 Nómina Electrónica DIAN when the gate clears. | Admin can build a schedule, the cashier clocks in/out from the POS, overtime is calculated per the tenant's country rules, and a payroll export ships in CSV + the accounting target's format; shift records survive cash-session close and are queryable per employee. | **High** |
| `ENG-141` | Day-close comprehensive report + manager sign-off | Pending | Plan V3 extension ticket. A printable end-of-day pack bundling sales, fiscal documents, cash variance, payment settlement, commissions, tips, waste, voids, refunds, AI anomaly summary, and a manager signature line. Renders to PDF + WhatsApp link + email + optional accounting push. Different from cash session close (which is per-shift per-cashier); this is the manager's day-level reconciliation surface. Required because every merchant wants one piece of paper per day that says "yesterday closed clean." | At day end the manager opens the report, reviews the bundled sections, signs off (electronic), and the resulting PDF is stored, emailed, optionally pushed to accounting, and produces an audit row tied to the signing user. | **High** |
| `ENG-142` | Loss-prevention rules engine (deterministic) | Pending | Plan V3 extension ticket. Per-cashier maximum discount, max void/refund per shift, no-sale rule, after-hours sale rule, dual-approval thresholds, and configurable alerts that route to WhatsApp or in-app notification. Complements ENG-130 AI anomaly detection (statistical drift) with hard rules that work offline. Every rule trigger writes an audit row + optional WA push to the manager. Required because statistical anomaly detection alone does not block in real time; merchants want hard guardrails that block-then-approve. | Admin can configure per-role rules, the cashier sees the block + approval CTA at the moment of violation, and every rule trigger writes an audit row + optional WA push to the manager; rules work fully offline. | **High** |
| `ENG-143` | In-transit transfer state + multi-day partial receipt | Pending | Plan V3 extension ticket. Schema extension on `inventory_transfers` for `in_transit` and `partially_received` states, partial-receive line table, discrepancy resolution UI, and audit chain that spans the days a transfer is moving. Chains move stock across days; the current one-shot model breaks when a transfer arrives partial on day 3 and the rest arrives on day 5. Depends on ENG-110 product model for serials/lots context, ENG-125 procurement for the matching receiving pattern, and ENG-126 chain HQ for governance. | A transfer can leave site A on day 1, arrive on day 3 with five of ten cartons received, surface the gap, allow the receiver to record a discrepancy, and reconcile the audit chain when the rest arrives; cross-tenant isolation preserved. | **Medium** |
| `ENG-144` | WhatsApp inbound commerce (catalog sync + order intake) | Pending | Plan V3 extension ticket. WA Business Platform Cloud API integration for the inbound direction: catalog sync (Puntovivo products → WA catalog with media + pricing), customer order intake via WA chat or catalog button, drop the order into Puntovivo as a draft sale with payment-link tender, and order-status replies back to the customer over WA. Depends on ENG-112 for the WA provider abstraction and ENG-124 for the payment-link contract. World-class LATAM POS treats WA as bidirectional commerce, not just a receipt sender. | A customer can browse the tenant catalog inside WA, place an order, pay via a generated link, and the cashier sees the order arrive as a draft sale ready to fulfill; the WA conversation continues with order status. | **High** |
| `ENG-145` | Tip pooling and distribution math | Pending | Plan V3 extension ticket. Multiple pool types (per-shift, per-section, per-station), distribution rules (by hours, by role, by station), back-of-house tip-out, payout schedule, and cash-session + day-close integration. Promotes the "tip compliance hooks" mentioned in ENG-117 into a full feature. Depends on ENG-117 for the restaurant lifecycle context and ENG-140 for the hours-worked source. | Admin configures a pool, tips collected on sales flow into the pool, the distribution rule fires at shift close, employees see their share, and the cash session report + ENG-141 day close show the payout owed. | **Medium** |
| `ENG-146` | Recipe scaling + batch production + waste tracking | Pending | Plan V3 extension ticket. Scale a recipe by % or batch size, create batch production runs that consume BOM ingredients, record waste against recipes or raw inventory with reason codes (over-portion, expiry, breakage, theft, prep), and surface waste in ENG-116 BI. Depends on ENG-110 BOM model. Required for bakeries, kitchens, food trucks, and pharmacies that compound on-site. | Admin can scale a recipe, run a batch that consumes the scaled ingredients, record waste with reason codes, and review waste cost in the BI control tower; batch runs are auditable. | **Medium** |
| `ENG-147` | Self-checkout + table QR ordering | Pending | Plan V3 extension ticket. A customer-facing kiosk surface that lets the customer scan, add to cart, and pay (writeable extension of ENG-107's read-only display); a per-table QR code that opens a writeable mobile menu and drops the order into the POS as a draft sale. Includes payment-terminal handoff and audit of which device originated the order. Required for cafés, food courts, pharmacies, and minimarkets where labor is scarce. | A customer can complete a self-checkout sale from the kiosk with payment-terminal or QR rail; a customer can scan a table QR, build an order on their phone, and the waiter sees it arrive in the POS as a draft sale with table + customer context. | **Medium** |
| `ENG-148` | Hardware compatibility matrix + certification gate | Pending | Plan V3 extension ticket. Publish a certified hardware list (scanners, printers, drawers, scales, payment terminals, customer displays) under `docs/HARDWARE-COMPAT.md` with a test plan per device; CI gate that runs the test plan on a known-good rig and blocks releases that regress a certified device. Different from `ENG-022` (hardware POS integration ticket) — this is the published commitment + the gate. Required because world-class POSes publish what they support and gate releases against it. | `docs/HARDWARE-COMPAT.md` lists certified devices + status; `ci:desktop` runs a smoke test against a configurable rig; a certified-device regression breaks the build; the public website consumes this matrix as authoritative copy. | **Medium** |
| `ENG-149` | Shared peripheral resolver (LAN print queue + drawer share) | Pending | Plan V3 extension ticket. LAN print queue + shared drawer resolver for multi-cashier stores: the Authority Node hub coordinates printer + drawer access so two registers do not collide on the same physical device. Depends on the ENG-071..ENG-075 Authority Node hub work. Required because tight stores share printers and drawers; today the hardware stack assumes one cashier per peripheral. | Two cashiers can complete sales in parallel on registers A + B with one shared thermal printer + one shared drawer; the hub serializes access and audit shows which register triggered each operation; a peripheral failure surfaces to both registers without blocking either's checkout flow. | **Medium** |
| `ENG-150` | Universal print server / browser-to-ESCPOS bridge | Pending | Plan V3 extension ticket. A LAN-resident print bridge using WebUSB / WebSerial on the hub so a tablet-only register can print to ESC/POS without a local driver. The bridge surfaces print queue status; offline + retry behavior matches the rest of the peripherals contract. Different from `ENG-149` — this enables tablet registers; that one shares peripherals across desktop registers. Required because tablet-only registers (the cheapest LATAM POS hardware) lose the ESC/POS path. | A tablet in the same LAN as the hub can print a receipt to a hub-connected thermal printer; the bridge surfaces print queue status; offline + retry behavior matches the rest of the peripherals contract; cross-tenant isolation preserved. | **Medium** |
| `ENG-151` | Chaos / disaster-recovery game day + automated restore drill | Pending | Plan V3 extension ticket. A scripted scenario library that simulates power loss, SQLite corruption, hub-client partition, payment provider 503, fiscal provider 500, and printer crash. The suite runs nightly in CI against a synthetic tenant; restore drills run weekly with audit. Pairs with `ENG-067` (chaos suite skeleton) and `ENG-136` (backup tooling) — this is the rehearsal layer. Required because a backup that has never been restored is not a backup. | A nightly CI job runs the chaos suite against a synthetic tenant; the suite produces a pass/fail report + diagnostic bundle; a weekly restore drill verifies that ENG-136 snapshots can be brought back within the documented RTO; failures alert support automatically. | **Medium** |
| `ENG-152` | Tenant data export portability (JSON Lines + Parquet) | Pending | Plan V3 extension ticket. Full per-tenant export to JSON Lines + Parquet covering every operational table (sales, payments, inventory, customers, audit, fiscal documents, attachments). Streaming export with progress; signed download URL with expiry; the export round-trips back via the launch migration importer (ENG-123) into a fresh tenant. Different from ENG-129 (Habeas-Data PII export per customer) — this is the full-tenant operator export. Required because merchants must own their data. | Admin can request a full export, see progress, receive a signed download, and the export round-trips back via ENG-123 importer into a fresh tenant with no data loss; export respects retention + consent state. | **Medium** |
| `ENG-153` | Customer cohort + LTV + RFM analytics | Pending | Plan V3 extension ticket. Cohort retention curves (by month / quarter / year), customer lifetime value computation, RFM segmentation (Recency / Frequency / Monetary), and integration with `ENG-127` so an RFM segment can be exported as a campaign audience. Depends on ENG-127 customer timeline and ENG-116 BI control tower. Required to compete with Shopify Audiences and Square Loyalty cohort views. | Owner sees cohort retention by month / quarter / year; an RFM segment can be exported as a campaign audience in ENG-127; LTV is computed per customer with explicit time-window; segments respect ENG-129 consent state. | **Medium** |
| `ENG-154` | Receipt branding studio + per-channel template renderer | Pending | Plan V3 extension ticket. Extend the ENG-016 receipt template editor with per-channel variants (print 80mm, print 58mm, email, WhatsApp, PDF, customer display) and a visual branding studio with logo, colors, copy, footer messaging, QR placement. The renderer produces the right artifact for each channel; the audit shows which channel sent which receipt for which sale. | Admin can edit one template and preview it in every channel; the renderer produces the right artifact for each channel; the audit shows which channel sent which receipt for which sale; locale parity preserved EN/ES (and pt-BR when ENG-161 lands locale parity). | **Medium** |
| `ENG-155` | Contextual help + in-product video library + product tours | Pending | Plan V3 extension ticket. Embedded video library (Loom-style host or self-hosted), contextual help launcher per screen, guided product tours for first-run + module activation, and an indexed search across help content. Lowers the install-to-first-sale time faster than docs alone. Depends on ENG-104 (readiness flow) for the first-run tour trigger and ENG-131 (workspace navigation) for the contextual launcher placement. | Every major screen has a contextual help affordance; clicking it opens a focused panel with the relevant video + doc snippet; the tour for first-run setup is mandatory before checkout works; help search returns relevant content within one second. | **Medium** |
| `ENG-156` | Multi-currency operations (FX, sell USD / settle COP) | Pending | Plan V3 extension ticket. Extend `tenant_locale_settings` with sell-currency + settle-currency, FX rate table sourced per tenant (manual or upstream feed), dual-currency receipt display, and a settle-currency-aware accounting export. Required for tourist-zone tenants (Cartagena, San Andrés, Cusco, Buenos Aires) that sell in USD and settle in COP / PEN / ARS. Depends on ENG-017 locale settings and ENG-115 accounting. | A tenant can sell in USD, the cashier sees the COP-equivalent on the cart, the receipt shows both, the fiscal document uses the sell currency per local rules, and the accounting export uses the settle currency. | **Medium** |
| `ENG-157` | Public demo sandbox + ephemeral tenant provisioning | Pending | **Depends on `ENG-164`** (hosted-SaaS substrate spike must close first; this ticket needs a hosted multi-tenant runtime that does not exist today). Plan V3 extension ticket. A marketing-site CTA spins up an ephemeral tenant with seeded retail + restaurant + pharmacy demo data, auto-expires after seven days, and never leaks across sessions. Required so the future public website can offer "try it now" instead of "request a demo". Depends on ENG-123 launch migration importer for the seed pipeline and ENG-136 for the auto-cleanup. | A visitor clicks "Try a demo" on the future public site, lands in a populated tenant within 15 seconds, and the tenant disappears after the configured TTL with a one-line audit row; demo tenants are clearly labeled inside the product and cannot accept real payments. | **Medium** |
| `ENG-158` | Tenant clone / sandbox for safe experimentation | Pending | **Depends on `ENG-164`** (hosted-SaaS substrate spike must close first; tenant clone requires a hosted multi-tenant runtime). Plan V3 extension ticket. Copy a production tenant to a sandbox tenant (same DB instance, different `tenant_id`, no real PII — customer names and emails are obfuscated), let the operator experiment with a configuration change or a vertical activation, and either promote the sandbox back or discard. Pairs with ENG-157 (the demo sandbox is fresh seed; this is "clone my real tenant"). Pilots and chains use this to test new features without risk. | Admin can clone tenant X to tenant X-sandbox, the clone preserves catalog + sites + users but obfuscates customer PII, changes in the sandbox can be diffed, and a destroy reverts everything within seconds; sandbox tenants cannot emit fiscal documents or charge real payments. | **Medium** |
| `ENG-159` | Data residency policy + per-tenant AI provider gating | Pending | Plan V3 extension ticket. Per-tenant policy declaring "AI calls must use a provider in region X" or "AI is disabled in this tenant"; gracefully degrade to deterministic flows when the policy cannot be met. Pairs with ENG-130 "safe degrade" contract. Required for banks, pharmacies, and government merchants that cannot accept cross-border AI calls. | Admin can declare a residency policy; an AI call that would violate it is blocked + logged + degraded; an audit screen shows the policy and the recent compliance log; the policy survives provider migration. | **Medium** |
| `ENG-160` | Nómina Electrónica DIAN (Colombia payroll fiscal) | Gated | Plan V3 extension ticket. Generate, validate, transmit, and reconcile Nómina Electrónica documents per Resolución DIAN; integrate with `ENG-140` shift management for hours + overtime; reuse the fiscal adapter seam from `ENG-021`. **Gate**: needs DIAN PT contract + credentials + Nómina-specific certificate; this is the same gate as `ENG-021` extended to payroll documents. Required because a LATAM POS that ships in Colombia but cannot emit Nómina Electrónica leaves a major customer ask unanswered. | When the gate clears, a tenant can generate a Nómina Electrónica document from a payroll period (sourced from ENG-140 shifts), transmit it, and reconcile the DIAN acknowledgment with audit. | **Medium** |
| `ENG-161` | pt-BR localization + NFe Brazil foundations | Pending | Plan V3 extension ticket. Two-step: (1) locale parity for `pt-BR` across every namespace (deliverable now, no external gate), (2) NFe / NFC-e foundations as schema + adapter seam (gated on a real Brazilian PT contract — same shape as `ENG-021` for DIAN and `ENG-035` for SAT). Locale slice is `Pending`; fiscal slice stays `Gated` inside the same ticket scope. Required because "LATAM" without Brazil is incomplete. | Locale slice: `apps/web/src/i18n/locales/pt-BR/**.json` reaches parity with `en` + `es`, fallback chain `pt-BR → pt → en` configured, neutral Brazilian Portuguese register. Fiscal slice (gated): NFe adapter seam compiles + tests; the gate documents what is missing to ship issuance. | **Medium** |
| `ENG-162` | Hosted micro-storefront per tenant | Pending | **Depends on `ENG-164`** (hosted-SaaS substrate spike must close first; the public storefront needs a hosted multi-tenant runtime). Plan V3 extension ticket. A per-tenant `shop.puntovivo.app/<slug>` route that shows the tenant catalog + customer ordering + WhatsApp handoff + payment link. Different from `ENG-113` (omnichannel bridge — full marketplace integrations) — this is the lighter "just give me a shareable link" wedge that a merchant can roll out in a day. Depends on ENG-124 payment link contract and ENG-144 WA handoff. | A merchant activates the micro-storefront, picks the catalog visibility per product, the public URL renders the catalog, and a customer order arrives in the POS as a draft sale; orders preserve idempotency + audit + fiscal boundaries. | **Medium** |
| `ENG-163` | Compliance readiness pack (PCI DSS SAQ-A, SOC 2 Type 1, ISO 27001 evidence) | Pending | Plan V3 extension ticket. Documented control set, evidence collection scripts, residency policy linkage (`ENG-159`), retention enforcement linkage (`ENG-129`), and a public trust portal stub. Aggregates ENG-129 + ENG-159 + ENG-148 + ENG-135 + ENG-136 + ENG-151 into a single answerable pack for enterprise pilots that ask for PCI / SOC 2 / ISO 27001 evidence before signing. | `docs/COMPLIANCE.md` enumerates each control with evidence path; the evidence-collection script runs in CI; a trust-portal stub renders the latest pack; the pack answers a SAQ-A questionnaire from existing artifacts. | **Medium** |
| `ENG-164` | Spike: hosted SaaS deployment substrate (Postgres vs SQLite vs hybrid) | Pending | Plan V3 extension spike. Decide the data substrate for the hosted SaaS deployment that `ENG-157` (demo sandbox), `ENG-158` (tenant clone), `ENG-162` (hosted micro-storefront), and the cross-tenant aggregate slice of `ENG-138` (billing back-office) implicitly require. Today Puntovivo is Electron + local SQLite with `tenant_id` row scoping; `STACK-EVOLUTION.md` Phase β mentions a central server as future and no ENG ticket covers the architectural decision. Evaluate three options: **(a) SQLite-everywhere** (better-sqlite3 or libSQL/Turso) — same Drizzle dialect local + cloud, simpler schema parity, weaker concurrent writers (writes serialize per DB), cross-tenant analytics requires joins across files or a secondary store; **(b) Postgres in cloud** — mature multi-tenancy patterns (RLS / schema-per-tenant / DB-per-tenant), better analytics + concurrent writes, deal-breaker risks are schema drift (local SQLite ↔ hosted Postgres) and the Drizzle multi-dialect refactor cost (every `sqliteTable` → `pgTable` with new defaults / timestamp handling), requires testcontainers test rig; **(c) hybrid** — SQLite local-authoritative (ADR-0001 preserved), Postgres mirror for hosted-only features fed via outbox replay or libSQL embedded replicas, protects local-first but doubles operational surface. The spike defines the use-case matrix (which hosted feature needs which capability), tenant isolation pattern, schema parity strategy, Drizzle multi-dialect cost, operational pricing comparison (managed Postgres on Aurora/RDS/Neon/Supabase vs libSQL/Turso vs SQLite on VM), and test ergonomics. Output is `docs/SPIKE-HOSTED-DEPLOYMENT.md` with a recommendation; the recommendation may spawn implementation sub-tickets (`ENG-164a`, `ENG-164b`, etc.) or land as Defer like `ENG-037`. **Blocks**: `ENG-157`, `ENG-158`, `ENG-162`, the cross-tenant aggregate slice of `ENG-138`. Pairs with deferred `ENG-037`. | A recommendation doc that picks one of the three options (or Defer) with explicit rationale, plus spawned implementation tickets matching the choice; the doc cites use-case matrix, tenant isolation pattern, schema parity strategy, Drizzle multi-dialect cost, operational pricing, and test ergonomics. | **High** |
| `ENG-165` | tRPC-aware rate limiting + per-tenant/site/user buckets | Shipped | Plan V3 extension ticket promoted from `BACKLOG.md` (`[security][infra][trpc]` item, 2026-04-29). Replace the single global 100/min/IP Fastify rate limit with tRPC-aware buckets: keep strict auth buckets, add tenant/site/user-scoped buckets for sales mutations, separate read vs write traffic, separate public-API traffic (`ENG-118`), keep env overrides per deployment. Rate-limit hits produce auditable events tied to the offending tenant + site + user. The current global default is a useful safety net but throttles legitimate high-demand stores behind one NAT and offers no defense against an abusive public-API key. Required prerequisite for `ENG-118` (public API) — without it a partner integration on a shared NAT can DOS one tenant. Implementation uses the same Fastify plugin pattern already in place; new tRPC middleware reads the bucket per procedure shape. **Shipped 2026-06-13**: new `bucketRateLimit` middleware runs on the base `publicProcedure` (so every tRPC call is bucketed) and classifies by path + type + auth into differentiated buckets — `auth.*` SKIPPED (the existing `procedureRateLimit` + DB-backed `loginRateLimit` are the strict auth buckets); authed `sales.*` mutation → `sales-write` (tenant+site+user); other authed mutation → `write`; authed query → `read`; unauthenticated non-auth → `public` (IP); `publicApi.*` → `public-api` (ready for ENG-118, no routes yet). All limits env-tunable (`PUNTOVIVO_RATE_LIMIT_{SALES_WRITE,WRITE,READ,PUBLIC,PUBLIC_API}_{MAX,WINDOW_MS}`) with generous defaults (sales-write 240/min, read 600/min, write 120/min, public 60/min). Reuses `procedureRateLimit`'s in-memory store (extended with `tenantId`/`siteId` key dimensions + a once-per-window first-denial signal) and its test bypass, so the full suite + Playwright stay green. A bucket hit writes ONE `systemAuditLogs` row per window (`action:rate_limit.exceeded`, offending tenant/site/user/ip in metadata — server-only, no web i18n) and throws `TOO_MANY_REQUESTS`. The global `@fastify/rate-limit` stays as the coarse cross-route DOS backstop. New `bucket-rate-limit.test.ts` (classifier + middleware denial + audit-once + independence) + tenant cases in `procedure-rate-limit.test.ts`; `ci:server` green; server-only. | Admin can configure per-bucket limits per env; sales mutations have their own bucket independent from auth attempts; public-API calls have a separate bucket; rate-limit hits produce auditable events; tests cover bucket exhaustion + reset behavior. | **High** |
| `ENG-166` | Security hardening — 2026-Q2/Q3 audit closure | Shipped | Bundle of medium/high findings from the 2026-05-24 audit. **Shipped**: CSPRNG SSE id; `@fastify/helmet` (full CSP + frame-ancestors) mirrored to web + Electron; refresh/realtime cookies `sameSite: 'strict'`; constant-time login not-found branch + `security/passwords.ts` (argon2id OWASP-2025 floor + lazy rehash); `emailField()` normalize + `.strict()` on auth/users/payments/sales inputs; scoped `trustProxy`; a table-driven `procedureRateLimit` middleware; a print-HTML sanitizer; DevTools gated on `!app.isPackaged`; 9 tests + SECURITY.md. ENG-168 stays separate. | Every finding has a regression test (response-header asserts, sanitiser unit tests, timing-equality mock, Argon2 wrapper params test, email normalisation test, `.strict()` reject test); `npm audit --omit=dev` stays at 0 high; the audit doc updates each item to "closed". | **Critical** |
| `ENG-167` | SQLite encryption at rest (SQLCipher) | Partial | Local DB at `app.getPath('userData')/data/local.db` carries every sale, customer, fiscal document, and Argon2 hash unencrypted (`apps/desktop/src/main/index.ts:110` + `packages/server/src/db/index.ts`). Evaluate `@journeyapps/sqlcipher` vs a SQLCipher build of `better-sqlite3` vs app-layer encryption on backup only; pick one and ship. Key lifecycle uses Electron `safeStorage` (macOS Keychain, Windows DPAPI, libsecret); never persisted to disk in cleartext. One-shot encryption migration on first launch of the upgraded build; new installs are encrypted by default. Re-validate the ENG-005 desktop CI matrix on Linux/macOS/Windows because SQLCipher ships its own native binary. Backup tooling (`apps/desktop/src/main/backup/`) writes encrypted bundles; restore prompts for the key when the source device differs. Pairs with ENG-174 (PRAGMA tuning) — both touch the DB open path. Document the threat model in `SECURITY.md`: defends against device theft and disk side-channel; does **not** defend against a running process with the unlocked key. Full design in [AUDIT-2026-05-24.md §ENG-167](./AUDIT-2026-05-24.md). **Shipped (Step-1 2026-05-25)**: swapped `better-sqlite3` to the `better-sqlite3-multiple-ciphers@^12.10.0` alias (prebuilds cover Node v137 + Electron v145 on every OS+arch); added `DatabaseOptions.encryptionKey` + `ServerOptions.encryptionKey` that issue `PRAGMA cipher='sqlcipher'`, `PRAGMA legacy=4`, and `PRAGMA key` before the ENG-174 cluster (skipped on `:memory:`); new `apps/desktop/src/main/db-key-store.ts` mints + seals the per-install 32-byte key via Electron `safeStorage` at `<userData>/data/.dbkey.enc` and aborts boot when `isEncryptionAvailable()` is false; new threat-model section in [`SECURITY.md` §"Database encryption at rest (ENG-167 Step-1)"](./SECURITY.md) + open-path map in [`ARCHITECTURE.md` §"Database open path + encryption (ENG-167)"](./ARCHITECTURE.md); regression suites pin every branch (`packages/server/src/__tests__/db-encryption.test.ts` + `apps/desktop/src/main/__tests__/db-key-store.test.ts`); `scripts/ensure-native-runtime.mjs` cache key now includes the package name so the swap invalidates stale plain-better-sqlite3 binaries. **Shipped (ENG-167b 2026-06-11 — cleartext migration + cross-device restore; closes the production gate)**: new `apps/desktop/src/main/db-migrate-encryption.ts` runs between key resolution and `createServer` — a pre-Step-1 cleartext `local.db` (detected by its readable SQLite header; SQLCipher encrypts page 1 so the check is a pure 16-byte read reusing `detectBackupFormat`) is WAL-checkpointed, copied to a temporary `.pre-encryption.bak`, encrypted IN PLACE via `PRAGMA rekey` (verified empirically against the mc fork), integrity-verified, and the `.bak` deleted (leaving it would bypass the at-rest model); a failed verification restores the original byte-for-byte and aborts the boot; the dev-shared `DATABASE_URL` route is excluded. Cross-device restore: when the staged bundle fails the LOCAL key, a legacy cleartext bundle restores directly (the next boot encrypts it) and a foreign-key bundle returns `{needsKey, token}` to the renderer, whose new key-prompt modal completes via the `provide-restore-key` IPC — the staged DB is verified with the source key, REKEYED in staging to the local key (one envelope per install), re-verified, and swapped; wrong keys keep the staging for retry. New admin-gated "View backup key" reveal (`get-backup-encryption-key` IPC, warning-confirmed, logged) makes the prompt operable; trade-off documented in `SECURITY.md` §ENG-167b. New main-i18n keys (en/es) + ~20 renderer settings keys (en/es, neutral LATAM). Tests: new `db-migrate-encryption.test.ts` (7 cases, real DB files: migrate/idempotent/fresh/skip/stale-bak cleanup/rollback) + 3 helper cases in `backup-restore.test.ts` (cleartext detection, cross-key rekey, malformed-key no-op) + 4 renderer cases (prompt flow, client-side shape gate, mismatch retry, gated reveal). The settings i18n chunk budget lifted 8→9 kB gz and CompanyPage 30→33 kB gz (deliberate copy/route growth, perf-budget $retune7/$retune8). **Remaining**: cross-OS validation through the manual `build-desktop.yml` matrix on Linux + macOS + Windows (operator-run); the standalone dev server keeps its cleartext fallback by design. | Packaged desktop build ships SQLCipher; the local DB cannot be opened with the standard sqlite3 CLI without the key; backup + restore round-trip succeeds; ENG-005 desktop CI matrix stays green on all three OSes; key resides only in the OS keychain. | **High** |
| `ENG-168` | Token + session lifecycle hardening | Shipped | Session-management gaps from the 2026-05-24 audit that erode the ENG-008b + ENG-025 invariants. **Shipped**: realtime token TTL 120s→900s + a 10-min `token-refresh-needed` SSE event (re-mints without dropping the socket); `authority.revokeDevice` bumps `sessionVersion` in-tx; `logout` clears the Electron `desktopSession` via IPC; a `loginAttemptsCleanup` worker sweeps stale rows hourly with a global audit row; `claimPairingCodeForDevice` writes a masked `device.pairing.claimed` audit row; tests + i18n. | SSE connection survives >24 h with continuous refresh; revoking a device disables every bound token on the next request; post-logout `db:*` IPC calls fail with `UNAUTHORIZED` (regression test); expired login attempts purged after 24 h; cleanup writes a global audit row per run; pairing claim writes an audit row. | **High** |
| `ENG-169` | Input boundary hygiene | Shipped | Lower-severity boundary findings from the 2026-05-24 audit ([AUDIT-2026-05-24.md §ENG-169](./AUDIT-2026-05-24.md)) that have a low fix cost but consistently leak surface. (1) `handleRestoreDatabaseBackup` at `apps/desktop/src/main/index.ts:1742-1768` accepts any ZIP and processes every entry — enforce a path allowlist (`local.db`, `device-id`, `manifest.json`), reject `..` and absolute paths, optionally require a manifest signature from ENG-167. (2) Re-audit every Zod schema that accepts an image URL (`companies.logoDataUrl`, `receipt_templates`, future `users.avatar`) and call `assertSafeImageUrl()` from `lib/urlSafety.ts` in the schema-level `refine` rather than at render time. (3) Strip whitespace and lowercase emails via Zod `transform` everywhere `trpc/schemas/users.ts:14` and equivalents accept an email. (4) Refuse to boot with `verbose === true && NODE_ENV === 'production'` unless `PUNTOVIVO_ALLOW_VERBOSE_PROD=1` is set, and log a structured warning when the override fires. **Shipped 2026-06-13**: (1) `extractBackupBundle` now gates the whole archive against an allowlist (`local.db` / `device-id.txt` / `manifest.json`) and rejects any entry with `..` / absolute / backslash before writing anything — previously such entries were silently ignored, never written. (2) `companies.logoUrl` + `products.imageUrl` (create + update) route through `isUrlSchemeBlocked` in a schema-level `.refine` (mirrors the existing `logos.ts` `safeImageUrl`); product imageUrl is refine-only so relative + `data:image` values still validate. (3) `companies` / `customers` / `providers` email fields swapped raw `.email()` for the existing `emailField()` (trim + lowercase) — users/auth already used it; lookups consume the parsed value so no case-match break. (4) `createServer` refuses to boot when `verbose && NODE_ENV=production` unless `PUNTOVIVO_ALLOW_VERBOSE_PROD=1`, warning when the override fires. Tests: new `eng169-input-hygiene.test.ts` (image-scheme + email + boot-guard) + 3 cases in `backup-restore.test.ts` (traversal + extra-entry rejection, realigned missing-`local.db`). Manifest-signing left out (no signing infra). Closes the audit-closure wave (W5). | All four bullets have regression tests; restore rejects a planted ZIP with `..` in entry names; verbose-mode prod boot is blocked without the override. | **Medium** |
| `ENG-170` | Web bundle optimisation | Shipped | 2026-05-24 frontend perf audit: gate prod sourcemaps off, lazy-load non-bootstrap i18n namespaces, add manual chunks, and disable export buttons until the chunk lands. **Shipped**: prod sourcemaps off across web + desktop; `manualChunks` splits pdf/xlsx/codemirror/dnd out of the entry (index ~152 KB gz, budget tightened); export-button disabling already satisfied by `useTableExport` (no new hook); the 30 non-bootstrap namespaces lazy-load via `i18next-resources-to-backend` + non-eager `import.meta.glob` (Electron `file://`-safe; ENG-170b), keeping 7 chrome namespaces eager; live smoke. | Main entry chunk gzip below `perf-budget.json` ceiling; `pdf` / `xlsx` / `codemirror` chunks load only on routes that need them (verified via `vite build --report`); login bundle no longer contains `fiscal` / `kds` / `aiSettings` namespaces. | **Medium** |
| `ENG-171` | React re-render + data-fetching hygiene | Shipped | 2026-05-24 frontend perf audit: memoize provider values, promote auth lambdas to `useCallback`, disable window-focus refetch, add `keepPreviousData` + hover-prefetch on `/sales`, and move Modules/Locale providers to Zustand. **Shipped**: `TenantProvider` memoizes `sites` (root instability) + value; `AuthProvider` `login`/`logout` to `useCallback` + memoized value; `refetchOnWindowFocus: false` on Sidebar + anomaly queries; `keepPreviousData` on 4 SalesPage queries + `usePrefetchSales`; Modules/Locale migrated to Zustand sync hooks keeping consumer signatures; tests. Unblocks ENG-178. | React DevTools Profiler shows the `SalesPage` header does not re-render when the cart updates; window-focus refetch disabled on the named queries; navigation from `/dashboard` to `/sales` no longer shows a blank shell. | **Medium** |
| `ENG-172` | DataTable virtualisation + CLS polish | Shipped | 2026-05-24 frontend perf audit: virtualise DataTable, paginate fiscal documents, and fix CLS on logo images. **Shipped**: additive `virtualised?` prop (auto-flip >30 rows) wires `@tanstack/react-virtual` via spacer rows, preserving `data-row-id` + roving-tabindex; `FiscalDocumentListPage` paginates `offset` from a `pageIndex` (page 20, `keepPreviousData`); explicit `width`/`height` on logo `<img>`; date-derived DatePicker keys; budget retune + single-React dedupe pin; follow-ups in BACKLOG. | Large DataTables scroll at 60 fps on the reference low-end laptop with a ≥10 k-row fixture; `FiscalDocumentListPage` paginates beyond 50 rows; Lighthouse CLS drops below 0.02 on the Company logo screen. | **Medium** |
| `ENG-173` | Web Vitals real-user monitoring | Shipped | Measure LCP/INP/CLS/TTFB/FCP via `web-vitals` and report to a new `observability.reportWebVital` mutation storing `web_vital_samples` rows; sample 100% dev / 10% prod; PERF-BUDGETS per-route targets; dashboard out of scope. **Shipped**: migration `0041` (nullable `tenant_id`, `tenant_plan` placeholder); `observability` router (`reportWebVital` public, telemetry-opt-in-gated, tenant server-derived + `recentWebVitals` managerOrAdmin); `installWebVitalsReporter()` in `main.tsx` with device-class bucketing; PERF-BUDGETS RUM section; 13 tests + smoke. Closes the W3 wave. | A packaged web build sends one `reportWebVital` per metric per page load; rows visible in the DB; per-tenant medians + p95 retrievable for the future dashboard. | **Low** |
| `ENG-174` | SQLite PRAGMA tuning + WAL backup safety | Shipped | Wave 2 entry, three sub-changes in one slice. **Shipped**: (1) an expanded PRAGMA cluster in `db/index.ts` (`busy_timeout`/`cache_size`/`temp_store` everywhere, `mmap_size`/`wal_autocheckpoint` on file DBs) sized to the 4 GB floor; (2) `createBackupBundle` does `wal_checkpoint(FULL)` + `synchronous = FULL` before the readonly snapshot, with integrity check after; (3) `scripts/ensure-migrations-bundled.mjs` verifies the bundled migrations + journal at preflight; 3 test files (12 cases) + SECURITY.md. | PRAGMA values pinned in a unit test against the production-config code path; backup + power-loss simulation produces a valid restore (chaos harness from ENG-067 if available); a build with a missing `migrations/` resources entry fails preflight with an actionable error. | **High** |
| `ENG-175` | Composite indexes for hot paths | Shipped | Wave 2 mid-piece bundled with ENG-175b. **Shipped**: migration `0034` adds six composites (`audit_logs` ×2, `inventory_movements`, `operation_events`, `quotations`) + widens `sync_outbox.entity` to include `status`; `restaurant_tables` partial-unique now declared in `schema.ts`; a 6-case `db-composite-indexes.test.ts` pins names/columns/WHERE; the `users` LIKE swap deferred to BACKLOG. | `EXPLAIN QUERY PLAN` on the listing routers for audit logs, inventory movements, cash sessions, restaurant tables, operation events, and quotations uses the new indexes; the `seed-dev.test.ts` large preset benchmark regresses no slower than baseline; user / customer / product autocompletes complete a 10 k-row fixture in <50 ms. | **High** |
| `ENG-175b` | Foreign-key `onDelete` policy review | Partial | Companion to ENG-175 ([AUDIT-2026-05-24.md §ENG-175b](./AUDIT-2026-05-24.md)). Default RESTRICT surfaces as confusing UX. **Shipped 2026-05-24**: explicit policy documented in `docs/ARCHITECTURE.md > FK onDelete policy` (cascade for parent-of-items, set null for optional context pointers, restrict for cross-aggregate references + multi-tenant invariant + audit immutability). Verified that every cascade-needed and set-null-needed relation already carries the explicit `onDelete` in `schema.ts` (sale_items, quotation_items, purchase_items, purchase_return_items, order_items, transfer_order_items, fiscal_document_items, sale_payments all cascade on parent delete; sync_outbox.device_id sets null). New `db-fk-policy.test.ts` (3 cases) pins the runtime behaviour: sale → sale_items cascade, tenant → audit_logs blocked by RESTRICT, user → audit_logs blocked by RESTRICT. **Remaining**: literal "every reference() declares explicit onDelete" — the ~140 implicit-RESTRICT relations (sales.customerId, products.categoryId, every `*.tenantId → tenants`, every `*.siteId → sites`, every `*.createdBy → users`) keep their default behaviour. Switching each to an explicit `onDelete: 'restrict'` would trigger ~140 drizzle-kit table recreations with zero runtime change (SQLite treats `NO ACTION` and `RESTRICT` identically). The operator can promote that to a follow-up if literal compliance is required; today the policy doc is the explicit declaration for the implicit-RESTRICT majority. | Each FK relation in `schema.ts` declares an explicit `onDelete`; the policy is documented; ENG-175 indexes do not regress; tests cover at least one happy-path delete per cascade rule. | **Medium** |
| `ENG-176` | Money model + multi-currency LATAM readiness | Shipped | Four schema-prep gaps from [AUDIT-2026-05-24.md §ENG-176](./AUDIT-2026-05-24.md) blocking ENG-156 (multi-currency) + ENG-161 (NFe Brazil): (1) money precision, (2) a `currency_code` seam on transactional tables, (3) the DIAN-hardcoded identification catalog, (4) the closed `fiscal_documents.status` enum. **Shipped** (a/b/c): ADR-0009 keeps `real` columns + `_nonneg`/`_2dec` CHECK invariants (migrations `0035`/`0036`); canonical `lib/money.ts::roundMoney` consolidates six duplicates and is applied at money write boundaries (see ENG-176a in the AGENTS.md marker index); migration `0037` adds the `currency_code` + `exchange_rate_at_sale` + nullable `settle_currency_code` seam plus `tenants.default_currency_code`; migration `0038` renames `dian_identification_types` → `fiscal_identification_types` (composite `(country_code, code)`, seeds CO/MX/PE/CL) and expands the status enum to 8 states. ENG-156 + ENG-161 can now begin. | Money columns pinned by a CHECK invariant; `currency_code` present and non-null on every transactional row, defaulting to the tenant locale; `fiscal_identification_types` covers CO + MX + PE + CL; status enum extends to at least the SAT + SUNAT set; new tests cover a USD-sold / COP-settled sale round-trip. | **High** |
| `ENG-177` | Optimistic versioning + soft-delete policy | Partial | Three reliability gaps from [AUDIT-2026-05-24.md §ENG-177](./AUDIT-2026-05-24.md). (1) Only `users.sessionVersion` exists today; `products`, `customers`, `providers`, `categories`, `sequentials`, `tenant_locale_settings` have no optimistic-concurrency guard — two operators editing a price from different tabs overwrite each other silently. Add `version: integer().notNull().default(0)` to every user-editable catalog, increment on UPDATE, and have a tRPC middleware throw `STALE_VERSION` when the client-supplied previous version diverges. (2) Soft-delete is inconsistent: catalogs use `is_active`, transactions use a `status` enum, neither captures who/when. Decide: introduce `deleted_at` + `deleted_by_user_id` on catalogs (preserving `is_active` as a derived view for one release) or remove soft delete on the catalogs that do not need it. Document the policy in `ARCHITECTURE.md`. (3) `services/inventory-balances.ts:81-120` seeds `inventory_balances` with an O(N) `forEach` insert loop — replace with a single `db.insert().values(rows)` chunked under `SQLITE_MAX_VARIABLE_NUMBER` (999), so site onboarding on a 50 k-product tenant drops from >1 s lock to <100 ms. (4) Add a `CHECK (cash_session_id IS NOT NULL OR status = 'draft')` constraint on `sales` once a migration shim verifies historical rows comply — today the `requireActiveCashSession` invariant lives in application code only. **Shipped slice a 2026-05-28 (ENG-177a)**: optimistic versioning (part 1) + the inventory-balance batch insert (part 3). Added a `version INTEGER NOT NULL DEFAULT 0` column (migration `0039_eng177a_catalog_version.sql`, pure additive) to `products` / `customers` / `providers` / `categories` / `tenant_locale_settings`; `sequentials` intentionally excluded (atomically incremented operational counter, not a two-tab edit surface). Each `*.update` procedure now requires a `version` input, runs the UPDATE with `... AND version = ?` + `version = supplied + 1`, and throws the new `STALE_VERSION` (`CONFLICT`) error code via the shared `lib/optimisticVersion.ts::assertVersionedWriteApplied` helper when the write changes zero rows — race-free because a single better-sqlite3 UPDATE is atomic. Web DTOs carry `version`; the five catalog edit forms round-trip it and invalidate the cached list/row on `STALE_VERSION`. `ResolvedLocale.version` surfaced so the admin locale card threads it through the upsert. `services/inventory-balances.ts::ensureInventoryBalancesForSite` replaced its O(N) per-row `forEach` insert with a chunked `.values([...]).onConflictDoNothing` insert (90 rows/chunk under SQLITE_MAX_VARIABLE_NUMBER) — byte-identical seed semantics, >10× faster on large catalogs. New `optimistic-version.test.ts` (8 cases) + `inventory-balances-seed.test.ts` (3 cases, multi-chunk boundary) + web `translateServerError` STALE_VERSION cases; `STALE_VERSION` localized en + es. ADR-0004 cited (mutation-layer guard, complementary to sync-layer auto-LWW), ARCHITECTURE.md extended. **Shipped slice c 2026-06-12 (ENG-177c — part 4)**: added `chk_sales_cash_session_or_draft` (`CHECK (cash_session_id IS NOT NULL OR status = 'draft')`) to `sales` via Drizzle table-rebuild migration `0001_eng177c_sales_cash_session_check.sql`, so the cash-session invariant (ENG-042/055) is enforced at the storage layer, not just in application code. A SQLite CHECK-add forces a full table rebuild, and drizzle-orm wraps every migration in one transaction where `PRAGMA foreign_keys` is a no-op — so `db/index.ts` now brackets the migrate span with connection-level `foreign_keys = OFF` (empirically the only lever that stops `DROP TABLE sales` from cascade-deleting `sale_items`/`sale_payments`/`sale_returns`/`kds_orders`), then restores enforcement and runs a `foreign_key_check` integrity gate. No historical-compliance shim was needed: both `sales` INSERT sites already bind a session (even drafts), so every existing row complies by construction. New `sales-cash-session-constraint.test.ts` (4 cases: AC enforcement bypassing the app layer + a positive/negative FK-safe-rebuild mechanism control pinning the bracket). **Remaining: ENG-177b** soft-delete policy (`deleted_at` + `deleted_by_user_id` decision, part 2) — captured in BACKLOG. | Concurrent edit from two browsers raises `STALE_VERSION`; soft-delete policy doc landed; site-onboarding benchmark improves >10× on the 50 k-product fixture; the new CHECK constraint is pinned by a unit test that bypasses the application layer. | **Medium** |
| `ENG-178` | Megafile decomposition wave 2 | Pending | ENG-029 captured the megafile list and deferred splits to "next time touched"; several files now generate merge conflicts on every PR ([AUDIT-2026-05-24.md §ENG-178](./AUDIT-2026-05-24.md)). Decompose proactively in this order, one self-contained commit per file: `packages/server/src/db/schema.ts` (4 879 LOC) into `schema/sales.ts`, `schema/inventory.ts`, `schema/fiscal.ts`, `schema/auth.ts`, `schema/audit.ts`, `schema/realtime.ts`, `schema/ai.ts`, `schema/catalogs.ts` re-exported from `schema/index.ts`; `apps/desktop/src/main/index.ts` (2 166 LOC) moves every `ipcMain.handle` to `main/ipc/{db,sync,print,auth,session,backup}.ts` with a register helper; `trpc/routers/sales.ts` (1 469 LOC) + `purchases.ts` (1 700 LOC) extract every procedure into `application/<domain>/<action>.ts` mirroring the existing `completeSale.ts` pattern; `application/sales/completeSale.ts` (1 511 LOC) splits into `runFreshSale.ts`, `runCompleteDraft.ts`, `pricing.ts`, `creditPolicy.ts`, `fiscalPostHook.ts`; `apps/web/src/features/sales/SalesPage.tsx` (1 599 LOC) extracts `useSalesMutations`, `useSalesCartOrchestrator`, `ReceiptPrintController` and splits JSX into `SalesCartSection`, `SalesCheckoutSection`, `CashSessionSection`, `SuspendedSalesSection`; `features/receipt-templates/ReceiptTemplateEditor.tsx` (1 296 LOC) extracts `BlockLibrary`, `PropertyPanel`, `PreviewRenderer` and adopts `useReducer`; `features/products/ProductFormModal.tsx` (1 222 LOC) extracts `useProductForm`, `ProductFormTabs`, `AISuggestionsPanel`; `services/inventory-transfers.ts` (1 144 LOC) extracts `validateTransferInput`, `resolveTransferSites`, `applyTransferMovements`; `services/ai/anomalyDetection.ts` (928 LOC) splits per detector strategy. Each split keeps tests green; the megafile list in ENG-029 closes out. | No source file (excluding tests, lockfiles, generated files) exceeds 500 LOC after the ticket lands; `ci:web` / `ci:server` / `ci:desktop` stay green; ENG-029 closes. | **Medium** |
| `ENG-179` | TS strict + lint upgrade + DTO consolidation | Shipped | From [AUDIT-2026-05-24.md §ENG-179](./AUDIT-2026-05-24.md): enable `noUncheckedIndexedAccess` / `exactOptionalPropertyTypes` / `noImplicitOverride`; promote `@typescript-eslint/no-explicit-any` to `error`; replace the `(ctx as unknown as { envelope? })` double-cast with a typed `CriticalCommandContext`; split `apps/web/src/types/index.ts` and promote DTOs to `inferRouterOutputs`. **Shipped** (a/b/c): the three strict flags enabled across server/web/desktop (~98 + ~213 surfaced sites fixed); `no-explicit-any` promoted to error (production `as any` down to 1 documented Drizzle generic-table boundary via the outbox `AnyBuilder` helpers); the envelope double-cast replaced by `asCriticalCommandContext`; `types/index.ts` split into `domain`/`ui`/`api` (re-export shim kept one release; wholesale DTO migration deferred to BACKLOG). `ARCHITECTURE.md` documents the strict-mode floor. | Every flag enabled with no remaining `@ts-ignore` / `@ts-expect-error` lacking a `// reason:` comment; `as any` drops to <5 in production code (seeds + fixtures exempted with documentation); `types/index.ts` removed bar a one-release re-export shim. | **Medium** |
| `ENG-180` | Critical-logic documentation pack | Shipped | Documentation pass on the load-bearing operations for fiscal correctness, audit defensibility, and restore reliability ([AUDIT-2026-05-24.md §ENG-180](./AUDIT-2026-05-24.md)). No code change. `services/fiscal/orchestrator.ts`: per-function JSDoc with `Preconditions:` / `Postconditions:` / `Invariants:` on `emitFiscalDocument`, `advanceSequential`, and the snapshot-before-write rule on customer + line snapshots, plus the reason the sequential advances inside the transaction. `services/cash-session.ts`: every public function (`createCashSession`, `closeCashSession`, `recordCashMovement`) gets an `Invariants:` block (one active session per cashier, recorded movements balance against the cash-drawer count, `notes` is operator-facing and never auto-translated). `application/sales/completeSale.ts`: document the rounding rule per country (Colombia half-away-from-zero at 2 decimals; Chile integer peso; Peru ICBPER per-bag) with a citation to the local tax authority; pair with ENG-181 to centralise the helpers. `services/fiscal/packs/cl/mappings.ts:167` `roundClp`: JSDoc the integer-only invariant + SII rejection rule. `AGENTS.md`: add an "ENG-NNN comment index" appendix mapping the 355 `// ENG-XXX` markers to the invariants they pin, so the markers do not drift into noise when tickets close. **Shipped** (2026-05-28): docs-only, zero runtime change. Structured `Invariants:` / `Preconditions:` / `Postconditions:` docblocks on the fiscal orchestrator (`emitFiscalDocument` / `enqueueFiscalEmission` / `safelyEmitFiscalDocument` — idempotency by `(tenantId, source, sourceId, kind)`, immutable buyer+line snapshot, in-tx sequential advance with the `FISCAL_SEQUENTIAL_NOT_ADVANCED` TOCTOU guard, best-effort post-commit semantics), the cash-session primitives + the three `application/cash-sessions/` use-cases, the `completeSale` top-level helpers, and `roundClp`. AGENTS.md gained the curated `// ENG-NNN` marker index + convention. Two AC corrections recorded as reality: (a) the AC's per-country rounding (CO 2-dec / CL integer / PE ICBPER) is **NOT implemented** — the money path rounds uniform 2-decimal `roundMoney()` for every country; the only integer rounding is `roundClp`, XML-serializer-only — documented as future work, not present; (b) the marker scale is ~2,900 occurrences / ~160 distinct ids (the AC's 355 was an estimate), so the index is curated to the load-bearing markers, not exhaustive. Cash-session functions documented at their real post-ENG-056 homes (`services/cash-session.ts` primitives + `application/cash-sessions/`), not the AC's `createCashSession`/`advanceSequential` names. | Every function in `services/fiscal/orchestrator.ts`, `services/cash-session.ts`, and the top-level helpers of `application/sales/completeSale.ts` carries a docblock with `Invariants:`, `Preconditions:`, `Postconditions:`, and external references where applicable; the ENG-NNN comment index lands. | **Medium** |
| `ENG-181` | Error code + error wrapping cleanup | Shipped | The codebase mixes raw `throw new Error('…')` with the `throwServerError(code, message)` helper from ENG-006 — only the latter survives the `onErrorToast` translation pipeline ([AUDIT-2026-05-24.md §ENG-181](./AUDIT-2026-05-24.md)). (1) Audit and replace every literal-string throw in `packages/server/src/services/**` and `packages/server/src/application/**` with `throwServerError(code, message)`. Start with the highest-traffic sites: `services/cash-session.ts:129,140`, `services/fiscal/orchestrator.ts:593,984`, and the body of `services/inventory-transfers.ts`. (2) Attach context via `new Error(message, { cause: { tenantId, siteId, documentType, sequentialId, … } })` and extend the pino redact policy to preserve `cause` while still redacting `password` / `token`. (3) On the frontend, audit `apps/web/src/features/**` for inline `onError: (err) => toast(...)` that bypasses `onErrorToast`; add an ESLint `no-restricted-syntax` rule that blocks the pattern. (4) Add an i18n parity test that fails when a server-emitted error code has no matching key under `errors.json` in both `en` and `es`. **Shipped**: split the 39 literal throws into Categoría A (14 user-facing → `throwServerError` with 8 new `SERVER_ERROR_CODES` + 17 new i18n keys across en+es) vs Categoría B (25 programmer-assert helpers wrapped with structured `cause: { country, document, missing, tenantId, … }` context); extended pino `REDACT_PATHS` so `cause.tenantId / siteId / errorCode` stay visible while `cause.password / token / refreshToken / email` are censored; added `no-restricted-syntax` lint rule blocking `onError: (err) => toast.*(…)` (refactored 4 violating call sites — AiConfigPage, SequentialsPage, PosTouchScreen, CompanyTelemetryCard); landed `error-codes-coverage.test.ts` that asserts every `SERVER_ERROR_CODES` value has a matching `errors.server.<CODE>` key in both locales (+ orphan + cardinality sentinels); extended `logger.test.ts` with cause-redaction coverage; ARCHITECTURE.md gained an "Error code policy" section documenting the Categoría A vs B split. | Zero `throw new Error(<string-literal>)` in `services/` and `application/` that does not pass through `throwServerError`; the new parity test fails on a planted missing key; the mutation-site lint catches a planted inline-onError regression in a unit test. | **Medium** |
| `ENG-182` | Product doctrine and README truth reset | Shipped | Focus-reset docs ticket. Rewrite the root README and sync the related status docs so the first thing a contributor or operator sees matches the current product reality: Puntovivo is a local-first, fiscal-native LATAM POS for 1-10 site retail operators first, not a generic mini-ERP or cloud-only suite. Replace stale npm/Node 22 commands with pnpm 11 + Node 24, remove the "tRPC in progress" claim, distinguish demo / private pilot / production readiness, list explicit non-targets and parked modules, and link the go/no-go checklist back to `SELLABILITY.md`, `PLAN-V3.md`, and ROADMAP §0. **Shipped 2026-06-01**: the README rewrite to retail-first doctrine + pnpm 11 / Node 24 quick start + demo/pilot/production status table + parked-features section had already landed in the prior `chore(docs)` sweep (commit `f749672`); this closeout finished the cross-doc linkage the AC named. README now links the go/no-go checklist back to `SELLABILITY.md`, ROADMAP §0, and `PLAN-V3.md` (Current Status block + Documentation Map). ROADMAP §0 carries an explicit `demo=Yes / pilot=Not yet / production=No` stage verdict that mirrors `SELLABILITY.md` (no longer just a non-contradictory link), and `SELLABILITY.md` Roadmap Link back-links `PLAN-V3.md` + ROADMAP §0. Verified no README claim contradicts `AGENTS.md` / `package.json`: Node `>=24`, `pnpm@11.4.0`, `@trpc/server ^11.17.0` so tRPC 11 is canonical with no in-progress claim, Electron 41.7.1, Vite 8, TS 6. Docs-only; gate was `git diff --check` + cross-doc claim audit (no code surface, no live smoke). | README quick start works from a fresh checkout with pnpm + Node 24; no README claim contradicts AGENTS.md, `package.json`, or the current tRPC-first architecture; `SELLABILITY.md` and ROADMAP §0 agree on demo / pilot / production status; parked features are labelled roadmap instead of shipped. | **High** |
| `ENG-182a` | Token-budget cleanup + dead-code hygiene | Shipped | Follow-through to ENG-182: keep the active planning corpus cheap for future LLM passes while removing only confirmed-zero-import code/deps from the codebase review. **Shipped 2026-06-01**: compacted verbose shipped history in ROADMAP / BACKLOG / ARCHIVED without altering any `Pending`, `Partial`, `Gated`, or `Deferred` row; removed stale desktop renderer scaffold, legacy web storage modules, unused UI primitives/exports, `concurrently`, `tinyrainbow`, and `scripts/test-trpc.sh`; centralized the tenant-site guard as `ensureTenantSite`; added `dev:stop` cleanup for orphaned launcher wrappers. Larger design-system/server-refactor ideas remain as dated BACKLOG bullets. | Pending ticket pool remains intact; zero-import checks plus `ci:web` / `ci:server` / `ci:desktop` pass; live `/geography` smoke proves the touched UI/i18n/modal surface still works. | **Low** |
| `ENG-183` | Retail Ring-1 scope gate and module exposure cleanup | Shipped | Product-focus implementation ticket. Convert the "retail first" doctrine into runtime/module behavior: classify modules as Core, Compliance, Optional, or Experimental; keep new tenants on a Ring-1 retail profile by default; preserve existing tenant settings through migration; hide restaurant / KDS / customer display / mobile waiter / delivery / public API / AI surfaces unless the module and tenant profile explicitly enable them; add docs explaining that non-core modules are pulled forward only when a pilot makes them the wedge. **Shipped 2026-06-01**: added a `classification` (core/compliance/optional/experimental) + `ring` to every module descriptor and a derived `RING1_RETAIL_PROFILE` (only the two `core` modules — operations-center + quotations — ON) written into a fresh tenant at creation (`db/seed.ts`); `defaultEnabled` is unchanged so existing tenants keep their choices (no migration — fresh tenants get the profile by construction). Closed the route leak by hoisting role+module gating to `SurfaceShellRoute` so `/touch` `/kds` `/customer-display` `/m` redirect to /dashboard BEFORE the lazy surface bundle loads; the 4 shells are now pure chrome. Server + web tests prove no surface route leaks + RING1 correctness; `MODULE-ACTIVATION.md` documents the class/default/ring of every module id. Also hardened the sidebar / command-palette / workspace-landing gating to hide module-gated entries while the tenant module snapshot is still hydrating (`isPlaceholder`), so a fresh retail tenant never flashes the AI entries (manifest `defaultEnabled` on) before the Ring-1 profile (AI off) loads. | A fresh retail tenant sees only Ring-1 sellability surfaces by default; existing tenants keep their module choices; sidebar/workspace/palette/module tests prove hidden modules do not leak visible routes; docs list the module class and default for every module id. | **High** |
| `ENG-184` | Colombia retail readiness profile + checkout preflight | Shipped | Turn readiness from a generic setup score into a market-profile gate. For a Colombia retail tenant, fiscal configuration, QR/XML retention path, fiscal outbox health, printer/drawer/scanner readiness, payment-terminal policy, sync backlog, backup status, and active cash session must be reflected as blockers or warnings instead of optional decoration. Extend `setupReadiness.get`, `CompanyReadinessCard`, `ReadinessBanner`, and `useCheckoutPreflight` so a cashier sees fiscal / hardware / sync / payment signals before charging, with recovery CTAs to the exact setup surface. **Shipped 2026-06-02 (operator override of the original "blocker" AC — DIAN + receipt hardware are OPTIONAL reminders, never hard blockers; a store must always be able to sell)**: added a CO-profile-aware readiness layer (`services/readiness/{profile,signals}.ts`) — for a Colombia tenant DIAN-off shows an optional reminder, DIAN-on-but-incomplete a warning, complete+healthy ready, transmission errors a warning; added a `sync` readiness section + a `warning` status (0.5 score weight). New cashier-callable `setupReadiness.checkout` (site-scoped via `ensureTenantSite`) feeds warning reminders into `useCheckoutPreflight` behind a strict local-first guard (a slow / errored query never blocks Cobrar; only cash-session / credit / discount still block). Built the basic CO fiscal config card (`services/fiscal/packs/co/settings.ts` + `fiscalSettings.updateCo` + a real `getByCountry` CO branch + `CompanyCoFiscalCard`) replacing the coming-soon placeholder, so an admin can capture NIT / resolution / numbering and toggle DIAN on; the legacy `fiscal_dian_enabled` flag stays the master switch and real CUFE / transmission / certificate stay mock (ENG-021). `ReadinessBanner` is `GlobalStatusStrip` today (driven by `blockerCount`, no change needed). Deferred to BACKLOG: a backup-readiness signal (no backup subsystem exists), real DIAN crypto / transmission validation (ENG-021), and the QR/XML retention-path check. | A Colombia retail tenant surfaces DIAN / receipt-hardware / payment / sync state as optional reminders or warnings — NEVER hard blockers (selling is never stopped by a missing DIAN or a broken printer); the basic CO fiscal config card lets an admin capture NIT / resolution / numbering and turn DIAN on; checkout preflight renders warning reminder rows for fiscal / hardware / payment / sync while Cobrar stays enabled; `ci:web` + `ci:server` pass and a live `/company` + `/sales` smoke proves en/es copy and recovery CTAs. | **High** |
| `ENG-185` | Fiscal adapter truth guard | Shipped | Remove misleading fiscal fallbacks before production claims harden. Unknown countries must fail with an explicit unsupported-country readiness/error state instead of falling back to the Colombia mock adapter. Mock, unsigned, pending, or structurally-valid-only packs must be labelled as draft/demo in readiness, fiscal document views, and diagnostics; production readiness cannot go green until the active country pack has a configured provider/certificate/credential path and a non-mock transmission strategy. **Shipped 2026-06-02 (labels-only; e-invoicing stays optional per operator)**: added a unified `FiscalAdapterMaturity` (`mock` / `draft` / `certified`) on the `FiscalAdapter` interface (CO=mock, MX/CL=draft; no pack ships `certified` yet) + `describeFiscalProvider(providerId)`. The registry no longer falls an unknown country back to the CO mock — `getFiscalAdapter` THROWS `FISCAL_PACK_NOT_AVAILABLE` and the orchestrator guards with `isSupportedFiscalCountry` so an unsupported country skips emission cleanly (the sale still completes, non-fatal — no Colombia-shaped document). Surfaced `maturity` on `fiscalSettings.getByCountry` + `reports.fiscal.list`/`getByCufe`; a reusable `FiscalMaturityBadge` ("Demo"/"Borrador") now labels the fiscal config cards, the fiscal document list, and the Operations fiscal-health diagnostics so nothing reads as production/accepted; the fiscal tab shows an explicit "not available for this country" message for non-CO/MX/CL. No real signing/transmission was built (still gated ENG-021/035c/036c); the ENG-184 readiness score/model is untouched. Server + web tests cover the registry throw, CO mock / MX-CL draft maturity, the orchestrator unsupported-skip, and the document-view labels. Follow-up: surface maturity inside `SaleDetailsFiscalBlock` (its summary does not carry providerId today) — captured in BACKLOG. | Calling the fiscal registry with an unsupported country returns a typed error, not a Colombia-shaped document; tests cover CO mock/demo, MX/CL draft-pending packs, and unsupported-country behavior; UI badges and diagnostics cannot describe unsigned/pending packs as production-ready. | **High** |
| `ENG-186` | Ring-1 screen focus pass (re-scoped: /sales sellability slice) | Shipped | First implementation slice from the product-focus audit, scoped to screens that matter for the initial retail wedge. Make `/sales` complete the common sale path without desktop scroll at 1440x900; move secondary sales history / diagnostics / exports behind drawers or workspace landings. This narrows ENG-132 to the minimum sellability UI pass before more vertical or platform work. **Re-scoped + Shipped 2026-06-03** (operator scoped this pass to the `/sales` sellability slice; the Operations "Needs attention" default and the Setup/`/company` readiness-hub restructure moved to `ENG-187`; surface-launcher module-gating already shipped in `ENG-183`): added a reusable `Drawer` slide-over primitive (`components/feedback/Drawer.tsx` + a `.drawer-shell` component class + a `slide-in-right` keyframe) mirroring `Modal`'s a11y one-for-one (portal, focus-trap, ESC, backdrop, body-scroll-lock, `restoreFocusTo`) — right slide-over at `sm:+`, bottom sheet on mobile; 11 unit tests incl. axe. Bounded the `/sales` POS view to the viewport at `xl` (`xl:h-[calc(100dvh-12.75rem)]` flex column + `xl:grid-rows-[minmax(0,1fr)]`) so the cart line-item list and the checkout panel scroll internally and the common sale completes without page scroll at 1440x900; mobile keeps normal flow + the fixed checkout bar (bounding is `xl:`-only). Removed the `POS | History` segmented control and the inline suspended `<aside>` (the scroll offender) — History + Suspended now open as drawers from two action-row buttons; Ctrl/Cmd+R still opens Suspended; `restoreFocusTo` returns focus to the product search input on close (ENG-105f cashier-speed seam). Removed orphaned `view.pos` / `page.viewToggleAriaLabel` i18n keys (en+es), reusing `view.history` / `park.panelTitle` for the new labels. `ci:web` green (1539 tests, coverage 81.23/71.85/76.73/82.72); live smoke at 1440x900 + 390x844 (en+es) proved no page scroll, Cobrar visible in the selling state, both drawers render (right slide-over desktop / bottom sheet mobile), and ESC closes + restores focus to search. | `/sales` completes the common sale without desktop scroll at 1440x900 (cart + checkout scroll internally; Cobrar stays in view); secondary sales history + suspended sales sit behind a reusable Drawer (right slide-over desktop / bottom sheet mobile); EN/ES strings complete + neutral LATAM; `ci:web` passes and a live `/sales` smoke at desktop + mobile widths proves it. | **High** |
| `ENG-187` | Ring-1 screen focus pass — Operations needs-attention (re-scoped: /operations slice) | Shipped | Deferred slices of the `ENG-186` screen-focus audit, split out so the `/sales` sellability slice could ship as one coherent commit. Make Operations open on a "Needs attention" queue rather than a flat sync tab (actionable failures first). **Re-scoped + Shipped 2026-06-04** (operator scoped this pass to the `/operations` slice; `/company` already opens on its readiness hub — `CompanyReadinessCard` is the admin default tab with per-section CTAs, ENG-104 + ENG-184 — so the Setup restructure moved to `ENG-188`): added a tenant-scoped server aggregation (`operations.needsAttention`, `managerOrAdminProcedure`) that reuses `readSyncBacklog` + `countFiscalOutboxFailures` from `services/readiness/signals.ts` and adds two mirror probes (`countHardwareOutboxFailures` / `countPaymentOutboxFailures` in `services/operations/attention.ts`); it returns per-area `{area, severity, count}` for the four retryable failure surfaces (sync conflicts/backlog, fiscal rejected/dead_letter, hardware failed/retrying/dead_letter, payment declined/timeout/retrying/dead_letter), each `area` doubling as the Operations `?tab=` deep-link target. A new `NeedsAttentionPanel` is the default `/operations` tab — one glance at what failed, a "Revisar" CTA per row that jumps to the resolving panel, and an "all clear" state; the eight existing tabs stay and `?tab=sync` deep-links still work. Cash over/short + inventory drift are deliberately excluded (reconciliation states that need a physical recount, not a retry) — noted in the panel copy. Added a `.pv-strip.warning` amber tone. `ci:server` (9 new tests incl. cross-tenant isolation) + `ci:web` green; live `/operations` smoke proved the failure queue (with CTA deep-link) en/es against the shared DB; the all-clear state is pinned by component + server tests. | A manager landing on `/operations` sees the retryable failures first (sync / fiscal / hardware / payments) with a deep-link to the resolving panel, or an "all clear" state; `?tab=` deep-links unchanged; the aggregation is tenant-scoped (cross-tenant test); EN/ES strings complete + neutral LATAM; gates + a live `/operations` smoke prove it. | **High** |
| `ENG-188` | Ring-1 screen focus pass — Setup/company readiness-hub restructure (ENG-187 remainder) | Shipped | Deferred slice of the `ENG-186`/`ENG-187` screen-focus audit. `/company` already opens on the readiness hub for admins (`CompanyReadinessCard`, the default `readiness` tab, reads `setupReadiness.get` and links to each setup surface), so the urgency is low. This ticket reorganizes the remaining flat 10-tab Setup strip around the readiness tasks (group/demote the tabs behind the readiness hub instead of a flat segmented strip), reusing the `readinessCtaHref` hub pattern. Surface-launcher module-gating (ENG-183) stays verify-only. **Shipped 2026-06-04** (web-only, no server change): replaced the flat 10-pill `segmented-control` in `CompanyPage` with a grouped Setup nav — the readiness hub is the pinned landing and the remaining 9 tabs are demoted into three labeled category groups (Negocio = general/locale/restaurant; Facturación y pagos = fiscal/payments; Sistema = modules/ai/data/device). Modeled as a navigation (`role="group"` + `aria-labelledby` + `aria-current`, active panel as `role="region"`) instead of a single tablist, since it is a categorized settings menu. The `?tab=` URL contract and every `company-tab-<key>` testid are byte-compatible, so the readiness-hub CTAs, `GlobalStatusStrip`, `AuthProvider` deep-link and the AnomalyDetection "Activa la IA" CTA keep working. Added `company.tabs.groups.*` (en/es, neutral LATAM) + a `.company-setup-nav` style cluster; `CompanyPage.test.tsx` moved to the `aria-current` contract + an `assertNoA11yViolations` lock. `ci:web` green; live `/company` smoke proved the grouped landing + group switch + a readiness CTA deep-link, en/es. Closes the ENG-186/187/188 screen-focus wave. | Live web smoke on `/company` proves Setup is organized around the readiness checklist rather than a flat tab strip; the readiness hub remains the admin landing; per-section CTAs still deep-link to the right surface; EN/ES strings complete + neutral LATAM. | **Medium** |
| `ENG-079b` | Sidebar slim-down (Phase 2: behavioral compaction) | Shipped | Out-of-band Phase 2 of the ENG-079 sidebar work. Phase 1 (`ENG-079a`) recovered ~390 px without behavioral changes; this slice ships the three ergonomic improvements deferred there. **Shipped 2026-05-13**: (O7) `sections.setup` ("Configuración") is now a collapsible group — the section title is a `<button>` with `aria-expanded` + `aria-controls` and a `ChevronDown` icon that rotates -90° when collapsed. State persists per browser tab via `window.sessionStorage` under key `puntovivo:sidebar:setupCollapsed` (matches the `active_site_id:` prefix convention from `siteStorage.ts`). Default state is **collapsed** so existing operators see the change clearly: "Configuración" becomes one row with a chevron instead of 16 expanded items. A new `SidebarCollapsibleSectionTitle` component encapsulates the toggle + chevron + aria wiring; only the setup section uses it, the other 3 sections keep their static `<p>` label. Helpers `readSetupCollapsed()` + `writeSetupCollapsed()` guard against private-mode browsers that throw on sessionStorage access — fall back to the conservative collapsed default rather than crash render. (O8) The collapse-rail button moves from the footer to the top header row, pairing with the existing `xl:hidden` mobile-close button via `hidden xl:inline-flex` so only one shows at a time per breakpoint. The button uses the same `btn-outline btn-icon` recipe as the mobile close (visual consistency); it renders `ChevronLeft` when expanded and `ChevronRight` when collapsed. The footer's collapse block (~50 px + wrapper `<div>`) is removed entirely. (O9) Three items (`providers`, `categories`, `locations`) move from `sections.flow` ("Operaciones") to `sections.setup` ("Configuración"), inserted after `customerCatalogs` and before `units` so they cluster with the rest of the data-setup catalog. "Operaciones" drops from 8 to 5 items (orders / purchases / quotations / customers / products); "Configuración" grows from 13 to 16 — but with the section collapsed by default, that's 16 items hidden behind 1 row. Collapsed-rail mode (`xl:w-[6.5rem]`) bypasses the toggle entirely — every icon renders regardless of the setup-collapsed state because section labels are hidden anyway and power users navigate by icon position. A minor TS narrowing issue surfaced when the `flow` and `setup` arrays became homogeneous in `requiredModule` shape (the `satisfies readonly NavigationSection[]` union dropped the optional field); resolved with an explicit `as readonly NavigationItem[]` cast inside the filter callback, documented inline. Zero new i18n keys — `sections.setup` / `actions.collapseRail` / `actions.expandNavigation` already exist. Zero server changes. ci:web 126 files / 1002 tests pass at coverage 78.25/70.47/75.11/79.43 (above the 70/70/70/70 floor). Live smoke captured 4 inline Playwright MCP screenshots: D1 fresh session with Configuración collapsed by default (toggle visible with chevron, 16 setup items hidden); D2 click toggle → `aria-expanded=true`, items unhidden including the re-anchored providers/categories/locations, `sessionStorage='false'` persisted; D3 page reload with sessionStorage='false' → Configuración stays expanded (no flash, lazy `useState` initializer runs once); D4 click new top collapse-rail button → sidebar rail at 104 px (6.5rem) with all 28 icons visible including setup items. Counter Plan v2.0 unchanged (out-of-band). | Operator on `/dashboard` with admin role + all modules active sees a focused 14-row sidebar on first load (5 overview + 5 ops + 4 surfaces + 1 setup toggle), can click the setup toggle to reveal the 16 admin settings and the choice persists across reloads within the same tab; can collapse the rail via a button in the top header; mobile drawer behavior unchanged; module gating + Dashboard anomaly badge unchanged. | **Medium** |
| `ENG-189` | /sales cash-control reachability at constrained viewports | Shipped | Promoted from a BACKLOG follow-up found during ENG-134g. The ENG-186 `/sales` no-page-scroll lockup (fixed-height grid, columns scroll internally) collapsed the checkout scroll region to **0px** at short viewports (1280x720 / 1366x768, common cashier screens), burying the cash-session controls (Registrar movimiento / Cerrar caja) off-screen at y≈1196 with no way to reach them — 4 cash-session E2E tests failed (`business.spec.ts:870/942/1012/1302`), undetected because the Playwright suite is local-only (not push-CI). **Shipped 2026-06-09** (web-only, no server change): new Tailwind v4 `@custom-variant pos` (`apps/web/src/styles/theme.css`) = `(min-width: 1280px) and (min-height: 900px)`; the height-lockup classes (`overflow-hidden` / `h-full` / `min-h-0` / `flex-1` / `overflow-y-auto` / `grid-rows-[minmax(0,1fr)]`) on the `sales-pos-shell` + grid section (`SalesPage.tsx`) + both column roots + their scroll regions (`SalesCartWorkspace.tsx`, `SalesCheckoutPanel.tsx`) move `xl:` → `pos:`. Below 900px tall the lockup falls back to the natural stacked flow and the app shell scrolls, so every control is reachable; at >= 900px the ENG-186 layout (pinned Cobrar, internal scroll, no page scroll) is byte-unchanged (`pos` ⊂ `xl` at 1440x900). Diagnosed live via `elementFromPoint`: at 840px tall the scroll region was still 0 (a lockup-on dead zone), so the threshold sits at the 900px ENG-186 target where the Cobrar footer stays pinned. No new strings (the existing es labels Registrar movimiento / Cerrar caja are neutral LATAM). Full `test:e2e:web` back to **55/55** green (the 4 cash tests pass); `ci:web` green (1602 tests + build + bundle/contrast); live smoke EN+ES at 1280x720 (cash-control CTA reachable + hit-tested; the open-cash-session dialog opens) + 1440x900 (lockup, Cobrar pinned, no page scroll), 0 console errors. | Cash-session controls are reachable + clickable at constrained cashier viewports (1280x720 / 1366x768); the 4 cash-session E2E tests pass and the full suite is green; the ENG-186 no-desktop-scroll-at-1440x900 invariant still holds; EN/ES live smoke proves both regimes. | **High** |

Sequencing recommendation:

1. **Sellability truth sync**: `ENG-050` is shipped and is now the source for demo / pilot / production go-no-go through [SELLABILITY.md](./SELLABILITY.md).
2. **Foundation Reset before new commercial scope**: ship `ENG-051` → `ENG-056` in order. These tickets introduce ADRs, device identity, idempotency, operation journal, sale lifecycle services, and the cash-session boundary without rewriting the whole app.
3. **Fiscal readiness, provider-agnostic first**: ship `ENG-057` and `ENG-058` before any real provider. Start `ENG-059` only after the PT contract, credentials, certificate, numbering resolution, and error map are available.
4. **Hardware readiness**: ship `ENG-060` and `ENG-061` without external gates. `ENG-062` can implement the adapter and fallback path, but full validation waits for a physical printer + drawer. `ENG-063` remains gated on payment sandbox + terminal.
5. **Sync / operations / resilience**: ship `ENG-064`, `ENG-065`, `ENG-067`, then `ENG-066`. The ordering intentionally gives operators visibility and chaos coverage before deeper local-security work.
6. **Expansion architecture**: ship `ENG-068`, `ENG-069`, `ENG-070` after the retail core is safe enough. Module activation precedes full restaurant/pharmacy/service vertical work.
7. **Authority Node early support**: ship `ENG-071` → `ENG-075` before treating multi-register stores as sellable. This keeps `device_local` as the default while adding explicit `site_hub` / `hub_client` support. `ENG-076` remains deferred until a pilot proves hub clients need satellite offline writes.
8. **Existing gated tickets stay parked**: `ENG-021`, `ENG-022`, `ENG-023`, `ENG-059`, and `ENG-063` require their documented external gates.
9. **Older pending quality tickets**: `ENG-010`, `ENG-011`, and `ENG-014` are superseded in practice by the Foundation Reset wave where their scope overlaps. Keep them until the overlapping `ENG-054..ENG-056` work closes or explicitly retire them in a follow-up docs pass.
10. **Claim-backed commercial polish**: close `ENG-096`, `ENG-099`, `ENG-101`, and the remaining external-copy slice of `ENG-102` before making public website claims stronger than the runtime.
11. **Product focus reset before more visible expansion**: execute `ENG-182`, `ENG-182a`, then `ENG-183..ENG-186` as a short recentering wave before adding more restaurant / AI / platform surface area. This locks the README and sellability truth (`ENG-182`), trims planning/code noise without changing the pending pool (`ENG-182a`), makes module exposure match the Ring-1 retail wedge (`ENG-183`), turns readiness and checkout preflight into Colombia-retail gates (`ENG-184`), removes misleading fiscal fallbacks (`ENG-185`), and ships the `/sales` sellability screen-focus slice (`ENG-186`). The remaining screen-focus slices ship as `ENG-187` (Operations needs-attention default — shipped) and `ENG-188` (Setup/`/company` readiness-hub restructure — pending, low urgency since `/company` already opens on its readiness hub).
12. **World-class LatAm POS wave**: execute `ENG-103..ENG-165` through [PLAN-V3.md](./PLAN-V3.md). The wave replaces the H0..H4 horizons with a single flat priority list rooted in the V3 §10 ranking. The reading order is: (1) `ENG-103`, `ENG-104`, `ENG-133`, `ENG-134`, `ENG-135` install the foundation rails (document trust, guided setup, perf budget, accessibility, observability); (2) `ENG-105`, `ENG-131`, `ENG-106`, `ENG-123`, `ENG-128`, `ENG-129`, `ENG-136`, `ENG-132`, `ENG-137` round out adoption, IA, supportability, privacy, backup, screen simplification, and auto-update; (3) `ENG-141`, `ENG-140`, `ENG-142` add the daily operator hooks (day close, shift, loss-prevention); (4) `ENG-110`, `ENG-111`, `ENG-125`, `ENG-124`, `ENG-143`, `ENG-148`, `ENG-108`, `ENG-109`, `ENG-107`, `ENG-149`, `ENG-150` complete the catalog/inventory/money trust foundation plus hardware ecosystem; (5) `ENG-112`, `ENG-144`, `ENG-115`, `ENG-113`, `ENG-114`, `ENG-127`, `ENG-153`, `ENG-116`, `ENG-154`, `ENG-164` (hosted-SaaS substrate spike), `ENG-138`, `ENG-155` ship the LatAm growth + monetization stack — `ENG-164` lands before `ENG-138`'s cross-tenant aggregate slice and before the hosted-only tickets in step (7); (6) `ENG-117`, `ENG-145`, `ENG-146`, `ENG-147`, `ENG-119..ENG-122` deliver restaurant + services + vertical depth; (7) `ENG-165` (tRPC-aware rate limiter) lands before `ENG-118` (public API), then `ENG-126`, `ENG-130`, `ENG-139`, `ENG-151`, `ENG-152`, `ENG-156`, `ENG-157`, `ENG-158`, `ENG-159`, `ENG-162`, `ENG-163` finish the platform + enterprise readiness layer (the hosted-SaaS-dependent tickets `ENG-157` / `ENG-158` / `ENG-162` are blocked by the `ENG-164` outcome). Gated/expansion tickets `ENG-160` (Nómina Electrónica DIAN) and the NFe slice of `ENG-161` wait on their external contracts; the pt-BR locale slice of `ENG-161` is implementable today. Pull `ENG-117` and `ENG-119..ENG-122` forward only when a restaurant, services, pharmacy, supermarket, or hardware-store pilot becomes the sales wedge.
13. **Cross-cutting audit closure** (`ENG-166..ENG-181`): the 2026-05-24 audit ([AUDIT-2026-05-24.md](./AUDIT-2026-05-24.md)) opened 17 tickets across security, frontend performance, database/Drizzle, and code quality. Run them as 5 sequential waves: **W1** ship `ENG-166` (Critical security closure) first — `Math.random` SSE id, missing helmet/CSP, login timing leak, Argon2 drift, Zod `.strict()` gaps. **W2** data-integrity wave: `ENG-174` (PRAGMA tuning) → `ENG-167` (SQLCipher at rest) → `ENG-175` + `ENG-175b` (composite indexes + FK onDelete) → `ENG-176` (money model + multi-currency LATAM seam — **blocker for `ENG-156` + `ENG-161`**) → `ENG-168` (token + session lifecycle). **W3** frontend perf: `ENG-170` (bundle optimisation) → `ENG-171` (provider memo + React Query hygiene) → `ENG-172` (DataTable virtualisation) → `ENG-173` (Web Vitals RUM). **W4** code-quality: `ENG-181` (error wrapping) → `ENG-179` (TS strict + lint) → `ENG-178` (megafile decomp wave 2 — needs ENG-176 + ENG-171 first) → `ENG-177` (optimistic versioning) → `ENG-180` (documentation pack — needs ENG-181 first). **W5** lower-priority polish: `ENG-169` (input boundary hygiene). The wave can run in parallel with step 12's later sub-steps (only `ENG-176` directly blocks something on the V3 ranking — `ENG-156` and `ENG-161`).

For sprint-level execution (per-commit sequencing, draft commit messages, per-iter verification matrix) see [SPRINT-PLAN.md](./SPRINT-PLAN.md). For the v2.0 phasing and architectural decisions closed by the 2026-Q2 audit see [PLAN-V2.md](./PLAN-V2.md).

---

## 4. i18n Plan — Pre-Phase 1

**Stack**: `i18next` + `react-i18next` — installed ✓
**Fallback chain**: `es-CO` → `es` → `en` — configured ✓
**Namespace splitting**: per feature area (common, auth, nav, sales, inventory, etc.)

**Why first**: English-only UI is a deployment blocker for LatAm. Every new phase adds more hardcoded strings. Foundation should land before cash management UI.

**Scope**: ~126 component files, ~300+ labels, ~137 toasts, ~187 form fields to extract.

### Progress

| Phase | Scope | Status |
| --- | --- | --- |
| i18n-1: Foundation | Install packages, scaffold `apps/web/src/i18n/`, wire in `main.tsx`, `en/` + `es/` stubs for `common`, `auth`, `nav` | **Done** |
| i18n-2: High-visibility surfaces | `LoginPage`, `Sidebar`, `Header` converted to `useTranslation` | **Done** |
| i18n-3: Full feature coverage | All feature modules (`sales`, `products`, `purchases`, `inventory`, `orders`, `customers`, settings screens) | Pending |
| i18n-4: Server + CI enforcement | Server error messages, Electron main process strings, CI lint rule blocking new hardcoded strings | Pending |

---

## 5. Technical Roadmap by Phase

Each phase includes DB, tRPC, UI, and Test tickets.

### Phase 0: Architecture Foundation and Multi-Vertical Module System

**Goal**: Prepare codebase for logistics, dual-database compatibility, multi-vertical modules, and compound tax.

**DB tickets**:
- `DB-001` Dialect-neutral schema conventions
- `DB-002` Replace raw schema bootstrap with versioned Drizzle migrations
- `DB-003` Money, quantity, and timestamp normalization rules
- `DB-004` Add `vertical` column to `sites` (enum: retail, supermarket, pharmacy, ferreteria, restaurant, etc.)
- `DB-005` Add `metadata` JSON column to `products` for vertical-specific attributes
- `DB-006` Add `settings` JSON column to `sites` for capabilities map
- `DB-007` Create `tax_groups` table (compound tax scenarios + mutual exclusivity)
- `DB-008` Create `tax_group_items` table (tax_type, rate, calculation_base, exclusive_with)
- `DB-009` Link products to tax_groups instead of single VAT rate

**tRPC tickets**:
- `API-001` Repository/service boundaries for core domains
- `API-002` Separate persistence concerns from router procedures
- `API-003` Sync acknowledgement contract
- `API-004` Module registry service: resolve site vertical → compute `SiteCapabilities`
- `API-005` Compound tax calculation engine
- `API-006` Module-conditional tRPC router composition

**UI tickets**:
- `UI-001` System diagnostics page for runtime topology
- `UI-002` Admin-facing sync topology indicators
- `UI-003` Vertical/module activation settings page with setup wizard
- `UI-004` `SiteCapabilities` React Context provider
- `UI-005` Capability-filtered sidebar navigation
- `UI-006` Checkout compound tax display (IVA, INC, impuesto saludable lines)

**Test tickets**:
- `TEST-001` Persistence contract tests reusable across dialects
- `TEST-002` Schema migration smoke tests
- `TEST-003` Sync contract tests
- `TEST-004` Compound tax: IVA 19% + INC 8% on same receipt
- `TEST-005` SiteCapabilities computed correctly per vertical
- `TEST-006` Module-conditional sidebar rendering

### Phase 1: Cash Management, Shift Control, and Fractional Quantity

**Goal**: Most critical missing commercial feature for LatAm retail + unblock fractional quantity sales.

**DB tickets**:
- `DB-050` **CRITICAL**: Convert `stock`/`quantity` from `integer` to `real` across ALL tables. Add `sell_by_fraction`, `fraction_step`, `fraction_minimum` flags.
- `DB-051` Create `cash_sessions` (register, cashier, site, opening_float, denominations, expected_balance, actual_count, over_short, status, timestamps)
- `DB-052` Create `cash_movements` (session_id, type [sale, refund, paid_in, paid_out, skim, replenishment], amount, reference, note)
- `DB-053` Create `denomination_templates` for standardized float breakdowns

**tRPC tickets**:
- `API-051` `cashSessions.open` with denomination counting and float validation
- `API-052` `cashSessions.close` with blind close support
- `API-053` `cashSessions.movements` for paid-in, paid-out, skim, replenishment
- `API-054` `cashSessions.report` with over/short history per cashier
- `API-055` Update `sales.create`/`sales.refund` to require active cash session

**UI tickets**:
- `UI-051` Cash session open dialog with denomination counting grid
- `UI-052` Cash session close dialog with blind close mode
- `UI-053` Cash session summary with movement timeline
- `UI-054` Cash management dashboard: active sessions, over/short trends
- `UI-055` Register assignment in POS checkout header

**Test tickets**:
- `TEST-051` Opening cash denomination count matches float
- `TEST-052` Sale increments session expected balance
- `TEST-053` Refund decrements session expected balance
- `TEST-054` Blind close hides expected amount until count submitted
- `TEST-055` Over/short calculation accuracy

### Phase 2: Site-Owned Inventory and Transfer Logistics

**Goal**: Make stock physically believable across sites and warehouses.

**DB tickets**:
- `DB-101` Create `inventory_balances` by product/site/location — **Step 0 shipped** at the (tenant, site, product) grain; location column deferred.
- `DB-102` Create `transfer_orders` and `transfer_order_items` — **Step 1 shipped** (immediate completion only — lifecycle states deferred).
- `DB-103` Create `transfer_shipments` and `transfer_receipts`
- `DB-104` Migrate tenant-wide stock to default site-owned balances — **Step 1 shipped** as a seed-only insert onto the earliest-created active site (balances are now authoritative).

**tRPC tickets**:
- `API-101` `inventory.listBalancesBySite` — **Done** (read-only).
- `API-102` `transfers.create`, `.ship`, `.receive`, `.void` — **Step 3 shipped**: `transfers.create` with optional `defer` flag (origin debited on create, destination credited on receive when `defer: true`), `transfers.list`, `transfers.receive` (completes an in_transit transfer), and `transfers.void` (reverses both completed and in_transit). `.ship` is collapsed into `.create({ defer: true })`; a standalone draft state remains deferred.
- `API-103` Update sales/purchases/orders to read/write site balances — **Step 4 shipped**: sales + purchases/order receiving + admin inventory tools now write through to `inventory_balances`, and `products.stock` is kept in lockstep as Σ(site balances). `applyInventoryBalanceDelta` recomputes and persists `products.stock` after every balance mutation via the new `syncProductStockFromBalances` helper, so the legacy tenant-wide field never drifts during normal operation. A new `inventory.reconcileBalances` (admin) mutation heals historical drift by recomputing `products.stock` for every product in the tenant. The site-resolution contract per mutation remains: `sales.create` debits the cash-session site; `sales.returnSale`/`sales.void` credit the ORIGINAL sale site; `purchases.create` credits the operator site; `purchases.createFromOrder` credits the order site; `purchases.returnPurchase`/`purchases.void` debit the original purchase site; `inventory.adjustStock` resolves the site via `input.siteId ?? ctx.siteId ?? primary` and applies the absolute delta; `inventory.recordEntry` writes through to the operator site, with `mode: 'initial'` crediting by `normalizedQuantity` and `mode: 'physical'` applying the absolute-count delta. A standalone draft transfer state remains deferred.

**UI tickets**:
- `UI-101` Inventory page: site/location balance tabs — **Done** (By Site tab in Inventory).
- `UI-102` Transfer Orders module — **Step 4 shipped** (Transfer stock button + modal with "Ship now, receive later" checkbox + transfer history table with Details/Receive/Void actions + confirmation modal + read-only detail drawer showing line items and lifecycle timestamps, all in the By Site tab; dedicated Transfer Orders page deferred).
- `UI-103` Transfer receive modal with discrepancy reporting — **Done**: receiving an in_transit transfer opens a modal that lists every line with an editable received-quantity input (defaulting to shipped), displays per-line variance badges, and reveals an optional discrepancy-notes textarea when any line diverges. The server credits the destination with the received quantity only (so the `shipped - received` delta shows up as shrinkage via the existing products.stock-in-lockstep invariant) and persists the per-line `received_quantity` + aggregate `discrepancy_notes` on the transfer. The history row renders a Discrepancy badge when present; voids debit the destination by `received_quantity` (coalescing to shipped for legacy rows) so partial-receipt reversals are symmetric. `received > shipped` is rejected up front (`TRANSFER_RECEIVED_EXCEEDS_SHIPPED`) — operators who genuinely received more should take the shipped quantity and post a separate `inventory.adjustStock`.

**Test tickets**:
- `TEST-101` Sales decrement active site only
- `TEST-102` Purchase receipts increment target site only
- `TEST-103` Transfer shipment creates in-transit without double counting
- `TEST-104` Transfer receipt resolves in-transit correctly

### Phase 3: Outbound Logistics Documents

**Goal**: Pick/pack/ship as first-class warehouse/store operations.

**DB**: `fulfillment_orders`, `pick_lists`, `packing_slips`, `delivery_notes`
**tRPC**: Fulfillment allocation, pick list generation/completion, packing slip, delivery note validation
**UI**: Fulfillment workbench, pick list barcode workflow, packing UI, delivery note printable
**Tests**: Pick respects balances, delivery note posts correct movement, partial shipment remains fulfillable

### Phase 4: Transport Execution and Tracking

**Goal**: Dispatch, transport, and delivery follow-through.

**DB**: `shipments`, `shipment_stops`, `drivers`, `vehicles`, `carriers`, `proof_of_delivery`, `delivery_exceptions`
**tRPC**: Dispatch assignment, shipment status transitions, POD mutation, exception handling
**UI**: Dispatch board, shipment timeline, driver POD screen, customer tracking stub
**Tests**: Status progression, POD closes shipment, exception consistency

### Phase 5: Payment Depth, Quotations, Layaway, and Credit Sales

**Goal**: Complex payment scenarios, pre-sale conversion, and LatAm credit practices.

**DB**: `quotations`, `sale_payments` (multi-tender), `customer_credit_accounts`, `gift_cards`, `store_credits`, `layaway_orders`, `special_orders`, `service_tickets`, company fiscal regime fields, `company_credit_settings`, `credit_sales`, `credit_installments`, `credit_payments` (abonos)
**tRPC**: Quotation CRUD/conversion, split payment processing, on-account sales, gift card/store credit, layaway/apartado workflow, special orders, service tickets, credit sale creation with installment schedule, abono posting, overdue scan, aging report
**UI**: Quotations module, multi-tender checkout dialog, credit account management, layaway management, credit sale checkout flow, abono screen, credit portfolio
**Tests**: Quote conversion preserves prices, split payment sum validation, credit limit enforcement, layaway inventory reservation, installment schedule accuracy, abono distribution to oldest installments

### Phase 6: Product Handling and Advanced Inventory

**Goal**: Broader product categories and operational complexity.

**DB**: `product_variants`, `serial_numbers`, `batches`/`batch_balances`, `bundle_components`/`recipes`, product weight/dimensions/shipping/reorder fields
**tRPC**: Variant-aware search, serial/batch assignment, FEFO allocation, bundle explosion, reorder alerts, ABC analysis, inventory aging, GMROI, cycle counting
**UI**: Variant matrix builder, serial/batch selector, expiry dashboard, bundle/recipe management, reorder dashboard, ABC view, aging heatmap, cycle count worksheet
**Tests**: Serialized capture, batch FEFO, bundle component decrement, reorder trigger, ABC distribution

### Phase 7: Loyalty, Promotions, and Commercial Expansion

**Goal**: Conversion, retention, omnichannel readiness.

**DB**: `promotion_rules`, `coupons`, `loyalty_accounts`, `loyalty_transactions`, `loyalty_tiers`, order channel/delivery mode
**tRPC**: Promotion engine (evaluate cart, apply best/stackable discounts), coupon validation, loyalty earn/redeem, points expiry, omnichannel fulfillment
**UI**: Promotion rule builder, coupon management, checkout auto-promotion, loyalty display/redeem, customer loyalty profile, omnichannel order queue
**Tests**: BOGO logic, promotion stacking, coupon single-use, loyalty refund reversal, tier upgrade

### Phase 8: Employee Management and Audit Trail

**Goal**: Employee lifecycle and operational accountability.

**DB**: `employee_shifts`, `employee_commissions`, `commission_rules`, `audit_logs`, `approval_policies`, `approval_events`
**tRPC**: Shift clock in/out, commission calc/clawback, audit log recording, approval workflows, employee performance metrics
**UI**: Shift management, commission config/reports, audit log viewer, approval inbox, employee dashboard
**Tests**: Clock timestamps, commission clawback on return, audit before/after state, approval blocking

### Phase 9: Advanced Reporting and BI

**Goal**: Actionable insights beyond basic views.

**DB**: `daily_sales_summary`, `daily_inventory_snapshot`, `customer_cohorts`
**tRPC**: Sales/Inventory/Customer/Employee KPIs, exception alerts, drill-down API, scheduled report export
**UI**: Executive dashboard with sparklines, operational dashboard, inventory intelligence, customer insights, exception alerts, drill-down navigation, report builder
**Tests**: Summary aggregation accuracy, GMROI formula, comp sales exclusions

### Phase 10: Hybrid Database Runtime

**Goal**: SQLite local + PostgreSQL-compatible remote truth.

**Recommended stack**: PowerSync for sync layer. Dual schema from shared types (near-term). PGlite evaluation for long-term.

**DB**: Dialect abstraction package, dual `sqliteTable`/`pgTable` variants, Postgres migration/bootstrap, operation log schema, boolean/timestamp/JSON/UUID normalization
**tRPC**: Repository interfaces for either dialect, remote sync/apply endpoints, conflict response model, capability negotiation
**UI**: Remote authority config, improved sync center, richer conflict resolution
**Tests**: Full contract suite against SQLite and Postgres, offline-then-reconnect replay, multi-client conflict scenarios

### Phase 11: Fiscal, Accounting, and Integration Layer

**Goal**: Market readiness for broader deployment, Colombia DIAN compliance first.

**Colombia DIAN**: DEE (mandatory since May-July 2024), Factura Electrónica (UBL 2.1), Nota Crédito/Débito, CUFE/CUDE (SHA-384), XAdES-EPES digital signature, SOAP web service, numbering range management, contingency mode.

**DB**: `fiscal_documents`, `fiscal_numbering_ranges`, `fiscal_certificates`, `credit_notes`/`debit_notes`, `fiscal_contingency_log`, `supplier_invoices`, `api_keys`, `webhooks`, `currency_rates`, `country_fiscal_profiles`, `company_fiscal_overrides`
**tRPC**: Fiscal adapter interface, Colombia DIAN adapter (UBL 2.1 XML), CUFE/CUDE service, XAdES-EPES signing, DIAN SOAP client, numbering range management, contingency mode, credit/debit note lifecycle, profile-driven tax engine, tip rules refactor, fiscal adapter factory, public API, webhooks, accounting events
**UI**: Fiscal document views, numbering range management, certificate management, DIAN habilitación wizard, contingency indicator, multi-currency settings, integration/webhook admin, API key management
**Tests**: CUFE/CUDE SHA-384 vs DIAN vectors, UBL XSD validation, XAdES-EPES validity, contingency activation, webhook signing, multi-currency accuracy

### Phase 12: Restaurant and Service Verticals

**Goal**: Restaurant, food service, and appointment-based businesses.

**Prerequisites**: Phase 0 (modules)

**DB**: `tables`, `table_sessions`, `kitchen_orders`, `product_modifiers`/`modifier_groups`, `appointments`, `tip_records`
**tRPC**: Table management (assign/transfer/merge/split), kitchen order routing, course firing, modifier application, split check, tip management (Ley 1935/2018), appointments, auto-86ing, daypart menus, combo engine, kitchen printer routing
**UI**: Floor plan editor, table status grid, KDS with timers, modifier selection, split check dialog, tip consent dialog, appointment calendar, combo builder
**Tests**: Table-sale link, kitchen status transitions, split check math, tip pool distribution, appointment conflict detection, daypart activation, combo pricing

### Phase 13: Pharmacy Vertical

**Prerequisites**: Phase 0, Phase 6 (lot/batch/expiry), Phase 11 (fiscal)

**DB**: `prescriptions`, `prescription_items`, `dispensation_records`, `controlled_substance_ledger`, `fne_reports`, `rips_records`, pharmacy product fields (INVIMA, controlled_schedule, INN/DCI, storage), `eps_contracts`, `equivalence_groups`
**tRPC**: Prescription CRUD with validity/partial dispensing, controlled substance ledger, FNE/RIPS/SISMED reports, regulated price ceiling enforcement, generic substitution, patient medication history, INVIMA recall processing
**Tests**: Controlled substance requires valid Rx, partial dispensing tracking, FNE ledger balance, expired Rx blocking, regulated price ceiling, RIPS format

### Phase 14: Supermarket Vertical

**Prerequisites**: Phase 0, Phase 1 (fractional qty), Phase 6 (lot/batch/expiry), Phase 7 (promotions)

**DB**: `scale_configurations`, `plu_codes`, supermarket `departments`, `shrinkage_records`, `dsd_receiving`, `vendor_promotions`, supermarket product fields
**tRPC**: Scale reading service, variable-weight barcode parsing, age restriction enforcement, department P&L, shrinkage tracking, DSD receiving, automated near-expiry markdowns, impuesto saludable
**Tests**: Variable-weight barcode decode, age restriction blocking, department shrinkage totals, impuesto saludable rates

### Phase 15: Ferretería Vertical

**Prerequisites**: Phase 0, Phase 1 (fractional qty), Phase 5 (quotations, credit)

**DB**: `product_sale_units` (multi-unit with conversion), `service_charges`, `project_templates`, FTS5 virtual table for product search
**tRPC**: Multi-unit sale with conversion, in-house barcode generation, project template management, service charges, partial-use returns, FTS5 search, bulk pricing auto-application
**Tests**: Unit conversion accuracy, barcode generation, template explosion, partial-use return

---

## 6. Competitive Context (Summary)

### What Puntovivo Has That Others Don't

| Capability | Puntovivo | Square | Shopify | Lightspeed | Odoo |
| --- | --- | --- | --- | --- | --- |
| True offline mode | **Strong** | Limited | Limited | No | No |
| Desktop native | **Yes (unique)** | No | No | No | No |
| Open source | **Yes** | No | No | No | Community |
| No subscription | **Yes** | No | No | No | No |
| Self-hosted | **Yes** | No | No | No | Yes |

### Biggest Competitive Gaps

| Gap | vs Colombia (Siigo/Alegra) | vs Global (Square/Shopify) | vs Open Source (Odoo/ERPNext) |
| --- | --- | --- | --- |
| Cash management | **Critical** | **Critical** | **Critical** |
| Fiscal compliance (DIAN) | **Critical** | N/A | Moderate |
| Split payments | High | **Critical** | High |
| Loyalty/promotions | Moderate | **Critical** | High |
| Credit sales (ventas a crédito) | **Critical** | N/A | Moderate |
| Omnichannel | Moderate | **Critical** | Moderate |
| Product variants | High | High | **Critical** |
| Lot/batch/expiry | N/A for retail | Low | **Critical** |
| Advanced reporting | Moderate | High | High |
| Public API/webhooks | High | **Critical** | High |

For detailed competitive capability matrices, payment ecosystem, hardware integration, LatAm integrations, multi-vertical readiness, and Colombian tax compliance status, see [PLAN.md](./PLAN.md) §6.3–6.8.

---

## 7. Reference Architecture Notes (summaries — full designs in PLAN.md)

### Credit Sales (Ventas a Crédito) Design

See Phase 5 Extension for full data model. Key concepts:
- **Cuotas**: Installment schedule generated at time of credit sale
- **Abonos**: Partial payments posted against oldest pending installments
- **Configuration**: per tenant → company → site (most specific wins)
- **Colombian legal**: Credit sales must generate Factura Electrónica (not DEE). Interest bounded by Superfinanciera usury rate.

### Country-Parametrizable Fiscal Rules Design

See Phase 11 Extension. Key architecture:
- `country_fiscal_profiles` table with JSON columns for tax, tip, e-invoicing, withholding, regime config
- `company_fiscal_overrides` for deviations from country defaults
- Fiscal adapter factory: `getFiscalAdapter(profile.adapter)` selects runtime adapter
- Tax engine becomes pure function: `computeTax(lineItems, profile)`
- Colombia behavior preserved as "CO" profile with zero behavior change

### Hybrid Data Architecture

Recommended: PowerSync for sync layer + dual schema from shared types (near-term).
- **Near term**: Keep SQLite local, formalize sync API, remove direct `better-sqlite3` dependency
- **Mid term**: PostgreSQL as remote truth, SQLite as offline working set
- **Long term**: Support standalone SQLite, managed remote SQLite, and PostgreSQL-backed topologies

### Multi-Vertical Module Architecture

Pattern: Configuration-driven module activation (not separate apps).
- `vertical` field on sites selects the active module
- JSON `metadata` on products stores vertical-specific attributes
- `SiteCapabilities` React Context drives conditional rendering
- Each module exports tRPC router + schema + capabilities declaration
- Checkout pipeline is configurable per vertical

---

## 8. Migration History

The migration from WinForms to Electron + React + Fastify is **functionally complete**.

**What was migrated**: desktop shell, embedded backend, tRPC transport, admin/catalog/product/pricing/inventory/sales/procurement/dashboard/export/reporting modules.

**What the current repo added beyond the original plan**: geography hierarchy, customer classification, locations, provider categories, orders with receive-into-purchase, tenant logo library, sale refunds, desktop backup/restore/tray/theme/update/print, merged sync conflict resolution.

When legacy references conflict with current repo: trust `apps/` and `packages/server/` code first, then this document.
