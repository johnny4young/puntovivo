# Puntovivo Roadmap

> Updated: May 2, 2026
> Single source of truth for project status, priorities, and actionable work plan.
> Replaces: `IMPLEMENTATION_STATUS.md`, `OPEN_BACKLOG.md`, `MIGRATION_PLAN.md`
> Strategic reference: see [PLAN.md](./PLAN.md) for competitive analysis, academic frameworks, and detailed technical designs.
> Sellability reference: see [SELLABILITY.md](./SELLABILITY.md) for the Colombian retail go/no-go checklist.
> Architecture diagram: [architecture.svg](./architecture.svg) (source: [architecture.mmd](./architecture.mmd))

---

## 0. MVP Colombia — Definition of Done

Concrete go/no-go checklist for declaring Puntovivo **ready to be sold to a
Colombian retail business**. Derived from the April 2026 audit of features,
DIAN regulation (Resolución 000165/2023), and hardware expectations of
neighborhood retail.

| # | Capability | Status | Blocking phase |
| --- | --- | --- | --- |
| 1 | Multi-tenant POS with cash sessions, sales, returns, voids, split payments, quotations | **Shipped** | Phase 0-5 done |
| 2 | Site-owned inventory + atomic transfers with discrepancy reporting | **Shipped** | Phase 2 done |
| 3 | Audit trail on sensitive actions (void, refund, cash close, stock adjust) | **Shipped** | Tier-2 #8 done |
| 4 | **Electronic invoicing (DEE + Factura Electrónica) via DIAN-authorized PT** | **Gated** — domain/model shipped, real PT pending | Phase 11 (was deferred, now **P0**) |
| 5 | **CUFE/CUDE generation + QR on printed receipt + XML ≥5-year retention** | **Partial** — CUFE/model/XML surfaces exist; final QR/retention proof pending | Phase 11 |
| 6 | **Contingency mode** (offline → queued → sent when online) for fiscal docs | **Partial** — status/UI scaffold exists; retry daemon pending | Phase 11 |
| 7 | **Barcode scanner support** (USB HID keyboard-wedge + EAN-13 checksum) | **Not started** | Phase 12 Hardware |
| 8 | **ESC/POS thermal printer driver + cash drawer opening via RJ11** | **Not started** | Phase 12 Hardware |
| 9 | **Payment terminal integration** (manual capture exists; Bold/Wompi adapter needed) | **Partial (manual only)** | Phase 12 Hardware |
| 10 | **Park-and-resume multiple carts** (suspended sales workspace) | **Shipped** | `ENG-018`, `ENG-018b`, `ENG-018c` |

**Overall MVP Colombia retail readiness: ~76% by shipped foundation,
but not sellable yet**. DIAN PT, fiscal contingency, ESC/POS + drawer,
scanner, and payment-terminal policy remain the go/no-go blockers. See
[SELLABILITY.md](./SELLABILITY.md) for the pilot / production checklist,
and section "Market Segments Coverage" below for the three-ring definition
(retail → restaurant/pharmacy → services).

---

## 1. Current State — What's Done

The application is past early migration. The core POS surface is live and operational.

### Completed Phases

| Phase | Scope | Status |
| --- | --- | --- |
| Phase 0 | Foundation, schema, transport baseline | **Complete** |
| Phase 1 | Administration and master catalogs | **Complete** |
| Phase 2 | Product management and pricing | **Complete** |
| Phase 3 | Inventory | **Complete** |
| Phase 4 | Sales / POS | **Complete** |
| Phase 5 | Procurement | **Complete** |
| Phase 6 | Reporting, sync, desktop ops, UX polish | **Advanced** |

### Implemented Surface

**Backend tRPC routers** (31 routers): health, auth, companies, countries, identificationTypes, personTypes, regimeTypes, clientTypes, commercialActivities, dashboard, departments, cities, logos, providers, sequentials, units, vatRates, categories, products, orders, customers, purchases, sales, **cashSessions** (Phase 1), inventory, locations, sites, sync, **transfers** (Phase 2), **quotations** (Phase 5), **auditLogs** (Phase 8 / Tier-2 #8), users. For the authoritative list, see [packages/server/src/trpc/router.ts](../packages/server/src/trpc/router.ts).

**Web route modules**: Dashboard, Company, Sites, Sequentials, Locations, Customer Catalogs, Geography, Providers, Categories, Units, VAT Rates, Products, Orders, Purchases, Customers, Sales, Cash Sessions, Inventory, Transfers, Quotations, Audit Logs, Users

**Desktop features**: embedded backend lifecycle, receipt printing, backup/restore, tray/theme/update settings, sync status and trigger APIs, offline DB bridge

### Unique Differentiators

- True offline-first with local SQLite
- Desktop-native Electron (no competitor offers this)
- Open source, no subscription fees, self-hosted
- Colombian/LatAm focus
- tRPC-first transport with type safety end-to-end

---

## 2. What to Build Next — Priority Order

This is the recommended implementation sequence. Each item links to its detailed phase below.

### Tier 1: Deployment Blockers (must ship before pilots)

| # | Item | Why | Phase |
| --- | --- | --- | --- |
| 1 | **i18n foundation** (es-CO/es/en) | English-only UI blocks LatAm deployment. Every new feature adds more hardcoded strings. | Pre-Phase 1 — **Shipped** |
| 2 | **Integer → real migration** for stock/quantity | Blocks ferreterías (2.5m cable) and supermarkets (0.75kg produce). #1 schema blocker. | Phase 1 — **Shipped** |
| 3 | **Cash management and shift control** | Every competitor has this. No cash session = no accountability = no LatAm retail adoption. | Phase 1 — **Shipped** |
| **3a** | **Colombia DIAN fiscal compliance** (DEE + Factura Electrónica via Proveedor Tecnológico) | **Legal blocker since May/July 2024.** Selling Puntovivo to a CO business without this creates fiscal exposure for the user and brand damage for us. See [FISCAL-INTEGRATION.md](./FISCAL-INTEGRATION.md) and [RECEIPT-TEMPLATES.md](./RECEIPT-TEMPLATES.md) (QR + CUFE on the print representation). | **Phase 11 → now P0** |
| **3b** | **POS hardware basics** (ESC/POS printer, cash drawer, barcode scanner) | Operational blocker — real stores need a physical receipt with cut, a drawer that opens, and scan-to-add. See [HARDWARE-POS.md](./HARDWARE-POS.md). | **Phase 12 → now P0** |
| **3c** | **POS touch UI + multi-surface routes** | Retail stores with all-in-one touch terminals and restaurants with KDS / customer display / mobile waiter need variant UI over the same bundle. See [UI-SURFACES.md](./UI-SURFACES.md) and [RECEIPT-TEMPLATES.md](./RECEIPT-TEMPLATES.md) for the print-layout editor that ships alongside. | **Phase 6c → P1** |

### Tier 2: Core Commercial Gaps (competitive table stakes)

| # | Item | Why | Phase |
| --- | --- | --- | --- |
| 4 | Site-owned inventory + transfers | Stock must belong to a site, not the tenant. Transfer workflows need in-transit state. | Phase 2 |
| 5 | Split payments / multi-tender | Basic expectation: pay partially with cash and card. Foundation **shipped** (`sale_payments` table, split-aware `sales.create`, split-tender UI, cash-method-only cash-session accounting). Credit-mix is deferred to Phase 5 alongside on-account sales and abonos. | Phase 5 |
| 6 | Quotations / estimates | B2B, ferreterías, service businesses all need pre-sale conversion. **MVP shipped**: `quotations` + `quotation_items` tables, `quotations.create/list/getById/updateStatus/delete` tRPC, dedicated Quotations page with status badges and draft-only delete, Blob-URL printable receipt + Electron bridge, CSV/Excel/PDF history export, and the `accepted → converted` terminal close that lets an operator mark a quote as resolved once the corresponding sale has been completed through the regular POS. Version history, margin analysis, follow-up reminders, and an auto-prefill sale flow are deferred to later steps. | Phase 5 |
| 7 | Credit sales (ventas a crédito) | Deeply embedded LatAm practice — installments, abonos, contractor accounts. | Phase 5 Ext |
| 8 | Audit trail for sensitive actions | Required for operational trust — void, refund, price override, user change. Foundation **shipped**: `audit_logs` table + transactional `writeAuditLog` helper; admin-only `auditLogs.list` tRPC with filters (action / resourceType / resourceId / actor / date range); `/audit-logs` admin page with filter bar, translated action badges, per-action summary, CSV/Excel/PDF export. **Sensitive-action coverage expanded**: `sales.void`, `sales.returnSale` (refund), `cashSessions.close` (captures over/short), and `inventory.adjustStock` (no-op delta skipped) now emit audit rows alongside the existing `transfers.void`, `quotations.delete`, `quotations.updateStatus` (convert only). `AuditSummary` renders a per-action human string for every audited surface. Remaining gaps: price override, `purchases.void`, `user.disable` — addable by calling `writeAuditLog` from those services with no schema migration. | Phase 8 |

### Tier 3: Market Differentiation

| # | Item | Why | Phase |
| --- | --- | --- | --- |
| 9 | Outbound logistics (pick/pack/ship) | Needed for any store with delivery. | Phase 3 |
| 10 | Promotions / loyalty / gift cards | Behind mainstream POS expectations. | Phase 7 |
| 11 | Colombia DIAN fiscal compliance | DEE + factura electrónica mandatory. Siigo/Alegra own this today. | Phase 11 |
| 12 | Country-parametrizable fiscal rules | Current Colombia-hardcoded logic blocks any non-CO deployment. | Phase 11 Ext |

### Tier 4: Vertical Expansion (after platform is solid)

| # | Item | Phase |
| --- | --- | --- |
| 13 | Multi-vertical module activation system | Phase 0 Foundation |
| 14 | Product variants, serial/lot/batch/expiry | Phase 6 |
| 15 | Restaurant module (tables, KDS, modifiers, tips) | Phase 12 |
| 16 | Pharmacy module (Rx, controlled substances, INVIMA) | Phase 13 |
| 17 | Supermarket module (scales, PLU, perishables) | Phase 14 |
| 18 | Ferretería module (unit conversion, project quoting) | Phase 15 |

### Tier 5: Platform Maturity

| # | Item | Phase |
| --- | --- | --- |
| 19 | Advanced reporting / BI (GMROI, ABC, CLV) | Phase 9 |
| 20 | Hybrid SQLite + PostgreSQL data topology | Phase 10 |
| 21 | Public API, webhooks, integration ecosystem | Phase 11 |
| 22 | Employee shifts, commissions, time tracking | Phase 8 |
| 23 | Transport execution (dispatch, tracking, POD) | Phase 4 |

### Market Segments Coverage — Three Rings

Puntovivo's market strategy is organized into three concentric rings. Each
ring activates via the **module activation system** (see [MODULE-ACTIVATION.md](./MODULE-ACTIVATION.md))
so a minimarket does not carry restaurant or pharmacy code at runtime.

**Ring 1 — Generic retail MVP (target: 8-10 weeks)**
- Neighborhood store, minimarket, papelería, ferretería, boutique with
  variants, carnicería/fruver with scale integration, panadería, heladería.
- Prerequisites: Tier-1 items 3a (DIAN fiscal) + 3b (hardware) + product
  variants + scale.
- Coverage goal: ~80% of Colombian retail POS market.

**Ring 2 — Restaurant + Pharmacy (target: +8-10 weeks after Ring 1)**
- Restaurant: composition/BOM + preparation lifecycle (KDS + tables +
  modifiers) + touch UI + one delivery integration (Rappi). See
  [RESTAURANT-LIFECYCLE.md](./RESTAURANT-LIFECYCLE.md) and
  [PRODUCT-COMPOSITION.md](./PRODUCT-COMPOSITION.md).
- Pharmacy/droguería: batches + expiration (Phase 7) + INVIMA code
  validation + prescription workflow.
- Coverage goal: ~35% additional market (gastronomy + pharma chains).

**Ring 3 — Service verticals (target: +12 weeks after Ring 2)**
- Salons, barbers, spas, vet clinics, workshops: shared appointments
  module + services-as-products + commissions + client assets.
- Optional extensions: gyms (subscriptions), laundromats (tickets),
  motels (rooms).

**Beyond Ring 3**: future verticals (CO + LatAm), LatAm country fiscal
expansion, cross-cutting platform features (BI, franchises, public API,
AI). See [FUTURE-VERTICALS.md](./FUTURE-VERTICALS.md),
[LATAM-EXPANSION.md](./LATAM-EXPANSION.md), and
[LONG-TERM-VISION.md](./LONG-TERM-VISION.md).

---

## 3. Open Technical Risks

### Platform Foundation

- **Fiscal rules are Colombia-hardcoded** — IVA rates, INC, propina (Ley 1935/2018), DIAN endpoints, fiscal regime codes are constants. Must become profile-driven before non-CO deployment. (Phase 11 Ext)
- **No module activation system** — prerequisite for multi-vertical support. (Phase 0)
- **Credit sales (ventas a crédito) missing** — no installment schedules, no abono posting, no configurable credit settings per tenant/company/site. (Phase 5 Ext)
- **Schema changes now flow through versioned Drizzle migrations** after `ENG-002`. `runSchemaSync()` and the raw-DDL mirror in `packages/server/src/db/index.ts` are retired; new schema work edits `schema.ts`, generates a migration, and updates the Drizzle journal. Remaining risk: non-additive migrations still need careful hand-written transition scripts and upgrade testing for existing SQLite installs. (Follow-up: `TEST-002`)

### Sync and Offline

- No formal hybrid data topology contract for local SQLite + remote authority. (Phase 10)
- Persistence is tightly coupled to SQLite-specific Drizzle and `better-sqlite3`. Repository interfaces are not yet extracted from router procedures. (Ticket `API-001` / `ENG-010`)
- Remote replication story is underspecified. (Phase 10)
- Structured logging is now in place across `packages/server` and the Electron main process, but request tracing / correlation IDs are still missing. (Follow-up to `ENG-006`)

### Security

- ~~Main `BrowserWindow` still runs with `sandbox: false`~~ — **shipped as ENG-004** (see Tier-6 table below). The main window now runs under `sandbox: true` with `contextIsolation: true` and `nodeIntegration: false`; the invariant is pinned by `apps/desktop/src/main/__tests__/window-config.test.ts` and enforced in `ci:desktop`.
- ~~`writeAuditLog` wired into only three operations~~ — **substantially shipped as ENG-007**. `writeAuditLog` now fires transactionally from `transfers.void`, `quotations.delete`, `quotations.updateStatus` (convert only), `sales.void`, `sales.returnSale`, `sales.price_override`, `cashSessions.close`, `inventory.adjustStock`, `purchases.void`, `users.create`, and `users.update` (role/isActive changes). Remaining gap: `company_credit_settings` audit, deferred until the credit-sales feature (Phase 5 Ext) lands the table.
- ~~Rate limiting only global~~ — **shipped as ENG-008 + ENG-008b**. `auth.login` has dedicated rate limits (10/IP/60s, 5-failure/username/15min) with `AUTH_RATE_LIMIT_EXCEEDED` error codes and 14 tests from ENG-008; ENG-008b promoted the counters to a DB-backed `login_attempts` table with the in-memory Maps demoted to a write-through cache so a server restart no longer wipes the buckets mid-attack.

### Testing

- `apps/desktop` has a single automated test today (`src/main/__tests__/window-config.test.ts` pinning the sandbox invariant from ENG-004). Broader desktop unit coverage is still pending; most verification is manual.
- A cross-surface E2E suite against the renderer + embedded backend + Electron bridge is partially shipped. The web Playwright suite (`npm run test:e2e:web`) now covers login, role gating, every sidebar module, Spanish localisation, responsive shell, quotations, split-payment sale details, the full transactional lifecycle for sales (complete / refund / void), purchases (complete / return / void), stock adjustments, inter-site transfers (with and without discrepancy, plus the over-receipt client-side block), opening a cash session from zero, manual paid-in cash movements, and cash-session closures (overage, shortage, balanced). 25 tests run in parallel locally and the web suite runs in CI; the local-only Electron smoke (`npm run test:e2e:electron`) covers embedded-server boot, login, dashboard, and the zero-client-issue invariant. Remaining gap: Electron-in-CI. (Ticket `ENG-001`)
- Coverage thresholds are enforced in `ci:web` and `ci:server` via `vitest --coverage --run`. Web thresholds are now `70/70/70/70` after ENG-003b shipped the 13 new + 6 extended test files closing every axis. (Tickets `ENG-003`, `ENG-003b`)

### Build / CI

- Desktop packaging for `.dmg` (macOS) and `.exe` (Windows) is still exercised exclusively via the manual `release.yml` workflow. `ENG-005` now covers `ci:desktop` on Linux/macOS/Windows, but packaging/signing regressions can still hide until release time. (Ticket `ENG-005`)
- No dependency audit (`npm audit` / Dependabot alerts) gate is wired into CI. (Ticket `ENG-009`)

### Frontend Health

- Several feature components exceed 900 lines (`ProductFormModal.tsx`, `InventoryPage.tsx`, `SalesPage.tsx`) and mix data-fetch, state, and presentation. Candidates for extraction of subcomponents + custom hooks. (Ticket `ENG-011`)
- ~~`zustand` is declared in `apps/web/package.json` but has **zero imports** in `apps/web/src/`~~ — **adopted via ENG-018b's `useCartWorkspaceStore`**. Zustand is now a live, tested dependency backing the multi-cart sales workspace (ENG-012 is Shipped). Migrating the remaining Context providers (`AuthProvider`, `TenantProvider`) stays available as a future refactor if ever desired.

### Performance and UX

- Responsive/mobile refinement is weaker in admin/maintenance screens.
- Not every screen uses the same feedback quality level yet.

---

## 3b. Engineering Quality Backlog — Cross-Cutting

These tickets don't belong to any single vertical or commercial phase. They protect the app's long-term maintainability and scalability, and should be sequenced against Tier 1–3 work rather than deferred to "someday". Each ticket is actionable in isolation and has a clear acceptance test.

**Status values** (machine-readable pool discovery):

- `Pending` — never started; eligible for next sprint.
- `Partial` — some sub-steps shipped, remainder listed at the end of the Scope cell under "Remaining:". Still eligible; the agent executes the remaining items as the ticket scope.
- `Shipped` — closed; Scope cell ends with a "Shipped: …" summary. Excluded from pool.
- `Gated` — external dependency (hardware, contract, credentials) blocks start. The gate is documented inside Scope. Excluded from pool until gate clears.
- `Deferred` — explicitly postponed by the operator, not a blocker but not in the active sprint plan. Excluded from pool.

Agents picking the next ticket read this column first, then follow the sequencing recommendation at the end of the section. New tickets are created with `Status: Pending`.

| ID | Title | Status | Scope | Acceptance | Priority |
| --- | --- | --- | --- | --- | --- |
| `ENG-001` | E2E test harness | Partial | Add Playwright against the web app + embedded backend in headless Electron. Cover: login → open cash session → create sale (split tender) → refund → close session. **Step 1 shipped**: `npm run test:e2e:web` runs a Playwright web suite against the standalone backend and Vite app. Global-setup seeds an idempotent baseline (admin/manager/cashier/viewer template users plus a secondary active site) and prunes prior-run artefacts. **Step 2 shipped**: 16 business-flow tests cover sales (complete / refund / void + split-payment details), purchases (complete / return / void), stock adjustments, inter-site transfers (discrepancy + perfect-receipt + over-receipt-blocked), cash-session opening from zero, manual paid-in movements, and cash-session closures (overage / shortage / balanced). Together with the smoke and quotations slices the suite now runs 25 tests in parallel. Every test seeds unique actors and products (random UUID suffix on email, sku, register name) so the suite is fully parallelisable. Every test attaches a client issue tracker that fails the run on unexpected console errors / page errors / 4xx-5xx responses. A `data-row-id` attribute on DataTable rows lets tests pick specific rows deterministically under parallelism. See `e2e/README.md` for the operator guide. **Step 3 shipped**: baseline helpers extracted into `e2e/shared/baseline.ts` so both runners share the same user + site seeding; new `e2e/electron/` suite launches Electron via `_electron.launch()` against a pre-seeded tmpdir DB under `test-results/electron-userdata/` with a smoke test covering login → dashboard and zero-console-error invariant; new `playwright.electron.config.ts`; new root `test:e2e:electron` + `test:e2e` scripts; new `scripts/ensure-electron-main-build.mjs` guard fails fast with an actionable message when the Vite main bundle is absent. New `e2e-web` job in `ci.yml` runs the web suite on every PR with a cached Playwright browser binary keyed on the `package-lock.json` hash and uploads `playwright-report/web` always plus `test-results/playwright-web` on failure (traces, screenshots, videos; retention 7 days). **Remaining**: Electron-in-CI — requires xvfb on ubuntu + a packaged-binary build step that `ci:desktop` does not produce today. Captured as a follow-up; the Electron suite stays local-only until the first externally-signed installer approaches. | `npm run test:e2e` green in CI on Linux; screenshots on failure uploaded as CI artifact. | **High** |
| `ENG-002` | Versioned Drizzle migrations | Shipped | Generate baseline migration from current schema. Replace `runSchemaSync()` raw DDL bootstrap with `migrate()` on startup. Retain `IF NOT EXISTS` only as a one-time adoption shim for existing installs. **Step 1 shipped**: baseline migration (`src/db/migrations/0000_0000_baseline.sql`) captures the full current schema with dynamic `(datetime('now'))` defaults; `initDatabase()` runs `drizzleMigrate()` at boot guarded by an `existsSync` check on `meta/_journal.json`; `ensureMigrationBaseline()` seeds `__drizzle_migrations` on pre-ENG-002 DBs so the baseline is not re-run against an existing schema. **Step 2 shipped**: `DatabaseOptions` / `ServerOptions` now accept a `migrationsFolder` override; `apps/desktop/forge.config.ts` ships `packages/server/dist/db/migrations` via `extraResource`; `apps/desktop/src/main/index.ts` passes `process.resourcesPath/migrations` when `app.isPackaged`, so packaged Electron builds exercise the real migrator end-to-end. A new override-path integration test in `migrations.test.ts` locks the contract. **Step 3 shipped**: `runSchemaSync()` + the `ensureColumn()` / `createIndexIfColumnsExist()` helpers are gone; `packages/server/src/db/index.ts` shrank from 1,638 to 444 lines. `drizzleMigrate()` is now the single schema path — the missing-migrations-folder branch hard-throws an actionable error instead of silently falling back. Catalog seeds hoisted into a defensive `seedCatalogs()` hook gated by table existence, so adopted DBs that skip the transitional release log an actionable warning instead of crashing. A new `migrations-parity.test.ts` pins the invariant that `drizzleMigrate` alone produces the full schema; `migrations.test.ts` gained a missing-folder-throws assertion and an adopted-DB catalog-seed assertion; `db.seed.test.ts` legacy case rewritten to validate the adoption shim's column-preservation contract. | Adding a column requires only editing `schema.ts` + `drizzle-kit generate`; re-running against an existing DB is a no-op. | **High** |
| `ENG-003` | CI coverage threshold | Shipped | Run `vitest --coverage --run` in `ci:web` and `ci:server`. Fail the job below the thresholds declared in `vitest.config.ts`. Upload LCOV. **Shipped**: both workspaces now declare enforceable v8 coverage thresholds (server 80/80/77/63 statements/lines/functions/branches; web 65/65/68/60 — the web floor replaces the previously unenforced 70/70/70/70 declaration, because the suite had drifted below). `ci:web` and `ci:server` invoke the new `test:coverage` variant that passes `--coverage`, making thresholds actually gate. `lcov` reporter is wired into both configs and `.github/workflows/ci.yml` uploads `coverage/lcov.info` as an artifact for both jobs. Follow-up `ENG-003b` tracks raising the web floor back toward 70% via new component/route tests; it is a pure test-writing effort with no CI plumbing left to do. | PR lowering coverage below threshold fails CI. | **High** |
| `ENG-003b` | Raise web coverage back to 70% | Shipped | The web floor landed at 65/65/68/60 in ENG-003 to match the actual state of the suite at the time. Write new component + route tests to bring every axis back to 70% (statements/lines/functions/branches) and then raise the thresholds accordingly in `apps/web/vitest.config.ts`. Scope is test-writing only; no CI or config plumbing change. **Shipped**: 13 new test files + 6 extended existing files closed every axis. New: `roleAccess.test.ts`, `useElectron.test.ts`, `siteSelection.test.ts`, `siteStorage.test.ts`, `authStorage.test.ts`, `AuthProvider.test.tsx`, `TenantProvider.test.tsx`, `saleHistoryExport.test.ts`, `purchaseHistoryExport.test.ts`, `quotationHistoryExport.test.ts`, `useTableExport.test.ts`, `exportService.csv.test.ts`, `utils.test.ts`. Extended: `auditLogsExport.test.ts`, `pricing.test.ts`, `defaultLayouts.test.ts`, `providerState.test.ts`, `checkoutPayment.test.ts`, `saleCart.test.ts`. Final coverage 78.64 / 79.31 / 80.16 / 70.20 (statements / lines / functions / branches) — all four above the new 70/70/70/70 floor. Branches axis was the toughest gap (61.42 → 70.20) and drove most of the test surface. Pure test-writing per the AC; the only production-code change was a defensive collateral fix to `formatDate`/`formatDateTime` returning empty string on Invalid Date (their existing call sites would have thrown on null inputs, surfaced by the new export-utility tests). | All four axes ≥ 70% in `apps/web/vitest.config.ts`. | **Medium** |
| `ENG-004` | Electron main window sandbox | Shipped | Audit every `ipcMain` handler and preload export; remove Node-only APIs from the renderer path. Flip `sandbox: true` on the main `BrowserWindow`. **Shipped**: the security-critical webPreferences now live in `apps/desktop/src/main/window-config.ts` as a single-source-of-truth constant (`MAIN_WINDOW_WEB_PREFERENCES`), and `buildMainWindowWebPreferences()` constructs the exact `BrowserWindow` shape consumed by `main/index.ts`. The bridge audit confirmed every preload export is a pure `contextBridge → ipcRenderer.invoke` wrapper (four namespaces, 29 methods), so no preload refactor was needed. A node-test regression pin in `src/main/__tests__/window-config.test.ts` asserts `sandbox === true`, `contextIsolation === true`, `nodeIntegration === false`, and that the assembled `webPreferences` object passed to `BrowserWindow` keeps those flags intact; it is wired into `ci:desktop` — a tamper-check confirmed the test fails with a clear `ERR_ASSERTION` when any field is weakened. | App boots with `sandbox: true`; all renderer→main calls still pass; add regression test that asserts `webPreferences.sandbox === true`. | **High** |
| `ENG-005` | Desktop CI build matrix | Shipped | Extend `ci.yml` to run `npm run ci:desktop` on `ubuntu-latest`, `macos-latest`, `windows-latest`. Packaging step may stay in `release.yml`, but typecheck + lint + unit tests must run on all three. **Shipped**: the `desktop` job in `.github/workflows/ci.yml` now uses `strategy.matrix.os` on all three runners with `fail-fast: false` so one OS's break does not mask the others. The other jobs (web, backend, release-automation) stay on `ubuntu-latest` because ENG-005 scopes the matrix specifically to desktop — where native modules compile per-platform and cross-OS regressions historically hid until release time. A cross-platform audit of every script that `ci:desktop` invokes (root `package.json`, server/web/desktop workspace scripts, `packages/server/scripts/copy-migrations.mjs`, `scripts/ensure-native-runtime.mjs`) found zero POSIX-only hazards — the repo was already path-portable and `ensure-native-runtime.mjs` has explicit `process.platform === 'win32'` handling. `better-sqlite3@12.8.0` and `argon2` ship prebuilt binaries for linux-x64, darwin-x64, darwin-arm64, and win32-x64, so `npm ci` does not need a native toolchain on any runner. Packaging itself (`electron-forge make`, signing) remains in `release.yml` because it needs signing material the CI runners do not carry. | CI badge reflects all three OS; a PR that breaks a Windows-only path fails CI. | **Medium** |
| `ENG-006` | Structured logging | Shipped | Replace `console.log`/`console.error` calls in `packages/server` and `apps/desktop/src/main` with a `pino` logger, namespaced per module (`sales`, `sync`, `cash-session`, …). Redact PII (emails, passwords, tokens). **Shipped**: `packages/server/src/logging/logger.ts` exports `rootLogger` plus `createModuleLogger(name)` which returns a pino child tagged `module: <name>`. Redact covers `password`, `passwordHash`, `token`, `refreshToken`, `jwtSecret`, `email`, `authorization`, `cookie`, nested `headers.*` and one-level wildcards (`*.password`, `*.token`, etc.) — matches are replaced with `[Redacted]` before pino emits. 29 server console sites + 12 Electron main sites migrated to module loggers (`auth`, `db`, `seed`, `trpc`, `server`, `sse`, `standalone`, `electron-main`, `renderer`, `auto-updater`, `backup`, `print`). Fastify adopts the shared root logger when `verbose: true` so HTTP + app logs share one NDJSON stream. Two banner call sites keep plaintext on `process.stdout.write`: the standalone startup banner and the first-run admin credentials — both are one-shot operator UX and the credentials banner deliberately bypasses the structured stream so downstream aggregators cannot leak plaintext. No pino `transport` is configured (pino-pretty's worker threads don't resolve under Electron's CJS Vite bundle); developers pipe manually via `npm run dev:server \| pino-pretty`. `no-console` ESLint rule added to server workspace and scoped to `apps/desktop/src/main/**`; tests keep their existing console allowances. 14 new unit tests in `logger.test.ts` lock the redact policy + module propagation. Level driven by `PUNTOVIVO_LOG_LEVEL` env var with `info`/`debug` defaults by `NODE_ENV`. | Logs are single-line JSON in production; `logger.child({ module: 'sync' })` works; no raw `console.*` remains outside tests. | **Medium** |
| `ENG-007` | Audit trail expansion | Partial | Call `writeAuditLog` from: `sales.void`, `sales.refund`, `purchases.void`, `users.create/disable/changeRole`, manual price overrides in `sales.create`, and any change to `company_credit_settings`. Each entry records `before`/`after` JSON where applicable. **Substantially shipped**: `sales.void`, `sales.returnSale`, `cashSessions.close`, `inventory.adjustStock`, `purchases.void`, `users.create`, `users.update` (role / isActive only), and manual price overrides in `sales.create` now all emit audit rows inside their own transaction. Only `company_credit_settings` audits remain, deferred until the credit-sales feature (Phase 5 Ext) lands that table. | Unit test per site asserts an `audit_logs` row is written with the correct `action` + `resource_type`. | **High** |
| `ENG-008` | Auth hardening | Shipped | Rate-limit `auth.login` specifically (per IP and per username). Lock-out escalation after N failed attempts. Document the policy in `docs/SECURITY.md`. **Shipped**: `packages/server/src/security/loginRateLimit.ts` carries two in-memory TTL buckets — 10 attempts per IP per 60 seconds and 5 failed attempts per username per 15 minutes. Both buckets increment on every unauthorized branch (user not found, disabled user, wrong password, disabled tenant); a successful login clears the username bucket while the IP bucket decays via TTL so a single legitimate login cannot amnesty a stuffing source. `auth.login` throws `TOO_MANY_REQUESTS` with `errorCode: AUTH_RATE_LIMIT_EXCEEDED` and `details: { kind, key, max, secondsUntilReset }` when either cap is hit. 14 new tests (10 service units + 4 integration scenarios incl. the 50-bad-logins acceptance gate) join the suite. `docs/SECURITY.md` documents policy, attack coverage, and future hardening tracked as `ENG-008b` for DB-backed persistence. | Load test: 50 bad logins from one IP returns `429` before the 60-second window completes. | **Medium** |
| `ENG-008b` | Persistent login rate-limit state | Shipped | ENG-008 used in-memory buckets that a server restart wiped. Promote the `loginRateLimit` service to read/write from a `login_attempts` Drizzle table so multi-tenant cloud deployments survive restarts and can observe attack telemetry historically. Keep the current in-memory fast path as a cache to avoid DB round-trips per login attempt. **Shipped**: new `login_attempts` table (migration `0006_login_attempts.sql`) keyed on `(kind, key)` with an `expires_at` index — intentionally NOT tenant-scoped so one IP hammering many tenants still trips the global cap. `packages/server/src/security/loginRateLimit.ts` rewritten to accept a `DatabaseInstance` on every call, with the Maps demoted to a write-through cache (reads fall back to the DB, writes upsert the row first and then mirror the state). Adopted-DB safety: a table-existence check logs one warning and falls back to cache-only if `ensureMigrationBaseline()` pinned the journal before 0006 ran. 11 unit tests in `loginRateLimit.test.ts` exercise the full service against `:memory:`; new `loginRateLimit-persistence.test.ts` boots against a tmpdir file, saturates the username cap, restarts via `closeDatabase() + initDatabase()`, and asserts the next attempt still throws `AUTH_RATE_LIMIT_EXCEEDED`. `auth.test.ts` gains a DB-row assertion covering the persistence contract. Docs synced: `SECURITY.md` "Persistence and restart behavior" section describes the cache-plus-DB model; the hardening list drops the now-closed ENG-008b bullet. | Attacker tripping the username cap, server restart, next attempt still 429. | **Medium** |
| `ENG-009` | Dependency audit gate | Shipped | Add `npm audit --production --audit-level=high` to `ci:web`, `ci:server`, and `ci:desktop`. Add a Dependabot config in `.github/`. **Shipped**: a new `ci:audit` script at the root runs `npm audit --production --audit-level=high` and is composed as the first step of every per-workspace CI script, so a new HIGH or CRITICAL CVE in any production dep fails CI immediately. Three pre-existing prod vulns (fast-jwt critical, fastify high, dompurify moderate) were cleared via a minimal `npm update fastify fast-jwt dompurify` bump that preserved npm workspace hoisting (the brute-force `npm audit fix` approach broke it). `.github/dependabot.yml` was rewritten to drop a stale `gomod` entry for the long-gone `/backend` module, and now opens grouped monthly npm PRs (production + development buckets; react / tanstack sub-groups) and weekly grouped github-actions PRs. Electron and `@electron-forge/*` are explicitly excluded from Dependabot because each bump requires a manual packaged smoke. Tamper-check against the pre-bump lockfile confirmed `npm run ci:audit` exits 1 with the three vulns — the gate fires, not just passes. `docs/SECURITY.md` carries the policy and threshold rationale. | A new transitive dependency with a known `high` CVE fails CI. | **Medium** |
| `ENG-010` | Repository interfaces (Phase 10 prep) | Pending | Extract persistence from routers into per-domain repository interfaces (`SalesRepository`, `InventoryRepository`, …) implemented today by a Drizzle-SQLite adapter. Keeps routers dialect-neutral. | All router procedures touch the DB exclusively through an interface; no router imports `better-sqlite3`. | **Medium** |
| `ENG-011` | Break up oversized components | Pending | Split `ProductFormModal.tsx` (960 l), `InventoryPage.tsx` (935 l), `SalesPage.tsx` (581 l), `QuotationCreateModal.tsx` (567 l), and `sales.ts`/`purchases.ts` server routers (1.4–1.6 k l) into focused subcomponents + custom hooks + per-sub-feature service files. No file over ~400 lines without justification. | Touched files drop below 400 l; behavior parity covered by existing + new unit tests. | **Medium** |
| `ENG-012` | Remove or adopt `zustand` | Shipped | Zustand is declared in `apps/web/package.json` but has zero imports. Either delete the dependency or migrate the two Context providers (`AuthProvider`, `TenantProvider`) that would benefit from it. **Shipped (closed as a no-op during ENG-016 docs audit)**: the "zero imports" premise is stale — ENG-018b shipped `useCartWorkspaceStore.ts` with Zustand (`import { create } from 'zustand'` + `persist` middleware), actively consumed by `SalesPage.tsx`, `SuspendedSalesPanel.tsx`, `AuthProvider.tsx` (for `resetAllWorkspaces` on logout), and the store's own tests. The dependency is legitimately adopted, so the AC's "either delete or adopt" is satisfied by the adopt branch. The AuthProvider/TenantProvider context-based design still works fine today and does not block any operator workflow — migrating them to Zustand is now a future refactor ticket (if ever raised) rather than a gate on this one. | Either `zustand` is gone from `package.json`, or both providers are refactored to a Zustand store with tests. | **Low** |
| `ENG-013` | Consolidate `CLAUDE.md` / `AGENTS.md` | Shipped | The two files are byte-for-byte duplicates in spirit but evolve separately. Keep `CLAUDE.md` as canonical and make `AGENTS.md` a one-line pointer, or vice versa, to remove drift. **Shipped (closed as a no-op during ENG-016 docs audit)**: the consolidation is already in place — `CLAUDE.md` is a filesystem symlink to `AGENTS.md` (verified via `ls -la`; line 5 of `AGENTS.md` explicitly documents it: "`CLAUDE.md` is a symlink to this file, so both tools see the same source of truth. Edit `AGENTS.md` directly."). The symlink enforces single-source-of-truth at the filesystem level — a divergence would require actively replacing the symlink with a regular file, which shows up as a file-mode change in `git diff` and is caught at review time. A dedicated CI check is overkill given the symlink invariant; if stricter enforcement is ever desired it can be tracked as a new follow-up. | A single source of truth remains for operational guidance; CI fails if the two files diverge. | **Low** |
| `ENG-014` | Split payments — credit mix | Pending | Current `sale_payments` covers cash + card + transfer split tender. Extend to mix on-account installments with immediate tender (needed for layaway / "abono a crédito"). | `sales.create` accepts `credit` + `cash` in the same payload; the sale becomes partial-credit and the `credit_sales` ledger is created only for the credit portion. | **Medium** |
| `ENG-015` | Dev seed command | Shipped | Single command that loads a realistic multi-site / multi-user / multi-product dataset into a fresh dev install so QA, demos, and reproducible bug repros do not need manual catalog clicking. Detailed spec: [DEV-SEED.md](./DEV-SEED.md). Dev-only; refuses to run in production. **Shipped**: `seedDevData()` creates the `demo-co` tenant isolated from `default`, with 6 role-mixed users sharing `Admin123!Dev`, 2 sites with site-specific sequential prefixes (`VTA-N-` / `VTA-S-`), 50 products with 60/40 per-site stock, 30 Colombian customers, 3 receipt templates, and ~20 historical sales per cashier across a closed + an open cash session. CLI entry at `packages/server/src/scripts/seed-dev.ts` with production guard, `SEED_PRESET=default\|large`, `SEED_RESET=true`, and `SEED_TARGET=desktop` (resolves Electron userData for macOS/Linux/Windows). Tests cover row counts, stock = Σ(inventory_balances) invariant, argon2 login roundtrip, idempotent short-circuit, and cross-tenant isolation. | `npm run seed:dev` populates 1 tenant, 2 sites, 6 users, 50 products, 30 customers, 5 providers, ~40 historical sales, 6 purchases, 5 quotations, and 1 receipt template per kind. Idempotent without `--reset`. | **Medium** |
| `ENG-016` | Receipt templates editor UX pass | Partial | Follow-up improvements tracked in [RECEIPT-TEMPLATES.md](./RECEIPT-TEMPLATES.md) §Follow-up improvements after the Iter 2 release: drag-and-drop reorder with animation, `{{autocomplete}}` + syntax highlighting + auto-close pairs in `text.value`, template functions (`limit`, `max`, `currency`, `date`, …), bindings captions for `itemsTable` and `totalsBlock`, a Puntovivo-branded footer atomic block, explicit variable-resolution error markers in the preview, and block-move animation. Pulls design cues from JasperReports / Crystal / Siigo / Metabase. **Shipped (pass 1)**: items 4, 5, 6 landed together. Item #4 — bindings captions above `itemsTable` (permanent) and `totalsBlock` (collapsible explainer) keyed off new i18n strings under `receiptTemplates.editor.blockFields`. Item #5 — new `appFooter` atomic block type in the Zod discriminated union + renderer case (`services/receipt-renderer.ts APP_FOOTER_METADATA`) that emits `Puntovivo <version>`, URL, and support contact in HTML + ESC/POS; soft-hidden by `show: false`; included in every default preset (editor starter layouts + dev seed) with a toggle in the editor form. Item #6 — small framework-agnostic FLIP helper at `apps/web/src/lib/flipAnimate.ts` wired into `ReceiptTemplateEditor.moveBlock` so `↑`/`↓` presses animate each card to its new position over 180ms, short-circuits instantly under `prefers-reduced-motion: reduce`. 3 new server tests + 9 FLIP-helper tests + 2 defaultLayouts assertions + 5 editor component tests; en + es i18n updated in neutral LATAM Spanish. **Shipped (pass 2)**: item #1 — drag-and-drop reorder via `@dnd-kit/sortable`. Adopted `@dnd-kit/core` + `@dnd-kit/sortable` (~8kB gzipped) and wrapped the block list with `<DndContext>` + `<SortableContext>` (vertical strategy + `PointerSensor` with 4px activation + `KeyboardSensor` with `sortableKeyboardCoordinates`). New internal `SortableBlockRow` attaches the drag listeners to a dedicated `GripVertical` icon so the row title stays clickable and the `↑/↓` buttons + trash button keep working as the a11y fallback. `<DragOverlay>` portal renders the dragged-card clone for visual continuity. `onDragEnd` routes through a new `moveBlockTo(fromIndex, toIndex)` helper that captures a FLIP snapshot before the state mutation so pass-1's `flipAnimate` plays the post-drop landing transition. New i18n keys under `editor.dragAndDrop.*` (en + es neutral LATAM). Three new component tests pin grip aria-label presence, `data-flip-key` survival across the wrapper, and `↑/↓` button coexistence. **Shipped (pass 3)**: item #3 — template functions. New `services/template-expression.ts` ships a recursive-descent parser, AST evaluator, and 12-function whitelist (`currency`, `date`, `upper`, `lower`, `round`, `limit`, `concat`, `default`, `abs`, `max`, `min`, `sum`). `currency()` reuses the renderer's `formatReceiptAmount` so it inherits ENG-017's tenant-locale formatting; `date()` defaults to the tenant's `dateFormatShort` (now exposed on `ReceiptRenderLocale`) with a `yyyy MM dd HH mm ss` mini-DSL. Zod refinement walks the AST, rejecting unknown functions, wrong arity, unknown namespaces, and `concat("javascript:", …)` scheme bypasses on `qr.source`. Defense-in-depth: `lookupPath` swapped from `in` to `Object.prototype.hasOwnProperty.call` so prototype-chain segments cannot leak; the evaluator re-checks arity before dispatch; `currency`/`round` clamp decimals to 20 to keep `Math.pow(10, n)` finite. HTML escape boundary preserved (escape runs after concat). Editor: a collapsible `<details>` "Available functions" cheat-sheet now sits below the `text.value` textarea, listing each function's signature + translatable description + canonical example. 12 new server-rendering tests (HTML, ESC/POS, escape boundary, Zod rejections, default-preset regression) plus 46 expression-engine tests (tokenizer, parser, evaluator per function, validator per failure mode, prototype-chain / arity / decimals-clamp regressions) plus 2 new editor component tests. Conditional family (`if/eq/gt`) deliberately deferred per the spec's "(later)" tag. **Shipped (pass 4)**: item #2 closed and item #7 parser-side error markers shipped via CodeMirror 6 adoption. New `apps/web/src/features/receipt-templates/{templateLanguage.ts, templateAutocomplete.ts, templateLinter.ts, TextBlockEditor.tsx}` + tests + 9 new deps (codemirror, @codemirror/{state,view,language,autocomplete,lint,commands}, @lezer/highlight, @uiw/react-codemirror, lazy-loaded into the admin-only `/receipt-templates` route; verified build emits ReceiptTemplatesPage at 493.00 kB / 158.00 kB gzip and main index at 681.98 kB / 210.76 kB gzip). The new `<TextBlockEditor>` replaces the inline `<textarea>` for `text.value` and ships: (a) custom `{{` ↔ `}}` auto-close via an EditorView.inputHandler watching for `{` after `{`; (b) syntax highlighting via a StreamLanguage tokenizer + standard `@lezer/highlight` tags (`bracket`, `variableName`, `function(variableName)`, `string`, `number`, `punctuation`, `invalid`); (c) keyboard-navigable autocomplete dropdown sourced from `templateAutocomplete.planSuggestions()` (5 namespaces + 12 functions + per-namespace property catalog mirroring the documented whitelist); (d) inline error markers via `@codemirror/lint` integration — unknown namespaces, unknown functions, wrong arity, and unparseable expressions surface as red squiggly underlines with translatable hover tooltips. The web-side parser in `templateLinter.ts` is a deliberate clone of the server parser (server import would pull in fastify/drizzle into the web bundle); two parity tests pin drift — function names on both web + server, plus a length-cap mirror at `MAX_EXPRESSION_LENGTH=200`. 60 new tests across the four files plus a 30-test TextBlockEditor harness driving real CodeMirror transactions. **Shipped (pass 5)**: item #7's runtime hints closed via `receiptTemplates.variableAvailability` admin tRPC procedure (reads the active tenant's `companies` row + `tenants.settings.fiscal_dian_enabled`, returns a `Record<namespace, Record<field, boolean>>` map; admin-only; sale/item/tender keys pin to true, fiscal keys reflect the tenant flag, company keys reflect actual column population) + a new CodeMirror 6 Decoration extension (`apps/web/src/features/receipt-templates/templateUnavailableDecorations.ts`) that walks each `{{ namespace.field }}` token in the buffer and dims paths whose availability resolves false — `opacity: 0.55; font-style: italic; underline dotted` style + a `hoverTooltip` rendering the translatable `editor.codeEditor.unavailableVariable` copy with the offending path interpolated. The extension layers under the linter so a single token can carry both severities. `useVariableAvailability` hook caches the response with a 60s staleTime; `TextBlockEditor` accepts an `unavailableVariables` prop plus a `useEffect` that dispatches a CM6 `StateEffect` to update the StateField without remounting on locale or settings changes. 6 server tests + 17 web decoration tests + 3 TextBlockEditor tests; locale-parity green; coverage floor preserved. **Remaining**: item 8 — parked low-priority ideas (WYSIWYG drag, per-site templates, JSON import/export). Item #7 fully shipped. | All eight items in RECEIPT-TEMPLATES.md §Follow-up improvements land with the matching tests and i18n. | **Medium** |
| `ENG-017` | Country / locale / currency configuration | Shipped | `formatCurrency()` currently defaults to `USD` regardless of tenant locale, so a Colombian tenant sees totals render as `0,00 US$` instead of `$ 0 COP`. Build a global `country_catalog` + `currency_catalog` (seeded with LATAM + USA, 21 countries / 18 currencies) + per-tenant `tenant_locale_settings` that bundles language, currency, symbol, decimals, timezone, date format, first-day-of-week, and the locally-expected tax-ID types. Every formatter (`formatCurrency`, `formatDate`, receipt renderer, fiscal doc emitter, quotation PDF) reads the active tenant's resolved locale through one helper instead of using hardcoded defaults. Add an admin "Locale & currency" section on Company with a live-preview strip. Full spec + country matrix in [LOCALE-CURRENCY.md](./LOCALE-CURRENCY.md). **Shipped**: migration 0003 adds the 3 catalog tables, `services/tenant-locale.ts` resolves per-tenant locale with override-shadow logic + US/USD fallback, `receipt-renderer.ts` formats currency amounts through the resolved locale, `tenantLocale` tRPC router exposes `get`/`update`/`listCountries`/`listCurrencies`, `LocaleProvider` wires a module-level singleton that the ~140 call sites of `formatCurrency` pick up without touching each call site, `CompanyLocaleSettingsCard` ships the admin picker with country dropdown + overrides + live-preview strip. Dev seed sets `demo-co` to `countryCode='CO'`. 9 server resolver tests + 3 card tests + locale-parity green. | A tenant with `countryCode='CO'` renders all currency displays as COP with 0 display decimals and `dd/MM/yyyy` dates; `countryCode='US'` stays as USD / MM/dd/yyyy; switching the setting invalidates the resolver cache and propagates to every page without a reload. | **Medium** |
| `ENG-018` | Sales park-and-resume (Iter 6) | Shipped | Backend supports `status='draft'` on sales but the UI has no affordance. Build `sales.suspend`, `sales.resume`, `sales.listDrafts`, `sales.discardDraft` procedures with suspendedAt/suspendedBy/suspendedLabel columns and per-cashier lock (manager override). Add a Zustand `useCartWorkspace` persisted per user for multi-cart workflow, a `SuspendedSalesPanel`, a "Suspender" button in `SalesCheckoutPanel`, and `Ctrl+P` / `Ctrl+R` shortcuts with input-focus guard. Warn when closing a cash session with outstanding drafts. Full per-commit spec in [SPRINT-PLAN.md §3](./SPRINT-PLAN.md). **Shipped** across 5d8f720 (server procedures), 05eea2d (audit labels for park/resume), 38e2a47 (ENG-018c server — `completeDraft` + `discardDraft` stock reversal), and the ENG-018b diff (Zustand multi-cart workspace, `SuspendedSalesPanel`, Suspend / New sale buttons, `Ctrl+P` / `Ctrl+R` / `Ctrl+Shift+P` shortcuts, SalesHistoryTable row-selection, `CashSessionCloseModal` drafts warning, E2E round-trip covering cart A → suspend → cart B → charge B → resume A → charge A). | Cashier creates cart A, suspends, creates cart B, charges B, resumes A, charges A. Both complete. Stock decrements correctly per site. Two cashiers cannot resume the same draft concurrently. | **High** |
| `ENG-019` | Sales receipt reprint (Iter 7) | Shipped | Server `sales.getForReprint({ saleId, reason?, reasonDetail? })` landed in 5d8f720 with `reprintCount` / `lastReprintedAt` / `lastReprintedBy` columns, `sale.reprint` audit rows carrying reason + ordinal count in metadata, and role-aware permissions (cashier limited to active cash session, manager/admin unrestricted). UI ship in 05eea2d adds a prominent reprint action in `SaleDetailsModal` with a reason-picker modal (paper_out / customer_request / prior_print_error / other + free-text detail), a pluralised history banner when `reprintCount > 0`, and full en + es localisation. The admin audit viewer surfaces the new action with a one-line summary derived from metadata. Reuses the Iter 2 receipt renderer directly — no duplication. **Follow-up (optional)**: row-level `Reimprimir` action in `SalesHistoryTable` and the `Ctrl+Shift+P` shortcut depend on ENG-018's Ctrl-guard lift and will land together; the modal entry point already covers the operator flow. | Admin completes a sale, navigates to history, reprints; DB row shows `reprintCount = 1` and an audit row with `action = 'sale.reprint'` exists. Cashier cannot reprint another cashier's past-shift sale. | **High** |
| `ENG-018b` | Sales park-and-resume UI (follow-up to ENG-018) | Shipped | Deliver the operator-facing UX for ENG-018's server plumbing: Zustand `useCartWorkspaceStore` persisted per tenant+user, refactor `SalesPage.tsx` to read the active cart from the store, add a "Suspend" button + label prompt to `SalesCheckoutPanel`, build `SuspendedSalesPanel` with resume/discard actions, lift the `Ctrl`/`Meta` early-return in `useSalesKeyboardShortcuts.ts` so `Ctrl+P` (suspend), `Ctrl+R` (open resume panel), and `Ctrl+Shift+P` (reprint selected row in history) work alongside the existing Alt+X shortcuts, add a close-session prompt when drafts remain, and extend E2E in `e2e/web/business.spec.ts`. **Shipped**: Zustand store with `persist` middleware + ownerKey scoping, `SuspendedSalesPanel` with Retomar/Descartar actions + ConfirmModal, SalesPage integration with suspend orchestration (`sales.create(draft) → sales.suspend`), resume orchestration (`sales.resume → hydrateFromResumed`), resumed-cart banner with items-locked hint, Charge detection that routes resumed carts through `sales.completeDraft` (018c) while fresh carts stay on `sales.create`, `CashSessionCloseModal` suspended-drafts warning, row-selection in `SalesHistoryTable` + Ctrl+Shift+P that opens the Reprint modal for the focused row, hook tests + close-modal tests + store tests, and the E2E round-trip in `business.spec.ts`. | Cashier creates cart A, suspends, creates cart B, charges B, resumes A, charges A. Two cashiers cannot resume concurrently (one gets FORBIDDEN). Close-session dialog warns when ≥1 draft outstanding. Ctrl+P / Ctrl+R / Ctrl+Shift+P fire outside editable inputs and are suppressed inside them. E2E extension green on all three runners. | **High** |
| `ENG-018c` | Sales draft completion procedure + discardDraft stock reversal | Shipped | Server gap that surfaced during ENG-018b planning. Added `sales.completeDraft({ saleId, paymentMethod, paymentStatus, payments, amountReceived, notes })` that flips a non-suspended draft to `status='completed'`, inserts real `sale_payments` rows (replacing the placeholder rows created at draft time), posts the cash movement against the caller's active cash session (re-binds `cashSessionId` so cash reports aggregate on the physical landing session), and emits a `sale.complete` audit row inside the same transaction. Items stay locked at draft-create time so stock is never double-debited. `sales.discardDraft` fixed to mirror `sales.void`'s stock reversal: loop line items, restore `products.stock`, re-credit `inventory_balances` at the original cash session site, insert an `inventoryMovements` row of type `return`, and record `reversedItems` in the audit metadata. Ownership gate widened to accept `createdBy` so orphan drafts (suspend failed mid-flight) are discardable by the cashier without a supervisor override. **Shipped in 38e2a47**; tests extend `sales-park-and-reprint.test.ts` to 20 cases total (+7). | After ENG-018c ships, a cashier can create a draft via `sales.create({ status: 'draft' })`, optionally suspend + resume, and finalize with `sales.completeDraft` — the draft's sale row becomes `status='completed'` with payments and cash movement attached and zero extra stock movement. Discarding a draft with N items restores N units to inventory, matching the void semantics. No existing tests regress. | **High** |
| `ENG-020` | Fiscal DIAN data model + MockAdapter (Iter 3 Fase A) | Shipped | Prepare the whole fiscal DIAN domain without waiting for a Proveedor Tecnológico contract. Add global read-only `dian_identification_types` (seeded with 10 official DIAN codes), `fiscal_documents` / `fiscal_document_items` / `fiscal_numbering_resolutions` / `fiscal_certificates` tables with immutable buyer and line snapshots, a `FiscalAdapter` interface with a deterministic `MockAdapter` (canonical CUFE SHA-384 from Anexo 1.9 vectors), hooks in `sales.complete` / `sales.void` / `sales.return` that route through the adapter, and an architectural lint (vitest) that forbids `reports.fiscal.*` routers from importing `customers` or `products` so historical reports stay immutable. Depends on **ENG-017** landing first. Full spec (including the 5 modeling decisions) in [FISCAL-INTEGRATION.md](./FISCAL-INTEGRATION.md); per-commit breakdown in [SPRINT-PLAN.md §5](./SPRINT-PLAN.md). **Shipped**: migrations `0004_dian_identification_types.sql` + `0005_fiscal_documents.sql` land the global DIAN ID catalog and 4 tenant-scoped fiscal tables with the frozen buyer + line snapshot columns; `services/fiscal/cufe.ts` computes deterministic SHA-384 CUFEs per Resolución 165/2023; `MockAdapter` + `FiscalAdapter` interface + `registry.ts` adapter singleton; `orchestrator.emitFiscalDocument` is idempotent by `(tenant, source, sourceId, kind)` and wires into four sale lifecycle points (`sales.create` when completed, `sales.completeDraft`, `sales.void`, `sales.returnSale`) as a best-effort post-tx hook behind `tenants.settings.fiscal_dian_enabled` (default false for backward compat). `reports.fiscal.list` + `getByCufe` admin-only router reads the frozen snapshots without joining `customers` / `products`; `architectural-lint.test.ts` pins that invariant. Four UI placeholders — `FiscalDocumentListPage`, `FiscalReportsPage`, `FiscalHabilitationWizard`, `FiscalContingencyIndicator` — with en/es i18n namespaces and admin-only nav entries. Dev seed enables the flag for `demo-co` and inserts one DEE resolution per site + one placeholder certificate; the 20 seeded sales now emit 20 fiscal documents. 33 new tests (6 CUFE, 7 MockAdapter, 10 orchestrator, 7 reports router, 3 architectural lint) + extended seed-dev assertion for fiscal parity. `MockAdapter` swap to a real PT is the one-file change gated as ENG-021. | Selling a sale emits a `fiscal_document` with a frozen buyer snapshot; editing the customer row afterwards does not change the emitted document. Consumidor final sales (no `customerId`) use NIT 222222222222 without creating a fake customer. Report queries parse-lint clean (no joins with customers/products). | **High** |
| `ENG-021` | Fiscal DIAN PT integration (Iter 3 Fase B) | Gated | 🟡 **Gated**: requires signed contract with a Proveedor Tecnológico (Facture / HKA / Gosocket), sandbox + production credentials, the tenant pilot's DIAN digital certificate, DIAN numbering resolution associated with the software, a validated PT POC (DEE + FEV + NC + ND + fetchStatus out-of-repo), and an agreed PT error-code → `ServerErrorWithCode` map. Swap the `MockAdapter` from ENG-020 for `FactureAdapter` (or `HkaAdapter`), add real CUFE SHA-384 signing with the tenant certificate, XAdES-EPES, contingency daemon with retry backoff, and the habilitation wizard wired to the real adapter. No domain changes beyond the adapter file — the seams from ENG-020 hold. | Production habilitation completes the DIAN test set; a simulated PT 503 writes to the contingency queue and resends on recovery; adapter-swap test proves Facture ↔ HKA parity on canonical vectors. | **High (gated)** |
| `ENG-022` | Hardware POS base (Iter 4) | Gated | 🟡 **Gated**: requires physical test lab hardware (Xprinter / Epson / Bixolon 58mm or 80mm thermal printer, Honeywell / Zebra USB HID scanner, RJ11 cash drawer chained to the printer). Add `site_peripherals` table, `PrinterAdapter` interface with `system` (current `webContents.print()`) and `escpos` (new — node-thermal-printer or escpos) drivers, cash drawer kick (`ESC p m t1 t2`) before paper cut, `useBarcodeScanner` hook capturing HID keyboard-wedge bursts with EAN-13 checksum and prefix 20-29 price-embedded parsing, and a peripherals configuration page under Setup. Full spec in [HARDWARE-POS.md](./HARDWARE-POS.md). | Sale print emits valid ESC/POS bytes, drawer opens before the cut, scanner captures EAN-13 and adds the product to the cart within 100ms of Enter. | **High (gated)** |
| `ENG-023` | Bold payment terminal (Iter 5) | Gated | 🟡 **Gated**: requires Bold sandbox credentials + physical Bluetooth terminal for tests, AND depends on **ENG-022 commit 1** which installs the `PaymentTerminalAdapter` interface. Implement `BoldAdapter` with `charge(amount, reference)`, `void(txnId)`, `printSlip(txnId)` via the Bold Bluetooth SDK. Integrate into `SalePaymentModal` — when method `card` is selected and a Bold adapter is configured for the site, invoke the adapter and persist the returned `authCode` in `sale_payments.reference`. Keep `ManualAdapter` as the fallback. | Happy path: charge → approved → `authCode` persisted. Decline → sale remains unpaid with a clear translated error. Timeout (60s no response) falls back to manual entry. Cancel mid-transaction cleans up. | **High (gated)** |
| `ENG-024` | Inter-site transfer requests with reservation (Iter 8) | Deferred | ⚪ **Deferred proposal** raised by the operator as a concern, not a firm requirement. Extend `transferOrderStatusEnum` with `requested` / `approved` / `rejected`, add `initiatedBy` / `requestedBy` / `approvedBy` / `rejectedBy` / `rejectionReason` / `expectedArrivalAt`, and finally wire `inventory_balances.reserved` (currently always 0). State machine: `requested → approved → in_transit → completed`; `rejected` or `void` paths release the reservation. UI: `TransferRequestsInboxPage` for approvers, `TransferRequestCreatePage` for requesters, extended `TransferDetailsPage` with a lifecycle timeline, sidebar badge for pending requests in the active site. Full spec in [SPRINT-PLAN.md §9](./SPRINT-PLAN.md). | Cashier at Sede A creates a request for a SKU with no stock at A; manager at Sede B approves (stock shows as reserved at B); manager at B dispatches (B's `on_hand` debits, `reserved` decrements); cashier at A receives with a discrepancy line; both sites reconcile. | **Medium (deferred)** |
| `ENG-025` | Critical security closure (multi-tenant + rate-limit + XSS + logout) | Shipped | Closes 1 HIGH + 3 MED security findings from the 2026-Q2 audit. (1) Electron IPC bridge `db.*` / `sync.*` accepts renderer-supplied `tenantId`, completely bypassing the tRPC scope — wire a server-validated `desktopSession` singleton in `apps/desktop/src/main/session/desktopSession.ts` that registers via a new IPC channel after login (token validated against `verifyAccessToken()`), and rewrite every `db:*` / `sync:*` handler to derive `tenantId` from the singleton instead of the renderer argument. (2) `@fastify/rate-limit` is registered with `global:false` and never applied to `/api/trpc/*` (only `auth.login` has its custom DB-backed bucket from `loginRateLimit.ts`) — apply via `addHook('onRoute', ...)` with tighter buckets on `auth.refresh` and `auth.changePassword`, exclude `auth.login`. (3) `logoDataUrl` / `imageUrl` accept any URL scheme and the rendered `<img src=...>` is unescaped — add a new `lib/urlSafety.ts::RESOLVED_URL_SCHEME_BLOCKLIST` (`javascript:`, `data:text/html`, `vbscript:`, `file:`, `about:`), Zod refine on `imageUrl`, and `escapeHtml()` on `logoDataUrl` in `renderLogoBlockHtml`. (4) `auth.logout` is `publicProcedure` and does not bump `users.sessionVersion` — promote to `protectedProcedure` and increment so post-logout access tokens fail `verifyAccessToken`'s `sessionVersion` check. Follow-up to fully remove the IPC bridge captured as `ENG-041` in BACKLOG. **Shipped** in commits `5734d0a` (4 vectors + tests + AuthProvider integration) + `6471007` (URL normalization hardening + sale_items JOIN guard via `assertSaleItemWriteBelongsToActiveTenant`). Live smoke: post-logout access token returns UNAUTHORIZED (Vector 4 verified), `/api/trpc/*` 429 fires at request 93 (Vector 2 verified). 10 desktopSession unit tests + 6 logos XSS tests + auth sessionVersion regression + rate-limit per-IP keying test. | All four vectors closed, regression tests added (`desktopSession.test.ts`, `ipc-handlers.test.ts`, `rateLimit.test.ts`, extended `auth.test.ts` + `receipt-renderer.test.ts`), `npm audit --omit=dev` stays at 0 high. | **Critical** |
| `ENG-026` | Vite 8 + plugin-react 6 + types/node 24 | Shipped | Coordinated major bump captured by the 2026-Q2 dependency audit: Vite 7→8 + `@vitejs/plugin-react` 4→6 (peer dep) + `@types/node` 22→24 (no jump to 25 to stay aligned with Node 22 runtime). Pin via lockfile, run full `ci:web` and `ci:desktop` matrix. **Shipped**: ranges bumped in root + `apps/web` + `apps/desktop` + `packages/server` `package.json`; lockfile refreshed (no `overrides` block needed — `@electron-forge/plugin-vite@7.11.1` declares no peerDeps so Vite 8 lands cleanly inside the Forge build chain). React Compiler intentionally NOT activated (`babel-plugin-react-compiler` not installed; v6's compiler is opt-in via that package). Bundle delta within 1% of the ENG-016 pass 4 baseline (`ReceiptTemplatesPage` 492.69 kB / 156.01 kB gzip vs 493.00 / 158.00, `index` 683.06 kB / 209.14 kB gzip vs 681.98 / 210.76). Vite 8 builds via Rolldown — full `npm run build --workspace=@puntovivo/web` finishes in ~470 ms (vs ~4-5 s on Vite 7). Live smoke: dev:web + dev:server boot, `/api/trpc/auth.login` round-trip via Vite proxy returns a valid token, `auth.me` resolves the seeded admin. Two collateral fixes were required to keep the gates green: `apps/web/tsconfig.json` `lib` raised from ES2020 to ES2022 (existing call sites use `Array.prototype.at`, only typing-visible after the bump), and a redundant `let rowTenantId: string | null = null` initializer in `apps/desktop/src/main/index.ts` removed (typescript-eslint v8 `no-useless-assignment` regression). | All `ci:web` and `ci:desktop` gates green; no runtime regressions in receipt-templates editor and lazy chunks. | **Low** |
| `ENG-027` | Dead code + dependency hygiene | Shipped | Delete confirmed-unused symbols flagged by the 2026-Q2 code-quality audit (~12 named exports across `cash-session.ts:71,37`, `tenant-locale.ts:178`, `fiscal/registry.ts:26,31`, `template-expression.ts:791`, `templateUnavailableDecorations.ts:253`, `LocaleProvider.tsx:163`, `syncQueue.ts:32`, `useTableExport.ts:289`, `TableToolbar.tsx:322`, `test/utils.tsx:40,68,94,183,227`) plus drop the `@tanstack/react-virtual` dependency (zero references in `apps/web/src`). Default exports redundant with named exports get cleaned up in the same pass. **Shipped**: stale exports and factories removed (server: `RegisterAssignmentTemplate`, `amountsMatch`, `composeResolvedLocale`, `setFiscalAdapter`, `resetFiscalAdapter`, `__test` from `template-expression.ts`; web: `useLocaleStatus`, `SyncQueueItemWithMeta`, `__test` from `templateUnavailableDecorations.ts`; redundant default exports on `useTableExport.ts` + `TableToolbar.tsx`; 5 mock factories from `test/utils.tsx` — `createMockAuthContext`, `createMockTenantContext`, `resetMockIdCounter`, `createMockCustomer`, `createMockSale`). `@tanstack/react-virtual` removed from both `apps/web/package.json` and `apps/desktop/package.json`; lockfile refreshed (`removed 2 packages`). The initially retained `template-expression.ts:791` and `test/utils.tsx:183` entries were rechecked and removed after zero-import grep. The `setFiscalAdapter` / `resetFiscalAdapter` removal is forward-compatible with ENG-034 — the FISCAL-CORE refactor will reintroduce the swap surface under the new pluggable adapter contract. Gates green: `ci:server` 84.18/68.43/81.04/85.08, `ci:web` 79.47/71.89/80.22/80.27, `ci:desktop` 14/14 tests pass. | Each removal verified by 0-import grep across `apps/web/src` + `packages/server/src` + `apps/desktop/src/main`; `ci:web` + `ci:server` + `ci:desktop` green. | **Low** |
| `ENG-028` | Cross-cutting helpers (mutate + sumBy + errorToast) | Shipped | Three small helpers that collapse repeated boilerplate found by the 2026-Q2 audit: `apps/web/src/lib/invalidateGroups.ts::invalidateGroups(utils, pickers[])` (typed picker-array form preserves the tRPC proxy), `apps/web/src/lib/numbers.ts::sumBy(items, selector)` (selector form keeps coercion at the call site), `apps/web/src/lib/mutationHelpers.ts::onErrorToast(toast, t, opts?)` (builder that returns an `onError` function — non-hook, drop-in at every call site, composes with caller-side state mutations via an `extra` callback). The audit's working name `useMutationWithErrorToast` was dropped in favor of `onErrorToast` because a wrapper hook would force every callsite to rewrite its `useMutation` invocation and pay a generic-inference rewrite at 27 sites — the builder shape is a one-line drop-in and the unit test stays free of provider plumbing. **Shipped**: helpers + 7 + 5 + 8 unit tests landed under `apps/web/src/lib/`; full debt-burn migration of 11 `invalidateGroups` blocks, 3 `sumBy` reducers, and ~30 `onErrorToast` callsites across SalesPage, SaleDetailsModal, SuspendedSalesPanel, PurchasesPage, PurchaseDetailsModal, OrdersPage, OrderDetailsModal, ProductsPage, CustomersPage, ProvidersPage, VatRatesPage, UnitsPage, SitesPage, LocationsPage, CategoriesPage, UsersPage, SequentialsPage, ReceiptTemplatesPage, ReceiptTemplateEditor, InventoryPage, InventoryTransferHistory, QuotationsHistoryTable, QuotationCreateModal, SalePaymentModal, CustomerCatalogsPage, and the seven Company* cards. The two private `getServerErrorMessage` helpers in `SalesPage.tsx` and `ProductsPage.tsx` were removed; their three remaining catch-block usages now call `translateServerError(error, t, t('errors:server.unknown'))` directly. Variant-A onError handlers (those that previously used the naive `getErrorMessage`) silently gain locale-aware error-code translation — a strict UX upgrade. New `common:toast.error` locale key added in en + es to back the helper's default `titleKey` fallback (caught by the typescript-react-reviewer agent before staging). Diff: 32 feature files modified, +272 / -597 (net 325 LOC reduction). Gates green: `ci:web` 79.94/71.92/81.48/80.76 (above the 70 floor), `ci:server` unchanged at 84.18/68.43/81.04/85.08. New helper chunks total 0.59 kB before gzip. Live smoke confirmed the `invalidateGroups` happy path on cash-session open and the locale-key shipping (fetched via dev server); UI error-path drive-through was blocked by client-side react-hook-form validation, gap declared explicitly in the Review Guide. | Each helper used in at least 3 call sites with the old pattern removed; coverage stays ≥70/70/70/70. | **Medium** |
| `ENG-029` | Hotspot file split (schema / sales router / desktop main) | Pending | Defensive splits triggered when those files are next touched by another ticket: `db/schema.ts` (3005 LOC) by domain, `trpc/routers/sales.ts` (2591 LOC) extracts `create` / `returnSale` / `void` / `discardDraft` to subhelpers, `apps/desktop/src/main/index.ts` (1728 LOC) moves IPC handlers to `main/ipc/{db,sync,print,auth}.ts`, `purchases.ts` (1648 LOC) follows the sales pattern, `ReceiptTemplateEditor.tsx` (1179 LOC) extracts `BlockForm` to its own file, `SalesPage.tsx` (1117 LOC) hoists the 31 hooks into `useSalesMutations()`. Trigger when each file enters another ticket's diff; do not refactor speculatively. | Each split keeps tests green and produces files under 500 LOC; touched ENG ticket signs off the split. | **Low** |
| `ENG-030` | AI-FOUNDATION (Vercel AI SDK + audit + settings) | Shipped | Vercel AI SDK v6 wiring — adopt `ai` core + `@ai-sdk/anthropic` v3 with default Anthropic Sonnet 4.6 (operator can override per tenant via Settings UI). Provider Strategy / Factory pattern at `services/ai/providers/` (`AIProvider` interface + `anthropic.ts` implementation + `openai.ts`/`ollama.ts` `notImplemented` stubs + `registry.ts` with default-resolution + per-tenant override) so adding a new provider is a single new file. Anthropic prompt-caching wired via `cacheControlForSystemPrompt()` returning `{ anthropic: { cacheControl: { type: 'ephemeral' } } }` for ~90% cost reduction on repeated system prompts. New `ai_audit_log` table (idx=7 migration) carries `tenant_id` + `site_id` + `user_id` + `feature` + `provider_id` + `model_id` + token columns + `cost_usd` + `duration_ms` + `error_code` + composite indexes for fast `currentMonthSpend` and per-site / per-feature / per-provider `byBreakdown` queries. Per-tenant `ai.settings` lives under `tenants.settings.ai` (enabled, monthlyBudgetUsd, providerId, modelId). Three new error codes (`AI_DISABLED`, `AI_BUDGET_EXCEEDED`, `AI_PROVIDER_ERROR`) registered in en + es. Admin-only `ai` tRPC router with `settings.{get,update}` + `usage` + `usageByBreakdown` + `completeTest` (fixed-prompt smoke endpoint that exercises the full pipeline). New `CompanyAISettingsCard` mounted in `CompanyPage` admin grid with enable toggle, provider selector (OpenAI/Ollama disabled with `availableInTicket` hint), model id override, monthly USD budget, current spend display, provider-configured badge, "Test connection" button. New `aiSettings` i18n namespace in en + es (neutral LATAM Spanish, `tú` register, no voseo). **Shipped**: 5 new test files (providers/registry + providers/anthropic + auditLog + client + ai-router) covering provider strategy, cost math, budget enforcement (post-check), audit-log write-per-call (with `site_id` + `provider_id`), failure-path `error_code='AI_PROVIDER_ERROR'` rows with `cost_usd=0`, multi-tenant isolation, role guards, and notImplemented-provider rejection at `settings.update`. `ai.completeTest` is gated by `adminProcedure`. `currentMonthSpend` window is `[startOfMonth, startOfNextMonth)` (caught + fixed during tests). API key is read from server-wide `ANTHROPIC_API_KEY` env var (per-tenant key storage deferred). Live smoke confirmed via Playwright MCP: card renders in CompanyPage admin grid; provider selector shows Anthropic enabled + OpenAI/Ollama disabled with localized hint; settings round-trip (enable + budget) produces translated success toast; without `ANTHROPIC_API_KEY`, "Test connection" stays correctly disabled. Gates green: `ci:server` 84.34/68.66/80.76/85.21, `ci:web` 79.94/71.92/81.48/80.76. Two reviewer-flagged dead-code redundant `??` fallbacks fixed inline; two MEDIUM follow-ups (recordCall failure-path masking under DB outage, listUsage cursor non-unique-timestamp boundary) captured as BACKLOG items. | Audit log writes per call, budget cutoff enforced, multi-tenant isolation tested, role guards admin-only, `ai.enabled=false` short-circuits all features without errors. | **Medium** |
| `ENG-031` | AI conversational analytics co-pilot | Shipped | Vercel AI SDK v6 tool-calling shipped server-side: manager/admin `ai.copilot.chat` uses `tool()` with `stopWhen: stepCountIs(5)` and reuses `ENG-030` settings, budget gates, provider registry, and `ai_audit_log` with `feature='copilot'`. `runReadOnlySQL` validates one `SELECT`/`WITH` statement, rejects mutation/PRAGMA/ATTACH/multiple statements/non-snapshot tables, and executes only against a tenant-scoped SQLite `:memory:` analytics snapshot built from bounded completed-sales rows (`sales_summary`, `sale_line_items`) with date/site caps and result truncation. Frontend route `/co-pilot` is admin/manager-only, uses `@ai-sdk/react` `useChat`, shows answer, SQL disclosure, table, and lightweight inline bar chart with responsive desktop/tablet layout. New `copilot` i18n namespace plus localized SQL guard/limit errors. **Shipped**: server tests cover manager allowed/cashier forbidden, `AI_DISABLED`, provider missing, budget exhausted, SQL guard rejection, allowed CTE, and cross-tenant snapshot isolation; web tests cover empty, localized error, send, SQL/table/chart rendering. | Demo conversation "¿cuánto vendí ayer en Sur?" returns answer + SQL + result rows + chart inline when provider credentials are present; without `ANTHROPIC_API_KEY`, UI/permissions/errors remain validated and provider call is correctly blocked. | **Medium** |
| `ENG-032` | AI anomaly + fraud detection | Shipped | Local-only models (no LLM dependency): four sub-detectors implemented in `services/ai/anomalyDetection.ts` covering the five most common POS retail fraud patterns documented for LATAM SMB context — `ticketsPerHourSpike` (per-cashier vs personal 30-day baseline), `voidRate` (cross-cashier ratio outlier from `audit_logs.action='sale.void'`), `refundAmount` (top-K outlier amounts from `sale_returns`), `noSaleSessions` (zero-sale cash sessions over 30 minutes). Algorithm chose **z-score with leave-one-out baseline** over isolation forest: ~80 LOC, deterministic, defensible if a cashier disputes a flag, robust on small populations (5-15 cashiers) where vanilla z-score caps the outlier's own score around 2σ. Threshold 3σ entry, 4.5σ for `high` severity. Tunable constants exported via `anomalyDetectionConstants` for future per-tenant tuning. New `ai.anomalies.list` tRPC procedure (manager+) reuses `tenants.settings.ai.enabled` for short-circuit when the master switch is off — no `ai_audit_log` write because the detector is purely statistical. New `aiAnomalies` i18n namespace en + es (neutral LATAM tú). New dashboard tile `AnomalyDetectionCard` with five UX states (loading / error / disabled / empty / has-alerts with severity pills) plus `AnomalyDetailsModal` drill-down with kind filter and table. Read-only by design in v1; "investigate cashier" CTA captured as BACKLOG follow-up. New Spanish-language doc `docs/AI-ANOMALY-DETECTION.md` carries the full design rationale, the five fraud patterns with examples, the privacy-driven local-only justification (Habeas Data CO, LFPDPPP MX, Ley 19.628 CL), and the upgrade criteria for migrating to isolation forest if pilot data warrants. **Shipped**: 10 detector unit tests with synthetic fixtures + narrative comments in Spanish; 5 `ai.anomalies.list` integration tests via createCaller (cashier FORBIDDEN, ai.enabled=false short-circuit, valid shape with enabled, from>to BAD_REQUEST, cross-tenant isolation); 5 dashboard card render tests (loading / disabled / empty / has-alerts / error). | Synthetic anomaly fixture triggers alert; clean baseline does not; multi-tenant isolation tested; cashier role does not see other cashiers' anomalies (manager+ only). | **Medium** |
| `ENG-033` | AI semantic product search + auto-categorization | Shipped | Backend completo: migración `0009_product_embeddings.sql` agrega `embedding TEXT` (JSON-encoded float array, ~6KB para 1536 dims) + `embedding_model` + `embedded_at` a `products`. Nuevo `services/ai/embeddings.ts` implementa cosine similarity en JS + funciones `embedText` (single query), `regenerateProductEmbeddings` (batch admin op con chunking 256 a OpenAI `embedMany`), `semanticSearchProducts` (top-K cosine con floor 0.30), `suggestProductCategory` (`generateObject` con `z.enum` constraint sobre categorías existentes — no hallucina). OpenAI provider extendido con `embeddingModel(modelId)` (ENG-044 lo había dejado parked); pricing table agrega `text-embedding-3-small` ($0.02/1M) y `text-embedding-3-large` ($0.13/1M). `AIProvider.embeddingModel?` interface ya existía como opcional desde ENG-030. Nuevo router `products.semanticSearch` (managerOrAdmin, devuelve `{mode:'semantic'\|'unavailable', results}` para fallback automático a LIKE), `products.regenerateEmbeddings` (admin), `products.suggestCategory` (managerOrAdmin). 15 tests pure-function en `embeddings.test.ts` cubren cosine identity/orthogonal/opposite/zero/dim-mismatch + JSON parse robustness + canonical text Unicode. Spanish doc `docs/AI-SEMANTIC-SEARCH.md` con contexto completo (algoritmo, dimensiones, costos, edge cases, follow-ups). **UI integration shipped en ENG-048** — toggle "Semántica" + input dedicado + columna "Coincidencia" + botón admin "Regenerar embeddings" en `ProductsPage`. | "vino tinto reserva" matches embedded productos sin substring exacto. Nuevo producto recibe `categoryId` sugerido con `confidence` 0..1 antes de guardar. AI disabled o tenant en Anthropic → procedures retornan `unavailable` sin error y caller cae a LIKE. | **Medium** |
| `ENG-034` | FISCAL-CORE refactor pluggable | Shipped | First Phase 2 ticket; promotes `services/fiscal/registry.ts` from an implicit `new MockAdapter()` singleton to a typed factory keyed by tenant `countryCode`. Country packs land as independent modules at `services/fiscal/packs/{co,mx,cl}/`. **Shipped**: Colombia adapter migrated first — `mock-adapter.ts` moved into `packs/co/mock-adapter.ts` and renamed `ColombiaMockAdapter` (the CUFE algorithm in `services/fiscal/cufe.ts` IS Colombia-specific; the old generic name was misleading). Mexico (`packs/mx/mexico-adapter.ts` → `MexicoNotImplementedAdapter`) and Chile (`packs/cl/chile-adapter.ts` → `ChileNotImplementedAdapter`) ship as parked stubs that throw `FISCAL_PACK_NOT_AVAILABLE` on `issue` / `voidDocument` / `fetchStatus` and report `validateConfig` issues pointing at ENG-035 / ENG-036; the stubs implement the new `NotImplementedFiscalAdapter` interface so the registry can list them with their `availableInTicket` strings. Adapter contract gained `validateConfig(input): Promise<FiscalAdapterValidationResult>` (pre-flight readiness probe — ColombiaMockAdapter returns `{ ok: true, issues: [] }`; real adapters at ENG-021 / ENG-035 / ENG-036 will probe NIT / RFC / RUT / certificate / resolution / environment) plus `readonly countryCode: string` so the orchestrator can introspect. Verb names from ENG-020 (`issue` / `voidDocument` / `fetchStatus`) preserved — they map 1:1 to DIAN lifecycle events; the ROADMAP's earlier `resolve` / `emit` wording was aspirational pre-ENG-020. New `getFiscalAdapter(countryCode)` is sync (factory dispatch); `listFiscalAdapterCountries()` is the discovery surface for a future fiscal-readiness admin card. `sales.ts::safelyEmitFiscalDocument` reads `resolveTenantLocale(ctx.db, ctx.tenantId).countryCode` once and dispatches via the factory. Unknown-country fallback returns `ColombiaMockAdapter` (defensive — orchestrator's `fiscal_dian_enabled` flag is the gate that activates fiscal at all; a throw would silently fail the non-blocking sale lifecycle path). New error code `FISCAL_PACK_NOT_AVAILABLE` registered server + web with localized en/es copy (neutral LATAM tú). Tests: 30 existing fiscal tests stay green with import + class rename updates (`fiscal-adapter` / `fiscal-orchestrator` / `fiscal-reports`); 6 new `fiscal-registry.test.ts` cases pin dispatch + listing + fallback + default constant; 6 new `fiscal-validate-config.test.ts` cases pin per-pack readiness + the FISCAL_PACK_NOT_AVAILABLE throw shape from MX / CL stubs. Mirrors the AI provider Strategy/Factory shipped in ENG-030 + ENG-044. | Sale completed under tenant `countryCode='CO'` writes the same CUFE shape as pre-refactor (regression smoke); flipping to `countryCode='MX'` makes the fiscal emission throw `FISCAL_PACK_NOT_AVAILABLE` with the sale itself completing per the existing non-blocking try/catch in `sales.ts`. Adapter contract documented in `FISCAL-INTEGRATION.md` ENG-034 section. ENG-035 + ENG-036 unblocked. | **High** |
| `ENG-035` | Pack México CFDI 4.0 | Partial | Pack completo de México con CFDI 4.0, PAC integration, CSD signing, complemento Pago 2.0. Demasiado grande para un solo commit (2-3 semanas + dependencias externas: contrato PAC, sandbox SAT, certificados CSD de prueba). Split en tres slices: ENG-035a (fundación: validador RFC + catálogos SAT + ajustes admin + adapter validateConfig real), ENG-035b (modelado XML CFDI 4.0 + emisión sin firmar), ENG-035c (PAC integration + firmado CSD + complemento Pago 2.0). **Remaining**: ENG-035c. | Tenant con `country_code='MX'` emite CFDI 4.0 válido contra PAC sandbox; tenant CO sin afectación; XML round-trip valida contra XSD del SAT. | **High** |
| `ENG-035a` | Pack México — fundación | Shipped | Primer slice del pack México. **Shipped**: validador `validateRfc` puro (12 PM / 13 PF + checksum SAT módulo 11 + lista negra de prefijos altisonantes + RFCs genéricos `XEXX010101000` / `XAXX010101000`); cuatro catálogos SAT como TS modules (regímenes fiscales con 23 entradas, usos CFDI con 24, formas de pago con 22, claves unidad con 25 + `CLAVE_UNIDAD_FALLBACK='H87'`); módulo `mx/settings.ts` con read/write helpers para `tenants.settings.fiscal.mx.*` (namespace nuevo, aditivo al `fiscal_dian_enabled` de CO); `MexicoCFDIAdapter` reemplaza al stub `MexicoNotImplementedAdapter` de ENG-034 — ahora `validateConfig` lee los settings reales y reporta `MISSING_RFC` / `MISSING_RESOLUTION` (régimen) / `MISSING_CERTIFICATE` (lugar de expedición) / `INVALID_ENVIRONMENT` cuando faltan campos; `issue` / `voidDocument` / `fetchStatus` siguen tirando `FISCAL_PACK_NOT_AVAILABLE` apuntando a ENG-035b porque la emisión XML real no shipa todavía; nuevo router admin `fiscal.settings.{getByCountry, updateMx}` con validación server-side de RFC + régimen contra catálogo + persistencia en `tenants.settings.fiscal.mx`; nueva tab `Fiscal` en `/company` con `CompanyMxFiscalCard` que muestra readiness badge + form de captura (RFC + régimen Select desde catálogo + lugar de expedición + ambiente); nuevos error codes `FISCAL_RFC_INVALID` + `FISCAL_REGIMEN_INVALID` server + web con i18n en/es; namespace `fiscal:settings.*` agregado a `fiscal.json` (en + es, neutral LATAM tú); convención nueva: comentarios JSDoc + docs fiscal en español a partir de aquí (sólo aplica a `services/fiscal/**` y prosa fiscal, los identificadores siguen en inglés porque cruzan el boundary tRPC). Tests: 17 nuevos del validador RFC, 17 de los catálogos, 11 actualizados de validate-config (ahora data-driven con MexicoCFDIAdapter real), 8 del router fiscal-settings, 4 de la card. | Admin con tenant flagged `country_code='MX'` ve la tab Fiscal, captura RFC válido + régimen 601 + lugar 06700 → readiness flips de rojo a verde; RFC inválido es rechazado server-side con `FISCAL_RFC_INVALID`; régimen no en catálogo rechazado con `FISCAL_REGIMEN_INVALID`. Tenants CO y CL ven placeholder apuntando a ENG-035c / ENG-036. | **High** |
| `ENG-035b` | Pack México — modelado XML CFDI 4.0 | Shipped | Slice 2 del pack México. **Shipped**: nuevo serializador `services/fiscal/packs/mx/cfdi40-xml.ts` genera comprobantes CFDI 4.0 estructuralmente válidos contra Anexo 20 SAT — root `cfdi:Comprobante` con todos los atributos requeridos (Version='4.0', Serie/Folio, Fecha ISO 8601 sin TZ, FormaPago, NoCertificado vacío, SubTotal, Moneda='MXN', Total, TipoDeComprobante='I' o 'E', MetodoPago='PUE' para ventas pagadas / 'PPD' para crédito, LugarExpedicion, Exportacion='01', Sello vacío) + namespaces `cfdi` + `xsi:schemaLocation`; nodos `cfdi:Emisor` (Rfc + Nombre + RegimenFiscal del catálogo SAT), `cfdi:Receptor` (Rfc + Nombre + DomicilioFiscalReceptor + RegimenFiscalReceptor='616' Público en general + UsoCFDI='S01'; consumidor final o cliente MX sin perfil fiscal dedicado → `XAXX010101000`, foreign buyer → `XEXX010101000` + ResidenciaFiscal SAT alpha-3 normalizada + NumRegIdTrib), `cfdi:Conceptos > cfdi:Concepto[]` (ClaveProdServ inferido + NoIdentificacion=sku + Cantidad 6 dec + ClaveUnidad + Descripcion + ValorUnitario e Importe tax-exclusive desde precios POS con IVA incluido + ObjetoImp '02' gravamen / '01' no objeto + nested `cfdi:Impuestos > cfdi:Traslados > cfdi:Traslado` per concepto), y `cfdi:Comprobante.cfdi:Impuestos` agregado consolidando Traslados por (Impuesto, TipoFactor, TasaOCuota); CfdiRelacionados con TipoRelacion='01' Nota crédito cuando source='return'/'void' + originalCufe. Nuevo catálogo curado `claveProdServ.ts` (40 códigos SAT más comunes para retail LATAM cubriendo alimentos, bebidas, abarrotes, limpieza, ferretería, papelería, ropa, electrónica, salud, mascotas, bebés, servicios, alcohol, tabaco) + `CLAVE_PROD_SERV_FALLBACK = '01010101'` (No existe en el catálogo, código SAT genérico válido); el catálogo completo de 50k+ entradas queda capturado como follow-up `ENG-035d` con seed-from-CSV o pull periódico del API SAT. Nuevo módulo `mappings.ts` con helpers puros: `mapPaymentMethodToFormaPago` (cash→01, card→04, card_debit→28, transfer→03, mercado_pago/nequi→06, other→01, credit→99, fallback 99 sólo para métodos desconocidos), `mapUnitToClaveUnidad` (unit→H87, kg→KGM, lt→LTR, mt→MTR, pkg→XPK, default H87), `inferProductClaveProdServ` (heurística por token completo + match multi-palabra para evitar falsos positivos como "vino reserva" matcheando 'res'), `mapTaxRateToTraslado` (acepta 16 o 0.16; tasa 0% → TipoFactor='Exento' sin TasaOCuota; tasa>0 → TipoFactor='Tasa' + TasaOCuota 6 dec + Importe 2 dec), `formatDecimal` con clamp [0,20] y throw para no-finitos. `MexicoCFDIAdapter` salió de `NotImplementedFiscalAdapter` y pasó a implementar `FiscalAdapter` directamente (`isImplemented=true`, sin `availableInTicket`); `issue()` lee `tenantSettings.fiscal.mx.*` desde el nuevo campo `FiscalAdapterIssueInput.tenantSettings` (poblado por el orchestrator), respeta `fiscal.mx.enabled`, llama `serializeCfdi40`, y retorna `{ cufe: <uuid v4 local>, status: 'pending', providerId: 'cfdi-mx', providerResponse: { kind: 'unsigned-draft', xmlSize, emisorRfc, receptorRfc, tipoComprobante }, xmlRef: <xml string> }`. `voidDocument()` (cancelación SAT explícita, NO sale.void del POS) sigue tirando `FISCAL_PACK_NOT_AVAILABLE` apuntando a ENG-035c — la cancelación es operación API separada, no nueva emisión XML. `fetchStatus()` retorna 'pending' (sin PAC). Nuevos campos `tenantSettings` + `issuerName` + `paymentMethod` en `FiscalAdapterIssueInput`; orchestrator lee `tenants.settings` + `companies.name` + `sales.paymentMethod` y los pobla. UUID local con `crypto.randomUUID()` (placeholder hasta ENG-035c PAC); status enum no cambia — usamos `'pending'` para draft sin firmar. xmlRef inline TEXT (~5-10kb por documento típico); follow-up `[fiscal][mx]` capturado para migrar a object storage cuando ENG-035c traiga PDFs firmados. Nueva dependencia `fast-xml-parser@^5.7` (~30kb, sin native deps, JS puro). Nueva UI `FiscalDocumentXmlModal` admin-only con XML pre-formateado en `<pre>` + botones Copiar (con success toast) + Descargar `.xml` (Blob → anchor download); modal lazy en `FiscalDocumentListPage` con row action "Ver XML" / "View XML" gated en `document.xmlRef !== null`. La query `reports.fiscal.list` ahora incluye `xmlRef` para que la UI sepa cuándo mostrar el botón. i18n keys `fiscal:document.xml.*` (viewButton, title, copy, copied, download, draftHint, emptyState, xmlCopiedToast, xmlCopyError, documentNumber, cufe) en + es como pareja, neutral LATAM tú. Dev seed extendido con `SEED_COUNTRY=mx` (default 'co' por backward-compat con todos los E2E + tests existentes) — flippea `tenantLocaleSettings.countryCode='MX'`, pobla `fiscal.mx.{enabled:true, rfc:'AAA010101AAA', regimenFiscalCode:'601', lugarExpedicion:'06700', environment:'sandbox'}`, y deja los 20 sales seeded emitiendo CFDI 4.0 vía `MexicoCFDIAdapter`. Tests: 7 nuevos para claveProdServ catálogo, 29 nuevos para mappings + formatDecimal, 28 nuevos para cfdi40-xml serializer (root + namespaces, FormaPago/MetodoPago PUE/PPD, Emisor, Receptor consumidor final + foreign + fallback público para cliente MX sin perfil fiscal, normalización de país a ResidenciaFiscal SAT, Conceptos tax-exclusive desde precios con IVA incluido, cantidad/precio precision, ObjetoImp gating, Impuestos agregados consolidados, exento, CfdiRelacionados, validaciones moneda/lines/RFC, idempotencia), 10 nuevos para mexico-adapter (issue shape + xmlRef + tipoComprobante + sale/return/void + missing/disabled settings reject + emisorName injection + voidDocument throws + fetchStatus pending + capabilities). Tests existentes actualizados: `fiscal-registry.test.ts` (MX `isImplemented=true`, sin `availableInTicket`), `fiscal-validate-config.test.ts` (issue ya no throws — test ahora exercita el path "settings vacíos → FISCAL_PACK_NOT_AVAILABLE"), `fiscal-settings.test.ts` (router devuelve `notImplemented:false` + `availableInTicket:null` para MX). 6 nuevos tests web del modal (render con xml + empty state + disabled buttons + copy click + download click + closed state). Coverage estable: server 83.52/68.54/80.62/84.62 (sobre el floor 80/63/77/80); web 78.83/70.76/79.11/79.97 (sobre el floor 70/70/70/70). Live smoke verde via Playwright MCP: `SEED_COUNTRY=mx` boot, login admin@demo.co, `/fiscal-documents` muestra 20 documentos con xmlRef poblado y 20 botones "Ver XML"; click → modal abre con XML CFDI 4.0 estructuralmente válido (Version="4.0", emisor `AAA010101AAA`, receptor `XEXX010101000` para foreign / `XAXX010101000` para consumidor final, Conceptos con cada line, Impuestos agregados); locale ES/EN ambos funcionan, cero console.error. Counter Plan v2.0 avanza 9/16 → 10/16. Phase 2 multi-country fiscal pasa de 1.66/3 (1 slice de 3 + foundations MX/CL) a 2/3 (foundations + emisión XML MX). | Sale completado en tenant MX persiste `fiscal_documents` con XML CFDI 4.0 estructuralmente válido (pasa XSD); `MexicoCFDIAdapter.notImplemented` se desactiva pero el documento queda en `status='pending'` hasta que ENG-035c lo firme + transmita. | **High** |
| `ENG-035c` | Pack México — PAC integration + firmado CSD + complemento Pago 2.0 | Pending | Slice 3 del pack México. Selección de vendor PAC (Solución Factible o Edicom — operator decide), integración HTTPS REST con el sandbox del PAC, firmado XAdES con CSD del emisor (carga del `.cer` + `.key` + password en `tenants.settings.fiscal.mx.csd.*` con encryption-at-rest), complemento Pago 2.0 para ventas a crédito + parcialidades, retry daemon para timbrado fallido, rename de `tenants.settings.fiscal_dian_enabled` a namespace country-aware `tenants.settings.fiscal.{country}.enabled`. | Tenant MX con CSD configurado y `OPENAI_API_KEY` (o equivalente PAC sandbox) cargado emite un CFDI 4.0 firmado y timbrado en el sandbox del PAC — incluye sello digital del SAT + folio fiscal UUID; `cancel` y `pago 2.0` shippean en este mismo ticket. | **High** |
| `ENG-036` | Pack Chile SII | Partial | Pack completo de Chile con DTE 1.0, certificación SII, firmado XAdES, entrega digital obligatoria desde mar-2026. Demasiado grande para un solo commit (2-3 semanas + dependencias externas: certificado digital del emisor, sandbox de certificación SII, CAF folios). Split en tres slices: ENG-036a (fundación: validador RUT + catálogos SII + ajustes admin + adapter validateConfig real), ENG-036b (modelado XML DTE + emisión sin firmar + manejo CAF), ENG-036c (certificación SII + firmado XAdES + entrega digital + retry daemon). **Remaining**: ENG-036c. **Shipped slice b 2026-05-07**: nueva migración `0019_fiscal_cafs.sql` agrega tabla `fiscal_cafs` con partial unique idx `(tenant_id, tipo_dte) WHERE status='active'` que enforce un solo CAF activo por (tenant, TipoDTE) — mirror del shape ENG-064 sync_outbox idempotency. Servicio nuevo `services/fiscal/packs/cl/caf-allocator.ts` con `allocateNextFolio(tx, args)` atómico (avanza el cursor + flippea status='exhausted' cuando se llega al último folio) + `peekActiveCaf(db, tenant, tipoDte)` read-only para la admin tab. Nuevo serializador `services/fiscal/packs/cl/dte10-xml.ts` (~450 LOC, espejo estructural de cfdi40-xml.ts) emite XML DTE 1.0 con namespace SII v1.0 + Encabezado/IdDoc/Emisor/Receptor/Totales + Detalle 1..n + Referencia para NC + TED.DD completo (RE/TD/F/FE/RR/RSR/MNT/IT1/CAF<DA>/TSTED) y FRMT placeholder vacío hasta ENG-036c. Mappings helper `mappings.ts` traduce paymentMethod a FmaPago SII (1=contado, 2=crédito), unit a UnmdItem ('un'/'kg'/'lt'/...), source+buyerHasRut a TipoDTE ('33'/'39'/'61') y compute totals afecto+exento+IVA con redondeo CLP. ChileSIIAdapter sale del stub NotImplementedFiscalAdapter — `issue()` real retorna `{cufe='sii-cl:<RUT>:<TipoDTE>:<F>', status='pending', xmlRef=string}`; `voidDocument()` y `fetchStatus()` siguen apuntando a ENG-036c. Adapter contract extendido con optional `chileAllocation` field (orchestrator pre-allocates inside writeTx → folio + fiscal_documents insert + outbox enqueue commit atómicos; si el allocator throws CAF_NOT_AVAILABLE/CAF_EXHAUSTED el tx rollback completo no quema folio). Dos errorCodes nuevos `CAF_NOT_AVAILABLE` + `CAF_EXHAUSTED` con i18n en + es neutral LATAM tú. tRPC procedure nueva `fiscalSettings.getActiveCaf({countryCode, tipoDte})` adminProcedure read-only para la card. CompanyClFiscalCard muestra sección read-only "CAF activo" con tipoDTE / rango / folios disponibles cuando hay CAF activo, mensaje de "Sube uno desde el portal SII" cuando no. SEED_COUNTRY=cl en dev-seed pobla fiscal.cl.* con valores de prueba (RUT 76123456-0, giro 4711, comuna 13101 Santiago, casa matriz Av. Principal 123, ambiente certificación) + inserta CAFs fixture fiscal_cafs (TipoDTE 33/39/61, folios 1..100). Helper `buildTenantSettings(country)` extraído del seed para limpiar el ternary chain de 3 ramas. ~28 nuevos tests server (caf-allocator 11 cases, dte10-xml 25 cases, mappings 12 cases, chile-adapter 6 cases). Counter Plan v2.0 sin cambio (10/16 still — ENG-036 sigue Partial hasta ENG-036c); narrative shifts de "1/3 slice Chile" a "2/3 slices Phase 2 Chile". | Tenant con `country_code='CL'` emite DTE válido contra SII sandbox; canal de entrega digital seleccionable por tenant; timbre impreso suprimido cuando locale es CL. | **High** |
| `ENG-036a` | Pack Chile — fundación | Shipped | Primer slice del pack Chile, espejo estructural de ENG-035a. **Shipped**: validador `validateRut` puro (algoritmo SII módulo 11 con pesos cíclicos `{2,3,4,5,6,7}` + dígito verificador `0-9` o `K` + RUT genérico extranjero `55555555-5` + threshold 50M para distinguir persona natural de jurídica + normalización de puntos y guión); tres catálogos SII como TS modules (`tipoDte` con 7 entradas para los DTE de retail — 33 Factura, 39 Boleta, 61 Nota Crédito, etc.; `giroComercial` con 26 giros curados de CIIU.cl rev 4; `comuna` con 35 entradas curadas — 18 del Gran Santiago + 16 capitales regionales + Ñuble + `COMUNA_FALLBACK=13101`); módulo `cl/settings.ts` con read/write helpers para `tenants.settings.fiscal.cl.*` (namespace nuevo, aditivo al `fiscal.mx.*` de ENG-035a y al `fiscal_dian_enabled` de CO); `ChileSIIAdapter` reemplaza al stub `ChileNotImplementedAdapter` de ENG-034 — ahora `validateConfig` lee los settings reales y reporta `MISSING_RUT` / `MISSING_RESOLUTION` (giro, mapeo semántico) / `MISSING_CERTIFICATE` (casa matriz + comuna agrupados como dato de identificación del emisor) / `INVALID_ENVIRONMENT` cuando faltan campos; `issue` / `voidDocument` / `fetchStatus` siguen tirando `FISCAL_PACK_NOT_AVAILABLE` apuntando a `ENG-036b` porque la emisión XML real no shipa todavía; router admin extendido con `fiscal.settings.updateCl` que valida RUT server-side via `validateRut` y giro contra catálogo CIIU.cl antes de persistir, re-corre `validateConfig` post-write para devolver readiness fresco; `getByCountry({ countryCode: 'CL' })` ahora desempaca settings (antes devolvía `null`); nueva `CompanyClFiscalCard` admin paralela a `CompanyMxFiscalCard` con form completo (RUT + giro Select + comuna Select + casa matriz + ambiente certificacion/produccion) + readiness badge; el dispatch entre cards de país en el tab Fiscal se movió a `CompanyPage.tsx` (lee `trpc.tenantLocale.get.useQuery()` y monta MX / CL / CO-placeholder según `countryCode`); la card MX simplifica — los branches CO/CL muertos se removieron y la card ahora returns null cuando `tenantCountry !== 'MX'` como red de seguridad defensiva. Nuevo error code `FISCAL_RUT_INVALID` server + web con i18n en/es (neutral LATAM tú); namespace `fiscal:settings.cl.*` expandido de placeholder a form completo (~20 keys nuevas). Tests: 15 nuevos del validador RUT, 16 de los catálogos CL, 6 actualizados de validate-config (ahora data-driven con `ChileSIIAdapter` real, removidos los 2 tests del stub `ChileNotImplementedAdapter`), 4 nuevos del router `updateCl`, 4 de la card CL, 1 actualizado de la card MX (placeholder branches removidos), 2 nuevos de CompanyPage (dispatch MX/CL/CO). | Admin con tenant flagged `country_code='CL'` ve la tab Fiscal con `CompanyClFiscalCard`, captura RUT válido `55555555-5` + giro 4711 + comuna 13101 + casa matriz "Av Apoquindo 4500" → readiness flips de rojo a verde; RUT inválido rechazado server-side con `FISCAL_RUT_INVALID`; giro no en catálogo CIIU.cl rechazado con `FISCAL_REGIMEN_INVALID`. Tenants MX siguen viendo `CompanyMxFiscalCard`; tenants CO ven placeholder apuntando a ENG-035c. | **High** |
| `ENG-036b` | Pack Chile — modelado XML DTE | Shipped | Slice 2 del pack Chile. **Shipped**: modelado XML DTE 1.0 estructural, mapeo de `payment_method` interno a FmaPago SII, TipoDTE por source + RUT comprador, generación de folio vía CAF atómico, persistencia del XML serializado en `fiscal_documents.xml_ref`, `ChileSIIAdapter.issue()` real con status `pending`, `fiscalSettings.getActiveCaf` read-only y CAFs fixture para TipoDTE 33/39/61 en `SEED_COUNTRY=cl`. Sin firmado XAdES ni transmisión a SII; eso queda para ENG-036c. Catálogo completo de comunas (~346 entradas) con refresh desde SUBDERE queda como follow-up. | Sale completado en tenant CL persiste `fiscal_documents` con XML DTE estructuralmente válido; `ChileSIIAdapter.notImplemented` se desactiva pero el documento queda en `status='pending'` hasta que ENG-036c lo firme + transmita. | **High** |
| `ENG-036c` | Pack Chile — certificación SII + firmado XAdES + entrega digital | Pending | Slice 3 del pack Chile. Flujo de certificación SII (registro del emisor + envío de prueba), firmado XAdES con certificado digital del emisor (carga del `.pfx` + password en `tenants.settings.fiscal.cl.certificate.*` con encryption-at-rest), entrega digital obligatoria desde mar-2026 (email + SMS + QR para boleta), eliminación del timbre impreso (1-jan-2026), retry daemon para timbrado fallido, rename de `tenants.settings.fiscal_dian_enabled` a namespace country-aware `tenants.settings.fiscal.{country}.enabled`. | Tenant CL con certificado configurado emite un DTE firmado y aceptado por el SII en el ambiente de certificación — incluye sello digital + folio CAF; cancel y entrega digital shippean en este mismo ticket. | **High** |
| `ENG-037` | libSQL/Turso embedded replicas spike | Shipped | 1-week investigation + 3-4 week implementation if greenlit. Adds read-replica + automatic cloud backup pattern without migrating off SQLite. Closes the multi-site sync gap referenced in `PLAN.md` §10. The spike report itself is the deliverable for the first week; if greenlit, the implementation lands in the same ticket. **Shipped 2026-05-08**: spike report committed at [`docs/SPIKE-LIBSQL-TURSO.md`](./SPIKE-LIBSQL-TURSO.md). Recommendation: **Defer (revisit after Phase 4)**. Three findings drive the call — (1) legacy Embedded Replicas are cloud-primary by default and conflict with ADR-0001 Local Store Authority; (2) current Turso Sync is still a beta surface whose documented conflict model is Last-Push-Wins, incompatible with Puntovivo's manual lane for money/fiscal/audit/cash/inventory data; (3) libSQL as a pure SQLite engine swap has not proven a simpler Electron 41 / Node 22 native runtime than `better-sqlite3`. The bespoke `sync_outbox` pipeline (ENG-064 + ENG-064b) plus a separate cloud-backup destination is the natural Phase 3+ shape. Reopen triggers documented in spike §2 (Turso Sync GA + manual-resolution API + proven native-runtime simplification or operator-reported `sync_outbox` shortcoming on a real central-server pilot). Implementation acceptance criteria preserved in spike §12 if any trigger fires. | Spike report committed (decision document); if greenlit, embedded replica works against Turso staging with conflict-resolution strategy documented and a fallback to local-only when offline. | **Medium** |
| `ENG-038` | LATAM payment rails + reconciliation | Partial | Unified abstraction over Wompi + Bold + ePayco + Mercado Pago + Nequi/Daviplata. Each rail behind a `PaymentRailAdapter` with `charge`, `refund`, `getStatus`. Plus AI-assisted reconciliation (idea AI #16) that maps gateway transaction reports to POS sales nightly via a server-side cron, surfacing mismatches in an admin dashboard. **Shipped slice 1 (2026-05-11)**: payment rail manifest + deterministic adapters for Wompi, Bold, ePayco, Mercado Pago, Nequi and Daviplata; new `payment_outbox` table with idempotency key, provider transaction reference, status/retry projection and sale-payment linkage; manager/admin read-only `payments.{getContract,peekOutbox,reconciliation}` tRPC namespace; `/operations?tab=payments` panel with rail summary, mismatch list and outbox tail; server + web tests cover contract, tenant isolation, role gate, ordering and mismatch classification. **Shipped slice 2 (2026-05-11)**: tenant credential model + admin settings UI. `PaymentRailAdapter` gained an optional `validateConfig` method; the shared `DeterministicPaymentRailAdapter` implements it by walking a new per-rail descriptor `CREDENTIAL_FIELDS_BY_RAIL` and emitting `PAYMENT_CREDENTIAL_MISSING` issues for each empty required field. New `services/payments/credentials.ts` exposes `readPaymentRailCredentials` / `mergePaymentRailCredentialsIntoTenantSettings` / `projectRailCredentials` (mask = `••••••••` + last-3 chars; full plaintext is never re-served after save). New admin tRPC namespace `paymentSettings.{getAll, updateRail}` writes to `tenants.settings.payments.<railId>.credentials.*`, rejects undeclared field keys with new error code `PAYMENT_CREDENTIAL_UNKNOWN_FIELD`, and treats empty-string + null as "clear the field". New `/company?tab=payments` admin tab renders one `<RailSection>` per rail with a readiness badge, live/stub badge, and per-field password input + Show/Hide reveal toggle; masked stored values surface in a hint paragraph below the input (never in the `placeholder` attribute). Diagnostic sanitizer's denylist extended with `merchantid`, `customerid`, `pkey` so the export bundle redacts every credential field automatically. 13 new server tests (8 router + 4 contract validateConfig + 1 sanitize) + 5 new web component tests + the CompanyPage tab-count assertion bumped to 8. Live smoke verified: admin saves Wompi credentials → readiness flips from "Missing credentials" to "Ready"; masked tail `••••••••abc` / `••••••••xyz` lands in the hint paragraph. **Remaining**: rail-specific API clients/workers, cron/import path for provider statements, automated 95%+ matching proof on a 30-day fixture, and admin retry/requeue flows once live providers exist. | All five rails callable via single interface; reconciliation cron correctly matches 95%+ of transactions on a 30-day fixture; mismatches surface with proposed resolutions. | **High** |
| `ENG-039` | Vertical restaurant Mexico | Pending | Restaurant mode (tables, kitchen display screen, tips, modifiers, course timing) integrated with CFDI MX from `ENG-035`. Vector against SoftRestaurant's legacy stack — UX comparable to Toast but on the Puntovivo desktop runtime. | Restaurant tenant flow runs end-to-end: open table → modify items → split bill → tip → print receipt → CFDI emitted. Course-timing flag visible to KDS. | **Medium** |
| `ENG-040` | AI Wave 2 (vision + voice) | Partial | Provider-invoice OCR via Vercel AI SDK `generateObject` with `image` content (Anthropic vision); voice ordering with local Whisper transcript piped through `generateObject` for structured command parsing. Local Ollama provider for `ENG-030` lands here as a second provider option if the operator pulls it in. **Shipped slice 1 (ENG-040a, 2026-05-11)**: provider abstraction extended with optional `visionModel?` (mirrors the ENG-033 `embeddingModel?` capability) and implemented for Anthropic + OpenAI; Ollama stays `notImplemented` pointing at ENG-040b. New `services/ai/vision/invoice-ocr.ts` runs `generateObject` against a strict `InvoiceOcrSchema` (supplier name + tax id + invoice number + invoice date + currency + lines `{description, quantity, unitPrice, totalLine}` + subtotal/tax/total). Reuses `resolveAISettings` + monthly-budget enforcement + `ai_audit_log` (new feature literal `invoiceOcr`). New tRPC `ai.extractInvoiceLines` mutation (`managerOrAdminProcedure`) takes `{imageBase64, mimeType}` with a 5 MB raw cap after base64 decode; data-URL prefixes are stripped server-side. New error codes `AI_VISION_NOT_AVAILABLE` (provider lacks `visionModel`), `AI_VISION_PARSE_FAILED` (schema-validation failure surfaces with a persisted failure audit row), `AI_VISION_IMAGE_TOO_LARGE`. New `PurchasesPage` toolbar button "Escanear factura" gated by manager/admin role opens an `<InvoiceOcrPreviewModal>` that uploads the image, calls the procedure, and renders a read-only preview (supplier card + line table + totals). 10 server unit tests (every gating branch + happy path + persisted failure audit row), 4 router integration tests (cashier FORBIDDEN, AI_DISABLED, data-URL prefix strip, MIME enum rejection), 4 web component tests (upload CTA, size guard, MIME guard, mutation payload + rendered preview); locale parity green; neutral LATAM Spanish copy in `tú` register. **Shipped slice 1b (2026-05-11)**: new `services/ai/vision/invoice-line-matcher.ts` reuses the ENG-033 embedding stack to cosine-match each OCR line against the tenant catalog. `embedTexts` is now public + batches via the existing 256-chunk path; new `loadTenantProductEmbeddings` centralises the tenant-scoped product-embedding load; `semanticSearchProducts` is refactored onto the same loader so the cosine floor + parse path stay in one place (`SEMANTIC_SIMILARITY_FLOOR` exported). New tRPC mutation `ai.matchInvoiceLines` gated by `managerOrAdminProcedureWithModule('semantic-search')` returns `mode:'matched'` (top-1 product per line above the 0.3 floor, with hydrated base-unit info) or `mode:'unavailable'` (reason `ai-disabled` | `no-embeddings`) so the modal can render a hint instead of throwing. One audit-log row per batch (`feature='invoiceLineMatch'`). `<InvoiceOcrPreviewModal>` grew a "Asociar productos" CTA, a "Producto sugerido" column with similarity badge, and a "Crear compra con coincidencias" footer button that pushes matched items into `PurchasesPage.cartItems` via the new `onMatchedLinesReady` callback (mirrors the `ProductSearchDialog` → `mergePurchaseCartItem` shape; cart merge sums quantities when the same product:unit key already exists). i18n `purchases.ocr.match.*` namespace added in + es with pluralized `toastMatched_one/_other` + `toastUnmatched_one/_other` (neutral LATAM tú). 8 new server tests in `ai-match-invoice-lines.test.ts` (happy path with audit row, AI-disabled + no-embeddings unavailable modes, cosine-floor rejection, cross-tenant isolation, cashier FORBIDDEN, module-off MODULE_NOT_ACTIVATED, empty-input short-circuit). 5 new web cases in `InvoiceOcrPreviewModal.test.tsx` (match CTA visibility, module-off branch + hint, unavailable hint render, create-purchase callback payload + close, retry-after-throw). Live smoke verified the modal renders cleanly via Playwright MCP and the `ai.matchInvoiceLines` route resolves end-to-end. **Shipped Ollama provider activation (ENG-040b slice 1, 2026-05-12)**: `services/ai/providers/ollama.ts` rewritten from `NotImplementedProvider` stub to a real `AIProvider` backed by `ollama-ai-provider-v2@^3.5.0` (community Vercel AI SDK package targeting `ai@^6`, pure JS, no native binding). `id='ollama'`, `defaultModelId='llama3.2'`, `pricing.calculateCostUsd` returns 0 for any usage shape (local inference). `isConfigured()` returns `true` unconditionally — Ollama needs no API key. `OLLAMA_BASE_URL` env var override (default `http://localhost:11434`); the provider strips the trailing slash and appends `/api` because the SDK uses `baseURL` verbatim. Exposes both `languageModel(modelId)` and `visionModel(modelId)` via the same `createOllama({ baseURL })` factory so the existing ENG-040a `extractInvoiceFromImage` + ENG-040 slice 1b `matchInvoiceLinesToProducts` paths route through Ollama with zero change. `embeddingModel` stays undefined (separate follow-up). 12 new server unit tests in `ollama.test.ts`; the stale "ollama is notImplemented" branches in `registry.test.ts` + `ai-router.test.ts` flipped to assert the new state (router test now exercises the Zod enum boundary against an unknown providerId instead of the now-dead stub-rejection branch — the `isNotImplemented` guard in `ai.settings.update` stays in place with a tracking comment for the next provider stub). New `.env.example` `OLLAMA_BASE_URL` block. Live smoke verified via Playwright MCP: admin sees the Ollama option enabled (no "(disponible con ENG-040)" hint), can save it as the active provider, success toast fires, and the selection persists across the mutation. Operator-side requirement: run `ollama serve` locally and `ollama pull llama3.2` (chat) + `ollama pull llava` (vision) before triggering real round-trips — the dev sandbox doesn't have Ollama installed, so end-to-end provider round-trips are operator-side. **Shipped Ollama embeddings activation (ENG-040b slice 2, 2026-05-12)**: `AIProvider` contract gained an optional `defaultEmbeddingModelId?: string` field; OpenAI advertises `text-embedding-3-small` (matches the prior hardcoded `DEFAULT_EMBEDDING_MODEL` in `embeddings.ts`), Ollama advertises `nomic-embed-text` (768-d, ~270 MB, multilingual). `services/ai/providers/ollama.ts` implements `embeddingModel(modelId)` via the `ollama-ai-provider-v2` SDK's primary `createOllama({baseURL}).embedding(modelId)` factory — returns the same `EmbeddingModelV3` shape OpenAI returns, so the provider-agnostic call site in `services/ai/embeddings.ts::embedText` + `embedTexts` works unchanged. Both resolver paths now read `provider.defaultEmbeddingModelId ?? DEFAULT_EMBEDDING_MODEL` so each provider's canonical model id flows through; the legacy constant stays for backwards-compat. Dimension drift (768-d Ollama vs 1536-d OpenAI) is intentionally not auto-handled — `cosineSimilarity` already returns 0 for mismatched dims, so the worst case is an incomplete result list (not wrong matches) and the operator clicks "Regenerate embeddings" after switching providers (same constraint as switching between OpenAI's small + large models). 3 test updates (ollama.test.ts flipped from "stays undefined" to assert the embedding factory + cross-checks the default against the exported `FALLBACK_EMBEDDING_MODEL_ID`; openai.test.ts gains a `defaultEmbeddingModelId === 'text-embedding-3-small'` assertion). No new dependencies (`ollama-ai-provider-v2` already in place from slice 1). Live smoke verified the AI settings card still loads cleanly with all three providers enabled and Products page navigation stays error-free. Operator-side activation: `ollama pull nomic-embed-text` then click "Regenerate embeddings" on the products page. **Shipped embedding-model drift admin banner (2026-05-12)**: new `services/ai/embeddings.ts::resolveActiveEmbeddingModelId(db, tenantId)` helper centralises the resolution that `embedText` + `embedTexts` already perform inline — returns the active provider's `defaultEmbeddingModelId` (or `DEFAULT_EMBEDDING_MODEL` legacy fallback) or null when AI is disabled / the provider does not embed. New `products.embeddingHealth` tRPC query (`managerOrAdminProcedureWithModule('semantic-search')`) returns `{mode, activeModelId, totalProducts, embeddedCount, unembeddedCount, staleCount, staleSampleModelIds, lastEmbeddedAt}` via one aggregate scan over `products` + a follow-up `SELECT DISTINCT embedding_model` capped at 3 for the inline hint. New `apps/web/src/features/products/EmbeddingDriftBanner.tsx` renders above the toolbar on `/products` only when `mode='available' && staleCount > 0`; manager-read + admin-CTA wiring matches the existing `regenerateEmbeddings` mutation gate (the CTA invalidates both `embeddingHealth` and `semanticSearch` on success). New i18n namespace `products.embeddingDrift.*` in + es (neutral LATAM tú; pluralised `sampleModelsHint_one/_other`). 9 new server tests (3 resolver helper cases + 6 router cases covering `mode='unavailable'` when AI off, aligned catalog, mixed-model drift with sample, module gate `MODULE_NOT_ACTIVATED`, cross-tenant isolation, manager-allowed) + 5 new web cases (null data, unavailable mode, aligned, drift + admin CTA fires mutation, manager read-only). **Remaining**: voice/Whisper transcript slice (audio capture + Whisper transcription wiring); ≥80% line extraction proof on a 10-invoice fixture once anonymized real invoices land under `packages/server/src/__tests__/fixtures/invoices/`; mobile/tablet camera capture. | Receipt photo round-trip extracts ≥80% of line items on a 10-receipt fixture; voice command "agrega 2 cocas y 1 pan" parses correctly to a cart action; both behind `ai.enabled` flag. | **Medium** |
| `ENG-042` | Sync resilience UX (stale conflicts + network errors) | Shipped | Out-of-band Phase-0-adjacent ticket opened to document an already-implemented body of work covering three concerns at once: (1) the sync conflict UI used to render `entityType · entityId` snake_case pills and stall the queue forever when the local row had been deleted server-side; (2) the embedded server returned `Failed to fetch` text directly to operators on transient network drops, mostly during the AuthProvider boot path; (3) Company* card error toasts surfaced raw `error.message` instead of the localized `errors:server.*` copy. **Shipped**: server `sync.listConflicts` + `sync.pull` now expose `localRecordExists: boolean | null` per item / conflict by probing `findEntity` against the active tenant scope; `sync.resolve` refuses `keepLocal` and `merged` when the local row is missing and steers the operator to `acceptRemote` (rebadged as "Discard Local Change" in the UI). New `apps/web/src/features/company/companySyncDisplay.ts` maps internal entity types + operations to localized labels via a `company.sync.entities.*` / `operations.*` namespace and turns the server `lastError` text into a translated hint via `getSyncQueueIssueMessage`. The Pending Queue and Conflicts cards now render an "Item title" plus a collapsible "Technical details" disclosure (entity type + id + operation + raw error stay accessible without dominating the card). Conflict modal copy moved from interpolating `entityType` + `entityId` into prose to interpolating a localized `entity` label across three dialog variants (keep local / accept remote / discard stale local change). `translateServerError` gains `isNetworkConnectivityError` (matches `Failed to fetch`, `fetch failed`, `Load failed`, `NetworkError when attempting to fetch resource.` recursively across `cause` chains) plus a new `errors:server.networkUnavailable` key in en + es so the AuthProvider boot path no longer logs raw fetch failures and every Company card surfaces a friendly hint when the embedded server is unreachable. Header layout tightened so the language and site selectors share a consistent baseline alignment across viewport widths. Tests: `sync.test.ts` covers the missing-local rejection path and asserts `localRecordExists` is exposed on `pull` and `listConflicts`; `CompanySyncCard.test.tsx` adds a missing-local conflict scenario that asserts the disabled buttons, the discard CTA copy, and the resolve mutation payload; `translateServerError.test.ts` covers `TypeError('Failed to fetch')`, the wrapped tRPC error with `cause`, and the missing-key fallback. `npm audit --omit=dev` stays at 0 high; `ci:web` + `ci:server` green. **Shipped (ENG-042 close-out)**: the two `[infra][sync]` follow-ups previously captured as working title `ENG-043` landed in a single TOCTOU sweep: (a) `sync.resolve` `findEntity` guard hoisted INSIDE `ctx.db.transaction(...)` so a concurrent delete between the precondition check and the transaction body can no longer leave keepLocal / merged paths resolving against stale data — better-sqlite3 transactions are connection-scoped so calling `findEntity(ctx.db, ...)` from inside the callback runs against the same in-flight BEGIN; (b) the prose `error.message` `"Cannot keep or merge local changes…"` was replaced with `throwServerError({ errorCode: 'SYNC_LOCAL_RECORD_MISSING', ... })`, registered in both `packages/server/src/lib/errorCodes.ts` and `apps/web/src/lib/translateServerError.ts::KNOWN_SERVER_ERROR_CODES`, with neutral LATAM Spanish copy `"El registro local ya no existe. Descarta el cambio local para aceptar la versión remota."` and the English equivalent landing in `apps/web/src/i18n/locales/{en,es}/errors.json`. Audit-driven scope expansion (operator-approved): the same TOCTOU shape was found at three `sales.ts` sites where `requireActiveCashSession` runs BEFORE `ctx.db.transaction(...)` and `activeCashSession.id` is then written as the FK inside; new module-private `assertCashSessionStillOpen(tx, tenantId, cashSessionId)` helper added with `CASH_SESSION_REQUIRED` re-validation as the FIRST statement inside `sales.create`, `sales.returnSale`, and `sales.completeDraft` transactions. Tests: 2 new `sync.test.ts` cases pin the new errorCode shape and no-partial-write behavior (conflict status stays `pending`, queue row survives); the existing missing-local rejection test was updated to assert on `cause.errorCode` instead of the now-changed prose. Coverage: server 84.19/68.46/81.06/85.09 (above the 80 floor); web locale-parity green. The sales.ts inner-guard throw branch is structurally pinned but not directly tested (testing the race window requires a concurrent-close harness — flagged as a follow-up). | Operator with a stale local-record conflict can clear it via "Discard Local Change" without database surgery; offline boot logs no console error; every server error in Company cards renders localized copy from `errors:server.*` instead of raw English / network strings. | **Medium** |
| `ENG-049` | Purga UX — headers + heroes minimalistas | Shipped | Out-of-band, operator-driven. Pasada de minimalismo sobre todo el chrome textual redundante que el design system de ENG-045 había shippeado: kicker `Punto de Venta` + h2 `Espacio de trabajo` + badge `currentTenant.name` removidos del header app-wide y el grid reescrito a una sola columna fluida (hamburger + search + status + language pill + site selector + notifications + user menu). Sidebar brand sin `kicker` ni `tagline` — queda ícono + "Puntovivo". Hero panels de Dashboard + SalesOverview perdieron el bloque kicker + headline + description completo (las métricas y el freshness panel sobreviven; freshness card también pierde su kicker `Actualización`). 12 páginas estándar (Products, Inventory, Customers, AuditLogs, Purchases, FiscalDocumentList, FiscalReports, ReceiptTemplates, Orders, Quotations, Copilot, Company) perdieron el `page.description` debajo del h1; `page.kicker` también borrado donde sólo aparecía en el hero (audit-logs, fiscal, receiptTemplates, quotations, copilot). `ResourcePage` componente compartido (Customers, Categories, Locations, Providers, Units, VAT Rates, CustomerCatalogs) ahora tiene `description?: string` opcional + render condicional; cada caller dropeó la prop. Cards `CompanyMxFiscalCard` y `CompanyClFiscalCard` perdieron el párrafo de description debajo del título (form + readiness badge bastan). Sidebar group labels (Overview/Flow/Setup), `page.permissionNote` (Products + Inventory) y `CompanyAISettingsCard description` se mantienen — aportan info real. Geography section descriptions discriminan tabs y se mantienen. i18n cleanup en + es como pareja: ~30 keys huérfanas borradas; locale-parity test verde 104/104. CSS classes `page-kicker` / `page-title` / `page-description` se conservan en `components.css` por ahora (limpieza opcional cuando llegue cero callsites). Convención fiscal-en-español NO aplica (sólo UI / i18n). Reversibilidad: la cleanup no acopla lógica; reinstalar cualquier sección es trivial si UX feedback lo pide. **Collateral fix**: tipo huérfano `EnvironmentChoice` en `CompanyClFiscalCard.tsx` (residuo de ENG-036a) borrado para que `tsc --noEmit` quede limpio; constante `catalogDescription` en `CustomerCatalogsPage.tsx` borrada por la misma razón. | Header sin chrome textual; sidebar con "Puntovivo" plano; cada página estándar abre con h1 corto + acción + contenido (sin párrafo descriptivo abajo). Mobile sigue mostrando hamburger; locale-parity verde; cero `t('page.kicker')` rendered como string crudo. | **Medium** |
| `ENG-050` | Sellability index + roadmap truth sync | Shipped | Align the roadmap with the current shipped state before the Foundation Reset wave: park-and-resume is fully shipped; fiscal core/modeling is shipped but real PT + contingency remain blockers; hardware remains gated; payment terminal remains manual-only. Add `docs/SELLABILITY.md` as the single Colombian retail go/no-go index for demo, pilot, and production readiness. **Shipped**: `ROADMAP.md` MVP status now matches the actual repo, `SELLABILITY.md` captures pilot/production blockers and next tickets, and docs indexes point to the new sellability source of truth. | `ROADMAP.md`, `SPRINT-PLAN.md`, and `SELLABILITY.md` do not contradict each other; every sellability blocker has a gate and next ticket. | **High** |
| `ENG-051` | Foundation ADR pack | Shipped | Add short ADRs for Local Store Authority, Command Envelope, Outbox taxonomy, and Conflict policy. The decisions lock Electron desktop as the local store authority, require `operationId` / `deviceId` / `idempotencyKey` / `clientCreatedAt` on critical commands, separate sync/fiscal/payment/webhook/hardware outboxes by risk, and forbid automatic conflict resolution for money/fiscal/cash. **Shipped**: nuevo directorio `docs/architecture/` con `README.md` (convenciones + template + index activo) y cuatro ADRs numerados — `0001-local-store-authority.md` (lock Electron desktop como autoridad del local store, con referencia al `desktopSession` de ENG-025), `0002-command-envelope.md` (`operationId` + `deviceId` + `idempotencyKey` + `clientCreatedAt` con lista cerrada de comandos críticos cubriendo sales lifecycle / cash sessions / inventory / fiscal / payment / users), `0003-outbox-taxonomy.md` (cinco outboxes — `sync_outbox`, `fiscal_outbox`, `payment_outbox`, `webhook_outbox`, `hardware_outbox` — con kernel compartido + retry policies independientes; sección `Fiscal outbox` en español por convención fiscal), y `0004-conflict-policy.md` (manual-only para money/fiscal/cash/inventory operacional; auto-LWW con audit log obligatorio para catalog/preferences; lista cerrada de entidades de alto riesgo en español + lista de non-financial en inglés). Cada ADR carries Decision + Alternatives Rejected + Implementation Impact + Affected Tickets, todas en estado `Accepted`. ADRs en inglés general; secciones que tocan `services/fiscal/**` o entidades fiscal/contables en español neutral LATAM tú per AGENTS.md. `docs/README.md` cross-doc map enlaza el directorio nuevo bajo "Architecture & stack". `docs/PLAN.md §18.1` registra los cinco archivos. Foundation Reset counter sube de 1/21 a 2/21. Las próximas implementaciones (ENG-052..ENG-056, ENG-057..ENG-058, ENG-064..ENG-065) tienen ahora las decisiones formalmente lockeadas; no requieren renegociar el shape del envelope ni el split de outboxes ni la política de conflictos. | Each ADR records decision, rejected alternatives, implementation impact, and affected future tickets. | **High** |
| `ENG-052` | Device registry + command envelope | Shipped | Add formal `devices` and idempotency-key models. tRPC/desktop propagate `x-device-id`; critical commands accept `operationId`, `idempotencyKey`, and `clientCreatedAt`; logs include request/operation/device context. **Shipped (ENG-052a foundation)**: migrations `0010_devices_and_idempotency_keys.sql` + `0011_idempotency_atomic_reservation.sql` agregan tablas `devices` (tenant-scoped, kind desktop\|web, registered_by_user_id FK, last_seen_at, is_active soft-delete, JSON metadata) e `idempotency_keys` (composite unique sobre tenant+device+key+operationKind, status processing/succeeded/failed, locked_at/completed_at, TTL default 24h, lazy eviction + sweeper) + nueva columna nullable `audit_logs.operation_id` con index. Servicios puros `services/devices/devicesService.ts` (`registerDevice` idempotent, `findActiveDevice`, `markSeen`, `deactivateDevice`) + `services/idempotency/idempotencyService.ts` (`reserveKey`, `completeKey`, `failKey`, `cleanupExpired`) + `services/idempotency/keyHasher.ts` (canonical JSON sort + SHA-256, deterministic across object key order). Middleware tRPC `commandEnvelope` valida `x-device-id` contra devices + parsea/valida `x-puntovivo-envelope` JSON header con Zod (operationId UUID v4 + idempotencyKey min 8 + clientCreatedAt ISO 8601), reserva atómicamente la key via composite unique, retorna `COMMAND_IN_PROGRESS` cuando la llamada original sigue processing, replay-cached cuando el hash matchea tras success, o levanta `IDEMPOTENCY_KEY_CONFLICT` cuando difiere; inyecta ctx.envelope + ctx.deviceId + request-scoped logger child con operationId/operationKind/userId/tenantId. Decorator `criticalCommandProcedure` con 4 variantes (base + admin + managerOrAdmin + cashierManagerOrAdmin) listo para que ENG-052b wrappee las 17 procedures restantes. Nuevo procedure `auth.registerDevice` admin/cashier/manager-callable, idempotent en deviceId existente, multi-tenant safe. **Proof procedure end-to-end**: `auth.changePassword` migrado de `protectedProcedure` a `criticalCommandProcedure`; los 5 tests existentes de auth.test.ts adaptados al nuevo helper `createCriticalCommandFixture` que registra device + mintea envelope per-call. 42 nuevos tests (idempotency 16 + devices 16 + commandEnvelope-via-changePassword 10: missing device, unknown device, cross-tenant device, missing envelope, malformed envelope JSON, invalid envelope shape, replay cached, replay-with-mismatch, command-in-progress, device markSeen). 4 nuevos error codes (DEVICE_NOT_REGISTERED, MISSING_COMMAND_ENVELOPE, IDEMPOTENCY_KEY_CONFLICT, COMMAND_IN_PROGRESS) en server + KNOWN_SERVER_ERROR_CODES + i18n en/es como pareja (neutral LATAM tú). Web: nuevo `lib/deviceId.ts` con localStorage persistence + Electron-bridge fallback + sync cache + `lib/commandEnvelope.ts` con `mintEnvelope()` + `buildCriticalCommandHeaders()` + `lib/useCriticalMutation.ts` wired para `auth.changePassword`. AuthProvider llama `auth.registerDevice` post-login con kind detectado de window.electron, persiste el id retornado, prime cache en boot, y conserva el id en logout. `getTrpcHeaders()` emite `x-device-id` global cuando el cache está poblado; `useCriticalMutation('auth.changePassword')` inyecta el envelope per-call para el proof procedure, y ENG-052b expande el wrapper al resto de mutaciones críticas. CORS Fastify permite los dos headers nuevos (colateral). Server coverage 83.57/68.94/80.53/84.72, web 722/722 tests verde, ci:server + ci:web verde, smoke Playwright verde (login en demo-co → device id `QWM6zFd0...` persistido en localStorage → dashboard sin console.error). **Shipped (ENG-052b close-out)**: las 17 procedures restantes de la closed list ADR-0002 quedaron envueltas con sus variantes `criticalCommand*` — `sales.create/completeDraft/suspend/resume/discardDraft/returnSale/void/getForReprint`, `cashSessions.open/close/recordMovement`, `inventory.adjustStock`, `transfers.create/receive/void`, `users.create/update`. Los 5 routers (`sales`, `cashSessions`, `inventory`, `transfers`, `users`) cambiaron solo el decorator; cero queries multi-tenant nuevas. Los 9 test files que recorren esas mutaciones (`sales`, `cashSessions`, `inventory`, `transfers`, `users`, `audit-logs`, `collections`, `sales-park-and-reprint`, `seed-dev`) ahora registran un device por archivo en `beforeAll` y exponen un Proxy de headers que mintea un Command Envelope fresco por acceso — el patrón evita rebuild del caller por mutación y respeta la unicidad composite `(tenant, device, idempotencyKey, operationKind)`. El helper `createCriticalCommandFixture` se extendió con `freshCriticalContext()` + `makeFreshContextFactory()` reusables. Web: `useCriticalMutation` se generalizó con type inference por `CriticalCommandPath` (split-template + bracket lookup contra `RouterInputs/Outputs`); 18 call sites en SuspendedSalesPanel, SaleDetailsModal, SalesPage, InventoryPage, InventoryTransferHistory, InventoryBalancesPanel, UsersPage migrados sin tocar onSuccess/onError. Electron: preload expone `electron.device.getId/setId` y main IPC `device:get-id`/`device:set-id` persiste atómicamente (tmp + rename) en `app.getPath('userData')/device-id.txt`; lógica I/O extraída a `device-id-store.ts` para test sin Electron. Fastify: nuevo hook `onRequest` cuelga `requestId` (Fastify reqId) + `deviceId` opcional en `request.log`; el middleware `commandEnvelope` ahora encadena su child off `req.log` cuando está disponible (fallback al module logger preserva tests sin Fastify). Tres archivos de test nuevos: `request-logger.test.ts` (5 cases sobre el helper de bindings), `device-id-store.test.ts` (5 cases sobre round-trip + atomicidad + rechazo de empty), `useCriticalMutation.test.tsx` (4 cases sobre device guard + dispatch dinámico + envelope fresco + propagación de errores). El seed-dev también registra un device permanente atribuido al admin para que los batches históricos pumpeen sales/cash/inventory/transfers a través del envelope. Live smoke OK: login `admin@demo.co` → device id persistido (`2Qz--gM-...`) → `cashSessions.open` desde el botón → "Cash session active" sin console.error. Server coverage 83.45/68.72/80.34/84.61 (≥ 80/63/77/80), web 726/726 verde (≥ 70/70/70/70), desktop 19/19 verde. Bug runtime corregido durante el smoke: el lookup dinámico hacía `proc.mutate.bind(proc)` que mis-bindea sobre el Proxy del cliente tRPC v11; refactor a invocación inline `proc.mutate(input)`. | Replaying the same idempotency key returns the same resource or rejects a different payload; logs can correlate UI click → tRPC → DB transaction. | **High** |
| `ENG-053` | Operation journal + outbox kernel | Shipped | Add an operational journal (`operation_events`, `operation_effects`, `operation_errors`) plus a shared outbox kernel with `kind`, status, attempts, `nextRetryAt`, and payload version. This is not full event sourcing; existing operational tables remain authoritative. **Shipped**: migración `0012_operation_journal_and_outbox_kernel.sql` agrega 4 tablas — `operation_events` (append-only intent log con composite UNIQUE `(tenant_id, operation_id)` para idempotencia, lifecycle `started → succeeded \| failed \| partial`), `operation_effects` (FK cascade, `kind` + `resource_type` + `resource_id` + `effect_data` JSON), `operation_errors` (FK cascade, `error_code` + `recoverable` flag + `error_data`), y `outbox_metadata` (cross-outbox health surface keyed por `(tenant_id, outbox_kind)`). Servicio `services/operation-journal/journal.ts` con `recordOperationStart` (idempotente, retorna `eventId` + `isNew`), `recordEffect`, `recordError`, `markOperationCompleted` (refusa transitions out-of-terminal), `getOperationTrail`. Kernel `lib/outbox/` con `types.ts` (NormalizedOutboxError + OutboxRetryPolicy + OutboxRow projection + BOUNDED_EXPONENTIAL_BACKOFF default `1m→5m→15m→1h→6h→24h`), `kernel.ts` (`createOutboxKernel<TStatus,TPayload>` factory expose `enqueue/claimNext/complete/fail/deadLetter/peek` con atomic claim via `claim_token` predicate), `worker.ts` (`tickOutbox` base con processor function), `metadata.ts` (`recordSuccess/recordFailure/refreshPendingCount/readMetadata/listMetadata`). El `commandEnvelope` middleware ahora encadena: tras la reservation `'reserved'`, llama `recordOperationStart` con el operationId del envelope; en éxito llama `markOperationCompleted(eventId, 'succeeded')`; en throw captura el `ServerErrorWithCode.errorCode` cuando aplica y emite `recordError` + `markOperationCompleted(eventId, 'failed')`; el `result.ok=false` path emite `errorCode='PROCEDURE_NOT_OK'`. Replay-cached calls reusan el evento existente sin crear segunda fila (composite UNIQUE). Pattern docs nuevos en `architecture/patterns/`: `operation-journal.md` (~150 líneas, código de ejemplo + lifecycle diagrama + multi-tenant invariant + best-effort POST-procedure semantics) y `outbox-kernel.md` (~190 líneas, ejemplo concreto de cómo ENG-057 va a definir `fiscalOutboxKernel = createOutboxKernel(...)` + retry policies + concurrency claim_token contract). 34 tests nuevos (operation-journal 14 + outbox-kernel 12 + outbox-worker 5 + commandEnvelope ext 3); server 882/882 verde, coverage 83.29/68.74/79.56/84.4 (≥ 80/63/77/80). Live smoke verde: 63 operation_events poblados automáticamente por el seed mega vía recent-via-tRPC pass + smoke explícito por Playwright (success → status=`succeeded`, replay con mismo envelope → cached sin nuevo event row, fail con productId inexistente → status=`failed` + 1 fila operation_errors `PROCEDURE_NOT_OK` recoverable=0). | Critical commands leave a correlated trail; post-commit failures are recorded without rolling back the original sale/cash/inventory operation. | **High** |
| `ENG-054` | Extract completeSale application service | Shipped | Move the main complete-sale orchestration out of the tRPC router into `application/sales/completeSale`, coordinating cash session, payments, stock, audit, fiscal/sync outbox, and journal effects behind a stable use-case boundary. **Shipped**: nuevo directorio `packages/server/src/application/` con README documentando la convención (use-cases en `application/` orquestan primitivas que viven en `services/`); primer feature `application/sales/` que aterriza el use-case `completeSale(ctx, input)` cubriendo los DOS paths a través de un input discriminado `{ mode: 'fresh' \| 'fromDraft', ... }`. Las pre-checks (validación de cliente, requireActiveCashSession, resolución de items, resolveSalePayments, ownership/draft-state guards) corren fuera de la transacción; una sola tx `db.transaction(...)` escribe sales/saleItems/salePayments/products/inventoryMovements/inventoryBalances/cashMovements/syncQueue/auditLogs según el mode; y POST-commit emite efectos al journal best-effort (`sale_row` + `payment_row` × N + `inventory_movement` × N + `cash_movement` + `sync_queue_emit` + `audit_log` + `fiscal_emit`) más el fiscal document via `safelyEmitFiscalDocument`. Policies puras movidas a `application/sales/policies.ts` (resolveSalePayments, getPaymentStatus, getCashCollectedAmount, getNormalizedSaleQuantity); `getSaleRecord` movido a `application/sales/sale-read.ts` para evitar circular imports; `safelyEmitFiscalDocument` movido a `services/fiscal/orchestrator.ts` con signature primitiva (`{ db, tenantId, userId, log, ... }`) para que returnSale/voidSale (ENG-055) lo hereden directamente; `writeAuditLog` ahora retorna el `auditLogId` para correlación de efectos. Router `sales.ts` simplificado de 2670 a ~1600 líneas: `sales.create` y `sales.completeDraft` son thin wrappers de ~30 líneas cada uno; helpers privados específicos del flow eliminados (`getSaleSequentialContext`, `validateCustomer`, `resolveSaleItems`, `resolveSalePayments`, `getPaymentStatus`, `getCashCollectedAmount`); los 4 callsites legados de `safelyEmitFiscalDocument` (returnSale, void) usan un adapter local `safelyEmitFiscalForCtx`. Primer service-level consumer del journal de ENG-053 — los `operation_effects` ahora se pueblan automáticamente en cada venta. 13 tests invariant nuevos en `__tests__/application-sales-completeSale.test.ts` que invocan el use-case directamente sin tRPC: fresh happy path con sequential advance, customer cross-tenant, descuento que excede total, split payment con dos rows persistidas, status='draft' (no fiscal, no cash movement), fromDraft happy con session rebind, fromDraft non-draft, fromDraft suspendido, fromDraft sin items, journal effects emitidos con envelope, journal effects skipeados sin envelope, multi-tenant isolation, post-condition snapshots. Los 45 tests existentes de sales (`sales.test.ts` + `sales-park-and-reprint.test.ts`) pasan sin modificación. ZERO breaking changes en el contrato tRPC `sales.create` / `sales.completeDraft`. Server coverage 83.65/69.37/80.20/84.76 (≥ 80/63/77/80), web 727/727 verde, desktop 19/19 verde. Live smoke OK con seed mega: 46 `operation_events` con `status='succeeded'` para `sales.create` y 328 `operation_effects` correlacionados — 46 sale_row + 60 payment_row (split) + 84 inventory_movement + 46 cash_movement + 46 sync_queue_emit + 46 fiscal_emit. | Behavior parity with current `sales.create` / `completeDraft`; invariant tests run without HTTP/tRPC. | **High** |
| `ENG-055` | Extract sale lifecycle services | Shipped | Extract `returnSale`, `voidSale`, `completeDraft`, and `discardDraft`; consolidate shared `SalePolicy`, `PaymentPolicy`, `InventoryPolicy`, and `FiscalPolicy` rules used by create/return/void/draft flows. **Shipped**: tres use-cases nuevos en `packages/server/src/application/sales/` — `returnSale.ts` (~340 líneas, refund flow con stock restore al site original, sale_returns insert, paymentStatus='refunded', cashMovement refund, audit `sale.return`, fiscal NC post-commit), `voidSale.ts` (~280 líneas, admin-only sin require active session del caller, restore stock, status='voided', cashMovement reversal CONDICIONAL al status del original session, audit `sale.void`, fiscal NC), y `discardDraft.ts` (~250 líneas, ownership check creator/suspender/manager/admin, reverse stock que se debitó al crear el draft, status='cancelled', clear suspended cols, audit `sale.park` con `discarded:true`, NO fiscal — drafts nunca tuvieron). Policies compartidas extraídas: `application/sales/inventory-policy.ts` con `reverseSaleItemsStock` (loop `restore stock + inventoryMovement insert + applyInventoryBalanceDelta` que las tres reversa duplicaban), `application/sales/fiscal-policy.ts` con `getOriginalDeeCufe` (query repetido por returnSale + voidSale), y extensión a `application/sales/policies.ts` con `buildReturnedSaleNotes` + `buildVoidedSaleNotes` + `getPersistedCashContribution`. Primitivas movidas a `services/cash-session.ts`: `assertCashSessionStillOpen` y `insertCashMovement` quedan con UNA sola fuente de verdad — `application/sales/completeSale.ts` (ENG-054) actualizó imports para consumirlas desde ahí. Router `sales.ts` simplificado de 1692 a ~990 líneas (-700): `returnSale`, `void`, `discardDraft` ahora son thin wrappers de ~10 líneas cada uno, y dead code eliminado (`safelyEmitFiscalForCtx`, `getNormalizedSaleQuantity`, `buildVoided/ReturnedSaleNotes`, `getPersistedCashContribution`, `insertCashMovement`, `assertCashSessionStillOpen`, `fiscalRouterLog`). Las tres operaciones ahora son service-level consumers del journal de ENG-053 — `operation_effects` se pueblan automáticamente para cada flow (returnSale: `sale_row` + `sale_return_row` + N `inventory_movement` + `cash_movement` + 2 `sync_queue_emit` + `audit_log` + opcional `fiscal_emit`; voidSale: análogo con `cash_movement` condicional al status del original session; discardDraft: `sale_row` + N `inventory_movement` + `sync_queue_emit` + `audit_log`, NUNCA `cash_movement` ni `fiscal_emit`). 27 invariant tests nuevos en `__tests__/application-sales-{returnSale,voidSale,discardDraft}.test.ts` que invocan los use-cases directamente sin tRPC. Los 58 tests existentes de sales (`sales.test.ts` + `sales-park-and-reprint.test.ts` + `application-sales-completeSale.test.ts` de ENG-054) pasan sin modificar assertions. ZERO breaking changes en el contrato tRPC `sales.returnSale` / `sales.void` / `sales.discardDraft`. | No duplicated cash/stock/fiscal lifecycle rules remain across sale mutations; existing sales tests stay green. | **High** |
| `ENG-056` | Cash session aggregate boundary | Shipped | Route every cash-affecting operation through a `CashSessionService`: sale, refund, void, paid-in/out, close, over/short, and fiscal/payment pending checks. **Shipped**: nuevo directorio `packages/server/src/application/cash-sessions/` espejo de `application/sales/` con tres use-cases — `openCashSession.ts` (~150 LOC, agrega `cash_session.open` audit log que faltaba para paridad con close), `closeCashSession.ts` (~200 LOC, integra `getPendingChecksForSession` previo al tx para enriquecer audit metadata + emite `pending_warning` journal effects post-commit, una por categoría no-cero), y `recordCashMovement.ts` (~120 LOC, ahora ruta por `insertCashMovement` eliminando el último INSERT directo en `cash_movements`). Helpers compartidos `pending-checks.ts` con `getPendingFiscalForSession` (join `fiscal_documents` × `sales` por `cashSessionId`, status IN pending/contingency, samples cap 5) + `getPendingPaymentForSession` (filter `status='completed' AND paymentStatus IN pending/partial`, excluye drafts/voided), `journal-effects.ts` con `emitCashSessionEffects` + `lookupCashSessionJournalEventId` mirror del pattern ENG-054, y `types.ts` con `CashSessionContext` paralelo a `CompleteSaleContext`. Router `cashSessions.ts`: las tres procedures críticas (open/close/recordMovement) son ahora thin wrappers de ~10 líneas via `buildCashSessionContext`; nueva query `cashSessions.pendingChecks` admin/manager-or-self gated mirror de `cashSessions.movements` access pattern. Schema bump: `auditLogActionEnum` agrega `cash_session.open` + `cash_session.movement` (TS-only narrowing, columna text libre — no migration); `auditLogResourceTypeEnum` agrega `cash_movement`. Servicio `services/cash-session.ts`: `insertCashMovement` widened de `type: 'sale'\|'refund'` + `referenceId: string` a full `CashMovementType` + `referenceId: string \| null` (sale-lifecycle callers ya pasan literales narrow, sin breaking change); export del type `CashMovementType` agregado. `closeCashSession` también gana `assertCashSessionStillOpen` TOCTOU re-check dentro del tx para paridad con sale-lifecycle services. Pending semantics: warning-only, NUNCA bloquea close — counts ride audit log metadata + `pending_warning` journal effects (kind por categoría con `samples`); UI consume `pendingChecks` query como pre-close gate. 35 invariant tests nuevos en `application-cashSessions-{open,close,recordMovement,pendingChecks}.test.ts` invocando use-cases directamente (no tRPC) cubriendo happy paths, denomination mismatches, missing-session/missing-site, pending detection con samples cap, pending mixed con dos `pending_warning` effects, cross-tenant isolation. 3 integration cases nuevos en `cashSessions.test.ts` (router-level): rejects manual movement sin sesión, pendingChecks zeroed para sesión vacía, pendingChecks empty cuando no hay sesión activa. Server 963 tests verde, coverage 84.15/69.98/80.5/85.25 (≥ 80/63/77/80). Web 727 tests verde con tipos `AuditLogAction`/`AuditLogResourceType` extendidos + 3 i18n keys nuevas en/es (`cash_session.open` / `cash_session.movement` / `cash_movement` resource). Live smoke: dev:server boot ok + `cashSessions.pendingChecks` route registrada y auth-gated (UNAUTHORIZED en GET sin token). Counter Foundation Reset sube de 6/21 a 7/21. ZERO breaking changes en el contrato tRPC; `cashSessions.open/close/recordMovement` retornan exactamente la misma shape que antes. Voidsale unification con shared session helper queda parqueada para un follow-up ticket — documentada en JSDoc de closeCashSession como asimetría conocida. | Tests cover no-sale-without-session, no cash movement outside a session, close-session totals, over/short, and pending fiscal/payment warnings. | **High** |
| `ENG-057` | Fiscal outbox + contingency engine | Shipped | Introduce a fiscal outbox separate from generic sync with queued/submitting/accepted/rejected/contingency/retrying/dead-letter lifecycle, retry backoff, normalized provider errors, and operator-visible recovery. **Shipped**: migración `0013_fiscal_outbox.sql` agrega la primera tabla concreta consumiendo el outbox kernel de ENG-053 — `fiscal_outbox` con 7-state status enum (`queued/submitting/accepted/rejected/contingency/retrying/dead_letter`), kind enum tightened a `['emit']` (cancel/retry_contingency/fetch_status diferidos per ADR-0003 sequencing), tres índices (`tenant_status_retry`, `fiscal_document`, `tenant_created`). Schema mirror: `fiscalOutboxStatusEnum` + `fiscalOutboxKindEnum` exports. Nuevo módulo `services/fiscal/errors.ts` con `FiscalProviderError` class + closed `NormalizedFiscalErrorKind` union de 10 kinds (4 recoverable: NETWORK_TIMEOUT/PROVIDER_5XX/RATE_LIMITED/UNKNOWN; 6 non-recoverable: PROVIDER_4XX/PROVIDER_REJECTED/MALFORMED_REQUEST/INVALID_CERT/EXPIRED_CERT/AUTH_FAILED). `services/fiscal/error-normalizer.ts` con `normalizeFiscalError(err, providerId)` dispatch a per-pack mappers + default heuristic (TypeError/SyntaxError → MALFORMED_REQUEST non-recoverable; everything else → PROVIDER_5XX recoverable). 3 stub mappers `packs/{co,mx,cl}/error-mapping.ts` retornan null para que el normalizer caiga al default heuristic — provider-specific code dispatch llega incrementalmente con ENG-021/ENG-035c/ENG-036c. Orchestrator `services/fiscal/orchestrator.ts` ahora exporta `enqueueFiscalEmission(args)` que en una sola tx local: idempotency probe en `(tenantId, source, sourceId, kind)`, validateConfig pre-flight (paquetes parqueados como MX sin RFC retornan null sin escribir fila — preserva legacy behavior), resolución de buyer/lines/resolution/locale/tenantSettings, INSERT `fiscal_documents` con `status='pending'` + CUFE placeholder `pending-<nanoid>` (la tabla unique constraint queda satisfecha por el random nanoid; el worker overwrite con el CUFE real del adapter en accepted), INSERT `fiscal_document_items`, advance `fiscal_numbering_resolutions.currentNumber`, INSERT `fiscal_outbox` row con payload completo `{countryCode, providerId, fiscalDocumentId, adapterInput}`. Después del commit hace fire-and-forget `tickDefaultFiscalWorker(tenantId)` para que happy-path latency esté cerca del status quo síncrono. Back-compat shim `safelyEmitFiscalDocument` ahora delega a `enqueueFiscalEmission` retornando la legacy `{id, cufe, documentNumber, status}` shape — los tres callers (`completeSale.ts`, `voidSale.ts`, `returnSale.ts`) no requirieron ediciones. `emitFiscalDocument` legacy permanece para los tests existentes que la invocan directamente. Nuevo daemon `services/fiscal/fiscal-worker.ts`: factory `createFiscalWorker({db, tenantIdsProvider, intervalMs, workerId, retryPolicy, resolveAdapter})` retorna `{tickOnce, start, stop}`. Boot wired en `index.ts::createServer` como singleton via `setDefaultFiscalWorker`, registered en `app.addHook('onClose')`, expuesto en `PuntovivoServer.fiscalWorker` para tests. Tick cadence: 30s setInterval + immediate `tickOnce` después de enqueue + stale-claim sweep a startup AND cada 5 min + per-tick adapter timeout 60s. Mirror invariant: en éxito worker UPDATE `fiscal_documents` (status='accepted', cufe real, providerResponse, xmlRef) ANTES del kernel transition; en recoverable error UPDATE doc to status='contingency' → outbox status='retrying' o 'dead_letter' al exhaustion; en non-recoverable error UPDATE doc to status='rejected' → outbox status='dead_letter' inmediato. Test seam `__setFiscalAdapterForTest(countryCode, adapter)` + `__clearFiscalAdapterOverridesForTest()` en `services/fiscal/registry.ts` permite inyectar stubs con `throwOracle?: (input) => Error \| undefined` (extensión simétrica del existente `contingencyOracle` en MockAdapter). Nueva admin tRPC procedure `reports.fiscal.retryDocument({fiscalDocumentId})`: re-arma rows en `retrying`/`contingency` (clear nextRetryAt) o re-enqueue rows en `dead_letter`/`rejected` (insert fresh outbox row con priority=-1 + reset doc status to `pending`); ambas paths fire `tickOnce` para drain inmediato. 6 integration tests nuevos en `__tests__/fiscal-outbox-integration.test.ts` cubriendo los 4 acceptance paths (happy → outbox=accepted + doc=accepted, recoverable outage → outbox=retrying + doc=contingency + sale completed sin rollback, non-recoverable error → outbox=dead_letter + doc=rejected, retry router rearm → next tick drains a accepted) + integration con pendingChecks (contingency rows count, rejected rows excluded) + retry dead-letter idempotente post-accepted. Server 969 tests verde, coverage 83.6/69.7/79.89/84.78 (≥ 80/63/77/80). Web 727 tests verde sin breakage de tRPC inference. Live smoke OK: dev:server boots, `fiscal_outbox` table presente con 3 índices, health endpoint responde. Counter Foundation Reset sube de 7/21 a 8/21. UI surface para retry button en `FiscalDocumentListPage` queda diferido — el operator puede llamar `reports.fiscal.retryDocument` via tRPC dev tools, y ENG-058/ENG-065 landearán la affordance proper. ENG-059 sigue gated en el real DIAN PT contract — el swap será una sola edición de `packs/co/index.ts` cuando el contract lande. | Offline sale completes locally and leaves a visible pending fiscal document; simulated provider outage writes contingency state and does not roll back the sale. | **High** |
| `ENG-058` | Receipt fiscal finalization | Shipped | Finalize fiscal proof rendering on receipts and reports: CUFE/CUDE or provider UUID, QR, XML reference, and explicit accepted/pending/contingency/rejected status. **Shipped**: Option A surgical extension del legacy `receiptPrinter.ts` + `SaleDetailsModal.tsx` con datos fiscales reales (rewiring del legacy printer al server `renderReceipt` queda diferido). Server: nuevo `services/fiscal/qr-builder.ts` con `buildFiscalQrPayload({country, environment, doc, tenant})` + `isPlaceholderCufe` helper; tres guard layers protegen contra placeholder leak (status gate `accepted/sent` solamente, placeholder gate `pending-<nanoid>` regex, country gate unknown→null). Per-country branches: **CO** emite la URL pública `https://catalogo-vpfe[-hab].dian.gov.co/document/searchqr?documentkey=<CUFE>`; **MX** emite la URL SAT Anexo 20 v4.0 `https://verificacfdi.facturaelectronica.sat.gob.mx/?id=<UUID>&re=<RFC_emisor>&rr=<RFC_receptor>&tt=<total padded 17.6>&fe=<sello last 8>` (sello extraído de providerResponse.sello, omite `&fe=` cuando ausente); **CL** retorna null con TODO ENG-036b para TED computation. `RenderFiscal` en el servidor renderer ahora incluye `status` y `statusLabel` (callers pre-resolve via i18n; renderer keeps pure). `application/sales/sale-read.ts::getSaleRecord` extendido con `fiscalDocuments[]` que cubre DEE/FEV (`source='sale' AND sourceId=saleId`), void NCs (`source='void' AND sourceId=saleId`) y return NCs (`source='return' AND sourceId IN saleReturns.id`); query única con OR + inArray, hits del índice `idx_fiscal_documents_source`. Cada doc carries `countryCode` derivado del `localeCode` congelado en el documento fiscal y `qrPayload` computado on-the-fly via `buildFiscalQrPayload`; el QR MX usa el RFC del emisor desde `tenants.settings.fiscal.mx.rfc` cuando existe. Web: nuevo `apps/web/src/components/fiscal/FiscalStatusBadge.tsx` con 5 variantes colored (success/primary/secondary/warning/danger para accepted/sent/pending/contingency/rejected); reusa los `fiscal:status.*` i18n keys existentes (zero duplication). Nuevo `apps/web/src/features/sales/SaleDetailsFiscalBlock.tsx` que renderiza por fiscal_document: kind label + status badge + document number, conditional CUFE display (full mono cuando `accepted/sent && !isPlaceholderCufe`; sino `cufePlaceholder` text), source label, country-aware "Verificar en DIAN/SAT/SII" link cuando qrPayload non-null, "Ver XML" affordance admin-only cuando xmlRef present. Wired en `SaleDetailsModal.tsx` debajo de `SaleDetailsContent`. `apps/web/src/features/sales/receiptPrinter.ts` extendido con `buildFiscalSection(docs)` async helper que dynamic-imports `qrcode` library (~16KB lazy chunk, never lands on first paint); siempre renderiza document number + kind + status copy (status copy es la SINGLE SOURCE OF TRUTH; receipt nunca infiere "accepted" de CUFE presence). Conditional CUFE display + conditional QR PNG via `<img src=<dataUrl>>` con error correction M, scale 6. `buildSaleReceiptHtml` y `printSaleReceipt` ahora son async; ambos callers (SaleDetailsModal lines 89, 188) ya estaban en async context. Nuevo i18n namespace `apps/web/src/i18n/locales/{en,es}/receipts.json` con framing copy (`fiscal.sectionTitle`, `fiscal.cufeLabel`, `fiscal.cufePlaceholder`, `fiscal.verifyLink`, `fiscal.qrCaption`, `fiscal.viewXml`, `fiscal.source.{sale,void,return}`, `fiscal.authority.{CO,MX,CL}`); registrado en `i18n/index.ts`. Spanish: neutral LATAM tú register, no voseo (Escanea, Verifica, Pendiente, Documento). Tests: 14 unit tests en `qr-builder.test.ts` (CO production/habilitación URL formats, MX SAT Anexo 20 con/sin sello, CL stub returns null TODO ENG-036b, status gate pending/contingency/rejected, placeholder gate defense-in-depth, isPlaceholderCufe table); 6 component tests en `FiscalStatusBadge.test.tsx` (5 statuses + 3 color-class assertions); 5 nuevos tests en `receiptPrinter.test.ts` (omits fiscal section sin docs, renders accepted full CUFE + QR, hides placeholder CUFE + skips QR en contingency con NEGATIVE REGEX `/receipt-fiscal[\s\S]*Accepted/`, rejected sin QR, MX labels render SAT + UUID instead of DIAN + CUFE, sale modal View XML opens the existing XML modal); 2 nuevos integration cases en `fiscal-outbox-integration.test.ts` (`getSaleRecord` exposes qrPayload non-null on accepted, returns null qrPayload on contingency con placeholder cufe). Server 985 tests verde, coverage 83.7/69.81/80.04/84.87 (≥ 80/63/77/80). Web 748 tests verde, build clean. Live smoke: dev:server boots, health endpoint responde, fiscal_documents schema intact (no migration needed — RenderFiscal extension es TS-only). Counter Foundation Reset sube de 8/21 a 9/21. Dependencia nueva `qrcode@^1.5.x` (16KB gzipped lazy chunk) + dev `@types/qrcode`. Templated-renderer wiring (Option B) queda diferido a future ticket; default templates `defaultLayouts.ts` no se modifican (fiscal_dee already has fiscal blocks, sale default needs conditional rendering primitive); CL TED computation queda parqueado a ENG-036b; CP-1252 codepage para ESC/POS queda BACKLOG follow-up (los 5 status labels en español son ASCII puro: Pendiente/Enviado/Aceptado/Rechazado/Contingencia). | Fiscalized receipt contains the legal proof fields; contingency documents never render as accepted. | **High** |
| `ENG-059` | Real DIAN PT adapter | Gated | Swap the mock Colombia adapter for the selected DIAN-authorized Proveedor Tecnologico adapter. Gate: signed PT contract, sandbox/prod credentials, tenant certificate, numbering resolution, and provider error-code map. | DIAN/PT sandbox set passes; adapter swap does not change sales or application-service code. | **High (gated)** |
| `ENG-060` | Peripheral registry + hardware ports | Shipped | Add `site_peripherals` and hardware contracts for `ReceiptPrinter`, `CashDrawer`, `BarcodeScanner`, and `PaymentTerminal`, plus site-level configuration UI. **Shipped**: migración `0014_site_peripherals.sql` agrega la tabla `site_peripherals` (`id, tenant_id FK tenants, site_id FK sites, kind, driver, config_json, display_name nullable, is_active, last_tested_at, last_test_result, last_test_details, created_at, updated_at`) con índice `(tenant_id, site_id, kind)` para el lookup del registry, índice `(tenant_id, kind)` para reportes cross-site y partial unique idx `(tenant_id, site_id, kind) WHERE is_active = 1` que enforza un periférico activo por kind por sede. Schema bump: `peripheralKindEnum = ['printer','cash_drawer','scanner','payment_terminal','customer_display']` + `peripheralTestResultEnum = ['ok','failed']` (TS-only narrowing). Cuatro contracts en `services/peripherals/contracts/` — `ReceiptPrinter.print(job)`/`testPrint`, `CashDrawer.kick`/`testKick`, `BarcodeScanner.getStatus`/`testScan`, `PaymentTerminal.charge`/`voidTxn`/`printSlip`/`testCharge` — más `BasePeripheralAdapter` shared base + `NormalizedHardwareError` discriminated union de 10 kinds (4 recoverable + 6 non-recoverable, mirror del fiscal pattern). Dos default drivers: `SystemReceiptPrinterAdapter` (driverId=`system`, `print()` retorna `{status:'ok'}` porque el renderer drives el actual print via `window.electron.printReceipt` — typed identifier, no code-path swap) + `ManualPaymentTerminalAdapter` (driverId=`manual`, `charge()` retorna `{status:'manual', requiresOperatorInput:true, prompt}`). Adapter registry `services/peripherals/registry.ts` mirror del fiscal pattern: static dispatch table `kind → driverId → factory`, `getPeripheralAdapter({db, tenantId, siteId, kind})` retorna el active adapter o `null` (no throws — sales code keeps working sin registration), `validatePeripheralConfig({kind, driver, config})` pre-write hook con per-driver Zod schema, `instantiateAdapter(row)` para evitar segundo SELECT en el test action, `__setPeripheralAdapterForTest` test seam. Cuatro nuevos error codes: `PERIPHERAL_NOT_FOUND`, `PERIPHERAL_DRIVER_INVALID`, `PERIPHERAL_CONFIG_INVALID`, `PERIPHERAL_ACTIVE_DUPLICATE`. Nuevo tRPC router `routers/peripherals.ts` admin-only con 6 procedures (`list({siteId})`, `register({siteId, kind, driver, config, displayName?})`, `update({id, driver?, config?, displayName?})`, `setActive({id, isActive})`, `test({id})`, `remove({id})`); cada procedure valida siteId via `ensureSiteBelongsToTenant`, captura UNIQUE constraint failures y los traduce a `PERIPHERAL_ACTIVE_DUPLICATE`, el `test` action stampa `last_tested_at` + `last_test_result` + `last_test_details` post-adapter call. Web admin UI nueva en `apps/web/src/features/peripherals/`: `PeripheralsPage.tsx` (admin route `/peripherals` con site selector, lista agrupada por kind, empty state con CTA, dialog states create/edit/closed), `PeripheralForm.tsx` (modal con kind picker locked en edit mode, driver picker filtrado por kind con hints `Disponible con ENG-062/063` en drivers parqueados, displayName + JSON config textarea, kind change handler snap-to-first-available), `PeripheralStatusBadge.tsx` (variant mapping `ok→success, failed→danger, null→secondary` + timestamp formateado por locale). Sidebar entry "Periféricos" agregada bajo Configuración con `Plug` icon (admin-only). Lazy route registration en `App.tsx` con ProtectedRoute admin gate. Nuevo i18n namespace `apps/web/src/i18n/locales/{en,es}/peripherals.json` registrado en `i18n/index.ts`; Spanish neutral LATAM tú: `Agregar periférico`, `Probar`, `Configura impresora térmica, cajón monedero, escáner de código de barras y datáfono por sede`, `Datáfono`, `Cajón monedero`. 32 server tests nuevos: `peripherals-registry.test.ts` (15 cases — listSupportedDrivers, validatePeripheralConfig PERIPHERAL_DRIVER_INVALID/CONFIG_INVALID, getPeripheralAdapter null/inactive/dispatch/override/cross-tenant, instantiateAdapter null para drivers no implementados, partial unique enforcement) + `peripherals-router.test.ts` (17 cases — admin gate cashier/manager/viewer FORBIDDEN, CRUD round-trip, register PERIPHERAL_ACTIVE_DUPLICATE, setActive PERIPHERAL_ACTIVE_DUPLICATE on activation conflict, test action stamps, foreign id PERIPHERAL_NOT_FOUND, update con driver desconocido rechazado). Web component test `PeripheralsPage.test.tsx` cubre admin-only render + empty state + row grouping + action affordances + Untested badge (5 cases). Server 1017 tests verde, coverage 83.81/69.92/79.94/84.95 (≥ 80/63/77/80). Web 758 tests verde, coverage 77.67/70.04/77.31/78.82 (≥ 70/70/70/70), build clean. Live smoke: login admin → /peripherals → empty state → register system printer "Caja principal" → status "Sin probar" → click Probar → status flips a "OK" + `Última prueba <timestamp>` → refresh + persistencia confirmada → register duplicate same kind → 409 Conflict surfacing `PERIPHERAL_ACTIVE_DUPLICATE` (sigue 1 row en SQLite); sqlite directly inspeccionado: `kind=printer, driver=system, display_name=Caja principal, is_active=1, last_test_result=ok`. Counter Foundation Reset sube de 9/21 a 10/21. Decisiones de scope: (1) `hardware_outbox` deferred a ENG-062 — los dos default drivers no tienen async fan-out (system delegates al renderer, manual es cashier-driven), shipping una unused table confunde al operador; ADR-0003 amended con footer single-line. (2) Legacy print path `apps/desktop/src/main/index.ts:1933 ipcMain.handle('print-receipt')` queda untouched — el SystemReceiptPrinterAdapter es un typed identifier, no un code-path swap; existing receipt print tests (10 cases en `apps/web/src/features/sales/receiptPrinter.test.ts`) stay green sin modificación. (3) Per-driver Zod schemas viven en la driver class (ENG-062's `escpos` driver agregará `{channel, host, port, vendorId, productId, paperWidth}` self-described). (4) `customer_display` en el enum pero sin contract/driver — registrarlo retorna `PERIPHERAL_DRIVER_INVALID`; ENG-062 o follow-up agrega contract + driver. (5) `peripherals.test` para system printer no dispara `webContents.print()` (evita system print dialog en operator screen) — stampa `last_tested_at` synchronously y retorna `{status:'ok'}`. ENG-061 (scanner pipeline), ENG-062 (ESC/POS + drawer), ENG-063 (Bold/Wompi/MercadoPago payment terminals) ahora landean como driver registrations adicionales sin tocar el registry plumbing ni el router. | Current system-printer path still works; ESC/POS, scanner, and payment terminal drivers can be registered without touching sales logic. | **High** |
| `ENG-061` | Barcode scanner pipeline | Shipped | Implement USB HID keyboard-wedge barcode capture, EAN-13 checksum, UPC/Code128 basics, and prefix 20-29 handling for weighted/price-embedded labels. **Shipped**: nuevo módulo puro `services/peripherals/barcode/parser.ts` con `validateEan13Checksum`, `validateEan8Checksum`, `validateUpcAChecksum` (mod-10 algorithms canónicos), `parseGs1WeightOrPrice({code, scheme})` para etiquetas LATAM prefix-2x weight/price-embedded (layout `2{role}{ITF-5 sku}{ITF-5 payload}{check}`; rol par → gramos, rol impar → centavos), y `parseScan(rawCode, {gs1Scheme})` que retorna una unión discriminada `{kind: 'ean13'|'ean8'|'upc-a'|'gs1-weight'|'gs1-price'|'unknown', code, lookupCode, weightKg?, priceMajor?, checksumValid}`. Per-country slices `co/mx/cl` actualmente piggyback en `generic`; se branchean cuando un piloto muestre divergencia real. Nuevo driver `KeyboardWedgeScannerAdapter` (driverId=`wedge`) en `services/peripherals/drivers/keyboard-wedge-scanner.ts` mirror del `SystemReceiptPrinterAdapter` pattern: typed identifier, no real hardware reach (HID keyboards no se pueden probar desde el server). `wedgeScannerConfigSchema` Zod: `{minLength: int [2,64] default 6, maxLength: int [2,64] default 32, interCharGapMs: int [10,500] default 30, endOfScan: 'enter'|'tab'|'gap-only' default 'enter', prefix?, suffix?, gs1Scheme: 'none'|'generic'|'co'|'mx'|'cl' default 'generic'}`. Registrado en `DRIVER_TABLE.scanner.wedge` del registry; el comentario "no drivers shipped for scanner" se reemplazó por la entrada activa. Nuevo procedure tRPC `products.lookupByBarcode` (`tenantProcedure`, sales-role accessible para que cajeros puedan escanear): toma `{barcode, parsePolicy: 'strict'|'permissive', gs1Scheme}`, parsea server-side via `parseScan`, busca exact match en `products.barcode` con `eq(products.tenantId, ctx.tenantId)` + `eq(products.isActive, true)` + el lookupCode (que es el SKU prefix para GS1 o el código verbatim para EAN/UPC), retorna `{product, parsed, suggestedQuantity, suggestedPrice} | null`. Strict mode rechaza checksum failures; permissive lo deja pasar para que el cashier pueda override en el carrito. Nuevo procedure `peripherals.activeForSite({siteId})` (`tenantProcedure`) que retorna una proyección minimal `Array<{kind, driver, config}>` de los rows activos — sales-role accessible (admin `peripherals.list` queda como la full-row read del admin UI). Nueva hook web `useBarcodeWedgeListener` modeled after `useSalesKeyboardShortcuts.ts`: global `document.addEventListener('keydown', ..., capture)` con cleanup; keystroke buffer con `interCharGapMs` reset timer (gap > threshold → reset buffer, así rejecta typing manual); end-of-scan emite onScan en Enter/Tab/gap-timeout según `endOfScan`; guards bail on `isProductSearchOpen || isPaymentModalOpen || isCashSessionModalOpen` y on `isEditableShortcutTarget(event.target)` (para no robar keystrokes que el cashier dirige a un input). Optional `prefix`/`suffix` se strippean antes de emitir; `enabled === false` corta el listener. SalesPage wiring: nueva query `trpc.peripherals.activeForSite.useQuery({siteId: currentSite.id})` con `staleTime: 5min` resolve `WedgeConfig` desde el row scanner-wedge active (fall back a `DEFAULT_WEDGE_CONFIG` cuando no hay registro — funciona out-of-the-box). `handleBarcodeScan(code)` async callback resuelve via `utils.products.lookupByBarcode.fetch({barcode})`; en hit, construye `ProductSearchSelection` con `unit = product.unitAssignments.find(isBase)`, `price = suggestedPrice ?? baseUnit.price`, llama `mergeCartItem`; cuando hay `suggestedQuantity` (GS1 weight) overrides la quantity post-merge via `updateCartItem`; toasts diferenciados `weightFromLabel` / `priceFromLabel` / `notFound` / `lookupFailed` / `resumedCartLocked`. Cuando `isResumedCart` es true, info-toast en lugar de silent drop. Nueva visual: el form admin de `/peripherals` ahora muestra `Teclado HID (USB)` como driver disponible (PeripheralForm DRIVER_OPTIONS.scanner = `[{id:'wedge', available:true}]`, reemplazando el placeholder parqueado). i18n nuevos: `peripherals.driver.wedge` (`Teclado HID (USB)` / `USB keyboard wedge`); `sales.scanner.{notFound,lookupFailed,weightFromLabel,priceFromLabel,noBaseUnit,resumedCartLocked}` (Spanish neutral LATAM tú: `No encontré el producto con ese código`, `Peso aplicado desde la etiqueta`, `Descarta el carrito retomado para agregar productos nuevos`). 26 unit tests del parser cubriendo EAN-13/EAN-8/UPC-A checksum válido + corrupto, GS1 prefix-20 weight + prefix-21 price, scheme=none disable, per-country co/mx/cl piggyback, lookupCode shape, length anomalies (5/14 → unknown), trim whitespace, non-digit → unknown. 8 router tests del lookupByBarcode (exact match → ean13, no match → null, GS1 weight → suggestedQuantity, GS1 price → suggestedPrice, cross-tenant isolation, strict policy reject corrupted checksum, permissive falls through, inactive products excluded). 4 nuevos router tests del activeForSite (empty array, minimal projection no leaks displayName/lastTestedAt/createdAt, excludes inactive, NOT_FOUND on foreign siteId). 5 nuevos registry tests del wedge driver (instantiateAdapter returns BarcodeScannerAdapter, validatePeripheralConfig defaults populate, validatePeripheralConfig rejects non-numeric minLength + interCharGapMs out of range). 7 web hook tests (HID burst + Enter emits onScan, manual typing all gaps > threshold rejected, buffer < minLength dropped, ProductSearchOpen suppresses, tab end-of-scan, prefix/suffix stripped, editable target ignored). Server 1043 tests verde, coverage 83.91/70.21/79.83/85.06 (≥ 80/63/77/80). Web 768 tests verde, coverage 78/70.5/77.93/79.18 (≥ 70/70/70/70), build clean. Live smoke (Playwright MCP): login admin → /sales con cash session abierta → fire synthetic 13-char HID burst + Enter dispatching keydown events al document → product `Producto Smoke Test SMK-001` lands en cart con `Cant. base 1` y total $100; fire unknown barcode `0000000000000` → cart unchanged (1 item), no duplicado; navigate to /peripherals → driver picker para kind=scanner muestra solo `Teclado HID (USB)` selected. Counter Foundation Reset sube de 10/21 a 11/21. Decisiones de scope: (1) SalesPage component-level integration test parqueado a follow-up — el SalesPage wraps 20+ tRPC procedures + multiples providers; el wedge listener unit tests + el lookupByBarcode router tests + la live smoke ya cubren la integración end-to-end. (2) Inter-screen scope: scanner pipeline ships en SalesPage only; inventory/returns adoption parqueada a follow-up cuando el caso real aparezca. (3) Paste-as-scan deliberadamente NO soportado — paste fires diferente event sin per-key cadence; ProductSearchDialog acepta pasted barcodes via su existing search field. (4) Per-country GS1 schemes (co/mx/cl) son TODO stubs piggybacking en generic — divergence real lleva a slice per-pack. (5) Code 128 / GS1-128 length 14+ retorna kind='unknown' por ahora; future ticket extiende el parser. (6) `useEffect` ref-sync para los options del hook satisface el `react-hooks/refs` ESLint rule sin sacrificar la estabilidad del listener install. ENG-062 (ESC/POS printer + cash drawer) ahora landea como driver registrations adicionales sin tocar el wedge pipeline ni el lookupByBarcode procedure. | Scanner adds a product to the cart within the target latency; manual typing is not misclassified as scanner input. | **High** |
| `ENG-062` | ESC/POS printer + cash drawer | Shipped | Implement the ESC/POS printer driver and RJ11 cash-drawer kick with the current system printer as fallback. Full validation is gated on physical printer + drawer hardware. **Shipped**: migración `0015_hardware_outbox.sql` (mirror estructural de `0013_fiscal_outbox`) cierra el deferral de ENG-060 — status enum `queued/submitting/printed/failed/retrying/dead_letter`, kind enum `print-{receipt,fiscal-dee,quotation,kitchen-ticket}|kick-drawer`, FK `peripheral_id` ON DELETE SET NULL, tres índices `(tenant,status,nextRetry)` + `(peripheral)` + `(tenant,createdAt)`. Schema bump: `hardwareOutboxStatusEnum` + `hardwareOutboxKindEnum` (TS-only narrowing). Pure byte builder en `services/peripherals/escpos/byte-builder.ts` con `buildEscPosBytes(doc, opts)` que emite la secuencia canónica ESC/POS (`ESC @` init → `ESC t n` codepage → `ESC a n` align / `ESC E n` bold / `GS ! n` doubleHeight → wrapped lines → opcional drawer pulse `ESC p 0 25 250` → cut `GS V 0`); soporte cp858 LATAM con override table para `ñáéíóú/¿¡€` (cp437 fallback ASCII-only); `wrapToColumns(text, cols)` 32 cols 58mm / 48 cols 80mm; `buildSaleReceiptDocument(input, opts)` derivado del sale shape; export del `ESCPOS_BYTES` constant table para tests. Transport wrapper `services/peripherals/escpos/transport.ts` con interface `EscPosTransport.write/close` + 4 implementations: `MockEscPosTransport` (in-memory buffer captura bytes para tests + smoke), `TcpEscPosTransport` (raw `net.Socket` con timeout 3s, defaults host `127.0.0.1` port `9100` Epson/Xprinter LAN convention), `UsbEscPosStubTransport` + `SerialEscPosStubTransport` (lazy stubs que arrojan `DRIVER_NOT_IMPLEMENTED` hasta que un follow-up agregue las native deps). `__setEscPosTransportForTest` test seam mirror del fiscal pattern. Dos drivers nuevos: `EscPosReceiptPrinterAdapter` (driverId=`escpos`, implements `ReceiptPrinterAdapter.print` lee `job.metadata.document` o `job.escposBytes`, normaliza `EscPosTransportError` a `NormalizedHardwareError`, applies `kickDrawerAfterReceipt` per-job override; `testPrint` envía banner canned), config Zod `escposReceiptPrinterConfigSchema = {channel: 'usb'|'tcp'|'serial'|'mock' default 'tcp', host?, port?, vendorId?, productId?, devicePath?, paperWidth: '58mm'|'80mm' default '80mm', characterSet: 'cp437'|'cp858'|'cp850'|'pc858_euro' default 'cp858', kickDrawerAfterReceipt default true, timeoutMs?}`; `EscPosCashDrawerAdapter` (driverId=`escpos`, kick() escribe `ESC p 0 25 250` via shared transport; testKick alias de kick), config schema mirror del printer. DRIVER_TABLE.printer.escpos + DRIVER_TABLE.cash_drawer.escpos registrados; el comentario "no drivers shipped for cash_drawer" reemplazado por la entrada activa. Nuevo daemon `services/peripherals/hardware-worker.ts` mirror de `services/fiscal/fiscal-worker.ts`: `createHardwareOutboxKernel(retryPolicy)` factory + `createHardwareWorker({db, tenantIdsProvider, intervalMs=30s, workerId, retryPolicy=BOUNDED_EXPONENTIAL_BACKOFF})`; tick cadence stale-claim sweep + periodic per-tenant fan-out con cap MAX_PER_TENANT_PER_TICK=25; `loadActivePeripheralForKind(siteId, targetKind)` resuelve printer vs drawer; dispatch por `kind` (`print-*` → ReceiptPrinterAdapter.print, `kick-drawer` → CashDrawerAdapter.kick); `toOutboxError` normaliza `NormalizedHardwareError → NormalizedOutboxError` (DEVICE_OFFLINE/DEVICE_TIMEOUT/PROTOCOL_ERROR/UNKNOWN recoverable; INVALID_CONFIG/DRIVER_NOT_IMPLEMENTED/PERMISSION_DENIED non-recoverable); boot-wired en `index.ts::createServer` paralelo al fiscal worker via `setDefaultHardwareWorker` + `app.addHook('onClose')`. Tres procedures tRPC nuevas en `routers/peripherals.ts`: `printReceipt({saleId, siteId})` `tenantProcedure` carga sale via `getSaleRecord` (cross-tenant guard) + valida site + lee active printer; cuando no hay row OR driver=system retorna `{status: 'system-fallback'}` (renderer keeps owning HTML); cuando driver=escpos llama `adapter.print(job)` con structured `ReceiptDocument` derivado de `buildSaleReceiptDocument(sale, ...)`; on success `{status: 'printed'}`, on error enqueue `hardware_outbox` row con kind=print-receipt + retry policy + fire-and-forget `tickDefaultHardwareWorker` y retorna `{status: 'fallback', error, errorMessage}` para que el renderer caiga al legacy HTML path AND emita toast traducido; `kickCashDrawer({siteId})` `managerOrAdminProcedure` carga active drawer; sin drawer registrado retorna `{status: 'no-drawer-registered'}` (UI hides the button — no toast); on success `{status: 'ok'}`, on error enqueue retry row (idempotente) + retorna `{status: 'error'}`; `peekHardwareOutbox({limit?})` `managerOrAdminProcedure` retorna minimal projection del outbox tail tenant-scoped, stub para que ENG-065 (Operations Center) lo consuma. Tres error codes nuevos en `lib/errorCodes.ts`: `HARDWARE_SALE_NOT_FOUND`, `HARDWARE_NO_DRAWER_REGISTERED`, `HARDWARE_TRANSPORT_FAILED`. Renderer wiring (Option A surgical, NO new IPC bridge — ESC/POS dispatch es SERVER-SIDE en main process via tRPC + native `net.Socket`): `apps/web/src/features/sales/receiptPrinter.ts::printSaleReceipt(sale, options?)` extendido con `escposDispatcher?` callback + `onEscposFallback?` hook; el dispatcher llama `trpc.peripherals.printReceipt.mutate({saleId, siteId})` y rama por `result.status`: `printed` → done; `system-fallback` → seguir al legacy `window.electron.printReceipt(html)` path (silent); `fallback` → seguir al legacy AND llamar `onEscposFallback` que dispara toast `La impresora ESC/POS falló; imprimimos por la del sistema`. Existing `apps/desktop/src/main/index.ts:1933 ipcMain.handle('print-receipt')` UNTOUCHED; preload UNTOUCHED. `SaleDetailsModal.tsx` usa `useTenant().currentSite` + builds the dispatcher closure + pasa a ambos call sites de `printSaleReceipt` (manual print + reprint). `SalesOverview.tsx` extendido con optional `onKickCashDrawer?` + `isKickingCashDrawer?` props; el botón `Abrir cajón` con `data-testid="sales-kick-drawer"` aparece junto a `Registrar movimiento` y `Cerrar caja` solo cuando (a) user.role manager/admin, (b) hay un drawer escpos registrado. SalesPage compute el handler condicional + onSuccess toast diferenciado (`Cajón monedero abierto` / `No hay cajón monedero registrado` / `No se pudo abrir el cajón monedero`). PeripheralForm flip: `DRIVER_OPTIONS.printer.escpos.available = true` + `DRIVER_OPTIONS.cash_drawer.escpos.available = true` (placeholder removed). i18n cleanup: drop `peripherals.driverHint.escpos` (en+es; el driver ya no está parqueado, los hints siguen para `bold/wompi/mercadopago` ENG-063); add `sales.printer.{escposFailedFallback, kickDrawerCta, kickDrawerSuccess, kickDrawerFailed, noDrawerRegistered}` en+es Spanish neutral LATAM tú: `Abrir cajón`, `Cajón monedero abierto`, `No hay cajón monedero registrado`, `La impresora ESC/POS falló; imprimimos por la del sistema`. 24 byte-builder unit tests cubren init/cut/drawer pulse/codepage/alignment-deduplication/bold-toggle/double-height/58mm-32-cols/80mm-48-cols/cp858-mapping/cp437-fallback/Euro-sign/wrap-on-whitespace/hard-cut-long-token/explicit-newlines/buildSaleReceiptDocument-line-ordering. 8 adapter tests (build from metadata.document → write via mock + assert init/cut prefix/suffix; drawer pulse appended when kickDrawerAfterReceipt; pulse skipped when false; pre-built escposBytes flow; DEVICE_OFFLINE error normalized; INVALID_CONFIG when neither bytes nor document; testPrint banner; drawer adapter pulses canonical bytes). 5 nuevos registry tests (escpos in listSupportedDrivers, instantiateAdapter para printer+escpos y cash_drawer+escpos, listSupportedDrivers cash_drawer→defined, customer_display→undefined, instantiateAdapter null para customer_display+escpos no implementado). 9 hardware-outbox-integration tests (system-fallback when no row, system-fallback when driver=system, printed when escpos+mock + bytes captured + no outbox row; fallback when transport throws + outbox row enqueued retrying + attempts=1; kickCashDrawer cashier FORBIDDEN, no-drawer-registered, ok writes pulse via mock; peekHardwareOutbox cashier FORBIDDEN, returns rows scoped to tenant). 2 router tests adjusted (replaced "escpos as unknown driver" assertions con `starprnt` placeholder ya que escpos ahora está registrado). Server 1108 tests verde, coverage 82.72/68.87/78.23/83.86 (≥ 80/63/77/80). Web 768 tests verde, coverage 77.82/70.36/77.93/79.01 (≥ 70/70/70/70), build clean. Live smoke via Playwright MCP: login admin → /peripherals → register escpos printer (kind=Impresora de recibos, driver=ESC/POS térmica, displayName=Caja principal ESC/POS, config=`{"channel":"mock","paperWidth":"80mm"}`) → register escpos drawer (kind=Cajón monedero, driver=ESC/POS térmica, displayName=Cajón principal, config=`{"channel":"mock"}`); SQLite directly verificó ambos rows con `is_active=1`. Navigate /sales → `data-testid="sales-kick-drawer"` button rendered con label "Abrir cajón"; click → `peripherals.kickCashDrawer` POST returned 200 OK + no `hardware_outbox` row enqueued (success path). Counter Foundation Reset sube de 11/21 a 12/21. Decisiones de scope: (1) **Native deps deferred** — `node-thermal-printer` + `usb` NOT installed; el byte-builder es pure JS y el transport ships con Mock + TCP (no native deps). USB + serial son stubs que arrojan `DRIVER_NOT_IMPLEMENTED`; un follow-up agrega los native bindings cuando el lab físico esté online. ci:server stays portable sin electron-rebuild. (2) **Server-side ESC/POS dispatch** (NO new IPC bridge): el escpos adapter abre USB/TCP desde el embedded Fastify in-process — el preload + `print-receipt` IPC handler quedan untouched. ENG-058's qrcode dynamic-import path stays intact. (3) **Auto-kick on cash sale parqueado**: ENG-062 ships solo el manual button + procedure; auto-kick on payment completion es follow-up cuando el operator confirme el UX. (4) **Server-side HTML fallback NOT shipped**: server retorna `{status:'system-fallback'}` signal only; el renderer sigue dueño de `buildSaleReceiptHtml`. Avoids HTML dual-source. (5) **Physical-hardware acceptance gated on test lab**: la acceptance "ESC/POS bytes print, cut, and open drawer in hardware smoke" se cumple a nivel byte (24 + 8 + 9 tests + smoke con mock); printer físico + drawer físico stay como follow-up smoke cuando el lab esté online. (6) **`hardware_outbox` deferral closed**: ADR-0003 footer "Updated 2026-05-04 (ENG-060): hardware_outbox deferred to ENG-062" eliminado; la tabla y el worker existen ahora. (7) **`escpos` driver picker para `cash_drawer`**: el config slot mirror del printer (channel + host/port/etc.) — operador apunta ambos al mismo physical hardware; JSDoc en `EscPosCashDrawerAdapter` lo documenta. ENG-063 (Bold/Wompi/MercadoPago) y un follow-up de auto-kick siguen pendientes. | ESC/POS bytes print, cut, and open drawer in hardware smoke; driver failure surfaces an actionable error and never duplicates a sale. | **High** |
| `ENG-063` | Payment terminal adapter | Gated | Add `ManualPaymentTerminalAdapter` fallback and prepare Bold/Wompi adapter seams. Gate: provider choice, sandbox credentials, and physical terminal. | Approved payment persists auth/reference in `sale_payments`; decline/timeout leaves the sale recoverable; offline-payment risk limits are explicit. | **High (gated)** |
| `ENG-064` | Sync contract v1 | Shipped | Formalize sync payload contract with entity type, operation, payload version, idempotency key, device ID, priority, dependencies, ordering, retry, duplicate suppression, and conflict policy. **Shipped**: migración `0016_sync_contract_v1.sql` agrega tabla `sync_outbox` (mirror estructural de `fiscal_outbox` ENG-057 / `hardware_outbox` ENG-062 con kernel projection — `id, tenantId, status, payload, payloadVersion, attempts, nextRetryAt, lastError, priority, claimToken, lockedAt, createdAt, updatedAt`) PLUS las 7 columnas de contrato per ADR-0002/0003/0004: `entityType, entityId, operation, conflictPolicy, idempotencyKey, deviceId, dependsOnOperationId, operationEventId`. Status enum `queued/submitting/synced/conflict/retrying/dead_letter`. Cuatro índices: `(tenant, status, nextRetry)` para el claimNext del kernel, `(entityType, entityId)` para drilldown del Operations Center, `(tenant, createdAt)` para listing, y partial unique `(tenant, entityType, entityId, operation, idempotencyKey) WHERE idempotency_key IS NOT NULL` que colapsa retries idempotentes en una sola fila. Data migration one-shot `INSERT OR IGNORE INTO sync_outbox SELECT ... FROM sync_queue` copia las pending rows con defaults sensatos (`payloadVersion=1`, `priority=0`) y aplica `conflictPolicy='manual'` para las 16 entidades de ADR-0004 high-risk (sales/sale_*/cash_*/fiscal_*/inventory_*/transfer_*/stock_adjustments/audit_logs), `auto_lww` para todo lo demás (catalog + preferences). Schema bump: `syncOutboxStatusEnum`, `syncConflictPolicyEnum`, `syncOperationEnum` (TS-only narrowing). Pure manifest en `services/sync/contract.ts` con `SYNC_PAYLOAD_VERSION=1`, `SYNC_ENTITY_TYPES` (45 entidades exhaustivas), `SYNC_CONFLICT_POLICY: Record<SyncEntityType, 'manual' | 'auto_lww'>` exhaustivo, `resolveConflictPolicy(entityType)` defense-in-depth runtime guard, `resolveDefaultPriority` (audit_logs=10, money-bound=5, catalog=0), `buildSyncContractManifest()` para `sync.getContract`. Helper `services/sync/enqueue.ts` con `enqueueSync(ctx, args)` que reemplaza el inline `db.insert(syncQueue).values(...)` pattern: lee envelope context (`ctx.envelope?.{operationId, idempotencyKey}` + `ctx.deviceId` inyectados por ENG-052's `commandEnvelope` middleware), resuelve `operation_event_id` via lookup en `operation_events` por `(tenantId, operationId)`, popula el contrato, escribe una fila a `sync_outbox` + emite `operation_effects` `kind=outbox_enqueue:sync` para el journal trail (ENG-053). Captura UNIQUE constraint failures del partial unique idempotent index y retorna `{id, deduped: true}` en lugar de throw — retries de la misma envelope colapsan a una sola fila. Tres procedures tRPC nuevas en `routers/sync.ts` (additivas — los 8 procedures existentes operan sobre `sync_queue` sin cambio): `sync.getContract` (managerOrAdmin) retorna `{payloadVersion, entities: Array<{entityType, conflictPolicy, defaultPriority}>}` desde el manifest para que ENG-068+ multi-store sync negocie el contrato; `sync.peekOutbox({limit})` (managerOrAdmin) projection minimal del tail de `sync_outbox` ordenada por `(priority DESC, createdAt ASC)` para que ENG-065 Operations Center renderice manual conflicts distintos; `sync.retry({id})` (admin) operator path para "esta fila se atascó, force retry now" — re-arma solo rows `retrying`/`dead_letter` reseteando `attempts=0`, `nextRetryAt=null`, `lastError=null`, `claimToken=null`, status back to `queued`; `queued`/`submitting`/`synced`/`conflict` son no-op para no replayear rows drenados; surfaces `SYNC_OUTBOX_NOT_FOUND` cuando el row id es foreign. Dos error codes nuevos en `lib/errorCodes.ts`: `SYNC_OUTBOX_NOT_FOUND`, `SYNC_OUTBOX_DEAD_LETTER`. Nueva ADR `docs/architecture/0005-sync-payload-contract.md` documenta el manifest + version-bump protocol + sync_outbox schema; cross-link agregado en `docs/architecture/README.md`; `docs/PLAN.md §18.1` registra el archivo. ADR-0003 footer "ENG-064 introduces sync_outbox and deprecates sync_queue" actualizado a "Shipped 2026-05-05" con nota explícita de que el writer rewrite + procedure cutover quedan parqueados a ENG-064b. 27 tests nuevos: `sync-contract-manifest.test.ts` (8 cases — payloadVersion positive, manifest keys match SYNC_ENTITY_TYPES, manual list (16 entidades) y auto_lww list (15 entidades) clasifican correctamente, resolveConflictPolicy throws on unknown, resolveDefaultPriority audit=10/money=5/catalog=0, manifest build con todas las entradas, exhaustiveness scan automated del entityType: '...' literal en cada router file que verifica zero drift) + `sync-contract-v1.test.ts` (19 cases cubriendo las 4 áreas de acceptance — Ordering: peekOutbox ordena audit_logs > sales > products por priority+createdAt; resolveDefaultPriority constants check; dependsOnOperationId persiste; Retry: sync.retry resetea attempts/nextRetryAt/lastError/status para retrying/dead_letter, no-op para synced, NOT_FOUND on unknown id, FORBIDDEN on cashier role, peekOutbox surfaces attempts+nextRetryAt; Duplicate suppression: idempotent retries con misma envelope key colapsan via partial unique idx, missing idempotencyKey produce filas separadas (catalog idempotent on consumer side), distintas keys producen filas separadas, cross-tenant isolation con shared key produce filas separadas; Manual conflict: 8 high-risk entities tagged manual, 5 catalog entities tagged auto_lww, peekOutbox surfaces conflictPolicy distinguible por entityType, runtime guard rechaza unknown entityType, admin can call getContract, cashier FORBIDDEN). Server 1135 tests verde, coverage 82.75/69.02/78.11/83.87 (≥ 80/63/77/80 floor). Web 768 tests verde sin cambios (todo el ticket es server-side). SQL-level smoke confirmó migration aplicada cleanly + las 4 indexes presentes. Counter Foundation Reset sube de 12/21 a 13/21. Decisiones de scope explícitas: (1) **Writer rewrite parqueado a ENG-064b**: los 19 router files (sales, cashSessions, purchases, orders, quotations, customers, products, categories, units, providers, vatRates, customerCatalogs, locations, sites, companies, sequentials, inventory, users, receiptTemplates) siguen usando `db.insert(syncQueue).values(...)` inline. ENG-064 v1 ships solo el HELPER + las nuevas procedures + los tests; cuando ENG-064b cierre, los 19 routers cambian a `enqueueSync(ctx, args)` y `sync.*` procedures corten a `sync_outbox`. La razón: el rewrite del 731-line `routers/sync.ts` + los 19 router edits son ~12h adicionales que pueden landear en una segunda pasada sin afectar la acceptance criteria, que solo requiere "Tests cover ordering, retry, duplicate suppression, and manual conflict paths for high-risk entities". (2) **Auto-LWW resolution behavior parqueado**: ENG-064 v1 ships solo el MARKER (`conflictPolicy='auto_lww'` en la fila); la lógica que aplica last-write-wins automáticamente en `sync.push` queda para ENG-064b o ENG-067. (3) **`sync_queue` legacy stays**: la tabla legacy sigue en place como deprecated read-only backstop hasta que ENG-064b la dropee post-cutover. (4) **No sync worker daemon**: ENG-064 v1 NO agrega un periodic worker — `sync.push` sigue siendo operator-triggered. ENG-066 chaos suite + ENG-068+ multi-store agregarán el daemon cuando el server-to-server sync arrive. (5) **No web changes**: `useOfflineSync.ts` + IndexedDB shadow stay untouched — el response shape de las procedures existentes sigue backward-compatible. ENG-065 Operations Center surface es el primer consumer del nuevo `peekOutbox`. **ENG-064b Shipped 2026-05-05** — see row below; cierra la cutover, dropea `sync_queue`, y elimina la colisión de nombre con el cliente IndexedDB. | Tests cover ordering, retry, duplicate suppression, and manual conflict paths for high-risk entities. | **High** |
| `ENG-064b` | Sync writer cutover + drop `sync_queue` | Shipped | Cierra la reescritura writer + cutover de procedures que ENG-064 dejó explicitamente parqueada: rutea cada `db.insert(syncQueue).values(...)` por `enqueueSync(ctx, args)`, corta las 8 procedures legacy `sync.*` (`status`, `listQueue`, `addToQueue`, `removeFromQueue`, `push`, `pull`, `resolve`, `listConflicts`) a `sync_outbox`, dropea `sync_queue`, y renombra el cliente web `services/storage/syncQueue.ts` → `offlineQueue.ts` para eliminar la colisión visual con la tabla server. **Shipped**: writer rewrite landed across 19 server router files (`categories`, `companies`, `customerCatalogs`, `customers`, `geography`, `inventory`, `locations`, `logos`, `orders`, `products`, `providers`, `purchases`, `sales`, `sequentials`, `sites`, `units`, `users`, `vatRates`, plus `sync.addToQueue` thin shim) + 4 application services (`completeSale`, `discardDraft`, `returnSale`, `voidSale`) + 1 dev seed (`seed-mega/historical-sync.ts` writes `sync_outbox` directly without envelope context for bulk performance). Para los writers tx-scoped, los inserts pasan POST-tx como `await enqueueSync(ctx, ...)` para preservar el callback síncrono de better-sqlite3 — la atomicidad de las entity rows se conserva; el sync_outbox enqueue corre post-commit y es idempotente vía el partial unique index si la misma envelope retry. El journal effect `sync_queue_emit` se retiró: `enqueueSync` escribe su propio efecto `outbox_enqueue:sync` vía `recordEffect` cuando hay envelope, así que los `effects.push({kind:'sync_queue_emit'...})` post-tx en los 4 sale lifecycle services se borraron. `routers/sync.ts` cutover: `sync.status` aggregates flip a `sync_outbox` con `status IN ('queued','submitting','retrying')` para pending count, `status='retrying'` para retry count, y `status IN ('retrying','dead_letter') AND lastError IS NOT NULL` para failed count; `sync.listQueue` + `sync.pull` aliasean `payload→data` y `payloadVersion→localVersion` en la projection para que `useOfflineSync.ts` siga consumiendo la misma shape; `sync.addToQueue` se vuelve thin shim de `enqueueSync`; `sync.removeFromQueue` borra de `sync_outbox`; `sync.push` reclama rows `status IN ('queued','retrying')` ordenado por `(priority DESC, createdAt ASC)`, transitions a `synced` en éxito y a `retrying` (con `lastError={kind:'UNKNOWN', message}`) en fallo — el helper legacy `incrementQueueFailure` se renombró a `markOutboxFailure`; `sync.resolve` UPDATEa `sync_outbox` a estado limpio y re-enqueue vía `enqueueSync` en vez de inline insert; `sync.listConflicts` queda untouched (la tabla `sync_conflicts` es su propia tabla). Migración `0017_drop_sync_queue.sql` dropea la tabla legacy con `DROP TABLE IF EXISTS sync_queue` (data ya backfilled a `sync_outbox` por el `0016` de ENG-064). `schema.ts` remueve `syncQueue` table + relations + types `SyncQueueItem`/`NewSyncQueueItem`; el JSDoc del outbox circundante se reescribe en past tense capturando el cutover. Web rename `apps/web/src/services/storage/syncQueue.ts` → `offlineQueue.ts` renombra archivo + clarifica JSDoc que esto es el buffer offline IndexedDB (independiente del server-side sync_outbox); el nombre interno del IndexedDB ObjectStore (`STORE_NAMES.SYNC_QUEUE`) se preserva para evitar version bump del DB cliente y migración de datos en devices existentes; imports actualizados en `StorageProvider.tsx`, `offlineStorage.ts`, `services/storage/index.ts`. Tests: 5 fixture inserts en `sync.test.ts` reshape de `data/localVersion` a `payload/payloadVersion/status/conflictPolicy`, `from(syncQueue)` flippea a `from(syncOutbox)` en todos los reads, `incrementQueueFailure`-driven assertions ahora esperan JSON `lastError` shape; `orders.test.ts` (3 sites), `purchases.test.ts` (3 sites), `application-sales-completeSale.test.ts` (4 sites), `application-sales-returnSale.test.ts` (kinds + test name), `application-sales-voidSale.test.ts` (kinds), `application-sales-discardDraft.test.ts` (kinds) — todos flippean refs `syncQueue` → `syncOutbox` y journal kind `sync_queue_emit` → `outbox_enqueue:sync`. `seed-mega/types.ts` + `seed-mega/index.ts` renombran `syncQueueRows` → `syncOutboxRows` y `syncQueuePending` → `syncOutboxPending`. `seed-dev.ts` clearTables list flippea `sync_queue` → `sync_outbox`. JSDoc cleanup atravesando `enqueue.ts` + `journal.ts` + `schema.ts` refleja el estado post-cutover. Counter Foundation Reset sube de 13/21 a 14/21. No quedan referencias server-side activas a `syncQueue` / `sync_queue`; los literales restantes son migraciones históricas, narrativa ADR, tests de migración/compatibilidad y el ObjectStore IndexedDB preservado del cliente. | Every writer routes through `enqueueSync`; `sync_queue` table is gone; `sync.*` procedures all read/write `sync_outbox`; web client file no longer collides on name. | **High** |
| `ENG-065` | Operations Center | Partial | Expand the sync center into an operations center covering Sync Health, Fiscal Health, Cash Reconciliation, Payment Reconciliation, Inventory Reconciliation, Device Health, and diagnostic export. **First slice (ENG-065a) Shipped 2026-05-05** — see row below. **Second slice (ENG-065b) Shipped 2026-05-06** — Cash + Inventory reconciliation panels Shipped, Payment carved out to ENG-065d. **Third slice (ENG-065c) Shipped 2026-05-06** — Diagnostic Export Shipped (3 outboxes today; payment + webhook outboxes append additively when their export integrations ship). **Payment baseline (ENG-038 slice 1) Shipped 2026-05-11** — `/operations?tab=payments` now reads `payment_outbox` and surfaces rail/mismatch/outbox health. **Remaining**: ENG-065d retry/requeue actions and per-method/provider breakdown after live payment workers exist. | Operator can see what is pending, what failed, and which action to take without reading logs. | **High** |
| `ENG-065a` | Operations Center: Sync + Fiscal + Device Health | Shipped | First slice of ENG-065. Ship the Operations Center page scaffolding plus the three health surfaces whose data sources are already on the wire (sync.peekOutbox, reports.fiscal.list, peripherals.peekHardwareOutbox). Cash + payment + inventory reconciliation defer to ENG-065b; diagnostic export defers to ENG-065c. **Shipped**: nuevo route `/operations` registrado en `App.tsx` con `managerOrAdminRoles` (managers ven la página, las acciones de retry quedan admin-only). Nueva página `apps/web/src/features/operations/OperationsPage.tsx` con segmented-control tabs (`?tab=sync|fiscal|device`, default `sync`, `replace: true` en URL) mirror del patrón de `CompanyPage`. Tres paneles nuevos: `SyncHealthPanel.tsx` reusa `<CompanySyncCard />` directamente (single source of truth — el mount existente en `/company?tab=data` se preserva por backward-compat con bookmarks operator); `FiscalHealthPanel.tsx` consume `trpc.reports.fiscal.list` con filtro de status (`contingency` por default, `rejected`, `accepted`), per-row badge `<FiscalStatusBadge />`, botón Reintentar admin-only que dispara `trpc.reports.fiscal.retryDocument`; `DeviceHealthPanel.tsx` aggregate `peripherals.list` cross-site agrupado por kind con last-test result + timestamp + variant Badge, plus hardware outbox tail via `peripherals.peekHardwareOutbox` con filtro default "problemas-only" (`failed/retrying/dead_letter`) + toggle "ver todo", botón Reintentar admin-only que dispara la nueva `peripherals.retryHardwareOutbox`. Server-side: nueva procedure `peripherals.retryHardwareOutbox` (admin) en `routers/peripherals.ts` mirror estructural de `sync.retry` y `reports.fiscal.retryDocument` — re-arma rows en status `retrying`/`dead_letter`/`failed` reseteando attempts=0/lastError=null/claimToken=null/lockedAt=null/nextRetryAt=null/status='queued'; `queued`/`submitting`/`printed` son no-op para no replayear rows drenados; surfaces `HARDWARE_OUTBOX_NOT_FOUND` cuando el row id es foreign al tenant; sin fire-and-forget tick (next periodic worker drain en 30s). Nuevo `retryHardwareOutboxInput` schema en `peripherals.ts`. Nuevo error code `HARDWARE_OUTBOX_NOT_FOUND` en `lib/errorCodes.ts`. **Coupled change**: `reports.fiscal.list` widened de `adminProcedure` → `managerOrAdminProcedure` (read-only fiscal status visibility por managers); el `retryDocument` mutation stays admin. Sidebar entry "Operations" / "Operaciones" agregada a `sections.overview` después de Inventory con `Activity` icon (managerOrAdmin gated). Nuevo i18n namespace `operations` registrado en `apps/web/src/i18n/index.ts` con keys `header.{title,subtitle}` + `tabs.{sync,fiscal,device,ariaLabel}` + `common.{loading,errorGeneric}` + `fiscal.{title,description,statusFilter,columns,retry,emptyState}` + `device.{peripherals,outbox,retry}` (en + es, neutral LATAM tú, sin voseo). Nav keys `items.operations` (en + es). Tests nuevos: `peripherals-retry-outbox.test.ts` (9 cases — admin gate cashier/manager FORBIDDEN, NOT_FOUND foreign id, retrying/dead_letter/failed reset, no-op queued/submitting/printed, cross-tenant isolation), `fiscal-reports.test.ts` extendido con manager-allowed assertion. Web: `OperationsPage.test.tsx` (7 cases — 3 tabs render, default sync, deep-link fiscal/device, garbage tab fallback, click switching, header copy), `FiscalHealthPanel.test.tsx` (4 cases — empty state, rows render, retry mutation, manager disabled), `DeviceHealthPanel.test.tsx` (5 cases — peripherals grouped, problems-only default, show-all toggle, admin retry, manager disabled). Counter Foundation Reset 14/21 → 15/21. Decisiones de scope: (1) **CompanySyncCard dual-mount** — preservada en `/company?tab=data` para backward-compat; un follow-up `[ux] retire CompanySyncCard from /company once /operations is established` retira el duplicate cuando los operadores migren su workflow. (2) **Sin nuevas tablas / migrations** — toda la data ya existe en sync_outbox/fiscal_documents/hardware_outbox/site_peripherals. (3) **No fire-and-forget tick en retry** — operator-driven retry; el periodic worker (30s) drena la row requeued sin riesgo de re-fail inmediato cuando el error transient aún no resolvió. (4) **Reconciliation panels parqueados a ENG-065b** — Cash necesita `cashSessions.pendingChecks` enriquecido + nueva query de discrepancia (expected vs actual cash); Payment necesita un `payment_outbox` que aún no shippea (gated por ENG-063); Inventory necesita reconciliation queries entre `inventory_balances` y stock movements. (5) **Diagnostic Export parqueado a ENG-065c** — el operator pide JSON download para tickets de soporte; necesita server-side bundle endpoint + zipped client download flow. | Operator can see what is pending, what failed, and which action to take without reading logs. | **High** |
| `ENG-065b` | Operations Center: Cash + Inventory Reconciliation | Shipped | Second slice of ENG-065. Add Cash + Inventory reconciliation tabs to the existing `/operations` page using only tRPC procedures — no migrations, no new tables. Payment reconciliation explicitly carved out to `ENG-065d` (gated on ENG-063 `payment_outbox`). **Shipped**: dos procedures nuevas bajo `reports.*` (managerOrAdmin) — `reports.cash.reconciliation` (input `{limit?}`) corre tenant-wide drop del per-(siteId, cashier) filter del legacy `cashSessions.report`; agrega open sessions sin cutoff + closed sessions en ventana 30 días devolviendo `summary.{openSessionCount, closedRecentCount, reviewCount, netOverShort, largestDiscrepancy, windowDays}`, `bySite.[]` (per-site openSessions/closedSessions/netOverShort/overShortCount con epsilon `0.009`), y `recentDiscrepancies.[]` (top |overShort| closures, capped por limit) — y `reports.inventory.discrepancies` (input `{limit?}`) hace single GROUP BY contra `inventory_balances` + LEFT JOIN a products devolviendo `summary.{productsScanned, discrepancyCount, deltaEpsilon}` + `rows.[]` (productId/Name/Sku, cachedStock vs sumOfBalances, signed delta, siteCount) flag rows donde `|cachedStock - Σ(on_hand)| > 0.001`. La discrepancia es cache-vs-cache (`products.stock` vs `Σ(inventory_balances)`) que es exactamente la drift que `inventory.reconcileBalances` ya healing — NO es Σ-de-movements porque `inventory_movements` no tiene `site_id`. Web: `OperationsPage.tsx` extiende `TAB_KEYS` a `['sync','fiscal','device','cash','inventory']` con tabs nuevas en orden. `CashHealthPanel.tsx` consume `reports.cash.reconciliation` y renderiza 4 summary tiles + bySite table + recentDiscrepancies table; sin retry mutation porque cash reconciliation es físico. `InventoryHealthPanel.tsx` consume `reports.inventory.discrepancies` y renderiza 2 summary tiles + drift table con sign-coded delta badge (`success` si dentro de epsilon, `warning` si positivo, `danger` si negativo); admin-only "Reconciliar balances" button dispara la existing `inventory.reconcileBalances` mutation con toast de count reconciliado, manager ve disabled con tooltip translated. i18n namespace `operations` extendido con `tabs.{cash,inventory}` + subtree `cash.{title,description,summary,bySite,recentDiscrepancies}` + subtree `inventory.{title,description,summary,emptyState,columns,reconcile}` (en + es, neutral LATAM tú, sin voseo). Tests: 7 server cases en `reports-cash-reconciliation.test.ts` (empty, role gate cashier FORBIDDEN, multi-site open agg, mixed signed overShort summed correctamente con largest abs y review count, bySite groups, ordering por |overShort| desc + 30-day window exclusion del fixture older-than-cutoff, cross-tenant isolation, limit clamp), 6 server cases en `reports-inventory-discrepancies.test.ts` (empty, role gate, ok product not surfaced, drift positive/negative ordered por |delta|, cross-tenant, limit clamps con discrepancyCount preserved). 5 web cases en `CashHealthPanel.test.tsx` (loading, error, summary tiles, bySite + recentDiscrepancies tables, both empty states), 5 web cases en `InventoryHealthPanel.test.tsx` (loading, empty, drift rows con +/- delta sign, admin reconcile fires mutation, manager disabled), 2 cases nuevas en `OperationsPage.test.tsx` (deep-link `?tab=cash`, deep-link `?tab=inventory`) + el test "5 tabs" + garbage fallback con value que ya no es válido. Server 1158 tests verde, web 813 tests verde, coverage thresholds enforcados. Counter Foundation Reset 15/21 → 16/21. Decisiones de scope: (1) **Payment carved out a ENG-065d (gated)** — `payment_outbox` aún no existe y depende de ENG-063 (Payment terminal adapter). El operator no ve placeholder UI; el row gated abajo documenta la dependencia. (2) **Sin migraciones / tablas nuevas** — toda la data fue inferible del schema actual (cash_sessions + inventory_balances + products). (3) **`inventory_movements.site_id` ausente** — la discrepancia se computa cache-vs-cache (`products.stock` vs `Σ(inventory_balances.on_hand)`) que matchea exactamente lo que `reconcileBalances` heals; cualquier otra framing requeriría migración. (4) **No retry mutation en CashHealthPanel** — recount de caja es física; el panel surfacing información, no muta estado. (5) **`inventory.reconcileBalances` queda admin-only** — manager ve la página Inventory pero el botón está disabled con tooltip translated. | Manager + admin abre `/operations?tab=cash` y ve open sessions + closed-window net overShort + per-site discrepancies sin filtros. Admin abre `/operations?tab=inventory` y ve productos en drift; click "Reconciliar balances" healing la cache + dispara invalidate del query, drift desaparece después del refresh. Cross-tenant isolation enforzado en ambas procedures. | **High** |
| `ENG-065c` | Operations Center: Diagnostic Export | Shipped | Third slice de ENG-065. Admin-only bulk export para support tickets — bundle JSON con `operation_events` + `operation_effects` + 3 outboxes existentes (sync, fiscal, hardware) filtrado por rango de fechas, empaquetado vía `jszip` en el cliente y descargado como `puntovivo-diagnostic-<tenantSlug>-<timestamp>.zip`. **Shipped**: dos procedures nuevas bajo `reports.diagnostics.*` (`adminProcedure`) — `preview({fromDate, toDate})` retorna `{counts, estimatedSizeBytes, rowLimit, willHitLimit, schemaVersion}` con counts por las 5 fuentes (3 reales + payment_outbox=0 + webhook_outbox=0 reservados para ADR-0003); `export({fromDate, toDate, includeOutboxes?})` retorna `{manifest, tables}` con manifest carrying schemaVersion=1 + tenantId + range + counts (los 5 nombres) + warnings (`rowLimitHit:<table>` cuando una fuente excede el cap de 10000) + includedOutboxes; tables carrying las filas crudas con `payload` + `lastError` JSON parseado por Drizzle. `fromDate <= toDate` enforzado a nivel Zod (refine) → rejecta swapped pairs como BAD_REQUEST. Hard cap 10000 por tabla mantiene el bundle bajo ~10MB serialized; warning surfaces si cualquier fuente lo alcanza para que el operador estreche el rango. Web: 6º tab `Diagnóstico` en `/operations` (`?tab=diagnostics`); `DiagnosticExportPanel.tsx` consume las dos procedures con `enabled: false` (operator-driven refetch); date range con presets last7d/last30d/custom + UTC-day rounding; "Vista previa" pinta 5 tiles + estimated size + warning banner cuando willHitLimit; checkboxes sync/fiscal/hardware (default todos); botón "Descargar ZIP" admin-only construye `manifest.json` + 5 `<table>.json` files via `jszip` (~30KB gzipped) y dispara el download via Blob + URL.createObjectURL → anchor.click → revokeObjectURL deferred (mirror del CSV pattern existente). Manager y cashier ven el tab pero los botones Vista previa y Descargar ZIP están disabled con tooltip translated. i18n namespace `operations.diagnostics.{title, description, range, preview, includeOutboxes, export}` agregado en + es neutral LATAM tú. Tests: 9 server cases en `reports-diagnostics.test.ts` (empty tenant, role gate cashier+manager FORBIDDEN, date range narrowing con fixtures inside/outside window, export full bundle, `includeOutboxes:['sync']` populate-only-sync con counts honest, `includeOutboxes:[]` journal-only, cross-tenant isolation, `fromDate>toDate` BAD_REQUEST, offset-aware chronological range). 8 web cases en `DiagnosticExportPanel.test.tsx` (default last7d preset, preview refetch + tile rendering, willHitLimit banner, admin Descargar ZIP triggers refetch + URL.createObjectURL spy, manager preview+export disabled con tooltip, all-outboxes-unchecked export enabled, fromDate>toDate + empty date disable Vista previa con inline error). 1 case nuevo en `OperationsPage.test.tsx` (?tab=diagnostics deep-link). Server 1170 tests verde, web 825 tests verde, coverage thresholds enforcados. Counter Foundation Reset 16/21 → 17/21. Decisiones de scope: (1) **5-name keyset locked en manifest.counts** — `payment_outbox=0` y `webhook_outbox=0` siempre presentes en el manifest aunque las tablas no existan (gated por ENG-063 + ENG-070). Cuando shippean, se agregan a `tables.*` y `schemaVersion` bumpa a 2 — additive, no breaking. (2) **Hard cap 10000 por tabla** — narrow-range hint via warning, no truncación silenciosa. Configurable en follow-up si los operadores lo piden. (3) **Date filter en `created_at` only** — operation_events tiene started_at/completed_at adicionales pero created_at matchea las outboxes; si los operadores piden por-timestamp filtering eso es un knob futuro. (4) **No PII redaction** — admin-only + tenant-scoped, surface paralelo a los datos que el admin ya ve en sus tabs UI existentes (Sales, Customers, Fiscal Documents). (5) **No new error codes** — Zod BAD_REQUEST suficiente para invalid input. (6) **`jszip` agregado vía npm install** — ESM, MIT, ~30KB gzipped, sin transitive deps; bundle size delta absorbed por el chunk del Operations Center. | Admin abre `/operations?tab=diagnostics`, escoge rango de fechas + outboxes, click Vista previa → ve counts. Click Descargar ZIP → archivo descarga; unzip muestra manifest.json + 5 JSON files. Manager ve el tab pero los botones de diagnóstico están disabled con tooltip. Cross-tenant isolation enforzado. | **High** |
| `ENG-065d` | Operations Center: Payment Reconciliation | Partial | Third slice carved out from ENG-065b at planning time because the data layer dependency (`payment_outbox`) did not exist yet. ENG-038 slice 1 now provides the table, deterministic rail contract, read-only reconciliation query and `/operations?tab=payments` panel. **Remaining**: admin retry/requeue actions and per-method/provider settlement breakdown once real provider workers populate `payment_outbox` from live statements. | Operator can see pending settlements, failed captures, and per-method breakdown for the recent payment window without reading logs. | **High** |
| `ENG-066` | Backup, restore, and local security | Shipped | Threat-model local POS data; implement backup/restore expectations around outbox and device identity; decide key storage per OS; document no PAN/CVV storage and token/reference-only payment data. **Shipped 2026-05-07**: tres ejes de hardening + ADR-0006. (1) **Backup atómico**: nuevo `apps/desktop/src/main/backup/backup-bundle.ts` con `createBackupBundle()` que usa `better-sqlite3.backup()` (online backup API de SQLite) para snapshot atómico mientras el server corre, integrity-check via `PRAGMA integrity_check` post-snapshot, y bundle ZIP que viaja con `local.db` + `device-id.txt` + `manifest.json` (jszip nuevo dep en `apps/desktop`). El handler `handleCreateDatabaseBackup` cutover al nuevo helper; filename ahora es `puntovivo-backup-<tenantSlug?>-<ISO8601>.zip`. (2) **Restore validado**: `extractBackupBundle()` detecta formato por magic bytes (ZIP `50 4b 03 04` vs SQLite `53 51 4c 69`), extrae a staging dir, corre `assertSqliteIntegrity()` ANTES del swap. Format-unrecognized + corruption → rechazo limpio sin tocar el live. ZIP bundles preservan device-id del backup; legacy `.db` bundles preservan destination identity. Backwards-compat con backups raw `.db` pre-ENG-066. (3) **Sanitization del diagnostic export** (ENG-065c hardening): `services/diagnostics/sanitize.ts::sanitizePayload()` walker recursivo con denylist anchored (`password`, `token`, `jwt`, `apiKey`, `clientSecret`, `pan`, `cvv`, `cardnumber`, `certificate`, `oauthtoken`, `authorizationcode` y ~25 más, ver `SENSITIVE_KEYS`). Aplicado a cada JSON-shaped column de `operation_events.summary`, `operation_effects.effectData`, `sync_outbox.payload`, `fiscal_outbox.payload`, `hardware_outbox.payload`. Manifest gana `sanitized: true` + `redactedKeysByTable` para auditabilidad del bundle. Matching anchored evita false-positives sobre keys benignas (`pancake_count`, `panel_layout`). Idempotente: re-aplicar sobre output ya saneado es no-op. (4) **Architectural lint anti-PAN/CVV**: nueva case en `__tests__/architectural-lint.test.ts` que escanea `db/schema.ts` por declaraciones `text(...)/integer(...)` con nombre matching `pan|cvv|cvc|card_number|cardholder_name|primary_account_number`. Bloquea cualquier futuro ticket que filtre una columna PAN al schema. (5) **ADR-0006** (`docs/architecture/0006-local-data-security.md`) documenta threat model + decisiones de scope: encryption-at-rest deferred, allowlist-vs-denylist tradeoff resuelto a denylist, per-OS keychain integration deferred a ENG-063 (`@node-gyp-build/keytar` candidate identificado), audit-log-on-restore deferred a ENG-066b. 15 server unit tests + 4 integration tests + 15 desktop `node --test` tests + 2 lint cases nuevos. ci:server / ci:web / ci:desktop verde. Counter Foundation Reset 19/21 → 20/21. | Backup/restore preserves sales, fiscal docs, outboxes, audit history, and device identity; logs/exported diagnostics avoid secrets and card data. | **High** |
| `ENG-067` | Chaos and resilience suite | Shipped | Add automated scenarios for app restart after sale, large queues, duplicate retry, provider outage, intermittent network, sync conflict, and close-session with pending work. **Shipped 2026-05-06**: 5 new chaos test files (4 server + 1 web) cubren los 5 scenarios sin coverage previo. (1) `chaos-app-restart-after-sale.test.ts` (4 cases) — bouncea fiscal/hardware workers vía `stop()`+`start()` para forzar el `sweepStaleClaims()` startup; asserta que rows en `submitting` con `lockedAt > 5min` se reclaman a `queued` con `claimToken=null`/`lockedAt=null`, y que fresh claims (lockedAt < 5min) quedan intactos; documenta el sync worker daemon gap (no existe hoy per ENG-064 close-out — el sweep SQL se ejecuta manualmente para pinear el pattern). (2) `chaos-large-queues.test.ts` (3 cases) — bulk-insert 10000 rows en sync_outbox split entre dos tenants con priorities 0..9 cíclicas y createdAt monotónico; asserta tenant split correcto, `sync.peekOutbox` clamps al limit y queda scoped por tenant (verificado vía entityId parity), single indexed read on 10k depth bajo 200ms ceiling. (3) `chaos-duplicate-retry.test.ts` (3 cases) — pina sync_outbox cross-tenant safety (mismo idempotencyKey en tenant A vs B → DOS rows, no UNO), documenta el gap de hardware_outbox idempotency a nivel schema (cerrado por `ENG-067b` Shipped 2026-05-06 — la assertion ya flippeó a `toHaveLength(1)`), confirma cross-tenant isolation en hardware_outbox. (4) `chaos-provider-outage-recovery.test.ts` (3 cases) — `StatefulStubAdapter` con call-count tracking; "fail 3 times PROVIDER_5XX, succeed on attempt 4" → final outbox status='accepted' con attempts=3, fiscal_documents.status='accepted'; non-recoverable PROVIDER_REJECTED → dead_letter en attempt 1, segundo tick es no-op terminal; tick on empty queue retorna `processed: false`. (5) `useOfflineSync.chaos.test.tsx` (4 cases) — mocks `vanillaClient.sync.{status,push}`, `Object.defineProperty(navigator, 'onLine')` + dispatch de `online`/`offline` events; asserta el hook reflecting state, NO auto-sync mientras offline, auto-sync exactamente una vez en offline → online transition con pendingItems > 0, buffer preservation cuando push rechaza + manual retry drena. Los 2 scenarios ya cubiertos se citan: sync-conflict en `sync.test.ts > sync.resolve`, close-with-pending en `application-cashSessions-closeCashSession.test.ts`. 17 nuevos test cases chaos total. **Review close-out**: el pre-commit review corrigió la inversión de prioridad del kernel común (`createOutboxKernel` ahora drena `priority DESC, createdAt ASC`) y agregó 1 test directo en `outbox-kernel.test.ts`, para 18 tests nuevos efectivos en el cierre. Counter Foundation Reset 17/21 → 18/21. Decisiones de scope: (1) **Hardware idempotency gap documentado, fixed inmediatamente en `ENG-067b`** — la assertion ya quedó en `toHaveLength(1)` y la entrada `enqueueHardware()` reemplazó los inserts inline en `routers/peripherals.ts`. (2) **Sync worker daemon gap documentado** — el sweep SQL se ejecuta manualmente; cuando el daemon shippea, el test cambia a `server.syncWorker.stop/start`. (3) **Performance ceilings generosos** son cliff detectors no benchmarks. (4) **No live UI smoke** — la suite ES el smoke. | Suite runs in CI where possible and local-only for Electron/hardware cases; failures point to a concrete recovery gap. | **High** |
| `ENG-067b` | `hardware_outbox` envelope-keyed idempotency | Shipped | Cierra el gap estructural que la suite chaos de ENG-067 documentó: dos enqueues del mismo comando lógico (un retry tRPC tras un network blip, un replay desde el offline buffer, un worker reboot vía stale-claim sweep) producían DOS rows en `hardware_outbox` y el worker dispatchea ambos — el recibo se imprime dos veces o el cajón se patea dos veces. **Shipped 2026-05-06**: nueva migración `0018_hardware_outbox_idempotency.sql` agrega columna nullable `idempotency_key` + partial unique idx `(tenant_id, kind, idempotency_key) WHERE idempotency_key IS NOT NULL` (mirror del shape ENG-064 sync_outbox). Nuevo helper `services/peripherals/enqueue-hardware.ts::enqueueHardware(ctx, args)` que captura el `UNIQUE constraint failed.*hardware_outbox` y retorna `{id, deduped: true}` mirror estructural de `enqueueSync()`. Los 2 sitios inline en `routers/peripherals.ts` (fallback de `printReceipt` + de `kickCashDrawer`) cutover al helper. Input schemas extendidas con optional `idempotencyKey` + preprocess que acepta `''` como omitida — opt-in via input, NO upgrade a `criticalCommandProcedure` (las procedures siguen como `tenantProcedure`/`managerOrAdminProcedure`; el envelope-middleware path se promueve en un futuro ticket cuando el cliente UI envíe el header). Empty-string keys se normalizan a omitida en el schema publico y a null en el helper para no chocar con la partial idx. 8 cases nuevos en `enqueue-hardware.test.ts` (omit key → fresh row, first call con key → fresh, second con misma key → deduped same id, cross-tenant independencia, cross-kind independencia, empty-string normalize, FK violation rethrow no swallowed, public schema empty-string normalize). El chaos test `chaos-duplicate-retry.test.ts` flippeó: el subsuite "hardware_outbox has NO schema-level idempotency today" ahora se llama "hardware_outbox idempotent retry collapses to one row (ENG-067b)", la assertion pasó de `toHaveLength(2)` → `toHaveLength(1)` + `expect(r2.deduped).toBe(true)`, y el cross-tenant case usa `enqueueHardware` en lugar de `db.insert` directo. ADR-0003 footer agregó nota de paridad con sync_outbox idempotency. Counter Foundation Reset 18/21 → 19/21. Decisiones de scope: (1) **Sin upgrade a `criticalCommandProcedure`** — opt-in via input mantiene las procedures backward-compatible y la primitiva queda lista para que cualquier futuro caller (offline-print queue, ENG-066 backup/restore replay) opte. (2) **Sin web client wave** — el cliente UI no genera la idempotencyKey aún; el "user clicks twice → two prints" sigue siendo el comportamiento por default cuando no hay key. (3) **Empty-string normalize** evita un foot-gun: callers que pasan `''` desde un form vacío no rompen la dedup ni stackean rows. | Same-envelope retry to `hardware_outbox` collapses to one row; legacy null-key path stays independent. | **High** |
| `ENG-068` | Module activation kernel | Shipped | Implement `tenant_modules`, server and web registries, route gating, procedure gating, and soft-disable semantics as the foundation for vertical packs. **Shipped 2026-05-07**: kernel + 5 demo modules cierra la wave Foundation Reset. Manifest `services/modules/manifest.ts` es la fuente de verdad — `MODULE_IDS` tuple + `MODULES_MANIFEST` record con exhaustiveness `Record<ModuleId, ...>` que bloquea typos en compile time. Estado per-tenant vive en `tenants.settings.modules` (sin migración nueva, usa el blob JSON existente). Server middleware `createModuleGuard(moduleId)` retorna FORBIDDEN con `errorCode: 'MODULE_NOT_ACTIVATED'` + `details.moduleId`; pre-composed `adminProcedureWithModule` / `managerOrAdminProcedureWithModule` en `trpc/middleware/modules.ts` evitan tax para procedures sin gate. tRPC namespace nuevo `modules.*` con tres procedures: `list` (managerOrAdmin) join manifest+state+isExplicit flag, `getEffective` (tenantProcedure) full effective map para el renderer, `setActive` (criticalCommandAdminProcedure) que escribe `tenants.settings.modules[id]` via `json_set(COALESCE(settings, '{}'), '$.modules.<id>', json('true'|'false'))` atómico + audit log row `action='module.toggle'` resourceType `'tenant_module'` con before/after snapshot. Cinco demo modules wired end-to-end: `copilot` gates `/co-pilot` route + `ai.copilot.chat` procedure, `operations-center` gates `/operations` route + `reports.diagnostics.{preview,export}`, `quotations` gates `/quotations` route + cinco procedures, `anomaly-detection` gates dashboard tile + `ai.anomalies.{list,snooze}`, `semantic-search` gates sparkles toggle on Products + `products.{semanticSearch,regenerateEmbeddings,suggestCategory}`. Default state `enabled: true` para los 5 → cero cambio de behavior visible para tenants sin toggle explícito. Renderer kernel en `apps/web/src/features/modules/`: `ModulesProvider` mounted bajo `TenantProvider`, expone `useIsModuleActive(moduleId)` + `useModulesSnapshot()`; `RequireModule` wrapper por route + sidebar item; `<ShellRoute allowedModule="..." />` redirect a `/dashboard` cuando off; sidebar `requiredModule` filtra del array antes de render. Admin tab nuevo `/company?tab=modules` con `CompanyModulesCard` que lista los 5 toggles con label + description + default-vs-explicit indicator; click flip dispara `modules.setActive` (critical mutation con device-id + envelope), invalida `modules.list` + `modules.getEffective` en el mismo tick. Error codes nuevos `MODULE_NOT_ACTIVATED` + `MODULE_UNKNOWN` en `lib/errorCodes.ts` + mirror en `translateServerError.ts` con copy traducida en + es neutral LATAM tú. i18n namespace nuevo `modules.json` con label + description per módulo en ambos locales; `settings.company.tabs.modules` extiende el segmented control existente. Audit log enums extendidos en `db/schema.ts` (`module.toggle` action, `tenant_module` resourceType) + mirror en `apps/web/src/types/index.ts`. Dev seed inicializa `settings.modules` con los 5 ON. ADR-0007 documenta decisión de JSON-vs-tabla, scope deferral de fiscal flag consolidation a ENG-068b, soft-disable semantics, optimistic-with-redirect tradeoff. Counter Foundation Reset 20/21 → **21/21 — wave complete**. | Tenant A can activate a module without Tenant B seeing its routes or procedures; deactivation hides behavior without deleting rows. | **Medium** |
| `ENG-069` | Multi-surface POS shell | Shipped | Formalize POS Desktop, POS Touch, KDS, Customer Display, and Mobile Waiter as configured surfaces over the same bundle without building the full vertical workflows yet. **Shipped 2026-05-07**: surfaces-as-modules pattern lifted on top of the ENG-068 kernel. New `services/surfaces/manifest.ts` declares `SURFACE_IDS` tuple + `SURFACES_MANIFEST` record with exhaustiveness `Record<SurfaceId, ...>`; POS Desktop is the implicit default (moduleId=null) and the 4 new surfaces gate behind dedicated module ids. Modules manifest extended with `pos-touch` / `kds` / `customer-display` / `mobile-waiter` ids — all defaultEnabled false so existing tenants see no new sidebar entries on the kernel ship. New tRPC procedure `surfaces.list` (managerOrAdmin) returns the manifest joined with the resolved module state. Renderer mirror at `apps/web/src/features/surfaces/manifest.ts` plus 4 layout shell components (`TouchShell`, `KdsShell`, `CustomerDisplayShell`, `MobileWaiterShell`) — each composes `<ProtectedRoute allowedRoles={salesRoles}>` + `<RequireModule id="..." fallback={<Navigate to="/dashboard" />}>` + `<Suspense>` around `<Outlet />`. 4 placeholder pages (`*HomePlaceholder`) reuse a shared `SurfacePlaceholder` component that renders the surface label + description + "Llega con ENG-039" badge + "Volver al panel" CTA. `App.tsx` registers the 4 new top-level routes OUTSIDE `MainLayout` so each shell owns its full viewport (KDS fullscreen black backdrop, customer-display gradient, mobile-waiter phone-width container, POS Touch wider chrome). Sidebar gets a new "Superficies" / "Surfaces" section gated by the 4 new modules — when all 4 are off the section title hides via the existing `visibleItems.length === 0` filter. New i18n namespace `surfaces.json` in en + es neutral LATAM tú with per-surface label + description + upcoming-ticket badge + shared `placeholder.dashboardCta`; `nav.json` extended with the 4 new entries + section title; `modules.json` extended with the 4 surface module entries. ADR-0007 surfaces subsection appended documenting the surfaces-as-modules pattern. ~24 new tests (server: surfaces-manifest exhaustiveness + surfaces-router list + flip + cross-tenant + roles, plus modules-manifest count update; web: surfaces manifest mirror + 4 shell module-gate tests + 4 placeholder render tests + ModulesContext + CompanyModulesCard fixture updates). Counter Foundation Reset stays at 21/21 (this is expansion-architecture, post-wave). | Each surface has permissions, route config, and layout shell without forking the app. | **Medium** |
| `ENG-070` | Event-based public API + webhook foundation | Shipped | Design public events from the operation journal/outbox (`sale.completed`, `sale.refunded`, `inventory.adjusted`, `cash_session.closed`, `fiscal_document.accepted`) for the future central server. No Kafka directly from POS devices. **Shipped 2026-05-08**: nueva migración `0020_webhook_outbox.sql` agrega la 5ª y última outbox (`webhook_outbox`) per ADR-0003 con kernel projection (status / attempts / next_retry_at / claim_token / locked_at) + partial unique idx `(tenant_id, event_type, idempotency_key) WHERE idempotency_key IS NOT NULL` espejo de ENG-067b para que un envelope replay colapse a una fila. Manifest `services/events/manifest.ts` cierra los 5 event types como `as const` con Zod payload schemas (`sale.completed/refunded`, `inventory.adjusted`, `cash_session.closed`, `fiscal_document.accepted`); `Record<PublicEventType, ZodSchema>` exhaustiveness + `PUBLIC_EVENTS_VERSION=1`. `buildPublicEventContract()` extrae per-field required flags para la response de `events.getContract`. Projector puro `services/events/projector.ts::projectOperationEvent(op)` lee `operationKind` + `summary` y devuelve un `PublicEvent` o null — mapeo: `sales.create`/`sales.completeDraft` → `sale.completed`, `sales.returnSale` → `sale.refunded`, `inventory.adjustStock` → `inventory.adjusted`, `cashSessions.close` → `cash_session.closed`. Helper `enqueueWebhook(tx, args)` sin throws + idempotency-collapse (mirror estructural de `enqueueSync` ENG-064 + `enqueueHardware` ENG-067b). Hook nuevo en `operation-journal/journal.ts::markOperationCompleted` que cuando status='succeeded' corre el projector + lee `tenants.settings.modules.events-api` (gate per-tenant) + enqueue best-effort sin bloquear el commit primario. `fiscal_document.accepted` se proyecta separado con `projectFiscalDocumentAccepted` desde el fiscal worker (path no command-driven). Helper nuevo `isModuleActiveInSettings(blob, moduleId)` añadido al manifest de módulos para hot-paths que solo necesitan un boolean. Module nuevo `events-api` en MODULES_MANIFEST (defaultEnabled false; admins flippean per-tenant). tRPC `events.{getContract, peekOutbox}` (managerOrAdmin) read-only — getContract devuelve manifest+fields, peekOutbox devuelve tail ordenado por priority desc + createdAt asc. i18n nuevo `eventsApi.{label, description}` en + es neutral LATAM tú; CompanyModulesCard renderiza el toggle automáticamente desde el manifest extendido (10 módulos total). Renderer manifest mirror extendido (`CLIENT_MODULE_IDS` + `CLIENT_MODULE_DEFAULTS`); fixture de `ModulesContext.test.tsx` re-pinned a 10 keys. ADR-0003 footer del 5º outbox flippea de "gated en ENG-070" a "shipped"; ADR-0007 nueva subsección "Public events (ENG-070)". HTTP delivery worker (POST a subscriber URLs + retry policy + signing + dead-letter) + el subscriber URL config UI quedan parqueados a ENG-070b — v1 ships ONLY el kernel (contract + projector + outbox + module gate), exactamente lo que el AC pide. ~50 nuevos tests server (events-manifest 21 cases, events-projector 14 cases, events-enqueue-webhook 7 cases, events-router 8 cases + 1 cleanup, events-journal-hook 5 cases, modules-manifest extension 5 cases). Counter §3b: ENG-068/069/070 expansion architecture trio cierra completo. | Future central server can publish integrations without reading internal POS tables arbitrarily. | **Medium** |
| `ENG-048` | Semantic search UI surface on ProductsPage | Shipped | Out-of-band ticket que conecta el backend semántico de ENG-033 a UI accionable. Pre-ENG-048 las tres procedures (`products.semanticSearch`, `products.regenerateEmbeddings`, `products.suggestCategory`) sólo eran llamables vía consola del browser o curl; ningún operador podía descubrirlas. **Shipped**: `ProductsPage.tsx` recibe una toolbar nueva sobre la tabla con (1) un toggle pill "Semántica" (ícono Sparkles) que cambia entre el filtro literal client-side de DataTable y la búsqueda semántica server-side, (2) un input dedicado con placeholder en español "Buscar por significado (p. ej. lácteo bajo en grasa)" que sólo aparece cuando el toggle está ON, (3) un botón admin-only "Regenerar embeddings" (con `RefreshCw` animado) que dispara `products.regenerateEmbeddings` y muestra toast localizado con conteo embedded. Debounce de 300ms en el input semántico evita un roundtrip + embed call por cada keystroke. Cuando el server responde `mode:'unavailable'` (AI off / proveedor sin embeddings) la UI surfacing un hint traducido y cae a búsqueda literal via `products.list` usando el mismo texto debounced. Una columna nueva "Coincidencia" / "Match" se agrega a la derecha del listado en modo semántico, mostrando un badge primario `${pct}%` por fila. La columna desaparece automáticamente cuando se vuelve a modo literal. i18n new keys bajo `products.semantic.*` y `products.table.{searchSemantic,match}` en + es (neutral LATAM tú; "Regenerar embeddings", "Buscando...", "Coincidencia semántica con IA"). El input de búsqueda interno de DataTable sigue funcionando idéntico cuando el toggle está OFF (cero regresión visual para usuarios que no abren el menú). Tipo helper `DisplayProduct = Product & { similarity?: number }` mantiene la columna optional sin cambiar el `Product` público para callers de listado. | Manager+ activa el toggle, escribe "leche descremada" → 300ms después la tabla muestra productos rankeados por similarity y la columna Coincidencia exhibe `92%`, `78%`, `65%`. Admin click "Regenerar embeddings" → toast `"Se generaron embeddings de N productos"`. Sin `OPENAI_API_KEY` → toast warning "no se puede regenerar". Sin AI activada → semantic search retorna `unavailable` y un hint de fallback aparece bajo el input mientras la tabla filtra literal por el mismo texto. Cashier no ve ni el toggle ni el botón regenerar (page.permissionNote ya redirige). | **Medium** |
| `ENG-047` | AI anomaly follow-ups (sidebar badge + audit_logs persistence + snooze) | Shipped | Out-of-band ticket que cierra los tres `[ai]` follow-ups que el review de ENG-032 había capturado en BACKLOG. **(1) Sidebar badge** — el ítem `Dashboard` del nav muestra un contador rojo cuando hay alertas Alta pendientes. La query reusa `ai.anomalies.list` con `staleTime: 5min` y solo se ejecuta para roles manager+ (la procedure es `managerOrAdminProcedure`); cashiers/viewers no la consultan. Sólo `severityCounts.high` activa el badge — las alertas Media siguen apareciendo en el tile pero no chasean al operador a través de pantallas. **(2) `audit_logs` persistence** — el orquestador `detectAnomalies` ahora escribe una fila `action='ai.anomaly.detected'` por cada alerta nueva, con metadata `{kind, severity, observed, baselineMean, baselineStdDev, distance, evidenceRef, detectedBy:'ai.anomaly.detector'}`. ID determinístico `anomaly:<kind>:<cashierId>:<YYYY-MM-DD>:<evidenceRef>` + `INSERT OR IGNORE` absorbe re-runs intra-día sin duplicar. Alertas con `cashierId=null` (agregadas sin sujeto individual) se omiten porque `audit_logs.actor_id` es NOT NULL. La escritura es best-effort: una falla no envenena el response del dashboard. **(3) Snooze flow** — nueva tabla `ai_anomaly_snoozes` (migración `0008_ai_anomaly_snoozes.sql`, journal idx=8) keyed por `(tenantId, kind, cashierId, evidenceRef)` con `snoozedUntil` ISO. Nueva procedure `ai.anomalies.snooze({kind, cashierId, evidenceRef?, durationDays:1-90, reason?})` (managerOrAdminProcedure). El detector carga snoozes activos en paralelo con los sub-detectores y filtra alertas cuya clave matchea uno con `snoozed_until > now`; un snooze con `evidenceRef=null` silencia todas las alertas de ese kind para ese cashier (útil para `voidRate`/`noSaleSessions` agregados). UI: botón "Silenciar 7 días" / "Snooze 7 days" en cada fila del `AnomalyDetailsModal` con toast localizado de éxito que muestra `snoozedUntil`. i18n keys `aiAnomalies.modal.snooze*` en + es; `nav.badges.unreadAlertsSr` en + es. | Manager con anomalía Alta abierta → badge rojo en el ítem Dashboard del sidebar visible desde cualquier pantalla. Click en "Silenciar 7 días" en una fila del modal → toast de éxito + la alerta desaparece del listado próximo. Misma alerta no reaparece por 7 días aún si el evento subyacente sigue dentro de la ventana de 30 días. `audit_logs` registra cada detección nueva con metadata estructurada para cross-reference desde `/audit-logs`. | **Medium** |
| `ENG-046` | CI minutes reduction — manual cross-OS desktop build | Shipped | Out-of-band ticket triggered by an operator pedido para reducir el consumo de minutos de GitHub Actions en cada push a main. Pre-ENG-046 el job `desktop` en `ci.yml` corría una matrix `ubuntu-latest + macos-latest + windows-latest` con `fail-fast: false` en cada push y PR — los minutos de macOS bill at 10x contra el free tier, así que un solo push gastaba ~12-15 minute-equivalents solo por el desktop matrix antes de contar web/backend/e2e. **Shipped**: el job `desktop` en `.github/workflows/ci.yml` queda como ubuntu-only single-runner que sigue corriendo `ci:desktop` (typecheck + lint + node --test window-config regression) en cada push — pinea las regresiones del main↔renderer boundary y las del preload sandbox sin pagar minutos cross-OS. Nuevo workflow `.github/workflows/build-desktop.yml` que es `workflow_dispatch`-only con cuatro inputs booleanos (`linux` / `mac` / `win` / `validate_only`); el operador elige qué OS quiere construir y si prefiere validar (solo `ci:desktop`) o también empaquetar (`make:desktop` + upload artifacts). Concurrency group por actor cancela runs duplicados accidentales. Los artifacts de packaging quedan unsigned (signing material vive solo en `release.yml` por tag); útiles para descargar y testear bugs platform-specific localmente. `release.yml` queda como está (workflow_dispatch con tag input para releases firmadas). `BACKLOG.md` bullet `[infra][ci]` removido (resuelto por este ticket). | Push a main dispara solo jobs ubuntu (web + backend + desktop validation + e2e + release-automation); cero minutos macOS/Windows facturados por push. Operador puede correr el matrix completo a demanda desde la UI de GitHub Actions seleccionando solo los OS que necesita validar. | **Medium** |
| `ENG-045` | Design system audit + tabbed Company page | Shipped | Out-of-band ticket triggered by an operator-supplied handoff bundle from Claude Design (`Puntovivo Design System-handoff.zip`). The handoff was reverse-engineered from the codebase itself (`apps/web/src/styles/theme.css`, `components.css`, `base.css`, `utilities.css` — the diff between the kit's CSS layer and the live repo was bit-identical for theme/base/utilities and only ~28 lines for components.css), so the work was an audit + targeted polish rather than a wholesale redesign. **Shipped**: `/company` refactored from a stacked grid of 8 admin-only cards (long vertical scroll) to a five-tab segmented-control layout (`General` / `Localización` / `Datos` / `Dispositivo` / `IA`) using the canonical `.segmented-control` + `.segmented-tab` + `.segmented-tab-active` recipe already living in `apps/web/src/styles/components.css` (matches preview `16-segmented.html` from the handoff exactly — radius-lg 22px, primary-50 hover, primary-700 text, 4px focus ring). Active tab is URL-stateful via `useSearchParams` so deep links work; AnomalyDetectionCard's "Activa la IA en Configuración" CTA now lands on `/company?tab=ai` directly instead of dumping the manager into the General tab. Non-admin users still see the form + logos without tab chrome. Settings i18n namespace gains `company.tabs.{ariaLabel,general,locale,data,device,ai}` in en + es (neutral LATAM tú). Six new render tests (`CompanyPage.test.tsx`) pin: five tabs render, default lands on General, `?tab=ai` deep-links the AI panel, click-switches update aria-selected and URL, device tab renders all four cards, locale tab renders only the locale card, non-admin sees no tablist. `.btn-success` deviation from the kit kept (single legitimate callsite in `QuotationsHistoryTable.tsx` for accepted quotation transitions; comment in components.css already documents the use case). **Cross-surface audit (extended scope)**: per the operator's expanded request, `LoginPage`, `DashboardPage` / `DashboardPanels`, `SalesPage` / `SaleCartTable` / `SalesCheckoutPanel`, `Header`, `Sidebar`, `OfflineStatusBanner`, `ThemeProvider`, `ToastProvider`, `Modal`, `Select`, and `App.tsx` were diffed against the kit's reference implementations. Every legitimate diff (Sidebar +4 nav icons, App +`/co-pilot` route, DashboardPage +AnomalyDetectionCard, SalesCheckoutPanel +ENG-018b park-and-resume props, Header +`FiscalContingencyIndicator`, Modal +nested-focus a11y guard) is **post-handoff feature work that the kit predates** — applying the kit blindly would regress shipped functionality, so no changes were applied to those surfaces. The `LoginPage` and `DashboardPanels` files are bit-identical to canon. The kit is descriptive of the codebase at a snapshot moment; the live tree is at or beyond it on every audited surface. | Operator with admin role lands on `/company` and sees the segmented control with five tabs. Click "IA" or open `/company?tab=ai` shows the AI settings card directly. AnomalyDetectionCard's CTA opens the AI panel without manual navigation. Cashier role redirected to `/sales` as before. | **Medium** |
| `ENG-071` | Authority Node ADR + runtime config contract | Shipped | Lock the Authority Node runtime-mode architecture before code changes. **Scope**: accept ADR-0008; keep `device_local` as the default; define `AuthorityMode = 'device_local' \| 'site_hub' \| 'hub_client'`; document that only Authority Nodes write operational tables and emit outboxes; define local runtime config fields (`authorityMode`, `hubUrl`, `bindHost`, `bindPort`, `siteId`, `deviceId`, `allowedLanOrigins`); document that shared SQLite over NAS/network share is rejected; explain central-server relationship in `docs/AUTHORITY-NODE.md`. **Shipped 2026-05-11**: docs-only Status closeout for the locking ticket of the Authority Node wave. ADR-0008 `docs/architecture/0008-authority-node-runtime-modes.md` was accepted 2026-05-08 and indexed in `docs/architecture/README.md` as `Accepted \| ENG-071`. `docs/AUTHORITY-NODE.md` is the operator-facing reference, covering: (a) the three-mode contract `AuthorityMode = 'device_local' \| 'site_hub' \| 'hub_client'` with default `device_local`; (b) the Authority Node invariant — only Authority Nodes write operational tables and emit operational outbox rows for accepted commands; (c) the seven runtime config fields `authorityMode`, `siteId`, `deviceId`, `hubUrl`, `bindHost`, `bindPort`, `allowedLanOrigins`; (d) explicit rejection of shared SQLite over NAS / network shares (a hub client must never mount or write the hub SQLite file directly); (e) the central-server boundary — a future central server consumes Authority Node outboxes / events downstream and is never required for local selling. The runtime contract is implemented in `packages/server/src/config/runtime.ts` exposing the exact `AuthorityMode` union and `RuntimeConfig` interface with the seven fields. Downstream tickets `ENG-072` (device-local default hardening, Shipped 2026-05-08), `ENG-073` (Store Hub server mode, Shipped 2026-05-09), `ENG-074` (Hub Client mode, Shipped 2026-05-09) and `ENG-074b` (hub-client local hardware bridge, Shipped 2026-05-09) all consumed this contract before the Status flip — they could not have shipped without ADR-0008 in force, so the ticket is operationally closed and is now indexed as such. Zero code, tests, migrations, i18n or build configs touched in this closeout commit — the entire surface is the Status column + cross-doc verb tense in ROADMAP §3b, SPRINT-PLAN §1 + §2.1, and PLAN.md §10.1 + §18.1. Acceptance criteria 2 and 3 ("no runtime behavior changes; existing installs remain `device_local`") trivially satisfied. Follow-up `ENG-075` (Device pairing + Authority health) shipped 2026-05-11; `ENG-076` remains deferred until a pilot proves hub-client satellite offline writes are needed. | ROADMAP, SPRINT, ADR index and `docs/AUTHORITY-NODE.md` agree on the modes; no runtime behavior changes; existing installs remain `device_local`. | **High** |
| `ENG-072` | Device-local default hardening | Shipped | Make the current runtime explicit in code without changing behavior. **Scope**: shared runtime config resolver for Electron + standalone server; missing config returns `device_local`; embedded desktop server still binds loopback; diagnostics export and server banner include authority mode/server URL; tests cover default config, invalid config, and no-regression desktop boot. **Shipped 2026-05-08**: new `packages/server/src/config/runtime.ts` exposes `resolveRuntimeConfig({env, defaults?})` returning the AUTHORITY-NODE.md `RuntimeConfig` shape with defaults `device_local + 127.0.0.1 + 8090`; reads `PUNTOVIVO_*` env vars (mode / bind host / bind port / hub URL / site id / device id / allowed LAN origins) plus legacy `HOST` / `PORT` fallback. `createServer` accepts an optional `runtime?: RuntimeConfig`, sets a process-singleton, and logs `{address, authorityMode, bindHost, bindPort}` (ENG-006 structured log). Standalone boot resolves the config + adds an `Authority mode: …` line to the operator banner. Embedded Electron server in `apps/desktop/src/main/index.ts` resolves the config from `process.env` before booting. The `reports.diagnostics.{preview, export}` responses both gain a `runtime: {authorityMode, bindHost, bindPort, hubUrl, siteId, deviceId, allowedLanOrigins}` projection (additive — `schemaVersion=1` and the locked counts keyset are unchanged); the ENG-066 sanitizer needs no extension because none of the new fields carry secrets. 24 new resolver tests, 4 new desktop default-boot tests, and a runtime-field assertion added to the existing diagnostics suite. Live smoke confirmed via `/operations` Diagnóstico tab end-to-end (server boot log → singleton → tRPC response surfaces `runtime.authorityMode='device_local'`). LAN bind enforcement (ENG-073) and `hub_client` tRPC base URL switch (ENG-074) deliberately deferred — the resolver returns those values, no behavior change today. | Fresh desktop install with no config boots exactly like today and reports `authorityMode='device_local'`; all existing desktop/server checks remain green. | **High** |
| `ENG-073` | Store Hub server mode | Shipped | Productize one local site hub that owns the site SQLite file and accepts LAN clients. **Scope**: explicit `site_hub` config can bind Fastify to `0.0.0.0` or a chosen LAN IP; startup refuses LAN bind without strong JWT secret + configured allowed origins; CORS/CSRF accept LAN origins from config; health reports authority mode, app version, DB schema, DB path fingerprint and active-device count; document operational setup. **Shipped 2026-05-09**: `createServer` gains a fail-fast LAN guard — when `runtime.authorityMode === 'site_hub'` the boot throws unless `JWT_SECRET` is supplied explicitly and passes the Store Hub strength policy (trimmed length at least 32 chars, at least 8 unique chars, not a common placeholder) AND `runtime.allowedLanOrigins.length > 0` (no operator-defined CORS surface, no LAN bind). Single error names both missing pieces and points at `docs/AUTHORITY-NODE.md > Store Hub Mode`. `device_local` and `hub_client` skip the guard entirely so the loopback default and the future hub-client terminal stay unaffected. CORS allow-list merges `runtime.allowedLanOrigins` into the existing dev origins for `site_hub` only — exact-match strings, no wildcards, the configured list is the audit surface. CSRF stays cookie+header (works cross-origin under `credentials: true`). New `packages/server/src/lib/runtimeMetadata.ts` exposes `fingerprintDbPath(path)` (12-char SHA-256 truncation, `:memory:` returns the literal `'memory'`), `getCurrentSchemaVersion(db)` (applied migration count from `__drizzle_migrations`), and `countActiveDevices(db)` (system-wide aggregate; per-tenant view ships in ENG-075). `/api/health` body extends with `authorityMode`, `appVersion`, `dbSchemaVersion`, `dbPathFingerprint`, `activeDeviceCount` while keeping every legacy field intact (compatibility, canonical procedure pointer). `appVersion` threads through `createServer({appVersion?})` → standalone (`process.env.npm_package_version`) → Electron (`app.getVersion()`); default `'unknown'` so tests do not need to wire it. 17 new tests across `site-hub-validation.test.ts` (11 cases) and `health-endpoint.test.ts` (6 cases) — guard refusals + whitespace-only/short/low-variety/placeholder secret rejection + dual-missing-piece message + doc pointer + device_local/hub_client skip + happy path + endpoint shape + dbPath leak check + site_hub explicit runtime. Live smoke confirmed via `curl http://localhost:8090/api/health` against the standalone server: response body carries the new identity block; default boot returns `authorityMode: 'device_local'`, fingerprint `'memory'` for `:memory:` DB. ENG-074 (`hub_client` tRPC base URL switch) and ENG-075 (pairing + Operations Center Authority tab) remain the next implementation tickets — this ticket does not touch either. | A hub machine can run the same server against one site DB and expose tRPC on LAN without allowing arbitrary origins or shared DB-file access. | **High** |
| `ENG-074` | Hub Client mode | Shipped | Let cashier terminals point at a Store Hub instead of their embedded backend. **Scope**: `hub_client` config sets the runtime tRPC base URL to `hubUrl`; device registration marks the terminal as a hub client; login, site selection, cash session open, sale completion, and barcode scan run against the hub; peripherals physically attached to the terminal execute through a client-local hardware bridge for receipt print/cash drawer flows; UI shows hub reachability before checkout; no local operational write fallback in v1. **Shipped 2026-05-09**: in two phases. Phase 1 (renderer + device + reachability) — Electron main caches `resolveRuntimeConfig` once and exposes it to the renderer via the new `runtime:get-config` sync IPC; preload publishes `window.electron.runtime.getConfigSync()`. New `apps/web/src/lib/runtimeConfigClient.ts` reads the bridge at module init (with `device_local` fallback when `window.electron` is absent or the bridge throws) and feeds `apps/web/src/lib/trpc.ts` so `API_URL` becomes `${hubUrl}` in `hub_client` mode. New `apps/web/src/hooks/useHubReachability.ts` polls `${hubUrl}/api/health` every 30s with a 5s abort timeout; the hook is a no-op outside `hub_client`. `OfflineStatusBanner` gains a hub-unreachable variant (mutually exclusive with the existing offline / pending / conflicts variants); the retry button hides when hub-gated. `SalesCheckoutPanel`, `SalesMobileCheckoutBar`, and `SalesOverview` all accept an optional `hubReachable` prop and disable their primary action when it is `false`; `SalesPage` wires `useHubReachability` into all three call sites so the gate cannot be bypassed by viewport. `auth.registerDevice` accepts `kind: 'hub_client'`; `devices.kind` enum extends to `['desktop','web','hub_client']` (no migration — text column with TS-only constraint). `AuthProvider` selects the kind from `{electron + hub_client → 'hub_client', electron + device_local → 'desktop', browser → 'web'}`. New i18n keys `offline.hubUnreachableTitle/Desc` (common) + `checkout.hubGatedHint` (sales) in en + es, neutral LATAM Spanish. Phase 2 (client-local hardware bridge) — see `ENG-074b` for the full implementation summary; the carve-out shipped same day with two read-only hub procedures, an Electron-main local dispatcher, an IPC channel, and a renderer fork that routes by authority mode. Cookie SameSite refresh fix remains deferred (separate follow-up): v1 hub clients re-login at access-token expiry (7 days) because `SameSite=Lax` blocks the cross-origin refresh cookie. | A second terminal can complete a sale through the hub; the hub DB records the sale, cash session, journal and outbox, with the terminal `deviceId` preserved for audit, while terminal-local hardware execution never writes operational tables. | **High** |
| `ENG-074b` | Hub-client local hardware bridge | Shipped | Carved out from `ENG-074`. **Scope**: a hub_client terminal that has physical USB / serial peripherals attached must dispatch ESC/POS receipt prints and cash-drawer kicks locally — the hub returns the printable payload, the terminal's local Electron main writes to the local printer / drawer. Per ADR-0008 rule 6, this client-local hardware execution NEVER writes operational tables; the hub remains the Authority Node for the sale, cash session, journal and outbox. **Shipped 2026-05-09**: two new read-only server procedures `peripherals.buildReceiptBytes` (cashier+) and `peripherals.buildDrawerKickBytes` (manager+) return `{status, bytes: number[], paperWidth?, characterSet?, transportHint}` mirroring the active `site_peripherals.config_json` shape. Both are `.query` typed and count zero `hardware_outbox` writes across calls (`peripherals-build-bytes.test.ts` asserts the invariant). Cross-tenant guarded via `getSaleRecord(ctx.db, ctx.tenantId, saleId)` + `ensureSiteBelongsToTenant(ctx.db, ctx.tenantId, siteId)` — both vectors covered by tests. New `apps/desktop/src/main/peripherals/local-bridge.ts` exposes `dispatchLocalEscpos({bytes, transport})` that calls `resolveTransport` (re-exported from `@puntovivo/server`) and writes synchronously; the transport is closed in a `finally` block whose errors are swallowed. The module has zero DB imports — pinned by an architectural lint test that substring-scans the source for `outbox` / `drizzle` / `db/` / `services/db`. New IPC channel `peripherals:dispatch-local-escpos` registered in `apps/desktop/src/main/index.ts`; preload publishes `window.electron.peripherals.dispatchLocalEscpos`. Renderer fork lives in `apps/web/src/features/sales/receiptPrinter.ts` as two helpers: `createEscposReceiptDispatcher({serverPrint, fetchHubReceiptBytes})` returns a `() => Promise<EscPosDispatchOutcome>` that routes by `getRuntimeConfigSync().authorityMode`; `dispatchDrawerKick({serverKick, fetchHubDrawerBytes})` does the same for the drawer. In `device_local` / `site_hub` the existing `peripherals.printReceipt` mutation + `peripherals.kickCashDrawer` mutation paths are unchanged. In `hub_client` the hub bytes are fetched via `utils.peripherals.buildReceiptBytes.fetch(...)` and dispatched through the IPC bridge; missing IPC degrades to `{status:'fallback', error:'BRIDGE_UNAVAILABLE'}` which the legacy HTML path handles. `SaleDetailsModal` (`buildEscposDispatcher`) and `SalesPage` (`handleKickCashDrawer`) consume the helpers; both extracted `mutateAsync` into stable bindings to keep callback identity stable across renders. **Tests**: 9 vitest cases in `peripherals-build-bytes.test.ts` (system-fallback / ready / cross-tenant for both saleId and siteId / role gates / hardware_outbox row-count invariant); 5 `node:test` cases in `apps/desktop/src/main/peripherals/__tests__/local-bridge.test.ts` (happy path with mock transport, Uint8Array passthrough, EMPTY_PAYLOAD short-circuit, EscPosTransportError normalization, architectural lint); 11 vitest cases in `apps/web/src/features/sales/receiptPrinter.localBridge.test.ts` (device_local + site_hub server-route, hub_client + IPC + ready bytes, hub_client + IPC missing, hub_client + system-fallback, hub_client + bridge write fails with errorCode, hub_client + hub fetch throws, plus drawer variants). **Live smoke** confirmed via dev:server + dev:web Playwright with stubbed bridge: receipt + drawer paths in hub_client mode dispatched the exact byte payloads through the stub and degraded to `BRIDGE_UNAVAILABLE` when the IPC was removed. USB and serial transports remain stubs (`DRIVER_NOT_IMPLEMENTED`) until a hardware lab session activates them. **Collateral fix**: replaced `header: { tenantName: sale.tenantId }` with a tenant-name lookup in both `printReceipt` and the new `buildReceiptBytes` so the printed receipt header shows the human business name instead of the raw nanoid. | A `hub_client` terminal completes a sale on the hub AND prints the receipt on its terminal-local printer; the hub DB records the sale + outbox row, the terminal main process executes the print without writing any operational table. | **High** |
| `ENG-075` | Device pairing + Authority health | Shipped | Make Store Hub deployments operable. **Scope**: short-lived pairing code from the hub; device registry tracks role, last seen, app version, DB schema version, health status and paired site; Operations Center gets an Authority tab for hub + clients; admin can revoke a hub client; diagnostics export includes authority topology. **Shipped 2026-05-11**: migration `0021_authority_device_pairing.sql` extends `devices` with Authority topology metadata and adds one-time hashed `device_pairing_codes`; new `authority` tRPC router exposes status, pairing-code create/consume and hub-client revoke with tenant/site guards and audit row `device.revoke`. Web `/operations?tab=authority` renders runtime/schema/device health, admin pairing codes, client revoke actions and localized error copy; `auth.registerDevice` now reports runtime site/app version/schema metadata, diagnostics preview/export include `authorityTopology`, and pairing code claims are status-guarded so a code cannot be reassigned after the first claim. | Admin can pair/revoke a terminal and see which devices are connected to a hub, which are stale, and which authority mode the installation is using. | **Medium** |
| `ENG-076` | Hub-client satellite offline fallback spike | Deferred | Decide later whether hub clients should write locally when the hub is down. **Scope**: compare fail-closed hub clients, local draft-only mode and full offline satellite writes; quantify duplicate-sale, cash-session, fiscal and inventory conflict risk; no implementation until a pilot proves fail-closed is insufficient. | Decision doc exists with a go/no-go recommendation and implementation gates; no hub-client local operational writes ship before this is reopened. | **Medium** |
| `ENG-044` | OpenAI provider activation (chat) | Shipped | Out-of-band ticket opened during the ENG-031 review when the operator's Anthropic billing reconciliation stalled two consecutive live smokes (ENG-030 + ENG-031). The ENG-030 Strategy / Factory abstraction was designed exactly for this — adding a real second provider lands as a single file rewrite plus tests. **Shipped**: `packages/server/src/services/ai/providers/openai.ts` rewritten from a `notImplemented` stub to a real `AIProvider` backed by `@ai-sdk/openai@^3.0.54`. `defaultModelId='gpt-4.1-mini'` (sweet spot quality/cost — better than `gpt-4o-mini` in multi-step tool calling and SQL generation, ~87% cheaper per output token than Anthropic Sonnet 4.6). `PRICING_TABLE` carries `gpt-4.1-mini`, `gpt-4.1`, `gpt-4o`, `gpt-4o-mini` plus their pinned snapshots so admins can override either down for cost or up for quality without a code change. `cacheControlForSystemPrompt()` returns `undefined` because OpenAI auto-caches prompts ≥1024 tokens server-side (no `providerOptions` needed; cache discounts are billed at row-level via `cacheRead`). Embeddings (`embeddingModel?`) intentionally stay undefined and land with ENG-033 alongside the cosine index + auto-categorization. Pricing verified at ship time via WebSearch (OpenAI's pricing page returns 403 to programmatic fetchers; cross-checked against pricepertoken, openrouter, livechat calculator, inworld). New `openai.test.ts` covers cost math for all four models + fallback + cache pricing + isConfigured + defaultModelId + embeddingModel-undefined. Existing `registry.test.ts` flipped: openai is now `isImplemented=true` with no `availableInTicket`; the `notImplemented` stub assertions switched to ollama (still parked for ENG-040). Existing `ai-router.test.ts` flipped the same way; the `settings.update({providerId})` rejection test switched from openai to ollama so the rejection flow stays under test. `.env.example` AI section retitled to `(ENG-030 + ENG-044)`, `OPENAI_API_KEY` block expanded with a real description, ollama hint kept. Counter Plan v2.0 stays at `(6 / 16 closed)` — out-of-band, not part of the original 16-ticket phase tabla. BACKLOG bullet `[ai][infra] Activate the OpenAI provider as a chat fallback…` removed (replaced by this row). | Admin can pick `openai` in the Settings → AI card, set `gpt-4.1-mini` (or override), click Test connection with `OPENAI_API_KEY` exported and get a translated `pong` toast plus an `ai_audit_log` row with `provider_id='openai'`; the co-pilot answers a Spanish question end-to-end with the same provider. Without `OPENAI_API_KEY`, the configured-badge stays red and calls fail with the existing `AI_PROVIDER_ERROR` code; ollama remains rejected with the `ENG-040` hint when an admin tries to select it. | **Medium** |

Sequencing recommendation:

1. **Sellability truth sync**: `ENG-050` is shipped and is now the source for demo / pilot / production go-no-go through [SELLABILITY.md](./SELLABILITY.md).
2. **Foundation Reset before new commercial scope**: ship `ENG-051` → `ENG-056` in order. These tickets introduce ADRs, device identity, idempotency, operation journal, sale lifecycle services, and the cash-session boundary without rewriting the whole app.
3. **Fiscal readiness, provider-agnostic first**: ship `ENG-057` and `ENG-058` before any real provider. Start `ENG-059` only after the PT contract, credentials, certificate, numbering resolution, and error map are available.
4. **Hardware readiness**: ship `ENG-060` and `ENG-061` without external gates. `ENG-062` can implement the adapter and fallback path, but full validation waits for a physical printer + drawer. `ENG-063` remains gated on payment sandbox + terminal.
5. **Sync / operations / resilience**: ship `ENG-064`, `ENG-065`, `ENG-067`, then `ENG-066`. The ordering intentionally gives operators visibility and chaos coverage before deeper local-security work.
6. **Expansion architecture**: ship `ENG-068`, `ENG-069`, `ENG-070` after the retail core is safe enough. Module activation precedes full restaurant/pharmacy/service vertical work.
7. **Authority Node early support**: ship `ENG-071` → `ENG-075` before treating multi-register stores as sellable. This keeps `device_local` as the default while adding explicit `site_hub` / `hub_client` support. `ENG-076` remains deferred until a pilot proves hub clients need satellite offline writes.
8. **Existing gated tickets stay parked**: `ENG-021`, `ENG-022`, `ENG-023`, `ENG-059`, and `ENG-063` require their documented external gates.
9. **Older pending quality tickets**: `ENG-010`, `ENG-011`, and `ENG-014` are superseded in practice by the Foundation Reset wave where their scope overlaps. Keep them until the overlapping `ENG-054..ENG-056` work closes or explicitly retire them in a follow-up docs pass.

For sprint-level execution (per-commit sequencing, draft commit messages, per-iter verification matrix) see [SPRINT-PLAN.md](./SPRINT-PLAN.md). For the v2.0 phasing and architectural decisions closed by the 2026-Q2 audit see [PLAN-V2.md](./PLAN-V2.md).

---

## 4. i18n Plan — Pre-Phase 1

**Stack**: `i18next` + `react-i18next` — installed ✓
**Fallback chain**: `es-CO` → `es` → `en` — configured ✓
**Namespace splitting**: per feature area (common, auth, nav, sales, inventory, etc.)

**Why first**: English-only UI is a deployment blocker for LatAm. Every new phase adds more hardcoded strings. Foundation should land before cash management UI.

**Scope**: ~126 component files, ~300+ labels, ~137 toasts, ~187 form fields to extract.

### Progress

| Phase | Scope | Status |
| --- | --- | --- |
| i18n-1: Foundation | Install packages, scaffold `apps/web/src/i18n/`, wire in `main.tsx`, `en/` + `es/` stubs for `common`, `auth`, `nav` | **Done** |
| i18n-2: High-visibility surfaces | `LoginPage`, `Sidebar`, `Header` converted to `useTranslation` | **Done** |
| i18n-3: Full feature coverage | All feature modules (`sales`, `products`, `purchases`, `inventory`, `orders`, `customers`, settings screens) | Pending |
| i18n-4: Server + CI enforcement | Server error messages, Electron main process strings, CI lint rule blocking new hardcoded strings | Pending |

---

## 5. Technical Roadmap by Phase

Each phase includes DB, tRPC, UI, and Test tickets.

### Phase 0: Architecture Foundation and Multi-Vertical Module System

**Goal**: Prepare codebase for logistics, dual-database compatibility, multi-vertical modules, and compound tax.

**DB tickets**:
- `DB-001` Dialect-neutral schema conventions
- `DB-002` Replace raw schema bootstrap with versioned Drizzle migrations
- `DB-003` Money, quantity, and timestamp normalization rules
- `DB-004` Add `vertical` column to `sites` (enum: retail, supermarket, pharmacy, ferreteria, restaurant, etc.)
- `DB-005` Add `metadata` JSON column to `products` for vertical-specific attributes
- `DB-006` Add `settings` JSON column to `sites` for capabilities map
- `DB-007` Create `tax_groups` table (compound tax scenarios + mutual exclusivity)
- `DB-008` Create `tax_group_items` table (tax_type, rate, calculation_base, exclusive_with)
- `DB-009` Link products to tax_groups instead of single VAT rate

**tRPC tickets**:
- `API-001` Repository/service boundaries for core domains
- `API-002` Separate persistence concerns from router procedures
- `API-003` Sync acknowledgement contract
- `API-004` Module registry service: resolve site vertical → compute `SiteCapabilities`
- `API-005` Compound tax calculation engine
- `API-006` Module-conditional tRPC router composition

**UI tickets**:
- `UI-001` System diagnostics page for runtime topology
- `UI-002` Admin-facing sync topology indicators
- `UI-003` Vertical/module activation settings page with setup wizard
- `UI-004` `SiteCapabilities` React Context provider
- `UI-005` Capability-filtered sidebar navigation
- `UI-006` Checkout compound tax display (IVA, INC, impuesto saludable lines)

**Test tickets**:
- `TEST-001` Persistence contract tests reusable across dialects
- `TEST-002` Schema migration smoke tests
- `TEST-003` Sync contract tests
- `TEST-004` Compound tax: IVA 19% + INC 8% on same receipt
- `TEST-005` SiteCapabilities computed correctly per vertical
- `TEST-006` Module-conditional sidebar rendering

### Phase 1: Cash Management, Shift Control, and Fractional Quantity

**Goal**: Most critical missing commercial feature for LatAm retail + unblock fractional quantity sales.

**DB tickets**:
- `DB-050` **CRITICAL**: Convert `stock`/`quantity` from `integer` to `real` across ALL tables. Add `sell_by_fraction`, `fraction_step`, `fraction_minimum` flags.
- `DB-051` Create `cash_sessions` (register, cashier, site, opening_float, denominations, expected_balance, actual_count, over_short, status, timestamps)
- `DB-052` Create `cash_movements` (session_id, type [sale, refund, paid_in, paid_out, skim, replenishment], amount, reference, note)
- `DB-053` Create `denomination_templates` for standardized float breakdowns

**tRPC tickets**:
- `API-051` `cashSessions.open` with denomination counting and float validation
- `API-052` `cashSessions.close` with blind close support
- `API-053` `cashSessions.movements` for paid-in, paid-out, skim, replenishment
- `API-054` `cashSessions.report` with over/short history per cashier
- `API-055` Update `sales.create`/`sales.refund` to require active cash session

**UI tickets**:
- `UI-051` Cash session open dialog with denomination counting grid
- `UI-052` Cash session close dialog with blind close mode
- `UI-053` Cash session summary with movement timeline
- `UI-054` Cash management dashboard: active sessions, over/short trends
- `UI-055` Register assignment in POS checkout header

**Test tickets**:
- `TEST-051` Opening cash denomination count matches float
- `TEST-052` Sale increments session expected balance
- `TEST-053` Refund decrements session expected balance
- `TEST-054` Blind close hides expected amount until count submitted
- `TEST-055` Over/short calculation accuracy

### Phase 2: Site-Owned Inventory and Transfer Logistics

**Goal**: Make stock physically believable across sites and warehouses.

**DB tickets**:
- `DB-101` Create `inventory_balances` by product/site/location — **Step 0 shipped** at the (tenant, site, product) grain; location column deferred.
- `DB-102` Create `transfer_orders` and `transfer_order_items` — **Step 1 shipped** (immediate completion only — lifecycle states deferred).
- `DB-103` Create `transfer_shipments` and `transfer_receipts`
- `DB-104` Migrate tenant-wide stock to default site-owned balances — **Step 1 shipped** as a seed-only insert onto the earliest-created active site (balances are now authoritative).

**tRPC tickets**:
- `API-101` `inventory.listBalancesBySite` — **Done** (read-only).
- `API-102` `transfers.create`, `.ship`, `.receive`, `.void` — **Step 3 shipped**: `transfers.create` with optional `defer` flag (origin debited on create, destination credited on receive when `defer: true`), `transfers.list`, `transfers.receive` (completes an in_transit transfer), and `transfers.void` (reverses both completed and in_transit). `.ship` is collapsed into `.create({ defer: true })`; a standalone draft state remains deferred.
- `API-103` Update sales/purchases/orders to read/write site balances — **Step 4 shipped**: sales + purchases/order receiving + admin inventory tools now write through to `inventory_balances`, and `products.stock` is kept in lockstep as Σ(site balances). `applyInventoryBalanceDelta` recomputes and persists `products.stock` after every balance mutation via the new `syncProductStockFromBalances` helper, so the legacy tenant-wide field never drifts during normal operation. A new `inventory.reconcileBalances` (admin) mutation heals historical drift by recomputing `products.stock` for every product in the tenant. The site-resolution contract per mutation remains: `sales.create` debits the cash-session site; `sales.returnSale`/`sales.void` credit the ORIGINAL sale site; `purchases.create` credits the operator site; `purchases.createFromOrder` credits the order site; `purchases.returnPurchase`/`purchases.void` debit the original purchase site; `inventory.adjustStock` resolves the site via `input.siteId ?? ctx.siteId ?? primary` and applies the absolute delta; `inventory.recordEntry` writes through to the operator site, with `mode: 'initial'` crediting by `normalizedQuantity` and `mode: 'physical'` applying the absolute-count delta. A standalone draft transfer state remains deferred.

**UI tickets**:
- `UI-101` Inventory page: site/location balance tabs — **Done** (By Site tab in Inventory).
- `UI-102` Transfer Orders module — **Step 4 shipped** (Transfer stock button + modal with "Ship now, receive later" checkbox + transfer history table with Details/Receive/Void actions + confirmation modal + read-only detail drawer showing line items and lifecycle timestamps, all in the By Site tab; dedicated Transfer Orders page deferred).
- `UI-103` Transfer receive modal with discrepancy reporting — **Done**: receiving an in_transit transfer opens a modal that lists every line with an editable received-quantity input (defaulting to shipped), displays per-line variance badges, and reveals an optional discrepancy-notes textarea when any line diverges. The server credits the destination with the received quantity only (so the `shipped - received` delta shows up as shrinkage via the existing products.stock-in-lockstep invariant) and persists the per-line `received_quantity` + aggregate `discrepancy_notes` on the transfer. The history row renders a Discrepancy badge when present; voids debit the destination by `received_quantity` (coalescing to shipped for legacy rows) so partial-receipt reversals are symmetric. `received > shipped` is rejected up front (`TRANSFER_RECEIVED_EXCEEDS_SHIPPED`) — operators who genuinely received more should take the shipped quantity and post a separate `inventory.adjustStock`.

**Test tickets**:
- `TEST-101` Sales decrement active site only
- `TEST-102` Purchase receipts increment target site only
- `TEST-103` Transfer shipment creates in-transit without double counting
- `TEST-104` Transfer receipt resolves in-transit correctly

### Phase 3: Outbound Logistics Documents

**Goal**: Pick/pack/ship as first-class warehouse/store operations.

**DB**: `fulfillment_orders`, `pick_lists`, `packing_slips`, `delivery_notes`
**tRPC**: Fulfillment allocation, pick list generation/completion, packing slip, delivery note validation
**UI**: Fulfillment workbench, pick list barcode workflow, packing UI, delivery note printable
**Tests**: Pick respects balances, delivery note posts correct movement, partial shipment remains fulfillable

### Phase 4: Transport Execution and Tracking

**Goal**: Dispatch, transport, and delivery follow-through.

**DB**: `shipments`, `shipment_stops`, `drivers`, `vehicles`, `carriers`, `proof_of_delivery`, `delivery_exceptions`
**tRPC**: Dispatch assignment, shipment status transitions, POD mutation, exception handling
**UI**: Dispatch board, shipment timeline, driver POD screen, customer tracking stub
**Tests**: Status progression, POD closes shipment, exception consistency

### Phase 5: Payment Depth, Quotations, Layaway, and Credit Sales

**Goal**: Complex payment scenarios, pre-sale conversion, and LatAm credit practices.

**DB**: `quotations`, `sale_payments` (multi-tender), `customer_credit_accounts`, `gift_cards`, `store_credits`, `layaway_orders`, `special_orders`, `service_tickets`, company fiscal regime fields, `company_credit_settings`, `credit_sales`, `credit_installments`, `credit_payments` (abonos)
**tRPC**: Quotation CRUD/conversion, split payment processing, on-account sales, gift card/store credit, layaway/apartado workflow, special orders, service tickets, credit sale creation with installment schedule, abono posting, overdue scan, aging report
**UI**: Quotations module, multi-tender checkout dialog, credit account management, layaway management, credit sale checkout flow, abono screen, credit portfolio
**Tests**: Quote conversion preserves prices, split payment sum validation, credit limit enforcement, layaway inventory reservation, installment schedule accuracy, abono distribution to oldest installments

### Phase 6: Product Handling and Advanced Inventory

**Goal**: Broader product categories and operational complexity.

**DB**: `product_variants`, `serial_numbers`, `batches`/`batch_balances`, `bundle_components`/`recipes`, product weight/dimensions/shipping/reorder fields
**tRPC**: Variant-aware search, serial/batch assignment, FEFO allocation, bundle explosion, reorder alerts, ABC analysis, inventory aging, GMROI, cycle counting
**UI**: Variant matrix builder, serial/batch selector, expiry dashboard, bundle/recipe management, reorder dashboard, ABC view, aging heatmap, cycle count worksheet
**Tests**: Serialized capture, batch FEFO, bundle component decrement, reorder trigger, ABC distribution

### Phase 7: Loyalty, Promotions, and Commercial Expansion

**Goal**: Conversion, retention, omnichannel readiness.

**DB**: `promotion_rules`, `coupons`, `loyalty_accounts`, `loyalty_transactions`, `loyalty_tiers`, order channel/delivery mode
**tRPC**: Promotion engine (evaluate cart, apply best/stackable discounts), coupon validation, loyalty earn/redeem, points expiry, omnichannel fulfillment
**UI**: Promotion rule builder, coupon management, checkout auto-promotion, loyalty display/redeem, customer loyalty profile, omnichannel order queue
**Tests**: BOGO logic, promotion stacking, coupon single-use, loyalty refund reversal, tier upgrade

### Phase 8: Employee Management and Audit Trail

**Goal**: Employee lifecycle and operational accountability.

**DB**: `employee_shifts`, `employee_commissions`, `commission_rules`, `audit_logs`, `approval_policies`, `approval_events`
**tRPC**: Shift clock in/out, commission calc/clawback, audit log recording, approval workflows, employee performance metrics
**UI**: Shift management, commission config/reports, audit log viewer, approval inbox, employee dashboard
**Tests**: Clock timestamps, commission clawback on return, audit before/after state, approval blocking

### Phase 9: Advanced Reporting and BI

**Goal**: Actionable insights beyond basic views.

**DB**: `daily_sales_summary`, `daily_inventory_snapshot`, `customer_cohorts`
**tRPC**: Sales/Inventory/Customer/Employee KPIs, exception alerts, drill-down API, scheduled report export
**UI**: Executive dashboard with sparklines, operational dashboard, inventory intelligence, customer insights, exception alerts, drill-down navigation, report builder
**Tests**: Summary aggregation accuracy, GMROI formula, comp sales exclusions

### Phase 10: Hybrid Database Runtime

**Goal**: SQLite local + PostgreSQL-compatible remote truth.

**Recommended stack**: PowerSync for sync layer. Dual schema from shared types (near-term). PGlite evaluation for long-term.

**DB**: Dialect abstraction package, dual `sqliteTable`/`pgTable` variants, Postgres migration/bootstrap, operation log schema, boolean/timestamp/JSON/UUID normalization
**tRPC**: Repository interfaces for either dialect, remote sync/apply endpoints, conflict response model, capability negotiation
**UI**: Remote authority config, improved sync center, richer conflict resolution
**Tests**: Full contract suite against SQLite and Postgres, offline-then-reconnect replay, multi-client conflict scenarios

### Phase 11: Fiscal, Accounting, and Integration Layer

**Goal**: Market readiness for broader deployment, Colombia DIAN compliance first.

**Colombia DIAN**: DEE (mandatory since May-July 2024), Factura Electrónica (UBL 2.1), Nota Crédito/Débito, CUFE/CUDE (SHA-384), XAdES-EPES digital signature, SOAP web service, numbering range management, contingency mode.

**DB**: `fiscal_documents`, `fiscal_numbering_ranges`, `fiscal_certificates`, `credit_notes`/`debit_notes`, `fiscal_contingency_log`, `supplier_invoices`, `api_keys`, `webhooks`, `currency_rates`, `country_fiscal_profiles`, `company_fiscal_overrides`
**tRPC**: Fiscal adapter interface, Colombia DIAN adapter (UBL 2.1 XML), CUFE/CUDE service, XAdES-EPES signing, DIAN SOAP client, numbering range management, contingency mode, credit/debit note lifecycle, profile-driven tax engine, tip rules refactor, fiscal adapter factory, public API, webhooks, accounting events
**UI**: Fiscal document views, numbering range management, certificate management, DIAN habilitación wizard, contingency indicator, multi-currency settings, integration/webhook admin, API key management
**Tests**: CUFE/CUDE SHA-384 vs DIAN vectors, UBL XSD validation, XAdES-EPES validity, contingency activation, webhook signing, multi-currency accuracy

### Phase 12: Restaurant and Service Verticals

**Goal**: Restaurant, food service, and appointment-based businesses.

**Prerequisites**: Phase 0 (modules)

**DB**: `tables`, `table_sessions`, `kitchen_orders`, `product_modifiers`/`modifier_groups`, `appointments`, `tip_records`
**tRPC**: Table management (assign/transfer/merge/split), kitchen order routing, course firing, modifier application, split check, tip management (Ley 1935/2018), appointments, auto-86ing, daypart menus, combo engine, kitchen printer routing
**UI**: Floor plan editor, table status grid, KDS with timers, modifier selection, split check dialog, tip consent dialog, appointment calendar, combo builder
**Tests**: Table-sale link, kitchen status transitions, split check math, tip pool distribution, appointment conflict detection, daypart activation, combo pricing

### Phase 13: Pharmacy Vertical

**Prerequisites**: Phase 0, Phase 6 (lot/batch/expiry), Phase 11 (fiscal)

**DB**: `prescriptions`, `prescription_items`, `dispensation_records`, `controlled_substance_ledger`, `fne_reports`, `rips_records`, pharmacy product fields (INVIMA, controlled_schedule, INN/DCI, storage), `eps_contracts`, `equivalence_groups`
**tRPC**: Prescription CRUD with validity/partial dispensing, controlled substance ledger, FNE/RIPS/SISMED reports, regulated price ceiling enforcement, generic substitution, patient medication history, INVIMA recall processing
**Tests**: Controlled substance requires valid Rx, partial dispensing tracking, FNE ledger balance, expired Rx blocking, regulated price ceiling, RIPS format

### Phase 14: Supermarket Vertical

**Prerequisites**: Phase 0, Phase 1 (fractional qty), Phase 6 (lot/batch/expiry), Phase 7 (promotions)

**DB**: `scale_configurations`, `plu_codes`, supermarket `departments`, `shrinkage_records`, `dsd_receiving`, `vendor_promotions`, supermarket product fields
**tRPC**: Scale reading service, variable-weight barcode parsing, age restriction enforcement, department P&L, shrinkage tracking, DSD receiving, automated near-expiry markdowns, impuesto saludable
**Tests**: Variable-weight barcode decode, age restriction blocking, department shrinkage totals, impuesto saludable rates

### Phase 15: Ferretería Vertical

**Prerequisites**: Phase 0, Phase 1 (fractional qty), Phase 5 (quotations, credit)

**DB**: `product_sale_units` (multi-unit with conversion), `service_charges`, `project_templates`, FTS5 virtual table for product search
**tRPC**: Multi-unit sale with conversion, in-house barcode generation, project template management, service charges, partial-use returns, FTS5 search, bulk pricing auto-application
**Tests**: Unit conversion accuracy, barcode generation, template explosion, partial-use return

---

## 6. Competitive Context (Summary)

### What Puntovivo Has That Others Don't

| Capability | Puntovivo | Square | Shopify | Lightspeed | Odoo |
| --- | --- | --- | --- | --- | --- |
| True offline mode | **Strong** | Limited | Limited | No | No |
| Desktop native | **Yes (unique)** | No | No | No | No |
| Open source | **Yes** | No | No | No | Community |
| No subscription | **Yes** | No | No | No | No |
| Self-hosted | **Yes** | No | No | No | Yes |

### Biggest Competitive Gaps

| Gap | vs Colombia (Siigo/Alegra) | vs Global (Square/Shopify) | vs Open Source (Odoo/ERPNext) |
| --- | --- | --- | --- |
| Cash management | **Critical** | **Critical** | **Critical** |
| Fiscal compliance (DIAN) | **Critical** | N/A | Moderate |
| Split payments | High | **Critical** | High |
| Loyalty/promotions | Moderate | **Critical** | High |
| Credit sales (ventas a crédito) | **Critical** | N/A | Moderate |
| Omnichannel | Moderate | **Critical** | Moderate |
| Product variants | High | High | **Critical** |
| Lot/batch/expiry | N/A for retail | Low | **Critical** |
| Advanced reporting | Moderate | High | High |
| Public API/webhooks | High | **Critical** | High |

For detailed competitive capability matrices, payment ecosystem, hardware integration, LatAm integrations, multi-vertical readiness, and Colombian tax compliance status, see [PLAN.md](./PLAN.md) §6.3–6.8.

---

## 7. Reference Architecture Notes (summaries — full designs in PLAN.md)

### Credit Sales (Ventas a Crédito) Design

See Phase 5 Extension for full data model. Key concepts:
- **Cuotas**: Installment schedule generated at time of credit sale
- **Abonos**: Partial payments posted against oldest pending installments
- **Configuration**: per tenant → company → site (most specific wins)
- **Colombian legal**: Credit sales must generate Factura Electrónica (not DEE). Interest bounded by Superfinanciera usury rate.

### Country-Parametrizable Fiscal Rules Design

See Phase 11 Extension. Key architecture:
- `country_fiscal_profiles` table with JSON columns for tax, tip, e-invoicing, withholding, regime config
- `company_fiscal_overrides` for deviations from country defaults
- Fiscal adapter factory: `getFiscalAdapter(profile.adapter)` selects runtime adapter
- Tax engine becomes pure function: `computeTax(lineItems, profile)`
- Colombia behavior preserved as "CO" profile with zero behavior change

### Hybrid Data Architecture

Recommended: PowerSync for sync layer + dual schema from shared types (near-term).
- **Near term**: Keep SQLite local, formalize sync API, remove direct `better-sqlite3` dependency
- **Mid term**: PostgreSQL as remote truth, SQLite as offline working set
- **Long term**: Support standalone SQLite, managed remote SQLite, and PostgreSQL-backed topologies

### Multi-Vertical Module Architecture

Pattern: Configuration-driven module activation (not separate apps).
- `vertical` field on sites selects the active module
- JSON `metadata` on products stores vertical-specific attributes
- `SiteCapabilities` React Context drives conditional rendering
- Each module exports tRPC router + schema + capabilities declaration
- Checkout pipeline is configurable per vertical

---

## 8. Migration History

The migration from WinForms to Electron + React + Fastify is **functionally complete**.

**What was migrated**: desktop shell, embedded backend, tRPC transport, admin/catalog/product/pricing/inventory/sales/procurement/dashboard/export/reporting modules.

**What the current repo added beyond the original plan**: geography hierarchy, customer classification, locations, provider categories, orders with receive-into-purchase, tenant logo library, sale refunds, desktop backup/restore/tray/theme/update/print, merged sync conflict resolution.

When legacy references conflict with current repo: trust `apps/` and `packages/server/` code first, then this document.
