# Puntovivo — Sprint Plan

> Tactical, iteration-level execution plan. Each iter here is **one ENG
> ticket** in [docs/ROADMAP.md §3b](./ROADMAP.md), with the extra
> granularity that ROADMAP intentionally keeps out of the table:
> commit sequencing, per-commit file list, edge-case coverage, verification
> matrix, and draft commit messages. ROADMAP remains the canonical ticket
> list; this file is the execution checklist the agent opens next to it.

Always work one iter at a time, in the order of the "Recommended sequence"
below. Do not interleave commits between iters. The first line of the
chat when starting an iter is `Executing <ENG-NNN> — <one-liner>`.

## 1. Status at a glance (2026-05-02)

This table mirrors the authoritative `Status` column in
[ROADMAP §3b](./ROADMAP.md). When discrepancies appear, ROADMAP wins.

| Iter | Ticket | Status | Scope |
|---|---|---|---|
| Iter 1 | (doc-only, no ENG) | Shipped | Design stubs + Mermaid architecture + 12 follow-up docs |
| Iter 2 | (pre-ENG numbering, follow-up in `ENG-016`) | Shipped | Receipt templates — editor + renderer. See [RECEIPT-TEMPLATES.md](./RECEIPT-TEMPLATES.md). ENG-016 follow-ups landed across pass 1 (items 4/5/6 — bindings captions + appFooter + FLIP), pass 2 (item 1 — dnd-kit drag-and-drop), pass 3 (item 3 — template functions: parser + 12-fn whitelist + Zod AST validator + editor cheat-sheet), pass 4 (item 2 + item 7 parser-side markers — CodeMirror 6 editor with custom StreamLanguage tokenizer, namespace/property/function autocomplete, `{{` `}}` auto-close, and inline error markers via @codemirror/lint), and pass 5 (item 7 runtime hints — `receiptTemplates.variableAvailability` admin endpoint + CM6 Decoration extension that dims unset variable tokens with translatable hover tooltips). |
| — | [`ENG-015`](./ROADMAP.md) | Shipped | Dev seed command. See [DEV-SEED.md](./DEV-SEED.md). |
| Iter 6 | [`ENG-018`](./ROADMAP.md) | Shipped | Sales park-and-resume (multi-cart workspace). Server + audit + UI shipped across 5d8f720 / 05eea2d / 38e2a47 / ENG-018b diff. Spec §3. |
| Iter 6b | [`ENG-018b`](./ROADMAP.md) | Shipped | ENG-018 UI follow-up: Zustand multi-cart workspace, SuspendedSalesPanel, suspend/new-sale/resume controls, Ctrl+P / Ctrl+R / Ctrl+Shift+P shortcuts, close-session drafts warning, row-selection in history, E2E round-trip. |
| Iter 6c | [`ENG-018c`](./ROADMAP.md) | Shipped | Server: `sales.completeDraft` + `sales.discardDraft` stock reversal + ownership gate widen. Shipped 38e2a47. |
| Iter 7 | [`ENG-019`](./ROADMAP.md) | Shipped | Sales receipt reprint — server 5d8f720 + UI 05eea2d. Spec §4. Ctrl+Shift+P + history row selection landed as part of ENG-018b. |
| Iter 3 Fase A | [`ENG-020`](./ROADMAP.md) | Shipped | Fiscal DIAN modeling + MockAdapter. Spec §5. |
| — | [`ENG-017`](./ROADMAP.md) | Shipped | Country / locale / currency configuration — 3 catalog tables, resolver service, LocaleProvider, admin CompanyLocaleSettingsCard. See [LOCALE-CURRENCY.md](./LOCALE-CURRENCY.md). |
| Iter 4 | [`ENG-022`](./ROADMAP.md) | Gated | Hardware POS (test lab hardware). Spec §6. |
| Iter 3 Fase B | [`ENG-021`](./ROADMAP.md) | Gated | Fiscal DIAN PT integration (PT contract). Spec §7. |
| Iter 5 | [`ENG-023`](./ROADMAP.md) | Gated | Bold payment terminal (depends on Iter 4 + Bold sandbox). Spec §8. |
| Iter 8 | [`ENG-024`](./ROADMAP.md) | Deferred | Inter-site transfer requests + `reserved` lifecycle. Spec §9. |
| Plan v2.0 | [`ENG-025..ENG-040`](./ROADMAP.md) | Partial (11 / 16 closed, 2 partial) | 16-ticket phased plan promoted from the 2026-Q2 audit (security + code-quality + dependency + market intelligence). Phase 0 hardening (`ENG-025..029`), Phase 1 AI Wave 1 (`ENG-030..033`, Vercel AI SDK + Anthropic), Phase 2 multi-country fiscal engine (`ENG-034..036`, MX + CL packs), Phase 3 sync + LATAM payment rails (`ENG-037..038`), Phase 4 restaurant vertical + AI Wave 2 (`ENG-039..040`). Per-quarter spec in [PLAN-V2.md](./PLAN-V2.md). **Closed so far**: `ENG-025` (critical security closure, commits `5734d0a` + `6471007`), `ENG-026` (Vite 8 + plugin-react 6 + `@types/node` 24), `ENG-027` (dead code + dep hygiene — stale exports/test factories + `@tanstack/react-virtual` removed), `ENG-028` (cross-cutting helpers: invalidateGroups + sumBy + onErrorToast — net 325 LOC reduction across 32 feature files), `ENG-030` (AI-FOUNDATION — Vercel AI SDK v6 + Anthropic provider via Strategy/Factory pattern + `ai_audit_log` table + per-tenant budget + admin `CompanyAISettingsCard`), `ENG-031` (AI conversational analytics co-pilot — manager/admin `/co-pilot`, server-side tool calling with `stepCountIs(5)`, bounded tenant-scoped SQLite `:memory:` analytics snapshot, SQL/table/chart UI), `ENG-032` (AI anomaly + fraud detection — local-only z-score with leave-one-out, four detectors covering voids/refunds/no-sale-opens/ticket-spikes, dashboard tile + drill-down modal + Spanish-language design doc `docs/AI-ANOMALY-DETECTION.md`), and `ENG-033` (AI semantic product search + auto-categorize — embeddings via OpenAI `text-embedding-3-small`, cosine similarity en JS, `products.semanticSearch` + `regenerateEmbeddings` + `suggestCategory` tRPC procedures, Spanish doc `docs/AI-SEMANTIC-SEARCH.md`; UI integration shipped en out-of-band ENG-048), and `ENG-034` (FISCAL-CORE refactor pluggable — `services/fiscal/registry.ts` ahora es factory typed por `countryCode`; Colombia migrada a `packs/co/` + `ColombiaMockAdapter` rename; MX y CL parked como `NotImplementedAdapter` apuntando a ENG-035 / ENG-036; nuevo `validateConfig` en el contract + `FISCAL_PACK_NOT_AVAILABLE` error code; espejo del AI provider Strategy/Factory de ENG-030 + ENG-044). y `ENG-035a` (Pack México fundación — primer slice de ENG-035: validador `validateRfc` puro con checksum SAT módulo 11 + lista negra + RFCs genéricos, cuatro catálogos SAT como TS modules, namespace `tenants.settings.fiscal.mx.*`, `MexicoCFDIAdapter` reemplaza al stub con `validateConfig` real, router `fiscal.settings.{getByCountry, updateMx}`, tab `Fiscal` en `/company` con `CompanyMxFiscalCard` + readiness badge; emisión XML real diferida a ENG-035b, PAC + firma a ENG-035c), y `ENG-036a` (Pack Chile fundación — primer slice de ENG-036, espejo estructural de ENG-035a: validador `validateRut` puro con algoritmo SII módulo 11 + DV K + RUT genérico extranjero, tres catálogos SII como TS modules — `tipoDte` con 7, `giroComercial` curado con 26, `comuna` curada con 35 + `COMUNA_FALLBACK=13101`, namespace `tenants.settings.fiscal.cl.*`, `ChileSIIAdapter` reemplaza al stub con `validateConfig` real, router extendido con `fiscal.settings.updateCl`, `CompanyClFiscalCard` paralela a la MX, dispatch entre cards por país movido a `CompanyPage.tsx`; emisión XML DTE real diferida a ENG-036b, certificación SII + firmado XAdES + entrega digital a ENG-036c), y `ENG-035b` (Pack México modelado XML CFDI 4.0 — segundo slice: nuevo serializer `cfdi40-xml.ts` genera comprobantes Anexo 20 estructuralmente válidos con root + Emisor + Receptor + Conceptos + Impuestos agregados + CfdiRelacionados; nuevo catálogo curado `claveProdServ` 40 entradas + fallback `01010101`; nuevo módulo `mappings.ts` con helpers puros para FormaPago/ClaveUnidad/ClaveProdServ/Traslado; `MexicoCFDIAdapter` salió del stub `NotImplementedFiscalAdapter` y ahora retorna `{cufe:uuid, status:pending, xmlRef:<xml>}`; orchestrator pobla nuevo `tenantSettings` + `issuerName` en el input; UI `FiscalDocumentXmlModal` admin-only con copiar + descargar `.xml`; dev seed `SEED_COUNTRY=mx` flippea el demo tenant a México; nueva dep `fast-xml-parser@^5.7` JS puro; XSD validation real, cancelación SAT explícita y complemento Pago 2.0 quedan parqueados a ENG-035c). y `ENG-036b` (Pack Chile modelado XML DTE 1.0 — segundo slice: nueva migración `0019_fiscal_cafs.sql` agrega tabla con partial unique idx `(tenant_id, tipo_dte) WHERE status='active'`; nuevo `caf-allocator.ts` con `allocateNextFolio` atómico + `peekActiveCaf` read-only; nuevo serializer `dte10-xml.ts` (~450 LOC, espejo estructural de cfdi40-xml.ts) emite XML DTE 1.0 con namespace SII v1.0 + Encabezado/IdDoc/Emisor/Receptor/Totales + Detalle 1..n + Referencia para NC + TED.DD completo + FRMT placeholder; mappings helper traduce paymentMethod→FmaPago, unit→UnmdItem, source+buyerHasRut→TipoDTE; `ChileSIIAdapter` sale del stub y `issue()` real retorna `{cufe='sii-cl:<RUT>:<TipoDTE>:<F>', status='pending', xmlRef=string}`; orchestrator pre-allocates inside writeTx → folio + fiscal_documents insert + outbox enqueue commit atómicos; dos errorCodes nuevos `CAF_NOT_AVAILABLE` + `CAF_EXHAUSTED`; tRPC `fiscalSettings.getActiveCaf` adminProcedure read-only; CompanyClFiscalCard muestra sección read-only "CAF activo"; SEED_COUNTRY=cl en dev-seed pobla fiscal.cl.* + inserta CAFs fixture fiscal_cafs (TipoDTE 33/39/61, folios 1..100); ~28 nuevos tests server. Certificación SII + firma XAdES + entrega digital quedan parqueados a ENG-036c). Phase 1 AI Wave 1 **completo (4/4)**; Phase 2 **iniciado (2/3)** — ENG-035 Partial-2-de-3 (slices a + b shipped, c parqueado por contrato PAC); ENG-036 ahora Partial-2-de-3 (slices a + b shipped, c parqueado por certificado SII). y `ENG-037` (libSQL/Turso embedded replicas spike Shipped 2026-05-08 — spike report at `docs/SPIKE-LIBSQL-TURSO.md` recommends **Defer (revisit after Phase 4)**. Three findings: (1) legacy Embedded Replicas are cloud-primary by default and conflict with ADR-0001 Local Store Authority; (2) current Turso Sync is still a beta surface whose documented conflict model is Last-Push-Wins, incompatible with Puntovivo manual handling for money/fiscal/audit/cash/inventory; (3) libSQL pure engine swap has not proven a simpler Electron 41 vs Node 22 native runtime than `better-sqlite3`. Recommended Phase 3+ shape: bespoke `sync_outbox` (ENG-064/064b shipped) + separate cloud-backup destination — not Turso replicas. Reopen triggers preserved in spike §2; implementation acceptance criteria preserved in spike §12. `ENG-038` is now Partial slices 1 + 2 (2026-05-11): slice 1 shipped rail manifest + deterministic adapters, `payment_outbox`, read-only `payments.*` tRPC and `/operations?tab=payments`. Slice 2 shipped the tenant credential model + admin settings UI — optional `validateConfig` on `PaymentRailAdapter`, per-rail `CREDENTIAL_FIELDS_BY_RAIL` descriptor, `services/payments/credentials.ts` storage helpers (masked projection never re-serves plaintext), admin `paymentSettings.{getAll, updateRail}` namespace under `tenants.settings.payments.<railId>.credentials.*`, new `/company?tab=payments` admin card with readiness badge + password inputs + Show/Hide toggle, diagnostic sanitizer extended with `merchantid`/`customerid`/`pkey`. Real provider API clients, statement-import cron, ≥95% nightly matching proof and admin retry/requeue flows remain. Phase 3 counter: 1/2 entries closed + ENG-038 Partial. y `ENG-040` is now Partial slices 1 + 1b (ENG-040a + slice 1b, 2026-05-11): slice 1 shipped the vision pipeline (AIProvider `visionModel?` for Anthropic + OpenAI, `services/ai/vision/invoice-ocr.ts`, `ai.extractInvoiceLines` managerOrAdmin, three new error codes, read-only `<InvoiceOcrPreviewModal>`). Slice 1b shipped line-to-product matching + cart pre-fill: new `services/ai/vision/invoice-line-matcher.ts` reuses ENG-033 embeddings via the new public `embedTexts` + `loadTenantProductEmbeddings` helpers (shared `SEMANTIC_SIMILARITY_FLOOR=0.3` exported); new `ai.matchInvoiceLines` mutation gated by `managerOrAdminProcedureWithModule('semantic-search')` returns `mode:'matched'` (top-1 product per line with hydrated base-unit info) or `mode:'unavailable'` (reason `ai-disabled` | `no-embeddings`) so the modal renders a hint instead of throwing; one audit-log row per batch (`feature='invoiceLineMatch'`). `<InvoiceOcrPreviewModal>` grew a "Asociar productos" CTA, similarity-badge column, and "Crear compra con coincidencias" footer button that pre-fills `PurchasesPage.cartItems` via a new `onMatchedLinesReady` callback (same shape as `ProductSearchDialog` → `mergePurchaseCartItem`). i18n `purchases.ocr.match.*` namespace en + es neutral LATAM tú with pluralized toast keys. 8 new server tests + 5 new web cases; ci:server 84.59/71.04/80.34/85.63, ci:web 79.08/70.52/77.37/80.39. ENG-040b slice 1 (Ollama provider activation, 2026-05-12) shipped on top: `services/ai/providers/ollama.ts` rewritten from `NotImplementedProvider` to a real `AIProvider` backed by `ollama-ai-provider-v2@^3.5.0` (pure JS, peerDep `ai@^6`). Chat + vision both flow through `createOllama({ baseURL })`; `OLLAMA_BASE_URL` env var override defaults to `http://localhost:11434`. `pricing.calculateCostUsd` returns 0 (local inference); `isConfigured()` true unconditionally. 12 new ollama.test.ts cases + registry.test.ts + ai-router.test.ts assertions flipped. Live smoke verified the AI settings card lists Ollama as enabled, the admin can save it, and the round-trip persists. Voice/Whisper transcript slice and the 10-invoice ≥80% accuracy benchmark remain. Phase 4 counter: 0/2 entries closed + ENG-040 Partial. |
| Out-of-band | [`ENG-049`](./ROADMAP.md) | Shipped | Purga UX minimalista — operator-driven. Header app-wide perdió kicker + h2 + tenant badge; sidebar brand sin kicker ni tagline; hero panels (Dashboard + SalesOverview) sin bloque kicker/headline/description (métricas + freshness sobreviven); 12 páginas estándar y `ResourcePage` (componente shared) sin description debajo del h1; cards fiscal MX/CL sin description redundante; ~30 keys i18n huérfanas borradas en/es como pareja. Convención fiscal-en-español NO aplica. Counter Plan v2.0 NO avanza. |
| Foundation Reset | [`ENG-050..ENG-070`](./ROADMAP.md) | **Complete (21 / 21 closed)** 🎉 | New sellability + foundation wave. **Closed so far**: `ENG-050` (roadmap truth sync + `SELLABILITY.md` go/no-go index), `ENG-051` (Foundation ADR pack), `ENG-052` (device registry + command envelope, two slices), `ENG-053` (operation journal + outbox kernel — migración 0012 + factory `createOutboxKernel<TStatus,TPayload>` + worker base `tickOutbox` + `BOUNDED_EXPONENTIAL_BACKOFF`), `ENG-054` (extract `completeSale` application service), `ENG-055` (extract sale lifecycle services — `returnSale`, `voidSale`, `discardDraft`), `ENG-056` (cash session aggregate boundary — `openCashSession` / `closeCashSession` / `recordCashMovement` use-cases + new `cashSessions.pendingChecks` query + `insertCashMovement` widened to full `CashMovementType`), `ENG-057` (fiscal outbox + contingency engine — migración `0013_fiscal_outbox.sql`, kernel-driven worker daemon, status mirror invariant; admin `reports.fiscal.retryDocument` para manual recovery), `ENG-058` (receipt fiscal finalization — Option A surgical fix con `qr-builder.ts` per-country QR payload builders, `RenderFiscal` extendido, multi-doc query, `FiscalStatusBadge`, `SaleDetailsFiscalBlock`, `receiptPrinter.ts` con `buildFiscalSection` async + qrcode lazy chunk; status copy es la SINGLE SOURCE OF TRUTH), `ENG-060` (peripheral registry + hardware ports — migración `0014_site_peripherals.sql` con partial unique idx `(tenant_id, site_id, kind) WHERE is_active = 1`, 4 contracts ReceiptPrinter/CashDrawer/BarcodeScanner/PaymentTerminal, registry Strategy/Factory, 2 default drivers `system` + `manual` que NO tocan el legacy `webContents.print()` IPC path, admin tRPC router con 6 procedures, web admin UI `/peripherals`; `hardware_outbox` deferred a ENG-062 — ADR-0003 amended), `ENG-061` (barcode scanner pipeline — pure parser con EAN-13/EAN-8/UPC-A checksum + GS1 prefix-2x weight/price labels; `KeyboardWedgeScannerAdapter` registrado; `products.lookupByBarcode` + `peripherals.activeForSite`; `useBarcodeWedgeListener` hook con keystroke-buffer + `interCharGapMs` reset; SalesPage wired via mergeCartItem + override quantity/price; admin form scanner driver picker muestra `Teclado HID (USB)`), y `ENG-062` (ESC/POS printer + cash drawer — migración `0015_hardware_outbox.sql` cierra el deferral de ENG-060 con status enum `queued/submitting/printed/failed/retrying/dead_letter` + kind enum `print-{receipt,fiscal-dee,quotation,kitchen-ticket}|kick-drawer`; pure byte builder `services/peripherals/escpos/byte-builder.ts` con `buildEscPosBytes(doc, opts)` emit init+codepage+align+wrap+drawer-pulse+cut + cp858 LATAM mapping; transport wrapper con Mock + TCP fully wired + USB/Serial stubs `DRIVER_NOT_IMPLEMENTED`; `EscPosReceiptPrinterAdapter` + `EscPosCashDrawerAdapter` registrados en `DRIVER_TABLE.printer.escpos` + `DRIVER_TABLE.cash_drawer.escpos`; `createHardwareWorker` mirror del fiscal worker boot-wired en `index.ts`; tres procedures `peripherals.printReceipt` (system-fallback / printed / fallback con outbox enqueue), `kickCashDrawer` (manager-only, no-drawer-registered signal), `peekHardwareOutbox` (manager-or-admin, ENG-065 stub); renderer wiring vía `printSaleReceipt(sale, {escposDispatcher, onEscposFallback})` Option A surgical en SaleDetailsModal — el legacy `print-receipt` IPC + receiptPrinter.test.ts 10 cases NO se modifican; SalesPage manager-gated `data-testid="sales-kick-drawer"` button visible solo cuando hay drawer escpos registrado + role manager/admin; PeripheralForm flips escpos `available: true` para printer + cash_drawer; i18n drop `driverHint.escpos` + add `sales.printer.{escposFailedFallback, kickDrawerCta, kickDrawerSuccess, kickDrawerFailed, noDrawerRegistered}` neutral LATAM tú; ADR-0003 deferral footer removido). y `ENG-064` (sync contract v1 — migración `0016_sync_contract_v1.sql` agrega `sync_outbox` mirror estructural de `fiscal_outbox`/`hardware_outbox` con kernel projection + 7 columnas de contrato (entityType, entityId, operation, conflictPolicy, idempotencyKey, deviceId, dependsOnOperationId, operationEventId); status enum queued/submitting/synced/conflict/retrying/dead_letter; partial unique idx idempotent que colapsa retries; data migration one-shot copia pending sync_queue rows con conflictPolicy auto-asignado por ADR-0004 manual list; pure manifest `services/sync/contract.ts` con SYNC_PAYLOAD_VERSION + SYNC_ENTITY_TYPES (45 entidades exhaustivas) + SYNC_CONFLICT_POLICY exhaustivo; `enqueueSync(ctx, args)` helper que reemplaza el inline insert pattern leyendo envelope context y resolviendo operation_event_id via lookup; tres procedures additivas `sync.{getContract, peekOutbox, retry}` operando sobre sync_outbox; ADR `0005-sync-payload-contract.md` documenta manifest + version-bump protocol; 27 tests nuevos cubren las 4 acceptance areas — ordering por priority+createdAt, retry reset solo de retrying/dead_letter, no-op para synced, duplicate suppression con partial unique, manual conflict marker en high-risk entities; counter 12/21 → 13/21. Writer rewrite (19 routers) + existing sync.* procedure cutover de sync_queue a sync_outbox parqueados a ENG-064b — la acceptance "Tests cover ordering/retry/dedup/manual-conflict for high-risk entities" se cumple via tests directos de enqueueSync + sync_outbox sin requerir el writer rewrite). y `ENG-064b` (writer cutover Shipped 2026-05-05 — los 19 routers + 4 application services + 1 dev seed flippean a `enqueueSync`, las 8 procedures legacy `sync.*` cortan a `sync_outbox`, migración `0017_drop_sync_queue.sql` dropea la tabla legacy, web cliente `services/storage/syncQueue.ts` se renombra a `offlineQueue.ts` para eliminar la colisión de nombre; no quedan referencias server-side activas a `syncQueue`/`sync_queue`; los literales restantes son migraciones históricas, narrativa ADR y el ObjectStore IndexedDB preservado del cliente). y `ENG-065a` (Operations Center foundation Shipped 2026-05-05 — nuevo `/operations` page con 3 health tabs: Sync (reusa `<CompanySyncCard />`), Fiscal (consume `reports.fiscal.list` con filtro de status + retry button admin-only), Device (peripherals + hardware_outbox tail con problemas-only filter + retry button admin-only via nueva `peripherals.retryHardwareOutbox` procedure); manager+admin gate en la página; `reports.fiscal.list` widened a managerOrAdmin para read; sidebar entry "Operaciones" en `sections.overview`; 16 web tests + 7 server tests; counter 14/21 → 15/21). y `ENG-065b` (Operations Center: Cash + Inventory Reconciliation Shipped 2026-05-06 — dos procedures nuevas `reports.cash.reconciliation` (managerOrAdmin, tenant-wide drop del per-(siteId, cashier) filter del legacy `cashSessions.report`) + `reports.inventory.discrepancies` (managerOrAdmin, single GROUP BY contra `inventory_balances` que detecta drift cache-vs-cache `products.stock` vs `Σ(on_hand)` con epsilon 0.001 — exactamente lo que `inventory.reconcileBalances` heals); 2 nuevos tabs `cash` + `inventory` en `/operations` page (TAB_KEYS extended a 5); `CashHealthPanel` renderiza 4 summary tiles + bySite + recentDiscrepancies con sign-coded badges; `InventoryHealthPanel` renderiza summary + drift table con admin-only "Reconciliar balances" button que dispara existing `inventory.reconcileBalances`; i18n namespace `operations.{cash,inventory}` agregado en + es neutral LATAM tú; 13 nuevos server tests + 12 nuevos web tests; sin migrations / sin tablas nuevas; counter 15/21 → 16/21; Payment slice carved out a `ENG-065d` (Gated en ENG-063)). y `ENG-065c` (Operations Center: Diagnostic Export Shipped 2026-05-06 — admin-only bulk export para support tickets; nuevas procedures `reports.diagnostics.{preview,export}` adminProcedure que retornan counts + bundle JSON con `operation_events` + `operation_effects` + 3 outboxes existentes (sync/fiscal/hardware) filtrados por rango de fechas, hard cap 10000 por tabla con warning surfacing si se alcanza; manifest.counts mantiene 5-name keyset locked (`payment_outbox=0` + `webhook_outbox=0` reservados para ENG-063 + ENG-070, additive sin breaking schemaVersion); 6º tab Diagnóstico en `/operations` con `DiagnosticExportPanel.tsx` que pinta date range + presets + preview tiles + checkboxes outbox + Descargar ZIP via `jszip` (nuevo dep, MIT, ~30KB gzipped); Blob download via URL.createObjectURL + anchor click + deferred revoke (mirror del CSV pattern existente); admin-only via adminProcedure server-side + preview + download buttons disabled con tooltip translated en cliente para manager; i18n namespace `operations.diagnostics.*` en + es neutral LATAM tú; 9 nuevos server tests + 9 nuevos web tests; sin migrations / sin tablas nuevas; counter 16/21 → 17/21). y `ENG-067` (Chaos and resilience suite Shipped 2026-05-06 — 5 nuevos test files que cubren los 5 scenarios sin coverage previo: `chaos-app-restart-after-sale.test.ts` (stale-claim sweep on fiscal+hardware worker bounce, fresh-claim preservation, sync worker daemon gap doc), `chaos-large-queues.test.ts` (10k rows split two-tenant, peek clamp + tenant scoping, indexed read <200ms ceiling), `chaos-duplicate-retry.test.ts` (sync_outbox cross-tenant safety, hardware_outbox idempotency gap doc, hardware cross-tenant), `chaos-provider-outage-recovery.test.ts` (StatefulStubAdapter fail-3-then-succeed full recovery, non-recoverable straight to dead_letter, empty-queue tick no-op), `useOfflineSync.chaos.test.tsx` (navigator.onLine reflection, no auto-sync offline, auto-sync on reconnect, buffer preservation + manual retry); cita explícita a los 2 scenarios ya cubiertos por tests anteriores (`sync.test.ts > sync.resolve`, `application-cashSessions-closeCashSession.test.ts`); 17 nuevos test cases chaos total; review close-out corrigió la inversión de prioridad del kernel común (`createOutboxKernel` ahora drena `priority DESC, createdAt ASC`) y agregó 1 test directo en `outbox-kernel.test.ts`, para 18 tests nuevos efectivos en el cierre; counter 17/21 → 18/21). y `ENG-067b` (`hardware_outbox` envelope-keyed idempotency Shipped 2026-05-06 — cierra el gap estructural que el chaos suite documentó: la migración `0018_hardware_outbox_idempotency.sql` agrega columna nullable `idempotency_key` + partial unique idx `(tenant_id, kind, idempotency_key) WHERE idempotency_key IS NOT NULL` mirror del shape ENG-064 sync_outbox; nuevo helper `services/peripherals/enqueue-hardware.ts::enqueueHardware()` mirror estructural de `enqueueSync()` que captura UNIQUE conflict y retorna `{deduped: true}`; los 2 sitios inline en `routers/peripherals.ts` cutover al helper; input schemas de `printReceipt` + `kickCashDrawer` extendidas con optional `idempotencyKey` (opt-in via input, NO upgrade a `criticalCommandProcedure` — primitiva queda lista para callers futuros); empty-string normalize via schema/helper; 8 cases nuevos en `enqueue-hardware.test.ts`; el chaos test `chaos-duplicate-retry.test.ts` flippeó (assertion `toHaveLength(2)` → `toHaveLength(1)` + `r2.deduped === true`); ADR-0003 nota de paridad con sync_outbox; counter 18/21 → 19/21). y `ENG-066` (Backup, restore, and local security Shipped 2026-05-07 — tres ejes de hardening + ADR-0006: (1) backup atómico via `better-sqlite3.backup()` + `PRAGMA integrity_check` + ZIP bundle con `local.db` + `device-id.txt` + `manifest.json` (nuevo helper `apps/desktop/src/main/backup/backup-bundle.ts`, jszip nuevo dep en desktop); (2) restore validado con detection por magic bytes + corruption rejection antes del swap (backwards-compat con backups raw `.db` legacy); (3) sanitization recursiva del diagnostic export — `services/diagnostics/sanitize.ts::sanitizePayload()` con denylist anchored de ~30 keys (password, token, jwt, apiKey, pan, cvv, cardnumber, certificate, etc.) que reemplaza valores con `[REDACTED]` y agrega `manifest.sanitized: true` + `redactedKeysByTable` al bundle ENG-065c. Plus architectural-lint nueva contra columnas PAN/CVV en `db/schema.ts`. ADR-0006 en `docs/architecture/0006-local-data-security.md` documenta threat model + decisiones de scope (encryption-at-rest deferred, allowlist-vs-denylist tradeoff, per-OS keychain integration deferred a ENG-063 con `@node-gyp-build/keytar` candidate). 15 server unit tests sanitizer + 4 integration tests + 15 desktop `node --test` cases backup-bundle + 2 lint cases. Counter 19/21 → 20/21). y `ENG-068` (Module activation kernel Shipped 2026-05-07 — kernel + 5 demo modules cierra la wave Foundation Reset. Manifest `services/modules/manifest.ts` es la fuente de verdad — `MODULE_IDS` tuple + `MODULES_MANIFEST` record con exhaustiveness `Record<ModuleId, ...>` que bloquea typos en compile time. Estado per-tenant vive en `tenants.settings.modules` (sin migración nueva, usa el blob JSON existente). Server middleware `createModuleGuard(moduleId)` retorna FORBIDDEN con `errorCode: 'MODULE_NOT_ACTIVATED'` + `details.moduleId`; pre-composed `adminProcedureWithModule` / `managerOrAdminProcedureWithModule` en `trpc/middleware/modules.ts`. tRPC namespace nuevo `modules.*` con tres procedures: `list` (managerOrAdmin), `getEffective` (tenantProcedure) full effective map para el renderer, `setActive` (criticalCommandAdminProcedure) que escribe atómicamente via `json_set` + audit log row `action='module.toggle'` resourceType `'tenant_module'` con before/after snapshot. Cinco demo modules wired end-to-end: copilot, operations-center, quotations, anomaly-detection, semantic-search — gates server-side + render-side. Default state `enabled: true` para los 5 → cero cambio de behavior visible. Renderer kernel en `apps/web/src/features/modules/`: `ModulesProvider`, `useIsModuleActive`, `RequireModule`. Admin tab nuevo `/company?tab=modules` con `CompanyModulesCard`. Error codes nuevos `MODULE_NOT_ACTIVATED` + `MODULE_UNKNOWN`. i18n namespace nuevo `modules.json` en + es neutral LATAM tú. Audit log enums extendidos. Dev seed inicializa `settings.modules` con los 5 ON. ADR-0007 documenta decisión de JSON-vs-tabla, scope deferral de fiscal flag consolidation a ENG-068b, soft-disable semantics. Counter Foundation Reset 20/21 → 21/21 — wave complete. y `ENG-069` (Multi-surface POS shell Shipped 2026-05-07 — surfaces-as-modules pattern lifted on top of ENG-068 kernel. Nueva `services/surfaces/manifest.ts` con `SURFACE_IDS` tuple + `SURFACES_MANIFEST` record exhaustivo `Record<SurfaceId, ...>`; POS Desktop es el default implícito (moduleId=null) y los 4 nuevos surfaces gate detrás de module ids dedicados. Modules manifest extendido con `pos-touch` / `kds` / `customer-display` / `mobile-waiter`, todos defaultEnabled=false. Nueva tRPC `surfaces.list` (managerOrAdmin) retorna manifest joined con module state. Renderer mirror + 4 layout shell components (`TouchShell`, `KdsShell`, `CustomerDisplayShell`, `MobileWaiterShell`) cada uno componiendo `<ProtectedRoute allowedRoles={salesRoles}>` + `<RequireModule fallback={<Navigate to="/dashboard" />}>` + `<Suspense>` alrededor de `<Outlet />`. 4 placeholder pages comparten un `SurfacePlaceholder` común con label + descripción + badge "Llega con ENG-039" + CTA "Volver al panel". `App.tsx` registra los 4 nuevos top-level routes FUERA de `MainLayout` para que cada shell controle el viewport completo. Sidebar nueva sección "Superficies" / "Surfaces" con 4 entries gated por los módulos (cuando los 4 están off el título de la sección se oculta vía el filtro existente). Nuevo i18n namespace `surfaces.json` en + es neutral LATAM tú; `nav.json` extendido con sección + 4 entries; `modules.json` extendido con 4 module entries. ADR-0007 subsección Surfaces apendizada. ~24 tests nuevos (server: surfaces-manifest exhaustiveness + surfaces-router + flip + cross-tenant + roles; web: surfaces manifest + 4 shell module-gate tests + 4 placeholder render tests). Foundation Reset wave count NO cambia (post-wave; expansion architecture). y `ENG-070` (Event-based public API + webhook foundation Shipped 2026-05-08 — cierra el trio expansion-architecture ENG-068/069/070 sobre el kernel ENG-068. Nueva migración `0020_webhook_outbox.sql` agrega la 5ª y última outbox per ADR-0003 con kernel-projection shape (status/attempts/next_retry_at/claim_token/locked_at) + partial unique idx `(tenant_id, event_type, idempotency_key) WHERE idempotency_key IS NOT NULL` espejo de ENG-067b para que un envelope replay colapse a una fila. Manifest `services/events/manifest.ts` cierra los 5 event types como `as const` con Zod payload schemas (`sale.completed/refunded`, `inventory.adjusted`, `cash_session.closed`, `fiscal_document.accepted`); `Record<PublicEventType, ZodSchema>` exhaustiveness + `PUBLIC_EVENTS_VERSION=1`. Projector puro `services/events/projector.ts::projectOperationEvent(op)` lee `operationKind` + `summary` y devuelve un `PublicEvent` o null — mapeo: `sales.create`/`sales.completeDraft` → `sale.completed`, `sales.returnSale` → `sale.refunded`, `inventory.adjustStock` → `inventory.adjusted`, `cashSessions.close` → `cash_session.closed`. Helper `enqueueWebhook(tx, args)` mirror estructural de enqueueSync/enqueueHardware con idempotency-collapse. Hook nuevo en `markOperationCompleted` que cuando status='succeeded' corre el projector + lee `tenants.settings.modules.events-api` (gate per-tenant) + enqueue best-effort sin bloquear el commit primario. `fiscal_document.accepted` proyectado separado vía `projectFiscalDocumentAccepted` para el path no command-driven del fiscal worker. Module nuevo `events-api` en MODULES_MANIFEST (defaultEnabled false; admins flippean per-tenant — existing tenants ven cero cambio en el ship). tRPC `events.{getContract, peekOutbox}` (managerOrAdmin) read-only — getContract devuelve manifest + per-field metadata, peekOutbox tail ordenado priority desc + createdAt asc. i18n nuevo `eventsApi.{label, description}` en + es neutral LATAM tú; CompanyModulesCard renderiza el toggle automáticamente desde el manifest extendido (10 módulos total). ADR-0003 footer del 5º outbox flippea a "shipped"; ADR-0007 nueva subsección Public events. HTTP delivery worker + subscriber URL config UI parqueados a ENG-070b — v1 ships ONLY el kernel (contract + projector + outbox + module gate). ~50 nuevos tests server. Counter §3b: ENG-068/069/070 expansion architecture trio cierra completo. Next: ENG-035c (parqueado contrato PAC), ENG-036c (parqueado certificado SII), ENG-038 (LATAM payment rails), ENG-070b (HTTP delivery worker). |
| Authority Node / Store Hub Mode | [`ENG-071..ENG-076`](./ROADMAP.md) | **Complete (6 / 6 closed; 1 deferred spike)** | Early support for two local authority shapes: default `device_local` and configurable `site_hub` + `hub_client`. `ENG-071` locked ADR-0008 + `AUTHORITY-NODE.md` — **Shipped 2026-05-11**, see ROADMAP §3b for the closeout summary; `ENG-072` makes the current device-local default explicit in runtime config — **Shipped 2026-05-08**, see ROADMAP §3b for the full summary; `ENG-073` productizes LAN Store Hub server mode — **Shipped 2026-05-09**, see ROADMAP §3b; `ENG-074` makes cashier terminals point to a hub — **Shipped 2026-05-09** (renderer + device + reachability + the carved-out `ENG-074b` hardware bridge all closed same day); `ENG-074b` shipped the terminal-attached printer / drawer dispatch — **Shipped 2026-05-09**, see ROADMAP §3b; `ENG-075` shipped pairing + Authority health on 2026-05-11; `ENG-076` is deferred until a pilot proves hub-client satellite offline writes are needed. Read [`AUTHORITY-NODE.md`](./AUTHORITY-NODE.md) before changing this runtime family. |
| Out-of-band | [`ENG-048`](./ROADMAP.md) | Shipped | Semantic search UI surface en `ProductsPage` — toggle pill "Semántica" (Sparkles), input dedicado con placeholder español "Buscar por significado (p. ej. lácteo bajo en grasa)", debounce 300ms, columna "Coincidencia" condicional con badge `${pct}%` en modo semántico, botón admin-only "Regenerar embeddings" con `RefreshCw` animado y toast `"Se generaron embeddings de N productos"`. Cae a `mode:'unavailable'` con hint traducido cuando AI off / proveedor sin embeddings; cae a búsqueda literal via `products.list` usando el mismo texto debounced, y mantiene la búsqueda literal client-side cuando el toggle está OFF (cero regresión). i18n bajo `products.semantic.*` + `products.table.{searchSemantic,match}` en + es (neutral LATAM tú). Conecta `semanticSearch` y `regenerateEmbeddings` a UI accionable; `suggestCategory` sigue como backend listo para un follow-up de auto-categorización en el modal de producto. Counter Plan v2.0 NO avanza. |
| Out-of-band | [`ENG-042`](./ROADMAP.md) | Shipped | Sync resilience UX — stale conflict handling + network error display + entity labels humanos. Server `sync.listConflicts` / `sync.pull` exponen `localRecordExists`; `sync.resolve` rechaza keepLocal/merged cuando el local row falta. Web `companySyncDisplay.ts` mapea entityType + operation a labels i18n; conflict modal gana flow "Discard Local Change". `translateServerError.isNetworkConnectivityError` silencia los `Failed to fetch` al boot del AuthProvider y los traduce a `errors:server.networkUnavailable`. Close-out (TOCTOU sweep): `findEntity` guard hoisted dentro de `db.transaction(...)`, errorCode `SYNC_LOCAL_RECORD_MISSING` reemplaza la prosa con copia neutral LATAM en/es, y `assertCashSessionStillOpen` agrega defense-in-depth en `sales.create` / `sales.returnSale` / `sales.completeDraft`. |
| Out-of-band | [`ENG-044`](./ROADMAP.md) | Shipped | OpenAI provider activation (chat) — abre `services/ai/providers/openai.ts` desde `notImplemented` stub a `AIProvider` real con `@ai-sdk/openai@^3.0.54`, default `gpt-4.1-mini` y pricing table de cuatro modelos (4.1-mini, 4.1, 4o, 4o-mini) verificada al ship time. Embeddings quedan parked para ENG-033. Tests: nuevo `openai.test.ts` cubre cost math, `cacheControlForSystemPrompt() === undefined`, `isConfigured()` lectura de env. `registry.test.ts` y `ai-router.test.ts` flippean expectativas (openai ahora `isImplemented=true`, las assertions de stub se mueven a ollama que sigue parked para ENG-040). `.env.example` AI section retitled `(ENG-030 + ENG-044)` con `OPENAI_API_KEY` activo. Counter Plan v2.0 NO avanza (out-of-band, no es uno de los 16 tickets originales). Desbloquea live-smoke de AI Wave 1 sin depender de Anthropic billing. |
| Out-of-band | [`ENG-047`](./ROADMAP.md) | Shipped | AI anomaly follow-ups — sidebar badge para alertas Alta en el ítem Dashboard, `detectAnomalies` ahora escribe `audit_logs` con `action='ai.anomaly.detected'` (ID determinístico para dedup intra-día), nueva tabla `ai_anomaly_snoozes` (migración `0008`) + procedure `ai.anomalies.snooze` que silencia un patrón por 1-90 días + botón en `AnomalyDetailsModal`. Cierra los tres BACKLOG bullets capturados durante el review de ENG-032 (sidebar badge, audit_logs persistence, snooze flow). |
| Out-of-band | [`ENG-046`](./ROADMAP.md) | Shipped | CI minutes reduction — desktop matrix (ubuntu+macos+windows) movida fuera del per-push CI a un `workflow_dispatch`-only en `.github/workflows/build-desktop.yml`. `ci.yml` job `desktop` queda ubuntu-only (sigue gateando boundary regressions). Inputs por OS (`linux`/`mac`/`win`) + `validate_only` para correr solo `ci:desktop` sin packaging. Concurrency group por actor evita runs duplicados. macOS bill at 10x contra free tier — push a main ahora gasta cero minutos macOS/Windows. BACKLOG bullet `[infra][ci]` removido. |
| Out-of-band | [`ENG-045`](./ROADMAP.md) | Shipped | Design system audit + tabbed `/company` — handoff de Claude Design (`Puntovivo Design System-handoff.zip`) reverse-engineered desde el repo, así que `theme.css` / `base.css` / `utilities.css` quedaron bit-idénticos y `components.css` solo difería en 28 líneas (`.btn-success`). Refactor de `/company` de un grid apilado de 8 cards a un segmented-control de 5 tabs (`General` / `Localización` / `Datos` / `Dispositivo` / `IA`) usando la recipe `.segmented-control` + `.segmented-tab` que ya vivía en `components.css`. Active tab URL-stateful (`?tab=ai`) y `AnomalyDetectionCard` ahora linkea directo al panel IA. Tests: `CompanyPage.test.tsx` (6 casos: 5 tabs renderizan, default lands en General, `?tab=ai` deep-link al panel IA, click cambia aria-selected + URL, device tab muestra las 4 cards, non-admin sin tablist). i18n `settings.company.tabs.*` en + es. Counter Plan v2.0 NO avanza. |

## 2. Recommended sequence

Value-per-day priority, skipping gated tickets:

1. **`ENG-051` — Foundation ADR pack** (Shipped). Locked the architecture decisions before mutating schema or extracting services: Local Store Authority, Command Envelope, Outbox taxonomy, and Conflict policy. See [`docs/architecture/`](./architecture/README.md).
2. **`ENG-052..ENG-056` — Foundation Reset core**. Device registry + idempotency, operation journal + outbox kernel, `completeSale` extraction, sale lifecycle extraction, and cash-session aggregate boundary. Run strictly in order against the ADRs in [`docs/architecture/`](./architecture/README.md).
3. **`ENG-057..ENG-058` — Fiscal readiness without PT gate**. Fiscal outbox + contingency engine first, then fiscal receipt finalization. `ENG-059` stays gated on the real DIAN provider package.
4. **`ENG-060..ENG-062` — Hardware readiness without payment gate**. Peripheral registry + ports, barcode scanner pipeline, then ESC/POS + drawer. Full hardware validation for `ENG-062` waits for the physical lab.
5. **`ENG-064..ENG-067` — Sync, operations, resilience, security**. Sync contract, Operations Center, chaos suite, then backup/restore/local-security hardening.
6. **`ENG-068..ENG-070` — Expansion architecture**. Module activation kernel, multi-surface shell, and event/webhook foundation only after the retail core is safer.
7. **`ENG-071..ENG-075` — Authority Node / Store Hub Mode** (Shipped). Multi-register retail now has the ADR + config contract, explicit `device_local` default, `site_hub` LAN server mode, `hub_client` terminal mode, local hardware bridge, and pairing/health. `ENG-076` stays deferred until a pilot proves terminal-side offline writes are worth the extra sync plane.
8. **Gated tickets** stay parked until their gates clear: `ENG-021`, `ENG-022`, `ENG-023`, `ENG-059`, `ENG-063`.
9. **Deferred / overlap**: `ENG-024` remains deferred; `ENG-010`, `ENG-011`, and `ENG-014` are revisited after `ENG-054..ENG-056` because the Foundation Reset overlaps their code-quality scope.

Per-quarter detail and architectural decisions closed by the 2026-Q2 audit live in [PLAN-V2.md](./PLAN-V2.md). Anything gated stops the flow and raises a question to the user — do not speculate a workaround.

### 2.1 Authority Node execution wave (`ENG-071..ENG-076`)

**One-liner**: keep `device_local` as the default Authority Node while adding explicit `site_hub` and `hub_client` runtime modes for multi-register stores.

| Ticket | Commit shape | Verification |
| --- | --- | --- |
| `ENG-071` | Docs/ADR only: ADR-0008, `AUTHORITY-NODE.md`, ROADMAP/SPRINT links. **Shipped 2026-05-11** — see ROADMAP §3b for the closeout summary. | `git diff --check`; cross-doc links resolve. |
| `ENG-072` | Runtime config resolver + diagnostics/banners. No behavior change. **Shipped 2026-05-08** — see ROADMAP §3b. | `ci:server` + `ci:desktop`; default config test proves loopback `device_local`. |
| `ENG-073` | Store Hub LAN bind + allowed origins + health. **Shipped 2026-05-09** — see ROADMAP §3b. | `ci:server`; local smoke with `HOST=0.0.0.0` and explicit LAN origin; no open CORS wildcard. |
| `ENG-074` | Hub Client runtime config + tRPC base URL switch + reachability UI + checkout gate + carve-out hardware bridge. **Shipped 2026-05-09** — see ROADMAP §3b; carve-out `ENG-074b` shipped same day. | `ci:web` + live browser smoke; bridge override flips API URL to hub URL; checkout gates on `hubReachable === false`. |
| `ENG-074b` | Hub-client local hardware bridge (printer + drawer dispatch). **Shipped 2026-05-09** — see ROADMAP §3b. | `ci:server` + `ci:web` + `ci:desktop`; mocked local bridge prints a hub-authorized receipt without writing operational tables; integration with real USB printer for ENG-075 pilot. |
| `ENG-075` | Pairing codes, device roles, Operations Authority tab. **Shipped 2026-05-11** — see ROADMAP §3b. | `ci:server` + `ci:web` + live Operations smoke. |
| `ENG-076` | Deferred spike: fail-closed vs draft-only vs full satellite offline. | Decision doc only; no runtime writes. |

Scope rules:

- Do not support shared SQLite over network drives.
- Do not add hub-client local sale writes before `ENG-076` is reopened.
- The hub is the only writer in `site_hub`; clients send commands.
- Hub clients may execute terminal-local hardware I/O through a bridge,
  but that bridge must never write operational tables or emit
  operational outboxes independently.
- The central server remains downstream of Authority Nodes.

## 3. Iter 6 / ENG-018 — Sales park-and-resume

**One-liner**: Multi-cart workspace with `sales.suspend` / `sales.resume` / `sales.listDrafts` + persistent Zustand workspace + panel for suspended sales + Ctrl+P / Ctrl+R shortcuts.

**Domain context**: the backend already has `status='draft'` on `sales` (schema.ts:20). The UI does not: a cashier cannot suspend a cart to attend a different customer. This iter fills the UI gap and extends the backend with the missing procedures.

**3.1 Sequencing (3 commits)**:

1. **Commit 1 — server procedures + schema patches** (~1 day)
   - Extend `sales` with nullable `suspendedAt` (ISO), `suspendedBy` (FK users.id), `suspendedLabel` (optional human number). Raw DDL mirror + Drizzle migration + `ensureColumn`.
   - `sales.suspend({ saleId, label? })` — cashierProcedure, idempotent, persists `status='draft'` + `suspendedAt=now` + `suspendedBy=ctx.userId`. Optional audit log if tenant flag `audit_park_sale` is set.
   - `sales.resume({ saleId })` — cashierProcedure, SELECT FOR UPDATE (SQLite busy_timeout emulates), FORBIDDEN when `suspendedBy !== ctx.userId && ctx.role not in {manager, admin}`. Returns items + discounts + customer + notes and clears the suspended flags.
   - `sales.listDrafts({ filter: { site, cashier?, date? }, page, perPage })` — paginated. Default: cashier sees only their own; manager/admin sees all in the site.
   - `sales.discardDraft({ saleId })` — marks `status='cancelled'` and reverses the stock debited when the draft was created, scoped by tenant and original cash-session site.
   - Tests: suspend→resume round-trip preserves items/discounts/customer/notes; two cashiers cannot resume the same draft concurrently; cashier A cannot resume cashier B's draft but manager can; `listDrafts` respects role scope.

2. **Commit 2 — UI web** (~2 days)
   - `apps/web/src/features/sales/useCartWorkspace.ts` — Zustand store with `Record<draftId, Cart>` + `activeId` + `createDraft` / `suspendActive({ label? })` / `resume(id)` / `discard(id)` / `switchTo(id)`. Persist in `localStorage` keyed `${tenantId}:${userId}` to survive refresh.
   - Refactor `SalesPage.tsx` to read the active cart from the store instead of local `useState`.
   - "Suspender" button in `SalesCheckoutPanel` next to "Charge sale"; "Nueva venta" creates a blank draft.
   - `SuspendedSalesPanel.tsx` (new) — side panel with cards (number + customer + item count + total + age + resume / discard). Badge with count in the header.
   - Extend `useSalesKeyboardShortcuts.ts`: `Ctrl+P` / `Cmd+P` = suspend, `Ctrl+R` / `Cmd+R` = open resume panel. Guard against firing when focus is inside `<input>/<textarea>/[contenteditable]` (reuse existing `isEditableFocused` helper).
   - When `cashSessions.close` is invoked with outstanding drafts from the shift, prompt: "X ventas suspendidas. ¿Continuar de todos modos? Quedarán accesibles para el supervisor" with options "Descartar todos" / "Continuar".
   - i18n: extend `sales` namespace with `park.suspend`, `park.resume`, `park.discard`, `park.emptyState`, `park.shortcutHint`, `park.closedSessionWarning`, `park.confirmDiscardTitle`, `park.confirmDiscardMessage` in both locales.
   - Tests: suspend+resume round-trip preserves state in UI; `Ctrl+P` does not fire while a text input is focused; two concurrent cashiers see a clean error when racing the same draft; close-session prompt appears when drafts exist.
   - E2E in `e2e/web/business.spec.ts`: cashier creates cart A → suspends → creates cart B → charges B → resumes A → charges A. Assert both sales complete and stock decrement is correct per site.

3. **Commit 3 — docs + audit** (~0.5 days)
   - Add `sale.park` and `sale.resume` to `auditLogActionEnum` (string enum, no migration), gated by the `audit_park_sale` tenant flag.
   - Update `ROADMAP.md` ENG-018 row with "Shipped: <summary>".
   - Update `TEST-PLAN.md` marking PARK-01 through PARK-06 as automated.

**3.2 Edge cases**: empty listDrafts → empty state with CTA; skeleton in panel while loading; suspend network failure → toast + keep local cart; offline (Electron) works 100% against embedded SQLite; Zod rejects suspending a `status !== 'draft'` sale; cashier trying to resume someone else's draft gets a translated FORBIDDEN; parity test green.

**3.3 Draft commit messages**:

```
feat(pos): sales.suspend sales.resume sales.listDrafts procedures

- Extend sales table with suspendedAt suspendedBy suspendedLabel nullable columns
- Suspend is idempotent, resume enforces per-cashier lock with manager override
- Cover cross-cashier lock, role based scope, round trip field preservation
```

```
feat(pos): park and resume UI with multi cart workspace and shortcuts

- Add useCartWorkspace Zustand store persisted per user
- Add SuspendedSalesPanel with list, resume, discard actions
- Wire Ctrl P and Ctrl R shortcuts with input focus guard
- Warn when closing a cash session with outstanding drafts
- Add park.* keys to sales i18n in en and es
```

```
docs(pos): mark ENG-018 park and resume shipped
```

## 4. Iter 7 / ENG-019 — Sales receipt reprint

**One-liner**: `sales.getForReprint` procedure + reprint counter + audit `sale.reprint` + reprint button in history and sale details modal + Ctrl+Shift+P shortcut.

**Domain context**: `print-receipt` IPC handler (apps/desktop/src/main/index.ts:1708) accepts HTML and prints immediately, but no procedure exposes a past sale for reprint. When the printer runs out of paper or the hardware fails, the cashier cannot reprint from history — the sale is trapped behind a one-way flow.

**4.1 Sequencing (2 commits)**:

1. **Commit 1 — server + audit** (~1 day)
   - Add `reprintCount: integer default 0`, `lastReprintedAt: text`, `lastReprintedBy: text (FK users.id)` to `sales`. Raw DDL + `ensureColumn` + migration.
   - `sales.getForReprint({ saleId, reason? })` — cashierProcedure. Returns the full `saleRecord` (items + payments + customer snapshot when Iter 3 Fase A lands). Increments counter + timestamp + user. Emits `sale.reprint` audit log with metadata `{ reason?: string, count: number }`.
   - Permissions: cashier may reprint their own sales from the current shift; manager and admin may reprint anything within the tenant.
   - Tests: round-trip (getForReprint matches getById shape); counter increments; audit row emitted; permissions honored; cross-tenant isolated.

2. **Commit 2 — UI** (~1.5 days)
   - `SalesHistoryPage.tsx` — add "Reimprimir" row action with `Printer` icon (disabled without permission).
   - `SaleDetailsModal.tsx` — prominent "Reimprimir recibo" button that calls `getForReprint` first then `printSaleReceipt()`; optional reason dropdown (Paper out / Customer request / Previous print error / Other with free text).
   - Banner in the modal when `reprintCount > 0`: "Reimpresa {count} veces. Última: {lastReprintedAt} por {lastReprintedBy}".
   - Shortcut: with a row selected on `SalesHistoryPage`, `Ctrl+Shift+P` reprints (`Ctrl+P` is taken by Iter 6).
   - i18n: extend `sales` namespace with `reprint.button`, `reprint.title`, `reprint.reasonLabel`, `reprint.reasonOptions.*`, `reprint.confirmTitle`, `reprint.successToast`, `reprint.errorToast`, `reprint.historyBanner`, `reprint.noPermission`.
   - Tests: button visible with permission, disabled without, reprint round-trip preserves items/payments/totals, toast success, banner updates after reprint.
   - E2E extension: admin completes a sale → navigates to history → reprints → DB shows `reprintCount = 1` and audit log has the `sale.reprint` row.

**4.2 Edge cases**: skeleton while fetch; network failure → toast, no counter bump; Electron works 100% offline; Zod rejects invalid `saleId`; cashier trying to reprint another cashier's past-shift sale gets FORBIDDEN; `status='draft'` sale disables the button (those are Iter 6 suspended carts); `status='void'` allows reprint with an "ANULADA" watermark for trace.

**4.3 Integration with Iter 2 and Iter 4**: reuses the `receipt-renderer.ts` from Iter 2 (no duplication). If Iter 4 shipped and `EscPosPrinterAdapter` is configured, reprint uses ESC/POS natively and can open the cash drawer on reprint (useful at end-of-shift reconciliation). If Iter 7 lands before Iter 4, fallback path is `SystemPrinterAdapter` — zero tech debt.

**4.4 Draft commit messages**:

```
feat(sales): sales.getForReprint procedure with reprint counter and audit

- Add sales.getForReprint returning full sale snapshot for re-emission
- Track reprintCount lastReprintedAt lastReprintedBy on sales row
- Emit sale.reprint audit log with optional reason
- Cover permissions cashier-own vs manager-any and cross tenant isolation
```

```
feat(sales): reprint receipt action in history and sale details

- Add reprint button in SalesHistoryPage and SaleDetailsModal
- Capture optional reason and show reprint history banner
- Add Ctrl Shift P shortcut and reprint.* keys to sales i18n in en and es
- Cover round trip reprint, disabled states, void sale watermark
```

## 5. Iter 3 Fase A / ENG-020 — Fiscal DIAN data model + MockAdapter

**One-liner**: `fiscal_documents` + `fiscal_document_items` with immutable buyer and line snapshots + global `dian_identification_types` + `FiscalAdapter` interface + `MockAdapter` + hooks in `sales.complete`/`return`/`void` + architectural lint banning fiscal reports from joining with `customers`/`products`.

**Gates**: none. The MockAdapter is deterministic against DIAN Anexo 1.9 canonical vectors. **Depends on ENG-017 landing first** so the buyer currency snapshot has the right currency code.

**Full detail**: see [FISCAL-INTEGRATION.md](./FISCAL-INTEGRATION.md) and the five architectural decisions captured in that doc (§Modelamiento del adquiriente y líneas):

1. Global read-only `dian_identification_types` (CC=13, NIT=31, TI=12, CE=22, PA=41, RC=11, NUIP=91, TDE, PEP, PPT) — not tenant-scoped.
2. Immutable buyer snapshot in `fiscal_documents` (`buyerTaxId`, `buyerTaxIdTypeCode`, `buyerName`, `buyerEmail`, `buyerAddress`, `buyerCity`, `buyerDepartment`, `buyerCountry`, `buyerPersonType`, `buyerFiscalRegime`, `buyerFiscalResponsibility`).
3. Consumidor final for DEE POS = `buyerTaxId='222222222222'` + `buyerTaxIdTypeCode='31'` + `buyerName='Consumidor final'` — no ficticious customer row.
4. Immutable line snapshot in `fiscal_document_items` (`productName`, `productSku`, `unitMeasureCode`, `quantity`, `unitPrice`, `discountAmount`, `taxRate`, `taxAmount`, `taxCategoryCode`, `lineTotal`).
5. Architectural lint: vitest that parses `routers/reports/fiscal*` imports and fails on any reference to `customers` or `products`.

**5.1 Sequencing (5 commits, ~2 weeks)**:

1. **Commit 1** — Global `dian_identification_types` + seed + migration for `customers.dianIdentificationTypeId` (data migration by `abbr`).
2. **Commit 2** — `fiscal_documents` + `fiscal_document_items` + `fiscal_numbering_resolutions` + `fiscal_certificates` tables with all snapshot fields.
3. **Commit 3** — `FiscalAdapter` interface + `MockAdapter` with canonical CUFE SHA-384 and simulated contingency.
4. **Commit 4** — `fiscal-documents` orchestrator + hooks in `sales.complete`/`void`/`return` + dev seed for `fiscal_numbering_resolution` + `fiscal_certificate`.
5. **Commit 5** — `FiscalHabilitationWizard` placeholder + `FiscalContingencyIndicator` + `FiscalDocumentListPage` + `FiscalReportsPage` + architectural lint.

**5.2 Status**: **Shipped**. All five commits staged end-to-end:

- Commit 1 — migration `0004_dian_identification_types.sql` + `seedDianIdentificationTypes` (10 DIAN codes).
- Commit 2 — migration `0005_fiscal_documents.sql` + 4 fiscal tables with the immutable buyer + line snapshots in `schema.ts` + `fiscalDocumentsRelations` drizzle joins.
- Commit 3 — `services/fiscal/{cufe,adapter,mock-adapter,registry}.ts` + 13 tests (6 CUFE avalanche + canonical-order assertions, 7 MockAdapter issue/void/fetchStatus assertions).
- Commit 4 — `services/fiscal/orchestrator.ts` with `emitFiscalDocument` idempotent by `(tenantId, source, sourceId, kind)`; four hooks wired into `sales.ts` (`create` completed / `completeDraft` / `void` / `returnSale`) as best-effort post-tx calls behind `tenants.settings.fiscal_dian_enabled`. Dev seed now enables the flag and inserts one DEE resolution per site + placeholder cert for `demo-co`; 10 orchestrator integration tests cover site-scoped resolution lookup and rollback when fiscal persistence fails mid-write, plus extended seed-dev assertion (20 sales → 20 fiscal documents).
- Commit 5 — `trpc/routers/reports/fiscal.ts` + `reports/index.ts` aggregator mounted on `appRouter`, `trpc/schemas/fiscal.ts`, `architectural-lint.test.ts` (regex-based guard + synthetic positive test), `fiscal-reports.test.ts` (7 tests across list, paginated total count, getByCufe, cross-tenant isolation, FORBIDDEN, NOT_FOUND). Web UI: `FiscalDocumentListPage`, `FiscalReportsPage`, `FiscalHabilitationWizard`, header-mounted `FiscalContingencyIndicator`; `fiscal` i18n namespace registered en/es; admin-only `/fiscal-documents` and `/fiscal-reports` routes.

ENG-021 is the one-file swap of `MockAdapter` → `FactureAdapter`/`HkaAdapter` — the seams hold (FiscalAdapter interface + registry + hooks).

## 6. Iter 4 / ENG-022 — Hardware POS 🟡

**Gates**: physical test lab (thermal printer, USB HID scanner, RJ11 cash drawer) — do not start without confirmed hardware.

**One-liner**: `PrinterAdapter` with `system` and `escpos` drivers + cash drawer kick via ESC/POS + `useBarcodeScanner` hook (EAN-13 + price-embedded prefix 20-29) + peripherals configuration page.

**Full detail**: see [HARDWARE-POS.md](./HARDWARE-POS.md).

## 7. Iter 3 Fase B / ENG-021 — Fiscal DIAN PT integration 🟡

**Gates** (six, documented in [FISCAL-INTEGRATION.md](./FISCAL-INTEGRATION.md) §Pre-implementation checklist):
1. PT contract signed (Facture / HKA / Gosocket).
2. Sandbox + production credentials delivered.
3. Tenant pilot's DIAN digital certificate.
4. DIAN numbering resolution associated with the software.
5. POC validated out-of-repo: DEE + FEV + NC + ND + fetchStatus.
6. PT error-code → `ServerErrorWithCode` mapping agreed.

**Swap-only diff**: replaces `MockAdapter` with `FactureAdapter` / `HkaAdapter`. No domain changes beyond the adapter file.

## 8. Iter 5 / ENG-023 — Bold payment terminal 🟡

**Gates**: Bold sandbox credentials + Bluetooth device for tests. Also **depends on ENG-022 commit 1** which installs the `PaymentTerminalAdapter` interface.

**One-liner**: `BoldAdapter` calls the Bluetooth SDK for charge / void / printSlip; `SalePaymentModal` integrates the adapter with `ManualAdapter` fallback.

## 9. Iter 8 / ENG-024 — Inter-site transfer with reservation ⚪

**Status**: deferred proposal. The user surfaced this as a concern, not a firm requirement.

**Scope**: extend `transferOrderStatusEnum` with `requested`/`approved`/`rejected`; use `inventory_balances.reserved` (currently always 0); enable destination-initiated requests; add `initiatedBy`/`requestedBy`/`approvedBy`/`rejectedBy`/`rejectionReason`/`expectedArrivalAt`; state machine `create → requested → approved → in_transit → completed`; UI inbox + timeline. ~1.5 weeks.

## 10. Cross-iteration concerns

- **i18n parity** must stay green after each iter (`apps/web/src/i18n/locale-parity.test.ts`).
- **Drizzle migrations** — one migration per iter that touches schema; update `schema.ts`, generate the SQL migration, register the journal entry, and use `IF NOT EXISTS` when Drizzle's SQLite dialect cannot emit the construct (partial unique indexes, `WHERE` clauses). `db/index.ts` is no longer a raw-DDL mirror after ENG-002 Step 3.
- **Audit trail** — `auditLogActionEnum` is free-form strings; adding a new action never needs a migration. But `AuditLogsTable.tsx` needs an i18n key per action.
- **E2E suite** — 25+ tests in `e2e/web/`; every closing commit runs `npm run test:e2e:web` before pushing.
- **Electron boundary** — any peripheral or FS call goes through `ipcMain.handle` → `contextBridge.exposeInMainWorld('electron', {...})` → `window.electron.*`. Never `require('fs')` in the renderer.
- **Seed data** — when an iter adds a schema with defaults that matter for tests (like a fiscal resolution row), extend `packages/server/src/db/seed-dev.ts` so `npm run seed:dev` keeps producing a ready-to-demo dataset.

## 11. Verification matrix (per iter, before the closing commit)

| Check | Command | Must pass |
|---|---|---|
| Typecheck web | `npm run typecheck --workspace=@puntovivo/web` | ✓ |
| Typecheck server | `npm run typecheck --workspace=@puntovivo/server` | ✓ |
| Typecheck desktop | `npm run typecheck --workspace=@puntovivo/desktop` (if main changed) | ✓ |
| Lint web | `npm run lint --workspace=@puntovivo/web` | 0 new errors |
| Lint server | `npm run lint --workspace=@puntovivo/server` | 0 new errors |
| Tests server | `npm run test --workspace=@puntovivo/server -- --run` | all pass |
| Tests web | `npm run test --workspace=@puntovivo/web -- --run` | coverage ≥ 70/70/70/70 |
| i18n parity | included in web test run | ✓ |
| E2E | `npm run test:e2e:web` | all pass |
| CI aggregate | `npm run ci:web` and `npm run ci:server` in parallel | ✓ |
| Smoke web | manual boot `dev:web` + `dev:server`, driven with Playwright MCP | concrete strings asserted |
| Smoke Electron | `npm run dev:desktop` + validate feature + IPC when applicable | ✓ (or explicit gap declared) |
| Review skills | `typescript-react-reviewer` + `node` in parallel on the diff | zero HIGH blockers |

## 12. Update protocol

When an iter closes, do three things in the last commit:

1. Update this file: move the iter row in §1 to "✓ Shipped", shorten the full iter section to a one-sentence summary + "Shipped <date> — see <ENG-NNN>", link to the closing commit.
2. Update the matching row in `docs/ROADMAP.md` §3b: append "Shipped: <2-3 line summary>" to the Description column (matching the existing ENG-003..009 style).
3. If the iter introduced new docs (spec, runbook), add them to `docs/PLAN.md` §18.1 "Already landed".
