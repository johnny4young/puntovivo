# Puntovivo — Sprint Plan

> Tactical, iteration-level execution plan. Each iter here is **one ENG
> ticket** in [docs/ROADMAP.md §3b](./ROADMAP.md), with the extra
> granularity that ROADMAP intentionally keeps out of the table:
> commit sequencing, per-commit file list, edge-case coverage, verification
> matrix, and draft commit messages. ROADMAP remains the canonical ticket
> list; this file is the execution checklist the agent opens next to it.

Always work one iter at a time, in the order of the "Recommended sequence"
below. Do not interleave commits between iters. The first line of the
chat when starting an iter is `Executing <ENG-NNN> — <one-liner>`.

## 1. Status at a glance (2026-05-02)

This table mirrors the authoritative `Status` column in
[ROADMAP §3b](./ROADMAP.md). When discrepancies appear, ROADMAP wins.

| Iter | Ticket | Status | Scope |
|---|---|---|---|
| Iter 1 | (doc-only, no ENG) | Shipped | Design stubs + Mermaid architecture + 12 follow-up docs |
| Iter 2 | (pre-ENG numbering, follow-up in `ENG-016`) | Shipped | Receipt templates — editor + renderer. See [RECEIPT-TEMPLATES.md](./RECEIPT-TEMPLATES.md). ENG-016 follow-ups landed across pass 1 (items 4/5/6 — bindings captions + appFooter + FLIP), pass 2 (item 1 — dnd-kit drag-and-drop), pass 3 (item 3 — template functions: parser + 12-fn whitelist + Zod AST validator + editor cheat-sheet), pass 4 (item 2 + item 7 parser-side markers — CodeMirror 6 editor with custom StreamLanguage tokenizer, namespace/property/function autocomplete, `{{` `}}` auto-close, and inline error markers via @codemirror/lint), and pass 5 (item 7 runtime hints — `receiptTemplates.variableAvailability` admin endpoint + CM6 Decoration extension that dims unset variable tokens with translatable hover tooltips). |
| — | [`ENG-015`](./ROADMAP.md) | Shipped | Dev seed command. See [DEV-SEED.md](./DEV-SEED.md). |
| Iter 6 | [`ENG-018`](./ROADMAP.md) | Shipped | Sales park-and-resume (multi-cart workspace). Server + audit + UI shipped across 5d8f720 / 05eea2d / 38e2a47 / ENG-018b diff. Spec §3. |
| Iter 6b | [`ENG-018b`](./ROADMAP.md) | Shipped | ENG-018 UI follow-up: Zustand multi-cart workspace, SuspendedSalesPanel, suspend/new-sale/resume controls, Ctrl+P / Ctrl+R / Ctrl+Shift+P shortcuts, close-session drafts warning, row-selection in history, E2E round-trip. |
| Iter 6c | [`ENG-018c`](./ROADMAP.md) | Shipped | Server: `sales.completeDraft` + `sales.discardDraft` stock reversal + ownership gate widen. Shipped 38e2a47. |
| Iter 7 | [`ENG-019`](./ROADMAP.md) | Shipped | Sales receipt reprint — server 5d8f720 + UI 05eea2d. Spec §4. Ctrl+Shift+P + history row selection landed as part of ENG-018b. |
| Iter 3 Fase A | [`ENG-020`](./ROADMAP.md) | Shipped | Fiscal DIAN modeling + MockAdapter. Spec §5. |
| — | [`ENG-017`](./ROADMAP.md) | Shipped | Country / locale / currency configuration — 3 catalog tables, resolver service, LocaleProvider, admin CompanyLocaleSettingsCard. See [LOCALE-CURRENCY.md](./LOCALE-CURRENCY.md). |
| Iter 4 | [`ENG-022`](./ROADMAP.md) | Gated | Hardware POS (test lab hardware). Spec §6. |
| Iter 3 Fase B | [`ENG-021`](./ROADMAP.md) | Gated | Fiscal DIAN PT integration (PT contract). Spec §7. |
| Iter 5 | [`ENG-023`](./ROADMAP.md) | Gated | Bold payment terminal (depends on Iter 4 + Bold sandbox). Spec §8. |
| Iter 8 | [`ENG-024`](./ROADMAP.md) | Deferred | Inter-site transfer requests + `reserved` lifecycle. Spec §9. |
| Plan v2.0 | [`ENG-025..ENG-040`](./ROADMAP.md) | Partial (13 / 16 closed, 1 partial) | 16-ticket phased plan promoted from the 2026-Q2 audit (security + code-quality + dependency + market intelligence). Phase 0 hardening (`ENG-025..029`), Phase 1 AI Wave 1 (`ENG-030..033`, Vercel AI SDK + Anthropic), Phase 2 multi-country fiscal engine (`ENG-034..036`, MX + CL packs), Phase 3 sync + LATAM payment rails (`ENG-037..038`), Phase 4 restaurant vertical + AI Wave 2 (`ENG-039..040`). Per-quarter spec in [PLAN-V2.md](./PLAN-V2.md). **Closed so far**: `ENG-025` (critical security closure, commits `5734d0a` + `6471007`), `ENG-026` (Vite 8 + plugin-react 6 + `@types/node` 24), `ENG-027` (dead code + dep hygiene — stale exports/test factories + `@tanstack/react-virtual` removed), `ENG-028` (cross-cutting helpers: invalidateGroups + sumBy + onErrorToast — net 325 LOC reduction across 32 feature files), `ENG-030` (AI-FOUNDATION — Vercel AI SDK v6 + Anthropic provider via Strategy/Factory pattern + `ai_audit_log` table + per-tenant budget + admin `CompanyAISettingsCard`), `ENG-031` (AI conversational analytics co-pilot — manager/admin `/co-pilot`, server-side tool calling with `stepCountIs(5)`, bounded tenant-scoped SQLite `:memory:` analytics snapshot, SQL/table/chart UI), `ENG-032` (AI anomaly + fraud detection — local-only z-score with leave-one-out, four detectors covering voids/refunds/no-sale-opens/ticket-spikes, dashboard tile + drill-down modal + Spanish-language design doc `docs/AI-ANOMALY-DETECTION.md`), and `ENG-033` (AI semantic product search + auto-categorize — embeddings via OpenAI `text-embedding-3-small`, cosine similarity en JS, `products.semanticSearch` + `regenerateEmbeddings` + `suggestCategory` tRPC procedures, Spanish doc `docs/AI-SEMANTIC-SEARCH.md`; UI integration shipped en out-of-band ENG-048), and `ENG-034` (FISCAL-CORE refactor pluggable — `services/fiscal/registry.ts` ahora es factory typed por `countryCode`; Colombia migrada a `packs/co/` + `ColombiaMockAdapter` rename; MX y CL parked como `NotImplementedAdapter` apuntando a ENG-035 / ENG-036; nuevo `validateConfig` en el contract + `FISCAL_PACK_NOT_AVAILABLE` error code; espejo del AI provider Strategy/Factory de ENG-030 + ENG-044). y `ENG-035a` (Pack México fundación — primer slice de ENG-035: validador `validateRfc` puro con checksum SAT módulo 11 + lista negra + RFCs genéricos, cuatro catálogos SAT como TS modules, namespace `tenants.settings.fiscal.mx.*`, `MexicoCFDIAdapter` reemplaza al stub con `validateConfig` real, router `fiscal.settings.{getByCountry, updateMx}`, tab `Fiscal` en `/company` con `CompanyMxFiscalCard` + readiness badge; emisión XML real diferida a ENG-035b, PAC + firma a ENG-035c), y `ENG-036a` (Pack Chile fundación — primer slice de ENG-036, espejo estructural de ENG-035a: validador `validateRut` puro con algoritmo SII módulo 11 + DV K + RUT genérico extranjero, tres catálogos SII como TS modules — `tipoDte` con 7, `giroComercial` curado con 26, `comuna` curada con 35 + `COMUNA_FALLBACK=13101`, namespace `tenants.settings.fiscal.cl.*`, `ChileSIIAdapter` reemplaza al stub con `validateConfig` real, router extendido con `fiscal.settings.updateCl`, `CompanyClFiscalCard` paralela a la MX, dispatch entre cards por país movido a `CompanyPage.tsx`; emisión XML DTE real diferida a ENG-036b, certificación SII + firmado XAdES + entrega digital a ENG-036c), y `ENG-035b` (Pack México modelado XML CFDI 4.0 — segundo slice: nuevo serializer `cfdi40-xml.ts` genera comprobantes Anexo 20 estructuralmente válidos con root + Emisor + Receptor + Conceptos + Impuestos agregados + CfdiRelacionados; nuevo catálogo curado `claveProdServ` 40 entradas + fallback `01010101`; nuevo módulo `mappings.ts` con helpers puros para FormaPago/ClaveUnidad/ClaveProdServ/Traslado; `MexicoCFDIAdapter` salió del stub `NotImplementedFiscalAdapter` y ahora retorna `{cufe:uuid, status:pending, xmlRef:<xml>}`; orchestrator pobla nuevo `tenantSettings` + `issuerName` en el input; UI `FiscalDocumentXmlModal` admin-only con copiar + descargar `.xml`; dev seed `SEED_COUNTRY=mx` flippea el demo tenant a México; nueva dep `fast-xml-parser@^5.7` JS puro; XSD validation real, cancelación SAT explícita y complemento Pago 2.0 quedan parqueados a ENG-035c). y `ENG-036b` (Pack Chile modelado XML DTE 1.0 — segundo slice: nueva migración `0019_fiscal_cafs.sql` agrega tabla con partial unique idx `(tenant_id, tipo_dte) WHERE status='active'`; nuevo `caf-allocator.ts` con `allocateNextFolio` atómico + `peekActiveCaf` read-only; nuevo serializer `dte10-xml.ts` (~450 LOC, espejo estructural de cfdi40-xml.ts) emite XML DTE 1.0 con namespace SII v1.0 + Encabezado/IdDoc/Emisor/Receptor/Totales + Detalle 1..n + Referencia para NC + TED.DD completo + FRMT placeholder; mappings helper traduce paymentMethod→FmaPago, unit→UnmdItem, source+buyerHasRut→TipoDTE; `ChileSIIAdapter` sale del stub y `issue()` real retorna `{cufe='sii-cl:<RUT>:<TipoDTE>:<F>', status='pending', xmlRef=string}`; orchestrator pre-allocates inside writeTx → folio + fiscal_documents insert + outbox enqueue commit atómicos; dos errorCodes nuevos `CAF_NOT_AVAILABLE` + `CAF_EXHAUSTED`; tRPC `fiscalSettings.getActiveCaf` adminProcedure read-only; CompanyClFiscalCard muestra sección read-only "CAF activo"; SEED_COUNTRY=cl en dev-seed pobla fiscal.cl.* + inserta CAFs fixture fiscal_cafs (TipoDTE 33/39/61, folios 1..100); ~28 nuevos tests server. Certificación SII + firma XAdES + entrega digital quedan parqueados a ENG-036c). Phase 1 AI Wave 1 **completo (4/4)**; Phase 2 **iniciado (2/3)** — ENG-035 Partial-2-de-3 (slices a + b shipped, c parqueado por contrato PAC); ENG-036 ahora Partial-2-de-3 (slices a + b shipped, c parqueado por certificado SII). y `ENG-037` (libSQL/Turso embedded replicas spike Shipped 2026-05-08 — spike report at `docs/SPIKE-LIBSQL-TURSO.md` recommends **Defer (revisit after Phase 4)**. Three findings: (1) legacy Embedded Replicas are cloud-primary by default and conflict with ADR-0001 Local Store Authority; (2) current Turso Sync is still a beta surface whose documented conflict model is Last-Push-Wins, incompatible with Puntovivo manual handling for money/fiscal/audit/cash/inventory; (3) libSQL pure engine swap has not proven a simpler Electron 41 vs Node 22 native runtime than `better-sqlite3`. Recommended Phase 3+ shape: bespoke `sync_outbox` (ENG-064/064b shipped) + separate cloud-backup destination — not Turso replicas. Reopen triggers preserved in spike §2; implementation acceptance criteria preserved in spike §12. `ENG-038` is now Partial slices 1 + 2 (2026-05-11): slice 1 shipped rail manifest + deterministic adapters, `payment_outbox`, read-only `payments.*` tRPC and `/operations?tab=payments`. Slice 2 shipped the tenant credential model + admin settings UI — optional `validateConfig` on `PaymentRailAdapter`, per-rail `CREDENTIAL_FIELDS_BY_RAIL` descriptor, `services/payments/credentials.ts` storage helpers (masked projection never re-serves plaintext), admin `paymentSettings.{getAll, updateRail}` namespace under `tenants.settings.payments.<railId>.credentials.*`, new `/company?tab=payments` admin card with readiness badge + password inputs + Show/Hide toggle, diagnostic sanitizer extended with `merchantid`/`customerid`/`pkey`. Real provider API clients, statement-import cron, ≥95% nightly matching proof and admin retry/requeue flows remain. Phase 3 counter: 1/2 entries closed + ENG-038 Partial. y `ENG-040` is now Partial slices 1 + 1b (ENG-040a + slice 1b, 2026-05-11): slice 1 shipped the vision pipeline (AIProvider `visionModel?` for Anthropic + OpenAI, `services/ai/vision/invoice-ocr.ts`, `ai.extractInvoiceLines` managerOrAdmin, three new error codes, read-only `<InvoiceOcrPreviewModal>`). Slice 1b shipped line-to-product matching + cart pre-fill: new `services/ai/vision/invoice-line-matcher.ts` reuses ENG-033 embeddings via the new public `embedTexts` + `loadTenantProductEmbeddings` helpers (shared `SEMANTIC_SIMILARITY_FLOOR=0.3` exported); new `ai.matchInvoiceLines` mutation gated by `managerOrAdminProcedureWithModule('semantic-search')` returns `mode:'matched'` (top-1 product per line with hydrated base-unit info) or `mode:'unavailable'` (reason `ai-disabled` | `no-embeddings`) so the modal renders a hint instead of throwing; one audit-log row per batch (`feature='invoiceLineMatch'`). `<InvoiceOcrPreviewModal>` grew a "Asociar productos" CTA, similarity-badge column, and "Crear compra con coincidencias" footer button that pre-fills `PurchasesPage.cartItems` via a new `onMatchedLinesReady` callback (same shape as `ProductSearchDialog` → `mergePurchaseCartItem`). i18n `purchases.ocr.match.*` namespace en + es neutral LATAM tú with pluralized toast keys. 8 new server tests + 5 new web cases; ci:server 84.59/71.04/80.34/85.63, ci:web 79.08/70.52/77.37/80.39. ENG-040b slice 1 (Ollama provider activation, 2026-05-12) shipped on top: `services/ai/providers/ollama.ts` rewritten from `NotImplementedProvider` to a real `AIProvider` backed by `ollama-ai-provider-v2@^3.5.0` (pure JS, peerDep `ai@^6`). Chat + vision both flow through `createOllama({ baseURL })`; `OLLAMA_BASE_URL` env var override defaults to `http://localhost:11434`. ENG-040b slice 2 (Ollama embeddings activation, 2026-05-12) added `defaultEmbeddingModelId?: string` to the `AIProvider` contract (OpenAI=`text-embedding-3-small`, Ollama=`nomic-embed-text`), implemented `ollamaProvider.embeddingModel(modelId)` via the SDK's `embedding(modelId)` factory, and wired the `embedText` + `embedTexts` resolver to read the per-provider default. Now ENG-033 semantic search runs fully offline for Ollama tenants once they `ollama pull nomic-embed-text` + click "Regenerate embeddings". ENG-040 embedding-model drift admin banner (2026-05-12) shipped on top: new `resolveActiveEmbeddingModelId(db, tenantId)` helper centralises the active model resolution that `embedText` + `embedTexts` already performed inline; new `products.embeddingHealth` tRPC query under `managerOrAdminProcedureWithModule('semantic-search')` returns `{mode, activeModelId, totalProducts, embeddedCount, unembeddedCount, staleCount, staleSampleModelIds, lastEmbeddedAt}`; new `apps/web/src/features/products/EmbeddingDriftBanner.tsx` rendered above the Products toolbar surfaces drift in warning-tone copy with an admin-gated "Regenerar embeddings" CTA that invalidates both `embeddingHealth` and `semanticSearch` on success; new i18n namespace `products.embeddingDrift.*` in + es neutral LATAM tú with pluralised hint. 9 new server tests + 5 new web cases. ci:server 84.66/71.06/80.45/85.74, ci:web 79.27/70.65/77.54/80.54. ENG-040c slice 1 (Whisper transcription pipeline, 2026-05-12) shipped on top: `AIProvider` contract gained three optional fields (`transcriptionModel?`, `defaultTranscriptionModelId?`, `transcriptionPricing?` with `perMinuteUsd`). OpenAI implements via `openai.transcription(modelId)`, default `whisper-1` ($0.006/min), pricing rows for the four supported Whisper variants. Anthropic + Ollama leave it undefined → capability gate surfaces `AI_VOICE_NOT_AVAILABLE`. New `services/ai/voice/transcribe.ts` mirrors the vision pipeline (10 MB raw budget after base64 decode, MediaRecorder MIME whitelist, reuses `resolveAISettings` + monthly-budget enforcement + `ai_audit_log` feature `voiceTranscribe`, computes cost as `audioSeconds/60 * perMinuteUsd` since Whisper bills per minute). New tRPC `ai.transcribeAudio` (managerOrAdmin). Three new error codes (`AI_VOICE_NOT_AVAILABLE`, `AI_VOICE_PARSE_FAILED`, `AI_VOICE_AUDIO_TOO_LARGE`) en + es neutral LATAM tú. 8 new server tests + 4 new OpenAI provider cases. ci:server 84.71/71.01/80.52/85.79, ci:web 79.32/70.71/77.61/80.6. ENG-040c slice 2 (Test transcription admin affordance, 2026-05-12) shipped on top: `ai.settings.get` exposes `transcriptionAvailable: boolean`; new `useVoiceRecorder` hook wraps MediaRecorder with MIME negotiation + 30s auto-stop + 3 error modes (permission-denied / no-microphone / unsupported-browser) + MediaStream track release; new `blobToBase64` helper converts to base64 + bare MIME; `CompanyAISettingsCard` gains a "Probar transcripción" button next to "Probar conexión" with countdown + inline transcript panel (transcript + language + duration + cost). ~13 i18n keys (en + es neutral LATAM tú). 4 hook tests + 4 component tests + 2 router test assertions for `transcriptionAvailable`. ENG-040c slice 3 (voice cart-command parser + shared modal, 2026-05-12) shipped on top: new `services/ai/voice/parse-cart-command.ts` runs `generateObject` against a bounded ADD-only Zod schema + resolves each `productHint` via the ENG-033 embeddings stack; new `ai.parseCartCommand` mutation under `tenantProcedureWithModule('semantic-search')` returns `mode='parsed' | 'unrecognized'`. `ai.transcribeAudio` widened to `tenantProcedureWithModule('semantic-search')` so cashier callers drive the flow. `useVoiceRecorder` + `blobToBase64` promoted to `apps/web/src/features/voice/`; new `VoiceCartCommandModal.tsx` ships the idle/recording/transcribing/parsing/reviewing state machine with an `onApply(VoiceCartItem[])` callback. **No SalesPage mount** — modal is reusable infra that `ENG-039a` will mount on `/touch` + `/m`. New `voice.json` i18n namespace (en + es, ~28 keys neutral LATAM tú) registered in `i18n/index.ts`. 8 new server tests + 4 new web cases + cashier-success flip on slice 1 test. New error code `AI_VOICE_COMMAND_UNRECOGNIZED`. ci:server 84.75/70.95/80.72/85.84, ci:web 79.10/70.41/77.31/80.31. Restaurant push-to-talk surface lives in `ENG-039a`; 10-invoice benchmark + mobile/tablet camera capture remain. Phase 4 counter: 0/2 entries closed + ENG-040 Partial. y `ENG-039a` (Restaurant voice-ordering MVP, 2026-05-12) shipped on top: real `/touch` + `/m` surfaces over the shared `VoiceOrderingScreen.tsx` (table label, lazy-loaded `VoiceCartCommandModal`, manual `ProductSearchDialog` fallback, cart preview with notes, two-step `sales.create({status:'draft'}) → sales.suspend({label})` save with discard compensation). Parser schema gained optional per-item `note` field (sin queso modifier round-trips through parser, modal review, cart, persisted sale-level `notes`). New `restaurants` i18n namespace (en + es, neutral LATAM tú). 15 new web component cases + 2 mount cases + 10 new server cases on top of slice 3. ci:server 84.75/70.97/80.72/85.84, ci:web 79.05/70.65/76.75/80.28. Live smoke captured 7 visual proofs (bare /touch, modal idle, cart populated with notes, save reset, desktop SuspendedSalesPanel surfacing `Mesa 5`, mobile `/m`, module-gate redirect to `/dashboard`). y `ENG-040d` (AI Wave 2 — OCR benchmark + mobile/tablet camera capture, 2026-05-13) closes ENG-040: new `packages/server/__fixtures__/invoice-ocr/` with 10 HTML+PNG+JSON triples (cafe/super/bakery/hardware/pharmacy/drogueria/restaurant/minimart/wholesale/corner; MX/CO/CL/PE; 51 ground-truth lines), Playwright fixture-render script, operator-runnable `benchmark:invoice-ocr` harness, scoring helper (Sørensen-Dice on alphanumeric-stripped bigrams + exact qty + ±1% price tolerance + greedy assignment) pinned by 22 mocked test cases. `EXTRACT_PROMPT_SYSTEM` upgraded to teach the model about COP/CLP/ARS/PYG dot-as-thousand-separator vs MXN/PEN/USD dot-as-decimal (one-line prompt change unlocked 76.47% → 100% benchmark). `<InvoiceOcrPreviewModal>` grew a second "Tomar foto" CTA backed by an input with `capture="environment"` + the explicit JPG/PNG/WebP whitelist (HEIC filtered at the chooser on iOS); 3 new web cases pin the dual render, the `capture` attribute, and HEIC rejection. i18n `purchases:ocr.{uploadFileCta, takePhotoCta}` shipped en + es neutral LATAM tú (orphan `uploadCta` deleted). ci:server 84.78/71.02/80.81/85.92, ci:web 79.04/70.66/76.69/80.27. Live OpenAI gpt-4.1-mini benchmark: 51/51 (100%) at $0.0090 / 60s; 2 inline Playwright proofs (desktop side-by-side, Pixel 7 stacked). Phase 4 counter: 2/2 entries closed (`ENG-039a` + `ENG-040` Shipped). y `ENG-039b` (Restaurant table catalog, 2026-05-14) is the second restaurant slice on top of ENG-039a. New migration `0023_restaurant_tables.sql` + Drizzle declaration with a hand-appended partial unique idx on `(tenant_id, site_id, name) WHERE is_active = 1` so archived rows free the name for re-use. New tRPC `restaurantTables.{list, getById, create, update, archive}` (admin mutations + managerOrAdmin reads). Three new audit-log actions + 1 resource type. Two new error codes (`RESTAURANT_TABLE_NOT_FOUND` cross-tenant collapse + `RESTAURANT_TABLE_NAME_DUPLICATE` partial-unique conflict). New admin page `/restaurants/tables` using `ResourcePage` + form modal. Sidebar entry gated by `pos-touch` module + `adminOnlyRoles`. `VoiceOrderingScreen` swaps the free-text input for a `<select>` when the catalog has entries; transparent fallback when empty/error. ~30 new i18n keys × 2 locales. 13 new server tests + 10 new web cases. ci:server 1650/1650 at 84.78/71.4/81.11/85.97; ci:web 1021/1021 at 78.14/70.49/74.9/79.33. y `ENG-065d` (Operations Center: Payment Reconciliation admin retry + mark-settled + per-rail breakdown, 2026-05-14) closes ENG-065d AND the parent ENG-065 sweep. Three new procedures on `payments.*`: `retryOutbox` (admin) resets a row to `queued` with `attempts=0`, refuses terminal `settled` rows via `PAYMENT_OUTBOX_NOT_RETRIABLE`; `markSettled` (admin) flips to `settled` with optional providerTransactionId override, idempotent on already-settled rows; `methodBreakdown` (managerOrAdmin) aggregates `payment_outbox` by `(rail, status)` for the last N days (default 7, max 90). Two new audit actions (`payment.retry`, `payment.mark_settled`) + `payment_outbox` resource type on both server enum and web mirror. Two new error codes (`PAYMENT_OUTBOX_NOT_FOUND`, `PAYMENT_OUTBOX_NOT_RETRIABLE`) en + es neutral LATAM tú. `PaymentHealthPanel.tsx` extended with FiscalHealthPanel-pattern row actions (Retry + Mark settled), ConfirmModal for retry, bespoke Modal for mark-settled carrying the optional provider-tx input, cache invalidation across `peekOutbox`/`reconciliation`/`methodBreakdown`, and a new "Volumen por rail y estado" card. 17 new server tests + 9 new web cases. ci:server 1634/1634 at 84.74/71.38/81.15/85.94; ci:web 1010/1010 at 78.3/70.57/75.03/79.5. y `ENG-038c` (Reconciliation cron + 30-day fixture + AI tie-break matcher, 2026-05-13) closes the matcher half of `ENG-038` without waiting on live-provider contracts. New `services/payments/payment-worker.ts` mirrors the fiscal worker shape with Timer A (outbox housekeeping + stale-claim sweep every 30s), Timer B (statement import every 2h walking every (tenant, rail)), and an explicit catch-up on boot that compares `tenants.settings.payments.<railId>.lastImportedAt` against a 12h threshold and dispatches a single import for the missing window before either timer's first tick. New `runReconciliationPass` in `services/payments/reconciliation.ts` walks injected statement rows and classifies via the four-kind deterministic taxonomy + a new `ambiguous` kind; matched rows write to `status='settled'` in place. New `services/payments/ai-tiebreak.ts` hands ambiguous candidates to the configured language provider via `generateObject` against a tight Zod schema with graceful degradation on every short-circuit (ai-disabled/budget-exceeded/not-implemented/provider-error/not-decisive) so the cron never throws. Three new error codes (`PAYMENT_RECONCILIATION_NO_MATCH/AMBIGUOUS/AI_DEGRADED`) ship en + es neutral LATAM tú. `AIFeature` union extends with `paymentReconciliation`. New `__fixtures__/payment-statements/index.ts` exports a deterministic mulberry32 generator producing 1068 statement rows across 6 rails × 30 days × 6 settlements/day with a controlled 5% mismatch rate sprinkled across all four classifier types. New operator-runnable `npm run benchmark:payment-reconciliation` script: 96.63% match rate in 32 ms against seed=7 (exits non-zero below the 95% threshold). Three new test files (19 cases total: 6 matcher + 9 worker + 4 ai-tiebreak). Webhook-push ingestion stays out of scope (POS desktop has no public IP); pull canal is the v1 path until a central server retransmits webhooks. ci:server 1615/1615 tests pass at coverage 84.67/71.33/81.05/85.87 (above 80/63/77/80 floor); ci:web 1002/1002 pass (above 70/70/70/70 floor). Phase 3 counter: 1/2 entries closed + `ENG-038` Partial (live providers + admin retry/requeue carved out as `ENG-065d` closeout). |
| Plan v2.0 slice | [`ENG-039d3`](./ROADMAP.md) | Shipped 2026-05-14 | Restaurant service charge / propina sugerida math — seventh restaurant slice. Sibling to `ENG-039d` (tip): a tenant-configured percentage that auto-applies on every checkout. New migration `0026_sales_service_charge.sql` adds `service_charge_amount REAL NOT NULL DEFAULT 0` + nullable `service_charge_rate REAL` on `sales`; `schema.ts` + `_journal.json` mirrored. Zod input schemas (`createSaleInput` + `completeDraftInput`) gained `serviceChargeAmount: z.number().min(0).default(0)` + optional `serviceChargeRate` capped at 30%, plus a `.refine()` rejecting the stale-form bug (`rate` set without positive amount). New `services/restaurant/settings.ts` exports `resolveRestaurantSettings` / `writeRestaurantSettings` (mirrors the AI client) over the `tenants.settings.restaurant.serviceChargeRate` JSON path. New `assertServiceChargeMatchesTenant` validates the caller's amount against the live tenant rate at submit time — drift > 1¢ → `SALE_SERVICE_CHARGE_DRIFT`; non-zero amount on rate = 0 → `SALE_SERVICE_CHARGE_DISABLED`. `completeSale.ts` extends the total formula on both paths to `subtotal + tax - discount + tip + service`; the fromDraft path recomputes `baseTotal` from frozen pieces so a draft-time charge does NOT compound at completion. Persisted on both paths; service metadata rides on the existing `sale.complete` audit row when amount > 0. `getSaleRecord` exposes the new columns. `RenderSale` shape + receipt-template variable-availability map + preview payload gain `serviceCharge` + `serviceChargeRate`. New `restaurantSettings.{get, update}` tRPC router (managerOrAdmin / admin) mounted on the app router. New admin Company tab `/company?tab=restaurant` rendering `CompanyRestaurantSettingsCard` (single 0–30 input + save flow that invalidates the get query). `AuthProvider` `DEFAULT_TENANT_SETTINGS` seeds `restaurant.serviceChargeRate: 0` so `useTenant().tenantSettings.restaurant?.serviceChargeRate` is always readable. `SalePaymentModal` reads the rate via a new `serviceChargeRate?` prop; when > 0 it renders a read-only "Servicio" section above the propina block, folds the amount into `grandTotal = total + service + tip`, and three breakdown i18n keys cover tip-only / service-only / both. `SalesPage.tsx` threads `serviceChargeAmount` + `serviceChargeRate` through both `sales.create` and `sales.completeDraft` next to the existing tip threading. New `payment.serviceCharge.*` (sales.json) + `company.restaurant.*` (settings.json) keys in en + es neutral LATAM tú; locale parity green. 11 new server tests + 4 modal cases + 3 settings-card cases. Refund flow unchanged in v1 (refund returns `total` which now includes both tip + service); future ticket can split if needed. |
| Plan v2.0 slice | [`ENG-039d`](./ROADMAP.md) | Shipped 2026-05-14 | Restaurant tip / propina capture — sixth restaurant slice; first of three concerns originally bundled into `ENG-039d` (modifiers → `ENG-039d2`, service charge math → `ENG-039d3`). New migration `0025_sales_tip.sql` adds `tip_amount REAL NOT NULL DEFAULT 0` + nullable `tip_method TEXT` (percentage / fixed) on `sales`; `schema.ts` + `_journal.json` mirrored. Zod input schemas (`createSaleInput` + `completeDraftInput`) gained `tipAmount: z.number().min(0).default(0)` + optional `tipMethod` + cross-field `refineTipMethodRequiresAmount` (rejects stale `tipMethod` with zero amount). `application/sales/completeSale.ts` rewires the fresh-path total to `subtotal + tax - discount + tipAmount` and layers the tip onto the frozen draft total in the fromDraft path; persisted on both branches; defensive clamp on negative inputs; tip metadata rides on the existing `sale.complete` audit row (suppressed at zero). `getSaleRecord` exposes `tipAmount` + `tipMethod`. Receipt preview now demos a non-zero tip so template designers can visualise the line. `SalePaymentModal.tsx` gains a "Propina" section with three preset buttons (0% / 10% / 15%) computed against the pre-tip total, an "Otro" custom amount, and a header breakdown `Base + propina`. `SalesPage.tsx` threads `tipAmount` + `tipMethod` through `sales.create` and `sales.completeDraft`. New `payment.tip.*` keys in en + es neutral LATAM tú. 9 new server tests + 3 new web cases. Refund flow unchanged (refund returns `total` which now includes tip). |
| Plan v2.0 slice | [`ENG-039a`](./ROADMAP.md) | Shipped 2026-05-12 | Restaurant voice-ordering MVP — `/touch` + `/m` real surfaces replace the ENG-069 placeholders, share `apps/web/src/features/restaurants/VoiceOrderingScreen.tsx` (table-label entry, lazy-loaded `VoiceCartCommandModal`, manual `ProductSearchDialog` fallback, cart preview with inline qty/notes, two-step `sales.create({status:'draft'}) → sales.suspend({label})` save with `discardDraft` compensation). Parser schema extended with optional per-item `note` (sin queso modifier round-trips). New `restaurants.json` i18n (~22 keys × en/es neutral LATAM tú). 15 new web tests + 10 new server tests; server CI 84.75/70.97/80.72/85.84, web CI 79.05/70.65/76.75/80.28. Live smoke confirmed end-to-end: `/touch` chrome bare, modal open, manual cart with notes, save → draft visible in desktop SuspendedSalesPanel as `Mesa 5`, module-gate redirect when off. |
| Plan v2.0 slice | [`ENG-039c3`](./ROADMAP.md) | Shipped 2026-05-14 | Restaurant split-bill mutation + UI — fifth restaurant slice. New `sales.splitDraft` `criticalCommandManagerOrAdminProcedure` carves a subset of `sale_items` from a suspended draft into a brand-new suspended draft (`status='draft' + suspendedAt=now + suspendedBy=ctx.user.id + tableId? + suspendedLabel` from the resolved row or `input.label`). Stock-neutral: items are relocated via `UPDATE sale_items SET sale_id=…` with an `AND sale_id=sourceSaleId` TOCTOU guard, never re-debited; totals recomputed from `sale_items` aggregates on BOTH drafts inside the same transaction; audit row `sale.splitDraft` lands with `resourceId=newDraftId + metadata.{sourceSaleNumber, newSaleNumber, movedItemCount, tableName?}`. Three new error codes (`SALE_SPLIT_INVALID_STATUS`, `SALE_SPLIT_NO_ITEMS_SELECTED`, `SALE_SPLIT_ITEMS_NOT_FOUND`). New `SplitBillModal.tsx` opens off a "Dividir cuenta" CTA gated on manager/admin role + `restaurantTables.list` non-empty + `draft.itemCount > 0`; reads source items via `sales.getById` (no new read procedure); table picker reuses `restaurantTables.listWithDraftStatus` with "Misma mesa" + "Liberar mesa" sentinels. On success: invalidate `sales.listDrafts` + `restaurantTables.listWithDraftStatus`, toast with the new draft's label, close modal. New `restaurants:split.*` i18n (en + es neutral LATAM `tú`; ~17 keys). 10 new server tests + 9 new web cases. ci:server 1679/1679 at 84.84/71.66/81.19/86.03; ci:web 1050/1050 at 78.24/70.19/75.34/79.50. Live smoke via Playwright MCP confirmed end-to-end. Colateral: `getSaleRecord` was missing `tableId` in its SELECT shape since ENG-039c (added inline); ADR-0002 closed list refreshed to include `sales.changeTable` (ENG-039c) + `modules.setActive` (ENG-068) + `sales.splitDraft` (new). |
| Plan v2.0 slice | [`ENG-039c2`](./ROADMAP.md) | Shipped 2026-05-14 | Restaurant transfer-to-table CTA on `SuspendedSalesPanel` — fourth restaurant slice on top of ENG-039c. New `TransferTableModal.tsx` owns a dropdown of active tables (with `(actual)` + `(ocupada)` suffixes resolved from the ENG-039c `restaurantTables.listWithDraftStatus` read surface — first consumer) plus a "Liberar mesa" sentinel that detaches the FK back to free-text (`tableId: null` per the ENG-039c mutation contract). The panel's CTA is gated on manager/admin role plus `restaurantTables.list` returning ≥1 active row (non-restaurant tenants see no CTA; loading / error keep it hidden — no flash, no leak). Post-success: toast + invalidate `sales.listDrafts` + `restaurantTables.listWithDraftStatus`. New `restaurants:transfer.*` i18n (en + es neutral LATAM tú; ~14 keys). 10 new web cases extend `SuspendedSalesPanel.test.tsx` with a per-procedure `useCriticalMutation` mock + mocks for `restaurantTables.list` / `listWithDraftStatus` / `useTenant`. Live smoke confirmed: manager/admin picks "Cambiar mesa" → modal preselects current table with `(actual)` suffix → operator picks another → toast + badge updates on the panel + cashier / cross-tenant / non-suspended attempts surface localized hints without closing the modal. |
| Plan v2.0 slice | [`ENG-039c`](./ROADMAP.md) | Shipped 2026-05-14 | Restaurant `sales.tableId` FK + open-table read surface + change-table mutation — third restaurant slice on top of ENG-039b. Migration `0024_sales_table_id.sql` adds nullable `sales.table_id` REFERENCES `restaurant_tables(id)` + `idx_sales_tenant_table` index covering the new leftJoin. New `resolveActiveRestaurantTable()` validates tenant + active flag before persisting (cross-tenant collapses to `RESTAURANT_TABLE_NOT_FOUND`). `sales.create` plumbs `tableId` through `completeSale.runFreshSale`; `sales.suspend` refreshes `suspendedLabel` from the catalog row when `tableId` is passed; `sales.listDrafts` leftJoins `restaurant_tables` to surface `tableId + tableName`. New `sales.changeTable` mutation (manager/admin critical-command role gate) requires `status='draft' && suspendedAt !== null` (otherwise `SALE_CHANGE_TABLE_INVALID_STATUS`) and emits a `sale.changeTable` audit row inside the UPDATE tx with before/after tableId + `priorTableName/nextTableName` metadata. New `restaurantTables.listWithDraftStatus` augments each catalog row with `openDraft: {...} \| null` via a leftJoin pinned on tenant + draft + suspended status. `VoiceOrderingScreen` forwards the picked `tableId` on both `sales.create` and `sales.suspend`; free-text fallback omits it. `SuspendedSalesPanel` renders a `MapPin` badge when a draft carries a tableId + tableName. New error code `SALE_CHANGE_TABLE_INVALID_STATUS` (en/es), new audit action `sale.changeTable`, new i18n keys `restaurants:tables.draftStatus.badgeLabel + badgeTooltip`. 9 new server router cases + 4 new restaurantTables-router cases + 2 VoiceOrderingScreen cases + new `SuspendedSalesPanel.test.tsx` (closes a BACKLOG bullet on the component's coverage gap). Split-bill + transfer-CTA UI surface deferred to ENG-039c2. |
| Plan v2.0 slice | [`ENG-040`](./ROADMAP.md) | Shipped 2026-05-13 | AI Wave 2 final close-out (slice `ENG-040d`) — 10-invoice OCR accuracy benchmark + mobile/tablet camera capture. New `packages/server/__fixtures__/invoice-ocr/` (10 HTML+PNG+JSON triples spanning MX/CO/CL/PE verticals, 51 ground-truth lines), Playwright fixture-render script, operator-runnable `npm run benchmark:invoice-ocr` harness, scoring helper (Sørensen-Dice over alphanumeric-stripped bigrams + exact qty + ±1% price tolerance) pinned by 22 mocked cases. `EXTRACT_PROMPT_SYSTEM` upgraded with a LATAM number-format hint (COP/CLP/ARS/PYG dot-as-thousand-separator, MXN/PEN/USD dot-as-decimal) — unlocked 76.47% → 100% benchmark. `<InvoiceOcrPreviewModal>` gained a "Tomar foto" CTA with `capture="environment"`; 3 new web cases. i18n `purchases:ocr.{uploadFileCta,takePhotoCta}` en + es neutral LATAM tú; orphan `uploadCta` deleted. Live benchmark 51/51 (100%) at $0.0090 / 60s on real OpenAI gpt-4.1-mini; ci:server 84.78/71.02/80.81/85.92, ci:web 79.04/70.66/76.69/80.27. Phase 4 closes 2/2. |
| Out-of-band | [`ENG-049`](./ROADMAP.md) | Shipped | Purga UX minimalista — operator-driven. Header app-wide perdió kicker + h2 + tenant badge; sidebar brand sin kicker ni tagline; hero panels (Dashboard + SalesOverview) sin bloque kicker/headline/description (métricas + freshness sobreviven); 12 páginas estándar y `ResourcePage` (componente shared) sin description debajo del h1; cards fiscal MX/CL sin description redundante; ~30 keys i18n huérfanas borradas en/es como pareja. Convención fiscal-en-español NO aplica. Counter Plan v2.0 NO avanza. |
| Foundation Reset | [`ENG-050..ENG-070`](./ROADMAP.md) | **Complete (21 / 21 closed)** 🎉 | New sellability + foundation wave. **Closed so far**: `ENG-050` (roadmap truth sync + `SELLABILITY.md` go/no-go index), `ENG-051` (Foundation ADR pack), `ENG-052` (device registry + command envelope, two slices), `ENG-053` (operation journal + outbox kernel — migración 0012 + factory `createOutboxKernel<TStatus,TPayload>` + worker base `tickOutbox` + `BOUNDED_EXPONENTIAL_BACKOFF`), `ENG-054` (extract `completeSale` application service), `ENG-055` (extract sale lifecycle services — `returnSale`, `voidSale`, `discardDraft`), `ENG-056` (cash session aggregate boundary — `openCashSession` / `closeCashSession` / `recordCashMovement` use-cases + new `cashSessions.pendingChecks` query + `insertCashMovement` widened to full `CashMovementType`), `ENG-057` (fiscal outbox + contingency engine — migración `0013_fiscal_outbox.sql`, kernel-driven worker daemon, status mirror invariant; admin `reports.fiscal.retryDocument` para manual recovery), `ENG-058` (receipt fiscal finalization — Option A surgical fix con `qr-builder.ts` per-country QR payload builders, `RenderFiscal` extendido, multi-doc query, `FiscalStatusBadge`, `SaleDetailsFiscalBlock`, `receiptPrinter.ts` con `buildFiscalSection` async + qrcode lazy chunk; status copy es la SINGLE SOURCE OF TRUTH), `ENG-060` (peripheral registry + hardware ports — migración `0014_site_peripherals.sql` con partial unique idx `(tenant_id, site_id, kind) WHERE is_active = 1`, 4 contracts ReceiptPrinter/CashDrawer/BarcodeScanner/PaymentTerminal, registry Strategy/Factory, 2 default drivers `system` + `manual` que NO tocan el legacy `webContents.print()` IPC path, admin tRPC router con 6 procedures, web admin UI `/peripherals`; `hardware_outbox` deferred a ENG-062 — ADR-0003 amended), `ENG-061` (barcode scanner pipeline — pure parser con EAN-13/EAN-8/UPC-A checksum + GS1 prefix-2x weight/price labels; `KeyboardWedgeScannerAdapter` registrado; `products.lookupByBarcode` + `peripherals.activeForSite`; `useBarcodeWedgeListener` hook con keystroke-buffer + `interCharGapMs` reset; SalesPage wired via mergeCartItem + override quantity/price; admin form scanner driver picker muestra `Teclado HID (USB)`), y `ENG-062` (ESC/POS printer + cash drawer — migración `0015_hardware_outbox.sql` cierra el deferral de ENG-060 con status enum `queued/submitting/printed/failed/retrying/dead_letter` + kind enum `print-{receipt,fiscal-dee,quotation,kitchen-ticket}|kick-drawer`; pure byte builder `services/peripherals/escpos/byte-builder.ts` con `buildEscPosBytes(doc, opts)` emit init+codepage+align+wrap+drawer-pulse+cut + cp858 LATAM mapping; transport wrapper con Mock + TCP fully wired + USB/Serial stubs `DRIVER_NOT_IMPLEMENTED`; `EscPosReceiptPrinterAdapter` + `EscPosCashDrawerAdapter` registrados en `DRIVER_TABLE.printer.escpos` + `DRIVER_TABLE.cash_drawer.escpos`; `createHardwareWorker` mirror del fiscal worker boot-wired en `index.ts`; tres procedures `peripherals.printReceipt` (system-fallback / printed / fallback con outbox enqueue), `kickCashDrawer` (manager-only, no-drawer-registered signal), `peekHardwareOutbox` (manager-or-admin, ENG-065 stub); renderer wiring vía `printSaleReceipt(sale, {escposDispatcher, onEscposFallback})` Option A surgical en SaleDetailsModal — el legacy `print-receipt` IPC + receiptPrinter.test.ts 10 cases NO se modifican; SalesPage manager-gated `data-testid="sales-kick-drawer"` button visible solo cuando hay drawer escpos registrado + role manager/admin; PeripheralForm flips escpos `available: true` para printer + cash_drawer; i18n drop `driverHint.escpos` + add `sales.printer.{escposFailedFallback, kickDrawerCta, kickDrawerSuccess, kickDrawerFailed, noDrawerRegistered}` neutral LATAM tú; ADR-0003 deferral footer removido). y `ENG-064` (sync contract v1 — migración `0016_sync_contract_v1.sql` agrega `sync_outbox` mirror estructural de `fiscal_outbox`/`hardware_outbox` con kernel projection + 7 columnas de contrato (entityType, entityId, operation, conflictPolicy, idempotencyKey, deviceId, dependsOnOperationId, operationEventId); status enum queued/submitting/synced/conflict/retrying/dead_letter; partial unique idx idempotent que colapsa retries; data migration one-shot copia pending sync_queue rows con conflictPolicy auto-asignado por ADR-0004 manual list; pure manifest `services/sync/contract.ts` con SYNC_PAYLOAD_VERSION + SYNC_ENTITY_TYPES (45 entidades exhaustivas) + SYNC_CONFLICT_POLICY exhaustivo; `enqueueSync(ctx, args)` helper que reemplaza el inline insert pattern leyendo envelope context y resolviendo operation_event_id via lookup; tres procedures additivas `sync.{getContract, peekOutbox, retry}` operando sobre sync_outbox; ADR `0005-sync-payload-contract.md` documenta manifest + version-bump protocol; 27 tests nuevos cubren las 4 acceptance areas — ordering por priority+createdAt, retry reset solo de retrying/dead_letter, no-op para synced, duplicate suppression con partial unique, manual conflict marker en high-risk entities; counter 12/21 → 13/21. Writer rewrite (19 routers) + existing sync.* procedure cutover de sync_queue a sync_outbox parqueados a ENG-064b — la acceptance "Tests cover ordering/retry/dedup/manual-conflict for high-risk entities" se cumple via tests directos de enqueueSync + sync_outbox sin requerir el writer rewrite). y `ENG-064b` (writer cutover Shipped 2026-05-05 — los 19 routers + 4 application services + 1 dev seed flippean a `enqueueSync`, las 8 procedures legacy `sync.*` cortan a `sync_outbox`, migración `0017_drop_sync_queue.sql` dropea la tabla legacy, web cliente `services/storage/syncQueue.ts` se renombra a `offlineQueue.ts` para eliminar la colisión de nombre; no quedan referencias server-side activas a `syncQueue`/`sync_queue`; los literales restantes son migraciones históricas, narrativa ADR y el ObjectStore IndexedDB preservado del cliente). y `ENG-065a` (Operations Center foundation Shipped 2026-05-05 — nuevo `/operations` page con 3 health tabs: Sync (reusa `<CompanySyncCard />`), Fiscal (consume `reports.fiscal.list` con filtro de status + retry button admin-only), Device (peripherals + hardware_outbox tail con problemas-only filter + retry button admin-only via nueva `peripherals.retryHardwareOutbox` procedure); manager+admin gate en la página; `reports.fiscal.list` widened a managerOrAdmin para read; sidebar entry "Operaciones" en `sections.overview`; 16 web tests + 7 server tests; counter 14/21 → 15/21). y `ENG-065b` (Operations Center: Cash + Inventory Reconciliation Shipped 2026-05-06 — dos procedures nuevas `reports.cash.reconciliation` (managerOrAdmin, tenant-wide drop del per-(siteId, cashier) filter del legacy `cashSessions.report`) + `reports.inventory.discrepancies` (managerOrAdmin, single GROUP BY contra `inventory_balances` que detecta drift cache-vs-cache `products.stock` vs `Σ(on_hand)` con epsilon 0.001 — exactamente lo que `inventory.reconcileBalances` heals); 2 nuevos tabs `cash` + `inventory` en `/operations` page (TAB_KEYS extended a 5); `CashHealthPanel` renderiza 4 summary tiles + bySite + recentDiscrepancies con sign-coded badges; `InventoryHealthPanel` renderiza summary + drift table con admin-only "Reconciliar balances" button que dispara existing `inventory.reconcileBalances`; i18n namespace `operations.{cash,inventory}` agregado en + es neutral LATAM tú; 13 nuevos server tests + 12 nuevos web tests; sin migrations / sin tablas nuevas; counter 15/21 → 16/21; Payment slice carved out a `ENG-065d` (Gated en ENG-063)). y `ENG-065c` (Operations Center: Diagnostic Export Shipped 2026-05-06 — admin-only bulk export para support tickets; nuevas procedures `reports.diagnostics.{preview,export}` adminProcedure que retornan counts + bundle JSON con `operation_events` + `operation_effects` + 3 outboxes existentes (sync/fiscal/hardware) filtrados por rango de fechas, hard cap 10000 por tabla con warning surfacing si se alcanza; manifest.counts mantiene 5-name keyset locked (`payment_outbox=0` + `webhook_outbox=0` reservados para ENG-063 + ENG-070, additive sin breaking schemaVersion); 6º tab Diagnóstico en `/operations` con `DiagnosticExportPanel.tsx` que pinta date range + presets + preview tiles + checkboxes outbox + Descargar ZIP via `jszip` (nuevo dep, MIT, ~30KB gzipped); Blob download via URL.createObjectURL + anchor click + deferred revoke (mirror del CSV pattern existente); admin-only via adminProcedure server-side + preview + download buttons disabled con tooltip translated en cliente para manager; i18n namespace `operations.diagnostics.*` en + es neutral LATAM tú; 9 nuevos server tests + 9 nuevos web tests; sin migrations / sin tablas nuevas; counter 16/21 → 17/21). y `ENG-067` (Chaos and resilience suite Shipped 2026-05-06 — 5 nuevos test files que cubren los 5 scenarios sin coverage previo: `chaos-app-restart-after-sale.test.ts` (stale-claim sweep on fiscal+hardware worker bounce, fresh-claim preservation, sync worker daemon gap doc), `chaos-large-queues.test.ts` (10k rows split two-tenant, peek clamp + tenant scoping, indexed read <200ms ceiling), `chaos-duplicate-retry.test.ts` (sync_outbox cross-tenant safety, hardware_outbox idempotency gap doc, hardware cross-tenant), `chaos-provider-outage-recovery.test.ts` (StatefulStubAdapter fail-3-then-succeed full recovery, non-recoverable straight to dead_letter, empty-queue tick no-op), `useOfflineSync.chaos.test.tsx` (navigator.onLine reflection, no auto-sync offline, auto-sync on reconnect, buffer preservation + manual retry); cita explícita a los 2 scenarios ya cubiertos por tests anteriores (`sync.test.ts > sync.resolve`, `application-cashSessions-closeCashSession.test.ts`); 17 nuevos test cases chaos total; review close-out corrigió la inversión de prioridad del kernel común (`createOutboxKernel` ahora drena `priority DESC, createdAt ASC`) y agregó 1 test directo en `outbox-kernel.test.ts`, para 18 tests nuevos efectivos en el cierre; counter 17/21 → 18/21). y `ENG-067b` (`hardware_outbox` envelope-keyed idempotency Shipped 2026-05-06 — cierra el gap estructural que el chaos suite documentó: la migración `0018_hardware_outbox_idempotency.sql` agrega columna nullable `idempotency_key` + partial unique idx `(tenant_id, kind, idempotency_key) WHERE idempotency_key IS NOT NULL` mirror del shape ENG-064 sync_outbox; nuevo helper `services/peripherals/enqueue-hardware.ts::enqueueHardware()` mirror estructural de `enqueueSync()` que captura UNIQUE conflict y retorna `{deduped: true}`; los 2 sitios inline en `routers/peripherals.ts` cutover al helper; input schemas de `printReceipt` + `kickCashDrawer` extendidas con optional `idempotencyKey` (opt-in via input, NO upgrade a `criticalCommandProcedure` — primitiva queda lista para callers futuros); empty-string normalize via schema/helper; 8 cases nuevos en `enqueue-hardware.test.ts`; el chaos test `chaos-duplicate-retry.test.ts` flippeó (assertion `toHaveLength(2)` → `toHaveLength(1)` + `r2.deduped === true`); ADR-0003 nota de paridad con sync_outbox; counter 18/21 → 19/21). y `ENG-066` (Backup, restore, and local security Shipped 2026-05-07 — tres ejes de hardening + ADR-0006: (1) backup atómico via `better-sqlite3.backup()` + `PRAGMA integrity_check` + ZIP bundle con `local.db` + `device-id.txt` + `manifest.json` (nuevo helper `apps/desktop/src/main/backup/backup-bundle.ts`, jszip nuevo dep en desktop); (2) restore validado con detection por magic bytes + corruption rejection antes del swap (backwards-compat con backups raw `.db` legacy); (3) sanitization recursiva del diagnostic export — `services/diagnostics/sanitize.ts::sanitizePayload()` con denylist anchored de ~30 keys (password, token, jwt, apiKey, pan, cvv, cardnumber, certificate, etc.) que reemplaza valores con `[REDACTED]` y agrega `manifest.sanitized: true` + `redactedKeysByTable` al bundle ENG-065c. Plus architectural-lint nueva contra columnas PAN/CVV en `db/schema.ts`. ADR-0006 en `docs/architecture/0006-local-data-security.md` documenta threat model + decisiones de scope (encryption-at-rest deferred, allowlist-vs-denylist tradeoff, per-OS keychain integration deferred a ENG-063 con `@node-gyp-build/keytar` candidate). 15 server unit tests sanitizer + 4 integration tests + 15 desktop `node --test` cases backup-bundle + 2 lint cases. Counter 19/21 → 20/21). y `ENG-068` (Module activation kernel Shipped 2026-05-07 — kernel + 5 demo modules cierra la wave Foundation Reset. Manifest `services/modules/manifest.ts` es la fuente de verdad — `MODULE_IDS` tuple + `MODULES_MANIFEST` record con exhaustiveness `Record<ModuleId, ...>` que bloquea typos en compile time. Estado per-tenant vive en `tenants.settings.modules` (sin migración nueva, usa el blob JSON existente). Server middleware `createModuleGuard(moduleId)` retorna FORBIDDEN con `errorCode: 'MODULE_NOT_ACTIVATED'` + `details.moduleId`; pre-composed `adminProcedureWithModule` / `managerOrAdminProcedureWithModule` en `trpc/middleware/modules.ts`. tRPC namespace nuevo `modules.*` con tres procedures: `list` (managerOrAdmin), `getEffective` (tenantProcedure) full effective map para el renderer, `setActive` (criticalCommandAdminProcedure) que escribe atómicamente via `json_set` + audit log row `action='module.toggle'` resourceType `'tenant_module'` con before/after snapshot. Cinco demo modules wired end-to-end: copilot, operations-center, quotations, anomaly-detection, semantic-search — gates server-side + render-side. Default state `enabled: true` para los 5 → cero cambio de behavior visible. Renderer kernel en `apps/web/src/features/modules/`: `ModulesProvider`, `useIsModuleActive`, `RequireModule`. Admin tab nuevo `/company?tab=modules` con `CompanyModulesCard`. Error codes nuevos `MODULE_NOT_ACTIVATED` + `MODULE_UNKNOWN`. i18n namespace nuevo `modules.json` en + es neutral LATAM tú. Audit log enums extendidos. Dev seed inicializa `settings.modules` con los 5 ON. ADR-0007 documenta decisión de JSON-vs-tabla, scope deferral de fiscal flag consolidation a ENG-068b, soft-disable semantics. Counter Foundation Reset 20/21 → 21/21 — wave complete. y `ENG-069` (Multi-surface POS shell Shipped 2026-05-07 — surfaces-as-modules pattern lifted on top of ENG-068 kernel. Nueva `services/surfaces/manifest.ts` con `SURFACE_IDS` tuple + `SURFACES_MANIFEST` record exhaustivo `Record<SurfaceId, ...>`; POS Desktop es el default implícito (moduleId=null) y los 4 nuevos surfaces gate detrás de module ids dedicados. Modules manifest extendido con `pos-touch` / `kds` / `customer-display` / `mobile-waiter`, todos defaultEnabled=false. Nueva tRPC `surfaces.list` (managerOrAdmin) retorna manifest joined con module state. Renderer mirror + 4 layout shell components (`TouchShell`, `KdsShell`, `CustomerDisplayShell`, `MobileWaiterShell`) cada uno componiendo `<ProtectedRoute allowedRoles={salesRoles}>` + `<RequireModule fallback={<Navigate to="/dashboard" />}>` + `<Suspense>` alrededor de `<Outlet />`. 4 placeholder pages comparten un `SurfacePlaceholder` común con label + descripción + badge "Llega con ENG-039" + CTA "Volver al panel". `App.tsx` registra los 4 nuevos top-level routes FUERA de `MainLayout` para que cada shell controle el viewport completo. Sidebar nueva sección "Superficies" / "Surfaces" con 4 entries gated por los módulos (cuando los 4 están off el título de la sección se oculta vía el filtro existente). Nuevo i18n namespace `surfaces.json` en + es neutral LATAM tú; `nav.json` extendido con sección + 4 entries; `modules.json` extendido con 4 module entries. ADR-0007 subsección Surfaces apendizada. ~24 tests nuevos (server: surfaces-manifest exhaustiveness + surfaces-router + flip + cross-tenant + roles; web: surfaces manifest + 4 shell module-gate tests + 4 placeholder render tests). Foundation Reset wave count NO cambia (post-wave; expansion architecture). y `ENG-070` (Event-based public API + webhook foundation Shipped 2026-05-08 — cierra el trio expansion-architecture ENG-068/069/070 sobre el kernel ENG-068. Nueva migración `0020_webhook_outbox.sql` agrega la 5ª y última outbox per ADR-0003 con kernel-projection shape (status/attempts/next_retry_at/claim_token/locked_at) + partial unique idx `(tenant_id, event_type, idempotency_key) WHERE idempotency_key IS NOT NULL` espejo de ENG-067b para que un envelope replay colapse a una fila. Manifest `services/events/manifest.ts` cierra los 5 event types como `as const` con Zod payload schemas (`sale.completed/refunded`, `inventory.adjusted`, `cash_session.closed`, `fiscal_document.accepted`); `Record<PublicEventType, ZodSchema>` exhaustiveness + `PUBLIC_EVENTS_VERSION=1`. Projector puro `services/events/projector.ts::projectOperationEvent(op)` lee `operationKind` + `summary` y devuelve un `PublicEvent` o null — mapeo: `sales.create`/`sales.completeDraft` → `sale.completed`, `sales.returnSale` → `sale.refunded`, `inventory.adjustStock` → `inventory.adjusted`, `cashSessions.close` → `cash_session.closed`. Helper `enqueueWebhook(tx, args)` mirror estructural de enqueueSync/enqueueHardware con idempotency-collapse. Hook nuevo en `markOperationCompleted` que cuando status='succeeded' corre el projector + lee `tenants.settings.modules.events-api` (gate per-tenant) + enqueue best-effort sin bloquear el commit primario. `fiscal_document.accepted` proyectado separado vía `projectFiscalDocumentAccepted` para el path no command-driven del fiscal worker. Module nuevo `events-api` en MODULES_MANIFEST (defaultEnabled false; admins flippean per-tenant — existing tenants ven cero cambio en el ship). tRPC `events.{getContract, peekOutbox}` (managerOrAdmin) read-only — getContract devuelve manifest + per-field metadata, peekOutbox tail ordenado priority desc + createdAt asc. i18n nuevo `eventsApi.{label, description}` en + es neutral LATAM tú; CompanyModulesCard renderiza el toggle automáticamente desde el manifest extendido (10 módulos total). ADR-0003 footer del 5º outbox flippea a "shipped"; ADR-0007 nueva subsección Public events. HTTP delivery worker + subscriber URL config UI parqueados a ENG-070b — v1 ships ONLY el kernel (contract + projector + outbox + module gate). ~50 nuevos tests server. Counter §3b: ENG-068/069/070 expansion architecture trio cierra completo. Next: ENG-035c (parqueado contrato PAC), ENG-036c (parqueado certificado SII), ENG-038 (LATAM payment rails), ENG-070b (HTTP delivery worker). |
| Authority Node / Store Hub Mode | [`ENG-071..ENG-076`](./ROADMAP.md) | **Complete (6 / 6 closed; 1 deferred spike)** | Early support for two local authority shapes: default `device_local` and configurable `site_hub` + `hub_client`. `ENG-071` locked ADR-0008 + `AUTHORITY-NODE.md` — **Shipped 2026-05-11**, see ROADMAP §3b for the closeout summary; `ENG-072` makes the current device-local default explicit in runtime config — **Shipped 2026-05-08**, see ROADMAP §3b for the full summary; `ENG-073` productizes LAN Store Hub server mode — **Shipped 2026-05-09**, see ROADMAP §3b; `ENG-074` makes cashier terminals point to a hub — **Shipped 2026-05-09** (renderer + device + reachability + the carved-out `ENG-074b` hardware bridge all closed same day); `ENG-074b` shipped the terminal-attached printer / drawer dispatch — **Shipped 2026-05-09**, see ROADMAP §3b; `ENG-075` shipped pairing + Authority health on 2026-05-11; `ENG-076` is deferred until a pilot proves hub-client satellite offline writes are needed. Read [`AUTHORITY-NODE.md`](./AUTHORITY-NODE.md) before changing this runtime family. |
| Out-of-band | [`ENG-048`](./ROADMAP.md) | Shipped | Semantic search UI surface en `ProductsPage` — toggle pill "Semántica" (Sparkles), input dedicado con placeholder español "Buscar por significado (p. ej. lácteo bajo en grasa)", debounce 300ms, columna "Coincidencia" condicional con badge `${pct}%` en modo semántico, botón admin-only "Regenerar embeddings" con `RefreshCw` animado y toast `"Se generaron embeddings de N productos"`. Cae a `mode:'unavailable'` con hint traducido cuando AI off / proveedor sin embeddings; cae a búsqueda literal via `products.list` usando el mismo texto debounced, y mantiene la búsqueda literal client-side cuando el toggle está OFF (cero regresión). i18n bajo `products.semantic.*` + `products.table.{searchSemantic,match}` en + es (neutral LATAM tú). Conecta `semanticSearch` y `regenerateEmbeddings` a UI accionable; `suggestCategory` sigue como backend listo para un follow-up de auto-categorización en el modal de producto. Counter Plan v2.0 NO avanza. |
| Out-of-band | [`ENG-042`](./ROADMAP.md) | Shipped | Sync resilience UX — stale conflict handling + network error display + entity labels humanos. Server `sync.listConflicts` / `sync.pull` exponen `localRecordExists`; `sync.resolve` rechaza keepLocal/merged cuando el local row falta. Web `companySyncDisplay.ts` mapea entityType + operation a labels i18n; conflict modal gana flow "Discard Local Change". `translateServerError.isNetworkConnectivityError` silencia los `Failed to fetch` al boot del AuthProvider y los traduce a `errors:server.networkUnavailable`. Close-out (TOCTOU sweep): `findEntity` guard hoisted dentro de `db.transaction(...)`, errorCode `SYNC_LOCAL_RECORD_MISSING` reemplaza la prosa con copia neutral LATAM en/es, y `assertCashSessionStillOpen` agrega defense-in-depth en `sales.create` / `sales.returnSale` / `sales.completeDraft`. |
| Out-of-band | [`ENG-044`](./ROADMAP.md) | Shipped | OpenAI provider activation (chat) — abre `services/ai/providers/openai.ts` desde `notImplemented` stub a `AIProvider` real con `@ai-sdk/openai@^3.0.54`, default `gpt-4.1-mini` y pricing table de cuatro modelos (4.1-mini, 4.1, 4o, 4o-mini) verificada al ship time. Embeddings quedan parked para ENG-033. Tests: nuevo `openai.test.ts` cubre cost math, `cacheControlForSystemPrompt() === undefined`, `isConfigured()` lectura de env. `registry.test.ts` y `ai-router.test.ts` flippean expectativas (openai ahora `isImplemented=true`, las assertions de stub se mueven a ollama que sigue parked para ENG-040). `.env.example` AI section retitled `(ENG-030 + ENG-044)` con `OPENAI_API_KEY` activo. Counter Plan v2.0 NO avanza (out-of-band, no es uno de los 16 tickets originales). Desbloquea live-smoke de AI Wave 1 sin depender de Anthropic billing. |
| Out-of-band | [`ENG-047`](./ROADMAP.md) | Shipped | AI anomaly follow-ups — sidebar badge para alertas Alta en el ítem Dashboard, `detectAnomalies` ahora escribe `audit_logs` con `action='ai.anomaly.detected'` (ID determinístico para dedup intra-día), nueva tabla `ai_anomaly_snoozes` (migración `0008`) + procedure `ai.anomalies.snooze` que silencia un patrón por 1-90 días + botón en `AnomalyDetailsModal`. Cierra los tres BACKLOG bullets capturados durante el review de ENG-032 (sidebar badge, audit_logs persistence, snooze flow). |
| Out-of-band | [`ENG-046`](./ROADMAP.md) | Shipped | CI minutes reduction — desktop matrix (ubuntu+macos+windows) movida fuera del per-push CI a un `workflow_dispatch`-only en `.github/workflows/build-desktop.yml`. `ci.yml` job `desktop` queda ubuntu-only (sigue gateando boundary regressions). Inputs por OS (`linux`/`mac`/`win`) + `validate_only` para correr solo `ci:desktop` sin packaging. Concurrency group por actor evita runs duplicados. macOS bill at 10x contra free tier — push a main ahora gasta cero minutos macOS/Windows. BACKLOG bullet `[infra][ci]` removido. |
| Out-of-band | [`ENG-077`](./ROADMAP.md) | Shipped 2026-05-13 | Co-pilot Anthropic prompt-cache repair — `services/ai/copilot.ts::buildSystemPrompt(window, siteId)` se vuelve `buildSystemPrompt()` parameter-less devolviendo un bloque de instrucciones byte-for-byte idéntico en cada call (cache stability invariant); el contexto dinámico (`analytics_window_from/to`, `analytics_window_defaulted`, `active_site_id`) se mueve a un bloque `<context>...</context>` que se prepend a la ÚLTIMA user message via los dos helpers nuevos `buildContextBlock(window, siteId)` + `injectContextIntoMessages(messages, contextBlock)` (los user turns históricos no se mutan, sólo el más reciente). `runReadOnlySQL` sigue usando la window por closure (server authoritative). Nuevo test file `ai-copilot-cache.test.ts` con 15 cases pinea: byte-identical system prompt across consecutive calls, no ISO timestamp leakage, `<context>` mention en el system prompt, field shape del context block (todos los campos en líneas separadas), defensive synthetic-user fallback, last-user-only injection, multi-turn cache stability invariant, OpenAI path keeps `providerOptions` undefined (no regression), cache_read/write tokens flow through to `ai_audit_log`, AI_DISABLED short-circuit antes de `generateText`. OpenAI / Ollama unaffected (devuelven `undefined` en `cacheControlForSystemPrompt()`). Invoice OCR + voice cart-command parser ya usaban system prompts estáticos. `ai-router.test.ts` 30 existing cases stay green. ci:server 1596/1596 tests pass at coverage 85.06/71.51/81.18/86.21 (sobre el floor 80/63/77/80). Promueve BACKLOG `[ai][perf] Anthropic prompt-cache hit rate likely zero on the co-pilot` (2026-04-29) y borra el bullet en el mismo commit. Counter Plan v2.0 NO avanza (out-of-band, fuera de los 16 tickets originales, mismo patrón que ENG-044 / ENG-048 / ENG-049 / ENG-050). |
| Out-of-band | [`ENG-078`](./ROADMAP.md) | Shipped 2026-05-13 | AI category suggestion en el modal de crear/editar producto — `apps/web/src/features/products/ProductFormModal.tsx` cablea `trpc.products.suggestCategory` (la tercera procedure ENG-033 que ENG-048 había dejado huérfana). Debounce 800 ms sobre `name` + `description` (manual `setTimeout`, espejo del idiom de `ProductsPage` semantic-search). El gate `useIsModuleActive('semantic-search') && (role admin/manager)` deja silencio total para cashiers y tenants sin el módulo activo. La auto-preselección de alta confianza (>= 0.7) corre en `onSuccess` de la mutation (event-driven, no useEffect — la regla del React Compiler `set-state-in-effect` se respeta) sólo cuando `mode === 'create'` y el dropdown está vacío; renderiza un badge "Sugerido por IA" verde con ícono `Sparkles` junto al label de Categoría. Confianza media (0.3-0.7) o alta-pero-bloqueada renderiza un chip debajo del dropdown con el nombre de la categoría, el porcentaje, un botón "Aplicar sugerencia" y un "✕" descartable. El chip se auto-oculta cuando el `categoryId` actual ya matchea la sugerencia (no ruido cuando AI coincide con lo elegido o lo guardado). Debajo de 0.3 es silencio total. Nuevo i18n namespace `products.suggestCategory.*` con 5 keys × en + es (neutral LATAM tú: "Sugerido por IA", "Aplicar sugerencia", "Descartar sugerencia", "{{name}} · {{percent}}% de confianza", "Sugerido"). Nuevo `ProductFormModal.test.tsx` con 14 cases (escalera de confianza, manual-pick-wins, edit-mode-no-preselect, click-Aplicar, dismiss-then-different-suggestion, module-off, cashier defensive, mutation-error silent, debounce coalesce, name-too-short short-circuit). Cero cambios de server (la procedure ya existía). ci:web 1001/1001 tests pass at coverage 78.24/70.45/75.08/79.42 (sobre el floor 70/70/70/70). Live smoke capturó 10 screenshots inline (5 pares antes/después): D1 create-mode preselect ("Pan tajado integral 500g" → "Panadería" 95% + badge); D2 operador picó "Bebidas" → chip con "Panadería 95%" + Aplicar sin pisar; D3 click Aplicar → dropdown salta a "Panadería"; D5 edit "Arroz Diana 500g" → AI coincide con "Abarrotes" y chip se suprime; D6 toggle `semantic-search` off → modal abre sin badge ni chip. Promueve BACKLOG `[ai][products][ux]` (2026-04-30, capturado en review de ENG-048) y borra el bullet. Counter Plan v2.0 NO avanza (out-of-band, mismo patrón que ENG-044/048/049/050/077). |
| Out-of-band | [`ENG-079a`](./ROADMAP.md) | Shipped 2026-05-13 | Sidebar slim-down (Phase 1, low-risk compaction) — operator surfaced redundancia visual directa (screenshot con dos bloques resaltados: `SidebarBrand` con `hero-surface` arriba y card SESIÓN INICIADA abajo). Pure visual compaction, cero cambio de comportamiento: cada item, cada gate, cada highlight de ruta activa quedan idénticos. `SidebarBrand` pierde el `hero-surface` (recipe con shadow + radial-gradient hecho para hero panels, no chrome de nav); el ícono pasa de 48×48 a 32×32 sin sombra y el heading baja de `font-display text-2xl` a `text-base font-semibold`. La card SESIÓN INICIADA del footer se borra entera — `Header.tsx:203,206,215,216` ya muestra `user.name + role + email` en el menú top-right, era duplicación pura. NavigationLink padding `py-3` → `py-2`; gap entre items `space-y-1.5` → `space-y-1`; gap entre secciones `space-y-5` → `space-y-3` (expandido) y `space-y-2` (colapsado, donde los labels ya están ocultos); custom tracking del section label se elimina (`tracking-[0.28em]` → normal). Key i18n huérfana `common:signedIn` borrada de `en/common.json` + `es/common.json` (grep confirmó cero consumidores fuera del Sidebar); `useTranslation` del Sidebar simplifica de `['nav', 'common']` a `'nav'`. Espacio vertical recuperado: ~390 px en modo expandido (~50 brand + ~80 SESIÓN card + ~120 nav padding + ~60 item gap + ~32 section gap + ~48 colapsado). Cero tests Sidebar existentes — sin ediciones de test. ci:web 126 archivos / 1002 tests pasa al coverage 78.25/70.49/75.11/79.43 (sobre el floor 70/70/70/70). Live smoke 4 screenshots inline (2 pares antes/después): C1 expandido — 17 items visibles vs 9 antes = 8 items adicionales sin scroll; C2 colapsado — 18 íconos visibles vs ~13 antes con gaps tightened. Counter Plan v2.0 NO avanza (out-of-band, mismo patrón que ENG-045/049/077/078). Phase 2 `ENG-079b` shippeada el mismo día con los cambios de comportamiento. |
| Out-of-band | [`ENG-079b`](./ROADMAP.md) | Shipped 2026-05-13 | Sidebar slim-down (Phase 2, behavioral compaction) — tres mejoras ergonómicas sobre el sidebar slim de Phase 1. **O7** convierte `sections.setup` ("Configuración") en grupo colapsable: el título de la sección es ahora un `<button aria-expanded aria-controls>` con `ChevronDown` que rota -90° cuando está cerrado. Nuevo componente `SidebarCollapsibleSectionTitle` encapsula toggle + chevron + aria. Estado persiste por pestaña en `window.sessionStorage` bajo la clave `puntovivo:sidebar:setupCollapsed` (mismo prefijo `puntovivo:` que `siteStorage.ts`). **Default colapsado** para que existing operators vean el cambio claramente y los 16 items admin queden detrás de una fila. Helpers `readSetupCollapsed()` + `writeSetupCollapsed()` con try/catch para guardar contra browsers private-mode que tiran al acceder sessionStorage — fallback al default colapsado en vez de crashear render. **O8** mueve el botón colapsa-rail del footer al header top, pairing con el botón mobile-close existente vía `hidden xl:inline-flex` (sólo uno visible por breakpoint). Footer eliminado entero. **O9** mueve `providers` / `categories` / `locations` de `sections.flow` ("Operaciones") a `sections.setup` ("Configuración"), insertados después de `customerCatalogs` y antes de `units` — clusterizan con el resto de data catalog setup. Operaciones baja de 8 a 5 items; Configuración sube de 13 a 16, pero con default colapsado son 16 items detrás de 1 fila. Modo collapsed-rail (xl:w-[6.5rem]) bypasea el toggle — todos los íconos render (sección labels ya ocultas en rail, power users navegan por posición). TS narrowing menor surgió cuando las arrays `flow` y `setup` quedaron homogéneas en shape de `requiredModule` (el `satisfies` union dropeó el optional); resuelto con cast `as readonly NavigationItem[]` dentro del filter callback, documentado inline. Cero nuevas i18n keys — `sections.setup` / `actions.collapseRail` / `actions.expandNavigation` ya existen. Cero cambios de server. ci:web 126 archivos / 1002 tests pass al coverage 78.25/70.47/75.11/79.43 (sobre el floor 70/70/70/70). Live smoke 4 screenshots inline: D1 fresh session con Configuración colapsada por default (toggle visible con chevron, 16 setup items hidden); D2 click toggle → aria-expanded=true, items unhidden incluyendo los re-categorizados providers/categories/locations, sessionStorage='false' persistido; D3 page reload con sessionStorage='false' → Configuración queda expandida (no flash, lazy `useState` initializer corre una vez); D4 click nuevo top collapse-rail button → rail a 104 px (6.5rem) con todos los 28 iconos visibles incluyendo setup. Counter Plan v2.0 NO avanza (out-of-band, mismo patrón que ENG-077/078/079a). |
| Out-of-band | [`ENG-045`](./ROADMAP.md) | Shipped | Design system audit + tabbed `/company` — handoff de Claude Design (`Puntovivo Design System-handoff.zip`) reverse-engineered desde el repo, así que `theme.css` / `base.css` / `utilities.css` quedaron bit-idénticos y `components.css` solo difería en 28 líneas (`.btn-success`). Refactor de `/company` de un grid apilado de 8 cards a un segmented-control de 5 tabs (`General` / `Localización` / `Datos` / `Dispositivo` / `IA`) usando la recipe `.segmented-control` + `.segmented-tab` que ya vivía en `components.css`. Active tab URL-stateful (`?tab=ai`) y `AnomalyDetectionCard` ahora linkea directo al panel IA. Tests: `CompanyPage.test.tsx` (6 casos: 5 tabs renderizan, default lands en General, `?tab=ai` deep-link al panel IA, click cambia aria-selected + URL, device tab muestra las 4 cards, non-admin sin tablist). i18n `settings.company.tabs.*` en + es. Counter Plan v2.0 NO avanza. |
| Out-of-band | [`ENG-007`](./ROADMAP.md) | Shipped 2026-05-19 | Audit trail expansion cierra el gap de credit-sales que quedaba Partial desde 2026-04. Wire `writeAuditLog` desde `customers.update` cuando `creditLimit` cambia (tx-atomic con la UPDATE, skip emission si el field no está en el payload o si el valor es idéntico al previo) y desde `completeSale` (ambos paths `runFreshSale` + `runCompleteDraft`) cuando `creditProjection.overrideApplied === true` — el override solo dispara cuando `exceedsLimit && allowOverride === true`, así que el row nunca fires en ventas admin que se quedaron bajo el cupo. Dos audit actions nuevas (`customer.credit_limit.update`, `sale.credit_override`) + un resource type nuevo (`customer`) en el server enum + web type mirror + en + es `auditLogs.json` traducciones. 8 tests en `credit-audit.test.ts` cubren: creditLimit cambiado (0→200000, 500000→0), payload sin creditLimit, mismo valor idempotente, override aplicado en fresh + draft, override pasado-pero-no-usado y sentinel `creditLimit=0`. Colateral inline: ENG-098 había dejado `kds.order.ready` / `kds.order.recalled` y resource type `kds_order` sin traducción en `auditLogs.json` — añadidos en en + es. Decisión de diseño documentada: el cell original mencionaba una tabla `company_credit_settings` que ENG-090 nunca creó (el feature eligió campo per-customer); el ticket cierra auditando las dos superficies reales de mutación. Counter Plan v2.0 NO avanza (out-of-band). |
| Out-of-band | [`ENG-098`](./ROADMAP.md) | Shipped 2026-05-19 | KDS comanda — kitchen display surface ahora muestra órdenes pendientes / ready en tiempo real. Nueva migración `0032_kds_orders.sql` con `UNIQUE(tenant_id, sale_id, station)` para enqueue idempotente + index compuesto `(tenant_id, site_id, status)` para la lectura del tablero. Tres helpers post-tx best-effort bajo `services/kds/` (`enqueueKdsOrder`, `refreshKdsOrderItems`, `removeKdsOrders`) absorben el lifecycle del restaurante: `sales.suspend` (con tableId) + `completeSale` enqueue; `sales.changeTable` + `sales.splitDraft` refresh; `sales.discardDraft` + `voidSale` remove. Cada helper corta cuando el módulo `kds` está apagado, cuando la venta no tiene tableId, o cuando no hay items. Snapshot de items en `items_json` porque los line-items son inmutables después de `sales.create({status:'draft'})` (no existe `sales.addLineItem` en el codebase). Nuevo namespace tRPC `kds.{list, markReady, recall}` (cashier+manager+admin role floor, gateado por `tenantProcedureWithModule('kds')`); `list` evicta cards ready más viejos que 5 minutos y rehidrata `tableLabel` live desde `restaurant_tables.name`. `markReady` escribe `kds.order.ready` audit row + broadcast SSE; `recall` (recovery affordance del cocinero misclick) flip ready → pending + `kds.order.recalled`. Dos error codes nuevos (`KDS_ORDER_NOT_FOUND`, `KDS_ORDER_NOT_READY`) con traducciones EN + ES neutral LATAM. Web: nuevo hook `useRealtimeChannel` envuelve `EventSource` con reconnect en visibility-change + exponential backoff (sandbox-safe, cero imports de Node); nuevo folder `features/kds/` con `KdsBoard` (query `kds.list` + suscripción realtime + mutations optimistas), `KdsStationColumn` (CSS-grid por estación; v1 una sola columna "Cocina"), `KdsOrderCard` (presentational puro con tick de elapsed time + cue ámbar después de 10 min + botón Listo + ghost link recall), y `KdsEmptyState` (surface silenciosa). `KdsHomePlaceholder` reemplazado por `<KdsBoard />`; el chrome fullscreen black de ENG-069 queda como shell de ruta. Nuevo namespace i18n `kds` (en + es neutral LATAM `tú`). Server: 22 casos en `__tests__/kds.test.ts` cubriendo suspend → enqueue, re-suspend idempotente, no-tableId skip, módulo-off skip, refresh por changeTable, discardDraft remove, list TTL eviction, markReady audit + idempotencia, recall, recall en pending → `KDS_ORDER_NOT_READY`, cross-tenant collapse a `KDS_ORDER_NOT_FOUND`. Web: 5 casos KdsBoard + 5 casos useRealtimeChannel hook. v1 single-station MVP: cada orden pool a `station='main'` (UI label "Cocina") porque no hay product-level station tagging todavía — schema + UI ya soportan multi-station. El follow-up coherente de cocina y servicio ahora es `ENG-117`: product station routing, prep-time thresholds, `served` state, mid-service `sales.appendItems`, waiter read-only view y optional sound cue. Counter Plan v2.0 NO avanza (out-of-band — `ENG-098` está fuera de la ventana `ENG-025..ENG-040`). |
| Plan v3 | [`ENG-131`](./ROADMAP.md) | Shipped 2026-05-20 (slice A — workspace data model + collapsible workspace sidebar) | Séptimo pick del wave V3 (post foundation rails + ENG-105 slice A + polish). ENG-131 entero es refactor de IA (8 nuevas workspace shells + Surface Switcher + redirects + mobile nav); slice A entrega el modelo de datos workspace + el sidebar colapsable por workspace sin mover rutas. Nuevo `apps/web/src/components/layout/workspaces.ts` declara las 8 workspaces de UI-REFRACTOR-V3 §3 (Sell, Operate, Catalog, Inventory, Procurement, Customers, Finance, Setup) + `TOP_LEVEL_DASHBOARD` arriba; cada workspace lleva id / labelKey / icon / allowedRoles / defaultRoute / items[]. Helpers `visibleItemsForWorkspace(workspace, role, modules)` + `visibleWorkspacesForRole(role, modules)` filtran por rol y módulo activo y omiten workspaces vacías (no headers sin contenido). `Sidebar.tsx` refactor reemplaza las cuatro secciones planas (`overview / flow / surfaces / setup`) por workspace groups; cada header es un disclosure widget WAI-ARIA (`<button aria-expanded aria-controls>`); estado de collapse persiste en localStorage por workspace en `puntovivo:sidebar:workspace:<id>:collapsed`; la workspace que contiene la ruta activa se auto-expande en mount via el lazy initializer de `useState` (sin setState-in-useEffect cascading renders). Cashier ve sólo la workspace Sell (las otras 7 gatean a manager+ o admin-only); admin ve Dashboard + 8 workspaces. Nueva i18n namespace `workspaces` en + es neutral LATAM (`tú` register, no voseo; sustantivos: Vender / Operar / Catálogo / Inventario / Compras / Clientes / Finanzas / Configuración). Tests: 11 en `workspaces.test.ts` (catalogue size, route coverage parity contra la lista plana legacy, no duplicates, role + module filtering, empty-workspace omission, anonymous shortcut) + 5 en `Sidebar.test.tsx` (admin 8 workspaces + Dashboard, cashier sólo Sell, auto-expand en active route, toggle persiste aria-expanded + localStorage, localStorage seed restaura collapsed state en next mount). Smoke targets via Playwright MCP: admin ve 8 headers de workspace + Dashboard, click en cada toggle expande/colapsa, deep link directo `/audit-logs` sigue cargando (router intacto), cashier ve sólo Sell, flip ES con labels neutral LATAM. **Remaining**: Surface Switcher (touch/KDS/customer-display/mobile-waiter/restaurant-tables launcher), nuevos shells `/catalog` / `/procurement` / `/finance` con landing pages de workspace, redirects de rutas legacy hijo a la workspace landing cuando llegue, mobile / tablet workspace nav redesign (drawer mantiene el render actual del Sidebar — ver si UX collapse-by-default necesita affordance específica), decisión Dashboard fold-into-Operate. Cada uno como `ENG-131b..d`. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-105`](./ROADMAP.md) | Shipped 2026-05-20 (slice A — global command palette + canonical shortcut map + aria-keyshortcuts hookup) | Sexto pick del wave V3 (post foundation rails ENG-103 / ENG-104 / ENG-133 / ENG-134 / ENG-135). ENG-105 enumera 11 entregables; este PR cierra el primero y abre el andamiaje para las 10 rebanadas siguientes. Nuevo `apps/web/src/lib/shortcuts.ts` declarativo: `SHORTCUTS` lista cada binding con `id / keys / scope / labelKey / roles`; helpers `getShortcutById`, `formatKeysForDisplay` (⌘ vs Ctrl según `navigator.platform`), `formatKeysForAria` (CamelCase canónico, `Mod` → `Meta` en macOS y `Control` elsewhere), `ariaKeyshortcutsFor`, `matchesShortcut` (acepta Ctrl + Meta como `Mod` en non-mac para teclados externos). Nuevo `apps/web/src/lib/commandPaletteActions.ts` con 17 acciones V1 (15 navegación + 2 comandos: Nueva venta, Cerrar sesión) filtradas por rol y módulo activo reusando `adminOnlyRoles / managerOrAdminRoles / salesRoles / dashboardRoles` de `roleAccess.ts` y los mismos ids de `RequireModule` del router. Nuevo `apps/web/src/components/feedback/CommandPalette.tsx` monta vía Modal compartido (focus trap + ESC + return-focus heredados) con navegación ArrowDown/ArrowUp/Home/End/Enter, filtro substring case-insensitive sobre label + descripción traducidos, listbox filtrado por rol/módulo con `aria-selected`, y hints de shortcut a la derecha cuando un action declara `shortcutId`. Nuevo `CommandPaletteProvider.tsx` registra un listener autenticado `keydown` para `Mod+K` via `matchesShortcut`, no abre en login, no se apila sobre otro modal abierto, toggle abre/cierra, marca `document.body.dataset.commandPaletteOpen` para que un segundo `Mod+K` cierre la paleta sin abrir otra encima. Montado dentro de `ModulesProvider` + `LocaleProvider` para leer módulos y lenguaje tenant. `SalesCheckoutPanel.tsx` recibe `aria-keyshortcuts` en Charge (F1), Suspend (Meta+P en macOS / Control+P elsewhere), Toggle Suspended (Meta+R en macOS / Control+R elsewhere), y los chips visibles de atajos ahora salen del mismo catálogo canónico — cierra el Remaining de ENG-134 que esperaba este mapa. Nuevas i18n namespaces `shortcuts` + `palette` en + es neutral LATAM. 35 tests: `shortcuts.test.ts` (matcher Ctrl/Meta/Cmd, formatters mac vs non-mac, catalogue surface, ariaKeyshortcutsFor), `CommandPalette.test.tsx` (render con search focused, filter, ArrowDown selección, Enter activa, click activa, empty state, role/module gating), `CommandPaletteProvider.test.tsx` (Mod+K abre/cierra, no login, no modal stacking, body dataset flag, useCommandPalette outside provider throws), `SalesCheckoutPanel.hubGate.test.tsx` (chips visibles + ARIA primario). Nuevo `docs/SHORTCUTS.md` documenta catálogo + cómo agregar shortcut + cómo agregar action + ARIA contract + conflictos con browser shortcuts. Smoke matriz extensiva: 44 ítems PASS/FAIL via Playwright MCP cubriendo open/close desde toda ruta, navegación teclado dentro del palette (Arrows + Home/End + Tab trap), filtering en tiempo real, action execution (Enter + click), role-based filtering (admin vs cashier), shortcut hints mac vs non-mac, aria-keyshortcuts en /sales buttons, no conflict con shortcuts existentes (F1 sigue cobrando, Mod+K abre paleta sobre /sales sin tocar cart state), focus return on close, flip ES con copy verificado, console errors check. **Remaining**: preflight, quick-create, fast-register, undo/recovery, customer attach pre-checkout, payment drawer redesign, focus rules vs barcode wedge, layout stability, most-used ordering (depende de ENG-135 sink), wrap-around. Cada uno como `ENG-105b..`. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-135`](./ROADMAP.md) | Shipped 2026-05-20 (engine + sink interface + helpers + opt-in + audit + tracing middleware + render-error wiring) | Production observability — quinto y último foundation rail post ENG-103 / ENG-104 / ENG-133 / ENG-134. Nuevo módulo `packages/server/src/observability/` con tres archivos. `sink.ts` expone `TelemetrySink` interface (captureException + recordSpan) + `noopSink` default + `registerTelemetrySink(adapter)` que devuelve el sink anterior para swap a runtime (cero side-effects en boot). `capture.ts` expone `captureException(err, attrs, db)` + `withSpan(name, attrs, fn, db)`; el log local pino corre siempre, el sink se invoca sólo cuando `tenants.settings.telemetryOptIn === true` (lookup cacheado 60 s para que un storm de errores no martille la DB, invalidado inmediatamente por el toggle admin). `redact.ts` walks el bag de atributos y enmascara `password / token / authorization / refreshToken / email` antes que el sink lo vea (mirrors el spirit de pino REDACT_PATHS de ENG-006). Sink wrapped en try/catch — adapter que tira nunca rompe al caller. Nuevo `packages/server/src/trpc/middleware/tracing.ts` se aplica via `publicProcedure = t.procedure.use(tracingMiddleware)` así que todo procedure (incluyendo unauth como `auth.login`) logea `procedure / durationMs / outcome / correlationId / tenantId / userId` en éxito, registra `recordSpan` en ok/error cuando el tenant está opt-in y rutea failures a `captureException` antes de re-throw. La middleware es función libre + `t.middleware()` wrap inline en init.ts para evitar el circular load init.ts → tracing.ts. Tracing diferencia ok vs error reading `result.ok === false` del `next()` (tRPC v10 no tira — devuelve shape) además del try/catch defensive. Nuevo procedure `companies.updateTelemetryOptIn` admin-only flipea `tenants.settings.telemetryOptIn` via `json_set` con fragmento booleano parametrizado, escribe audit `telemetry.opt_in.updated` con `before` / `after` en la misma transacción e invalida el cache opt-in. `companies.getCurrent` ahora retorna `telemetryOptIn` derivado via nuevo helper `selectCompanyByIdWithTelemetry(...)` que también extiende `upsert` + `setLogo` para que `setData` de react-query nunca pierda el campo en cache writes. Nuevo entry en `auditLogActionEnum`: `telemetry.opt_in.updated`; nuevo entry en `auditLogResourceTypeEnum`: `tenant`; mirrors en `apps/web/src/types/index.ts`. Web: nuevo `apps/web/src/lib/observability.ts` con `captureRenderError(err, ctx)` + `installGlobalErrorListeners()` + `setActiveTenantId(...)` + `registerRenderTelemetrySink(...)` + `RenderTelemetrySink` interface. `AppErrorBoundary.componentDidCatch` rutea via captureRenderError con `componentStack`. `main.tsx` llama `installGlobalErrorListeners()` antes del render para que `window.error` + `window.unhandledrejection` lleguen al pipe. `AuthProvider` llama `setActiveTenantId(...)` en init + login + logout. Nuevo `CompanyTelemetryCard` mount en tab `data` de `/company` (debajo de Sync + Backup): status panel, botón toggle, mensaje legal. Nuevas i18n keys `settings.company.telemetry.*` + `auditLogs.actions.telemetry.opt_in.updated` + `auditLogs.resourceTypes.tenant` en + es neutral LATAM. Tests: 15 en `observability-capture.test.ts` (sink swap, opt-in/opt-out, anonymous, redaction nested + cycles, withSpan ok+error, cache TTL con fake timers) + 4 en `trpc-tracing.test.ts` (ok / error / anonymous / parallel correlationId) + 6 en `companies-telemetry.test.ts` (flip-up + flip-down + cashier FORBIDDEN + cross-tenant + getCurrent surface + immediate revoke) + extended AppErrorBoundary + CompanyTelemetryCard (4 cases). Nuevo `docs/OBSERVABILITY.md` documenta contrato + redaction + opt-in semantics + adapter wiring recipe. **Remaining**: instalar `@sentry/node` + `@sentry/browser` (o GlitchTip) como adapter wired a las sink interfaces, per-tenant error rate dashboard + crash-free sessions, trace propagation end-to-end renderer→main→server→DB (v1 se queda en server boundary), Electron main process crash-path init. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-134`](./ROADMAP.md) | Shipped 2026-05-21 (slice B — Playwright a11y smoke 8/12 routes green + design-system fixes + ES marketing doc) | Slice B continúa el cuarto foundation rail. Nuevo `e2e/web/support/a11y.ts` exporta `runAxeOnPage(page, options?)` construido sobre `@axe-core/playwright` y refleja exactamente el contrato del helper de componente: tag set WCAG 2 A + AA, severity floor `serious`, mismo output indentado por bullets. Nuevo `e2e/web/a11y.spec.ts` declara 12 entries / 10 rutas y **8 entries pasan hoy en AA serious-floor** (Login anon, Dashboard, Inventory, Customers, Orders, Quotations, Company, Audit log). Las otras 4 (`Sales cashier`, `Sales admin`, `Products`, `Purchases`) llevan un campo `skipUntilEng134c` con la razón concreta — `--primary` en el Charge button a 2.81:1, `text-primary-600` en DataTable cells a 3.68:1, `.badge-warning` foreground a 4.27:1. Cada uno es un token bump único que ENG-134c cierra de una pasada (ver BACKLOG §1). El smoke piggybacks sobre el job `e2e-web` (ubuntu-latest, cache Chromium, 30-min timeout, artifact upload on failure) — cero cambios en `ci.yml`. `@axe-core/playwright` como devDep root; `npm run ci:audit --production --audit-level=high` sigue en 0 vulns. Design-system fixes inline para cerrar las 8 rutas verdes: `--muted-foreground` 0.5→0.4, `--secondary-500` 0.61→0.42 (relaja monotonicidad 400→500 a propósito), `.page-kicker` color: var(--primary-800), header page-kicker text-primary-600→text-primary-800, Sidebar workspace headers + inactive nav → `text-fg2`, Header + LoginPage + ChangePasswordModal body-text → `text-fg2`, `.label` default → `text-fg2`, Customers table edit/delete icon buttons ganan `aria-label` + `title` (`common:actions.{edit,delete}`), CSP `connect-src` widened con fonts.googleapis.com + fonts.gstatic.com (preconnects de index.html dejaron de disparar CSP errors). El contrast gate (`scripts/check-contrast.mjs`) extiende ENFORCED_PAIRS con `(background, muted-foreground)`, `(card, muted-foreground)`, `(popover, muted-foreground)` así un relax futuro del token falla en CI no en route smoke. Nuevo `docs/ACCESIBILIDAD.md` (~140 líneas, neutral LATAM `tú`) marketing/pre-venta — WCAG 2.2 AA en lenguaje no técnico + respaldo regulatorio Ley 1346 CO + NMX-R-050 MX + los tres gates técnicos. Indexado en `docs/PLAN.md §18.1`. `docs/A11Y.md` gana sección "Playwright a11y smoke" con catálogo de rutas + how to run + how to fix regression + suppression contract. Sub-ítem #4 del Remaining (`aria-keyshortcuts` aligned con ENG-105) ya cerrado inline por ENG-105 slice A. Inline collateral en `e2e/shared/baseline.ts`: cleanup FK ordering extendido (product_x_provider, initial_inventory, order_items, by-product-id pruning de sale_items/purchase_items/purchase_return_items/transfer_order_items) + nuevo `ensureSetupAcknowledged(db, tenantId)` para que el redirect post-ENG-104 admin a `/company?tab=readiness` no rompa `loginAs` en e2e. **Remaining**: ENG-134c (cerrar shared-token debt para 12/12 verde), keyboard-only smoke `/sales` end-to-end, screen-reader sweep cashier + admin, smoke de superficies module-gated. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-131c`](./ROADMAP.md) | Shipped 2026-05-23 (slice C — workspace landing routes `/catalog` `/procurement` `/finance`) | IA closeout slice on top of ENG-131 slice A (workspace data model, 2026-05-20) y slice B (ENG-131b Surface Switcher, commit `ce5b45b`). Cierra el segundo ítem del Remaining de ENG-131 ("new workspace shell routes (`/catalog`, `/procurement`, `/finance` with subnav landing pages)"). Hasta ahora los 8 workspaces eran agrupación visual del sidebar, no destinos navegables — typear `/catalog` en la barra del navegador caía a 404. Slice agrega un componente genérico `WorkspaceLandingPage` (~70 LOC, prop `workspaceId`) que llama `useAuth()` + `useModulesSnapshot()` y reusa el helper `visibleItemsForWorkspace()` de slice A para producir el subset que el operator puede ver; renderiza header (icon + label + descripción — reusa las keys `workspaces:<id>.{label,description}` ya shippeadas en slice A, cero nuevas i18n keys) + grid responsive (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`) de cards `<Link to={item.href}>` (no buttons — preserve cmd+click + screen reader). Empty state defensivo (0 items) → Navigate a `/dashboard`. 3 nuevas lazy routes en `App.tsx` con role gate (managerOrAdminRoles para catalog+procurement, adminOnlyRoles para finance); sin module gate a nivel route porque el componente filtra items individualmente. Refactor del workspace header en `Sidebar.tsx > WorkspaceGroupHeader`: el `<button>` único se separa en `<Link to={workspace.defaultRoute}>` (icon + label, navega) + `<button aria-expanded aria-controls aria-label>` adyacente (chevron, mantiene disclosure WAI-ARIA). Para los 3 workspaces con landing, `defaultRoute` flipea de "primer item" a la URL de la landing; los otros 5 mantienen el primer item. El `data-testid="sidebar-workspace-<id>"` queda con el chevron para preservar los 6 tests de slice A; nuevo `sidebar-workspace-link-<id>` para el Link. 11 nuevos test cases: 6 en `WorkspaceLandingPage.test.tsx` (admin sees all 9 catalog items, manager sees only /products, módulo OFF oculta cards, workspaceId desconocido → Navigate, 0 items → Navigate, cards son `<a>` no `<button>`) + 3 en `Sidebar.test.tsx > Sidebar workspace header navigation (ENG-131c)` (landing workspaces tienen Link href correcto, no-landing workspaces tienen Link al primer item, chevron sigue como button aria-expanded) + 2 en `workspaces.test.ts > workspace defaultRoute (ENG-131c)` (3 landings con defaultRoute correcto, 5 sin landing con defaultRoute === items[0].href). ci:web 1454/1454 al 81.10/71.82/76.41/82.65 sobre el floor 70/70/70/70 (+11 vs baseline 1443); bundle SalesPage 35.56 kB gz (-13.3% del budget 41); contrast + locale-parity verdes. Live smoke Playwright MCP green: admin → `/catalog` 9 cards ES neutral, `/procurement` 4 cards, `/finance` 3 cards; click card "Auditoría" → /audit-logs; click sidebar header link catalog → /catalog; mobile resize 375×667 → grid 1 columna stacked; deep link `/products` directo sigue funcionando (no regression); cashier intenta `/finance` directo → redirige a /sales (role gate); 0 console errors. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-131b`](./ROADMAP.md) | Shipped 2026-05-23 (slice B — Surface Switcher via CommandPalette) | IA closeout slice on top of ENG-131 slice A (workspace data model, 2026-05-20). Cierra el primer ítem del Remaining de ENG-131 ("Surface Switcher launcher for /touch /kds /customer-display /m /restaurants/tables"). 100% additive sobre la infraestructura de ENG-105 slice A — cero cambios en CommandPalette.tsx / CommandPaletteProvider.tsx / shortcuts.ts. Slice agrega 5 acciones nuevas a `apps/web/src/lib/commandPaletteActions.ts` siguiendo el patrón del navigate group existente: navigate.posTouch (/touch, salesRoles, pos-touch module), navigate.kds (/kds, salesRoles, kds module), navigate.customerDisplay (/customer-display, salesRoles, customer-display module), navigate.mobileWaiter (/m, salesRoles, mobile-waiter module), navigate.restaurantTables (/restaurants/tables, adminOnlyRoles, pos-touch module). Reusa role tuples + ClientModuleId types + visibleActionsForRole filter + filterActionsByQuery substring matcher ya shippeados. 5 nuevas labelKey + 5 descriptionKey en en/es palette.json (neutral LATAM `tú`). 5 nuevos test cases en CommandPalette.test.tsx > ENG-131b describe (admin sees 5, cashier hides tables, pos-touch OFF hides 2 actions con shared gate, substring "touch" → posTouch only, Enter en posTouch navigates a /touch + cierra palette). ci:web 1443/1443 al 81.06/71.80/76.38/82.61 sobre el floor (+8 vs baseline 1435). Live smoke MCP green: admin login → /company?tab=modules (3 de 4 surfaces ya ON en demo seed, customer-display OFF), Mod+K en /sales muestra 4 surfaces (no customerDisplay — validación implícita del module gate), tipeo "touch" filtra a posTouch único, Enter navega /touch + palette cierra. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-134d`](./ROADMAP.md) | Shipped 2026-05-22 (slice D — keyboard-only /sales E2E smoke) | Accessibility closeout sobre ENG-134f. Cierra el item 1 del Remaining de ENG-134 ("keyboard-only smoke for /sales end to end"). Nuevo `e2e/web/sales-keyboard-only.spec.ts` con 10 tests en 3 describes (base flows: mount autofocus / happy-path con 3 axe scans / F2 / tab order; quick-create + palette: Mod+K round-trip / qc product / qc customer; cart manipulation: empty cart F1 / Mod+Z undo / SalesHistoryTable row Enter via ENG-134f). Nuevo helper `e2e/web/support/sales-keyboard.ts` con MOD_KEY platform-aware + expectSearchInputFocused + addProductToCartViaKeyboard composite. **3 colateral fixes** descubiertos por los tests y arreglados inline en el mismo commit per la inline-fix policy: (a) OfflineStatusBanner.tsx — dropea opacity-80 del "last sync" hint (axe color-contrast surface: text-danger-700/bg-danger-50/90 estaba en 4.16:1, ahora 5.20:1 AA); (b) useSalesKeyboardShortcuts.ts — F2 y el Mod+anything branch (incluyendo Mod+Z undo) estaban gateados por isEditableShortcutTarget, suprimiendo los atajos desde el search input — exactamente la posición de focus natural del cajero per ENG-105F. Fix whitelistea #sales-product-search-input en ambos guards; (c) SalesCartWorkspace.tsx — el toolbar Undo + Clear renderizaba disabled-but-visible (contract original de ENG-105d por discoverability del keybinding), pero el `disabled:opacity-45` global en `components.css:191` colapsaba el contraste a 2.55:1 sobre el toolbar surface. El operator rechazó el workaround `axe exclude '[disabled]'` y pidió un fix de raíz: cambio de UX — los botones solo renderizan cuando son usables (`canUndo` resp. `items.length > 0`). El keybinding Mod+Z queda discoverable via CommandPalette (Mod+K). Test ENG-105d en `SalesCartWorkspace.test.tsx` reescrito (4/4 verde). Spec completo Playwright corre **22 segundos verde** local con `--workers=1 --retries=0` (10/10 tests). Live smoke MCP green confirma (a) y (b). ci:web 1435/1435 al 81.09/71.73/76.52/82.64 sobre el floor 70/70/70/70; contrast gate verde. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-134f`](./ROADMAP.md) | Shipped 2026-05-22 (slice F — DataTable row activation + apply to 6 consumer tables) | Accessibility closeout slice on top of ENG-134e. Descubrí planeando ENG-134d que `DataTable.tsx` ya tenía roving tabindex (ENG-134c colateral) pero el branch Enter/Space estaba gateado por `enableRowSelection` y solo llamaba `row.toggleSelected()` — ningún consumer interactivo lo activaba, así que Enter sobre fila era no-op en Sales/Purchases/Orders/Quotations/Customers/Products. Slice extiende DataTable con prop opcional `onRowActivate?: (row: TData) => void` (~15 LOC); el branch Enter/Space prioriza la activación sin gate de `enableRowSelection` (`onRowActivate` es la primaria; toggleSelected legacy queda como fallback). `handleRowKeyDown` recibe `rowData` como parámetro explícito en vez de leerlo del closure (evita dep cycle futuro). `ResourcePage.tsx` gana passthrough (~5 LOC) para que CustomersPage wirea sin tocar layout. 6 consumers wired: SalesHistoryTable / PurchasesHistoryTable / OrdersHistoryTable → `onView(row.id)` abre detail modal; QuotationsHistoryTable → `onOpenDetails(row.id)`; CustomersPage (vía ResourcePage) → `handleOpenEdit`; ProductsPage → const `handleOpenEditRow` extraído reusable, gated por `canManage`. Skip InventoryBalancesPanel (read-only, confirmed con operator); ProductSearchDialog intacto (ENG-134e ya cerró su versión bespoke). 11 nuevos tests: 6 en DataTable.test.tsx > Row activation (Enter fires, Space fires, no enableRowSelection required, fallback to toggleSelected, no-op cuando ambos ausentes, event.target!==currentTarget guard) + 1 por history table (focus row → Enter → onView called con id correcto). CustomersPage + ProductsPage sin test file dedicado (E2E business.spec.ts cubre mouse path). Live smoke Playwright green: admin@demo.co → /sales → row VTA-N-000417 focus + Enter → "Sale Details" modal; /customers → row focus + Enter → "Edit Customer" modal; /products → row focus + Enter → "Edit Product" modal. Purchases/Orders/Quotations vacíos en seed admin — unit tests cubren contrato. ci:web 1435/1435 al 81.08/71.71/76.52/82.64 sobre el floor 70/70/70/70 (+10 vs baseline 1425); DataTable.tsx coverage 95.77/83.09/100/95.77; bundle SalesPage 42.49 kB gz vs budget 41 (+3.6%, within threshold). docs/A11Y.md reframea la subsección "Applying the pattern to other tables" — la regla canónica para consumers de DataTable es ahora "pasa onRowActivate", la regla del patrón inline ENG-134e queda restringida a tablas bespoke sin DataTable. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-134e`](./ROADMAP.md) | Shipped 2026-05-22 (slice E — ProductSearchDialog keyboard navigation) | Accessibility closeout pre-req descubierto durante el planning de ENG-134d. La tabla del `ProductSearchDialog` tenía `<tr>` con sólo `onClick` — un cajero solo-teclado no podía seleccionar producto. Slice agrega roving tabindex sin tocar la semántica tabular: nuevo state `activeRowIndex` + companion `lastItemsKey` que detecta cambios de identidad de resultados durante render (patrón React 19, sin useEffect cascade), `rowRefs` callback-ref por fila, cada `<tr>` gana `tabIndex={index === activeRowIndex ? 0 : -1}` + `aria-selected` + `onFocus` (sync) + `onKeyDown` (switch: ArrowDown/Up clamp no-wrap mirror CommandPalette, Home/End jumps, Enter/Space → `handleProductSelect`). CSS `focus-visible:bg-primary-50/60 + ring` solo bajo nav keyboard. El `onClick` mouse existente queda intacto (E2E `business.spec.ts` y `productRow.click()` siguen verdes). 13 nuevos tests cubren tabIndex roving inicial, aria-selected sync, ArrowDown/Up/Home/End cada uno, no-wrap edges, Enter+Space selección (via unit select mount proxy para evitar ambigüedad de `getByText`), mouse click sync activeRowIndex, items identity change reset a 0, lista vacía sin throw. Live smoke Playwright green: cashier.norte@demo.co → /sales → typing "arroz" + Enter abre dialog → row ABR-0001 con tabindex=0 → row.focus() + KeyboardEvent Enter → aria-selected flips a true + #product-search-unit-select monta con unit ID real + selection panel populates con "Arroz Diana 500g / $3,200.00 / stock 21" + Add to cart enabled + screenshot con anillo de foco visible → Escape el dialog → activeElement.id === sales-product-search-input + :focus-visible true (cross-ENG-105F intacto). ci:web 1425/1425 al 80.98/71.61/76.41/82.53 sobre el floor 70/70/70/70 (+14 vs baseline 1411); bundle SalesPage 42.48 kB gz vs budget 41 (+3.6%, within threshold). Pre-req para ENG-134d (keyboard-only /sales smoke). Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-105f`](./ROADMAP.md) | Shipped 2026-05-22 (slice F — focus rules vs barcode wedge) | Cashier-speed closeout slice sobre ENG-105e (commit `d1fd2c7`). Cierra el ítem `focus rules tuned against barcode wedge input` del Remaining de ENG-105. Nuevo hook `apps/web/src/features/sales/useScannerFocusRestoration.ts` centraliza el contrato del foco — `requestAnimationFrame` para el focus al montar `/sales` + watcher de transición open→closed sobre los 4 flags cashier-flow (ProductSearchDialog, SalePaymentModal, QuickCreateProductGate, QuickCreateCustomerGate). Sin auto-focus en open transitions ni cuando el ref está null; `prevOpenStateRef` evita re-fire en renders idénticos. `useBarcodeWedgeListener.ts` gana opción opcional `scannerInputRef`: cuando `event.target === scannerInputRef.current` el guard de editable target se desactiva (el wedge sigue procesando el burst y, tras flush exitoso, hace `input.value = ''` + dispatch sintético de `input` event para que React capte la limpieza). `BufferState` trackea `lastTarget` para que el flush diferido de `gap-only` mode reporte el origen correcto. La discriminación scan vs typing manual descansa en el `interCharGapMs` ya existente — typing humano >30ms entre teclas resetea el buffer en cada gap, el Enter cae al form submit normal. Modal primitive (`apps/web/src/components/form-controls/Modal.tsx`) acepta prop opcional `restoreFocusTo?: () => HTMLElement | null` para override de la restauración por defecto del `previousActiveElement`; el callback se sincroniza vía useEffect (satisface `react-hooks/refs`). 16 nuevos tests + 1 ajustado: 3 en `Modal.test.tsx` (default no provisto, override retorna elemento, override retorna null → fallback), 8 en `useScannerFocusRestoration.test.ts` (mount focus, no refocus en open transition, 4 cierres restauran focus, multi-modal close en mismo render → 1 sola focus call, ref null sin throw), 4 en `useBarcodeWedgeListener.test.ts` (scan whitelistado procesa, input value cleared post-flush, otros editables siguen bailando, typing lento >30ms gap no se interpreta como scan). Nuevo `e2e/web/sales-scanner-focus.spec.ts` con 4 escenarios: mount auto-focus, cierre del ProductSearchDialog restaura foco, cierre del QuickCreateCustomerGate restaura foco (vía Mod+K → palette → "Create new customer"), typing manual lento cae al form submit. Live smoke Playwright green: mount focus + restauración tras ProductSearchDialog Escape + restauración tras QuickCreateCustomerGate Escape + burst sintético de 13 keydowns sobre el input enfocado → wedge fires (toast aparece, value vacío, sin dialog abierto) + typing manual a 120ms/key + Enter → ProductSearchDialog "Agregar producto" abre. ci:web 1418/1418 al 80.77 sobre el floor 70/70/70/70; bundle `SalesPage` 42.47 kB gz contra budget 41 kB +3.6% dentro del threshold 5%; contrast + locale-parity verdes. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-105e`](./ROADMAP.md) | Shipped 2026-05-22 (slice E — fast-cash F2) | Cashier-speed closeout slice sobre ENG-105d (commit `b66b1d5`). Cierra el ítem `fast-register / rapid-cash F2 layout` del Remaining de ENG-105. Nuevo entry `sales.fastCash` en el catálogo canónico (`F2`, scope `sales`, roles cashier+manager+admin). `useSalesKeyboardShortcuts` gana `onFastCash` opcional con guard `isProductSearchOpen` y `isEditableShortcutTarget` fuera del payment modal — F2 sigue activo cuando el modal está abierto, incluso desde el campo de monto recibido. `SalesPage.handleFastCash` reutiliza `handleOpenPaymentModal(true)` para compartir preflight + cash-session checks con F1; cuando el modal ya está abierto, incrementa `fastCashTrigger`. `SalePaymentModal` recibe una sola señal `fastCashTrigger`: `0` abre normal por F1, `>0` aplica el flow (paymentMethod=cash + amountReceived=grandTotal + splitMode off + tenders=[] + queueMicrotask focus al Confirmar via `id="sale-payment-confirm"`). Toast `sales:fastCash.toast.applied` confirma. `SalesCheckoutPanel` gana chip "F2 Cobro rápido" en el shortcuts card. `ModalButton` mantiene prop opt-in `id?` para el foco programático. i18n EN+ES neutral LATAM `tú`: `sales:fastCash.toast.applied` + `sales:checkout.shortcut.fastCash` + `shortcuts:sales.fastCash.{label,description}`. 11 nuevos tests cubren registration, F2 keyboard branches (outside modal, inside modal, no handler, editable input outside modal, editable amount inside modal, productSearch suppressed), modal flows (default no auto-fill, fastCashTrigger inicial aplica + toast, increment re-aplica, baseline no re-fire). ci:web 1395/1395 al 80.7/71.31/75.93/82.25; `SalesPage` budget sube de 40 a 41 kB gz tras el coste medido 42.19 kB gz (+2.9% dentro del umbral 5%). `docs/SHORTCUTS.md` extendido con la fila F2. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-105d`](./ROADMAP.md) | Shipped 2026-05-22 (slice D — undo/recovery del cart + wrap-around en CommandPalette) | Cashier-speed closeout slice sobre ENG-105c2 (commit `c6c7511`). Cierra dos ítems del Remaining de ENG-105 sin abrir scope nuevo. `useCartWorkspaceStore` persiste un `historyStack: SaleCartItem[][]` per workspace (cap 20, FIFO eviction); `updateCart` pushea el snapshot previo en cada cambio real, no-ops referenciales no inflan el stack. Nueva acción `undoCart(id): boolean` hace pop + restaura; selector `selectActiveUndoDepth(state)` deja la depth cubierta/testeable para consumers del store. Bump del `PERSIST_VERSION` 1→2 con migración que backfilla `historyStack: []` para que el cajero que upgrade mid-shift no vea runtime errors. Stack se resetea en `hydrateFromResumed` (la baseline del cajero post-resume es el server state, no se deshace más allá), `removeWorkspace` y `resetAllWorkspaces`. `useSalesKeyboardShortcuts` gana `onUndo` opcional que dispara en `Mod+Z` y respeta los guards existentes (input editable, modal de pago, productsearch). `SalesPage.handleUndoCart` invoca undo + toast success `sales:undo.cartActionUndone` o info `sales:undo.nothingToUndo`, y limpia `selectedItemKey` post-pop. Resumed-draft carts cortocircuitan el handler. `SalesCartWorkspace` toolbar gana botón "Deshacer" con chip `⌘Z`/`Ctrl+Z`, `aria-keyshortcuts="Meta+Z"`/`"Control+Z"`, `aria-disabled` cuando historia vacía, `data-testid="sales-cart-undo"`. `CommandPalette` cambia ArrowDown/ArrowUp de clamp a wrap (last→first, first→last); Home/End siguen como absolute jumps. Nuevo entry `sales.undo` (`Mod+Z`, scope `sales`, roles cashier+manager+admin) en `shortcuts.ts`. i18n EN+ES neutral LATAM `tú`: `sales:undo.{cartActionUndone,nothingToUndo,button.label}` + `shortcuts:sales.undo.{label,description}`. 22 nuevos tests: 10 hook (initial empty stack, push, no-op no push, undoCart pops y restaura, undoCart vacío retorna false, cap HISTORY_CAP FIFO, hydrateFromResumed reset, resetAllWorkspaces clear, cross-workspace independence, selectActiveUndoDepth tracks active), 4 component (botón oculto sin onUndo, disabled + aria-disabled, fire on click, no-op cuando disabled), 4 keyboard hook (fires + preventDefault, sin onUndo no preventDefault, payment modal blocks, input editable no fires), 3 palette wrap-around (last→first, first→last, Home/End conservados), 1 catalogue registration. ci:web 1384/1384 al 80.65/71.23/75.89/82.20. `docs/SHORTCUTS.md` extendido con la nueva fila `Mod+Z` en el catálogo /sales. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-105c2`](./ROADMAP.md) | Shipped 2026-05-22 (slice C2 — auto-attach del customer recién creado) | Closeout slice sobre ENG-105c. Cierra el gap documentado: el customer creado vía CommandPalette o gate ya no requiere selección manual en el customer-picker del payment modal. `useQuickCreateStore` gana slot one-shot `pendingCustomerAttachId` con setter + consume. `QuickCreateCustomerGate.handleCreated` dispatcha el id al store antes del callback prop y normaliza strings opcionales vacíos a `undefined` para que el alta name-only no falle por email vacío. `SalePaymentModal` gana `useEffect` que espera `isOpen=true` y que `customers` contenga el id pendiente antes de consumirlo, cerrando la carrera entre invalidate/refetch y el `<select>` nativo; después dispatcha `form.setValue('customerId', id)` y toast success "Cliente creado y vinculado a la venta." Nuevo i18n key `sales:quickCreate.customer.autoAttachToast` mirror EN+ES neutral LATAM `tú`. Stub de ToastProvider en `SalePaymentModal.{test,credit.test}.tsx` para mantener backward compat. 6 nuevos hook tests cubren slot lifecycle (initial null, setPendingCustomerAttach fills, consume returns + clears, returns null when empty, overwrite, reset clears, cross-slot independence) + 2 component tests cubren attach inmediato y attach diferido tras refetch. ci:web 1362/1362 al 80.55/71.17/75.87/82.09. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-105c`](./ROADMAP.md) | Shipped 2026-05-21 (slice C — quick-create modals + command palette dispatch) | Slice cashier-speed sobre ENG-105b. Nuevo `useQuickCreateStore` (Zustand transient) coordina los 2 quick-create flows. Empty-state CTA en `ProductSearchDialog` cuando no hay matches + `canCreateProducts` role gate (admin/manager). Nuevos `QuickCreateProductGate` + `QuickCreateCustomerGate` lazy-load lookups (5 queries cada uno) y montan `ProductFormModal` + `CustomerFormModal` ya extendidos con `defaultName` + `onCreated` callbacks. SalesPage fold el nuevo product al cart vía `mergeCartItem`; el customer creado invalida la cache de customers (auto-attach al sale queda como follow-up `ENG-105c2`). CommandPalette gana 2 actions `command.createProduct` + `command.createCustomer` con `managerOrAdminRoles` gate. Nuevas keys i18n EN + ES neutral LATAM `tú` en `common:productSearch.quickCreate.*` + `palette:actions/descriptions.command.create*`. 22 tests: 11 hook + 11 dialog. ci:web 1354/1354 al 80.52/71.15/75.85/82.07. Live smoke captured 3 screenshots: empty-state CTA visible con copy ES, click CTA abre ProductFormModal pre-filled, palette muestra las 2 nuevas actions. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-105b`](./ROADMAP.md) | Shipped 2026-05-21 (slice B — checkout preflight panel + F1 gate) | Octava pick del wave V3 (post foundation rails + ENG-105 slice A + ENG-134c). ENG-105 enumera 11 entregables; slice A (commit `3aafec8` + `5d20dcb`) ya había cerrado el catálogo canónico de shortcuts + command palette + `aria-keyshortcuts`. Slice B convierte el descubrimiento reactivo de bloqueadores en información proactiva: un panel arriba del botón Cobrar enumera los bloqueadores ANTES de F1 en lugar de descubrirlos vía toast del server. Nuevo `apps/web/src/features/sales/useCheckoutPreflight.ts` (hook puro, 6 blockers + 1 warning) computa el estado preflight desde primitives ya cargados (cash session, cart items + summary, customer + credit, user role, tenant settings, isResumedDraft). Nuevo `apps/web/src/features/sales/CheckoutPreflightPanel.tsx` renderiza `role="status"` `aria-live="polite"` con blockers en `danger-50` (AlertCircle) y warnings en `warning-50` (AlertTriangle), recovery CTAs por item, primer blocker con `id="checkout-preflight-primary"` para `aria-describedby` desde Cobrar. `SalesCheckoutPanel.tsx` monta el panel entre `ShortcutsCard` y el botón primario, deshabilita Cobrar cuando hay blocker (no warning), agrega `aria-describedby` al botón. `SalesPage.tsx` invoca `useCheckoutPreflight` y wirea `handleOpenPaymentModal` con gate F1: si `primaryBlocker.id === 'cash_session_required'` preserva el legacy convenience (abre cash session modal), otherwise toast con mensaje del primer blocker. Nuevo namespace i18n `sales:preflight.*` en + es neutral LATAM `tú` (6 mensajes + 5 recovery labels + título + toast template). Tests: 15 hook (uno por blocker + happy path + cart vacío + resumed draft skip + multi-blocker stack + memo stability) + 8 component (render con 0/1/N items, primary id sólo en blockers, recovery CTAs, axe-AA pass, locale ES) + 3 nuevos hubGate (preflightItems opcional, Cobrar disabled con blocker + aria-describedby, Cobrar enabled con warning-only). ci:web 1331/1331 al 80.43/70.81/75.74/81.98; bundle y contrast gates verdes. Pre-modal primitives (paymentMethod, selectedCustomer, pendingDiscountAmount) NO se pluméan aún — el hook skipea silenciosamente esos blocker families; follow-up slice los integrará. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-134c`](./ROADMAP.md) | Shipped 2026-05-21 (slice C — shared-token contrast debt closed; a11y smoke 12 / 12 green) | Closeout slice sobre la slice B de ENG-134 (commit `87c5cdf`). ENG-134 slice B había shipped el Playwright a11y smoke con 8 / 12 rutas en verde y un campo `skipUntilEng134c` por ruta bloqueada (4 rutas: `/sales` cashier, `/sales` admin, `/products`, `/purchases`) — cada uno reportaba una falla de contraste con raíz en un token compartido del design system. Slice C cierra esos tres tokens en un solo barrido. Light-theme `--primary` bumped de `oklch(0.55 0.126 244)` a `oklch(0.43 0.115 244)` (slot ligeramente más oscuro que `--primary-800` para absorber el gap de aproximación OkLab→sRGB que el script subestima en tonos azules; chroma trimmed 0.126→0.115 para mantener la curva del scale a este L; dark-theme se queda en L=0.74 — ya pasa axe-AA contra el foreground L=0.22). Light-theme `--warning-700` bumped de L=0.57 a L=0.50 para que `.badge-warning` (`bg-warning-50` + `text-warning-700`) cruce 4.5:1 en ambos: el script (donde no hay gap de aproximación en tonos cálidos) y axe-core. Barrido de body-text `text-primary-600` → `text-primary-800` en cell renderers de DataTable: `SalesHistoryTable` (saleNumber), `PurchasesHistoryTable` (purchaseNumber), `OrdersHistoryTable` (orderNumber, parity), `InventoryPage` (reference cell, parity), `SplitBillModal` (toggle-all link), `QuickDenominationSelector` (exactKicker), `QuotationDetailsModal` (validityKicker), `RestaurantFloorMapPreview` (area kicker), `TableToolbar` ("Show all columns" item). `components/ui/Button.tsx` link variant también sigue el barrido; el primary filled variant cambia `hover:bg-primary-700` → `hover:bg-primary-900` para que la nueva base más oscura (L=0.43) siga oscureciéndose en hover (de otro modo, primary-700 L=0.53 habría aclarado el botón — regresión introducida por mi propio bump que el fix neutraliza inline); el outline variant cambia `hover:text-primary-700` → `hover:text-primary-800` para alinearse con la nueva convención de mid-contrast. `scripts/check-contrast.mjs` adopta `(warning-50, warning-700)` como nuevo enforced pair (el `(primary, primary-foreground)` ya estaba). `scripts/check-contrast.test.mjs` extendido con un caso badge-warning (mirror del primary). `e2e/web/a11y.spec.ts` retira completamente el mecanismo `skipUntilEng134c` — el campo de la interface, el JSDoc, los 4 valores por ruta, y el `test.skip(...)` en el loop. Colateral inline (mismo commit): destrabar las 4 rutas con `skipUntilEng134c` reveló un segundo axe rule (`scrollable-region-focusable`) sobre `.data-table-scroll` en `/products` + `/purchases` admin, donde el conteo de columnas seedeado fuerza overflow horizontal. La causa raíz era pre-existente, sólo enmascarada por el skip: el wrapper de scroll en `components/tables/DataTable.tsx` no era focusable ni tenía nombre semántico. Fix: añade `tabIndex={0} + role="region" + aria-label={t('table.scrollableLabel')}`; nuevo i18n key `common:table.scrollableLabel` en + es neutral LATAM `tú` ("Scrollable table region" / "Región de tabla con desplazamiento"); `DataTable.test.tsx` extiende la aserción legacy de `toHaveClass('data-table-scroll')` con `tabindex='0'` + `role='region'` + `aria-label` presence. El smoke ahora corre 12 / 12 verde en la WCAG 2 A + AA serious-floor sin axe-rule exclusions. Doc sync: ROADMAP §3b promueve la fila ENG-134c con Status=Shipped y recorta el Remaining de ENG-134 (sub-item #1 cerrado inline); BACKLOG.md borra el bullet que reservaba el ID; `docs/A11Y.md` actualiza la sección "Enforced pairs" con el nuevo `(warning-50, warning-700)` + menciona el DataTable keyboard fix. Counter Plan v2.0 NO avanza (Plan V3 wave). |
| Plan v3 | [`ENG-133`](./ROADMAP.md) | Shipped 2026-05-20 (engine + 2 of 4 metrics) | Performance budgets — tercer foundation rail post ENG-103 / ENG-104. Nuevo `perf-budget.json` en repo root como single source of truth (versionado + JSDoc). Bundle-size gate: `scripts/check-bundle-size.mjs` corre después de `vite build` en `ci:web`, strip de Rolldown hash, gz size con `zlib.gzipSync`, falla la build si algún chunk excede `budget * (1 + thresholdPercent/100)`. Tolerante con chunks nuevos / removidos (warnings, no fail). Initial baselines capturan los 23 chunks ≥ 5 KB gz del último build verde con 5% threshold. tRPC p95 latency gate: nuevo `packages/server/src/perf/budgets.ts` con `loadPerfBudget()` + `computePercentile()` (linear interpolation, NumPy default); nuevo `__tests__/perf-trpc-latency.test.ts` seedea 30 products + 20 customers, corre 10 warmups + 50 measured samples por procedure, asserta p95 contra budget × 1.2. 5 procedures cubiertos: products.list, customers.list, reports.fiscal.list, auditLogs.list, setupReadiness.get. Sibling `node --test scripts/check-bundle-size.test.mjs` (7 cases) pinea strip-hash regex + compare logic. Nuevo `docs/PERF-BUDGETS.md` documenta el contrato + cómo bumpiar baselines en mismo PR + cómo agregar procedures / chunks. **Remaining**: Lighthouse-CI integration, Electron memory ceiling, Operations Center surface (re-routed a ENG-128 para no duplicar attention queue). Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-104`](./ROADMAP.md) | Shipped 2026-05-20 | Guided store setup readiness — segundo step de foundation rails. Nuevo `setupReadiness.get` (managerOrAdmin + tenant-scoped) agrega 10 secciones de `SELLABILITY.md` (locale, sites, fiscal, peripherals, payments, modules, users, ai, catalog, cashSession) en `{score, blockerCount, sections, acknowledgedAt}`. Status enum `ready|blocker|optional-pending|not-applicable`; score formula `round((ready + 0.5 × optionalPending) / applicable × 100)`. Companion `companies.acknowledgeSetup` mutation persiste `tenants.settings.setupAcknowledgedAt` via `json_set` (mismo merge pattern que `modules.setActive`). Web: nuevo `CompanyReadinessCard` como primera tab de `/company` (default para admin); donut con tone flips (`<50` danger, `50-79` warning, `≥80` success), section list con icons + deep-linked CTAs a superficies existentes (`/company` tabs, `/sites`, `/peripherals`, `/users`, `/products`, `/sales`), acknowledge button mientras `acknowledgedAt` es null para que el opt-out funcione aun con blockers. Nuevo `ReadinessBanner` mount en `MainLayout` — aparece para admin cuando hay blockers Y no se acknowledged Y la ruta no es `/company`; sessionStorage dismiss intra-sesión + acknowledge durable cross-session. Nuevo `EmptyStateReadinessNudge` para admin en `/products` y `/customers` cuando table está vacía. Post-login routing en `AuthProvider` consume readiness para admin y manda con blockers a `/company?tab=readiness`; defensa en profundidad: error en readiness query cae al `/dashboard` default. Helper `getDefaultRouteForRoleWithSetup` en `roleAccess.ts` mantiene managers en `/dashboard` porque `/company` sigue admin-only. Nuevo namespace i18n `setup.json` en + es neutral LATAM. 10 server tests (`setup-readiness.test.ts`) + web tests para `CompanyReadinessCard` y `roleAccess`. Scope explícitamente excluido (re-routed a otros tickets): NO fullscreen first-run wizard (ENG-132), NO task center (ENG-128), NO catalog import (ENG-110/123), NO owner BI home (ENG-116), NO Setup workspace fullscreen layout (ENG-132 monta encima). Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-103`](./ROADMAP.md) | Shipped 2026-05-20 | Audit-grade export and download contract — primera pick del wave V3. Nuevo `services/exports/envelope.ts` (`ServerExportEnvelope { data, filename, mimeType }` + helpers `buildFiscalXmlFilename` / `buildDiagnosticZipFilename`) ancla la convención de filename + MIME en el server. Nuevo `reports.fiscal.getXml(documentId)` procedure cierra el TODO de ENG-035b: `managerOrAdminProcedure`, scope por `ctx.tenantId`, collapse cross-tenant + null-xmlRef a `FISCAL_DOCUMENT_NOT_FOUND` (sin leak de existencia), audit row `fiscal.xml.downloaded` con `{cufe, documentNumber, countryCode, byteSize}` en metadata. DTE CL recibe `application/xml;charset=iso-8859-1`; CFDI MX + DIAN CO van UTF-8. `reports.diagnostics.export` extendido con `suggestedFilename: puntovivo-diagnostic-<slug>-<timestamp>.zip` para que el cliente no invente el patrón. Web: `exportService.ts` exporta `downloadFile`, `MIME_BY_EXT` registry, y `buildSemanticFilename` con type union de kinds (statement / ledger / diagnostic / fiscal / report). `FiscalDocumentXmlModal` refactor: deja de recibir `xml` prop, ahora pide lazy via `reports.fiscal.getXml({documentId})`, descarga vía `downloadFile` centralizado, y el server decide el filename canónico — eliminada la duplicación inline de `URL.createObjectURL`+anchor que vivía en líneas 56-66. Consumers (`FiscalDocumentListPage`, `SaleDetailsFiscalBlock`) pasan `documentId` en vez de `xml`. `CustomerLedgerModal.buildStatementFilename` migra a `buildSemanticFilename({kind:'ledger'})`; `DiagnosticExportPanel` consume el `suggestedFilename` del envelope con fallback local defensivo. Audit enum widened: nueva action `fiscal.xml.downloaded` + nuevo resource type `fiscal_document` en `db/schema.ts` + web `types/index.ts` mirror. 6 server tests en `fiscal-reports.test.ts > getXml (ENG-103)` cubren happy CO, ISO-8859-1 CL, cross-tenant collapse, null-xmlRef collapse, audit row + metadata, cashier FORBIDDEN; `reports-diagnostics.test.ts` extendido con regex pin del suggestedFilename. 11 web tests: 6 modal (envelope render, empty, loading state, disabled, copy toast, download con server filename) + 5 helpers (statement / ledger / fiscal / diagnostic patterns + unsupported-extension throw + MIME registry pins). Nuevas i18n keys: `fiscal:document.xml.loading`, `auditLogs:actions.fiscal.xml.downloaded`, `auditLogs:resourceTypes.fiscal_document` en + es neutral LATAM. Counter Plan v2.0 NO avanza (Plan V3 wave). |
| Out-of-band | [`ENG-102`](./ROADMAP.md) | Shipped 2026-05-19 (quota engine slice) | AI usage quotas — primer slice del cell de ENG-102. Implementa el motor de enforcement detrás de la promesa del website draft "800 preguntas al Co-pilot + 200 facturas OCR por sitio al mes". Nuevo `services/ai/quotas.ts` con `AI_QUOTAS` hardcoded constants (copilot:800, invoiceOcr:200), `countMonthlyAiCalls` que reutiliza el índice `idx_ai_audit_log_tenant_site_created` para contar rows exitosos (errorCode IS NULL) en el mes calendario actual, y `requireAiQuotaAvailable` que tira `AI_QUOTA_EXCEEDED` con `{feature, used, limit, resetsAt}`. Wired ANTES del provider call en `ai.copilot.chat` y `ai.invoiceOcr.extract` — un request rechazado nunca escribe audit row. `ai.settings.get` extendido con `quotas: { copilot, invoiceOcr }` scoped a `ctx.siteId`. UI en `CompanyAISettingsCard.tsx` con sección "Cuotas mensuales de IA" — barra de progreso por feature que flippea warning a 80% y danger a 100%, footer con reset date. 7 tests server en `ai-quotas.test.ts` (under/at quota por feature, errored rows excluded, cross-site + cross-tenant isolation, calendar boundary). 3 tests web extendidos en `CompanyAISettingsCard.test.tsx` (idle/warning/danger render, hide cuando master toggle off). Nuevo error code `AI_QUOTA_EXCEEDED` con en + es neutral LATAM. Reset mensual es implícito (cero job) — la query usa `startOfMonth ≤ createdAt < startOfNextMonth`. Los 800/200 son hardcoded por diseño; runtime-tunable queda follow-up. Ticket pasa `Pending → Partial`; Remaining: website-copy review cuando el site público entre al repo. Counter Plan v2.0 NO avanza (out-of-band). |
| Plan v3 audit | [`ENG-166..ENG-181`](./ROADMAP.md) | Partial — 7 shipped + 2 partial / 17 (Wave 1 closed; Wave 2 entry closed; ENG-176 money model fully closed via Step-a/b + ENG-176b currency seam + ENG-176c catalog rename; ENG-168 token lifecycle closed 2026-05-26; ENG-181 error wrapping cleanup closed 2026-05-27 → opens Wave 4 code-quality; ENG-179 TS strict + lint upgrade Shipped fully — ENG-179a `noUncheckedIndexedAccess` 2026-05-27, ENG-179b `exactOptionalPropertyTypes` + `noImplicitOverride` 2026-05-28, ENG-179c `no-explicit-any` → error + `CriticalCommandContext` + `types/index.ts` split 2026-05-28) | Cross-cutting audit closure wave promoted from the 2026-05-24 deep audit ([AUDIT-2026-05-24.md](./AUDIT-2026-05-24.md)) covering security, frontend perf, database/Drizzle, and code quality. **Wave 1 (Critical) — SHIPPED 2026-05-24**: `ENG-166` security closure landed the 11 audit bullets. **Wave 2 (data integrity, blocks ENG-156 + ENG-161)** — `ENG-174` PRAGMA tuning + WAL backup safety **SHIPPED 2026-05-24** (5-pragma cluster + writable wal_checkpoint before readonly backup + preflight migrations guard); `ENG-175` composite indexes **SHIPPED 2026-05-24** (migration 0034 adds 6 composite indexes, declara el partial unique existente de restaurant_tables en schema, y amplía sync_outbox.entity para incluir status); `ENG-175b` FK onDelete policy **PARTIAL 2026-05-24** (policy documented in ARCHITECTURE.md; cascade/setNull patterns confirmed already explicit in schema; literal "every reference explicit" deferred since mass adding RESTRICT triggers ~140 table recreations with zero runtime change); `ENG-167` SQLCipher at rest **PARTIAL 2026-05-25 (Step-1)** — swap to `better-sqlite3-multiple-ciphers@^12.10.0` via npm alias, `DatabaseOptions.encryptionKey` + `ServerOptions.encryptionKey` plumbing, new `db-key-store.ts` mints + seals the per-install 32-byte key via Electron `safeStorage` at `<userData>/data/.dbkey.enc`, threat-model section in SECURITY.md + open-path section in ARCHITECTURE.md, two regression suites; Remaining (ENG-167b): one-shot migration of pre-encryption cleartext DBs, restore-from-different-device key prompt UX, cross-OS matrix validation; `ENG-176` money model **SHIPPED 2026-05-26 (Step-a + Step-b + ENG-176b + ENG-176c)** — Step-a (ADR-0009 + migration 0035 recreates 17 monetary tables with `>= 0` CHECKs across ~40 columns, defensive UPDATE prelude); Step-b (canonical `lib/money.ts` with `roundMoney()` consolidating 6 duplicates + application sweep at covered money write boundaries in completeSale / quotations / cash-session.expectedBalance UPDATE + schema reinstates `moneyTwoDecimalCheck` extending to 7 signed columns + migration 0036 recreates the same 17 tables to attach the precision CHECK with a defensive UPDATE prelude for signed columns); ENG-176b (migration 0037 adds `tenants.default_currency_code` FK to `currency_catalog` with COALESCE backfill, `currency_code` + `exchange_rate_at_sale` + nullable `settle_currency_code` to `sales` / `sale_items` / `quotations` / `quotation_items`, `currency_code` to `products`, nullable `credit_limit_currency_code` to `customers`, and finally `_nonneg` + `_2dec` CHECKs on `fiscal_documents` / `fiscal_document_items` / `payment_outbox` — closes the snapshot-chain gap from Step-a/b in the same pass; new helper `lib/currency.ts:resolveTenantCurrency` consumed by completeSale + quotations + products + customers; new `currency-seam.test.ts` plus extended `db-money-checks.test.ts`); ENG-176c (migration 0038 renames `dian_identification_types` → `fiscal_identification_types` with composite PK `(country_code, code)` so DIAN + SAT + SUNAT + SII rows coexist; `fiscal_documents` gains `buyer_country_code` + composite FK; boot seed grows 10 → 23 rows covering CO/MX/PE/CL; `fiscalDocumentStatusEnum` extends 5 → 8 with `voided` + `notified_correction` + `partial_send`; FiscalStatusBadge mirrors with tone map + EN/ES neutral LATAM i18n; new `fiscal-identification-types-catalog.test.ts` + `fiscal-document-status.test.ts`); ENG-176 ships fully. `ENG-168` token lifecycle **SHIPPED 2026-05-26** — five audit gaps closed: realtime TTL raised 120s → 900s with `token-refresh-needed` SSE event every 10 min so the renderer can re-mint the cookie via `auth.realtimeToken.mutate()` without dropping the socket; `authority.revokeDevice` bumps the registering user's `sessionVersion` in the same transaction so revoked devices' tokens fail next-request verification; `AuthProvider.logout` invokes `window.session?.clear()` to clear the Electron `desktopSession` singleton post-logout (silent in pure web); new `services/cleanup/loginAttemptsCleanup.ts` worker sweeps stale rows on a 1 h cadence + tickOnce at boot for downtime catch-up; pairing code claim emits `device.pairing.claimed` audit with masked code + site + kind. New `login-attempts-cleanup.test.ts` (4 cases) + extended `authority-router.test.ts` (sessionVersion bump + pairing audit). New en/es i18n keys; audit enum widened. Closes Wave 2 except ENG-167b. `ENG-181` error code + error wrapping cleanup **SHIPPED 2026-05-27** — categorised 39 literal `throw new Error()` into Categoría A (14 user-facing → `throwServerError` with 8 new `SERVER_ERROR_CODES` + 17 new i18n keys spanning the new codes plus 9 retroactive backfills) vs Categoría B (25 programmer-assert helpers wrapped with structured `cause: { country, document, missing, tenantId, … }` context that the orchestrator upstream re-emits with the right error code); pino `REDACT_PATHS` extended so `cause.tenantId/siteId/errorCode/kind` survive logs while `cause.password/token/refreshToken/email` are censored; new ESLint `no-restricted-syntax` rule blocks the inline `onError: (err) => toast.*(…)` pattern that bypasses `onErrorToast` (refactored 4 violating call sites: AiConfigPage, SequentialsPage, PosTouchScreen, CompanyTelemetryCard); new `packages/server/src/__tests__/error-codes-coverage.test.ts` asserts every `SERVER_ERROR_CODES` value has a matching `errors.server.<CODE>` key in both locales (en + es) and emits an explicit list when a code lacks a translation, plus orphan + cardinality sentinels; `logger.test.ts` extended with the ENG-181 cause-chain redaction block (5 new cases). Closes Wave 4 entry. `ENG-179a` TS strict flag `noUncheckedIndexedAccess` **SHIPPED 2026-05-27** — enabled in `packages/server/tsconfig.json` + `apps/web/tsconfig.json` + `apps/desktop/tsconfig.json`; fixed ~98 surfaced sites (server 15 / web 83 / desktop 0). Server side: AI provider pricing-row helper (`anthropic.ts` + `openai.ts` extracted `resolvePricingRow` that throws via Categoría B `cause` chain when the fallback row is missing), SSE broadcast (`?? eventName` defensive fallback), fiscal RUT / RFC validators (explicit `if (ch === undefined)` narrow + `!` non-null assertion with `reason:` comment on bounded-modulo indexing), `extractNit` regex narrowing, products router post-INSERT length-1 guard, recent-via-trpc seed `?? undefined` over `?? null`. Web side: DataTable selection filter (`.filter((row): row is TData => row !== undefined)`), QuickDenominationSelector empty-denominations early return, useCheckoutPreflight post-length-check non-null assertion, useTableExport + commandEnvelope fixed-buffer narrows, CopilotPage `?? null` to satisfy formatter signature, ~50 test-site `!` non-null assertions with `reason:` comment after `toHaveLength()` / `getAllByRole(...)` setup. New `ARCHITECTURE.md` section "TypeScript strict-mode floor (ENG-179)" documents the flag, the three fix patterns (explicit narrow / `?? fallback` / `!` with reason), and lint guardrails (no `@ts-ignore` without reason, no `as any` to silence strict-mode). Remaining: ENG-179b (`exactOptionalPropertyTypes` + `noImplicitOverride` — ~203 surfaced errors), ENG-179c (`no-explicit-any` warn → error + `as any` cleanup + double-cast `(ctx as unknown as ...)` → typed `CriticalCommandContext` + `apps/web/src/types/index.ts` 990 LoC split + receipt-renderer DTO export). `ENG-179b` TS strict flags `exactOptionalPropertyTypes` + `noImplicitOverride` **SHIPPED 2026-05-28** — enabled both flags in all three tsconfigs; fixed ~213 surfaced sites following Patrón A (widen target `?: T` → `?: T | undefined`) as default with isolated Patrón B (conditional spread) at the Electron `HeadersReceivedResponse` boundary and at the restaurant-settings `Partial<RestaurantSettings>` consumer site. Server: KdsHookContext + FiscalAdapterIssueInput + EmitFiscalDocumentArgs widening cascades through ~50 call sites in completeSale / discardDraft / returnSale / voidSale / orchestrator; AI client `WriteAISettingsPatch` rewritten with `PartialWithExplicitUndefined<T>` utility + `stripUndefined` runtime helper for AIFeatureFlags spread merge; LanguageModelUsage → UsageForPricing widened; CopilotContextInput / CopilotChatInput / SnapshotOptions widened; ProviderListing.availableInTicket + ListAuditLogsOptions + ListQuotationsOptions + ReceiveTransferArgs + ListReceiptTemplatesOptions + ValidateOptions + FractionPolicyInput widened; CompleteSaleTender / CompleteSaleItemInput / VoidSaleInput / ReturnSaleInput widened; DatabaseOptions / ServerOptions widened; `throwServerError` args.details widened; tRPC routers (ai / auditLogs / auth / authority / categories / customerCatalogs / geography / locations / peripherals / products / quotations / receiptTemplates / restaurantSettings / sales / transfers) updated. Web: Modal / DataTable / TableErrorState / ToastInput / ProtectedRoute / SalesCheckoutPanel / SalesOverview / SalesMobileCheckoutBar / SalePaymentModal / RefundConfirmOverlay / PrintSaleReceiptOptions / PreflightItem / PreflightRecoveryWiring / SaleCartItem / `Product` DTO / `SyncQueueItem.lastError` / RealtimeEvent / ExportOptions / TextBlockEditorProps / EditorReceiptBlock variant / InventoryAdjustmentProduct / InventoryAdjustmentModalProps / InventoryEntryModalProps / CustomerFormModalProps / ProductFormModalProps / SyncTechnicalDetailsProps / Window global (electron / db / sync / session / api) widened; i18next consumers (`OfflineStatusBanner.getBannerCopy`, `AuditLogsTable.translateQuotationStatus`, `SalesRegisterAssignmentField.getAssignmentOptionLabel`, `auditLogsExport.TranslateFn`) switched to `import type { TFunction } from 'i18next'` to honour the branded generic; `t(key, optionalValues)` direct-call sites gated via ternary instead of forwarding `undefined`. Desktop: `BackupOptions.encryptionKey` + `ExtractBackupBundleResult.deviceIdPath / manifest` widened; Electron `HeadersReceivedResponse` callback uses conditional spread (`details.responseHeaders === undefined ? {} : { responseHeaders }`); `AppErrorBoundary` `state` field + `componentDidCatch` + `render` annotated with `override`. New `ARCHITECTURE.md` subsections "### `exactOptionalPropertyTypes` (ENG-179b, 2026-05-28)" covering Patrón A widen vs Patrón B conditional spread + i18next `TFunction` carve-out, and "### `noImplicitOverride` (ENG-179b, 2026-05-28)" naming the AppErrorBoundary hot spot. Zero new `as any` / `@ts-ignore` introduced. `ENG-179c` TS strict closer **SHIPPED 2026-05-28** — promoted `@typescript-eslint/no-explicit-any` `'warn'` → `'error'` in all three ESLint configs; consolidated the 13 outbox-kernel `as any` casts into a single documented `AnyBuilder` boundary (3 typed helpers + `SQLiteColumn` on the column map) and typed the lone unsuppressed `deliveryOrders` `db: any` as `DatabaseInstance` (production `as any` now 1, documented Drizzle generic-table boundary); added `-- reason:` to the 6 seed-mega + seed-dev exemptions and the 3 bare `@ts-expect-error` in auth-email-normalisation.test.ts; replaced the 9 `(ctx as unknown as { envelope? })` double-casts (sales 6 / cashSessions 2 / inventory 1; purchases carries none) with an exported `CriticalCommandContext` + single `asCriticalCommandContext(ctx)` boundary helper (tRPC can't propagate the middleware override through its idempotency-cache short-circuit); split `apps/web/src/types/index.ts` (~995 LOC / 87 types) into `domain.ts` + `ui.ts` + `api.ts` with `index.ts` kept as a re-export shim for one release (142 import sites untouched; wholesale DTO→`inferRouterOutputs` migration deferred to BACKLOG given offline/IndexedDB coupling); receipt-renderer DTOs already exported → no-op. Extended `ARCHITECTURE.md` strict-mode floor with the ENG-179c subsection. ENG-179 closes fully. **Wave 3 (frontend perf)**: `ENG-170` bundle → `ENG-171` React hygiene → `ENG-172` DataTable virt → `ENG-173` Web Vitals RUM. **Wave 4 (code quality)**: `ENG-181` error wrapping ✅ → `ENG-179` TS strict + lint ✅ (179a/b/c) → `ENG-178` megafile wave 2 (after ENG-176 + ENG-171) → `ENG-177` optimistic versioning → `ENG-180` documentation pack (now unblocked). **Wave 5 (polish)**: `ENG-169` input boundary hygiene. Sequencing detail + dependency map in [AUDIT-2026-05-24.md §Recommended pick order](./AUDIT-2026-05-24.md). The wave can run in parallel with the late steps of `ENG-103..ENG-165` — only `ENG-176` directly blocks something on V3 (`ENG-156` multi-currency + `ENG-161` Brazil NFe). Next recommended pick: `ENG-167b` (SQLCipher migration UX, unblock production rollout) o iniciar Wave 3 con `ENG-170` (bundle optimisation) — el operador escoge según riesgo y ventana. Counter Plan v2.0 NO avanza. |
| Plan v3 | [`ENG-103..ENG-165`](./ROADMAP.md) | Pending | World-Class LatAm POS wave promoted from strategic docs, BACKLOG, staged 2026-05-19 design draft, current competitor table-stakes, and the May 20 live UI-density audit into [PLAN-V3.md](./PLAN-V3.md). The wave (61 tickets after the 2026-05-20 extension + coverage-closure passes) packages market objective, ICP, sellability gates, export trust, setup, migration, cashier speed, staff approvals, customer display, loyalty, promotions, advanced inventory, replenishment, WhatsApp outbound + inbound commerce, omnichannel, delivery, accounting, BI, KDS v2, public API, vertical-depth packs, payment settlement, procurement, chain HQ, CRM/consent, supportability, security/privacy, AI automation, information architecture simplification, progressive disclosure, performance budgets (`ENG-133`), accessibility AA (`ENG-134`), production observability (`ENG-135`), backup/restore (`ENG-136`), auto-update (`ENG-137`), subscription/billing (`ENG-138`), SSO/MFA (`ENG-139`), shift management (`ENG-140`), day-close report (`ENG-141`), loss-prevention rules (`ENG-142`), in-transit transfers (`ENG-143`), tip pooling (`ENG-145`), recipe scaling + waste (`ENG-146`), self-checkout / QR table (`ENG-147`), hardware certification matrix (`ENG-148`), shared peripheral resolver (`ENG-149`), universal print server (`ENG-150`), chaos/DR (`ENG-151`), data portability (`ENG-152`), cohort/LTV/RFM (`ENG-153`), receipt branding studio (`ENG-154`), contextual help + video (`ENG-155`), multi-currency (`ENG-156`), public demo sandbox (`ENG-157`), tenant clone (`ENG-158`), data residency + AI gating (`ENG-159`), Nómina Electrónica DIAN (`ENG-160`, Gated), pt-BR + NFe Brazil (`ENG-161`, locale Pending / NFe Gated), hosted micro-storefront (`ENG-162`), compliance readiness pack (`ENG-163`), the hosted-SaaS substrate spike (`ENG-164`, blocks `ENG-157`/`ENG-158`/`ENG-162` + cross-tenant slice of `ENG-138`), and the tRPC-aware rate-limiter (`ENG-165`, prerequisite for `ENG-118`). First recommended pick: `ENG-103`; first UI-refactor pick: `ENG-131` before adding more top-level routes; first cross-cutting spike: `ENG-164` before the hosted-only tickets start. |
| Out-of-band | [`ENG-100`](./ROADMAP.md) | Shipped 2026-05-19 | Offline messaging copy audit — cierra el AC de "every offline claim on the marketing site has a corresponding capability tile in the product that backs it up; no claim outpaces shipped behavior". `OfflineCapabilityCatalog` ahora exporta `OFFLINE_CAPABILITY_CATALOG` como single source of truth de las 6 tiles offline; JSDoc contract pinea las 3 reglas de gobernanza (cada tile respalda un feature shippeado, `status` del enum cerrado refleja runtime real, cambios al catálogo actualizan `WEBSITE-CAPABILITY-AUDIT.md §Tile Catalog` en el mismo commit). Nuevo `OfflineCapabilityGrid.audit.test.ts` (6 cases) pinea cardinalidad + id set + status enum, exige que cada `available` tile declare su backing ENG ticket en un map hardcoded en el test (living documentation que falla si alguien añade un séptimo `available` sin pensar), y bloquea lenguaje absoluto ("100%" / "siempre" / "always" / "totalmente" / "totally" / "completamente" / "fully") en copy de tiles `limited` / `pending` / `blocked` en en + es. `WEBSITE-CAPABILITY-AUDIT.md` gana sección `## Tile Catalog (source of truth)` con tabla de las 6 tiles + status + copy es/en + backing feature, que el website externo debe consumir como copy autoritativa. Cero behavior change runtime — refactor de rename más test contract más doc. Counter Plan v2.0 NO avanza. |
| Out-of-band | [`ENG-014`](./ROADMAP.md) | Shipped 2026-05-19 | Split payments — credit mix ("apartado" / layaway). Lifts the ENG-090 "credit-as-exclusive-tender" restriction so a sale can mix instant tenders (cash / card / transfer) with a credit portion that lands as an IOU on `customer_ledger_entries`. `splitPaymentMethodEnum` widened to include `'credit'`; Zod refines on `createSaleInput` + `completeDraftInput` require `customerId` whenever a tender row is credit. `application/sales/completeSale.ts` sums credit tenders into `creditSaleAmount` across both `runFreshSale` + `runCompleteDraft` and threads that portion through `requireCreditLimitNotExceeded` + `recordCreditSaleLedger`; only the credit slice writes a `customer_ledger_entries.kind='sale'` row, the non-credit tenders settle through the cash session as usual. `policies.ts::resolveSalePayments` demotes credit from the dominant `payment_method` echo when other tenders exist (operator thinks of the sale through the initial-installment lens). `policies.ts::getPaymentStatus` gained an optional `creditAmount` so a split with credit lands as `'partial'` rather than `'paid'`. Defensive throw: `returnSale` rejects refunds for any sale carrying a credit tender with new `REFUND_PARTIAL_CREDIT_NOT_SUPPORTED` error code (en + es neutral LATAM `tú`) until the dedicated reversal flow ships. Web `SalePaymentModal.tsx`: `SplitTenderMethod` widened to mirror `PaymentMethod`; split-tender row select renders credit option gated by `canLendCredit && watchedCustomerId`; V10 customer card lifted out of `!splitMode` so it surfaces in both modes, sized to the credit portion only (not grand total); admin override sanitizer accepts split-credit. New `payment.partialCredit.summary` key shows "$50 de inicial + $150 a crédito" inline. 8 new server tests in `credit-mix-flow.test.ts` cover happy path + cupo per-portion enforcement + admin override on split + customer required + pure-split regression + single-tender regressions; 4 new web tests extend `SalePaymentModal.credit.test.tsx`. Closes the AC of "sales.create accepts credit + cash in the same payload; the sale becomes partial-credit and the credit_sales ledger is created only for the credit portion". ENG-090 deferred mention updated. Counter Plan v2.0 NO avanza (out-of-band, sub-task of ENG-090). |
| Out-of-band | [`ENG-039d2`](./ROADMAP.md) | Shipped 2026-05-19 | Structured modifiers (per-line `sale_items.notes`) — última sub-task implementable del umbrella `ENG-039` antes del Gated `ENG-039f` (CFDI MX). Nueva migración `0033_sale_items_notes.sql` añade `notes TEXT` (nullable, cap 280 chars, mismo límite que `restaurant_tables.notes`) a `sale_items`; `schema.ts` + `_journal.json` mirrored. `saleItemInput` Zod gana `notes: z.string().trim().max(280).nullable().optional()`. `application/sales/completeSale.ts` extiende `ResolvedSaleItem` con `notes: string | null`, normaliza empty-string → NULL en el resolver, y persiste el campo en el `tx.insert(saleItems).values({...})` block. `sale-read.ts::getSaleRecord` añade `notes: saleItems.notes` al SELECT — el web client recibe el campo automáticamente por inferencia de `AppRouter`. KDS pipeline propaga: `KdsItemSnapshot` gana `notes?: string | null`, `services/kds/enqueue.ts::loadItemSnapshots` + `services/kds/refresh.ts::loadItemSnapshots` ambos hacen `select notes`, y `KdsOrderCard.tsx` renderiza la nota como segunda línea italic dim debajo del product name con `data-testid="kds-order-card-item-note"`. El panel sale-level `order.notes` queda como fallback para ventas pre-ENG-039d2 que aún cargan notas agregadas. Voice-ordering surface (`VoiceOrderingScreen.tsx`) drop `buildAggregatedNotes()` — la helper que prepended el table label a `sales.notes` (redundante porque ENG-039c persiste `tableId` separadamente) y el voice cart-command parser ya captura notas per-item en `VoiceCartItem.note`. Cada item ahora attach `notes: trimmedNote && trimmedNote.length > 0 ? trimmedNote : null`; el `notes:` sale-level se elimina del payload de `sales.create`. Nuevo `sale-item-notes.test.ts` (5 cases): item con nota → persistido + visible en `getSaleRecord`; sin nota → NULL; whitespace-only trimmed → NULL; round-trip suspend → resume → completeDraft preserva el campo (los items son inmutables después de draft create); enqueue KDS snapshot incluye notes en `items_json`. `VoiceOrderingScreen.test.tsx` actualizado: el caso "save with notes" ahora asserta `createCall.items[0].notes === null`, `createCall.items[1].notes === 'sin queso'` (per-item), y `createCall.notes === undefined` (no agregación). Colateral inline: rename del umbrella `ENG-039` para drop el sufijo "Mexico" — el restaurante es vertical multi-país LATAM y sólo `ENG-039f` (CFDI MX) es México-específico. Strings cambiados: ROADMAP.md título + body, PLAN-V2.md tabla scope, `architecture/0007-module-activation.md` comentario, `services/modules/manifest.ts:150` JSDoc, `SurfacePlaceholder.tsx:8` + `KdsShell.tsx:5-7` JSDoc. Cero cambios de comportamiento. BACKLOG cleanup: removed `[restaurant][kds] Per-line sale_items.notes column` bullet (ya shippeado). Counter Plan v2.0 NO avanza (out-of-band, sub-task del umbrella `ENG-039`). |

## 2. Recommended sequence

Value-per-day priority, skipping gated tickets:

1. **`ENG-051` — Foundation ADR pack** (Shipped). Locked the architecture decisions before mutating schema or extracting services: Local Store Authority, Command Envelope, Outbox taxonomy, and Conflict policy. See [`docs/architecture/`](./architecture/README.md).
2. **`ENG-052..ENG-056` — Foundation Reset core**. Device registry + idempotency, operation journal + outbox kernel, `completeSale` extraction, sale lifecycle extraction, and cash-session aggregate boundary. Run strictly in order against the ADRs in [`docs/architecture/`](./architecture/README.md).
3. **`ENG-057..ENG-058` — Fiscal readiness without PT gate**. Fiscal outbox + contingency engine first, then fiscal receipt finalization. `ENG-059` stays gated on the real DIAN provider package.
4. **`ENG-060..ENG-062` — Hardware readiness without payment gate**. Peripheral registry + ports, barcode scanner pipeline, then ESC/POS + drawer. Full hardware validation for `ENG-062` waits for the physical lab.
5. **`ENG-064..ENG-067` — Sync, operations, resilience, security**. Sync contract, Operations Center, chaos suite, then backup/restore/local-security hardening.
6. **`ENG-068..ENG-070` — Expansion architecture**. Module activation kernel, multi-surface shell, and event/webhook foundation only after the retail core is safer.
7. **`ENG-071..ENG-075` — Authority Node / Store Hub Mode** (Shipped). Multi-register retail now has the ADR + config contract, explicit `device_local` default, `site_hub` LAN server mode, `hub_client` terminal mode, local hardware bridge, and pairing/health. `ENG-076` stays deferred until a pilot proves terminal-side offline writes are worth the extra sync plane.
8. **`ENG-039a` — Restaurant voice-ordering MVP**. First restaurant slice: use the existing multi-surface shells and sales draft model before adding real table/KDS schemas.
9. **Gated tickets** stay parked until their gates clear: `ENG-021`, `ENG-022`, `ENG-023`, `ENG-059`, `ENG-063`.
10. **Deferred / overlap**: `ENG-024` remains deferred; `ENG-010`, `ENG-011`, and `ENG-014` are revisited after `ENG-054..ENG-056` because the Foundation Reset overlaps their code-quality scope.
11. **`ENG-103..ENG-165` — World-Class LatAm POS wave**. Use [PLAN-V3.md](./PLAN-V3.md) as the tactical bridge. Start with `ENG-103` export/download trust, `ENG-104` setup readiness, the foundation rails `ENG-133` perf budgets / `ENG-134` accessibility AA / `ENG-135` observability, then `ENG-123` launch migration, `ENG-105` cashier speed, `ENG-106` staff accountability, and `ENG-131` navigation simplification unless a pilot demands another V3 capability first; keep public-claim gates in `ENG-102` honest. Run the first `ENG-132` screen-simplification slices before adding more visible complexity through `ENG-110`, `ENG-123`, `ENG-124`, or `ENG-125`. Land `ENG-164` (hosted-SaaS substrate spike — Postgres vs SQLite vs hybrid) before the hosted-only tickets `ENG-157` / `ENG-158` / `ENG-162` and before `ENG-138`'s cross-tenant aggregate slice; land `ENG-165` (tRPC-aware rate limiter) before `ENG-118` (public API). Pull `ENG-117` and `ENG-119..ENG-122` forward when a restaurant, services, pharmacy, supermarket, or hardware-store pilot needs that vertical depth.

Per-quarter detail and architectural decisions closed by the 2026-Q2 audit live in [PLAN-V2.md](./PLAN-V2.md). The post-v2 commercial/product wave lives in [PLAN-V3.md](./PLAN-V3.md). Anything gated stops the flow and raises a question to the user — do not speculate a workaround.

### 2.1 Authority Node execution wave (`ENG-071..ENG-076`)

**One-liner**: keep `device_local` as the default Authority Node while adding explicit `site_hub` and `hub_client` runtime modes for multi-register stores.

| Ticket | Commit shape | Verification |
| --- | --- | --- |
| `ENG-071` | Docs/ADR only: ADR-0008, `AUTHORITY-NODE.md`, ROADMAP/SPRINT links. **Shipped 2026-05-11** — see ROADMAP §3b for the closeout summary. | `git diff --check`; cross-doc links resolve. |
| `ENG-072` | Runtime config resolver + diagnostics/banners. No behavior change. **Shipped 2026-05-08** — see ROADMAP §3b. | `ci:server` + `ci:desktop`; default config test proves loopback `device_local`. |
| `ENG-073` | Store Hub LAN bind + allowed origins + health. **Shipped 2026-05-09** — see ROADMAP §3b. | `ci:server`; local smoke with `HOST=0.0.0.0` and explicit LAN origin; no open CORS wildcard. |
| `ENG-074` | Hub Client runtime config + tRPC base URL switch + reachability UI + checkout gate + carve-out hardware bridge. **Shipped 2026-05-09** — see ROADMAP §3b; carve-out `ENG-074b` shipped same day. | `ci:web` + live browser smoke; bridge override flips API URL to hub URL; checkout gates on `hubReachable === false`. |
| `ENG-074b` | Hub-client local hardware bridge (printer + drawer dispatch). **Shipped 2026-05-09** — see ROADMAP §3b. | `ci:server` + `ci:web` + `ci:desktop`; mocked local bridge prints a hub-authorized receipt without writing operational tables; integration with real USB printer for ENG-075 pilot. |
| `ENG-075` | Pairing codes, device roles, Operations Authority tab. **Shipped 2026-05-11** — see ROADMAP §3b. | `ci:server` + `ci:web` + live Operations smoke. |
| `ENG-076` | Deferred spike: fail-closed vs draft-only vs full satellite offline. | Decision doc only; no runtime writes. |

Scope rules:

- Do not support shared SQLite over network drives.
- Do not add hub-client local sale writes before `ENG-076` is reopened.
- The hub is the only writer in `site_hub`; clients send commands.
- Hub clients may execute terminal-local hardware I/O through a bridge,
  but that bridge must never write operational tables or emit
  operational outboxes independently.
- The central server remains downstream of Authority Nodes.

### 2.2 Restaurant voice-ordering MVP (`ENG-039a`) — **Shipped 2026-05-12**

**One-liner**: turn `/touch` and `/m` into dedicated restaurant ordering surfaces that accept push-to-talk voice commands, review parsed cart lines, and save the order as a suspended sale labeled by table.

The slice landed as one staged commit. Parser schema gained an optional `note` per item (the "sin queso" modifier survives round-trip) plus 10 new integration cases covering note-set / multi-word / special-chars / whitespace / English / word-numerals / 1000-char boundary / prompt-injection bound / 50-item cap. New `apps/web/src/features/restaurants/VoiceOrderingScreen.tsx` shared component renders both `/touch` (tablet two-column) and `/m` (phone-width stacked) variants; new `TouchHome.tsx` + `MobileWaiterHome.tsx` wrappers replace the ENG-069 placeholders in `App.tsx`. The voice modal is lazy-loaded; the manual `ProductSearchDialog` is wired as a fallback. Save flow mirrors `SalesPage.handleSuspendConfirm` (two-step `sales.create({status:'draft'})` + `sales.suspend({label})` with `sales.discardDraft` compensation on step-2 failure). Per-line notes ride into `sales.notes` as a structured aggregate so the cashier can read them on resume; per-item structured notes wait for a later `ENG-039` child. New `restaurants` i18n namespace (en + es, ~22 keys, neutral LATAM `tú`). 15 new web component tests + 2 mount tests; server CI 84.75 / 70.97 / 80.72 / 85.84; web CI 79.05 / 70.65 / 76.75 / 80.28. Live smoke confirmed `/touch` chrome (no sidebar), modal open, manual cart fill with two notes, save round-trip, desktop SuspendedSalesPanel surfacing `Mesa 5`, and the gate redirect when `pos-touch` is off.

Scope rules honored:

- Used the existing sales cart and `sales.suspend` / `sales.resume` draft model; no restaurant-table schema added in this slice.
- Free-form modifiers ("sin queso") flow through the parser, the modal review row, the cart preview, and the persisted sale-level `notes` field.
- No sidebar, sales history, KDS, floor plan, structured tips, or administrative controls render inside the dedicated surfaces.
- Voice stays push-to-talk only (real voice flow requires operator-side mic permission; headless smoke exercised the wiring via the modal open path; the 18-case parser suite + 4-case modal suite pin the transcript → parse → review chain).

## 3. Iter 6 / ENG-018 — Sales park-and-resume

**One-liner**: Multi-cart workspace with `sales.suspend` / `sales.resume` / `sales.listDrafts` + persistent Zustand workspace + panel for suspended sales + Ctrl+P / Ctrl+R shortcuts.

**Domain context**: the backend already has `status='draft'` on `sales` (schema.ts:20). The UI does not: a cashier cannot suspend a cart to attend a different customer. This iter fills the UI gap and extends the backend with the missing procedures.

**3.1 Sequencing (3 commits)**:

1. **Commit 1 — server procedures + schema patches** (~1 day)
   - Extend `sales` with nullable `suspendedAt` (ISO), `suspendedBy` (FK users.id), `suspendedLabel` (optional human number). Raw DDL mirror + Drizzle migration + `ensureColumn`.
   - `sales.suspend({ saleId, label? })` — cashierProcedure, idempotent, persists `status='draft'` + `suspendedAt=now` + `suspendedBy=ctx.userId`. Optional audit log if tenant flag `audit_park_sale` is set.
   - `sales.resume({ saleId })` — cashierProcedure, SELECT FOR UPDATE (SQLite busy_timeout emulates), FORBIDDEN when `suspendedBy !== ctx.userId && ctx.role not in {manager, admin}`. Returns items + discounts + customer + notes and clears the suspended flags.
   - `sales.listDrafts({ filter: { site, cashier?, date? }, page, perPage })` — paginated. Default: cashier sees only their own; manager/admin sees all in the site.
   - `sales.discardDraft({ saleId })` — marks `status='cancelled'` and reverses the stock debited when the draft was created, scoped by tenant and original cash-session site.
   - Tests: suspend→resume round-trip preserves items/discounts/customer/notes; two cashiers cannot resume the same draft concurrently; cashier A cannot resume cashier B's draft but manager can; `listDrafts` respects role scope.

2. **Commit 2 — UI web** (~2 days)
   - `apps/web/src/features/sales/useCartWorkspace.ts` — Zustand store with `Record<draftId, Cart>` + `activeId` + `createDraft` / `suspendActive({ label? })` / `resume(id)` / `discard(id)` / `switchTo(id)`. Persist in `localStorage` keyed `${tenantId}:${userId}` to survive refresh.
   - Refactor `SalesPage.tsx` to read the active cart from the store instead of local `useState`.
   - "Suspender" button in `SalesCheckoutPanel` next to "Charge sale"; "Nueva venta" creates a blank draft.
   - `SuspendedSalesPanel.tsx` (new) — side panel with cards (number + customer + item count + total + age + resume / discard). Badge with count in the header.
   - Extend `useSalesKeyboardShortcuts.ts`: `Ctrl+P` / `Cmd+P` = suspend, `Ctrl+R` / `Cmd+R` = open resume panel. Guard against firing when focus is inside `<input>/<textarea>/[contenteditable]` (reuse existing `isEditableFocused` helper).
   - When `cashSessions.close` is invoked with outstanding drafts from the shift, prompt: "X ventas suspendidas. ¿Continuar de todos modos? Quedarán accesibles para el supervisor" with options "Descartar todos" / "Continuar".
   - i18n: extend `sales` namespace with `park.suspend`, `park.resume`, `park.discard`, `park.emptyState`, `park.shortcutHint`, `park.closedSessionWarning`, `park.confirmDiscardTitle`, `park.confirmDiscardMessage` in both locales.
   - Tests: suspend+resume round-trip preserves state in UI; `Ctrl+P` does not fire while a text input is focused; two concurrent cashiers see a clean error when racing the same draft; close-session prompt appears when drafts exist.
   - E2E in `e2e/web/business.spec.ts`: cashier creates cart A → suspends → creates cart B → charges B → resumes A → charges A. Assert both sales complete and stock decrement is correct per site.

3. **Commit 3 — docs + audit** (~0.5 days)
   - Add `sale.park` and `sale.resume` to `auditLogActionEnum` (string enum, no migration), gated by the `audit_park_sale` tenant flag.
   - Update `ROADMAP.md` ENG-018 row with "Shipped: <summary>".
   - Update `TEST-PLAN.md` marking PARK-01 through PARK-06 as automated.

**3.2 Edge cases**: empty listDrafts → empty state with CTA; skeleton in panel while loading; suspend network failure → toast + keep local cart; offline (Electron) works 100% against embedded SQLite; Zod rejects suspending a `status !== 'draft'` sale; cashier trying to resume someone else's draft gets a translated FORBIDDEN; parity test green.

**3.3 Draft commit messages**:

```
feat(pos): sales.suspend sales.resume sales.listDrafts procedures

- Extend sales table with suspendedAt suspendedBy suspendedLabel nullable columns
- Suspend is idempotent, resume enforces per-cashier lock with manager override
- Cover cross-cashier lock, role based scope, round trip field preservation
```

```
feat(pos): park and resume UI with multi cart workspace and shortcuts

- Add useCartWorkspace Zustand store persisted per user
- Add SuspendedSalesPanel with list, resume, discard actions
- Wire Ctrl P and Ctrl R shortcuts with input focus guard
- Warn when closing a cash session with outstanding drafts
- Add park.* keys to sales i18n in en and es
```

```
docs(pos): mark ENG-018 park and resume shipped
```

## 4. Iter 7 / ENG-019 — Sales receipt reprint

**One-liner**: `sales.getForReprint` procedure + reprint counter + audit `sale.reprint` + reprint button in history and sale details modal + Ctrl+Shift+P shortcut.

**Domain context**: `print-receipt` IPC handler (apps/desktop/src/main/index.ts:1708) accepts HTML and prints immediately, but no procedure exposes a past sale for reprint. When the printer runs out of paper or the hardware fails, the cashier cannot reprint from history — the sale is trapped behind a one-way flow.

**4.1 Sequencing (2 commits)**:

1. **Commit 1 — server + audit** (~1 day)
   - Add `reprintCount: integer default 0`, `lastReprintedAt: text`, `lastReprintedBy: text (FK users.id)` to `sales`. Raw DDL + `ensureColumn` + migration.
   - `sales.getForReprint({ saleId, reason? })` — cashierProcedure. Returns the full `saleRecord` (items + payments + customer snapshot when Iter 3 Fase A lands). Increments counter + timestamp + user. Emits `sale.reprint` audit log with metadata `{ reason?: string, count: number }`.
   - Permissions: cashier may reprint their own sales from the current shift; manager and admin may reprint anything within the tenant.
   - Tests: round-trip (getForReprint matches getById shape); counter increments; audit row emitted; permissions honored; cross-tenant isolated.

2. **Commit 2 — UI** (~1.5 days)
   - `SalesHistoryPage.tsx` — add "Reimprimir" row action with `Printer` icon (disabled without permission).
   - `SaleDetailsModal.tsx` — prominent "Reimprimir recibo" button that calls `getForReprint` first then `printSaleReceipt()`; optional reason dropdown (Paper out / Customer request / Previous print error / Other with free text).
   - Banner in the modal when `reprintCount > 0`: "Reimpresa {count} veces. Última: {lastReprintedAt} por {lastReprintedBy}".
   - Shortcut: with a row selected on `SalesHistoryPage`, `Ctrl+Shift+P` reprints (`Ctrl+P` is taken by Iter 6).
   - i18n: extend `sales` namespace with `reprint.button`, `reprint.title`, `reprint.reasonLabel`, `reprint.reasonOptions.*`, `reprint.confirmTitle`, `reprint.successToast`, `reprint.errorToast`, `reprint.historyBanner`, `reprint.noPermission`.
   - Tests: button visible with permission, disabled without, reprint round-trip preserves items/payments/totals, toast success, banner updates after reprint.
   - E2E extension: admin completes a sale → navigates to history → reprints → DB shows `reprintCount = 1` and audit log has the `sale.reprint` row.

**4.2 Edge cases**: skeleton while fetch; network failure → toast, no counter bump; Electron works 100% offline; Zod rejects invalid `saleId`; cashier trying to reprint another cashier's past-shift sale gets FORBIDDEN; `status='draft'` sale disables the button (those are Iter 6 suspended carts); `status='void'` allows reprint with an "ANULADA" watermark for trace.

**4.3 Integration with Iter 2 and Iter 4**: reuses the `receipt-renderer.ts` from Iter 2 (no duplication). If Iter 4 shipped and `EscPosPrinterAdapter` is configured, reprint uses ESC/POS natively and can open the cash drawer on reprint (useful at end-of-shift reconciliation). If Iter 7 lands before Iter 4, fallback path is `SystemPrinterAdapter` — zero tech debt.

**4.4 Draft commit messages**:

```
feat(sales): sales.getForReprint procedure with reprint counter and audit

- Add sales.getForReprint returning full sale snapshot for re-emission
- Track reprintCount lastReprintedAt lastReprintedBy on sales row
- Emit sale.reprint audit log with optional reason
- Cover permissions cashier-own vs manager-any and cross tenant isolation
```

```
feat(sales): reprint receipt action in history and sale details

- Add reprint button in SalesHistoryPage and SaleDetailsModal
- Capture optional reason and show reprint history banner
- Add Ctrl Shift P shortcut and reprint.* keys to sales i18n in en and es
- Cover round trip reprint, disabled states, void sale watermark
```

## 5. Iter 3 Fase A / ENG-020 — Fiscal DIAN data model + MockAdapter

**One-liner**: `fiscal_documents` + `fiscal_document_items` with immutable buyer and line snapshots + global `dian_identification_types` + `FiscalAdapter` interface + `MockAdapter` + hooks in `sales.complete`/`return`/`void` + architectural lint banning fiscal reports from joining with `customers`/`products`.

**Gates**: none. The MockAdapter is deterministic against DIAN Anexo 1.9 canonical vectors. **Depends on ENG-017 landing first** so the buyer currency snapshot has the right currency code.

**Full detail**: see [FISCAL-INTEGRATION.md](./FISCAL-INTEGRATION.md) and the five architectural decisions captured in that doc (§Modelamiento del adquiriente y líneas):

1. Global read-only `dian_identification_types` (CC=13, NIT=31, TI=12, CE=22, PA=41, RC=11, NUIP=91, TDE, PEP, PPT) — not tenant-scoped.
2. Immutable buyer snapshot in `fiscal_documents` (`buyerTaxId`, `buyerTaxIdTypeCode`, `buyerName`, `buyerEmail`, `buyerAddress`, `buyerCity`, `buyerDepartment`, `buyerCountry`, `buyerPersonType`, `buyerFiscalRegime`, `buyerFiscalResponsibility`).
3. Consumidor final for DEE POS = `buyerTaxId='222222222222'` + `buyerTaxIdTypeCode='31'` + `buyerName='Consumidor final'` — no ficticious customer row.
4. Immutable line snapshot in `fiscal_document_items` (`productName`, `productSku`, `unitMeasureCode`, `quantity`, `unitPrice`, `discountAmount`, `taxRate`, `taxAmount`, `taxCategoryCode`, `lineTotal`).
5. Architectural lint: vitest that parses `routers/reports/fiscal*` imports and fails on any reference to `customers` or `products`.

**5.1 Sequencing (5 commits, ~2 weeks)**:

1. **Commit 1** — Global `dian_identification_types` + seed + migration for `customers.dianIdentificationTypeId` (data migration by `abbr`).
2. **Commit 2** — `fiscal_documents` + `fiscal_document_items` + `fiscal_numbering_resolutions` + `fiscal_certificates` tables with all snapshot fields.
3. **Commit 3** — `FiscalAdapter` interface + `MockAdapter` with canonical CUFE SHA-384 and simulated contingency.
4. **Commit 4** — `fiscal-documents` orchestrator + hooks in `sales.complete`/`void`/`return` + dev seed for `fiscal_numbering_resolution` + `fiscal_certificate`.
5. **Commit 5** — `FiscalHabilitationWizard` placeholder + `FiscalContingencyIndicator` + `FiscalDocumentListPage` + `FiscalReportsPage` + architectural lint.

**5.2 Status**: **Shipped**. All five commits staged end-to-end:

- Commit 1 — migration `0004_dian_identification_types.sql` + `seedDianIdentificationTypes` (10 DIAN codes).
- Commit 2 — migration `0005_fiscal_documents.sql` + 4 fiscal tables with the immutable buyer + line snapshots in `schema.ts` + `fiscalDocumentsRelations` drizzle joins.
- Commit 3 — `services/fiscal/{cufe,adapter,mock-adapter,registry}.ts` + 13 tests (6 CUFE avalanche + canonical-order assertions, 7 MockAdapter issue/void/fetchStatus assertions).
- Commit 4 — `services/fiscal/orchestrator.ts` with `emitFiscalDocument` idempotent by `(tenantId, source, sourceId, kind)`; four hooks wired into `sales.ts` (`create` completed / `completeDraft` / `void` / `returnSale`) as best-effort post-tx calls behind `tenants.settings.fiscal_dian_enabled`. Dev seed now enables the flag and inserts one DEE resolution per site + placeholder cert for `demo-co`; 10 orchestrator integration tests cover site-scoped resolution lookup and rollback when fiscal persistence fails mid-write, plus extended seed-dev assertion (20 sales → 20 fiscal documents).
- Commit 5 — `trpc/routers/reports/fiscal.ts` + `reports/index.ts` aggregator mounted on `appRouter`, `trpc/schemas/fiscal.ts`, `architectural-lint.test.ts` (regex-based guard + synthetic positive test), `fiscal-reports.test.ts` (7 tests across list, paginated total count, getByCufe, cross-tenant isolation, FORBIDDEN, NOT_FOUND). Web UI: `FiscalDocumentListPage`, `FiscalReportsPage`, `FiscalHabilitationWizard`, header-mounted `FiscalContingencyIndicator`; `fiscal` i18n namespace registered en/es; admin-only `/fiscal-documents` and `/fiscal-reports` routes.

ENG-021 is the one-file swap of `MockAdapter` → `FactureAdapter`/`HkaAdapter` — the seams hold (FiscalAdapter interface + registry + hooks).

## 6. Iter 4 / ENG-022 — Hardware POS 🟡

**Gates**: physical test lab (thermal printer, USB HID scanner, RJ11 cash drawer) — do not start without confirmed hardware.

**One-liner**: `PrinterAdapter` with `system` and `escpos` drivers + cash drawer kick via ESC/POS + `useBarcodeScanner` hook (EAN-13 + price-embedded prefix 20-29) + peripherals configuration page.

**Full detail**: see [HARDWARE-POS.md](./HARDWARE-POS.md).

## 7. Iter 3 Fase B / ENG-021 — Fiscal DIAN PT integration 🟡

**Gates** (six, documented in [FISCAL-INTEGRATION.md](./FISCAL-INTEGRATION.md) §Pre-implementation checklist):
1. PT contract signed (Facture / HKA / Gosocket).
2. Sandbox + production credentials delivered.
3. Tenant pilot's DIAN digital certificate.
4. DIAN numbering resolution associated with the software.
5. POC validated out-of-repo: DEE + FEV + NC + ND + fetchStatus.
6. PT error-code → `ServerErrorWithCode` mapping agreed.

**Swap-only diff**: replaces `MockAdapter` with `FactureAdapter` / `HkaAdapter`. No domain changes beyond the adapter file.

## 8. Iter 5 / ENG-023 — Bold payment terminal 🟡

**Gates**: Bold sandbox credentials + Bluetooth device for tests. Also **depends on ENG-022 commit 1** which installs the `PaymentTerminalAdapter` interface.

**One-liner**: `BoldAdapter` calls the Bluetooth SDK for charge / void / printSlip; `SalePaymentModal` integrates the adapter with `ManualAdapter` fallback.

## 9. Iter 8 / ENG-024 — Inter-site transfer with reservation ⚪

**Status**: deferred proposal. The user surfaced this as a concern, not a firm requirement.

**Scope**: extend `transferOrderStatusEnum` with `requested`/`approved`/`rejected`; use `inventory_balances.reserved` (currently always 0); enable destination-initiated requests; add `initiatedBy`/`requestedBy`/`approvedBy`/`rejectedBy`/`rejectionReason`/`expectedArrivalAt`; state machine `create → requested → approved → in_transit → completed`; UI inbox + timeline. ~1.5 weeks.

## 10. Cross-iteration concerns

- **i18n parity** must stay green after each iter (`apps/web/src/i18n/locale-parity.test.ts`).
- **Drizzle migrations** — one migration per iter that touches schema; update `schema.ts`, generate the SQL migration, register the journal entry, and use `IF NOT EXISTS` when Drizzle's SQLite dialect cannot emit the construct (partial unique indexes, `WHERE` clauses). `db/index.ts` is no longer a raw-DDL mirror after ENG-002 Step 3.
- **Audit trail** — `auditLogActionEnum` is free-form strings; adding a new action never needs a migration. But `AuditLogsTable.tsx` needs an i18n key per action.
- **E2E suite** — 25+ tests in `e2e/web/`; every closing commit runs `npm run test:e2e:web` before pushing.
- **Electron boundary** — any peripheral or FS call goes through `ipcMain.handle` → `contextBridge.exposeInMainWorld('electron', {...})` → `window.electron.*`. Never `require('fs')` in the renderer.
- **Seed data** — when an iter adds a schema with defaults that matter for tests (like a fiscal resolution row), extend `packages/server/src/db/seed-dev.ts` so `npm run seed:dev` keeps producing a ready-to-demo dataset.

## 11. Verification matrix (per iter, before the closing commit)

| Check | Command | Must pass |
|---|---|---|
| Typecheck web | `npm run typecheck --workspace=@puntovivo/web` | ✓ |
| Typecheck server | `npm run typecheck --workspace=@puntovivo/server` | ✓ |
| Typecheck desktop | `npm run typecheck --workspace=@puntovivo/desktop` (if main changed) | ✓ |
| Lint web | `npm run lint --workspace=@puntovivo/web` | 0 new errors |
| Lint server | `npm run lint --workspace=@puntovivo/server` | 0 new errors |
| Tests server | `npm run test --workspace=@puntovivo/server -- --run` | all pass |
| Tests web | `npm run test --workspace=@puntovivo/web -- --run` | coverage ≥ 70/70/70/70 |
| i18n parity | included in web test run | ✓ |
| E2E | `npm run test:e2e:web` | all pass |
| CI aggregate | `npm run ci:web` and `npm run ci:server` in parallel | ✓ |
| Smoke web | manual boot `dev:web` + `dev:server`, driven with Playwright MCP | concrete strings asserted |
| Smoke Electron | `npm run dev:desktop` + validate feature + IPC when applicable | ✓ (or explicit gap declared) |
| Review skills | `typescript-react-reviewer` + `node` in parallel on the diff | zero HIGH blockers |

## 12. Update protocol

When an iter closes, do three things in the last commit:

1. Update this file: move the iter row in §1 to "✓ Shipped", shorten the full iter section to a one-sentence summary + "Shipped <date> — see <ENG-NNN>", link to the closing commit.
2. Update the matching row in `docs/ROADMAP.md` §3b: append "Shipped: <2-3 line summary>" to the Description column (matching the existing ENG-003..009 style).
3. If the iter introduced new docs (spec, runbook), add them to `docs/PLAN.md` §18.1 "Already landed".
