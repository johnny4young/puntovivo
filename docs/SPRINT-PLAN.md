# Puntovivo — Sprint Plan

> Tactical execution plan for the next implementation slices.
> `ROADMAP.md` remains the canonical ENG ticket list; this file only keeps
> the active sequence, commit shape, and verification checklist. Historical
> shipped detail lives in [ARCHIVED.md](./ARCHIVED.md).

Updated: 2026-06-01.

## Current Focus

Run one ticket at a time. The first line of a shipping turn is:

```text
Executing <ENG-NNN> — <one-liner>
```

The current focus wave is the product-truth and retail-scope reset, now continuing into the
`ENG-132` list-screen simplification track (smallest useful column set + secondary-into-drawer,
one screen per commit):

| Order | Ticket    | Status  | Intent                                                    | Required proof                                                                                                                                       |
| ----- | --------- | ------- | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | `ENG-182` | Shipped | Product doctrine and README truth reset.                  | README, SELLABILITY, ROADMAP §0, docs index, and command docs agree on pnpm 11, Node 24, local-first retail scope, and demo/pilot/production status. |
| 1a    | `ENG-182a` | Shipped | Token-budget cleanup + dead-code hygiene.                 | ROADMAP / BACKLOG / ARCHIVED are compact without losing non-shipped tickets; confirmed-zero-import code/deps are removed; ci + live smoke are green. |
| 2     | `ENG-183` | Shipped | Retail Ring-1 scope gate and module exposure cleanup.     | Fresh retail tenant hides non-core surfaces unless profile/module enables them; tests prove no route/sidebar/palette leaks.                          |
| 3     | `ENG-184` | Shipped | Colombia retail readiness profile and checkout preflight. | `/company` + `/sales` surface fiscal/hardware/sync/payment as optional reminders (never blockers); basic CO DIAN config card ships; en/es + recovery CTAs proven.          |
| 4     | `ENG-185` | Shipped | Fiscal adapter truth guard.                               | Unsupported countries fail with a typed error; mock/draft packs are labelled Demo/Draft across cards, document views, and diagnostics; e-invoicing stays optional.   |
| 5     | `ENG-186` | Shipped | Ring-1 screen focus pass (re-scoped: `/sales` sellability slice). | `/sales` completes the common sale without desktop scroll at 1440x900 (cart + checkout scroll internally); History + Suspended behind a reusable Drawer; live smoke desktop + mobile, en/es. Operations + Setup slices split to `ENG-187`. |
| 6     | `ENG-187` | Shipped | Ring-1 screen focus pass — Operations needs-attention (re-scoped: `/operations` slice). | `/operations` defaults to a Needs-attention queue (server aggregation of retryable sync/fiscal/hardware/payment failures, each row deep-links to its panel) + tested all-clear state; live smoke en/es covered the failure row + CTA. Setup/`/company` restructure split to `ENG-188`. |
| 7     | `ENG-188` | Shipped | Ring-1 screen focus pass remainder (Setup/`/company` readiness-hub restructure). | **Shipped** (web-only): the flat 10-pill `segmented-control` in `CompanyPage` became a grouped Setup nav — readiness pinned as the admin landing, the other 9 tabs demoted into three labeled groups (Negocio / Facturación y pagos / Sistema), modeled as a navigation (`role="group"` + `aria-current`, panel `role="region"`). The `?tab=` deep-link contract is preserved, so the readiness CTAs / GlobalStatusStrip / AuthProvider keep working. `ci:web` green; live `/company` smoke en/es. Closes the screen-focus wave. |
| 8     | `ENG-132a` | Shipped | Screen simplification — Products list (first `ENG-132` list-screen slice). | `/products` table trimmed to the smallest useful column set (name+SKU / category / tier-1 / stock / status); provider, location and tier-2/3 prices moved into a row-detail `ProductDetailsDrawer` (additive Details eye action + edit hand-off). ENG-134f keyboard edit + CSV/XLSX export parity preserved. `ci:web` green; live `/products` smoke en/es. Remaining `ENG-132` screens (one per commit): Inventory, Orders, Purchases, Quotations, Customers, Finance. |
| 9     | `ENG-132b` | Shipped | Screen simplification — Customers list (`ENG-132` list-screen slice). | `/customers` table trimmed to name(+identification) / status / actions; email, phone, type and location moved into a row-detail `CustomerDetailsDrawer` (additive Details eye action + ungated edit hand-off mirroring the row). ENG-134f keyboard edit + ledger/delete gating preserved. Added the FIRST tests for the customers folder (drawer + page). `ci:web` green; live `/customers` smoke en/es. Remaining `ENG-132` screens: Inventory, Orders, Purchases, Quotations, Finance. |
| 10    | `ENG-132c` | Shipped | Screen simplification — Inventory Stock tab (`ENG-132` list-screen slice). | Stock table trimmed to name(+sku/category) / stock / status / actions; min stock, sell price, valuation and updated date moved into a new `InventoryStockDetailsDrawer` (additive Details eye action + canManage-gated Adjust hand-off to the existing adjustment modal). Scope = Stock tab only (Movements/Entries/Balances untouched). Added drawer test + a pure-function column-set test (exported `getStockColumns`). `ci:web` green; live `/inventory` Stock smoke en/es. Remaining `ENG-132` screens: Inventory Movements/Entries, Orders, Purchases, Quotations, Finance. |
| 11    | `ENG-132d` | Shipped | Screen simplification — Quotations list (`ENG-132` list-screen slice). | Quotations history table trimmed to number / customer / total / status / actions; site, items, valid-until and created-at lean on the EXISTING View → `QuotationDetailsModal` (no new drawer — it already has one). The modal already showed valid-until / created-at / line items; Site added to it (web-only, payload already carries `siteName`). Added `quotations:details.site` + a column-set test assertion. `ci:web` green; live `/quotations` smoke en/es. Remaining `ENG-132` screens: Orders, Purchases, Inventory Movements/Entries, Finance. |
| 12    | `ENG-132e` | Shipped | Screen simplification — Orders list (`ENG-132` list-screen slice). | Orders history table trimmed to order # / provider / status / total / actions; date, site and receipts lean on the EXISTING View → `OrderDetailsModal` (no new drawer — it already shows created / site / staged-delivery + receipts list). No new i18n key and no server change (cleanest 132 slice); Status badge keeps receiving progress legible on the list. Dropped the unused `formatDateTime` import; updated `OrdersHistoryTable.test.tsx` (receipt-progress moved to the modal) + added a column-set assertion. `ci:web` green; live `/orders` smoke en/es. Remaining `ENG-132` screens: Purchases, Inventory Movements/Entries, Finance. |
| 13    | `ENG-132f` | Shipped | Screen simplification — Purchases list (`ENG-132` list-screen slice). | Purchases history table trimmed to purchase # / provider / status / total / actions; date, site and returns lean on the EXISTING View → `PurchaseDetailsModal` (no new drawer — it already shows created / site / supplier-returns summary + return history). No new i18n key and no server change; the CSV/XLSX export is separate so it keeps every field (export test stays green); Status badge keeps return state legible on the list. Dropped the unused `formatDateTime` import; updated `PurchasesHistoryTable.test.tsx` (returns-cell text moved to the modal) + added a column-set assertion. `ci:web` green; live `/purchases` smoke en/es. Remaining `ENG-132` screens: Inventory Movements/Entries, Finance. |
| 14    | `ENG-132g` | Shipped | Screen simplification — Inventory Movements + Entries tabs (`ENG-132` list-screen slice; closes the Inventory track). | Both Inventory log tabs trimmed to the smallest useful set (Movements: date / product / movement / type; Entries: date / mode / product / counted qty), each + a Details (eye) action. These tabs had no detail view, so — per operator decision — added NEW `InventoryMovementDetailsDrawer` / `InventoryEntryDetailsDrawer` (mirror the 132c stock drawer) holding the trimmed fields. Extracted the inline columns to pure `getMovementColumns` / `getEntryColumns` modules (testable); exports unchanged (keep every field). Added `inventory:movements.details.*` + `inventory:entries.details.*` (en/es). 4 new tests (2 column-set, 2 drawer). `ci:web` green; live `/inventory` Movements + Entries smoke en/es. Remaining `ENG-132` screen: Finance/Compliance (undefined scope — needs operator definition). |
| 15    | `ENG-132h` | Shipped | Screen simplification — Fiscal Documents list (`ENG-132` list-screen slice; CLOSES the track). | `/fiscal-documents` compliance table trimmed 8→6 + actions (when / document number / kind / status / buyer / total); the raw provider id + truncated CUFE move into a new `FiscalDocumentDetailsDrawer` (shows full CUFE + a View XML hand-off to the existing modal), opened by a Details (eye) action. Custom HTML table kept; frozen-snapshot read invariant untouched. Of the 3 Finance routes only Fiscal Documents needed a trim (fiscal-reports = shell; audit-logs = already minimal). Added `FiscalDocumentListItem` to types/api.ts + `fiscal:list.details.*` (en/es). 2 new tests (drawer + first page test). `ci:web` green; live `/fiscal-documents` smoke en/es. **ENG-132 is now fully Shipped.** |
| 16    | `ENG-133b` | Shipped | Perf budgets — Electron main+renderer memory ceiling (slice of `ENG-133`). | Extended the perf-budget engine with `electronMemoryMb` (perProcessMb main/renderer + 25% threshold). Flag-gated measure mode in the desktop main (`PUNTOVIVO_MEASURE_MEMORY=1` → `app.getAppMetrics()` on did-finish-load, print + quit); `scripts/check-electron-memory.mjs` launches the built app, maps Browser→main/Tab→renderer, compares warn-first + self-skips when Electron can't launch (keeps ci:desktop green on ubuntu). 12-case `node --test`. Baseline captured locally (main ~250 / renderer ~160 MB, PASS). Wired into `ci:desktop`. `ENG-133` stays Partial: Lighthouse-CI + the hard-fail CI launch (xvfb) remain. |
| 17    | `ENG-133c` | Shipped | Perf budgets — Lighthouse web-vitals gate (slice of `ENG-133`). | Fourth perf metric: `lighthouse` section in `perf-budget.json` (perRoute LCP/TTI/CLS/score + 30% threshold). `scripts/check-lighthouse.mjs` launches Chromium via Playwright, logs in once (refresh-cookie re-auth survives `disableStorageReset` so authenticated routes measure), warms each route (Vite on-demand compile), runs Lighthouse, compares warn-first + self-skipping. 14-case `node --test` wired into `ci:web`; the browser gate stays LOCAL (`pnpm run perf:lighthouse`, not push-CI). Baseline captured locally (warm dev run, PASS). `ENG-133` stays Partial: the Electron memory hard-fail (xvfb) + promoting Lighthouse into push-CI remain. |
| 18    | `ENG-134g` | Shipped | Accessibility — module-gated surface a11y smoke (slice of `ENG-134`). | The 5 module-gated surfaces (`/touch`, `/kds`, `/customer-display`, `/m`, `/delivery`) join the Playwright axe catalogue: new `ensureModulesEnabled` in `e2e/shared/baseline.ts` force-enables their modules in the e2e seed (same `json_set` of `$.modules.<id>` as `modules.setActive`); 5 new `A11yRoute` entries (admin role, locale-agnostic `data-testid` settled locators). All 5 pass the WCAG 2 A+AA serious floor with ZERO inline fixes — 17/17 a11y entries green; no app/i18n change. Surfaced (not caused) a pre-existing ENG-186 `/sales` cash-panel layout regression (4 cash-session E2E tests fail at 1280x720, reproduces with modules off) → promoted and closed as `ENG-189`. `ENG-134` stays Partial: only the VoiceOver / NVDA screen-reader sweep remains. |
| 19    | `ENG-189` | Shipped | /sales cash-control reachability at constrained viewports (promoted from the ENG-134g BACKLOG find). | The ENG-186 no-page-scroll lockup collapsed the checkout scroll region to 0px at short viewports (1280x720 / 1366x768), burying the cash-session controls (Registrar movimiento / Cerrar caja) off-screen → 4 cash-session E2E tests failed. New Tailwind v4 `@custom-variant pos` (>= 1280px wide AND >= 900px tall) gates the height-lockup classes `xl:`→`pos:` across `SalesPage` + both column components + `theme.css`; below 900px tall the layout falls back to natural page scroll (everything reachable), at >= 900px the ENG-186 pinned-Cobrar internal-scroll layout is unchanged. Diagnosed live (`elementFromPoint`: 0px scroll region at 840px tall = lockup-on dead zone → threshold 900). Full `test:e2e:web` 55/55 green; `ci:web` green; live smoke EN+ES at 1280x720 + 1440x900, 0 console errors. |
| 20    | `ENG-135b` | Shipped | Observability — Sentry/GlitchTip adapter + Electron main crash path (slice of `ENG-135`; non-UI rail). | `@sentry/node` + `@sentry/browser` wired to the EXISTING sink interfaces, DSN-gated on both sides (server: `PUNTOVIVO_SENTRY_DSN` read inside `createServer`, dynamic SDK import, never-throw init, `defaultIntegrations: false` so the ENG-133 latency budgets stay untouched; web: lazy `lib/sentry.ts` chunk behind `VITE_PUNTOVIVO_SENTRY_DSN` — a DSN-less build DCEs it to 0 bytes, so no perf-budget entry). Electron main gains its first `uncaughtException`/`unhandledRejection` handlers (`crash-telemetry.ts`: log + `captureProcessCrash` + bounded flush + exit 1; rejections never exit). Two-layer consent model documented in OBSERVABILITY.md (per-tenant opt-in for tenant-attributed events; DSN = operator consent for tenant-less diagnostics; render events forwarded tenant-less). 15 server + 6 web + 8 desktop tests, SDK mocked. Review follow-ups closed in the same diff: standalone crash handlers forward to the sink, vitest setupFile strips ambient DSN, CSRF 403 became a tRPC-shaped envelope, and 4 drifted chunks got perf-budget entries (new-chunk warnings now zero). `ci:server` + `ci:web` + `ci:desktop` green; live smoke both DSN states vs a local envelope catcher. `ENG-135` stays Partial: dashboard + trace propagation + real-instance validation remain. |
| 21    | `ENG-135c` | Shipped | Observability — trace propagation renderer → server via x-correlation-id (slice of `ENG-135`; non-UI rail). | Web client mints a UUID per tRPC request in the existing headers() callback; new `observability/correlation.ts` sanitizes the attacker-controlled header (strict 8..64 URL-safe intake, correlation-only — Fastify requestId stays as the non-spoofable binding) and feeds the request-scoped logger bindings + tracing middleware (valid header wins, reqId fallback keeps SSE/curl identical); CORS allowlist + captureRenderError stamping (last-request id on client error events). 22 server + 6 web tests; `ci:server` + `ci:web` green; live smoke proved the same id on renderer header, server log line, and sink event. `ENG-135` stays Partial: dashboard + real-instance validation (both externally gated). |
| 22    | `ENG-105g` | Shipped | Sales Cockpit — palette most-used / recent ordering (slice of `ENG-105`). | Device-local usage tracking (new `lib/paletteUsage.ts`, tenant-scoped localStorage, never sent to the server) feeds a Recent section (top 5, count desc + recency tiebreak) above the stable catalogue when the palette opens with an empty query; active query keeps the plain filter. Ranking runs AFTER the role/module gate; section headers are aria-hidden so the listbox stays flat for AT; clean storage renders the exact pre-105g order (pinned — 17 pre-existing palette tests untouched). New `palette.groups.recent` en/es. 11 store + 6 component tests; `ci:web` green; live smoke EN+ES round-trip (use actions → reopen → ranked section). `ENG-105` stays Partial: payment drawer redesign + layout polish await operator visual direction. |
| 23    | `ENG-167b` | Shipped | SQLCipher — one-shot cleartext migration + cross-device restore key prompt (closes the ENG-167 production gate). | First boot of the upgraded build encrypts a pre-Step-1 cleartext local.db in place (header-detect → WAL checkpoint → temporary .bak → PRAGMA rekey → integrity verify → drop the .bak; failure restores the original and aborts the boot; dev-shared DATABASE_URL excluded). Cross-device restores prompt for the source device's key, rekey the staged DB to the local key, and swap; legacy cleartext bundles restore directly and encrypt on next boot. Admin-gated "View backup key" reveal makes the flow operable (trade-off in SECURITY.md). 7+3 node --test cases with real DB files + 4 renderer cases; `ci:desktop` + `ci:web` green; settings chunk budget 8→9 plus CompanyPage 30→33 (deliberate, $retune7/$retune8). `ENG-167` stays Partial: only the operator-run cross-OS `build-desktop.yml` matrix remains. |
| 24    | `ENG-177c` | Shipped | Optimistic versioning W4 part 4 — DB-level cash-session invariant on `sales`. | Added `chk_sales_cash_session_or_draft` (`CHECK (cash_session_id IS NOT NULL OR status = 'draft')`) via Drizzle table-rebuild migration `0001`, pinning the ENG-042/055 cash-session rule at the storage layer. Because drizzle wraps migrations in one transaction (where `PRAGMA foreign_keys` is a no-op), `db/index.ts` now brackets the migrate span with connection-level `foreign_keys = OFF` (empirically the only lever that stops `DROP TABLE sales` cascading into `sale_items`) and runs a post-migrate `foreign_key_check`. No data shim needed — both INSERT sites already bind a session. New `sales-cash-session-constraint.test.ts` (AC enforcement + FK-safe-rebuild positive/negative); `ci:server` green; server-only, no UI/smoke. `ENG-177` stays Partial: only `ENG-177b` (soft-delete policy) remains. |
| 25    | `ENG-169` | Shipped | Input boundary hygiene — closes the audit-closure wave (W5). | Four bounded hardenings: backup-restore ZIP allowlist (rejects `..`/absolute/non-allowlisted entries instead of silently ignoring them); `companies.logoUrl` + `products.imageUrl` route through `isUrlSchemeBlocked` in a schema `.refine`; `companies`/`customers`/`providers` emails normalise via the existing `emailField()` (trim+lowercase); `createServer` refuses to boot with `verbose && NODE_ENV=production` unless `PUNTOVIVO_ALLOW_VERBOSE_PROD=1`. New `eng169-input-hygiene.test.ts` + 3 backup-restore cases; `ci:server` + `ci:desktop` green; server + desktop-main only, no UI smoke. Manifest-signing deferred (no signing infra). |
| 26    | `ENG-165` | Shipped | tRPC-aware rate limiting + per-tenant/site/user buckets (first V3 infra slice after the audit wave; prerequisite for ENG-118 public API). | New `bucketRateLimit` middleware on the base `publicProcedure` classifies by path+type+auth into differentiated buckets — `auth.*` skipped (existing strict limits), authed `sales.*` mutation → sales-write (tenant+site+user), other mutation → write, query → read, unauth → public (IP), `publicApi.*` → public-api (ready for ENG-118). Env-tunable generous defaults; reuses `procedureRateLimit`'s in-memory store (extended with `tenantId`/`siteId` dimensions + once-per-window first-denial signal) + its test bypass. Hits write one `systemAuditLogs` row per window and throw `TOO_MANY_REQUESTS`; the global `@fastify/rate-limit` stays as the coarse net. New `bucket-rate-limit.test.ts` + tenant cases; `ci:server` green; server-only, no UI smoke. |
| 27    | `ENG-178` | Partial | Megafile decomposition wave 2 — anomaly detection split (slice 1). | Server-only refactor of `services/ai/anomalyDetection.ts` into `anomalyDetection/{types,stats,snooze,audit,detectAnomalies,index}.ts` plus per-detector modules for tickets/hour, void rate, refund amount, and no-sale sessions. Public AI exports stay stable through `anomalyDetection/index.js`, the existing anomaly test import moved with the barrel, and every new module is < 500 LOC. `ci:server` green; no UI smoke required. `ENG-178` stays Partial: schema, desktop main, sales/purchases routers, completeSale, SalesPage, ReceiptTemplateEditor, ProductFormModal, and inventory-transfers remain. |
| 28    | `ENG-178` | Partial | Megafile decomposition wave 2 — inventory-transfers split (slice 2). | Server-only refactor of `services/inventory-transfers.ts` (1 146 LOC) into `inventory-transfers/{types,helpers,create,voidTransfer,receive,queries,index}.ts`. Behavior-preserving move: all 20 declarations (11 interfaces + 9 functions) match HEAD after ignoring the helper `export` keywords needed for cross-file reuse; the sole importer `trpc/routers/transfers.ts` updated its import path; `transfers.test.ts` unchanged since it drives the tRPC router (no direct import). Every new module < 500 LOC (largest `voidTransfer.ts` ~290). `ci:server` green (2183 tests); server-only, no UI smoke. `ENG-178` stays Partial: schema, desktop main, sales/purchases routers, completeSale, SalesPage, ReceiptTemplateEditor, ProductFormModal remain (8 files). |
| 29    | `ENG-178` | Partial | Megafile decomposition wave 2 — sales router split (slice 3). | Structural split of `trpc/routers/sales.ts` (1 479 LOC) into `sales/{helpers,queries,lifecycle,drafts,splitDraft,index}.ts`. All 15 procedures are byte-identical vs HEAD, and all 7 shared helpers match HEAD after ignoring the `export` keywords needed for cross-file reuse; the procedures are regrouped into sibling record modules (`salesQueryProcedures` / `salesLifecycleProcedures` / `salesDraftProcedures` / `salesSplitDraftProcedures`) and re-assembled with `router({ ...records })` — preserving every flat path (`sales.create`, etc.), so `AppRouter`'s inferred shape, the web client, and the caller-based tests are unchanged. Only `trpc/router.ts` updated its import path. The repo has no `mergeRouters`, so the spread-record pattern avoids new infra. Fat procedures (suspend/resume/changeTable/splitDraft) keep inline logic — application-layer extraction deferred per plan Q2. `ci:server` + `ci:web` green; server-only, no UI smoke. `ENG-178` stays Partial: schema, desktop main, purchases router, completeSale, SalesPage, ReceiptTemplateEditor, ProductFormModal remain (7 files). |
| 30    | `ENG-178` | Partial | Megafile decomposition wave 2 — DB schema split (slice 4, the big one). | Structural split of `db/schema.ts` (5 430 LOC, 83 tables / 73 relations / 389 declarations) into 17 domain modules under `db/schema/` (base, auth, catalogs, products, customers, purchasing, sales, salesAux, inventory, quotationsAudit, devices, config, fiscal, hardware, syncAi, types, realtime). `db/schema.ts` kept as a thin `export *` barrel → all 263 importers + the drizzle-kit `schema:` path resolve unchanged (zero importer churn; strict improvement over the ticket's literal `schema/index.ts`). All 389 declarations are present: 385 byte-identical vs HEAD, and only the 4 base check/now helpers (`moneyPositiveChecks`, `moneyTwoDecimalCheck`, `nowIso`, `sqliteNow`) gained `export`; FK refs are thunks + relations are callbacks, so the real cross-module cycles (sales↔salesAux, etc.) are load-safe. Verified by per-symbol byte-equivalence + `drizzle-kit generate` emitting NO migration (schema shape identical) + `ci:server` green (2183 tests, coverage 84.96/72.35/80.98/86.24; 263 importers typecheck + in-memory migrate-and-boot). Every new module < 500 LOC (largest `types.ts` 480). Server-only, no UI smoke. `ENG-178` stays Partial: desktop main, purchases router, completeSale, SalesPage, ReceiptTemplateEditor, ProductFormModal remain (6 files). |
| 31    | `ENG-178` | Partial | Megafile decomposition wave 2 — purchases application-layer extraction (slice 5, first behavior-change slice). | `trpc/routers/purchases.ts` (1 700 LOC) extracted into `application/purchases/` (types, helpers, resolveItems, purchase-read, createPurchase, receiveFromOrder, returnPurchase, voidPurchase, createOcrDraftPurchase, index) mirroring `application/sales/`; the router thins to 196 LOC — `list` inline, the 4 mutations adapt the tRPC ctx to a standalone `PurchaseContext` and delegate to the use-cases. Unlike slices 1-4 (pure relocations) the procedure bodies become application functions, but all 16 moved bodies (the 4 mutation transactions + OCR draft + 11 helpers/resolvers/read) are byte-identical vs HEAD; the new `PurchaseContext` is a minimal structural subset assignable to `enqueueSync`'s context (no `application → trpc/context` dependency, mirroring `CompleteSaleContext`). The sole external importer (`ai.ts` OCR flow) repointed. Verified by per-body byte-equivalence + the UNCHANGED `purchases.test.ts` caller suite + `ci:server` green (2183 tests, coverage 84.97/72.35/81.02/86.25). Server-only, no UI smoke. `ENG-178` stays Partial: desktop main, completeSale, SalesPage, ReceiptTemplateEditor, ProductFormModal remain (5 files). |
| 32    | `ENG-178` | Partial | Megafile decomposition wave 2 — completeSale application split (slice 6, second behavior-change slice). | `application/sales/completeSale.ts` (1 685 LOC) decomposed into a thin dispatcher (`completeSale.ts` 80 LOC) + the two path bodies `runFreshSale.ts` (493) / `runCompleteDraft.ts` (450) + shared leaves `pricing.ts` (`resolveFreshSaleTotals` + `resolveSalePaymentPlan`), `item-resolution.ts` (`validateCustomer` / `getSaleSequentialContext` / `resolveSaleItems` / `detectPriceOverrides`), `creditPolicy.ts` (`runCreditPreflight` + `safelyRecordCreditSaleLedger`), `fiscalPostHook.ts` (`emitSaleFiscalDocument` + `enqueueSaleKdsOrder`); `journal-effects.ts` gained `lookupJournalEventId` / `safeUpdateSaleCompletedSummary` / `buildFreshSaleEffects` / `buildDraftSaleEffects`, and `sale-read.ts` the `CompleteSaleSaleRecord` type (acyclic: orchestrator imports leaves, leaves never import back). The two `db.transaction` bodies + every `roundMoney` site move verbatim; the blocks inlined identically in both paths (payment plan, credit, fiscal, KDS, journal) collapse to single helpers parametrized only on the genuine fresh-vs-draft deltas (cash-collected / credit / fiscal `status==='completed'` gate, customer + saleId source, the two effect sets). Verified by a call-conservation diff vs HEAD (every side-effect call count preserved; reductions are exact fresh+draft dedups) + the UNCHANGED `application-sales-completeSale.test.ts` (19) and full caller suite + full `ci:server` green (`ci:audit`, `typecheck`, `lint`, `test:coverage`: 188 files / 2183 tests, coverage 84.99/72.34/81.05/86.28); `node` review clean. Server-only, no UI smoke. `ENG-178` stays Partial: desktop main, SalesPage, ReceiptTemplateEditor, ProductFormModal remain (4 files). |
| 33    | `ENG-178` | Partial | Megafile decomposition wave 2 — desktop main split (slice 7, PARTIAL — first half). | `apps/desktop/src/main/index.ts` (2 822 LOC) first half extracted VERBATIM: a new Electron-free `runtime.ts` server hub (embedded-server handle + `getServerDatabase`/`getSqliteClient`/`getServerUrl`/`getServer`/`setServer`; index.ts repointed its server lifecycle to it) + `ipc/db.ts` (the `db:*` bridge handlers + table helpers + ENG-025 cross-tenant guards + `ALLOWED_DESKTOP_TABLES`) + `ipc/sync.ts` (the `sync:*` handlers + helpers + `SYNC_ENTITY_CONFIG`). Bodies byte-identical (conservation diff: all 35 `ipcMain` channels + every side-effect call preserved); `db.ts`/`sync.ts` Electron-free (node --test-able); inline `ipcMain.handle` registrations stay in index.ts delegating to the imported handlers (move to `ipc/register.ts` in the continuation). index.ts 2 822 → 1 769; full `ci:desktop` green; `node` review clean. NOT YET < 500 — the bootstrap-entangled half (backup crash-window restore, `createWindow`/tray, print, preferences) + `ipc/sync.ts` split + `ipc/register.ts` + final thinning are DEFERRED to a focused continuation: that surface has no behavioral test net + no in-session Electron smoke, so forcing the verbatim extraction of the entangled half one-shot was judged too risky. `ENG-178` stays Partial: desktop main (remaining half + `ipc/sync.ts` split), SalesPage, ReceiptTemplateEditor, ProductFormModal. |
| 34    | `ENG-178` | Partial | Megafile decomposition wave 2 — ProductFormModal split (slice 8, first web slice). | `apps/web/src/features/products/ProductFormModal.tsx` (1 329 LOC) decomposed into a 139-LOC thin shell + `useProductForm.ts` (RHF bundle: useForm + handleSubmit wrapper + 2 useFieldArray + register configs + syncTier/syncAllTiersFromCost/handleBaseUnitChange/validateProviderAssignment + watches) + `productForm.types.ts` + `productForm.helpers.tsx` (createDefaultValues/mapProductToForm/FieldGroup/errorProp/REQUIRED_LABEL) + `AISuggestionsPanel.tsx` (ENG-078: state + suggestCategoryMutation + the 2 React-Compiler-safe setTimeout-wrapped useEffects + derived flags + the Category-field UI, verbatim) + 4 per-tab components. Behavior-preserving React split: RHF threaded by props (not FormProvider); JSX/logic moved intact; i18n keys conserved (76=76); a11y roles preserved. Verified by the UNCHANGED `ProductFormModal.test.tsx` (15) + full `ci:web` green (188 files / 1 641 tests + build + bundle + contrast + locale-parity) + `typescript-react-reviewer` clean (key={field.id} not index; derived flags in render; effects preserved; no barrel/any). Every new module < 500 (largest `ProductGeneralTab.tsx` 302 before reviewer split-out). Reviewer live smoke (Playwright MCP) passed product create across General/Precios/Unidades/Proveedores and confirmed the row in `/products`; the follow-up 403 was fixed inline by holding semantic module-gated queries until `modules.getEffective` resolves, and repeat `/products` smoke showed no `products.embeddingHealth` / `products.semanticSearch` request plus zero console errors. `ENG-178` stays Partial: desktop main (remaining half + `ipc/sync.ts` split), SalesPage, ReceiptTemplateEditor. |
| 35    | `ENG-178` | Partial | Megafile decomposition wave 2 — ipc/sync.ts split (slice 9, Slice-7 debt paydown). | `apps/desktop/src/main/ipc/sync.ts` (668 LOC, over the 500 ceiling from slice 7) split into `sync.ts` (317: the `sync:*` + `db:addToSyncQueue`/`db:getPendingSyncItems` handler surface — `assertDesktopSyncOperation` + the 3 interfaces + `getDesktopSyncStatus` + the 4 `handleDesktop*` handlers, all still exported) + new `sync-helpers.ts` (386: `getLastSyncKey`/`desktopPendingSyncWhere`/`desktopProcessableSyncWhere`/`resolveDesktopConflictPolicy`/`getLastSyncAt`/`saveLastSyncAt`/`findLocalSyncEntity`/`markSyncEntityAsSynced`/`incrementQueueFailure`/`ensurePendingConflict`/`normalizeSyncEntityType` + `SYNC_ENTITY_TYPE_MAP`/`SYNC_ENTITY_CONFIG`/`DESKTOP_MANUAL_SYNC_ENTITIES` + status/config constants). Pure verbatim relocation: every body byte-identical vs HEAD (conservation: sync-helpers.ts 0 lines not in HEAD; sync.ts's 14 deltas are the new import-list members). Both Electron-free + acyclic; `index.ts` untouched (same 8-symbol surface); `SyncConflictPolicy`/`getSqliteClient` pruned from sync.ts. Verified: full `ci:desktop` green; `node` review clean; SEC-003/ADR-0004/ENG-064b markers preserved; both < 500. Server/main-process, no UI smoke. `ENG-178` stays Partial: desktop main (remaining entangled half), SalesPage, ReceiptTemplateEditor (3 files). |
| 36    | `ENG-178` | Partial | Megafile decomposition wave 2 — SalesPage.tsx split (slice 10, first web hot-path slice, PARTIAL). | `apps/web/src/features/sales/SalesPage.tsx` (1 745) → 1 156 (~34%) via 4 hooks (`useReceiptAutoPrint`/`useCashDrawerController`/`useBarcodeProductScanner` acyclic leaves + `useSalesMutations`: the 8 `useCriticalMutation` handles + `finishSaleEpilogue`) + 4 presentational sections (`SalesHeaderSection`/`WorkspaceTabsSection`/`CashSessionModals`/`SalesModals`). Shell keeps ALL shared state; hooks receive state + setters as params → acyclic DAG (only hook→hook edge: useReceiptAutoPrint → useSalesMutations via `maybeAutoPrint`). React Compiler OFF → every `useCallback` + dep array moved verbatim (the 2 stable-setter dep additions `setProductSearchQuery`/`setSaleError` are identity-stable). Conservation diff: every moved handler/mutation/JSX body byte-identical vs HEAD (only signatures/params/returns differ). Verified: full `ci:web` green (1 642 tests, coverage 81.13/72.15/75.92/82.58, build/bundle/contrast); `typescript-react-reviewer` clean (DAG, verbatim memoization, remount keys on inner modal elements, single peripherals subscription, onErrorToast + status!=='completed' short-circuit intact); live Playwright smoke proved render + the cash-session-open coupling (useSalesMutations + setter injection closed the modal + flipped the panel) + SalesModals/CashSessionModals/SalesHeaderSection wiring at 0 console errors (product→charge→suspend not driven — smoke tenant lacked seeded products; those flow handlers stay shell-inline). Coupled flow core (handleCheckout/handleSuspendConfirm/handleResumeFromPanel + cash/cart handlers, ~340 LOC) deferred to **slice 10b** as one `useSalesFlows` module. AC discrepancy (non-enumerated >500 source files: seed-dev 2 134, products router 1 280, receipt-renderer 1 204, fiscal/orchestrator 1 190, SalePaymentModal 1 048, +5) captured in BACKLOG. `ENG-178` stays Partial: main/index.ts, SalesPage 10b coupled core, ReceiptTemplateEditor. |
| 37    | `ENG-178` | Partial | Megafile decomposition wave 2 — ReceiptTemplateEditor.tsx split (slice 11, safest web slice). | `apps/web/src/features/receipt-templates/ReceiptTemplateEditor.tsx` (1 296) → 248 via a `useReceiptLayoutEditor` hook (387: state + add/remove/move/patch handlers + FLIP useLayoutEffect + dnd-kit sensors/accessibility/handlers + create/update mutations + handleSave) + 4 leaf components (`BlockForm` 455, `SortableBlockRow` 143, `TotalsBlockCaption` 46, `TemplateFunctionsCheatSheet` 77) + `receiptEditor.constants.ts` (52). Behavior-preserving: kept useState (useReducer adoption deferred to BACKLOG); conservation — the 4 leaf components + constants are 0 lines not in HEAD; shell + hook differ only by destructure/return member names + the 2 new interfaces. FLIP blockListRef + the stable-key (init-N/b-N) scheme + the dnd-kit wiring + every useMemo/useLayoutEffect dep array moved intact (React Compiler OFF). Public API unchanged (ReceiptTemplateEditorProps['initial'] now via the hook-owned ReceiptTemplateInitial); sole importer ReceiptTemplatesPage untouched; no import cycle. Verified: UNCHANGED ReceiptTemplateEditor.test.tsx (~14 cases) + full ci:web green (1 642 tests, coverage 81.14/72.15/75.93/82.59) + typescript-react-reviewer clean + live Playwright smoke (editor renders, select-block → BlockForm, patchBlock → preview round-trip, addBlock → metaTable form, 0 console errors). Every file < 500. `ENG-178` stays Partial: 2 tracked files left — main/index.ts (Slice 7), SalesPage 10b coupled core — plus the non-enumerated >500 set in BACKLOG. |
| 38    | `ENG-178` | Partial | Megafile decomposition wave 2 — products router split (slice 12, third router, operator-folded). | `trpc/routers/products.ts` (1 280) decomposed into `routers/products/{index,product-read,helpers,queries,mutations,semantic}.ts` mirroring the slice-3 `sales/` sibling-record pattern (NOT slice-5 application extraction — products is CRUD + shared resolvers, not fat orchestration). `index.ts` re-assembles `router({ ...productQueryProcedures, ...productMutationProcedures, ...productSemanticProcedures })` so all 11 flat paths (`products.list` … `products.suggestCategory`) + `AppRouter`'s inferred shape stay unchanged; the ~488 LOC helper block split into two import-leaves — `helpers.ts` 369 (mutation-side unit/provider/tax/location validate+resolve+replace + types) and `product-read.ts` 177 (`productSelection` projection + `getProductWithRelations` + the batch unit loader). Pure structural relocation: a conservation diff vs HEAD shows every procedure + helper body byte-identical — the only deltas are the `export` keyword on now-cross-file declarations, the 3 `product*Procedures` record wrappers + barrel spreads, and the redistributed imports (zero body/logic line in either delta). Module gates (ENG-068), role guards, tenant scopes, the ENG-177a optimistic-lock pairing on `update`, and every marker (ENG-061/033/068/040/176b/177a) preserved; only `trpc/router.ts` repointed (`products.js` → `products/index.js`). The 3 caller-suite test files (28 cases) unchanged. Verified: full `ci:server` green (2183 tests, coverage 85/72.35/81.05/86.28) + `ci:web` green + `node` review clean. Server-only, no UI smoke. Every new module < 500 (largest `helpers.ts` 369). Folds `products.ts` into ENG-178 per operator pick. `ENG-178` stays Partial: main/index.ts (Slice 7), SalesPage 10b coupled core — plus the remaining non-enumerated >500 set in BACKLOG. |
| 39    | `ENG-178` | Partial | Megafile decomposition wave 2 — SalePaymentModal split (slice 13, web modal, operator-folded). | `apps/web/src/features/sales/SalePaymentModal.tsx` (1 048) → 267-LOC thin shell + `useSalePaymentModal.ts` (377 — all useState/useMemo/useWatch/useEffect + handlers + the creditBalance tRPC query, returns the bundle) + `salePaymentModal.types.ts` (106) + `salePaymentModal.constants.ts` (69) + 4 presentational sub-components (`SalePaymentTipSection` 94, `SalePaymentSingleTenderSection` 194, `SalePaymentSplitTenderSection` 180, `SaleCreditCustomerCard` 151), mirroring the slice-8 ProductFormModal pattern (RHF threaded by props, NOT FormProvider; one shared useForm in the hook). Behavior-preserving: a conservation diff vs HEAD shows every handler/effect/JSX body byte-identical — only the `export` keyword on the now-cross-file types/constants, the lucide import reformatting, the 8 props interfaces + the hook params + the return bundle, the shell's prop-threading attributes, the per-child useTranslation('sales'), and one rename (`creditBalanceQuery.isLoading` → `balanceLoading` prop). React Compiler OFF → every dep array verbatim (fastCashTrigger `eslint-disable`, auto-attach `shouldDirty:false`, `setValueAs`). Stable keys kept (split rows `key={field.id}`); markers preserved (ENG-014/039d/039d3/090/105e/105c2). Public API unchanged (`SalePaymentModal` + the 3 value types re-exported from the shell), so the 4 importers untouched; no import cycle. Verified by the UNCHANGED 34-case dual suite (SalePaymentModal.test.tsx 20 + .credit.test.tsx 14) + ci:web code gates green (typecheck/lint 0, 1 642 tests, coverage 81.16/72.15/75.98/82.61, build/bundle/contrast — sales chunks shrank) + `typescript-react-reviewer` clean + a live Playwright smoke (modal opened in-app; tip preset → grand-total/amount-sync round-trip; split toggle → tender validation gating; 0 console errors during modal use). ci:audit pre-existing undici advisory (electron toolchain) is dep-independent — reported, not fixed. ES live-flip not driven (no in-session locale switcher reachable) — covered by locale-parity + verbatim keys. Every file < 500 (largest the hook 377). Folds `SalePaymentModal.tsx` into ENG-178. `ENG-178` stays Partial: main/index.ts (Slice 7), SalesPage 10b coupled core — plus the remaining non-enumerated >500 set (geography/receipt-renderer next per the operator batch). |
| 40    | `ENG-178` | Partial | Megafile decomposition wave 2 — geography router split (slice 14, three-router file, operator batch). | `trpc/routers/geography.ts` (878) decomposed into `routers/geography/{index,helpers,countries,departments,cities}.ts`. Non-obvious vs products: the file already exported THREE independent routers (`countriesRouter`/`departmentsRouter`/`citiesRouter`) + the shared helper `ensureCityExists`, so the barrel RE-EXPORTS (not re-assembles) — one file per entity router + a shared `helpers.ts` leaf (the 8 ensure*/buildXSelection helpers, the 7 previously-private ones gaining `export`). Pure structural relocation: a conservation diff vs HEAD shows every router-procedure + helper body byte-identical (only deltas: the `export` keywords, the import redistribution `../`→`../../`/`../../../`, and the barrel replacing the old trailing `export { ensureCityExists }`; zero logic line moved). Role guards (tenantProcedure/adminProcedure), tenant scopes, the cross-entity FK-guard deletes (country→departments, department→cities, city→providers), and the ENG-179b markers preserved. Two importers repointed to `./geography/index.js`: `trpc/router.ts` (3 routers) + `trpc/routers/providers.ts` (`ensureCityExists`). The UNCHANGED `geography.test.ts` caller suite is the net. Verified: full `ci:server` green (`ci:audit`, typecheck, lint, test:coverage: 188 files / 2183 tests, coverage 85/72.34/81.05/86.28) + web typecheck green (AppRouter stable) + `node` review clean + Playwright MCP smoke on `/providers` and `/geography` Países/Departamentos/Ciudades. `ci:audit` is green after the scoped pnpm override `@electron/get@5.0.0>undici=7.28.0` (Electron toolchain transitive, no app code change). Every module < 500 (largest `cities.ts` 261). Folds `geography.ts` into ENG-178. `ENG-178` stays Partial: main/index.ts (Slice 7), SalesPage 10b coupled core — plus the remaining non-enumerated >500 set (receipt-renderer next, then SalesPage 10b per the operator batch). |
| 41    | `ENG-178` | Partial | Megafile decomposition wave 2 — receipt-renderer split (slice 15, server service, operator batch). | `services/receipt-renderer.ts` (1 204, the pure HTML + ESC/POS receipt renderer) decomposed by render concern into `services/receipt-renderer/{index,types,labels,escape-resolve,format-helpers,scanner-urls,html-blocks,escpos,render}.ts`. Pure structural relocation: a conservation diff vs HEAD shows every function/const/type body byte-identical (only deltas: the `export` keyword on previously-private internals, import redistribution one level deeper `./`→`../` and `../`→`../../`, and the barrel; zero logic line moved). The two security guards moved verbatim — `RESOLVED_URL_SCHEME_BLOCKLIST`/`safeResolvedScannerSource` (scanner-urls) + the `hasOwnProperty` proto-pollution guard in `lookupPath` (escape-resolve) + the `escapeHtml`/`resolveAndEscape` escape-at-exit; fiscal contract stays pure (ENG-058). Markers ENG-016/017/025/039d/039d3/058/080/086/097 preserved. Acyclic (format-helpers does NOT import escape-resolve back). Public API re-exported from `receipt-renderer/index.js`; 2 importers (receiptTemplates router + the receipt-templates test) repointed. The UNCHANGED 63-case `receipt-templates.test.ts` (direct + tRPC) is the net. Verified: full `ci:server` green end-to-end (ci:audit now passing post-slice-14, typecheck/lint 0, 2183 tests, 85/72.35/81.05/86.28) + `node` review clean. Server-only, no UI smoke. Every module < 500 (largest `html-blocks.ts` 338). Folds `receipt-renderer.ts` into ENG-178. `ENG-178` stays Partial: main/index.ts (Slice 7), SalesPage 10b coupled core — plus the remaining non-enumerated >500 set (SalesPage 10b is the last of the operator batch). |

## Recommended Sequence

1. The retail screen-focus wave (`ENG-182..ENG-188`) and the **`ENG-132` list-screen simplification
   are both fully closed**. ENG-132 trimmed every list screen to the smallest useful column set with
   secondary detail behind a row drawer/modal: Products (`ENG-132a`), Customers (`ENG-132b`),
   Inventory Stock (`ENG-132c`), Quotations (`ENG-132d`), Orders (`ENG-132e`), Purchases (`ENG-132f`),
   Inventory Movements/Entries (`ENG-132g`), Fiscal Documents (`ENG-132h`). Finance's other two routes
   needed no trim (`/fiscal-reports` shell; `/audit-logs` already minimal). Pick the next track from
   [PLAN-V3.md](./PLAN-V3.md).
2. Resume the other pending Plan v3 tickets from [PLAN-V3.md](./PLAN-V3.md) (non-UI rails like
   `ENG-133` / `ENG-135` can go in parallel).
3. Keep gated tickets parked until their gate clears:
   `ENG-021`, `ENG-022`, `ENG-023`, `ENG-059`, `ENG-063`, `ENG-160`, and the
   Brazil NFe slice of `ENG-161`.
4. Run `ENG-164` before hosted-only work (`ENG-157`, `ENG-158`, `ENG-162`) or
   the cross-tenant aggregate slice of `ENG-138`.
5. Run `ENG-165` before `ENG-118` public API exposure.

## Ticket Execution Shape

For each ticket:

1. Read `ROADMAP.md §3b` for the ticket row and acceptance criteria.
2. Read the specialty docs named by the row.
3. Keep edits scoped to the ticket plus collateral fixes needed for truth.
4. Update docs in the same commit when behavior, commands, gates, or product
   claims change.
5. Move long shipped-history detail to [ARCHIVED.md](./ARCHIVED.md) instead of
   extending active planning files.

## Verification Matrix

| Touched area                           | Required command                              |
| -------------------------------------- | --------------------------------------------- |
| Web React/TypeScript                   | `pnpm run ci:web`                             |
| Server/Node/tRPC/DB                    | `pnpm run ci:server`                          |
| Electron main process                  | `pnpm run ci:desktop`                         |
| Web E2E, login, sales, inventory flows | `pnpm run test:e2e:web`                       |
| Electron bootstrap or E2E              | `pnpm run test:e2e:electron`                  |
| Docs-only cleanup                      | `git diff --check` plus link/claim inspection |

Any user-facing UI change also needs live browser or Electron smoke. Tests do
not replace the smoke.

## Closing A Ticket

When a ticket closes:

1. Change its `ROADMAP.md §3b` status to `Shipped`.
2. Append a concise `Shipped:` summary to the scope cell.
3. Capture follow-ups in `BACKLOG.md` or ask the operator before stopping.
4. If the closeout gets long, move detail to `ARCHIVED.md` and leave a link.
5. Keep the commit message conventional and scoped, with no AI co-author
   trailer.
