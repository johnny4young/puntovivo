# Puntovivo — Sprint Plan

> Tactical execution plan for the next implementation slices.
> `ROADMAP.md` remains the canonical ENG ticket list; this file only keeps
> the active sequence, commit shape, and verification checklist. Historical
> shipped detail lives in [ARCHIVED.md](./ARCHIVED.md).

Updated: 2026-06-01.

## Current Focus

Run one ticket at a time. The first line of a shipping turn is:

```text
Executing <ENG-NNN> — <one-liner>
```

The current focus wave is the product-truth and retail-scope reset, now continuing into the
`ENG-132` list-screen simplification track (smallest useful column set + secondary-into-drawer,
one screen per commit):

| Order | Ticket    | Status  | Intent                                                    | Required proof                                                                                                                                       |
| ----- | --------- | ------- | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | `ENG-182` | Shipped | Product doctrine and README truth reset.                  | README, SELLABILITY, ROADMAP §0, docs index, and command docs agree on pnpm 11, Node 24, local-first retail scope, and demo/pilot/production status. |
| 1a    | `ENG-182a` | Shipped | Token-budget cleanup + dead-code hygiene.                 | ROADMAP / BACKLOG / ARCHIVED are compact without losing non-shipped tickets; confirmed-zero-import code/deps are removed; ci + live smoke are green. |
| 2     | `ENG-183` | Shipped | Retail Ring-1 scope gate and module exposure cleanup.     | Fresh retail tenant hides non-core surfaces unless profile/module enables them; tests prove no route/sidebar/palette leaks.                          |
| 3     | `ENG-184` | Shipped | Colombia retail readiness profile and checkout preflight. | `/company` + `/sales` surface fiscal/hardware/sync/payment as optional reminders (never blockers); basic CO DIAN config card ships; en/es + recovery CTAs proven.          |
| 4     | `ENG-185` | Shipped | Fiscal adapter truth guard.                               | Unsupported countries fail with a typed error; mock/draft packs are labelled Demo/Draft across cards, document views, and diagnostics; e-invoicing stays optional.   |
| 5     | `ENG-186` | Shipped | Ring-1 screen focus pass (re-scoped: `/sales` sellability slice). | `/sales` completes the common sale without desktop scroll at 1440x900 (cart + checkout scroll internally); History + Suspended behind a reusable Drawer; live smoke desktop + mobile, en/es. Operations + Setup slices split to `ENG-187`. |
| 6     | `ENG-187` | Shipped | Ring-1 screen focus pass — Operations needs-attention (re-scoped: `/operations` slice). | `/operations` defaults to a Needs-attention queue (server aggregation of retryable sync/fiscal/hardware/payment failures, each row deep-links to its panel) + tested all-clear state; live smoke en/es covered the failure row + CTA. Setup/`/company` restructure split to `ENG-188`. |
| 7     | `ENG-188` | Shipped | Ring-1 screen focus pass remainder (Setup/`/company` readiness-hub restructure). | **Shipped** (web-only): the flat 10-pill `segmented-control` in `CompanyPage` became a grouped Setup nav — readiness pinned as the admin landing, the other 9 tabs demoted into three labeled groups (Negocio / Facturación y pagos / Sistema), modeled as a navigation (`role="group"` + `aria-current`, panel `role="region"`). The `?tab=` deep-link contract is preserved, so the readiness CTAs / GlobalStatusStrip / AuthProvider keep working. `ci:web` green; live `/company` smoke en/es. Closes the screen-focus wave. |
| 8     | `ENG-132a` | Shipped | Screen simplification — Products list (first `ENG-132` list-screen slice). | `/products` table trimmed to the smallest useful column set (name+SKU / category / tier-1 / stock / status); provider, location and tier-2/3 prices moved into a row-detail `ProductDetailsDrawer` (additive Details eye action + edit hand-off). ENG-134f keyboard edit + CSV/XLSX export parity preserved. `ci:web` green; live `/products` smoke en/es. Remaining `ENG-132` screens (one per commit): Inventory, Orders, Purchases, Quotations, Customers, Finance. |
| 9     | `ENG-132b` | Shipped | Screen simplification — Customers list (`ENG-132` list-screen slice). | `/customers` table trimmed to name(+identification) / status / actions; email, phone, type and location moved into a row-detail `CustomerDetailsDrawer` (additive Details eye action + ungated edit hand-off mirroring the row). ENG-134f keyboard edit + ledger/delete gating preserved. Added the FIRST tests for the customers folder (drawer + page). `ci:web` green; live `/customers` smoke en/es. Remaining `ENG-132` screens: Inventory, Orders, Purchases, Quotations, Finance. |
| 10    | `ENG-132c` | Shipped | Screen simplification — Inventory Stock tab (`ENG-132` list-screen slice). | Stock table trimmed to name(+sku/category) / stock / status / actions; min stock, sell price, valuation and updated date moved into a new `InventoryStockDetailsDrawer` (additive Details eye action + canManage-gated Adjust hand-off to the existing adjustment modal). Scope = Stock tab only (Movements/Entries/Balances untouched). Added drawer test + a pure-function column-set test (exported `getStockColumns`). `ci:web` green; live `/inventory` Stock smoke en/es. Remaining `ENG-132` screens: Inventory Movements/Entries, Orders, Purchases, Quotations, Finance. |
| 11    | `ENG-132d` | Shipped | Screen simplification — Quotations list (`ENG-132` list-screen slice). | Quotations history table trimmed to number / customer / total / status / actions; site, items, valid-until and created-at lean on the EXISTING View → `QuotationDetailsModal` (no new drawer — it already has one). The modal already showed valid-until / created-at / line items; Site added to it (web-only, payload already carries `siteName`). Added `quotations:details.site` + a column-set test assertion. `ci:web` green; live `/quotations` smoke en/es. Remaining `ENG-132` screens: Orders, Purchases, Inventory Movements/Entries, Finance. |
| 12    | `ENG-132e` | Shipped | Screen simplification — Orders list (`ENG-132` list-screen slice). | Orders history table trimmed to order # / provider / status / total / actions; date, site and receipts lean on the EXISTING View → `OrderDetailsModal` (no new drawer — it already shows created / site / staged-delivery + receipts list). No new i18n key and no server change (cleanest 132 slice); Status badge keeps receiving progress legible on the list. Dropped the unused `formatDateTime` import; updated `OrdersHistoryTable.test.tsx` (receipt-progress moved to the modal) + added a column-set assertion. `ci:web` green; live `/orders` smoke en/es. Remaining `ENG-132` screens: Purchases, Inventory Movements/Entries, Finance. |
| 13    | `ENG-132f` | Shipped | Screen simplification — Purchases list (`ENG-132` list-screen slice). | Purchases history table trimmed to purchase # / provider / status / total / actions; date, site and returns lean on the EXISTING View → `PurchaseDetailsModal` (no new drawer — it already shows created / site / supplier-returns summary + return history). No new i18n key and no server change; the CSV/XLSX export is separate so it keeps every field (export test stays green); Status badge keeps return state legible on the list. Dropped the unused `formatDateTime` import; updated `PurchasesHistoryTable.test.tsx` (returns-cell text moved to the modal) + added a column-set assertion. `ci:web` green; live `/purchases` smoke en/es. Remaining `ENG-132` screens: Inventory Movements/Entries, Finance. |
| 14    | `ENG-132g` | Shipped | Screen simplification — Inventory Movements + Entries tabs (`ENG-132` list-screen slice; closes the Inventory track). | Both Inventory log tabs trimmed to the smallest useful set (Movements: date / product / movement / type; Entries: date / mode / product / counted qty), each + a Details (eye) action. These tabs had no detail view, so — per operator decision — added NEW `InventoryMovementDetailsDrawer` / `InventoryEntryDetailsDrawer` (mirror the 132c stock drawer) holding the trimmed fields. Extracted the inline columns to pure `getMovementColumns` / `getEntryColumns` modules (testable); exports unchanged (keep every field). Added `inventory:movements.details.*` + `inventory:entries.details.*` (en/es). 4 new tests (2 column-set, 2 drawer). `ci:web` green; live `/inventory` Movements + Entries smoke en/es. Remaining `ENG-132` screen: Finance/Compliance (undefined scope — needs operator definition). |
| 15    | `ENG-132h` | Shipped | Screen simplification — Fiscal Documents list (`ENG-132` list-screen slice; CLOSES the track). | `/fiscal-documents` compliance table trimmed 8→6 + actions (when / document number / kind / status / buyer / total); the raw provider id + truncated CUFE move into a new `FiscalDocumentDetailsDrawer` (shows full CUFE + a View XML hand-off to the existing modal), opened by a Details (eye) action. Custom HTML table kept; frozen-snapshot read invariant untouched. Of the 3 Finance routes only Fiscal Documents needed a trim (fiscal-reports = shell; audit-logs = already minimal). Added `FiscalDocumentListItem` to types/api.ts + `fiscal:list.details.*` (en/es). 2 new tests (drawer + first page test). `ci:web` green; live `/fiscal-documents` smoke en/es. **ENG-132 is now fully Shipped.** |
| 16    | `ENG-133b` | Shipped | Perf budgets — Electron main+renderer memory ceiling (slice of `ENG-133`). | Extended the perf-budget engine with `electronMemoryMb` (perProcessMb main/renderer + 25% threshold). Flag-gated measure mode in the desktop main (`PUNTOVIVO_MEASURE_MEMORY=1` → `app.getAppMetrics()` on did-finish-load, print + quit); `scripts/check-electron-memory.mjs` launches the built app, maps Browser→main/Tab→renderer, compares warn-first + self-skips when Electron can't launch (keeps ci:desktop green on ubuntu). 12-case `node --test`. Baseline captured locally (main ~250 / renderer ~160 MB, PASS). Wired into `ci:desktop`. `ENG-133` stays Partial: Lighthouse-CI + the hard-fail CI launch (xvfb) remain. |
| 17    | `ENG-133c` | Shipped | Perf budgets — Lighthouse web-vitals gate (slice of `ENG-133`). | Fourth perf metric: `lighthouse` section in `perf-budget.json` (perRoute LCP/TTI/CLS/score + 30% threshold). `scripts/check-lighthouse.mjs` launches Chromium via Playwright, logs in once (refresh-cookie re-auth survives `disableStorageReset` so authenticated routes measure), warms each route (Vite on-demand compile), runs Lighthouse, compares warn-first + self-skipping. 14-case `node --test` wired into `ci:web`; the browser gate stays LOCAL (`pnpm run perf:lighthouse`, not push-CI). Baseline captured locally (warm dev run, PASS). `ENG-133` stays Partial: the Electron memory hard-fail (xvfb) + promoting Lighthouse into push-CI remain. |
| 18    | `ENG-134g` | Shipped | Accessibility — module-gated surface a11y smoke (slice of `ENG-134`). | The 5 module-gated surfaces (`/touch`, `/kds`, `/customer-display`, `/m`, `/delivery`) join the Playwright axe catalogue: new `ensureModulesEnabled` in `e2e/shared/baseline.ts` force-enables their modules in the e2e seed (same `json_set` of `$.modules.<id>` as `modules.setActive`); 5 new `A11yRoute` entries (admin role, locale-agnostic `data-testid` settled locators). All 5 pass the WCAG 2 A+AA serious floor with ZERO inline fixes — 17/17 a11y entries green; no app/i18n change. Surfaced (not caused) a pre-existing ENG-186 `/sales` cash-panel layout regression (4 cash-session E2E tests fail at 1280x720, reproduces with modules off) → promoted and closed as `ENG-189`. `ENG-134` stays Partial: only the VoiceOver / NVDA screen-reader sweep remains. |
| 19    | `ENG-189` | Shipped | /sales cash-control reachability at constrained viewports (promoted from the ENG-134g BACKLOG find). | The ENG-186 no-page-scroll lockup collapsed the checkout scroll region to 0px at short viewports (1280x720 / 1366x768), burying the cash-session controls (Registrar movimiento / Cerrar caja) off-screen → 4 cash-session E2E tests failed. New Tailwind v4 `@custom-variant pos` (>= 1280px wide AND >= 900px tall) gates the height-lockup classes `xl:`→`pos:` across `SalesPage` + both column components + `theme.css`; below 900px tall the layout falls back to natural page scroll (everything reachable), at >= 900px the ENG-186 pinned-Cobrar internal-scroll layout is unchanged. Diagnosed live (`elementFromPoint`: 0px scroll region at 840px tall = lockup-on dead zone → threshold 900). Full `test:e2e:web` 55/55 green; `ci:web` green; live smoke EN+ES at 1280x720 + 1440x900, 0 console errors. |
| 20    | `ENG-135b` | Shipped | Observability — Sentry/GlitchTip adapter + Electron main crash path (slice of `ENG-135`; non-UI rail). | `@sentry/node` + `@sentry/browser` wired to the EXISTING sink interfaces, DSN-gated on both sides (server: `PUNTOVIVO_SENTRY_DSN` read inside `createServer`, dynamic SDK import, never-throw init, `defaultIntegrations: false` so the ENG-133 latency budgets stay untouched; web: lazy `lib/sentry.ts` chunk behind `VITE_PUNTOVIVO_SENTRY_DSN` — a DSN-less build DCEs it to 0 bytes, so no perf-budget entry). Electron main gains its first `uncaughtException`/`unhandledRejection` handlers (`crash-telemetry.ts`: log + `captureProcessCrash` + bounded flush + exit 1; rejections never exit). Two-layer consent model documented in OBSERVABILITY.md (per-tenant opt-in for tenant-attributed events; DSN = operator consent for tenant-less diagnostics; render events forwarded tenant-less). 15 server + 6 web + 8 desktop tests, SDK mocked. Review follow-ups closed in the same diff: standalone crash handlers forward to the sink, vitest setupFile strips ambient DSN, CSRF 403 became a tRPC-shaped envelope, and 4 drifted chunks got perf-budget entries (new-chunk warnings now zero). `ci:server` + `ci:web` + `ci:desktop` green; live smoke both DSN states vs a local envelope catcher. `ENG-135` stays Partial: dashboard + trace propagation + real-instance validation remain. |
| 21    | `ENG-135c` | Shipped | Observability — trace propagation renderer → server via x-correlation-id (slice of `ENG-135`; non-UI rail). | Web client mints a UUID per tRPC request in the existing headers() callback; new `observability/correlation.ts` sanitizes the attacker-controlled header (strict 8..64 URL-safe intake, correlation-only — Fastify requestId stays as the non-spoofable binding) and feeds the request-scoped logger bindings + tracing middleware (valid header wins, reqId fallback keeps SSE/curl identical); CORS allowlist + captureRenderError stamping (last-request id on client error events). 22 server + 6 web tests; `ci:server` + `ci:web` green; live smoke proved the same id on renderer header, server log line, and sink event. `ENG-135` stays Partial: dashboard + real-instance validation (both externally gated). |
| 22    | `ENG-105g` | Shipped | Sales Cockpit — palette most-used / recent ordering (slice of `ENG-105`). | Device-local usage tracking (new `lib/paletteUsage.ts`, tenant-scoped localStorage, never sent to the server) feeds a Recent section (top 5, count desc + recency tiebreak) above the stable catalogue when the palette opens with an empty query; active query keeps the plain filter. Ranking runs AFTER the role/module gate; section headers are aria-hidden so the listbox stays flat for AT; clean storage renders the exact pre-105g order (pinned — 17 pre-existing palette tests untouched). New `palette.groups.recent` en/es. 11 store + 6 component tests; `ci:web` green; live smoke EN+ES round-trip (use actions → reopen → ranked section). `ENG-105` stays Partial: payment drawer redesign + layout polish await operator visual direction. |
| 23    | `ENG-167b` | Shipped | SQLCipher — one-shot cleartext migration + cross-device restore key prompt (closes the ENG-167 production gate). | First boot of the upgraded build encrypts a pre-Step-1 cleartext local.db in place (header-detect → WAL checkpoint → temporary .bak → PRAGMA rekey → integrity verify → drop the .bak; failure restores the original and aborts the boot; dev-shared DATABASE_URL excluded). Cross-device restores prompt for the source device's key, rekey the staged DB to the local key, and swap; legacy cleartext bundles restore directly and encrypt on next boot. Admin-gated "View backup key" reveal makes the flow operable (trade-off in SECURITY.md). 7+3 node --test cases with real DB files + 4 renderer cases; `ci:desktop` + `ci:web` green; settings chunk budget 8→9 plus CompanyPage 30→33 (deliberate, $retune7/$retune8). `ENG-167` stays Partial: only the operator-run cross-OS `build-desktop.yml` matrix remains. |
| 24    | `ENG-177c` | Shipped | Optimistic versioning W4 part 4 — DB-level cash-session invariant on `sales`. | Added `chk_sales_cash_session_or_draft` (`CHECK (cash_session_id IS NOT NULL OR status = 'draft')`) via Drizzle table-rebuild migration `0001`, pinning the ENG-042/055 cash-session rule at the storage layer. Because drizzle wraps migrations in one transaction (where `PRAGMA foreign_keys` is a no-op), `db/index.ts` now brackets the migrate span with connection-level `foreign_keys = OFF` (empirically the only lever that stops `DROP TABLE sales` cascading into `sale_items`) and runs a post-migrate `foreign_key_check`. No data shim needed — both INSERT sites already bind a session. New `sales-cash-session-constraint.test.ts` (AC enforcement + FK-safe-rebuild positive/negative); `ci:server` green; server-only, no UI/smoke. `ENG-177` stays Partial: only `ENG-177b` (soft-delete policy) remains. |
| 25    | `ENG-169` | Shipped | Input boundary hygiene — closes the audit-closure wave (W5). | Four bounded hardenings: backup-restore ZIP allowlist (rejects `..`/absolute/non-allowlisted entries instead of silently ignoring them); `companies.logoUrl` + `products.imageUrl` route through `isUrlSchemeBlocked` in a schema `.refine`; `companies`/`customers`/`providers` emails normalise via the existing `emailField()` (trim+lowercase); `createServer` refuses to boot with `verbose && NODE_ENV=production` unless `PUNTOVIVO_ALLOW_VERBOSE_PROD=1`. New `eng169-input-hygiene.test.ts` + 3 backup-restore cases; `ci:server` + `ci:desktop` green; server + desktop-main only, no UI smoke. Manifest-signing deferred (no signing infra). |

## Recommended Sequence

1. The retail screen-focus wave (`ENG-182..ENG-188`) and the **`ENG-132` list-screen simplification
   are both fully closed**. ENG-132 trimmed every list screen to the smallest useful column set with
   secondary detail behind a row drawer/modal: Products (`ENG-132a`), Customers (`ENG-132b`),
   Inventory Stock (`ENG-132c`), Quotations (`ENG-132d`), Orders (`ENG-132e`), Purchases (`ENG-132f`),
   Inventory Movements/Entries (`ENG-132g`), Fiscal Documents (`ENG-132h`). Finance's other two routes
   needed no trim (`/fiscal-reports` shell; `/audit-logs` already minimal). Pick the next track from
   [PLAN-V3.md](./PLAN-V3.md).
2. Resume the other pending Plan v3 tickets from [PLAN-V3.md](./PLAN-V3.md) (non-UI rails like
   `ENG-133` / `ENG-135` can go in parallel).
3. Keep gated tickets parked until their gate clears:
   `ENG-021`, `ENG-022`, `ENG-023`, `ENG-059`, `ENG-063`, `ENG-160`, and the
   Brazil NFe slice of `ENG-161`.
4. Run `ENG-164` before hosted-only work (`ENG-157`, `ENG-158`, `ENG-162`) or
   the cross-tenant aggregate slice of `ENG-138`.
5. Run `ENG-165` before `ENG-118` public API exposure.

## Ticket Execution Shape

For each ticket:

1. Read `ROADMAP.md §3b` for the ticket row and acceptance criteria.
2. Read the specialty docs named by the row.
3. Keep edits scoped to the ticket plus collateral fixes needed for truth.
4. Update docs in the same commit when behavior, commands, gates, or product
   claims change.
5. Move long shipped-history detail to [ARCHIVED.md](./ARCHIVED.md) instead of
   extending active planning files.

## Verification Matrix

| Touched area                           | Required command                              |
| -------------------------------------- | --------------------------------------------- |
| Web React/TypeScript                   | `pnpm run ci:web`                             |
| Server/Node/tRPC/DB                    | `pnpm run ci:server`                          |
| Electron main process                  | `pnpm run ci:desktop`                         |
| Web E2E, login, sales, inventory flows | `pnpm run test:e2e:web`                       |
| Electron bootstrap or E2E              | `pnpm run test:e2e:electron`                  |
| Docs-only cleanup                      | `git diff --check` plus link/claim inspection |

Any user-facing UI change also needs live browser or Electron smoke. Tests do
not replace the smoke.

## Closing A Ticket

When a ticket closes:

1. Change its `ROADMAP.md §3b` status to `Shipped`.
2. Append a concise `Shipped:` summary to the scope cell.
3. Capture follow-ups in `BACKLOG.md` or ask the operator before stopping.
4. If the closeout gets long, move detail to `ARCHIVED.md` and leave a link.
5. Keep the commit message conventional and scoped, with no AI co-author
   trailer.
