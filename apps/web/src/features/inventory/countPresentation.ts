/** Shared lifecycle colors for the count list and its independently loaded editor. */
export const countStatusTone = {
  counting: 'info',
  submitted: 'warning',
  approved: 'success',
  rejected: 'danger',
} as const;
